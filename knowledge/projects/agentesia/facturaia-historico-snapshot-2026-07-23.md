---
title: facturaia — histórico snapshot 2026-07-23
date: 2026-07-23
tags: [facturaia, historico, snapshot]
---

## Movido desde el hub — 2026-07-23

_Bloque movido desde `00-home/facturaia.md` en la limpieza de cierre (regla `/fia-cierre` → histórico). Contenido original íntegro, condensado en el hub a 1-2 líneas._

- ✅ **Obras — "Toda la obra"/ficha/certificación: pulido de UX + bugs reales sobre feedback en vivo, push directo a main (9 commits, 2026-07-21 tarde)** — sesión rápida de fixes tras dogfooding de Manu, sin PR (confirmado explícitamente, gates locales verdes en cada push): (1) desplegable Responsable sin glass + tabla de certificación con huecos + franja vacía sobre la ficha (#1120); (2) ficha integrada en la fila de totales (ya no card suelta aparte) + barra de certificación a `position:fixed` con más glass (`glass-panel`+`glass-sheen`); (3) espacio bajo la barra fija + campo % gigante (heredaba `min-width:260px` de `.set-input`, pensado para líneas contables largas) + rail azul de selección quitado + partida en una sola línea; (4) árbol "Estructura" persistente en TODAS las pestañas de facturación (antes desaparecía al ver una factura/Total con salto de altura) + panel plegable con chevron animado + skeleton real de la tabla de partidas (antes `SkeletonList` genérico con avatar); (5) fila del listado de Obras clicable (#1128); (6) nombre de material en líneas de pedido resuelto server-side, mismo patrón que #1103 → [[resolver-label-nombre-en-cliente-contra-endpoint-paginado-cae-al-uuid]]; (7) click en un capítulo del árbol viendo una factura salta a "Pendiente de facturar" con el nodo ya seleccionado; (8) tooltips + cabecera de columnas en líneas de presupuesto (PP/CMO/reloj sin explicar) + fix de doble símbolo € en 4 plantillas PDF (`fmt2()` ya añade " €") + fix de la cabecera desalineada → [[css-grid-cabecera-y-filas-en-contenedores-distintos-auto-no-sincroniza]]. **Investigado y cerrado sin bug de código** (dato/estructura esperada de wapi, no tocar): `db_error` transitorio por DDL concurrente de otra sesión, "perepepe" (basura real tecleada en wapi 2014), mano de obra del material `0199000946` (coincide exacto con wapi id 568/1,4918h).

- ✅ **Obras — "Toda la obra": Fases 1-2 + BLOQUE P1 del follow-up COMPLETO, smoke verde (2026-07-21)** — árbol multi-presupuesto (obra = base + adicionales por `obra_id`, migs 533/534/535 en prod), certificar por selección + barra viva + candado solo-aprobados. **Follow-up P1 cerrado — 5 PRs #1112-1117 mergeados+desplegados** (cada uno con `/fia-precommit` + `/fia-cierre` + Slack): P1.1 líneas certificadas por factura (endpoint lazy leyendo `obras_facturacion.lineas_snapshot`, precio congelado); P1.2 "Añadir adicional" por UI (endpoint aceptar con `target_obra_id`, OB050/OB051→409, **guard same-family** que evitaba doblar el contratado); P1.3 regen `database.types.ts` (vía MCP; decisión razonada: NO retirar clientes tipados aislados, aportan precisión de RPC que el generado da como Json); P1.4 N+1 `getObraArbol`→`Promise.all` ([[obras-endpoints-n+1-paralelizar]]); P1.5 lógica de selección → `obra-arbol-seleccion.ts` puro + 17 tests. P1.6 **smoke prod verde** end-to-end (certificar→adicional→mezcla base+adicional→anular/revertir) + QA visual claro/oscuro (artifact https://claude.ai/code/artifact/dbcde8c6-3559-4a7a-8778-d27d7ee3e8b4).
  - **P1 retención de garantía (5%) CERRADA + EN PROD (PR #1121, mig 538, 2026-07-21)**: factura sigue emitiendo el 100% (base+IVA intactos, VeriFactu sin cambios), PDF con bloque "Retención por garantía" + "Importe total a pagar" (5 plantillas), ledger `obras_retenciones`, config en ficha de obra. De paso, fix de un bug real: `cobros_pending_for_org` reclamaba el % retenido antes de su vencimiento contractual. Smoke Sandbox verde (24,20€×5%=1,21€ exacto). **P2 (facturación multi-obra) CERRADO SIN CONSTRUIR**: hipótesis de una sesión anterior (capturas Salvador de Madariaga) resultó ser un falso positivo — Natalia confirmó que cada obra/planta se factura SIEMPRE por separado (el caso mezclado fue un apaño puntual por retraso del cliente, no una necesidad recurrente); Amadeus (multi-planta como obras separadas) es además un caso atípico de gran escala, lo normal es 2-3 plantas = 1 sola obra. Spec conservada en `issues/obras-facturacion-multi-obra.md` (marcada CERRADO, por si reaparece con evidencia real). **P3 (BC3/FIEBDC-3) backlog, sin empezar, solo si un cliente lo pide.**
  - **Smoke — 3 salvedades (ver §Smoke tests pendientes)**: (1) VeriFactu `no_aplica` en la sandbox → encadenamiento AEAT NO ejercitado; (2) posible glitch visual del panel de detalle (filas difuminadas en capturas, datos+a11y OK, prob. fade-in a mitad de captura, preexistente — verificar en localhost); (3) residual: adicional 5/2026 quedó colgando de la obra demo en Sandbox (contrato 143,48 en vez de 96,30).
  - Learnings: [[obras-endpoints-n+1-paralelizar]] · [[levantar-supabase-local-en-facturaia-config-puertos-analytics-dup519]] · [[db-push-remote-versions-not-found-es-checkout-stale-o-num-duplicado-no-repair]].

- ✅ **Obras — nombres de componente por UUID + modales adaptables EN PROD (2 PRs, 2026-07-20)** — dogfooding de Manu sobre el modal de composición de UO.
  - **#1103 (nombres → UUID)**: la composición de UO y el detalle de presupuesto pintaban el **UUID crudo** de cada material. Causa: el nombre se resolvía en cliente contra `/api/obras/materiales`, que desde obras-079 pagina (50/pág, sin descatalogados) → el id caía fuera del mapa → fallback `?? id`. Fix: resolver `denominacion` **server-side** en el detalle (por id, sin paginar, incl. descatalogados); desplegables de alta con `page_size=500`. El endpoint de presupuesto ya lo hacía bien y el front lo ignoraba. +2 tests. Regla nueva → [[resolver-label-nombre-en-cliente-contra-endpoint-paginado-cae-al-uuid]].
  - **#1106 (modales adaptables, desde el origen)**: los tamaños del componente compartido `Modal` eran anchos fijos px → a ancho **fluido acotado** `width: min(cap, calc(100vw - 2rem))` (patrón bounded-fluid web.dev/MDN + escala responsiva Carbon DS). Escala `sm 380 · md 560 · lg 800 · xl 960 · full 1200` (antes `full`=95vw sin tope). Modal de UO → `full` + filas de composición con más aire. Beneficia a TODOS los modales (admin/feedback incluido). Verificado local: 1200@≥1232 · 968@1000 · bottom-sheet@460 · dark ok · sin overflow. Artifact https://claude.ai/code/artifact/087b29af-858e-4238-874c-03fb6108e2d4.
  - **Datos mockup en Sandbox** (pendiente decidir borrar): UO `MOCK · Cuadro eléctrico básico` (ref `MOCK-CUADRO`) + material `MOCK-REGLETA-DESC` descatalogado, para ver ambos fixes en vivo.

- ✅ **Obras — Calendario (barras/festivos) + SISTEMA DE DISEÑO en 5 olas EN PROD (7 PRs #1042–#1048, 2026-07-19)** — dogfooding de Manu sobre el módulo Obras.
  - **Calendario (#1042)**: obras e incidencias de personal (vacaciones/baja/permiso/ausencia, ya en BD mig 516) pasan de chip-por-día a **barra continua de 1 línea** estilo Google Calendar (`_parts/calendario-view/week-layout.ts`: semanas + carriles globales + segmentos con contL/contR + overflow "+N"). Capa **Festivos** (tinte de celda + tag) e **import de festivos nacionales** de España calculados en código (`festivos-nacionales.ts`, fijos + Viernes Santo por computus, sin API) → endpoint idempotente `POST /api/obras/festivos/importar-nacionales` + botón en Ajustes. Sin migración (tablas existían).
  - **Dark + glass (#1043)**: celdas del calendario general menos negras (velo azul); calendario de equipo (columnas fluidas `minmax(1fr)`) y de partes con piel glass coherente.
  - **Sistema de diseño (auditoría multi-agente previa, 5 dimensiones)**: Ola 0 tokens semánticos POR ROL en `tokens.css` (`--text-page-title/-section/-subsection/-overline/-body/-label/-caption/-figure`) + `.set-label` (se usaba **sin definir** → labels sin estilo) + fix `PaginationBar` (padding lateral 0 + tamaños 12/13/14 mono/sans → unificados) (#1044); Ola 1 `title` nativo → `<Tooltip>` glass en todas las pestañas (#1045); Ola 2 calendario de partes **altura fija** + chip 2 líneas line-clamp + resto en tooltip (#1046); Ola 3 `PageHeader`/`SectionHeader` (26 cabeceras copiadas unificadas) + `FormGrid` (fin del hueco de Ajustes/Descuentos) + ancho capado `--content-max 1400` (#1047); Ola 4 barrido ~195 px→tokens en 29 módulos (#1048). **Fuente única**: cambiar un token/componente cambia toda la web.
  - **Cabos sueltos**: 11 px frontera sin token exacto (badges/pills 11px, cifras KPI/total 16-18px → faltan `--text-badge` / `--text-figure-lg`); 3 cabeceras con botón-back sin migrar a `PageHeader`; calendario general `/calendario` mantiene `title` nativo en barras (no es pestaña de Obras); delegaciones-view usa tokens t-shirt con fallback px (migrar a roles en pasada aparte).
  - **Smoke pendiente (Manu)**: QA visual en prod de las 5 pantallas clave (calendario obras/equipo/partes, Ajustes rejilla, Descuentos tarjetas) claro+oscuro. Ver [[worktree-qa-next-standalone-symlink-node-modules]].

- ✅ **Obras — IA + WhatsApp + MCP COMPLETO (7 PRs mergeados + desplegados, 2026-07-19)** — paridad de obras en el copiloto WhatsApp/voz y en el conector MCP. Aditivo, doble-gated por sector Obras + rol, **sin frontera fiscal** (facturar/certificar/aceptar siguen solo deep-link), reutilizando motores existentes.
  - **IA (#1018)**: `generarPresupuestoObra` (borrador NL, deep-link) + `explicarPrecioObra` (verbaliza el motor de precios) + insight `buildDesvioCosteObraInsight` (margen previsto <15% del contratado, reutiliza `resumenObras`, sin SQL ad-hoc).
  - **Copiloto operativa (#1024 + #1027)**: `imputarParteObra`, `aceptarPresupuestoObra`, `crearPedidoObra`, `enviarPedidoObra`, `registrarSalidaMaterial`, `recepcionarAlbaran` — todos delegan en backend/RPC existente (`obras_imputar_parte`/`obras_aceptar_presupuesto`/`obras_confirmar_salida`/`obras_validar_albaran`), resolución fuzzy, preview con confirmación.
  - **Multimodal (#1031)**: enrutado por intención de imágenes WhatsApp con **factura como default seguro** (triple barrera miembro+sector+caption); el adjunto se preserva SIEMPRE en `_pending` antes de desviar (una clasificación errónea no destruye el documento); "recibo" fuera de gastos. Hallazgo ALTO de la revisión (pérdida de adjunto) corregido antes del merge.
  - **MCP (#1026 read + #1028 read-resto + #1033 write)**: scope `obras:read/draft`, namespace `/api/v1/obras/*` **user-token-only** (guard 3 capas: api_key→404 ANTES de sector/scope; resuelve `obra_id∉v1` sin tocar el contrato genérico de `/api/v1/facturas`). **15 tools** (11 read + 4 write draft: `imputar_parte/crear_pedido/crear_instalador/crear_material_obra`). Fuente única: funciones de dominio compartidas web↔v1. **Sin migración** (la matriz de roles ya tenía `obra`; solo faltaba `RESOURCE_DRAFT_SCOPE.obra`).
  - **Despliegue**: MCP `autoDeploy=false` (compose `pvUJ67f8qdLKOitI8RERO`) → **3 redeploys manuales** vía `compose.deploy`, verificados por `/health`: **48→54→60→64 tools** (hash `ab0125b5`, status ok). App autodeploy en cada merge.
  - **Verificación**: gates verdes por PR; write MCP + multimodal con **revisión adversarial** (write limpio). Ola 3.3 **rebasada** sobre origin/main (avance de sesiones paralelas) resolviendo el conflicto de la ficha-ampliada de instalador (mig 522) sin revertir ajeno.
  - **Smokes pendientes (Manu, externo)**: WhatsApp real (generar/imputar/aceptar/explicar/pedido/salida/albarán) + MCP con token `obras:read`/`obras:draft`.
  - **Follow-ups CERRADOS 19-jul**: umbral de margen del insight configurable por org (#1035, `obras_settings`), single-source de las tools copiloto `crearPedidoObra`/`recepcionarAlbaran` (#1038, web+v1+copiloto comparten dominio), OCR estructurado del adjunto multimodal + clasificación por visión (#1039, factura default intocable). Artifact análisis https://claude.ai/code/artifact/fea2648e-f0ff-4f2b-93ce-88f92096fafe. Ver [[namespace-v1-reservado-user-token-para-sacar-campo-del-contrato-api-key]].
  - **Follow-ups + smoke real WhatsApp CERRADOS 2026-07-20** (mig 523 aplicada por MCP): manuales foto→acción + umbral margen (#1041), negrita WhatsApp `**`→`*` en `sendText` (#1060, el LLM emitía Markdown), y **resolución tolerante de entidades del copiloto** (#1068, prompt v34): materiales por RPC fuzzy + obra/proveedor/instalador por AND-de-palabras normalizado + tool `buscarMaterialesObra` + fix generador. El smoke destapó la causa raíz: el copiloto verificaba materiales con `buscarCatalogo` genérico (ciego a `obras_materiales`) → ver [[copiloto-entidad-tabla-propia-necesita-tool-busqueda-y-regla-en-prompt]]. **Verificado E2E en BD**: parte por WhatsApp (`imputarParteObra`, 210 €). **Smoke pendiente (Manu, tras deploy #1068)**: pedido/salida/albarán con "cable"/"base suko" + foto factura-sin-caption→factura (inviolable PR-C). MCP `/health` intacto 64/`ab0125b5` (ningún PR tocó `services/mcp-server` → sin redeploy).

- ✅ **Botón «Ask» glass + spark real (topbar + Copiloto) — PR #1054 MERGEADO (2026-07-20, `7675b8f4`)** — réplica 1:1 del Glass Button 2.0 (Figma) extraída por Figma MCP del **fichero editable** (`ABZN9UCmOcTARfhDXwIp0F`, ≠ community bloqueado): `.ai-btn` con borde-degradado + 10 sombras exactas, `ai-spark-*.svg` = vector real del nodo `1:667`, avatares del Copiloto a chip glass con el spark arcoíris. Fuente M PLUS Rounded 1c. Follow-up **#1056** (`e8a2c69d`): botón −15% + destello animado (glint recortado al texto, `background-position` en recorrido irregular ~8.5s, respeta reduced-motion). **Smoke pendiente**: prod claro+oscuro (topbar + abrir Copiloto + fuente/SVGs cargan + glint anima). Ver [[figma-mcp-community-bloqueado-vs-editable]].

- ✅ **Sidebar rail plegado: centrado + pestaña visible + tooltips por icono — PR #1010 MERGEADO (2026-07-19, `953a1ba0`)** — en modo plegado: cabecera del rail a columna centrada sobre el eje de iconos de familia (44px) + gap fantasma del logo eliminado; pestaña de expandir con fila propia y tinte brand (antes caja translúcida al 4%, invisible); cada icono icon-only (familias, CTAs, Admin, toggle) usa el `<Tooltip>` de floating-ui en vez del `title` nativo. 3 archivos (`globals.css`, `sidebar.tsx`, `nav-section.tsx`). Sin learning (UI derivable). **Smoke pendiente** (ver Smoke).

- ✅ **Obras — polish UI + auditoría de mejoras EN PROD (PRs #1005 + #1006 + mig 513, 2026-07-19)** — **#1005 (polish)**: módulo alineado al kit `src/components/ui/` — selector superior y sub-selectores → material glass-rail de `Segmented` (con `<Link>`), 9 checkbox → `Checkbox`, 4 date → `DatePicker`, 18 empty states → `EmptyState`, inputs bespoke de delegaciones → `set-input`, fix chevron "Mes anterior". **#1006 (perf+fix)**: N+1 de presupuestos → RPC batch `obras_presupuesto_total_batch` (**mig 513**, aplicada a prod por psql+registro NNN — smoke single==batch==96,30 €), pre-chequeos de tenancy dan 500 real (no 422 enmascarado), validación de params (uuid + fecha calendario-estricta) → 422, `/api/obras/pedidos/[id]` devuelve `obra_descripcion`, catálogos de referencia cargados una vez (no por tecla/línea), paginación en tipos-M.O. (~700 filas), `fmtDate` a `@/lib/format` en 8 vistas, `fetchJson<T>` genérico (~91 call sites, fin de casts a ciegas), cap 2000 solo en listado (retirado de la agregación de informes). **Guardarraíl nuevo en `CLAUDE.md`**: reutilizar SIEMPRE el componente compartido de `src/components/ui/`, nunca reimplementar control nativo. `/fia-cierre` (9 dim, rápido) → 1 bloqueante operativo (mig sin aplicar) + 3 avisos, todo resuelto antes del merge. **Smoke pendiente**: QA visual de la pasada-2 en pantalla (DatePicker/EmptyState/chevron/delegaciones) — el navegador QA local se colgó. **Deuda menor diferida**: paginar tipos-M.O. server-side (hoy client), contexto `org_id` en logs de tenancy, `fetchJson` con Zod-en-borde (opcional, API interna ya tipada). Learnings [[cap-en-id-set-que-alimenta-agregacion-infra-reporta-en-silencio]] · [[aplicar-migracion-por-psql-y-registrar-version-cuando-el-cli-supabase-esta-bloqueado]].

- ✅ **Módulo Clasificador OCR: gate real de `auto_categorizar` + métricas de aprendizaje IA + polish — PR #990 MERGEADO a prod (2026-07-18)** — QA visual del modal (`/agentes`). (1) El toggle admin `auto_categorizar` era **decorativo** (`implemented:true` sin consumidor: la categoría se aplicaba siempre) → ahora gatea la escritura en proveedor+factura, +2 tests. (2) **Sacada a la UI del cliente la señal de aprendizaje** que solo vivía en `/admin/ia-ops`: tarjetas "Precisión IA (30d)" (`auto_accuracy`) y "Hacia modo automático" (progreso al gate ≥50 decisiones y ≥95% acierto), datos reales de `agentic_decisions` vía `getAutoAccuracy`+`getAutomationState`. (3) Polish: `.mod-stats-grid` auto-fill→auto-fit (fin del descuadre), min-height uniforme, valores de texto sin desbordar, loading en Config tab, token `--muted-foreground` (inexistente)→`--muted`. Glass intacto. Gate+43 tests verdes. **Smoke pendiente** (ver Smoke). Ver [[defensa-cableada-vs-codigo-muerto]].

- ✅ **Cashflow a base caja + capa "Vencimientos" calendario — PR #986 MERGEADO a prod (2026-07-17)** — reportado en org Instalaciones Eléctricas: saldo 90d duplicado (−4499 vs −2249). Causa: mezclaba devengo (meses reales por emisión) + caja (futuros por vto) → misma factura 2×. Fix: base caja pura, cada factura una vez (cobrada→fecha cobro/pago; pendiente→vto; vencida impagada→arrastra a hoy), saldo anclado en HOY. 6 consumidores (web/dashboard/API v1/MCP/voz/cron). 23 tests. **Dato**: esa org tiene 2 recibidas pendientes (2249 vto 20/09 + 66,45 vencida desde 08/12/2025) → saldo correcto −2315,88. Ver [[cashflow-forecast-base-caja-no-mezclar-devengo-con-vencimiento]].

- ✅ **Calendario: aclarar cobros/pagos del día = vencimientos — PR #987 MERGEADO (2026-07-17)** — del smoke de #986: modal del día mostraba "Cobros/Pagos 0,00 €" junto a una recibida ese día (recepción ≠ caja hasta vto), leía como fallo. Línea aclaratoria bajo los totales. Solo copy+estilo.

- ✅ **Dashboard: KPIs "Salud del negocio" vacíos en Vista cliente — PR #988 MERGEADO + mig 495 aplicada (2026-07-17)** — del smoke de #986: el cliente de impersonación stubbea TODAS las `.rpc()` → `dashboard_kpis` (mig 460) falla → skeletons. Afectaba a cualquier superadmin viendo cualquier cliente. Fix: `dashboard_kpis` gana `p_org_id` opcional (mig 495) + endpoint `/api/admin/impersonate/rpc` (superadmin, allowlist read-only) + el impersonate-client reenvía las del allowlist. Verificado en BD (RPC devuelve datos reales con p_org_id). **Smoke pendiente**: refrescar Vista cliente tras deploy → tarjetas pobladas. Ver [[impersonate-client-stubbea-rpc-datos-de-rpc-vacios-en-vista-cliente]].

- 🟢 **Módulo Obras (mini-ERP instalaciones eléctricas/telecom, sustituye a WAPI) — EN PRODUCCIÓN · ACTUALIZADO 2026-07-19 (FASE 4 MERGEADA)** — **FASE 4 (cierre del gap WAPI) MERGEADA a main + desplegando (PR #1008, squash `684e6110`).** Migs renumeradas a **514-518** (main ya tenía 513 de #1006). Merge con #1004-1006 resuelto (kit de #1005 + injerto FASE 4). **FOLLOW-UPS**: `migration repair --status applied 514..518` (red con Postgres; auto-sana en próximo `db push`) · smoke prod obras (salida con material mapeado) · revisar manuales borrador. Detalle FASE 4 ↓. 11 issues obras-040..050: G2 estadísticas en vivo, G1 almacén ligero (salidas+stock+ubicaciones, `stock_consumo` default `al_facturar`, N1 resuelto: Natalia tiene almacén, adopción gradual), G3 incidencias/festivos (aviso no bloqueo), G4 conciliación pedido↔factura línea a línea (enlaza F3), G5 importador Telematel masivo (preview diff + `obras_import_tarifas_runs`), G7 reabrir obra + fecha finalización + avisos. Migs **514-518 en main** (514 import_runs, 515 g7_cabos, 516 incidencias/festivos, 517 pedido_factura_lineas, 518 obras_salidas — la 518 amplía el CHECK vivo de `stock_movimientos` sin regresión). Objetos ya en prod; falta solo `migration repair --status applied 514..518` (auto-sana en próximo `db push`). `/fia-cierre` (12 dim) ❌→✅: 1 bloqueante (parrilla escondía obra abierta el día `hasta`, TIMESTAMPTZ vs date-only) corregido+test; QA visual claro/oscuro (8 vistas, sin defectos) + smoke 6 caminos; 3 defectos UI corregidos (form anidado incidencias, aviso parte no mostrado, copy cierre irreversible). Artifact https://claude.ai/code/artifact/916dc848-d8c9-4740-9fae-bc3f83362f7b. Plan `docs/architecture/obras/fase4-plan-decisiones.md` + ADR-obras-004/005/006; durable `issues/CONTINUAR-obras-fase4.md`. **MERGE HECHO (PR #1008, squash `684e6110`)**: rebasado sobre origin/main (que había avanzado con #1004-1006), 9 conflictos UI resueltos (base = kit compartido de #1005 + injerto FASE 4 + `fetchJson<T>` de #1006), migs renumeradas 513-517→**514-518**. Deploy auto Dokploy. Si cifrado (#998) va después, renumera lo suyo (main llega a 518). Diferido: G6 factura liquidación = solo **ADR-obras-005** (checkpoint fiscal N3, sin código); Telematel API = Plan B tras N5. — _FASE 3 abajo:_ **FASE 3 mergeada (PR #999) + smoke prod verde.** FASE 3 = decisiones de Natalia: coste MO fiel (tarifa por instalador `obras_tarifas_hora_instalador`, `precio_hora_especial` por obra, `dieta_importe_default`, RPC `obras_imputar_parte` v2 cascada `manual>obra>instalador>categoría`, calendario mensual de partes), módulo **Herramientas** (mig 511: foto+event log append-only+RPC transición atómica; +alta/movimiento por WhatsApp vía tools copiloto `consultarHerramientas`/`registrarMovimientoHerramienta` con confirmación), **corregir descuento (derivado del neto facturado) Y precio de tarifa desde la recibida** (obras-024 rematado), **proforma a origen** (informe PDF informativo, NO createDocument — ADR-obras-001), **generar pedido desde la obra** + expansión de UO en el desglose Pre/Ped/Ext, chip "recibido X/Y". Migs **510-511** aplicadas y **registradas** (`schema_migrations` local==remote, se acabó la deuda de reconciliación). Plan `docs/architecture/obras/fase3-plan-decisiones.md` + ADRs 001/002/003; estado durable `issues/CONTINUAR-obras-fase3-issues.md`. **`/fia-cierre` cross-issue (11 dim) cazó 2 bloqueantes que los gates por-issue NO vieron** (`.or()` sin entrecomillar reintroducido en tools copiloto con test que mockea `.or()` no-op → [[postgrest-or-no-escapa-delimitadores]]; unicidad de clave React/dedup rota al componer las olas) + 5 avisos, todo corregido; 1509 tests verdes. **Único pendiente diferido: activar el sector en la org REAL de Natalia cuando exista** (switch superadmin). — _Histórico FASE núcleo/2 abajo._ vertical gateado por `organizations.sector` (activo solo en **Sandbox** `b5f86e8f`, con demo sembrado incl. **1 obra end-to-end**). **HECHO**: catálogo + motor de precio con el **modelo CORRECTO** (descuento por **proveedor×familia**, precio = proveedor **MÁS BARATO/MIN**, +33%; validado con datos reales de Natalia — audio+vídeos+CSV, doc autoritativo en repo `docs/architecture/obras/modelo-wapi-y-precios.md`), unidades de obra recursivas, delegaciones/contactos, **importador CSV Telematel** (cp1252, precio=P.V.P./UNIDADES), **búsqueda fuzzy**, **presupuestos→aceptación→obra** (numeración atómica + snapshot congelado + proveedor por línea + estados borrador/aceptado/ampliado), catálogo de **tipos de M.O.** (**745 tipos sembrados 18-jul**, trigger propaga horas→precio de materiales), **auditoría+notificaciones**, y **conexión con la plataforma** (widget en dashboard + obras en calendario, gateados por sector). Migs **471-494** aplicadas a prod vía MCP **SIN registrar** → `supabase migration repair --status applied 471..494` al mergear. **3 auditorías `/fia-cierre` pasadas** (bloqueantes corregidos: RPC fuzzy ×1000, cero-auditoría, y **RPC obras `obras_cerrar`/`obras_aceptar_presupuesto`/`obras_siguiente_numero_presupuesto` ejecutables por `authenticated`** — escalada de privilegios, 4ª reincidencia del patrón mig 213, revocado en archivo+BD, ver [[postgres-revoke-public-no-elimina-grants-individuales]]). **Deuda diferida app-only CERRADA (18-jul)**: N+1 (proveedores UO + proveedores-resueltos por lote, clonado por lotes), dedup fetchJson (helper compartido), paginación presupuestos, test paridad SQL↔TS del motor + reflow móvil + mensajes de error accionables. Smoke/ux/resiliencia verificados con login real (magic-link usuario e2e, claro+oscuro). **Diferido al merge**: `gen:types` post-merge, `migration repair 471..494`, decisión `precio_venta_calculado` denormalizado, tools MCP `obra:*` (issue 015), y **facturación/certificación de obra** (issue 009, Checkpoint B, decisión cashflow). **FASE FACTURACIÓN CERRADA (18-jul, 8 agentes paralelos)** — Checkpoint B D1-D4 aprobado por Manuel: **D1** certificación = factura serie A ordinaria; **D2** stock descuenta SI módulo Stock activo (gated, mapeo `obras_materiales.catalogo_servicios_id`); **D3** cashflow = pipeline de obras abiertas como potencial (separado del comprometido); **D4** atomicidad = reservar→emitir(createDocument)→confirmar/revertir + reconciliador con **recuperación-hacia-delante** (ver [[consumir-tope-y-emitir-documento-reservar-emitir-confirmar]]) + ADR en repo `docs/architecture/adr-obras-facturacion-atomicidad.md`. HECHO: facturar por líneas (máximo en vivo sin truncar, preview==emitido) + certificación (issues 009/010), Estado de Obra (011, agregados neto excluyendo borrador/anulada), anulación revierte acumulador (hook idempotente en `anularFactura`), IA (insights 014 + generador presupuesto 016 + patrones UO 017), tools copiloto obras (015 parcial: consultar/ver/enviar, gated por sector), **PDF propio del presupuesto de obra (árbol capítulos) + envío email**, enlaces cruzados factura↔obra. Migs **495-501** aplicadas a prod SIN registrar. `/fia-precommit`+`/fia-cierre full` (14 dim) pasados: **1 bloqueante RESUELTO** (banner "Revisar" comparaba base neta vs Σtotal con IVA → sumar `base`). **Ola avisos W2**: IVA seleccionable 21/10/0% (+exención E1-E6, ISP fuera de plataforma), idempotencia (api_idempotency), orquestación extraída a `facturar-obra.ts`, fix N+1 modal, **tools copiloto facturarObra/certificarObra** (015 completo, gated propietario/admin), prefill UO, OpenAPI obras_potencial, **detector de deriva** (mig 501 + cron + incidencia superadmin). **DOGFOOD E2E AUTENTICADO HECHO** (agent-browser sesión real e2e+smoke Sandbox): facturar A2026-0168 con IVA/preview→/emitidas→cross-link→anular→reversión obra (acum 0, deriva 0). Cazó y fijó un **bug real** `fuente='obras'` (viola facturas_fuente_check; ni unit tests que mockean createDocument ni smoke SQL lo vieron), ver [[mock-de-createdocument-oculta-violaciones-de-check-del-nucleo-fiscal]]. Artifact de Natalia actualizado (guía, §09 facturación; solo tema claro + imágenes grandes). **W3**: preview VISUAL del PDF (plantilla+watermark) en facturar/certificar como en /generar. **2ª `/fia-cierre full` (revisión de mejora, 12 dim) → 0 bloqueantes, 8 avisos APLICADOS**: cerrado riesgo de duplicado fiscal (UPDATE obra_id post-insert best-effort, ver [[update-post-insert-que-lanza-sobre-documento-fiscal-numerado-causa-reemision]]), detector de deriva sin falsos positivos de alerta superadmin (SUM reservada+emitida, en prod), EstadoPill abono, paridad notify copiloto, logs enriquecidos, test regresión anulación, manual IVA+ISP. Revisado vs manual WAPI: núcleo fiel, gaps menores (FT/FP, %, MAT/MO toggle) y albaranes/pedidos = fase 2. **3 avisos menores cerrados (18-jul, `e85ebfea`)**: detector deriva acotado (mig 502, `p_org_id` opcional + solo obras tocadas por facturación), toast honesto (`confirmacion_fallida` → "vinculación en curso" si confirmar se difiere al cron), y **abono hereda `obra_id`** para la traza EXCLUYÉNDOLO del sumatorio `facturado_real` (evita doble-resta + falso banner "revisar", ver [[documento-reversor-con-clave-de-agrupacion-doble-resta-en-agregado]]; +2 tests regresión). ⚠️ **Al mergear a main**: mig 495 obras COLISIONA con 495_dashboard_kpis ya en main → renumerar obras 495-502 al primer hueco libre + `migration repair 471..502`. **FASE 2 COMPLETA (18-jul, 11 issues obras-019..029, rama `feat/obras-f2-int`)**: C1 pedidos de compra + integración recibidas/coste/cashflow, C2 albaranes/recepción (reusa stock D2, sin ledger paralelo), C3 instaladores/tarifas/partes → coste real MO en Estado de Obra, C4 textos tipo (presupuesto/pedido), C5 informes+export CSV, C6 facturar por % + toggles MAT/MO + bloque certificaciones. Migs **503-509** aplicadas a Sandbox SIN registrar. `/fia-cierre` ⚠️ con-reservas 0-bloqueantes → **9 avisos corregidos** (bug cazado en revisión: mig 509 preservó `whatsapp_quick`, ver [[check-constraint-recrear-incluye-valores-existentes]]). QA claro/oscuro + smoke E2E real + artifact nuevo **https://claude.ai/code/artifact/404caab9-e92b-4a46-8ffc-ee893c1318aa** (el `b6114dec` viejo está BORRADO). **PENDIENTE: merge a main** (QA OK + `migration repair 471..509` + renumerar colisión 495) y **NEXT con Natalia**: flujo corrección descuento familia desde factura (obras-024, acción explícita+auditada). Plan/decisiones Checkpoint C en `docs/architecture/obras/fase2-plan-decisiones.md`. Super-prompt `issues/CONTINUAR-obras-facturacion.md`. **Estado general DURABLE: `issues/CONTINUAR-obras.md`** (al día; PRD+issues en `issues/prd-modulo-obras.md`+`issues/obras-*.md`). Ver [[rpc-returns-table-subconjunto-tras-add-column-cast-miente]] · [[importar-csv-erp-espanol-encoding-cp1252-y-precio-por-unidad-multiple]] · [[transcribir-video-sin-audio-extraer-fotogramas-ffmpeg]] · [[server-component-no-puede-llamar-funcion-use-client]] · [[consumir-tope-y-emitir-documento-reservar-emitir-confirmar]].

- ✅ **Registro standalone + onboarding en claro + validación en vivo — PR #984 MERGEADO a prod (2026-07-17)** — `/registro` a layout propio de columna única (ya no se confunde con `/login`); onboarding (perfil/fiscal/canales) migrado de dark hardcodeado a tokens de tema; check verde en vivo tras `onBlur` en email/NIF/IBAN/teléfono (nuevo prop `success` en `ui/input.tsx` + `.input-validated-wrap`). De paso: modal "Cambiar teléfono" migrado del `.field` genérico (se veía sin diseño) al componente `Input` real. **Pendiente**: mismo bug de `.field` en `enroll-2fa-modal.tsx`/`cerrar-cuenta-modal.tsx`/`seguridad-section.tsx` (no tocados) + smoke prod (ver Smoke). Ver [[facturaia-multiples-dialectos-input-field-generico-rompe-fuera-de-contexto]].

- ✅ **Iconos de marca reales para integraciones — PRs #973/#975 MERGEADOS a prod (2026-07-17)** — reemplazado el glifo monocromo genérico por SVGs reales (Simple Icons CC0 para Google Workspace/iCloud/M365/Slack/Stripe/PayPal/HubSpot/Zapier/Odoo; Holded/Bizum/Contasol reconstruidos a mano a partir de capturas reales que pasó Manu tras rechazar la 1ª versión dibujada de memoria). Módulo único `src/lib/integrations/providers/brand-icons.tsx`, dedupe de 3 SVGs inline duplicados en `canales-ingesta.tsx`. Sin pendientes. A3 Software sigue con monograma genérico — si llega captura, se ajusta igual. Ver [[react-hooks-static-components-lookup-dinamico]].

- ✅ **Dedup OCR proveedores/clientes — PR #965 MERGEADO + DESPLEGADO + verificado en prod (2026-07-17, mig 467+468)** — proveedor archivado se reactiva por NIF exacto (nunca por fuzzy) + fuzzy match por nombre (word_similarity, tolera sufijo societario) en `resolve_proveedor`; badge "Nuevo · OCR" (`origen`/`revisado_at`) hasta que un humano confirma; aviso `similarWarning` opt-in en alta de clientes por el copiloto. CI atrapó un bug real (RPC advisory sin proteger rompía `createDocument`+voz) → fix `checkSimilarWarning` opt-in+try/catch, 5217/5217 tests. Deploy+health+smokes reales verificados en Sandbox, revertidos. Manuales actualizados. Sin pendientes. Ver [[advisory-lookup-en-funcion-compartida-debe-ser-opt-in]].

- ✅ **Clase 42703 CERRADA en TODO `src/**` — en prod (2026-07-16, #944/#961/#964/#969)** — un `.select('columna_inexistente')` ya rompe el typecheck en cualquier capa (backend, hooks, componentes), no en runtime; guardarraíl ESLint global permanente. Tipar `useOrgClient<Database>` destapó 55 mismatches reales (nullable, enums TEXT, `org_id` en NOT NULL, embed ambiguo por alias FK) + pulido `cashflow.vto`→nullable con fallback centralizado. Verificado: tsc/eslint/vitest, smoke supabase-js + smoke visual (artifact) 0×42703. Detalle → [[facturaia-historico-detallado]] · spec [[facturaia-erradicar-casts-supabase-42703]].

- ✅ **Unificación UI COMPLETA — los 4 Frentes en prod (2026-07-16, 11 PRs #953-#963)** — admin.css/globals.css sin clases `.adm-*`/`.btn-*` huérfanas, todo migrado a `<Button>`/`<Input>`/`<Segmented>` o relocalizado donde el primitivo no encajaba. Sin pendientes. Histórico completo → [[facturaia-historico-detallado]].

- ✅ **Cierre de pendientes (2026-07-13) — 3 PRs #871/#825/#872 MERGEADOS + repo aseado.** Vivo: **#825 (Centro Fiscal, migs 452/453 en prod) — Smoke pendiente** (ver Smoke). Narrativa completa → [[facturaia-historico-detallado]].

- ✅ **Centro Fiscal auditado a fondo — PR #825 MERGEADO + migs 452/453 en prod (2026-07-13).** **Pendiente Manu**: smoke prod fiscal (180/347/303) tras deploy · HITL export posicional + sandbox AEAT (necesita certificado). Detalle → [[facturaia-historico-detallado]].

- ✅ **Sistema de cupones de descuento — 5 PRs MERGEADOS + migs 437-439 en prod (2026-07-07).** **Pendiente = acción Manu**: crear el primer cupón real (50%/3 meses acordado) desde `/admin/descuentos` (nada activo todavía, ningún cliente afectado). Ver [[stripe-coupon-no-es-tecleable-necesita-promotion-code]] · [[worktree-add-sin-cd-bash-cae-en-checkout-principal]]. Detalle → [[facturaia-historico-detallado]].

- ✅ **Auditoría billing Stripe (suscripción) — PR #777 MERGEADO + mig 436 en prod (2026-07-07).** **Smoke pendiente**: los fixes son webhook-driven (reactivación/change-plan/3DS/paused) → validar con eventos Stripe reales o Stripe CLI trigger; la coherencia display↔cobro entra sola en el sweep de salud. Ver [[stripe-subscription-item-resolver-por-price-no-por-indice]]. Detalle → [[facturaia-historico-detallado]].

- ✅ **Export gestoría v1.5 — CERRADO, en prod y verificado con datos reales (2026-07-03).** **Único pendiente real**: Pre303 HITL, bloqueado sin certificado. Detalle completo en [[facturaia-export-gestoria-v15]]. Narrativa → [[facturaia-historico-detallado]].

- ✅ **PR #640 — 5 docs legales compliance pulidos — MERGEADO** (2026-07-02, `e5c90594`). Pendiente acción Manu: revisión final y envío real a FNMT/OpenAI/Anthropic/abogado/seguro (no resuelto por el merge). Detalle → [[facturaia-historico-detallado]].

- ✅ **Smoke /soporte (sandbox, 2026-07-02)** — `/soporte` carga con empty-state (bug del Dashboard arreglado). **Manu verifica #NNN+badge «IA revisando» en `/admin/feedback` como superadmin** (no verificable con e2e+smoke). Detalle → [[facturaia-historico-detallado]].

- ✅ **Auditoría de cobertura de tools de agente (MCP/copiloto/v1/web) + fixes — 4 PRs (#890-#893) en prod 2026-07-14.** Frontera fiscal intacta y blindada. **Smoke pendiente** (ver `Smoke tests pendientes`). Learning [[capacidad-en-dos-capas-tool-mas-gate-endpoint-verificar-paridad-o-queda-muerta]]. Detalle → [[facturaia-historico-detallado]].

- ✅ **Plan consolidación WhatsApp/copiloto (post G5) — PR #705 MERGEADO A MAIN (2026-07-05).** Feature `cobro_stripe` gated off en `org_features` (sin riesgo en prod hasta activarla). **Pendiente (navegador Manu)**: crear cuenta Connect test → capturar payload real `checkout.session.completed` → QA localhost Ajustes›Cobros → config prod (webhook Connect + `STRIPE_CONNECT_WEBHOOK_SECRET`) → activar `cobro_stripe` en org piloto (el E2E de pago quedó bloqueado por config del sandbox Stripe, no código). **Pendiente (bloqueado por red al pooler Supabase)**: `supabase migration repair --status applied 430 431 433 434`. Ver [[payment-link-importe-congelado-revalidar-pendiente-al-conciliar]] · [[stripe-connect-signup-gotcha-crear-cuenta-conectada]]. Narrativa completa → [[facturaia-historico-detallado]].

- **Integración Slack — LIVE EN PROD ✅ (#002-007b).** App `TuFacturaIA` en Slack (App ID `A0BDL7PQX8V`, workspace dev *Agentesia Lab*), envs `SLACK_*` + `SLACK_ENABLED=true` en Dokploy, workspace conectado. **`/factura resumen`/`cobros` verificados con datos reales.** #007 (#558): vinculación de identidad (`slack_account_links` mig 410, página `/integraciones/slack/vincular`) + escrituras autoría `agent:slack` (`cobrada`/`aceptar`/`rechazar`/`borrador`/`presupuesto`, mig 411, `canWriteInOrg`, no emite/anula). #007b (#564): lecturas `cliente`/`cashflow`/`facturas`/`presupuestos`. Migs 406-411 en prod. Reusa `createDocument`/`CopilotoStore`/`getOrgCashflow`. **Smoke pendiente**: escritura real (`/factura cobrada <nº>`→deep-link→vincular→reintentar) + comandos #007b en vivo. **Opcional futuro**: Events URL `…/events`+`file_shared` para OCR por subida (#005); *Manage Distribution* para multi-cliente. ADR-009 · manual-admin §9.

- ✅ ~~Registro branded + onboarding (PR #256/#258)~~ — ciclo antiguo ya desplegado; superado por el rediseño de hoy (ver entrada de arriba).

- **Iniciativa agéntica S7+G6 CERRADA ✅** (2026-06-30): batch recordatorios headless (#590) · config por-org UI (#591) · smoke 9/9 ✅ · cron `mcp-dcr-cleanup` (`mjZJcyyRo4wRopEoNCqZf`) ✅ · cron `copiloto-recordatorios-batch` (`M0ncm_6cJfjuGLJgaWwIw`) ✅ · smoke prod `243f42c3` verde (1 email enviado, 4 candidatas, 0 fallos). G6 cerrado.

- **G5 — Desacople copiloto↔n8n — CUTOVER COMPLETO ✅** (2026-06-30): envs `WHATSAPP_VERIFY_TOKEN`+`WHATSAPP_APP_SECRET` en Dokploy ✅ · webhook Meta Dashboard = `https://app.tufacturaia.com/api/whatsapp/webhook` (confirmado screenshot) ✅. (1) canary G5 y (2) smoke `doc_confirm:change`/multi-org doc — ejecutados 2026-07-05, ver entrada arriba (bug real encontrado+arreglado PR #764, falta reverificar). (3) ~~`supabase migration list --linked` → confirmar mig 418 aplicada en prod~~ — confirmado por SQL directo 2026-06-30. G8 (futuro): selector org interactivo in-WhatsApp.

- **Botón "Enviar a Conciliación" (Bandeja IA) reconectado ✅** (2026-06-30, `b1d61b4d`+`2242db18`): el endpoint vivía huérfano en rama `fix/ingesta-progress-css` sin mergear; portado y reescrito (el approach original no funcionaba con datos reales — ver [[ocr-clasificacion-doc-type-no-factura-sin-campos]]). Smoke real con PDF + clic UI verificado en Sandbox. Manuales actualizados. **Pendiente cleanup menor**: borrar rama `fix/ingesta-progress-css` (ya portada, queda obsoleta).

- **Dropzone "Importar extracto bancario" (/conciliacion) — CERRADA ✅ (2 PRs, 2026-07-22)**: #1148 sustituyó el recuadro punteado plano por la pieza compartida "Border Beam glass" (misma que `importar-factura-modal`); #1151 (reporte de Manu tras el primer merge) corrigió el hueco muerto bajo la caja — el modal `size="lg"` fuerza `min-height:480px` con independencia del contenido — subiendo el `min-height` de la caja a 340px + mejoras de texto/contraste (badge circular, chips de formato). QA visual claro/oscuro/hover en ambas rondas. Ver [[next-typed-routes-validator-stale-tras-cambio-de-rama]] (variante: `pkill` a `next dev` a medio-generar corrompe `.next/dev/types/validator.ts` y bloquea el pre-commit).

- ✅ **Módulo Obras — kit de listas (filtros/columnas/orden/selección) + botón Importar tarifa — EN PROD** (mergeado con FASE 3, PR #999; smoke prod verde). _Checklist original conservado abajo por si hace falta re-verificar tras cambios:_ en localhost (claro+oscuro), verificar: botón "Importar tarifa" en Materiales lleva a `/obras/materiales/importar` y sube un CSV Telematel real (Orbis/General Cable/Normalit) con preview; **filtros** marca/familia/estado en Materiales; **ordenar** pulsando cabecera e **invertir al re-pulsar** (bug StrictMode-dev ya corregido, ver [[setstate-anidado-en-updater-rompe-bajo-strictmode]]); **mostrar/ocultar columnas** persiste por vista (localStorage `obras:*:cols`); **selección múltiple + acciones en lote** (descatalogar/dar de baja/eliminar, checkbox indeterminate, toast de fallo PARCIAL con material en uso, la selección de fallidos se conserva); pills unificadas a `EstadoPill` en TODAS las pestañas (Activo/Descatalogado, Dada de baja) + Familia como chip de color — verificar que se ven como Pendiente/Cobrada del resto de la app; **navegación del sidemenu** corregida (antes "Obras" y la subpágina salían activas a la vez — ver [[nav-activo-por-startswith-enciende-el-ancestro]]): confirmar que solo se resalta la pestaña actual. Reservas preexistentes anotadas (orden client-side sobre listas topadas a 2000; endpoints `[id]` de obras sin `logAgentAction`). Gate `/fia-cierre` pasado (0 bloqueantes).

- ✅ **Holded rol cliente/proveedor — CERRADO y verificado en prod (#908/#915/#918, 2026-07-15)** — Separación cliente/proveedor viva y correcta (MAKRO/TeamViewer/Zoom en Proveedores, fuera de Clientes; 283 clientes / 131 prov holded_sync, 0 dup NIF). `holded_sync_enabled` REACTIVADO. Pull **verificado limpio en prod: `errors=0`** (cron_runs 18:30, 434 contactos, 0 inserts/0 dup). Causa raíz de los 4 errores que fallaban: `PROVEEDOR_COLS` pedía `updated_at`, columna que `proveedores` NO tiene → 42703 → `findByNif` lo tragaba → INSERT en vez de link → choque unique (Telefónica dup + Santander). Fix #918: quitar la columna + findByNif throw. Historial completo en [[facturaia-historico-detallado]]. **Barrido post-fix (#922, mergeado):** subagente validó 5019 cols de select + 3422 filtros contra `information_schema` → 4 más del mismo patrón 42703 arreglados: `presupuestos.iva_pct` (pestaña Presupuestos del cliente salía vacía, user-facing), `lineas_factura.created_at` (docs de producto en Stock), `plan_features.plan_id` (admin features), `facturas.num_factura` (anomaly, dead/tests). Todos verificados error=null contra prod. Ver [[column-list-drift-clientes-proveedores-select-inexistente-42703]] · [[holded-v2-contacto-rol-por-record-no-por-type]] · [[dokploy-redeploy-mismo-commit-es-no-op-cambiar-sha-fuerza-recreacion]].

- ✅ **#892 capacidades de agente — SMOKE PROD COMPLETO 2026-07-14 (3/3)** — vía copiloto REAL (UI web, usuario e2e admin, Sandbox): (1) **aprobar recibida** Mesoestetic 895,40 € → preview + Confirmar → `pendiente` (BD `189d7d1c`, updated 20:51); (2) **conciliar** mov +60,50 € 28-jun ↔ A2026-0166 → preview "aplicando 60,50 €" + Confirmar → mfa 60,50 cobro/manual + factura **cobrada** (updated 20:52); (3) **v1** `POST /v1/facturas/recibidas/{id}/aprobar` (api_key test): 401 sin auth · 200 `pendiente` · replay idempotente · 401 con key revocada. Gate destructive+confirmación funciona; frontera fiscal intacta. MCP sigue `MCP_PUBLIC_ENABLED=off` (no probado en prod). Artifact: claude.ai/code/artifact/9f95e641. PRs #890/#892/#893. Auditoría en `scratchpad/auditoria-tools-superficies.md`.

- ✅ **#875 aprobación masiva recibidas — QA 2026-07-14 OK** (contador+relleno del botón, `num_factura`). El QA destapó 2 bugs → arreglados en **#888** (abajo). Ver [[accion-masiva-cliente-n-round-trips-serie-cuelga-usar-batch-y-pool]].

- ✅ **#888 selección por id + auto-orientación OCR escaneos — MERGEADO + VERIFICADO EN PROD (2026-07-14, `ec13cb1b`)** — (1) `docKey`→`id`: el nº de recibidas se repite → la aprobación masiva saltaba filas en silencio y "seleccionar todo" no limpiaba. (2) Escaneos del revés (PDF/imagen) que no se leían → auto-orientación (reintento rotado solo en fallos: pdf-lib PDF / sharp imagen) + rotar manual en el visor + toggle admin `auto_orientacion`. Verificado en prod (Alpine): PDF fallido → `Tamme Air Conditioners · 254,21 €` persistido; sharp/pdf-lib cargan en Alpine. Mig 461 aplicada+reconciliada. Ver [[clave-seleccion-fila-debe-ser-id-unico-no-campo-de-negocio]] · [[openai-vision-lee-pdf-del-reves-rotando-con-pdf-lib]] · [[dep-nativa-import-dinamico-defensivo-en-ruta-api]].

- ✅ **#873 /informes selector de periodo — SMOKE PROD OK (2026-07-13)** — el selector (cabecera del Modelo 303) gobierna toda la página (tarjeta 303 + IRPF + comparativa + exports) y permite trimestres/ejercicios anteriores. Verificado en prod. Ticket bgchivite `c1274624` cerrado.

- ✅ **Auditoría Fable 5 — smokes runtime post-merge CERRADOS (2026-07-13)** — los 3 verdes en prod con datos revertidos: 403 cuenta cerrada (mig455) · #857 duplicado por caja conserva unidad_medida/cantidad_presentacion (mig456) · cashflow excluye recibidas sin_aprobar/disputada (#845, mig460). Detalle+evidencia en la entrada 🟢 de arriba. Migs 455/456/457/459/460 en prod (MCP `apply_migration` + ledger reconciliado a NNN; ver learning `supabase-mcp-apply-migration-version-timestamp`).

- ✅ **Drawer «Personalizar» del Resumen (#819 + fondos #821) — SMOKE PROD OK (Manu, 2026-07-10)** — panel frost sólido + fondo más claro confirmados en prod. Ticket `678aa22c` (Borja) cerrado. Nota: en móvil ya no es bottom-sheet, es panel lateral como el resto.

- ✅ **ia-ops diagnóstico shadow (#798) — SMOKE REAL PROD OK (2026-07-10)** — verificado en app.tufacturaia.com con org FacturaIA Sandbox (superadmin temporal, revertido): funnel 0 verde/26 ámbar/17 rojo, motivos con etiqueta legible (`Proveedor aún no de confianza` 43), maduración 0/2/34. Exactamente lo diseñado. Gate cerrado por 0 verdes = confirmado el cuello de botella.

- ✅ **#810 follow-up — etiquetas cuadres `irpf_*` del 130 — RESUELTO (#812, main 2026-07-10)**: los 6 tipos IRPF del 130 añadidos a `cuadreLabel` (`cuadre-meta.ts`) con etiqueta legible + test. Gap del smoke cerrado.

- ✅ **feat SKU inventario (PR #752) — smoke real en prod CERRADO (2026-07-05)** — auditoría NOTES.md completa: dedup `generar-view` (#756/#757), PR drift de tipos (#763), smoke real con agent-browser contra org sandbox (login real + SQL directo) que encontró y corrigió 2 bugs (#766): precio import CSV corrupto ×100 en formato US, producto invisible en /inventario tras import sin columnas de stock. **Pendiente real**: confirmar deploy Dokploy de #766 antes de darlo por verificado en producción. Detalle en [[facturaia-historico-detallado]].

- ✅ **Reorg split in-place — COMPLETA, 14 ficheros en main** (2026-07-05). Detalle completo movido a [[facturaia-historico-detallado]].

- ✅ **PR #703 (cron housekeeping — falso positivo mcp-dcr-cleanup) — MERGEADO** (confirmado `gh pr view 703` → MERGED). Pendiente menor: confirmar que no recurre el email ALTA falso si un cron housekeeping salta un tick (visible en panel como 'media'). Ver [[cron-mantenimiento-auto-sanable-no-debe-paginar-severidad-por-criticidad]].

- ✅ **Datos y backups fases 3-4 — smoke prod COMPLETO 2026-07-05** — a3 SUENLACE.DAT (151 registros, 255c fijo) + SII emitidas/recibidas XML bien formados; ciclo cierre-de-cuenta con org de test (gracia vencida → `runManually` → bloqueo real sin DELETE, trigger anti-borrado confirmado, revertido); `data-export-runner` sano (60/60 success, nada caducado). Detalle completo movido a [[facturaia-historico-detallado]].

- **✅ "Resolver con Claude directo" — smoke E2E CONFIRMADO (2026-07-03)** — primer ciclo real: ticket de Borja (`/emitidas`, exportar selección múltiple solo daba CSV, faltaba PDF en ZIP) → runner headless generó PR #660 (`fix/ticket-ad1ac8e8`) con `gh pr create` (nunca merge auto) → CI verde (lint/typecheck/test/build/integration/CodeQL) → revisión manual (código correcto: ZIP writer verificado byte a byte contra spec, sin deps nuevas) → aprobado y mergeado a mano. Cierra el pendiente de `CLAUDE.md` §Pendientes acordados (fases 1-3 del botón). Queda solo QA visual en localhost/prod del botón "Descargar PDF (ZIP)".

- **✅ "Resolver con Claude" ciclo 3 (PR #704, ticket `9edebeee`) — MERGEADO (`--admin`), fix correcto sin bugs** (2026-07-04) — login con mitad derecha en blanco en iPad portrait (768px exacto): `@media (max-width:768px)` de MOBILE AUTH y `@media (min-width:768px)` de TABLET AUTH se solapaban en ese punto, la rejilla pasaba a tablet (2 columnas) pero `.auth-left` seguía en `display:none` de mobile. Runner bajó el límite mobile a `max-width:767px`. Diff mínimo (1 media query, CSS puro), revisión manual sin hallazgos. PR quedó en draft porque el gate interno del runner falló typecheck (deps `jose`/`@testing-library/react` ausentes en `node_modules` del contenedor runner, ajeno al cambio) — se marcó ready a mano. Checks de GitHub (`quality`/`integration`/`Analyze`/`Visual regression`) fallaron en 1-3s por el billing de Actions bloqueado (misma recurrencia que #684-#698), no por el cambio → merge con `--admin` tras confirmar diff trivial y correcto.

- **✅ Post-#648 — los 4 smokes PASADOS en prod (2026-07-02, ejecutados por agente)**: (1) email factura real con branding completo (subject/from "FacturaIA Sandbox", NIF+logo en HTML, delivered, PDF adjunto — verificado en email_log_bodies; copia en dansanch@tecnocloud.es para ojo visual); (2) duplicar v1 → 201 P2026-0033 con líneas copiadas (borrador borrado, keys temporales revocadas) — **queda avisar a Borja del contrato resucitado**; (3) worker VeriFACTU → PRIMER run en cron_runs: success {procesadas:0} — necesitó fix extra PR #649 (la ruta no estaba en isServiceRoute → 307; el cron JAMÁS había ejecutado el handler) — **confirmado post-deploy #649**: run real del cron Dokploy 2026-07-02 15:15 UTC, success, {errores:0, procesadas:0}; (4) copiloto getURLPDF → URL firmada que descarga PDF válido.

- ✅ **`/admin/features` — toggle "core" devolvía 400 silencioso — MERGEADO** (PR #652, 2026-07-02, `4bf2ad80`): `core` añadido a `adminUpdateFeatureSchema`. **Falta: Manu confirma en `/admin/features` como superadmin que el toggle ya no da 400 (no verificable con mis credenciales, sin rol superadmin).**

- ✅ **MCP stock: 2 bugs reales encontrados explorando las 48 tools — AMBOS arreglados y verificados en prod** (main `826540be`, doc completo en `docs/qa/mcp-exhaustive-exploration.md`). (1) `buscar_catalogo`/`consultar_stock` 500 por falta de columna `created_at` en `catalogo_servicios` (mig 423). (2) `ajustar_stock` sobre producto con lotes ahora devuelve "se gestiona por partidas..." en vez del 422 genérico — confirmado en vivo vía MCP tras autoDeploy.

- ✅ **Reorg split + auditoría NOTES.md — CERRADA (2026-07-05)** — split verbatim de los 14 ficheros grandes completo (Tier A/B/C) + las 9 duplicaciones idénticas de `NOTES.md` resueltas (5 PRs #757-761, ver entrada arriba y [[facturaia-historico-detallado]]). Mecanismo + gotchas: [[split-verbatim-checker-parts-por-basename]] · [[gh-pr-merge-no-confirma-verificar-state-merged]].

  - ~~**▶ 017 (modelo 349): calculador**~~ ✅ **HECHO 2026-06-10** — calculador #172 (`460dd2e`) + vista de detalle + IA #173 (`cfd604e`) + **mig 250 EN PROD** (enum 4→10 claves, seed plazos). Ver Progreso. **Fast-follows pendientes**: (1) **export fichero posicional oficial AEAT** (HITL sandbox, como 390 — falta sourcear posiciones byte-a-byte del Anexo); (2) **drill-down por registro** (requiere enriquecer el snapshot con clave/nif por fila); (3) **captura auto-intracom por OCR + detección de operaciones UE olvidadas** (fase 2 — hoy el pipeline OCR no extrae la clave por línea).

- **✅ Auditoría UX Ley de Jakob — EN PROD (2026-07-14).** PR #880 (`fix/jakob-ux-audit`, 18 commits) squash-merged (`eebdadec`) + manual #881 (`cbd7c9f5`). Los 3 "propuesto" del artifact cerrados tras auditar su alcance real: **`ListToolbar`** común (+ adopción en `/inventario`; aging/remesas/recurrentes no encajan → no retrofit), **`BulkBar`** común (+ bulk en Presupuestos y Clientes), y **modales** sin migración segura pendiente (Fase 3 ya migró los standalone; el resto es drawer/panel/bubble o nested-in-drawer con Escape a mano — ver [[migrar-submodal-a-modal-choca-con-escape-del-contenedor]]). Detalle en `archive-completed`. Selección en la vista Tarjetas de Clientes ✅ (#882, 2026-07-14). Sidebar: se mantiene **nav visible por defecto** (Opción B), decisión confirmada 2026-07-14 — no se revierte a colapsado+TTL.

- **✅ Sidebar flotante glass + `.reveal` + secciones plegables + skeleton Clientes — EN PROD desde 2026-07-07** (`fff933e6` + #880). NO era working-tree: el `stash` "ui-glass-sidebar-reveal-skeletons WIP" era obsoleto → **soltado** 2026-07-14. **Decisión 2026-07-14: Opción B** (nav desplegado por defecto, sin TTL; #880 quitó el auto-cierre a propósito) — artifact comparativo antes/A/B. Follow-ups de consistencia cerrados esta sesión: skeleton-que-calca en **emitidas/recibidas** (#883), **bulk en tarjetas de Clientes** (#882), **fix teclado** (checkbox no abre el detalle, #884). Ingesta ya calcaba. Queda menor: consolidar ~14 keyframes shimmer duplicados al `.skeleton` canónico + smokes (ver `Smoke tests pendientes`). Learnings: [[skeleton-debe-calcar-el-layout-real-para-no-hacer-snap]] · [[transform-o-filter-en-ancestro-rompe-hijos-position-absolute]] · [[glass-flotante-necesita-fondo-detras-liquid-glass-portable]] · [[full-bleed-flex-item-margin-negativo-stretch]].

- ~~Calendario como agenda de trabajo~~ **HECHO 2026-07-17** — las obras ya afloran en `/calendario` como capa de rango (inicio→cierre), gateada por sector. Ver §Módulo Obras.

- ~~**🔴 P0 FISCAL — PDF abono muestra importes POSITIVOS en lugar de negativos**~~ ✅ **PR #64 MERGEADO + SMOKE POST-DEPLOY VERDE 2026-05-21** (commit `d84f10d` en main). Smoke: anulada A2026-0014 (605€) → abono nuevo generado con código del fix → PDF renderiza importes negativos correctos. Único pendiente menor: decisión regen masivo PDFs históricos abonos pre-fix (en sandbox solo B2026-0001 con PDF antiguo).

- ~~**🔴 P1 UX — Conciliación: vinculación factura↔movimiento INVISIBLE en la UI**~~ ✅ **PR #65 MERGED 2026-05-21 12:05 UTC** (verificado 2026-05-22 10:25 via `gh pr view 65`, branch `feat/conciliacion-vinculacion-visible`). 10 archivos, +906/-33. Mig 131 helper SQL `desvincular_asignacion` con orden crítico last_revert_at-first (evita loop trigger infinito descubierto durante implementación) — **función verificada en prod 2026-05-22 10:25** (PostgREST hint confirma signature `(p_asignacion_id, p_razon, p_user_id)`). Endpoint `POST /api/conciliacion/asignaciones/[id]/desvincular` role-gate propietario/admin/contable, drawer rediseñado (border-left ok, SVG link, factura prominente, botón desvincular + modal confirm), microtexto match en tabla `/conciliacion` y listados `/emitidas` `/recibidas`, modal detalle factura con bloque "Cobro/Pago registrado" leyendo N:N para pagos parciales. **Auditado pre-código con 3 agentes paralelos** (backend/UX/arquitectura) — 3 P0 reales aplicados (loop trigger, leak roadmap, native confirm). Smoke SQL verde. Docs gotchas §Conciliación añadido en commit `f871232` main. Smoke UI Playwright `tests/e2e/smoke/conciliacion-vinculacion.spec.ts` listo (Progreso 2026-05-22 00:35), pendiente ejecución headless con storageState.

- ~~**UX `/emitidas` — acción "Anular y emitir abono" desde menú 3-puntitos de la fila**~~ ✅ **HECHO 2026-05-21 21:00** (commit `270d110` en main, componente reutilizable `anular-confirm-modal.tsx`). Smoke UI pendiente post-Dokploy redeploy. Ver Progreso 21:00.

- ~~**Bug ruido — `GrandfatherBanner` llama `/api/profile/me` (404)**~~ ✅ **HECHO 2026-05-21** (commit `aa6b27b` en main). Refactor server-side: props `phoneVerifiedAt`+`isSuperadmin` desde layout-server (no fetch client).

- ~~**🔐 Rotar OpenAI key + fix typo Anthropic**~~ ✅ **HECHO 2026-05-21 19:30**. OpenAI rotada (Dokploy app + n8n credentials), Anthropic key nueva añadida en Dokploy nuevo (typo del viejo no aplicaba). Smoke `/admin/llm` con ambos providers verde. Desbloquea PR-A3.1. Ver Progreso 2026-05-21 19:30.

- ~~**Bug latente — `memberships_count` no llega a `v2-calc-totals` en path texto**~~ ✅ **HECHO 2026-05-22 00:35** (workflow `pqSWkDIHqmSVHotB` patcheado vía REST API, no commit en repo — todo el patch vive en n8n). Causa raíz confirmada: nodos jsCode `Resolver Chat State Texto` y `Resolver Chat State Voz` hacían `return [{ json: { ...parsed, org_id, org_nombre, ... } }]` con `parsed = $('Parsear Mensaje').first().json` (payload Meta crudo sin memberships_count). El campo vivía en `$('Buscar Org Texto').first().json` pero solo se extraía `org_id`/`org_nombre`. Fix: añadidos `memberships_count: org?.memberships_count || 1` + `candidate_orgs: org?.candidate_orgs || null` en los 3 return paths (early-guard, try-success, catch-error) de ambos nodos. Marker idempotente `// PR_BOT_MEMBERSHIPS_FIX`. Verificado en execution 746: `Resolver Chat State Texto` → `memberships_count: 2 ✓`, propaga hasta `Preparar Input Agente`. Ver Progreso 2026-05-22 00:35.

### Auditoría de seguridad 2026-06-04 (6 agentes + red team activo)
- **C1 (crítico) RESUELTO en prod** — RPCs SECURITY DEFINER ejecutables por anon/authenticated vía PostgREST con anon key público (bypass de pago `change_billing_status`, IDOR, DoS numeración, abuso OTP). Mig 213 REVOKE EXECUTE FROM PUBLIC/anon/authenticated en verbos admin-only. Validado desde fuera: anon→401, authenticated→403. Ver [[supabase-rpc-security-definer-execute-public]].
- **PR #131 mergeado + DESPLEGADO en prod** (SSRF render-pdf+webhooks via `src/lib/security/ssrf.ts`, security headers anti-clickjacking en `next.config.ts`, signout CSRF, voice/generate guard phone↔org, periodo/file.name/email-outbox/idempotency). Headers live verificados (`x-frame-options: DENY`, CSP frame-ancestors, HSTS, nosniff; `x-powered-by` eliminado).
- **PR #133 mig 217 — MEDIO cerrado** — `enqueue_outbox_event` revocado de authenticated (anti-spoofing webhook cross-tenant; tenía args → invocable por PostgREST). Aplicado a prod (vía SQL+schema_migrations porque el CLI no conecta al pooler), smoke anon→401.
- **Fuga RLS `billing_accounts` (multiempresa F1) DETECTADA Y RESUELTA en prod 2026-06-04** — la policy SELECT (mig 215) usaba `user_billing_account_ids()` (todas las orgs-membresía activas), exponiendo plan/`stripe_customer_id`/`payment_failed_*` de orgs que `organizations` oculta (RLS = `id=get_user_org_id`, solo la activa). Fix **mig 218** (`billing_accounts_rls_solo_org_activa`): la cuenta visible es solo la de la org activa, heredando la visibilidad de `organizations`. Validado contra prod con usuario real miembro de 2 orgs: **fuga=0**. Aplicado a prod + PR #134. Detectado al validar RLS antes del deploy del código F2.
- **Confirmado seguro (red team):** RLS multi-tenant sin fuga cross-org, superficie sin-auth (v1/admin/internal/voice bloquean), billing sin bypass, XSS escapado por React, escalada admin imposible para no-superadmin.
- **Config dashboard (acción user):** bucket `logos` listing off ✅ HECHO; leaked-password protection ⚠️ requiere Supabase Pro (plan FREE no lo permite) — mientras, min password length subido a 8 + requisitos fuertes ✅; cuenta E2E (`.env.test`) sin rol superadmin de plataforma (pendiente, higiene).

### Auditoría de seguridad 2026-06-19 (red team 4 agentes) — ✅ CERRADA en prod (main `31f134f1` + mig 336)
- **HIGH bypass de pago en `PATCH /api/settings/features`** — `source='manual'` activaba cualquier módulo de pago gratis (`org_has_feature` da precedencia al override sobre el plan; vector NUEVO, distinto del `change_billing_status` RPC de 06-04). Fix: enable solo si feature en plan efectivo **o** compra previa real (override `source='addon'`, que solo escribe el webhook Stripe); disable siempre; preservar `source='addon'`. +6 tests. Ver [[endpoint-toggle-feature-debe-gatear-enable-por-plan-o-compra]].
- **HIGH deps** — nodemailer ≤9.0.0 (SSRF/file-read) + undici 7.25 (bypass validación TLS que debilitaba el Agent anti-SSRF, **además sin declarar como dep de prod** pese a importarse en `dispatcher.ts`). Overrides a versiones parcheadas + undici declarado. `npm audit`=0. Ver [[import-runtime-dep-no-declarada-solo-transitiva]].
- **MED** — `/api/seed` destructivo desde sesión (borraba facturas de la org) → guard is_test/superadmin; **mig 336** `search_path` en `merge_*`/INVOKER + `logos_select` scopeado por org (advisors search_path/logos → 0).
- **Sano (4 agentes + verificación manual + review de composición):** RLS multi-tenant, API v1/webhooks (firma timing-safe, SSRF pin), XSS escapado, agregaciones fiscales. Advisor `*_security_definer_executable` = ruido (trigger fns no RPC-invocables; helpers RLS no se revocan), ver [[supabase-advisor-trigger-functions-definer-son-ruido]].
- **Residual (no explotable):** HIBP sigue requiriendo Supabase Pro (ver `Config dashboard` arriba); `extension_in_public` **wontfix** — verificado que `anon`/`authenticated` NO tienen `CREATE` en `public` → sin vector de shadowing.

### Migración crons → HMAC v2 (2026-06-05) — ✅ COMPLETA (2026-06-09)
Motivo: el legacy `x-service-key` es un secreto estático sin anti-replay; cerrarlo (`SIGNING_LEGACY_UNTIL`) requiere que NINGÚN caller lo use.
- **24 schedules Dokploy → `sign-call.sh` v2** (firma HMAC con node leyendo `/app/.env`; openssl NO está en node:alpine, node sí en /usr/local/bin). PRs #135/#141. 16+ validados (success en cron_runs), 0 errores. Bug corregido: `enrich-batch` firmaba `{}` → quitar body (endpoint firma sobre body vacío).
- **Drive Sync** movido de n8n (`TwDd1OkMqr3nhSso`, solo timer, **desactivado**) → cron Dokploy v2 + catalogado en `CRON_REGISTRY` con watchdog (antes no se monitorizaba). PR #149.
- **Catálogo reconciliado** (PR #147): añadidos fiscal-avisos, fiscal-recalcular-borrador, cron-watchdog, conciliacion-sugerencias-run (el panel solo veía 20/24). Duplicado `sugerencias-run` borrado en Dokploy. Filtros panel admin crons: PR #142.
- **WhatsApp Receptor v2 HMAC v2** (2026-06-09): 6 nodos migrados (`Hash Session`, `crear_cliente`, `crear_proveedor`, `Obtener Orgs Para Crear`, `Ejecutar Creacion Pendiente`, `Log Bot Error → Error Agente`). Smoke WA OK.
- **`SIGNING_LEGACY_UNTIL=2026-06-01T00:00:00Z`** (pasada) aplicado vía API Dokploy 2026-06-09. Legacy cerrado definitivamente. Deuda pendiente: Voice* inactivos (no bloquean). Ver [[dokploy-cron-docker-exec-no-hereda-env-de-app-env]].
- **⚠️ "COMPLETA" tenía huecos — auditoría 2026-07-03**: `agentic-gate-sweep` y `slack-dispatcher` nunca tuvieron schedule real en Dokploy (solo en `CRON_REGISTRY`, 0 runs, sin alerta posible — [[cron-health-desconocido-para-cron-sin-ningun-run]]); `verifactu-process` (fiscal, el más crítico) deshabilitado desde mayo. Los 3 arreglados + 3 schedules duplicados limpiados. Detalle en [[facturaia-historico-detallado]] / `Stack/incidents.md` 2026-07-03.

- ~~**OTP teléfono al onboarding WhatsApp**~~ — **HECHO 2026-05-16 19:55**. E2E verificado en prod, WhatsApp llega con wamid + cost 3¢, verify funcional. Ver `Progreso en vivo` y `Histórico de hitos`.
