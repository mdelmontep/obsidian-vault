---
title: facturaia histórico snapshot 2026-08-03
date: 2026-08-03
source: claude-code-session
tags: [facturaia, historico, snapshot]
---

Entradas retiradas del NOW del hub el 03-ago-2026, ya cerradas. Contexto completo aquí; el hub se queda con 1-2 líneas mirando hacia delante.

- 🟢 **Ticket 133 en prod: seleccionar partidas y copiar / mover / borrar / sumar (03-ago, #1491 `e3ae0a2f`)** — tope 200/lote, destino = otro capítulo u otro borrador emparentado, una facturada no se borra ni sale. **Decisión cerrada: el precio viaja CONGELADO**; se descartó el interruptor por `es_valido` (tocaría 1 línea de 59.232 y exige cerrar antes la clave del descuento). Rationale completo en `gotchas.md` §48. Ver [[el-parte-de-un-job-caido-no-es-evidencia-de-lo-que-dejo]] · [[un-segundo-git-fetch-pisa-fetch-head-y-auditas-el-arbol-equivocado]]

- 🟢 **El color de marca de la org, en prod (03-ago, #1499 `03261b09`)** — no sobrevivía a una recarga (`af-brand` se guardaba y nadie lo leía) y los textos seguían azules (`applyBrand` solo pisaba `--brand`). Afecta a **1 org real**. Restaurar de `localStorage` **abría fuga entre empresas**, así que lo siembra el servidor (`<AppearanceSeed>`); tokens de texto por contraste, cero cambio para las otras 13. **El smoke encontró lo que los 46 unitarios no veían**: `:root` perdía contra `:root[data-theme]`, el color llegaba al DOM y no pintaba. Ver [[localstorage-global-en-app-multitenant-filtra-entre-organizaciones]] · [[token-de-relleno-no-sirve-como-token-de-texto]] · [[style-inyectado-con-root-pierde-contra-root-data-theme]]

- 🟢 **El azul dejó de estar clavado, y con él cinco defectos que nadie miraba (03-ago, #1500 `eb384a85`)** — los 47 `rgba(61,123,245,…)` a mano de `globals.css` pasan a `color-mix` sobre `--brand`; **diez se quedan a propósito** (cashflow, calendario, chips: ahí el color dice QUÉ es cada cosa, no de quién). Además: `.secondary`+`tone="danger"` sin regla («Anular factura» no parecía destructivo), `--ease-back` usada y nunca declarada, `fadeIn` duplicada, y **tres fallos dentro de `prefers-reduced-motion`** (interruptor al revés por un `var(--x)` inexistente, barra del toast congelada al 100% mintiendo, y el mantener-pulsado sin su única señal). Ver [[lo-que-vive-dentro-de-prefers-reduced-motion-no-lo-mira-nadie]]

- 📗 **Referencia visual exportable → `docs/design/sistema-de-diseno.html`** (03-ago) — 21 secciones, tokens y material real de cada componente. 459 KB **con cero peticiones a la red**: doble clic y funciona offline. HTML y no PDF porque el PDF pierde tema, `hover`/`focus` y componentes vivos. En el repo, no aquí, para tocarla en el mismo PR que `tokens.css`. **Escribirla destapó los siete defectos del #1500.** Ver [[escribir-la-doc-de-exportacion-de-un-sistema-lo-audita-entero]]

- 🟢 **Las tres incidencias del panel, resueltas (03-ago, #1492)** — ninguna era lo que decía y **mis dos hipótesis del 02-ago sobre el OCR eran falsas**. **Los dos smokes pasados el mismo día**, el de cobros forzando el cron. **Cabo cerrado (#1495 · #1496)**: no era falta de caducidad sino de DUEÑO, y el guard destapó **8 emisores más** igual de huérfanos, nombrados y sin arreglar porque ninguno ha disparado nunca. Ver [[4xx-que-culpa-al-archivo-no-es-caida-del-pipeline]] · [[dominio-reservado-exento-de-la-captura-de-correo-lo-rechaza-el-proveedor]] · [[un-guard-enumera-la-clase-que-la-regla-escrita-solo-documenta]]

- 🟢 **Los avisos por email de los tickets funcionaban; el falso negativo era el buzón (03-ago)** — «no me llegan los emails cuando se crea un ticket» salió siendo la **bandeja categorizada de Apple Mail**: el correo estaba en la Entrada, en otra categoría, y la carpeta «Archivado» sobre Gmail/IMAP es en realidad «Todos los mensajes». La app estaba entera: 36 avisos `delivered` en 30 días, DKIM/SPF/DMARC correctos, y respuestas y cierres cuadrando 1:1 con los tickets. Verificado end-to-end con dos tickets de prueba en la org sandbox (#140/#141) recorriendo los cinco correos —nuevo, respuesta, resuelto y **descartado**, este último nunca ejecutado en prod desde que se escribió el 28-jul—. `info@agentesia.madrid` añadido a `new_ticket`/`user_reply` en `system_config` vía el endpoint admin (queda en `audit_log`). Único fallo real, ya pasado: 7 avisos `failed` el 29-jul porque `RESEND_API_KEY` no estaba en el contenedor y `pickProvider` cayó a SMTP, que tampoco tenía credenciales. Ver [[delivered-del-proveedor-no-es-visto-el-cliente-lo-categoriza]]

- 🟢 **Los avisos, donde se arreglan (03-ago, #1507 `54664d37`)** — lo que solo AVISA salió de la franja superior, que aparecía igual en el listado de obras, donde no se puede hacer nada con ello; vive ahora en la tarjeta «Termina de configurar tu empresa» del inicio y junto al documento. La franja se reserva a lo que BLOQUEA. **De paso, medido sobre el CSS de producción: seis cristales sin blur** porque escribir `-webkit-backdrop-filter` DETRÁS de la estándar hace que Lightning CSS **borre la estándar** — `.ai-btn` («Preguntar a IA», la firma visual de la casa) entre ellos, y uno dentro de `prefers-reduced-transparency` que dejaba el blur puesto justo a quien había pedido no tenerlo. Nada lo detectaba: ni test, ni lint, ni build, ni leer el fuente. Guard en `pre-commit`. El gate de cierre devolvió **6 bloqueantes reales** y 9 avisos que también eran defectos.

- 🟢 **Cabos de esa tanda, y tres gates que no vigilaban nada (03-ago, #1510 `2f1e0dfe`)** — el aviso llega ya a **los cinco caminos que emiten sin pasar por `/generar`** (convertir presupuesto, ticket rápido, ticket desde un movimiento del banco, facturar y certificar obra), con la pieza compartida en `components/pendientes/aviso-emision.tsx`. **El hallazgo gordo, silencioso**: el diálogo de `useConfirm()` se renderiza en el árbol del `ToastProvider` y el shell montaba `PendientesProvider` DEBAJO, así que ese aviso salía vacío **sin error** (el contexto devuelve lista vacía fuera del provider en vez de lanzar). Doble arreglo: subir el provider y permitir pasar los datos por prop. **Los tres gates**: la tarjeta del inicio se monta en 4 salidas y solo 1 estaba cubierta —borrarla del hub pasaba el gate entero—; el guard de cristal miraba solo el fuente y ahora mira también el compilado en `pre-push`; y la regresión visual comparaba **la pantalla de login contra la pantalla de login** desde que alguien corrió `--update-snapshots` contra un servidor donde `/design-system` redirige. Verificado con navegador en 2 de los 5 caminos, sin emitir. **Queda**: baselines visuales de linux viejos, hace falta el container oficial (esta máquina no tiene Docker) — declarado en `docs/design/13-gobernanza.md §5`. Ver [[elemento-pasado-como-prop-lee-el-contexto-de-donde-se-renderiza]] · [[baseline-de-screenshot-capturado-de-la-pagina-equivocada-es-verde-para-siempre]]

## Smokes cerrados, retirados del hub el 08-ago

- ✅ **[03-ago, #1514] Smoke completo VÁLIDO: 128 pass / 0 fail** (build + start en :3002, servidor vivo al terminar). La tanda previa de 9 fallos no valía (servidor caído con `next dev` y sesiones compitiendo); 7 eran lentitud y 2 del arnés, no de la app: [[getbyrole-casa-el-nombre-accesible-por-subcadena]] y [[fixture-de-bytes-fijos-contra-deduplicacion-por-contenido-es-un-test-de-un-solo-uso]] (este SIN arreglar: hay que variar el contenido, no el nombre).
- ✅ **[30-jul] El rescate del timeout, VALIDADO en prod sin provocarlo** — el ticket `4bbf7118` agotó los 30 min y el runner dejó su trabajo en el draft #1368 (`rescate/…`, prueba de que la imagen nueva estaba dentro). Los 3 PRs resueltos el 31-jul: #1370 y #1371 mergeados, #1368 cerrado. Ver [[el-nombre-de-la-variable-no-es-su-procedencia]].
- ✅ **Smokes de prod verificados por Manu y archivados (27 y 29-jul)** — runner, y el resto de la tanda.
- ✅ **Módulo SEPA — SMOKE COMPLETO (2026-07-25)** — verificado en la app: modal, los 6 campos de Configuración y preview del concepto.
- ✅ **Rendimiento Ola 1-3 (BD+Realtime) — CERRADO en prod con smokes (2026-07-21/22, migs 539-544)** — índices FK Obras (DELETE 36s→ms), `get_org_us…` y el resto de la ola.

- 🟢 **La unidad del tiempo de MO: EN PROD y mergeada (07-ago, mig 656, PR #1531)** — f = **1,42114** (R²=0,9999998), uno solo, despejado por org de las 44 filas que declaran sus horas en el nombre. **Ningún precio se movió.** Verificado en prod: 44/44 filas valen lo que dicen en las tres orgs, `precio_hora_mo` de IET 34 → 48,3185901, QA Multiempresa intacta; y conduciendo el navegador, el material del tipo `TIEMPO 4 HORAS` muestra **4 h** donde antes ponía 5,68. Los 7 en singular conservan su +4,98 % hasta que Natalia conteste. **Abortó DOS veces antes de entrar, y las dos las cazó su propio assert con rollback completo**: (1) el trigger de la mig 491 no divide, PISA, y resincronizaba los 1.329 materiales desincronizados de la sandbox → 188 precios movidos; (2) la partida con 1000 «horas» que son euros amplificaba el redondeo de `NUMERIC(12,4)` → 3 céntimos. → [[un-trigger-que-pisa-en-vez-de-calcular-resincroniza-al-migrar]] · [[una-fila-basura-amplifica-el-error-de-redondeo-de-toda-la-tabla]] · [[alter-column-type-choca-con-cualquier-trigger-update-of]]
