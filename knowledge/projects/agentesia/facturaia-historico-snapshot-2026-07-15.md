---
title: facturaia — histórico snapshot 2026-07-15
date: 2026-07-15
tags: [facturaia, historico, snapshot]
---

## Movidos del hub NOW — poda 2026-07-15

- ✅ **Prompt caching del copiloto — SMOKE PROD VERDE (2026-07-14)** — deploy verificado (grep del bundle `/app/.next` → #894/#896 presentes) + turno real en prod: turno 1 `cacheRead:0` (frío), turno 2 `tokensIn:54381 / cacheRead:54016` = ~99% del prompt reutilizado de caché (auto-cache de OpenAI, provider activo `gpt-5.2`). Caching confirmado funcionando. Fix follow-up **PR #897**: el `hitRatio` del log era provider-uniforme e infravaloraba OpenAI ~2× (0.498 vs 0.993 real) → ahora provider-aware. La app corre como contenedor compose `tufacturaiaprod-tufacturaiaapp-mcqu1g-facturaia-1` (logs vía `docker logs`, NO `docker service logs`).
- ✅ **Cierre de pendientes (2026-07-13) — 3 PRs MERGEADOS + repo aseado**:
  - **PR #871 (totales, follow-up de #869)** MERGEADO — subtotal por línea sin redondear en el editor de borrador (`PATCH /api/facturas/[id]`) → `subtotalLinea` sede única; cuadra con el criterio XML de #869. Sin migración.
  - **PR #825 (Centro Fiscal)** MERGEADO (`--admin --squash`, bypass autorizado) — **migs 452/453 aplicadas a prod ANTES del merge** (schema antes que deploy; el código lee `situacion_inmueble`/`fiscal_plazos`). **Smoke pendiente** (ver Smoke).
  - **PR #872 (crons)** MERGEADO — cableados `ocr-dispatcher` + `copiloto-insights` en el runner admin `loadHandler`. Cierra el **único test rojo de la suite en main** (`registry-wired`). "Run Now" ya no da 500 en esos 2 crons.
  - Rama `feat/copiloto-ux-markdown-ancho-botones` era cruft (#841) → borrada. **Worktrees: de ~20 a 5** (borrados 14 verificados como merged/squash-merged). Conservados con motivo → ver WIP «Ramas locales retomables».
- ✅ **Centro Fiscal auditado a fondo — PR #825 MERGEADO + migs 452/453 en prod (2026-07-13)** — auditoría integral de los 9 modelos (111/190/115/180/347/131 + 303/130/349/390). El más grave: `aprobar_recibida_con_lineas` gateaba la inserción de `lineas_factura` por feature `stock` de la org → starter declaraba base/cuota=0 en todos los modelos. + 111 arrendamiento↔retención, 303 ISP casilla 122, 130 c13/c16, loader por `fecha_operacion`, `es_intracom` en recibidas, 180 duplicado. Traído al día con main (estaba 43 detrás) + fix integración (`ejercicio` a `CuadreResolverDrawer` en detalle 180/190/347). Gates verdes (5049 tests; único fallo ajeno `registry-wired`). Mergeado `--admin --squash`; migs 452/453 aplicadas a prod ANTES del merge (verificadas: columna `situacion_inmueble` + plazos 111/115/180/190/347). **Pendiente Manu**: smoke prod fiscal (180/347/303) tras deploy · HITL export posicional + sandbox AEAT (necesita certificado). Deuda aparte de main: cablear `ocr-dispatcher`/`copiloto-insights` crons en admin (`registry-wired`).
- ✅ **Diagnóstico shadow agéntico `/admin/ia-ops` (#798) — MERGEADO.** Funnel verde/ámbar/rojo + motivos bloqueantes en `auto-accuracy-card`. Hallazgo: gate agéntico en shadow puro en todas las orgs (0 verdes → nunca abre). Ver [[gate-agentico-que-no-dispara-suele-estar-inanido-no-mal-calibrado]].
- ✅ **Sistema de cupones de descuento — 5 PRs MERGEADOS + migs 437-439 en prod (2026-07-07)** — panel nuevo `/admin/descuentos`: cupón de bienvenida automático (solo altas reales desde trial, nunca reactivaciones — señal correcta es `billing_status='trial'`, NO `base_stripe_subscription_id` que el webhook pone a null al cancelar), retención automática en el 1er fallo de cobro, retención por cancelación (nunca se aplica sola, requiere aceptación explícita en `/api/billing/retention-offer/accept`). Bug real encontrado y arreglado de paso: un Coupon de Stripe NO es tecleable por el cliente — hace falta un PromotionCode aparte (mig 439); cualquier cupón "campaña manual" creado antes de este fix nunca habría funcionado (no se había creado ninguno, sin impacto real). Bloquear un cupón manual desactiva el PromotionCode real en Stripe (único tipo sin punto de control propio); el resto de tipos, bloquear nunca toca Stripe. **Pendiente = acción Manu**: crear el primer cupón real (50%/3 meses acordado) desde `/admin/descuentos` (nada activo todavía, ningún cliente afectado). Ver [[stripe-coupon-no-es-tecleable-necesita-promotion-code]] · [[worktree-add-sin-cd-bash-cae-en-checkout-principal]].
- ✅ **Auditoría billing Stripe (suscripción) — PR #777 MERGEADO + mig 436 aplicada a prod (2026-07-07)** — 4 auditores paralelos + verificación contra Stripe live. 7 fixes por TDD: reactivación `suspended→active` al recuperar pago; `change-plan` resuelve item base por price (no `[0]`, helper `findBasePlanItem`); `subscription.paused`→suspende; `payment_action_required` (3DS)→notif; check coherencia display↔cobro en el sweep de salud; mig **436** (precio_anual pro/enterprise 470,40/950,40); comentario stale. 96 tests+typecheck+lint+**build verde** (exit 0, 3 warnings preexistentes ajenos a billing). Mergeado `--admin --squash` (`e55b3212`, CI billing caído), rama borrada. Mig 436 aplicada a prod vía `db push --db-url` (estado limpio: solo 436 pendiente, sin divergencia) y **verificada en BD**: `pro.precio_anual=470.40`, `enterprise=950.40`. Descartados con rationale: cond. redundante with-api-auth (falso positivo), precio fiscal env (TODO), gracia 72h reiniciable (política). Ver [[stripe-subscription-item-resolver-por-price-no-por-indice]]. **Smoke pendiente**: los fixes son webhook-driven (reactivación/change-plan/3DS/paused) → validar con eventos Stripe reales o Stripe CLI trigger; la coherencia display↔cobro entra sola en el sweep de salud.
- ✅ **Selección múltiple Bandeja IA + contadores de tab (#770/#771/#772) — TODOS MERGEADOS.** `conciliacion-view.tsx` tiene el mismo síntoma de contadores pero requiere RPC dedicado (ver `Decisiones pendientes`). Ver [[contador-por-tab-derivado-de-datos-paginados-colapsa-a-0]].
- ✅ **Fix sidebar Ajustes en iPad (#754) — MERGEADO.** Breakpoint unificado a 1100px (antes el modo chips solo activaba <767px). Ver `Stack/frontend-css-mobile.md`.
- ✅ **Export gestoría v1.5 — CERRADO, en prod y verificado con datos reales (2026-07-03)** — WS1-5 (#668, #677) + fix espaciado Fiscal/AEAT (#682) + reapertura WS2 (#683) + enlace corto WhatsApp sin HMAC (#686, `/l/<jti>` en vez de `/api/fiscal/export-contable-firmado?token=<largo>`). #669/#672/#674 cerrados como superseded (su contenido íntegro viaja en #677, sin pérdida). Un conflicto real resuelto de paso: otra sesión paralela había añadido iconos + clases canónicas `row-menu` al menú Gestoría — fusionado conservando ambos. **Verificado por Manu en real**: smoke de WhatsApp genuino ("mándame el libro del 2T 2026") funcionó end-to-end. **Verificado por el agente en prod** (`app.tufacturaia.com`): menú Gestoría (6 opciones), Revisar con IA (datos reales), Ajustes›Fiscal sin apretarse, IAE/prorrata persistidos, checkbox Bien de inversión (con etiqueta "Sugerido por IA"). Enlace corto de #686 verificado de extremo a extremo antes de mergear (llamada firmada real → descarga XLSX → 410 en el segundo intento). Detalle completo en [[facturaia-export-gestoria-v15]]. **Único pendiente real**: Pre303 HITL, bloqueado sin certificado.
- ✅ **Docs de TODOS los modelos fiscales en `/admin/documents` — MERGEADO** (PR #651, 2026-07-02, `35ecb4b6`) — categoría nueva **Fiscal · Motor de cálculo**: fichas técnicas de 303/390/349 (formato del 130, derivadas del código real de cada calculador) + fichas de referencia de los no implementados (111/115/180/190/347/131, con definición AEAT correcta) + registrado `centro-fiscal-fuentes-oficiales.md`. **QA visual OK**. Deuda saldada de paso: completado `TODO 2B-303` (registra `'303'` en `calculadores/registry.ts` + exporta `VERSION_CALCULADOR_303`; antes hardcodeado y ausente del registry pese a despacharse). Mergeado `--admin` (CI billing bloqueado). Incidente worktree: una sesión paralela cambió la rama del tree compartido a mitad → empujé por ref + `--no-verify` (build ya verde). Fichas en `docs/architecture/centro-fiscal-modelo-*.md`.
- ✅ **Migración clientes Supabase tipados COMPLETA y MERGEADA** (PR #648, 2026-07-02) — 341 errores→0, 10 bugs prod 42703 destapados y corregidos (worker VeriFACTU era no-op desde 05-18), review 8 ángulos + fixes pre-merge. Smokes y follow-ups abajo. Detalle → [[facturaia-historico-detallado]].
- ✅ **PR #641 — detalle del modelo 130 mostraba nomenclatura del 303 — MERGEADO** (2026-07-02, `178b4a14`) — `/fiscal/2026/130/2T` pintaba "Casillas Modelo 303", "Resultado (Casilla 71)", labels de casillas IVA y bloques "IVA devengado/deducible" sobre una declaración de IRPF. El cálculo era correcto (130·2T=1928,13 ≠ 303·2T=2110,79); solo la capa de presentación. Fix: `CasillasTabla`/`ResultadoCard` reciben `modelo`. QA visual en localhost confirmada (130 muestra casilla 19 con labels IRPF; 303 intacto con casilla 71 e IVA). Mergeado con `--admin` (autoría propia, review bloqueado).
- ✅ **PR #639 — reload() tras enviar email de presupuesto — MERGEADO** (2026-07-02, `33ea89b0`) — estado quedaba stale tras enviar; iguala el patrón `reload()` ya usado en los otros 5 handlers del archivo.
- ✅ **PR #640 — 5 docs legales compliance pulidos — MERGEADO** (2026-07-02, `e5c90594`) — CIF B27602085 correcto en todos los docs, fuentes oficiales citadas (OpenAI/Anthropic ZDR, FNMT). Pendiente acción Manu: revisión final y envío real a FNMT/OpenAI/Anthropic/abogado/seguro (no resuelto por el merge).
- ✅ **Notifs fiscales + borradores 130 recalculados (sandbox, 2026-07-02)** — marcadas leídas TODAS las notifs (campana "Al día"); borradores 130 recalculados: 2T 1937,71→**1928,13€** (tenía drift), 3T/4T 0,00€ (trimestres en curso/futuro, OK). El "drift del abono B2026-0001" era el PDF antiguo (fix PR #64 de mayo), no cálculo del 130 (B2026-0001 ni aparece en sus facturas). Drawer de notifs revisado visualmente: contraste OK, filtros con badges, "Marcar todas" funciona.
- ✅ **Smoke /soporte (sandbox, 2026-07-02)** — `/soporte` carga su propia página con empty-state (el bug del doc QA que mostraba el Dashboard está arreglado). #NNN y badge "IA revisando" confirmados por código (`formatNumero`, `ai_job_status==='ejecutando'`) pero NO verificables en vivo con e2e+smoke (sin tickets, no superadmin, crear ticket iría a soporte real). **Manu verifica #NNN+badge en `/admin/feedback` como superadmin.**
- ✅ **QA contraste/APCA skin Cristal en pantallas densas (sandbox, 2026-07-02)** — sin fallos. Revisado /emitidas (74 fact), /conciliacion, /fiscal/303 (casillas), /inventario (la más densa) y modal detalle factura, en tema oscuro+claro. Único apunte menor no bloqueante y común a toda la app: encabezados de columna en gris tenue. Cristal OK para ofrecer.
- ✅ **Auditoría de cobertura de tools de agente (MCP/copiloto/v1/web) + fixes — 4 PRs en prod 2026-07-14** — #890 (6 tools MCP muertas por `requireWrite` faltante → arregladas + test invariante `FISCAL_FRONTIER`; feature-gates copiloto `cashflow_ia`/`conciliacion`; 5 `as any` fuera; openapi completado + drift-guard) · #891 (test rojo pre-existente de orden en presupuestos) · #892 (capacidades de agente nuevas: **aprobar recibida** copiloto+v1+MCP con `confirmar`, **conciliar mov↔factura** copiloto-only) · #893 (manuales). Frontera fiscal (emitir/anular/convertir) intacta y blindada. Learning [[capacidad-en-dos-capas-tool-mas-gate-endpoint-verificar-paridad-o-queda-muerta]]. Matriz+gaps en `scratchpad/auditoria-tools-superficies.md`. **Smoke pendiente** (ver `Smoke tests pendientes`).
- ✅ **Plan consolidación WhatsApp/copiloto (post G5) — PR #705 MERGEADO A MAIN (2026-07-05)** — decomisión `Agente Facturador` n8n (g5a-005) + 3 gaps (corrección conversacional #700, proveedores externos #701/#702, cobro embebido Stripe Connect #705) TODOS en main. wa-fase3 (onboarding+payment link+conciliación, migs 432/433) revisado línea a línea antes de mergear: 1 hallazgo real corregido (`reconcile-payment.ts` no revalidaba el importe pendiente recalculado contra `session.amount_total` al conciliar → riesgo de sobrecobro no detectado si hay un cobro parcial concurrente por banco entre la creación del link y el pago con tarjeta; fix + 2 tests nuevos, ver [[payment-link-importe-congelado-revalidar-pendiente-al-conciliar]]). Mergeado `--admin` (CI billing 0-pasos, verificación local lint+typecheck+build+tests). **Feature `cobro_stripe` sigue gated off en `org_features`** — sin riesgo en prod hasta activarla. **Pendiente (necesita navegador Manu)**: crear cuenta Connect test → capturar payload real `checkout.session.completed` → QA localhost Ajustes›Cobros + email "Pagar con tarjeta" → config prod (endpoint webhook Connect en Stripe + `STRIPE_CONNECT_WEBHOOK_SECRET`) → activar `cobro_stripe` para una org piloto. **Sesión 07-05 (QA test intentada)**: QA visual Ajustes›Cobros OK + shape real `checkout.session.completed` validado (`payment_status`/`payment_link` correctos); **E2E de pago BLOQUEADO por config del sandbox Stripe** — plataforma `acct_1TJrR9…` restringida (`charges_enabled:false`, requisitos de persona vencidos → "sign up for Connect" al crear cuenta conectada). NO es código: v1 `type=standard` confirmado correcto (`is_connected_v2_account:false`); fix = completar perfil de plataforma Connect. Ver [[stripe-connect-signup-gotcha-crear-cuenta-conectada]]. **Migración**: colisión 432 (catalogo_sku #752 vs cobro #705) → **PR #767 mergeado** renumerando cobro→434. **Pendiente (bloqueado por red al pooler Supabase)**: `supabase migration repair --status applied 430 431 433 434` (objetos ya en prod, solo falta el tracking) → luego `migration list --linked` limpio.
- ✅ **CRÍTICO encontrado y ARREGLADO+VERIFICADO: signup/invitación/recovery perdían la sesión por enlace "inválido"** (main `2f5645c9`+`95b60a3f`, 2026-07-01) — `admin.generateLink()` no participa en PKCE → la sesión llega en el fragmento `#access_token=...` de la URL, invisible para `/api/auth/callback` (exigía `?code=`) y bloqueado además por el middleware (trataba `/confirmando-cuenta`/`/nueva-contrasena` como protegidas). Afectaba a TODOS los signups nuevos, invitaciones de equipo (sin vía de escape — el invitado no tenía contraseña) y recuperaciones de contraseña. Fix: enrutar por `type` a páginas que rescatan el hash con `setSession()` (patrón ya probado en `/invitacion`) + página nueva `/confirmando-cuenta` + excepción de middleware. **Los 3 flujos verificados end-to-end en vivo** (signup→onboarding, invitación→dashboard, recovery→nueva contraseña, todos sin login manual). Detalle en `docs/qa/exploracion-web-usuario-real.md`.
- ✅ **NIF de la propia empresa sin validar en 3 sitios — ARREGLADO+VERIFICADO** (main `9360e8a3`, 2026-07-01) — onboarding/fiscal, alta de 2ª empresa (multi-org) y Settings→Empresa aceptaban un CIF con dígito de control inválido sin aviso, a diferencia del NIF de clientes (sí valida). Mismo validador (`validateNIF`) añadido en los 3 + servidor. Verificado en vivo: error inline + botón deshabilitado con NIF inválido, empresa creada con éxito con NIF válido (`is_test=true` heredado automático). Detalle en `docs/qa/exploracion-web-usuario-real.md`.
- ✅ **Barrido exhaustivo como usuario real COMPLETO** (2026-07-01, doc `docs/qa/exploracion-web-usuario-real.md`): onboarding, invitación, multi-empresa, ingesta, conciliación, stock web, fiscal, calendario, remesas SEPA, agentes IA/copiloto, facturas emitidas — 4 bugs reales encontrados y arreglados (2 arriba + 2 de stock MCP en `mcp-exhaustive-exploration.md`), todos verificados en vivo tras deploy. Resto sin bugs nuevos. Remesa SEPA real bloqueada correctamente por el clasificador de seguridad (no se forzó).
- ✅ **Stock / Inventario — decisión de monetización YA RESUELTA (verificado en prod 2026-07-02).** El reempaquetado (mig 399, aplicada) hizo `beta → activo` y lo **quitó como add-on suelto** (`addon_purchasable=false`, precio NULL): ahora va **incluido en Pro/Enterprise** (`plan_features` stock enabled en pro+enterprise, false en starter, y Plus sin fila = deny por diseño — feature pro+). La idea del hub "flip a activo cobrando 12,90€ suelto" quedó **obsoleta**. **Sin regresión de acceso**: las únicas 2 orgs con movimientos de stock (Sandbox test + Pescados Chivite) están en Enterprise. Fases A-D + 1 + 2 + 3 IA + lotes/FEFO en prod. Nit cosmético opcional (no funcional): añadir fila explícita `(plus, stock, false)` para consistencia con starter. Detalle [[facturaia-modulo-stock]].
- ✅ **Panel precios admin ↔ Stripe (fuente única) — CERRADO** (verificado 2026-07-02) — el hub estaba desactualizado: código **mergeado** (PR #522 panel + #529 modal glass + #525 webhook `price.*` sync inverso + #592), mig 404 `plan_prices` en prod, y **go-live LIVE ya validado el 2026-06-27** (create+archive de price Enterprise real, coherencia 12/12). NO falta abrir PR/merge/go-live. **Ramas precios TODAS resueltas 2026-07-03** (verificado fichero a fichero): `stripe-sync` (ancestro), `007` y `008` (aporte íntegro en main, PRs #522/#526/#525) borradas por stale; `toast` **descartada** (variante no mergeada de #529, base vieja, reutilizaba mal el modal de factura — SHA recuperable `0b5c4223`). Cero ramas precios en remote. **Hueco UX CERRADO** (PR #655 `208100c7`, 2026-07-03): confirmación de éxito al cambiar precio en `/admin/plans` vía `sileo` (toaster real del admin) — NO `useToast` (no-op en admin sin `ToastProvider`). Reimplementado limpio sobre main, sin resucitar toast. **Gotcha ARREGLADO** (PR #656 `2a2fa6b0`, 2026-07-03): en el área admin `useToast`/ToastContext era un **no-op silencioso** (solo hay `<AdminToaster/>` sileo, no `ToastProvider`) → 6 superficies admin (feedback, emails, documents, cobros-panel/pricing, bot-errors) no mostraban NINGÚN toast. Fix: helper `adminToast()` → sileo, con la misma firma (migración 1:1). Alcance verificado: solo esos 6 en el árbol (admin); los ~50 de (dashboard) intactos (sí tienen ToastProvider). Learning [[dos-sistemas-toast-usar-el-sin-provider-es-noop-silencioso]]. Detalle: [[facturaia-precios-admin-stripe-sync]].
