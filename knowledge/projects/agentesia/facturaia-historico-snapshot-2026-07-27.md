---
title: facturaia histórico snapshot 2026-07-27
date: 2026-07-27
tags: [cliente, facturaia, historico]
---

# TuFacturaIA — histórico, snapshot 2026-07-27

Extraído del NOW del hub en el `/obsidian-1` del 27-jul: el hub pasaba de 150 KB y su NOW pesaba 42 KB con 37 entradas. Aquí va el detalle íntegro de las 12 entradas ya cerradas; en el hub queda una línea por cada una con su pendiente si lo tenía.

- ✅ **Gate de cierre del 26-jul: 4 defectos (3 los introdujo esa sesión) — MERGEADO Y EN PRODUCCIÓN (27-jul, PR #1234 `b5da7d85` + mig 571 aplicada)**. (1) El cron borraba cada noche los cuadres de todo **130** con gastos: el universo unificado declaraba recibidas y el snapshot del 130 solo guarda emitidas — 6 declaraciones de 3 orgs la primera noche. (2) Un **130 no se podía marcar presentado** con un solo gasto aprobado (mig 571: se exige un tipo de factura solo si el snapshot lo representa). (3) **"Volver a abrir y recalcular" devolvía 400 siempre** → el remedio de fiscal-002 nunca llegó al usuario. (4) El health-sweep daba por recuperadas las alertas de cuota que acababa de omitir y las reemailaba cada hora. **Verificado en prod tras aplicar**: el predicado viejo del recheck bloqueaba hasta 180 facturas en 6 de 6 declaraciones del 130 y el nuevo bloquea 0; el 303 conserva su guard idéntico (2=2, 12=12); el único 303 que cae en el hueco documentado (snapshot sin recibidas) es de la sandbox y **sigue bloqueado por `requires_recalc`**, o sea exposición real cero. Sonda no mutante contra `app.tufacturaia.com`: `POST` sin body ya llega al handler (`action_requerida` en vez de `Invalid JSON`) → el deploy está vivo. Migraciones **0 pendientes / 0 huérfanas**. **Falta solo la confirmación automática**: el run del cron de deriva de las 04:15 debe salir con los 130 en `sin_cambios` (no se disparó a mano a propósito: notifica a usuarios reales). Y queda un hallazgo nuevo: las RPC fiscales abren con `SELECT … FOR UPDATE` **sin `lock_timeout`**, así que con contención en `fiscal_declaraciones` (medido en `pg_locks`) el botón se cuelga 125 s en vez de fallar rápido. Ver [[el-universo-comparable-es-lo-que-se-persiste-no-lo-que-se-carga]] · [[mockear-el-wrapper-de-auth-esconde-el-400-que-ve-el-usuario]] · [[lo-que-un-barrido-omite-no-puede-darse-por-recuperado]]

- ✅ **Remates del cierre — TODO MERGEADO Y EN PRODUCCIÓN (27-jul, PRs #1240 y #1243 + migs 572/573)**. Salen de verificar el cierre anterior, no de suponer. (1) **"Marcar cuadrada" y "Marcar presentada" tardaban 125 s en fallar** cuando el hash estaba obsoleto, que es el caso para el que existe el guard. **NO era un lock**: son las dos únicas RPC que lanzan `ERRCODE 40001` (`serialization_failure`), y PostgREST **reintenta** ese código porque significa "conflicto transitorio"; como el fallo es determinista, reintentaba hasta cortar el gateway. Las otras dos RPC, que no usan 40001, respondían en 73 ms. Pasadas a `P0001` conservando el texto del mensaje → **125.135 ms → 145 ms**, medido en prod. La mig 572 (`lock_timeout=3s`) se queda como red de seguridad, pero su hipótesis era errónea y así está escrito en la 573. (2) **Un documento sin casilla dejaba la declaración sin salida**: recalcular devolvía `INTERNAL: FISCAL_GAP: iva_0_emitida_sin_rectificativa_2026`. Ahora es **422 `hueco_fiscal`** con mensaje que NOMBRA la factura ("La factura A-E2EF2-… tiene una línea al 0% de IVA…"), verificado en prod. Los errores de estado (`hash_mismatch`, `snapshot_outdated`) también dejan de salir crudos. (3) El **test que faltaba** del arreglo del sweep (cubría el helper, no el bucle) y los **manuales**: la sección del incidente partía la tabla del modelo de datos en manual-admin, y "Volver a abrir una declaración" no estaba documentada pese a que el texto de recibidas ya remitía a ella. (4) `issues/fiscal-004` con la medición: el aborto solo lo disparan las EMITIDAS al 0% sin exención y hoy existe **una** en toda la base (la que dejó un run E2E en la sandbox); las otras 33 del patrón son recibidas y calculan bien. La validación del 0% está duplicada en 2 endpoints y **ausente de `createDocument`**. **Cerrado también el residuo (27-jul, PR #1246)**: la emitida que dejó un run E2E en la sandbox (1.000 € al 0% sin líneas) bloqueaba el cálculo del 303 2T entero. Retirada con los guards de la mig 568 más dos propios por ser emitida (sin cadena VeriFactu, org de pruebas), validando antes la transacción con ROLLBACK. Luego se recalculó **por la app**: el 303 2T pasa de 0 cuadres y snapshot 59 a **4 cuadres (0 críticos) y snapshot 127** —ahora entran las recibidas—, y los tres 130 más el resto de 303 quedan recalculados sin banderas. En toda la base: 0 emitidas que aborten el cálculo, 0 facturas E2E vivas, 0 asignaciones ni snapshots huérfanos. **Con esto el pendiente 3 del 303 2T queda cerrado.** Sigue abierto lo que lo causó: los E2E emiten en la misma org y periodo con que se prueba el centro fiscal y no limpian (`issues/fiscal-004` punto 4). Ver [[error-determinista-con-sqlstate-40001-provoca-reintentos-infinitos]]

- 🟢 **Vigilante de uptime EXTERNO — HECHO y PROBADO (27-jul)**. Workflow n8n `XmZtBwIoScp3lnHa` ("FacturaIA — Vigilante de uptime EXTERNO"), activo, cada 5 min: `GET /api/health` y aviso por DM de Slack al **segundo** fallo seguido, con recordatorio cada hora y aviso de recuperación. Detecta los tres modos del incidente: HTTP != 200 (el 404 de Traefik), `status != ok`, y Supabase por encima de 3 s (el precursor que llegó a 11,5 s). Vive en n8n, que aguantó toda la caída porque no depende de esa base — al contrario que `cron-watchdog`, `health-sweep` y `emitSystemAlert`. **Probado de verdad** apuntándolo a una URL que da 404 con trigger de 1 min: avisó al segundo fallo (23:31 del 26-jul) y luego el aviso de recuperación al devolverlo a `/api/health`. El JSON versionado en `ops/n8n/uptime-watchdog-externo.json`; el token del bot vive en una credencial Header Auth de n8n, no en el JSON. **NO sustituye a averiguar por qué NotCaído no avisó** (sigue pendiente, ver punto siguiente).

- ✅ **Lote conciliación + fiscal-001/002 + ingesta-001 — CERRADO en prod (26-jul)**. Las 5 quejas de `/conciliacion`, el 303 sin las recibidas (136.503,94 € de base fuera), la deriva fiscal y los duplicados de recibidas. 12 PRs (#1203→#1232), migs 553-569 aplicadas, **0 pendientes / 0 huérfanas**. Cifras corregidas respecto a lo que dejé escrito el 25-jul: de los 62 "nums duplicados", **17 eran proveedores DISTINTOS con el mismo número** (no son duplicados), y el daño no eran 4,38 € del doble pago bancario sino **532,20 € de IVA soportado deducido dos veces** (7.393,88 € de base, 3 orgs) → 17 copias eliminadas y **0 grupos duplicados en orgs de cliente**; medido de nuevo el 27-jul, en la **sandbox** `b5f86e8f` quedan 24 grupos: 19 son recibidas a **0,00 €** (el OCR guardó el nombre del escaneo como número y no leyó importe — no descuadran nada pero ensucian el libro), 2 son duplicados reales con el mismo importe y año, y 3 son los ambiguos que se dejaron a propósito. Detalle y aprendizajes → [[facturaia-historico-detallado]] · issues `fiscal-001/002`, `ingesta-001`.

- ✅ **Retención del historial del Copiloto (`modia-009`) — CERRADA END-TO-END EN PROD (27-jul)**. Tres piezas, las tres verificadas: (1) **mig 562 aplicada**, `copiloto_tool_calls.message_id` y `copiloto_feedback.message_id` pasan de `CASCADE` a `SET NULL` — la poda ya no se lleva la auditoría de las acciones que mueven dinero; (2) **bug cazado al dar de alta el cron, no al escribirlo**: usaba UNA ventana y habría borrado los mensajes de **WhatsApp a las 2 h**, anulando los 30 días del canal (los hilos de canal y los web viven en las mismas tablas). Arreglado con dos pasadas + el `NULL` de `titulo` contemplado; nunca corrió, así que no hubo pérdida; (3) **schedule dado de alta en Dokploy** (`GNHrCHAibKUdgXt2-Xo9C`, `25 * * * *`, 42 schedules en el stack) y **smoke real pasado**: `cron_runs` con `triggered_by: dokploy`, 211 ms, `deleted: 0` — coincide con la medición en seco previa (hoy **0 orgs** con el toggle apagado, así que el cron es un no-op hasta que alguien lo apague). El registry ya casaba `25 * * * *`, así que con el primer run el `max_intervalo` de 2 h 10 min queda armado y el `cron-watchdog` sí avisaría; antes estaba en `desconocido` y no habría avisado de nada. Ver [[fk-cascade-desde-tabla-de-auditoria-la-poda-borra-la-prueba]] · [[retencion-en-tabla-compartida-por-dos-superficies-una-ventana-borra-la-otra]] · [[cron-health-desconocido-para-cron-sin-ningun-run]] · [[dokploy-api-schedule-runmanually-trigger-cron-on-demand]].

- 🟢 **Módulo Obras (mini-ERP instalaciones eléctricas/telecom, sustituye a WAPI) — EN PRODUCCIÓN, Fases núcleo→4 completas (última: FASE 4 MERGEADA 2026-07-19, PR #1008)** — catálogo/precios, presupuestos→obra, pedidos/albaranes/stock, partes MO, facturación/certificación atómica, IA+copiloto+MCP, Herramientas. Detalle completo de las 4 fases + auditorías `/fia-cierre` + migs 471-518 → [[facturaia-historico-snapshot-2026-07-23]]. Ver [[rpc-returns-table-subconjunto-tras-add-column-cast-miente]] · [[importar-csv-erp-espanol-encoding-cp1252-y-precio-por-unidad-multiple]] · [[postgres-revoke-public-no-elimina-grants-individuales]] · [[mock-de-createdocument-oculta-violaciones-de-check-del-nucleo-fiscal]] · [[consumir-tope-y-emitir-documento-reservar-emitir-confirmar]].

- 🟢 **VeriFactu auditoría A-Z + 10/10 blockers + Fase 5 — PR #947 MERGEADO a prod (`4a13c3db`) + mig 465 aplicada (`supabase db push`), 2026-07-16** — cruce multi-agente contra docs oficiales AEAT destapó que fallaba el 100% de envíos. Corregido y verificado: **huella** (mig **465**: faltaba `&FechaHoraHusoGenRegistro=`, timestamp único en `verifactu_fecha_gen`, hex MAYÚSCULAS → reproduce el vector oficial firmado) · **XML/SOAP** (namespaces NS_LR/NS_SI, orden desglose, `IdSistemaInformatico`=**TF** → validado con xmllint) · **transporte** (mTLS nativo `node:https`, parser real `EstadoEnvio`/`EstadoRegistro`, host prod, sin firma en VERI*FACTU) · **QR** (al principio, «QR tributario:», 30mm, número bajo el logo → render Puppeteer de las 5 plantillas) · **coherencia SIF** (TF/1.0 en fuente única XML↔declaración). Gate verde, tests 127/127. Informe: `docs/compliance/verifactu/verifactu-auditoria-cumplimiento.md`, registrado y visible en `/admin/documents` (PR #958, categoría legal·cumplimiento). **Fase 5 CERRADA (2026-07-16, +5 commits en PR #947, gate+build verdes, 108 tests)**: `IndicadorMultiplesOT` dinámico por cuenta (`countOwnerEmpresas`, S si ≥2 empresas del propietario, validado xmllint) · Σ cuota a helper único testeado (`src/lib/verifactu/cuota.ts`, property test vs oráculo SQL) · `TiempoEsperaEnvio` respetado entre envíos + cron fijado `*/5` · `buildRegistroBaja` BORRADO (código muerto; anulación = rectificativa) · informe §4.1 análisis encadenamiento. Toggle `verifactu_activo` → **sin guarda, diferido a 2027** (decisión Manu). **HECHO 2026-07-16**: merge a prod (auto-deploy Dokploy, commit `4a13c3db`) + mig 465 aplicada a prod (remoto 465, sin timestamps huérfanos) + `AGENTESIA_NIF` ya estaba en Dokploy. **PENDIENTE — TODO externo, nada más depende de nosotros**: (1) **smoke real contra AEAT `pre`** = único paso a "operacional" — bloqueado por **certificado cualificado** de AgentesiaLab por org (Gonzalo/Dani); al llegar: subir P12 en Ajustes→VERIFACTU de una org test, activar `pre`, emitir F1/F2, verificar `aceptada` + vigilar decimal 41,40 vs 41,4. (2) **encadenamiento por-serie vs por-obligado** (MAJOR #5): hoy `(org,serie)` → cadenas A y B paralelas; AEAT probablemente exige cadena única por obligado → **pregunta binaria al asesor fiscal** (¿cadena única por NIF o se admite por serie?); si dice única: mig nueva (lock+last_huella por org) + reordenar worker a orden cronológico global (análisis completo en informe §4.1, trigger NO tocado). (3) firmar/publicar la DR → `VIGENTE=true` en `declaracion-responsable.ts` (Gonzalo; borrador en `docs/compliance/verifactu/declaracion-responsable-BORRADOR.md`). Ver [[verifactu-huella-8-campos-mtls-parser-respuesta]] y [[totales-multilinea-redondeo-por-grupo-de-iva-una-sede]].

- 🟢 **Instrumentación de coste LLM (measure-first) — PR #896 MERGEADO a prod (2026-07-14)** — ningún path LLM medía tokens reales de caché y el OCR (`gpt-4o` visión, el MAYOR gasto) no leía `usage`. Añadido: dispatcher `callLlm` expone `usage` (input/output/cacheRead/cacheCreate) de ambos providers + log `[llm/cost]` (cubre enrich/fiscal/verifactu/inventario/diagnosticar); OCR log `[ocr/cost]`. `hitRatio` por-provider (Anthropic input excluye cacheado; OpenAI lo incluye). NO toca modelo/detail del OCR (precisión crítica → A/B con golden set + OK). **Siguiente paso (data-driven)**: tras ~1 día de tráfico, revisar logs `[llm/cost]`/`[ocr/cost]` en el host → dimensionar el ahorro y decidir si vale un A/B de modelo OCR. **OJO**: el copiloto en prod corre OpenAI `gpt-5.2` (`system_config.llm_provider`, ver `/admin/llm`) → auto-cache gratis; mi caching Anthropic (#894) queda latente hasta volver a Anthropic. 804/804 tests. [[anthropic-prompt-cache-prefijo-system-tools]]

- 🟢 **Prompt caching del copiloto — PR #894 MERGEADO a prod (2026-07-14)** — el runner Anthropic solo cacheaba system+tools; el HISTORIAL (turnos, tool_results, imágenes = lo que crece) iba a precio completo. Fix: 3er breakpoint MÓVIL en el último mensaje (recolocado antes de cada llamada, limpiando el previo; máx 4 breakpoints). + instrumentación `[copiloto/cache]` por turno (`cacheRead`/`cacheCreate`/`hitRatio`, solo log de servidor). Aislado en worktree off origin/main (no arrastró WIP ajeno). lint/typecheck/build limpios, 752/752 tests. **Smoke pendiente (Manu)**: tras deploy, `ssh -p 5251 root@185.47.13.170 "docker service logs \$(docker service ls --format '{{.Name}}' | grep tufacturaiaapp) --since 20m 2>&1 | grep copiloto/cache | tail"` → en turnos con historial, `cacheRead>0`/`hitRatio>0` = OK (1er turno de cada conversación es 0 normal). El classifier de auto-mode me bloqueó el SSH a prod, por eso lo verifica Manu. Learning [[anthropic-prompt-cache-prefijo-system-tools]].

- 🟢 **Auditoría de código con Fable 5 — 14 PRs (#842-#863 + #866) MERGEADOS + migs 455/456/457/459/460 aplicadas a prod (2026-07-12/13)** — barrido find→verify adversarial (bugs/arquitectura/seguridad/eficiencia), 30 hallazgos confirmados (JSON en `knowledge/projects/agentesia/facturaia-auditoria-fable5-hallazgos.json`). **Todo en prod salvo el recargo de equivalencia (espera OK gestoría).** Tanda 1 (#842-#850): gate cuenta cerrada API v1/MCP/copiloto + revoca api_keys (mig 455), JWT n8n fuera, ciclo circular export-* roto, cashflow excluye recibidas sin_aprobar/disputada, duplicar recibida→sin_aprobar, service key tiempo constante, getActiveFeatures sin N+1. Tanda 2: #857 duplicado copia unidad_medida/cantidad_presentacion (RPC mig 456) · #856 docs sync · #858 filtros fiscales `esEstadoEnLibro` · #855 índice único WhatsApp phone_number_id (mig 457) · #851 v1 rechaza abono. Tanda 3: #860 "Total filtrado" server-side (mig 459) · #862 KPIs dashboard sin cap 1000 (mig 460) · #863 Stripe cancela sub al cerrar cuenta (inmediata, sin reembolso; primer run tras deploy, 0 orgs afectadas hoy) · #861 claveRegimen VeriFACTU desde régimen real + gate cutover. Smoke tanda 2 (artifact QA): duplicado A/B PASS · cashflow excluye 36.622€ en limbo · cierre revoca keys. Issue #853 (retirada mirror conciliación, nunca hotfix). **PENDIENTES**: (1) **recargo de equivalencia** → ClaveRegimen bloqueada esperando OK gestoría (evidencia AEAT apunta a clave **18**, no 01; doc para la gestoría en `docs/compliance/verifactu-clave-regimen-recargo-equivalencia.md`; tras OK = 1 línea). (2) ✅ Aviso fiscal "confirma con tu gestoría" (#866, nivel 2 banner régimen sin clave + nivel 3 disclaimer asesor) MERGEADO tras QA visual en localhost con agent-browser (banner + disclaimer verificados, Sandbox revertido). **CERRADO 2026-07-13 (fan-out de agentes + drive iterativo)**: (a) 3 smokes runtime en prod TODOS VERDES con datos revertidos — 403 cuenta cerrada (mig455, gate handler.ts:158 + revoca api_key en misma tx) · duplicado por caja (mig456, borrador conserva unidad_medida='caja'/cantidad_presentacion) · cashflow excluye recibidas sin_aprobar/disputada (RPC dashboard_kpis mig460). (b) **PR #869 totales unificados MERGEADO + desplegado + re-smoke prod VERDE**: `calcularTotales` a `lib/documents/totales.ts` con redondeo por GRUPO de IVA (criterio XML VeriFACTU) + `round2` en create-document (persistido y devuelto) + `agregarTotales` en /generar; property test 500/500 backend==XML; sin migración. Re-smoke: borrador mixto 5×0,99@21%+1×10@10% → pantalla 16,99 = BD (base 14,95/total 16,99) = XML (cuota 2,04); el bug antiguo daba 17,00. Ver [[totales-multilinea-redondeo-por-grupo-de-iva-una-sede]]. Recargo de equivalencia NO entró (bloqueado por gestoría). **Follow-up RESUELTO (PR #871, 2026-07-13)**: subtotal por línea se redondeaba en `facturas/[id]/route.ts` (editor borrador) pero no en `buildLineasFactura` → `subtotalLinea` exportado y usado en ambos; pendiente merge Manu. Contadores paginación = eran #772 ya mergeado; worktree `fix-tab-counts` es cruft.

- 🟢 **Cola OCR (`bandeja_ingesta` → cola + dispatcher) + auditoría fallo silencioso — EN PROD via PR #802 (mergeado 2026-07-08), pendiente SOLO smoke** — recuperado tras crash del portátil (4 worktrees `fix/ocr-audit-*` con fixes sin commitear sobre `ocr-cola-001..006`). Cutover ejecutado: mig **449** aplicada a prod + cron `ocr-dispatcher` (scheduleId `MeIipOSGXc-1t4wxJcB2x`, `* * * * *`) creado y verificado (`runManually`→true). Cola (worker + `claim_ocr_batch` SKIP LOCKED + callers insertan `pendiente` sin OCR inline) + 4 fixes de fallo silencioso (upload/email/worker/reintentar: 200 falso → 500 + rollback + alerta). #800 duplicado cerrado + worktrees `ocr-audit-*` limpiados (2026-07-10). **Smoke Manu (prod)**: subir lote grande y confirmar que pasan de "en cola" a `listo`/`revisar` en ~1-2 min. Ver [[recuperar-trabajo-en-vuelo-tras-crash-via-worktrees]].

- 🟢 **Ingesta — UX de subida masiva y revisión EN PROD via PR #811 (mergeado 2026-07-10), pendiente SOLO smoke** — tras el feedback de que "parecía congelado"/confuso: BatchTracker (un solo indicador subiendo→en cola→leyendo→listas, sustituye las 2 barras) · preview "en cola" calmado (adiós al error rojo "No se pudo cargar") · avisos de revisión en español (mapear `total_mismatch` etc. + fallback sin código crudo) · "Eliminar definitivamente" en bloque + barra multi-select con wrap · duplicados por-lote ("Omitir todas"/"Subir todas de nuevo") + fix re-subida (`force` con `file_hash=null`, chocaba con índice único) · errores de servicio del login en español. Artifact de revisión visual entregado. **Smoke Manu**: subir lote grande + probar duplicados/borrado en prod.


## Módulos IA de `/agentes` — fases 1 y 2, cierre completo (2026-07-27, tarde)

Los 27 slices abordados y **en producción**: #1221 (fase 1, 18 commits) y #1248 (fase 2, 25 commits)
mergeadas, migración **574** aplicada y verificada en prod.

Fase 1: pila LIFO de Escape, borrado del editor de voz muerto (2.316 líneas), `FieldRow`/`SaveStatus`/
`useAutosave` promovidos a `ui/`, WhatsApp con Zod, multiempresa honesta con 409 bajo impersonación.
Cerrados 3 riesgos de datos: el PUT de `/admin/modules` estripaba `pattern`/`maxLength`/`preview`, una
clave de sistema en el `config_schema` dejaba la pestaña inguardable, y el override congelaba las 8
cuentas del asiento contable en prod.

Fase 2: los cuatro tabs de configuración autoguardan por campo con los primitivos compartidos (antes
eran cuatro dialectos, uno con botón "Guardar cambios"); una sola barra en el modal con pestañas
sticky y paneles con `hidden`; `TimeField` y `RadioCardGroup` nuevos. Tres bugs de datos: el régimen de
IVA que el cliente cambiaba en `/agentes` no llegaba al 303 (el dueño es `perfil_fiscal`), una cadencia
de cobros desordenada mandaba "reclamación formal" como primer aviso, y diez campos pintados que nadie
leía. Y tres más que destapó el propio gate: cadencia guardada a medias, dígitos intermedios
persistidos al teclear, y esqueleto infinito ante un 403.

Migración 574 (renumerada 3 veces: 568 → 570 → 574, porque `origin/main` ocupaba el hueco cada vez —
es exactamente por lo que el número se asigna al ABRIR el PR): borró las 11 claves placebo, los 2
separadores del copiloto y el override de `fiscal`, y **retiró `omc_insert`/`omc_update`/`omc_delete`**,
que permitían a un propietario escribir el jsonb directamente por PostgREST con el anon key. Verificado
en prod: solo queda `omc_select`.

**Incidente**: un `git push --force-with-lease` desde el checkout principal sin fetch previo rebobinó
`origin/main` 40+ commits (tanda fiscal #1238-#1246, middleware→proxy #1239, fase 1, migraciones
566-573 ya aplicadas en la BD). Recuperada reconstruyendo `cdf41473 + squash(rama rebasada)` y
verificando por ÁRBOL, no por log. Ver [[force-with-lease-sin-fetch-no-protege-nada]].

## Suite smoke E2E — de 61 a 105 verdes (2026-07-27)

Correr la suite completa contra una org de verdad (features, sector, NIF, datos) en vez de una vacía
destapó 2 bugs de producto y 6 derivas UI↔spec. Detalle y los 16 fallos restantes en
`issues/e2e-001-suite-smoke-contra-sandbox.md` del repo. PRs #1249, #1251 y #1254.

Los dos falsos verdes, que son lo que más importa: en `convertir-presupuesto` el locator muerto vivía
dentro de un `if (isVisible)` que nunca entraba, y `cierre-cuenta` tenía la org y el usuario cableados
con UUIDs de otra máquina. Ver
[[locator-de-test-atado-a-la-implementacion-caduca-y-da-falso-verde]] ·
[[spec-con-ids-de-entorno-cableados-mide-una-org-inexistente]].

## Sesión de tarde/noche — repaso de PRs pendientes, runtime fuera de EOL y limpieza del PDF

**23 PRs mergeadas.** Se arrancó revisando las 24 abiertas con TODOS los checks en rojo; el rojo
era el billing de Actions, no el código, así que la verificación fue manual: worktree de
integración desde `origin/main`, los 12 PRs de contenido mergeados JUNTOS y un solo gate
(lint + typecheck + build + 7.534 tests) sobre la composición, no PR a PR.

- **Cazado antes de mergear**: #1226 traía la migración `568`, número ya ocupado en main por
  `568_recibida_eliminar_borrador_no_bloquea` (main iba por 576). Renumerada a **577**, cabecera y
  referencia en `gotchas.md` actualizadas. El check del `pre-push` existe pero solo mira al empujar:
  una rama en review no se revalida cuando otra le ocupa el número.
- **Dependabot**: mergeadas las 5 seguras tras probarlas (react 19.2.8, ioredis, lucide, jest-dom 7 con
  la suite entera, los 4 bumps de Actions). Rechazadas con evidencia: eslint 10 **rompe el lint**
  (`eslint-config-next` trae un `eslint-plugin-react` incompatible), `node:24-alpine3.20` congelado,
  `@types/node` 26 por delante del runtime, e `impeccable` que no lo usa nadie (+474 líneas de lockfile).
- **Migración 577 aplicada a prod** y verificada leyendo el `indexdef`: predicado
  `nif IS NOT NULL AND nif <> ''`. Antes, el segundo cliente sin NIF de una org petaba con duplicate key.

**#1257 — salir de Node 20 (EOL hace 88 días).** Node 24 LTS + Alpine 3.23 en las tres imágenes,
`@types/node` a la 24, `node-version: '24'` en los 4 workflows. Se instaló colima para construir las
imágenes de verdad: A/B de 16 PDFs (4 plantillas × 1 y 5 páginas × Chromium 131 vs 149) con páginas,
tamaño A4 y texto extraído **idénticos**, y 0,000 % de píxeles distintos salvo 195 px de antialiasing
en el logo de una plantilla. Falso positivo descartado por el camino: el `next build` moría por OOM en
el contenedor **igual con Node 20**, era el heap de la VM.

**#1261 — la vista previa enseñaba 150 px del A4** desde el 25-may: `flex:1` en un iframe cuyo padre
no es flex. Verificado en prod con el navegador, generando un PDF real desde `/generar` (que además
cerró la última pieza de #1257: el render con Chromium 149 en producción).

**#1262 — retirado el microservicio `pdf-renderer`**: no lo importaba nadie y devolvía 500 en todos los
renders desde el #951. Con él se fue `/render-invoice`, pública sin sesión. El MCP se desplegó a mano
por `compose.deploy` porque su workflow está muerto con Actions (64 tools, hash `ab0125b5` intactos).

**5 issues de robustez abiertas con números**: #1267 (CI: 235 PRs sin verificar desde el 19-jul, y las
mitigaciones de incidentes viven dentro de Actions), #1268 (nadie vigila el EOL del runtime), #1269
(el camino que genera todas las facturas no tiene smoke), #1270 (grafo de dependencias con meses de
deriva), #1271 (4 ficheros de tests flaky por timeout bajo carga).

