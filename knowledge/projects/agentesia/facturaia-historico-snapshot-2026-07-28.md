---
title: TuFacturaIA — histórico snapshot 2026-07-28
date: 2026-07-28
source: poda del NOW del hub
tags: [facturaia, historico, snapshot]
---

# Snapshot 2026-07-28

Entradas ✅ retiradas del `NOW` de [[facturaia]] en el cierre del 28-jul. Todas
cerradas y en producción; se conservan aquí por trazabilidad.

- ✅ **El IVA negativo de los presupuestos — EN PRODUCCIÓN** (migs 575/576, #1250/#1252, 27-jul). `presupuestos.base` guardaba la bruta y ~6 sitios derivaban un IVA falso (−123,20 € en P2026-0019); ahora es la imponible NETA con invariante que aborta antes del INSERT. Ojo: #1250 no cerró el bug que decía cerrar (el INSERT de creación seguía descuadrando) → #1252. Detalle → [[facturaia-historico-snapshot-2026-07-27]] · [[base-persistida-debe-ser-la-imponible-o-total-menos-base-miente]] · [[ADR-042-base-imponible-neta-en-todos-los-tipos]]
- ✅ **`/api/health` ya dice qué build está sirviendo** (#1255, 27-jul) — devolvía `"version":"unknown"` y hubo que gastar tres escrituras contra la BD de prod para saber si el contenedor era el nuevo. La versión la calcula el build (marca de tiempo, y `APP_VERSION` como build arg si algún día el orquestador puede inyectar el commit); **no** un literal en el panel de Dokploy, que se congelaría en el primer build. Verificar despliegue = `curl /api/health` contra el `finishedAt` del deployment.
- ✅ **Ticket de IET: el vencimiento de una recibida ya aprobada — EN PRODUCCIÓN** (5 PRs, 27-jul). `datos_extraidos` solo se copia a la factura EN la aprobación, así que editarlo después no cambiaba nada; ahora hay endpoint propio, la bandeja muestra el `vto` de la FACTURA y la ficha permite añadir/editar. Detalle → [[facturaia-historico-snapshot-2026-07-27]] · [[staging-deja-de-ser-fuente-de-verdad-tras-el-commit-y-editarlo-no-cambia-nada]] · [[iet]]
- ✅ **Panel `/admin` sin 12 falsas incidencias de precios + `middleware.ts`→`proxy.ts` — EN PRODUCCIÓN** (#1238 `a2f5f7b5`, #1239 `52c1d64a`, 27-jul). Verificado contra prod: sin sesión `/dashboard` y `/admin` acaban en `/login`, `/api/v1/facturas` da 401 JSON y `robots.txt`/`sw.js`/`manifest` responden 200. Detalle → [[facturaia-historico-snapshot-2026-07-27]] · [[fallo-de-credencial-no-es-dato-ausente-en-un-check-de-coherencia]] · [[no-verificar-una-clave-read-only-escribiendo-con-ella]]
- ✅ **Gate de cierre del 26-jul: 4 defectos, 3 introducidos por esa sesión — EN PRODUCCIÓN** (PR #1234, mig 571). El cron borraba los cuadres de todo 130 con gastos, un 130 no se podía presentar, "Volver a abrir" daba 400 siempre y el sweep resolvía lo que omitía. Detalle → [[facturaia-historico-snapshot-2026-07-27]] · [[el-universo-comparable-es-lo-que-se-persiste-no-lo-que-se-carga]] · [[mockear-el-wrapper-de-auth-esconde-el-400-que-ve-el-usuario]] · [[lo-que-un-barrido-omite-no-puede-darse-por-recuperado]]
- ✅ **Remates del cierre — EN PRODUCCIÓN** (PRs #1240/#1243/#1245/#1246, migs 572/573). Lo gordo: marcar cuadrada/presentada tardaban **125 s** en fallar por marcar un error determinista como `40001`, que PostgREST reintenta → **145 ms** tras el arreglo. Detalle → [[facturaia-historico-snapshot-2026-07-27]] · [[error-determinista-con-sqlstate-40001-provoca-reintentos-infinitos]]
- ✅ **Lote conciliación + fiscal-001/002 + ingesta-001 — CERRADO en prod (26-jul)** y su último pendiente cerrado el 27-jul: el 303 2T de la sandbox recalculado (snapshot 59→127, 4 cuadres). Detalle → [[facturaia-historico-snapshot-2026-07-27]]
- ✅ **Módulos IA de `/agentes` — LOS 27 SLICES EN PRODUCCIÓN (#1221 + #1248, mig 574, 2026-07-27)** — los cuatro tabs de configuración autoguardan por campo con los primitivos compartidos, una sola barra en el modal, `TimeField` y `RadioCardGroup` nuevos. Seis bugs de datos cerrados, entre ellos el régimen de IVA que el cliente cambiaba en `/agentes` y no llegaba al 303. La **574** borró las 11 claves placebo y retiró la escritura directa al jsonb por PostgREST (verificado en prod: solo queda `omc_select`). **Cabos vivos**: baselines visuales a regenerar **en el contenedor oficial de Playwright**, 9 overlays con listener de Escape propio sin migrar, `factura-detail-managed`→`.fdoc-tabs` en PR aparte, y `ratchet:design` rojo de base (deuda ajena, no blanquear). Detalle → [[facturaia-historico-snapshot-2026-07-27]] · [[ADR-039-org-module-config-patch-merge-con-allowlist]] · [[override-de-bd-que-sustituye-al-schema-del-codigo-congela-claves]] · [[force-with-lease-sin-fetch-no-protege-nada]]
- ✅ **Suite smoke E2E EN VERDE — 61 → 105 → 122, y tres falsos verdes menos (#1263 + #1293, 2026-07-28)** — de los 16 que quedaban, **ninguna de las tres causas que yo había apuntado era la real**: los 8 de conciliación ya se sembraban solos (el "0 movimientos" era la foto DESPUÉS del teardown), los 2 del copiloto no eran la falta de clave (el mensaje **no se enviaba nunca**: el manejador de Enter lee el estado de React, no el valor del campo) y cuatro de los 5 "sin diagnosticar" eran el servidor de desarrollo muriéndose a mitad de tanda. Lo que sí apareció: **tres tests en verde sin comprobar nada**, uno de ellos tapando un bug de producto (el microtexto "Cobro DD/MM/YY · X €" de `/emitidas` llevaba oculto por `display:none` desde el #160; retirado con su Tooltip y un `toFixed(2)` que pintaba "121.00 €"), y el guard de "sin sesión no se entra" que salía **con la sesión puesta** porque `pwRequest.newContext()` hereda el `storageState`. #1293 remató cuatro cabos: el aviso que culpaba al usuario de un fallo nuestro, la cuota del Copiloto que contaba tokens y los llamaba "consultas", la consulta a `movimientos_bancarios` que ya no alimentaba nada, y `cwv-report.json` (ignorado pero **trackeado**, ensuciaba el árbol en cada tanda). **Config del sandbox que hizo falta**: `LLM_PROVIDER=openai` (el default del código es `anthropic` y se va a Anthropic sin avisar) y `org_limits.copiloto_tokens_mes` subido (13 mensajes agotaron el tope del plan). **Pendiente**: `conciliacion-ciclo:172` sigue **sin causa conocida** — el spec ya captura la respuesta de `/asignar` para la próxima; refuté yo mismo la hipótesis del signo reproduciéndola (responde 200). Ver [[locator-de-test-atado-a-la-implementacion-caduca-y-da-falso-verde]] · [[tanda-e2e-sin-comprobar-el-servidor-vivo-al-final-no-es-medicion]] · [[error-de-validacion-de-un-payload-que-arma-la-propia-ui-no-es-revisa-los-campos]] · [[cuota-medida-en-tokens-no-se-le-ensena-al-usuario-como-consultas]] · [[e2e-smoke-skip-honesto]]
- ✅ **Retención del historial del Copiloto (`modia-009`) — CERRADA end-to-end en prod** (mig 562 + cron en Dokploy + smoke real verde). Detalle → [[facturaia-historico-snapshot-2026-07-27]]
## Entradas del NOW condensadas el 2026-07-28 (2ª pasada del día)

Texto íntegro de las entradas 🟢/✅ que en el hub quedan como una línea.

- ✅ **`column fiscal_plazos.org_id does not exist` — CAUSA ENCONTRADA, PR #1301 abierto (28-jul)** — la hipótesis de la política RLS **era falsa, y no podía ser cierta**: Postgres valida las columnas al crear la política. `fiscal_plazos` es el calendario AEAT global y nunca tuvo `org_id` (mig 146). Quien lo inyecta es el proxy de impersonación (`api/admin/impersonate/query/route.ts:38`), que aplicaba `.eq('org_id', …)` a CUALQUIER tabla con una sola excepción hardcodeada. Solo pasa con superadmin impersonando, y el cliente se comía el error porque el endpoint responde 200 y `calendario-view.tsx:155` desestructuraba sin mirar `error`. Fix: allowlist de catálogos globales **en servidor** (`fiscal_plazos`, `plans`) + el calendario deja de tragarse el error. `profiles` fuera a propósito: elegir entre `active_org_id` y `default_org_id` cambia la semántica y es donde el arreglo obvio podría colar datos de otro usuario. 5 tests (los primeros del endpoint), verificados por mutación.- 🟢 **Barrido del NOW con 7 agentes en paralelo — LOS 6 PRs EN MAIN (28-jul, `a8a267c3`)**: #1301 `fiscal_plazos` · #1302 copy de billing · #1303 botón fiscal mudo · #1304 `conciliacion-ciclo` · #1305 CLS de `/settings` · #1306 teardown E2E. **Main compuesto verificado entero después de los 6 merges** (no solo cada PR por su lado): lint, typecheck, **777 ficheros / 7709 tests**, build y grafo sin deriva. El warning de Turbopack es preexistente, comprobado construyendo main en igualdad de condiciones. Pendiente de **desplegar**: `/api/health` seguirá dando el build viejo hasta que Dokploy reconstruya. Detalle de cada uno más abajo en su entrada. **Tres correcciones al propio hub**, que daba por abiertos PRs mergeados hace semanas: #1288, #810, #643-647 y #1156 están todos MERGED; el único abierto de verdad de aquella lista es **#610 Salt Edge** (draft, con la fuga cross-tenant sin arreglar). Ver [[playwright-imprime-la-linea-de-declaracion-del-test-no-la-del-fallo]]
- 🟢 **La retención ya se ve donde el cliente mira, y un bug latente menos — EN PROD (28-jul, #1309 `481c53de`)** — el ticket #86 preguntaba por qué el total del PDF no cuadra con el de la interfaz. Los dos números eran correctos (1.215,00 fiscal vs 1.144,71 que transfiere el cliente) y lo que faltaba era enseñar el segundo: la ficha ya lo desglosaba, el LISTADO no, que es donde él miraba. Ahora las emitidas con retención muestran "A cobrar" bajo el total, en escritorio y móvil. **Lo que destapó investigarlo**: el editor rápido persistía el total NETO también en emitidas, donde `facturas.total` es el bruto fiscal, así que `importe_cobrable` habría restado la retención dos veces. La mig 559 documenta esa fórmula como deliberada **y señala esa misma línea por número**, pero solo vale para recibidas: el comentario le daba coartada. **Cero filas dañadas** y sin riesgo fiscal (solo se edita un borrador, y al emitir `compose.ts` recalcula y aborta si base+cuota ≠ total). Criterio movido a `totalDesdeEdicion()` y regla de redondeo a `retencion-irpf.ts`, que retira la copia a mano; 18 tests con las 4 emitidas reales de prod como fixtures, verificado con mutación. Manuales de usuario y admin corregidos: describían el comportamiento CON el bug. Ver [[comentario-que-declara-una-formula-deliberada-solo-cubre-su-mitad]] · [[antes-de-tocar-un-ticket-mira-si-otra-sesion-ya-lo-esta-cerrando]]
- 🟢 **Los dos crons que faltaban, programados y verificados + la deriva del catálogo, en prod (28-jul, #1300)** — `runtime-eol-check` estaba en `CRON_REGISTRY` sin schedule en Dokploy (el registro es metadata, no crea nada), así que el vigilante de EOL existía y no se disparaba. Al darlo de alta comparé las 43 entradas contra los 43 schedules reales: cuatro discrepancias, y una gorda — **`obras-reservas-reconciliar` no se había ejecutado NUNCA** desde su despliegue, siendo la red de seguridad de las reservas de facturación de obra. Invisible por diseño: sin runs la salud es `'desconocido'` y el colector solo miraba `'rojo'`; el hueco estaba documentado en `gotchas.md` con un "auditar de vez en cuando" y mordió igual. Ahora se emite `cron_nunca_ejecutado` (ALTA aunque sea housekeeping, se resuelve sola al primer run). Corregidas además `copiloto-recordatorios-batch` (corriendo a diario desde el 29-jun **sin entrada en el catálogo**), `mcp-dcr-cleanup` (semanal desde el 3-jul con umbral aún de 48 h → rojo cada martes estando sano) y `module-events-purge`. **Los dos crons creados y verificados con run real**; `obras-reservas-reconciliar` devolvió `derivas_detectadas: 0`, o sea que los meses sin correr no dejaron descuadre. Gotcha nuevo: cada schedule de Dokploy tiene su `timezone` (`NULL`=UTC vs `Europe/Madrid`, conviven 31 y 13) → no deducir la expresión de las horas de `cron_runs`. Ver [[estar-en-el-catalogo-de-crons-no-es-estar-programado]]
- 🟢 **Runtime fuera de EOL + camino del PDF limpio — TODO EN PRODUCCIÓN (27-jul, 23 PRs)**. Prod corría desde hace 88 días sobre Node 20 (EOL 30-abr) y 117 sobre Alpine 3.20: #1257 lo pasa a Node 24 LTS + Alpine 3.23, validado con A/B de las imágenes reales (Chromium 131→149: páginas, texto y píxeles idénticos) y con un PDF generado en prod desde el navegador. #1261 arregla la vista previa, que enseñaba 150 px del A4 desde mayo. #1262 retira el microservicio `pdf-renderer` (muerto y roto desde el #951) y `/render-invoice` (pública sin sesión). Migración **577** aplicada y verificada por `indexdef`. MCP a Node 24 a mano, porque su workflow de deploy está muerto con Actions. Detalle → [[facturaia-historico-snapshot-2026-07-27]] · [[dependabot-no-avisa-de-eol-de-runtime]] · [[camino-critico-sin-smoke-se-pudre-meses]] · [[validar-cambio-de-motor-de-render-con-ab-de-misma-imagen]] · [[iframe-con-flex-1-en-contenedor-no-flex-cae-a-150px]]
- 🟢 **Las 5 issues de robustez, CERRADAS y en producción (28-jul, 6 PRs)** — **#1271** flaky: los 4 ficheros de copiloto (#1287; el coste de compilar el route se cobraba al primer test) y el de cobros (#1295; ahí el coste es trabajo real del test y va con timeout medido). **#1269** el camino del PDF ya tiene smoke: render con Chromium real (#1289) y visor en Playwright (#1296), los dos verificados con MUTACIÓN, no solo con verde. **#1268** vigilante mensual de EOL del runtime (#1290), que mira lo que se EJECUTA, no los Dockerfiles. **#1270** guardarraíl anti-deriva del grafo en el `pre-push` (#1294). **#1267** cerrada por decisión: no habrá CI → [[ADR-043-sin-ci-el-gate-local-es-el-contrato]]. Además, `brace-expansion` parcheada (#1291) y la alerta de `@hono/node-server` descartada con motivo. Ver [[verificar-que-un-test-tiene-dientes-con-una-mutacion]] · [[el-coste-de-compilar-el-modulo-se-cobra-al-primer-test]] · [[e2e-smoke-skip-honesto]]
- 🟢 **Vigilancia de la ventana de firma legacy, en producción (28-jul, #1299 `dfb7ee8c`)** — `legacySigningCollector` en `SYSTEM_COLLECTORS`: lo ejecuta el `system-health-sweep` que ya corría cada 10 min, así que no hay nada que dar de alta. Cuatro `type`, uno por caso porque el `dedupeKey` normaliza cifras a `#` (la cuenta atrás no re-emailea, el salto media→alta sí): `cerrable` (alta, 0 uso en 30d: se puede cerrar YA sin riesgo), `sin_fecha` (alta), `pendiente` (media, >7d) e `inminente` (alta, ≤7d — el que evita que el cierre tumbe los crons sin que nadie relacione la causa). Fecha pasada o mal formada no alerta y ni consulta la BD. Reconstruido desde el transcript tras perderse el worktree en `/private/tmp` sin commitear. **Cabo suelto**: un typo en `SIGNING_LEGACY_UNTIL` cierra la ventana al instante y el collector calla; solo queda el `console.error` de `auth.ts`. Ver [[transcript-jsonl-sobrevive-al-worktree-borrado]] · [[migracion-auth-sin-downtime-con-signing-legacy-until]]