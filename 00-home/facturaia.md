---
title: facturaia
date: 2026-05-10
updated: 2026-07-25
tags: [cliente, facturaia, hub]
---

# TuFacturaIA — Hub maestro

App SaaS de facturación con IA (OCR, agente WhatsApp, voz, recomendador). Multi-tenant. **Este archivo es el ÚNICO punto de entrada al proyecto.** Todo lo nuevo (idea, pendiente, smoke test, decisión, doc) entra aquí o se enlaza desde aquí.

## Cómo usar este hub

**Modelo de 2 niveles — no leer todo, leer lo que toca:**
- **Arranque de sesión (siempre, barato ~6-8K tokens)**: leer solo este bloque superior — `Estado` + `NOW` + `Bloqueos` + `Decisiones pendientes` + `Índice de áreas`. Es la foto del proyecto.
- **Durante (bajo demanda)**: abrir el subfile del área en la que trabajas (ver `Índice de áreas`) o la sección inferior concreta (NEXT / Smoke / WIP). NO leer el hub entero ni el histórico salvo que se pida.
- **Cierre — ritual `/fia-precommit` → `/fia-cierre` (gate DoD multi-agente, 14 dimensiones: MCP, diseño, UX, fallo/éxito, BD, observabilidad, regresión, rendimiento, contrato, fiscal, smoke, cabos; en `.claude/commands/`) → `/obsidian-1`**: lo cerrado (✅ / ~~tachado~~) **se mueve a [[facturaia-historico-detallado]]**, no se acumula aquí. Entradas del hub = 1-2 líneas mirando hacia delante. Sección que pasa de ~30 líneas → extraer a subfile.

---

## Estado actual

- **Prod**: `app.tufacturaia.com` + `n8n.tufacturaia.com` (Dokploy `185.47.13.170`, SSL Let's Encrypt OK). BD Supabase `lahqlyaxvobqjgdiftag`. (dominio viejo `facturaia.agentesia.world` caído/obsoleto)
- **Stack**: Next 16 · Supabase · n8n · OpenAI Vision · Anthropic Claude
- **Planes**: Starter 14 / **Plus 29** / Pro 49 / Enterprise 99 €/mes (+IVA 21%, anual −20%) + add-on Centro Fiscal IA 14,90. **Stripe LIVE** desde 2026-06-01. Reempaquetado escalonado + tier Plus + grandfathering aplicados **en BD** (mig 399, #509/#513). **Pendiente (acción Manu)**: crear los price IDs de Plus (mensual 29 / anual 278,40) en Stripe live + rotar envs `STRIPE_PRICE_ID_PLUS_*` (Starter 14 ya creado). Hasta entonces el tier Plus no es comprable.
- **Migraciones**: prod = main; ledger 450-460 completo y verificado en prod (2026-07-13). 452/453 (fiscal #825: `fiscal_plazos` 111/115/180/190/347 + `facturas.situacion_inmueble`) **aplicadas a prod** vía `db push --include-all` tras merge de #825. 454-460 = auditoría Fable5. Secuenciales `NNN_`, sin timestamps.
- **Tests**: Vitest 4320/0 verde (post-#648) · E2E smoke **63 pass / 21 skip** (2026-06-30, `6a0f8456`+`f5e736b4`): cashflow-v2 selectors, conciliacion-ciclo tablist, duplicar-lineas skip, PDF presupuesto precondición, feedback-multi-imagen timeout. Skip honesto por precondición (#531); helper `tests/e2e/fixtures/preconditions.ts` ([[e2e-smoke-skip-honesto]]).
- **Orgs prod**: AgentesiaLab · tecnocloud · Borja Galván · AgenteIA PRUEBA (todas `billing_status=active` complimentary).
- _Snapshot detallado anterior y changelog completo → [[facturaia-historico-detallado]]._

---

## NOW (trabajo activo)

- 🔴 **P0 — COBRO INDEBIDO vivo en prod: SEPA y cobros reclaman el importe BRUTO en facturas con retención (detectado 2026-07-25, SIN arreglar)** — `facturas.total` = `base+IVA` (lo que firma VeriFactu) pero el PDF imprime `base+IVA−retención` (`template-types.ts:387`). Con IRPF 15%: PDF 1.060 €, BD 1.210 €. `src/lib/sepa/remesa.ts:195,272,386` → `pain008.ts:173 <InstdAmt>` **adeuda 150 € de más por factura** (exposición a R-transaction y al mandato); `src/lib/cobros/orchestrator.ts:196-197` reclama al cliente final la cifra equivocada por email/WhatsApp. **Bloqueado por concurrencia**: `remesa.ts` está siendo editado en el worktree `facturaia-sepa` (`feat/sepa-modulo-config`) → o lo aplica esa sesión, o esperar su merge. NO dejar caer. Ver [[importe-fiscal-no-es-importe-a-cobrar-retenciones]].
- 🟡 **Conciliación v4 — PLAN MAESTRO listo, CERO código tocado (2026-07-25)** — auditoría completa del módulo (motor SQL, 25 endpoints, capa TS, UI, 3 docs) + revisión adversarial de 3 agentes (Postgres/fiscal · record-linkage/eval · producto/riesgo). 5 hallazgos: retenciones nunca casan (**0% de automatización** para profesionales que facturan a empresa) · matching es el único motor que muta estado fiscal **sin medición de acierto** (`agentic_decisions.dominio` CHECK excluye `matching`) · techo estructural (candidato por importe, el texto solo reordena) · `movimientos_bancarios.referencia` se guarda y el motor nunca la lee · el aprendizaje es un modal opt-in y la conciliación manual no aprende nada. Arranca por **PR-0a** (columna generada `importe_cobrable_eur`, cero conflicto con las otras ramas). **Superprompt + secuencia de 6 PRs + §0 mapa de colisión entre worktrees + §7 los 14 errores del borrador → [[facturaia-conciliacion-v4-plan]]**. Recortado del plan: cash coding y KPI de precisión al cliente; 3 puntos de la fase UX ya estaban en producción. Learnings: [[importe-fiscal-no-es-importe-a-cobrar-retenciones]] · [[relajar-filtro-duro-que-era-techo-implicito-abre-automatizacion-no-disenada]] · [[gin-trgm-no-resuelve-like-invertido-aguja-variable-pajar-fijo]] · [[gate-de-automatizacion-n50-al-95-no-sostiene-el-95-usa-cota-wilson]].
- ✅ **Conciliación: parcial en recibidas + import XLS/XLSX + 3 bugs reales de import EN PROD (7 PRs #1187-#1194, 23-jul)** — probado con extracto real de Agentesia Lab (.xls legado BIFF, SheetJS vía CDN propio por CVEs de npm). Bugs cazados en el propio dogfooding: dedup por existencia (no conteo) perdía comisiones repetidas el mismo día; cabecera "F. Valor"/"F. Operativa" no se detectaba (normalización inconsistente entre dos matchers); 90 tests preexistentes rotos en main (`connection()` Next 16 sin mock + varios). De paso: drawer al 40% + wizard "¿qué es este cobro?" con ejemplos. **Pendiente/idea en curso**: botón "volver a buscar sugerencias" por movimiento individual (hoy solo hay recalculo global) — evaluar. Ver [[hash-dedup-necesita-indice-secuencial-para-movimientos-repetidos]] · [[matching-texto-header-debe-normalizar-igual-en-ambos-lados]] · [[sheetjs-xlsx-npm-abandonado-cves-usar-cdn-propio-version-fijada]].
- ✅ **ticket-runner — smoke funcional CONFIRMADO 2×, 2026-07-24**: **PR #1198** (ticket real de Borja "permitir crear cliente sin dni", NIF opcional al crear cliente) y **PR #1199** (nuevo ticket, `bandeja_ingesta.fuente_account` — muestra qué buzón de email recibió cada documento cuando una org tiene varios conectados) — ambas con diff correcto, ambas en **draft** al fallar su propio gate typecheck (self-diagnosticado en el PR body — deps `xlsx`/`jsdom` del sandbox, no del repo), **nunca auto-mergearon**. Revisadas, verificadas localmente (lint/typecheck limpios; build falla solo por `.env` ausente en el worktree de review, no por el código) y mergeadas a mano. Confirma end-to-end el fix de PR #1182 (23-jul). **Hallazgo aparte**: #1199 traía su propia migración numerada `553`, la misma que #1111 acababa de fijar — 2º choque de numeración el mismo día → **fix sistémico**: `.githooks/pre-push` ahora aborta el push si detecta el mismo nº NNN en 2 migraciones distintas contra `origin/main`. Ver [[facturaia-migracion-numero-duplicado-536-553]].
- ✅ **ticket-runner — time tracking: sesiones sí llegaban bajo "Manuela" pero con `project=ticket-xxxxxx` en vez de "facturaia" (PR #1200, 2026-07-24, CERRADO)** — `resolveProject()` usaba el basename del worktree efímero (`mkdtempSync`), no el repo real; fix prioriza `REPO_SLUG` del entorno del runner. Mergeado directo (solo tocaba `facturaia`, no el panel), **redeploy manual + Autodeploy activado** en el servicio `ticket-runner` de Dokploy (estaba apagado), y **16 filas históricas reescritas** en `work_sessions` (agency-portal) de `ticket-xxxxxx` a `facturaia` vía PATCH directo a PostgREST. Ver [[hook-en-worktree-efimero-no-debe-derivar-nombre-de-basename-cwd]].
- 🔴 **Módulos IA de `/agentes` — auditoría cerrada + plan de 27 slices escrito, CERO código tocado (2026-07-25)** — 6 agentes en paralelo sobre el modal de configuración de los 9 módulos, cada hallazgo verificado en código. PRD en `issues/PRD-modulos-ia-config.md`, orden y avisos en `issues/modia-000-indice.md`, 27 slices `issues/modia-0NN-*.md` (5 marcados HITL). **Tres bugs P0 vivos en prod, ninguno arreglado:** (1) el `PATCH /api/modules/[id]` reemplaza el jsonb entero de `org_module_config` (13 escritores) → guardar la config de tesorería **borra el saldo bancario inicial manual** y la previsión se recalcula desde 0 €; guardar fiscal borra el borrador del wizard; (2) **13 ajustes `implemented:true` sin ningún consumidor**, incl. `guardar_historial` (promete no persistir y persiste siempre: promesa de privacidad incumplida), `alerta_bajo_minimo` (apagarlo no silencia el email diario) y `regimen_iva`/`periodicidad_iva`/`estimacion_irpf`, duplicado divergente de la tabla `perfil_fiscal` **con impacto fiscal directo**; (3) la cadencia de cobros solo se valida en cliente y la RPC elige nivel con cascada `>= d3` primero → con 90/3/7 (alcanzable por API o desde admin) el **primer** aviso que recibe un cliente final es "última comunicación antes de iniciar procedimientos de reclamación formales". Decisiones cerradas: retención corta para `guardar_historial`, autoguardado por campo en los 4 tabs, multiempresa mantiene el cambio de empresa pero canalizado y honesto, borrar los ~1.100 líneas de editor de voz muerto. **Empezar por** `modia-003`/`004`/`014`/`021`/`022`/`018` (no tocan ficheros de la rama `feat/sepa-modulo-config`, en vuelo y que bloquea `013`/`015`). Ver [[ADR-039-org-module-config-patch-merge-con-allowlist]] · [[jsonb-compartido-varios-escritores-patch-parcial-borra-claves-ajenas]] · [[config-escalera-ordenada-validar-el-orden-no-solo-min-max]] · [[escape-en-modal-el-culpable-no-es-el-popover-de-floating-ui]].
- 🔴 **OCR WhatsApp — sin aviso en fallo + `forma_pago` inventada, PR #1156 MERGEADO (2026-07-22)** — bug real de cliente (ticket restaurante no reconocido, sin respuesta al WhatsApp; reintento manual OK pero puso "Efectivo" sin que el ticket lo dijera). Fix: `noLegibleComoFactura` ahora avisa por WhatsApp ("no he podido leer la foto, reenvíala o revisa en la web"); prompt OCR con REGLA 17 anti-invención de `forma_pago`. **Pendiente: smoke real** (foto ilegible → llega aviso; ticket sin medio de pago → `forma_pago` vacío). Ver [[ocr-prompt-campo-sin-regla-anti-invencion-se-alucina]] · [[pipeline-async-solo-notifica-camino-feliz-deja-fallo-silencioso]].
- ✅ **Seguridad — auditoría de vulnerabilidades npm COMPLETA (PRs #1157/#1159/#1177, 12→22-23 jul)** — 12→2 vulnerabilidades. sharp 0.34.5→0.35.3 (CVE libvips, `overrides` para forzar copia única pese al `optionalDependency` de next) con QA exhaustiva en Alpine/musl (binding nativo, rotación real de píxeles, build standalone, servidor arrancado — ver [[npm-overrides-necesario-cuando-dependencia-fija-optionaldependency-vieja]]); de paso se coló y cerró un CVE real de next (16.2.9→16.2.11, 3 high: bypass middleware/proxy, 2×SSRF), verificado sin regresión. **Quedan solo 2 en `.audit-baseline.json`**: `@hono/node-server`/`@modelcontextprotocol/sdk`, bloqueadas upstream. Ver [[dependencias-npm-parcheadas-upstream-nunca-se-recogen-sin-npm-update-rutinario]] · [[trinquete-baseline-bloquea-solo-lo-nuevo-patron-reusable]].
- ✅ **306 tests rotos por `revalidateTag`/Cache Components (E4)** — diagnosticado y cerrado en PR #1181 (mismo día). Detalle en §E4 más abajo.
- 🔄 **Migración de datos wapi → módulo Obras (IET), org de prueba "Obras tufacturaia sandbox" `9a8227cb…` — CASI COMPLETA (2026-07-21)** — maestros + 9.110 materiales + 3.945 UOs + 2.380 presupuestos + 122.410 partidas + descuentos (7.168) + delegaciones/contactos (167/284) + instaladores/tarifas (534/668) + 554 obras (RPC `obras_aceptar_presupuesto`) + 1.339 facturas históricas no-fiscales (`registro_externo`, mig 470) + 2.370 partes de trabajo + 8.853 pedidos/28.149 líneas/10.055 albaranes. **Hueco de producto cerrado**: mig 537 (`es_ajuste` en líneas de presupuesto) aplicada + PR #1119 mergeado → **los 5 presupuestos de total negativo reimportados penny-exact** contra `sp_s_totalPresupuesto` (7898 −7932,22 · 7920 −34189,52 · 8200 −2314,06 · 8688 −330,57 · 9792 −23748,82). PR #1118 (presupuestos de obra visibles en `/presupuestos`, solo lectura sin duplicar numeración) también mergeado. `obras_pedidos.estado` verificado 0 discrepancias/8.853 contra la fórmula real de la RPC `obras_validar_albaran`. **PR #1111 (perf listado UO, N+1→batch) MERGEADO 2026-07-24** — SQL batch (mig, renumerada 536→553 por choque con `536_obras_responsable.sql` de #1114 del 21-jul, ver [[facturaia-migracion-numero-duplicado-536-553]]) verificada término a término equivalente a la single, `SECURITY INVOKER` correcto, sin regresión. **Pendiente**: QA visual de #1118 en prod (Manu) + decisión sobre las 165 delegaciones "Principal" sintéticas (wapi no las tiene; adaptación por `delegacion_id NOT NULL` de FacturaIA — Manu ya OK'd el criterio) + **OK explícito aparte para replicar en producción real IET `b9d5d6f7…`** (facturas históricas incluidas). **Gap nuevo detectado 21-jul (dogfooding tarde)**: `obras.importe_facturado_acumulado` no se recalcula tras el import directo (solo lo actualizan las RPCs de certificación/facturación en vivo) → banner "acumulado no coincide" en obras con facturas históricas reales; mismo patrón que el capítulo "General" siempre vacío (placeholder default junto al nodo real de wapi). Falta decidir si el loader añade un paso de reconciliación post-import (recalcular acumulado desde las facturas reales de cada obra + opcionalmente suprimir el "General" cuando ya hay un root propio). Detalle completo en memoria `project_facturaia_migracion_wapi`; cargador/informes/extracción en `~/Downloads/wapi_analysis/`. Ver [[migrar-precios-erp-congelar-neto-con-aritmetica-del-origen]] · [[wapi-lineas-deduccion-cantidad-o-precio-negativo-transform-sign-preserving]] · [[sqlcmd-w-y-mutuamente-excluyentes-usar-s-separador]] · [[db-error-transitorio-por-ddl-concurrente-entre-sesiones-paralelas]].
- ✅ **Obras — «Toda la obra»/ficha/certificación: pulido UX + bugs reales** (push directo a main, 2026-07-21) — 9 commits tras dogfooding de Manu (desplegable Responsable, barra de certificación fija, árbol Estructura persistente, tooltips, fix doble símbolo €). [[resolver-label-nombre-en-cliente-contra-endpoint-paginado-cae-al-uuid]] · [[css-grid-cabecera-y-filas-en-contenedores-distintos-auto-no-sincroniza]]
- ✅ **Obras — «Toda la obra»: Fases 1-2 + BLOQUE P1 del follow-up COMPLETO** (2026-07-21, PRs #1112-#1117 + #1121 retención garantía) — árbol multi-presupuesto, certificar por selección, smoke prod verde. [[obras-endpoints-n+1-paralelizar]] · [[levantar-supabase-local-en-facturaia-config-puertos-analytics-dup519]] · [[db-push-remote-versions-not-found-es-checkout-stale-o-num-duplicado-no-repair]]
- ✅ **Obras — nombres de componente por UUID + modales adaptables** (#1103/#1106, EN PROD 2026-07-20) — resolución server-side del nombre de material (antes UUID crudo) + modales a ancho fluido acotado. [[resolver-label-nombre-en-cliente-contra-endpoint-paginado-cae-al-uuid]]
- ✅ **Obras — Calendario (barras/festivos) + sistema de diseño en 5 olas EN PROD** (7 PRs #1042-#1048, 2026-07-19) — calendario tipo Google Calendar + festivos nacionales + tokens semánticos por rol unificados. [[worktree-qa-next-standalone-symlink-node-modules]]
- ✅ **Obras — IA + WhatsApp + MCP COMPLETO** (7 PRs, desplegado, 2026-07-19) — paridad de obras en copiloto (generar/explicar/imputar/pedidos) + 15 tools MCP `obras:read/draft`. [[namespace-v1-reservado-user-token-para-sacar-campo-del-contrato-api-key]] · [[copiloto-entidad-tabla-propia-necesita-tool-busqueda-y-regla-en-prompt]]
- ✅ **Botón «Ask» glass + spark real (topbar + Copiloto)** — PR #1054 MERGEADO (2026-07-20) — réplica del Glass Button 2.0 de Figma vía Figma MCP. [[figma-mcp-community-bloqueado-vs-editable]]
- ✅ **Sidebar rail plegado: centrado + tooltips por icono** — PR #1010 MERGEADO (2026-07-19).
- ✅ **Obras — polish UI + auditoría de mejoras EN PROD** (PRs #1005+#1006+mig 513, 2026-07-19) — kit `src/components/ui/` + N+1 presupuestos a RPC batch. [[cap-en-id-set-que-alimenta-agregacion-infra-reporta-en-silencio]] · [[aplicar-migracion-por-psql-y-registrar-version-cuando-el-cli-supabase-esta-bloqueado]]
- ✅ **Módulo Clasificador OCR: gate real de `auto_categorizar` + métricas de aprendizaje** — PR #990 MERGEADO (2026-07-18). [[defensa-cableada-vs-codigo-muerto]]
- ✅ **Cashflow a base caja + capa «Vencimientos»** — PR #986 MERGEADO (2026-07-17), fix saldo duplicado devengo+caja. [[cashflow-forecast-base-caja-no-mezclar-devengo-con-vencimiento]]
- ✅ **Calendario: aclarar cobros/pagos del día = vencimientos** — PR #987 MERGEADO (2026-07-17).
- ✅ **Dashboard: KPIs vacíos en Vista cliente** — PR #988 MERGEADO + mig 495 (2026-07-17). [[impersonate-client-stubbea-rpc-datos-de-rpc-vacios-en-vista-cliente]]
- 🐞 **Bug tragado `column fiscal_plazos.org_id does not exist`** (visto en logs prod 2026-07-17) — se repite pero está capturado en cliente (la capa fiscal del calendario simplemente no pinta, no rompe nada visible). NO está en los 6 usos TS (todos correctos), ni en funciones/vistas/triggers SQL → apunta a una política RLS de una tabla fiscal relacionada que referencia `org_id`. Baja severidad, pendiente pasada dedicada.
- 🧹 **Reconciliar migraciones timestamp** — remote tiene migs formato timestamp (de la rama Obras vía `migration new`, + mi 495 que MCP grabó como timestamp). El pre-push hook las flaggea. Requiere `supabase migration repair` batch desde máquina sin bloqueo de puertos Postgres. Ligar a merge de Obras.

- 🟢 **Módulo Obras (mini-ERP instalaciones eléctricas/telecom, sustituye a WAPI) — EN PRODUCCIÓN, Fases núcleo→4 completas (última: FASE 4 MERGEADA 2026-07-19, PR #1008)** — catálogo/precios, presupuestos→obra, pedidos/albaranes/stock, partes MO, facturación/certificación atómica, IA+copiloto+MCP, Herramientas. Detalle completo de las 4 fases + auditorías `/fia-cierre` + migs 471-518 → [[facturaia-historico-snapshot-2026-07-23]]. Ver [[rpc-returns-table-subconjunto-tras-add-column-cast-miente]] · [[importar-csv-erp-espanol-encoding-cp1252-y-precio-por-unidad-multiple]] · [[postgres-revoke-public-no-elimina-grants-individuales]] · [[mock-de-createdocument-oculta-violaciones-de-check-del-nucleo-fiscal]] · [[consumir-tope-y-emitir-documento-reservar-emitir-confirmar]].
- ✅ **Registro standalone + onboarding en claro + validación en vivo** — PR #984 MERGEADO (2026-07-17). [[facturaia-multiples-dialectos-input-field-generico-rompe-fuera-de-contexto]]
- ✅ **Iconos de marca reales para integraciones** — PRs #973/#975 MERGEADOS (2026-07-17). [[react-hooks-static-components-lookup-dinamico]]
- ✅ **Dedup OCR proveedores/clientes** — PR #965 MERGEADO+DESPLEGADO (2026-07-17, mig 467+468). [[advisory-lookup-en-funcion-compartida-debe-ser-opt-in]]
- ✅ **Clase 42703 CERRADA en TODO `src/**`** — en prod (2026-07-16, #944/#961/#964/#969). [[facturaia-historico-detallado]] · [[facturaia-erradicar-casts-supabase-42703]]
- 🟢 **VeriFactu auditoría A-Z + 10/10 blockers + Fase 5 — PR #947 MERGEADO a prod (`4a13c3db`) + mig 465 aplicada (`supabase db push`), 2026-07-16** — cruce multi-agente contra docs oficiales AEAT destapó que fallaba el 100% de envíos. Corregido y verificado: **huella** (mig **465**: faltaba `&FechaHoraHusoGenRegistro=`, timestamp único en `verifactu_fecha_gen`, hex MAYÚSCULAS → reproduce el vector oficial firmado) · **XML/SOAP** (namespaces NS_LR/NS_SI, orden desglose, `IdSistemaInformatico`=**TF** → validado con xmllint) · **transporte** (mTLS nativo `node:https`, parser real `EstadoEnvio`/`EstadoRegistro`, host prod, sin firma en VERI*FACTU) · **QR** (al principio, «QR tributario:», 30mm, número bajo el logo → render Puppeteer de las 5 plantillas) · **coherencia SIF** (TF/1.0 en fuente única XML↔declaración). Gate verde, tests 127/127. Informe: `docs/compliance/verifactu/verifactu-auditoria-cumplimiento.md`, registrado y visible en `/admin/documents` (PR #958, categoría legal·cumplimiento). **Fase 5 CERRADA (2026-07-16, +5 commits en PR #947, gate+build verdes, 108 tests)**: `IndicadorMultiplesOT` dinámico por cuenta (`countOwnerEmpresas`, S si ≥2 empresas del propietario, validado xmllint) · Σ cuota a helper único testeado (`src/lib/verifactu/cuota.ts`, property test vs oráculo SQL) · `TiempoEsperaEnvio` respetado entre envíos + cron fijado `*/5` · `buildRegistroBaja` BORRADO (código muerto; anulación = rectificativa) · informe §4.1 análisis encadenamiento. Toggle `verifactu_activo` → **sin guarda, diferido a 2027** (decisión Manu). **HECHO 2026-07-16**: merge a prod (auto-deploy Dokploy, commit `4a13c3db`) + mig 465 aplicada a prod (remoto 465, sin timestamps huérfanos) + `AGENTESIA_NIF` ya estaba en Dokploy. **PENDIENTE — TODO externo, nada más depende de nosotros**: (1) **smoke real contra AEAT `pre`** = único paso a "operacional" — bloqueado por **certificado cualificado** de AgentesiaLab por org (Gonzalo/Dani); al llegar: subir P12 en Ajustes→VERIFACTU de una org test, activar `pre`, emitir F1/F2, verificar `aceptada` + vigilar decimal 41,40 vs 41,4. (2) **encadenamiento por-serie vs por-obligado** (MAJOR #5): hoy `(org,serie)` → cadenas A y B paralelas; AEAT probablemente exige cadena única por obligado → **pregunta binaria al asesor fiscal** (¿cadena única por NIF o se admite por serie?); si dice única: mig nueva (lock+last_huella por org) + reordenar worker a orden cronológico global (análisis completo en informe §4.1, trigger NO tocado). (3) firmar/publicar la DR → `VIGENTE=true` en `declaracion-responsable.ts` (Gonzalo; borrador en `docs/compliance/verifactu/declaracion-responsable-BORRADOR.md`). Ver [[verifactu-huella-8-campos-mtls-parser-respuesta]] y [[totales-multilinea-redondeo-por-grupo-de-iva-una-sede]].
- ✅ **Unificación UI COMPLETA — los 4 Frentes** — en prod (2026-07-16, 11 PRs #953-#963). [[facturaia-historico-detallado]]
- 🟢 **Instrumentación de coste LLM (measure-first) — PR #896 MERGEADO a prod (2026-07-14)** — ningún path LLM medía tokens reales de caché y el OCR (`gpt-4o` visión, el MAYOR gasto) no leía `usage`. Añadido: dispatcher `callLlm` expone `usage` (input/output/cacheRead/cacheCreate) de ambos providers + log `[llm/cost]` (cubre enrich/fiscal/verifactu/inventario/diagnosticar); OCR log `[ocr/cost]`. `hitRatio` por-provider (Anthropic input excluye cacheado; OpenAI lo incluye). NO toca modelo/detail del OCR (precisión crítica → A/B con golden set + OK). **Siguiente paso (data-driven)**: tras ~1 día de tráfico, revisar logs `[llm/cost]`/`[ocr/cost]` en el host → dimensionar el ahorro y decidir si vale un A/B de modelo OCR. **OJO**: el copiloto en prod corre OpenAI `gpt-5.2` (`system_config.llm_provider`, ver `/admin/llm`) → auto-cache gratis; mi caching Anthropic (#894) queda latente hasta volver a Anthropic. 804/804 tests. [[anthropic-prompt-cache-prefijo-system-tools]]
- 🟢 **Prompt caching del copiloto — PR #894 MERGEADO a prod (2026-07-14)** — el runner Anthropic solo cacheaba system+tools; el HISTORIAL (turnos, tool_results, imágenes = lo que crece) iba a precio completo. Fix: 3er breakpoint MÓVIL en el último mensaje (recolocado antes de cada llamada, limpiando el previo; máx 4 breakpoints). + instrumentación `[copiloto/cache]` por turno (`cacheRead`/`cacheCreate`/`hitRatio`, solo log de servidor). Aislado en worktree off origin/main (no arrastró WIP ajeno). lint/typecheck/build limpios, 752/752 tests. **Smoke pendiente (Manu)**: tras deploy, `ssh -p 5251 root@185.47.13.170 "docker service logs \$(docker service ls --format '{{.Name}}' | grep tufacturaiaapp) --since 20m 2>&1 | grep copiloto/cache | tail"` → en turnos con historial, `cacheRead>0`/`hitRatio>0` = OK (1er turno de cada conversación es 0 normal). El classifier de auto-mode me bloqueó el SSH a prod, por eso lo verifica Manu. Learning [[anthropic-prompt-cache-prefijo-system-tools]].
- ✅ **Cierre de pendientes (2026-07-13)** — 3 PRs #871/#825/#872 MERGEADOS. [[facturaia-historico-detallado]]

- 🟢 **Auditoría de código con Fable 5 — 14 PRs (#842-#863 + #866) MERGEADOS + migs 455/456/457/459/460 aplicadas a prod (2026-07-12/13)** — barrido find→verify adversarial (bugs/arquitectura/seguridad/eficiencia), 30 hallazgos confirmados (JSON en `knowledge/projects/agentesia/facturaia-auditoria-fable5-hallazgos.json`). **Todo en prod salvo el recargo de equivalencia (espera OK gestoría).** Tanda 1 (#842-#850): gate cuenta cerrada API v1/MCP/copiloto + revoca api_keys (mig 455), JWT n8n fuera, ciclo circular export-* roto, cashflow excluye recibidas sin_aprobar/disputada, duplicar recibida→sin_aprobar, service key tiempo constante, getActiveFeatures sin N+1. Tanda 2: #857 duplicado copia unidad_medida/cantidad_presentacion (RPC mig 456) · #856 docs sync · #858 filtros fiscales `esEstadoEnLibro` · #855 índice único WhatsApp phone_number_id (mig 457) · #851 v1 rechaza abono. Tanda 3: #860 "Total filtrado" server-side (mig 459) · #862 KPIs dashboard sin cap 1000 (mig 460) · #863 Stripe cancela sub al cerrar cuenta (inmediata, sin reembolso; primer run tras deploy, 0 orgs afectadas hoy) · #861 claveRegimen VeriFACTU desde régimen real + gate cutover. Smoke tanda 2 (artifact QA): duplicado A/B PASS · cashflow excluye 36.622€ en limbo · cierre revoca keys. Issue #853 (retirada mirror conciliación, nunca hotfix). **PENDIENTES**: (1) **recargo de equivalencia** → ClaveRegimen bloqueada esperando OK gestoría (evidencia AEAT apunta a clave **18**, no 01; doc para la gestoría en `docs/compliance/verifactu-clave-regimen-recargo-equivalencia.md`; tras OK = 1 línea). (2) ✅ Aviso fiscal "confirma con tu gestoría" (#866, nivel 2 banner régimen sin clave + nivel 3 disclaimer asesor) MERGEADO tras QA visual en localhost con agent-browser (banner + disclaimer verificados, Sandbox revertido). **CERRADO 2026-07-13 (fan-out de agentes + drive iterativo)**: (a) 3 smokes runtime en prod TODOS VERDES con datos revertidos — 403 cuenta cerrada (mig455, gate handler.ts:158 + revoca api_key en misma tx) · duplicado por caja (mig456, borrador conserva unidad_medida='caja'/cantidad_presentacion) · cashflow excluye recibidas sin_aprobar/disputada (RPC dashboard_kpis mig460). (b) **PR #869 totales unificados MERGEADO + desplegado + re-smoke prod VERDE**: `calcularTotales` a `lib/documents/totales.ts` con redondeo por GRUPO de IVA (criterio XML VeriFACTU) + `round2` en create-document (persistido y devuelto) + `agregarTotales` en /generar; property test 500/500 backend==XML; sin migración. Re-smoke: borrador mixto 5×0,99@21%+1×10@10% → pantalla 16,99 = BD (base 14,95/total 16,99) = XML (cuota 2,04); el bug antiguo daba 17,00. Ver [[totales-multilinea-redondeo-por-grupo-de-iva-una-sede]]. Recargo de equivalencia NO entró (bloqueado por gestoría). **Follow-up RESUELTO (PR #871, 2026-07-13)**: subtotal por línea se redondeaba en `facturas/[id]/route.ts` (editor borrador) pero no en `buildLineasFactura` → `subtotalLinea` exportado y usado en ambos; pendiente merge Manu. Contadores paginación = eran #772 ya mergeado; worktree `fix-tab-counts` es cruft.
- ✅ **Centro Fiscal auditado a fondo** — PR #825 MERGEADO + migs 452/453 en prod (2026-07-13). Smoke pendiente: ver §Smoke tests pendientes. [[facturaia-historico-detallado]]
- 🟢 **Cola OCR (`bandeja_ingesta` → cola + dispatcher) + auditoría fallo silencioso — EN PROD via PR #802 (mergeado 2026-07-08), pendiente SOLO smoke** — recuperado tras crash del portátil (4 worktrees `fix/ocr-audit-*` con fixes sin commitear sobre `ocr-cola-001..006`). Cutover ejecutado: mig **449** aplicada a prod + cron `ocr-dispatcher` (scheduleId `MeIipOSGXc-1t4wxJcB2x`, `* * * * *`) creado y verificado (`runManually`→true). Cola (worker + `claim_ocr_batch` SKIP LOCKED + callers insertan `pendiente` sin OCR inline) + 4 fixes de fallo silencioso (upload/email/worker/reintentar: 200 falso → 500 + rollback + alerta). #800 duplicado cerrado + worktrees `ocr-audit-*` limpiados (2026-07-10). **Smoke Manu (prod)**: subir lote grande y confirmar que pasan de "en cola" a `listo`/`revisar` en ~1-2 min. Ver [[recuperar-trabajo-en-vuelo-tras-crash-via-worktrees]].
- 🟢 **Ingesta — UX de subida masiva y revisión EN PROD via PR #811 (mergeado 2026-07-10), pendiente SOLO smoke** — tras el feedback de que "parecía congelado"/confuso: BatchTracker (un solo indicador subiendo→en cola→leyendo→listas, sustituye las 2 barras) · preview "en cola" calmado (adiós al error rojo "No se pudo cargar") · avisos de revisión en español (mapear `total_mismatch` etc. + fallback sin código crudo) · "Eliminar definitivamente" en bloque + barra multi-select con wrap · duplicados por-lote ("Omitir todas"/"Subir todas de nuevo") + fix re-subida (`force` con `file_hash=null`, chocaba con índice único) · errores de servicio del login en español. Artifact de revisión visual entregado. **Smoke Manu**: subir lote grande + probar duplicados/borrado en prod.

- 🔴 **Marketing/Growth (panel interno superadmin `admin/marketing`) — infra 000/003/012 EN PROD (#799 + #801, 2026-07-08); resto (11 issues) BLOQUEADO en cuentas externas** — NO es feature de clientes, es growth de la propia plataforma (Google Ads/Meta Ads/GA4/GSC/cupones). Desplegado: fundaciones (mig 447: `marketing_connections`/`marketing_audit_log`/`superadmin_permissions`+permiso `marketing_write`), guardarráiles de gasto (`assertBudgetLimit`/`runGuardedMutation`, mig 448) + vista de auditoría (`/auditoria`), cupones Stripe; migraciones aplicadas, tipos regenerados y casts fuera. **Bloqueado**: Google Ads (proyecto GCP+developer token+cuenta test), Meta Ads (app+App Review `ads_management`), GA4/GSC (propiedades verificadas) — acción Manu, fuera de alcance de código. Sin bloqueo: smoke prod (ver Smoke) + criterio 4 de 003 (no-bypass e2e + cablear confirm modal) diferido a issues 004/008. Detalle en [[facturaia-historico-detallado]].

- ✅ **Sistema de cupones de descuento** — 5 PRs MERGEADOS + migs 437-439 en prod (2026-07-07). Pendiente acción Manu: crear el primer cupón real desde `/admin/descuentos`. [[stripe-coupon-no-es-tecleable-necesita-promotion-code]] · [[worktree-add-sin-cd-bash-cae-en-checkout-principal]] · [[facturaia-historico-detallado]]
- 🔴 **Intento de crear el cupón vía conector MCP de Stripe — BLOQUEADO por cuenta equivocada, empezar por aquí (2026-07-07)** — el conector se autenticó contra `acct_1TJrQvQY4tV8FMxQ` ("AgentesIA", cuenta del Stripe CLI), NO la de prod. Cuenta correcta **verificada sin ambigüedad**: `acct_1Td5ccGgQMT2aOqB`, display name Dashboard **"Tufacturaia"** (razón social AgentesiaLab S.L.) — confirmado con `curl https://api.stripe.com/v1/account -u "$(op read 'op://FacturAIA/Stripe API/credential')":` (item 1Password ya localizado). Desconectar el conector desde OTRA sesión de Claude Code **no invalida la sesión actual** — cada sesión cachea su propia conexión OAuth; hace falta desconectar y reautorizar dentro de la MISMA sesión que va a usarlo, eligiendo la cuenta "Tufacturaia" en el selector de Stripe (no "AgentesIA") antes de aceptar el consentimiento. **Recomendado**: saltarse el conector MCP para esto y crear el cupón directo desde `/admin/descuentos` (usa el `STRIPE_SECRET_KEY` real de prod, cero ambigüedad de cuenta). Ver [[stripe-mcp-conector-cuenta-equivocada-oauth-no-invalida-entre-sesiones]].
- ✅ **Auditoría billing Stripe (suscripción)** — PR #777 MERGEADO + mig 436 en prod (2026-07-07). [[stripe-subscription-item-resolver-por-price-no-por-indice]] · [[facturaia-historico-detallado]]
- 🔴 **Cobro Stripe Connect — bloqueado en acción Manu, todo lo previo preparado (2026-07-07)** — billing usa `acct_1Td5ccGgQMT2aOqB` (sk_live) pero el setup Connect/KYC se hizo en `acct_1TJrQvQY4tV8FMxQ` (la del CLI) = cuenta equivocada. El cobro Connect en prod usa `STRIPE_SECRET_KEY`=`acct_1Td5cc`. **Runbook completo versionado en repo: `docs/architecture/cobro-stripe-connect-runbook.md`** (PR #781, incluye el SQL de activación de la feature en AgentesiaLab `ea201784…` — NO sandbox `b5f86e8f`). Verificado en código: `/api/internal/stripe-connect-webhook` ya en `isServiceRoute` (sin redirect a /login), `features.cobro_stripe` activo+no-beta (override por-org manda). **Pendiente = SOLO acción Manu en dashboard Stripe (`acct_1Td5cc`)**: (1) KYC plataforma aprobada, (2) webhook LIVE `account.updated`+`checkout.session.completed`, (3) `STRIPE_CONNECT_WEBHOOK_SECRET` en Dokploy (container recreado). Hecho eso → aplico el SQL + smoke E2E. Conciliación (#705) validada localhost/test. Ver [[stripe-cli-cuenta-distinta-de-la-app-price-no-existe-falso-positivo]].
- 🔴 **Plantillas: logo S/M/L/XL + ocultar nombre bajo logo + fix empresa-vs-contacto — MERGEADO directo a main (2026-07-03)** — 2 bugs reales destapados: sanitizador de layout descartaba `mostrarNombre` en cada render (ver [[sanitizador-allowlist-reconstruye-objeto-descarta-campos-nuevos]]) y `clientes/proveedores.empresa` no se seleccionaba en 5 sitios pese a que las plantillas ya lo preferían (ver [[campo-opcional-en-tipo-compartido-no-implica-seleccionado-en-todos-los-selects]]). Dato ABBA corregido de paso. **Incidente**: `compose.one` de Dokploy volcó el env de prod al chat, ver `Seguridad`. **Falta**: confirmar en prod que ya no sale el nombre bajo el logo de ABBA.
- ✅ **Export gestoría v1.5** — CERRADO, en prod y verificado con datos reales (2026-07-03). Único pendiente: Pre303 HITL (bloqueado sin certificado). [[facturaia-export-gestoria-v15]] · [[facturaia-historico-detallado]]
- ✅ **PR #640 — 5 docs legales compliance pulidos** — MERGEADO (2026-07-02). Pendiente acción Manu: envío real a FNMT/OpenAI/Anthropic/abogado/seguro. [[facturaia-historico-detallado]]
- ✅ **Smoke /soporte (sandbox, 2026-07-02)** — carga con empty-state (bug del Dashboard arreglado). [[facturaia-historico-detallado]]
- ✅ **Auditoría de cobertura de tools de agente (MCP/copiloto/v1/web)** — 4 PRs (#890-#893) en prod (2026-07-14). Frontera fiscal intacta y blindada. [[capacidad-en-dos-capas-tool-mas-gate-endpoint-verificar-paridad-o-queda-muerta]] · [[facturaia-historico-detallado]]
- 🔴 **Retirada de n8n (WhatsApp) — 5 PRs mergeados 2026-07-10, faltan smokes prod + Fase 6** — n8n receptor fuera del runtime; receptor real = `src/app/api/whatsapp/webhook`. Cerró 2 regresiones del cutover (opt-out STOP #814 · extractos foto #816+mig451) + portó 2 flujos (facturar-movimiento tool copiloto #820 · cobro-confirmar cliente-final #822) + docs #817. **Smokes prod pendientes** (ver `Smoke tests pendientes`). **Fase 6 (borrar dead-code + archivar workflow) pospuesta hasta smokes OK** (n8n = rollback). Detalle: memoria agente `project_facturaia_retirada_n8n`. Learnings previos del canary siguen válidos: [[whatsapp-multi-tenant-matching-debe-ser-por-numero-remitente-no-por-phone-number-id]] · [[fire-and-forget-resultado-descartado-esconde-fallo-loguear-en-transporte]] · [[internal-fetch-res-ok-silencioso]].
- ✅ **Plan consolidación WhatsApp/copiloto (post G5)** — PR #705 MERGEADO A MAIN (2026-07-05). `cobro_stripe` gated off, sin riesgo en prod. [[payment-link-importe-congelado-revalidar-pendiente-al-conciliar]] · [[stripe-connect-signup-gotcha-crear-cuenta-conectada]] · [[facturaia-historico-detallado]]
- ✅ **Integración Slack — LIVE EN PROD** (#002-007b, migs 406-411) — lecturas+escrituras con autoría `agent:slack`, vinculación de identidad. Ver ADR-009 · manual-admin §9. [[facturaia-historico-detallado]]
- 🔴 **Smoke UI conciliación en sandbox** (tras PR #259/#261) — `/conciliacion`: contraste del drawer, sugerencia visible, tabla sin scroll horizontal (móvil+web), banner legible. Si chirría → 3-4 variantes numeradas. Pasos en `Smoke tests pendientes`.
- 🔴 **Smoke post-deploy PR #675 (filtro cid/tamaño ingesta email)** — confirmar que un email transaccional con imágenes embebidas (tipo Salt Edge) ya no genera filas nuevas en `bandeja_ingesta`/alerta OCR. Detalle en [[incidents]] 2026-07-03.
- 🔴 **Smoke post-deploy tren #653-658** — descargar XLSX/CSVs/Libros AEAT desde detalle 303 y /informes en prod, cuadrar vs /fiscal, confirmar Libro IVA revive; smoke WhatsApp «¿cuánto IVA debo?» → elegir org → responde LA PREGUNTA (#662) + vista detalle sin hueco y menús canónicos (#661).
- ✅ ~~Registro branded + onboarding (PR #256/#258)~~ — ciclo antiguo superado por el rediseño de #984. [[facturaia-historico-detallado]]
- ✅ **Iniciativa agéntica S7+G6 CERRADA** (2026-06-30) — batch recordatorios headless + config por-org + crons `mcp-dcr-cleanup`/`copiloto-recordatorios-batch` verificados. [[facturaia-historico-detallado]]
- ✅ **G5 — Desacople copiloto↔n8n — CUTOVER COMPLETO** (2026-06-30) — envs+webhook Meta en Dokploy, canary y smokes verificados. [[facturaia-historico-detallado]]
- ✅ **Botón «Enviar a Conciliación» (Bandeja IA) reconectado** (2026-06-30) — endpoint huérfano portado y reescrito, smoke real verificado. [[ocr-clasificacion-doc-type-no-factura-sin-campos]] · [[facturaia-historico-detallado]]
- **`missing_nif_emisor` ya no fuerza revisión en proveedor extranjero** (`src/lib/ocr/anomaly.ts`, 2026-06-30, caso real: recibo Resend en sandbox) — severidad `medium` si `pais≠ES`/`moneda≠EUR`. Lint+typecheck+build verde. **Pendiente: commit+push+deploy+smoke** (ver [[ocr-clasificacion-iva-zero-seguros]]).
- **`fix/ingesta-progress-faster-feedback` — audit discriminadores OCR staged, pendiente commit+PR** — prerequisito canary G5 (paths non-success pipeline eran silenciosos). Cambios: shapes ingesta, !res.ok webhook, float clamp progress.ts.
- **🔴 Smoke «Convertir en recurrente» pendiente** — factura emitida real con SEPA activo → toggle topbar → cadencia/fecha/modo → confirmar entrada en `facturas_recurrentes` + redirect `/recurrentes`. (`27d0c773` en main, no verificado E2E.)
- ✅ **Dropzone «Importar extracto bancario» (/conciliacion) CERRADA** — 2 PRs (#1148/#1151, 2026-07-22), pieza glass compartida + fix de hueco muerto. [[next-typed-routes-validator-stale-tras-cambio-de-rama]]
- ✅ **API v1 Obras — 15 endpoints documentados en `openapi.json`, drift-guard cerrado (PR #1192, 2026-07-23)** — hallazgo al arreglar tests rotos preexistentes: los endpoints reales bajo `/api/v1/obras/*` (obras, pedidos, presupuestos, instaladores, materiales, partes, albaranes, salidas, informes) eran invisibles al SDK del portal. Hecho en worktree tras perder el primer intento (agente en background editando `main` compartido, ver [[claude-code-sesiones-paralelas-mismo-repo-colisiones-git]]).
- _Hitos cerrados (PR #607 «Convertir en recurrente» lista+modal+ingesta justificante, toggle topbar `27d0c773`, MCP server LIVE + Directory enviado, Resolver-con-Claude Fase 4, perf merge-train, incidente host OOM, debouncing, emisión web, Copiloto Fase 1+2 + set fiscal, stock lotes, copiloto paridad MCP 001-014, onboarding 021-030, conciliación Fase 2, modelo 349, multiempresa) → [[facturaia-historico-detallado]]._
## Bloqueos / esperando a terceros

- ~~PR #851 (v1 rechaza `tipo:'abono'`)~~ ✅ **RESUELTO 2026-07-13** — decisión Manu: agency-portal NO crea abonos vía `POST /api/v1/facturas` → mergeado (`d5bb63a4`). Canal correcto = `POST /v1/facturas/{id}/anular`.
- ~~**[ACCIÓN MANU 2026-07-04] Reautorizar 1Password — bloquea `g5a-005`**~~ **RESUELTO/MOOT (2026-07-04)**: la API key de n8n en memoria seguía viva → audit hecho sin `op`. **Veredicto audit g5a-005 (CONFIRMADO): 0 tráfico real del Agente Facturador.** Las 21 ejecuciones con agente=true en la ventana retenida (2026-06-27→29) vienen SOLO de testers internos (Manu 34617314938, Sory 34661253928); cero remitentes externos. Receptor `pqSWkDIHqmSVHotB` sin ejecuciones desde 2026-06-29 (coherente con cutover webhook Meta→Next.js del 30/06). Caveat: retención n8n limita visibilidad a ~2 días. **Hallazgo extra**: `fallback_n8n_agent` NO existe en código; el endpoint copiloto ya responde siempre in-house. La decomisión pendiente es 100% cirugía del workflow n8n (irreversible, requiere OK) — quitar nodo Agente Facturador + toolCode con SERVICE_ROLE_KEY, dejar rama texto como relay puro.
- **[ACCIÓN MANU — registro Google OAuth no verificado]** Usuarios que se registran con Google ven aviso de app "no verificada" porque la OAuth app no ha pasado el proceso de verificación de Google. Impacta la confianza en el registro. Fix: Google Cloud Console → APIs & Services → OAuth consent screen → Submit for verification. Requiere privacy policy URL, dominio verificado `tufacturaia.com`, justificación de scopes usados. Plazo revisión Google: 2-6 semanas. Hasta entonces el aviso es cosmético pero genera abandono en el registro.

- **[ACCIÓN MANU 2026-06-17] Billing GitHub Actions re-bloqueado** — desde ~13:00 todos los runs fallan a 0 pasos ("payments failed / spending limit"). Repo privado → Actions de pago; se agotó cupo/límite. CI ya optimizado para no derrochar (#367 CI/Visual solo en PR + concurrency; #365 visual determinista). Regularizar pago / subir spending limit en Settings → Billing org `AgentesIA-MAdrid`. Hasta entonces no se valida/mergea nada que dependa de CI.
- **[MENOR MANU] Publisher Verification Azure — Microsoft 365** — _verificado 2026-06-22: el App Registration YA está operativo en prod_ (provider `microsoft-365` activo, `MICROSOFT_CLIENT_ID/SECRET` en Dokploy, **1 conexión `active` real** — AgentesiaLab desde 2026-06-09). El alta de Azure y el OAuth funcionan. **Solo queda la Publisher Verification (cosmético)**: sin ella, tenants M365 corporativos ven aviso "app no verificada del editor" al conectar Outlook (Outlook personal y la conexión actual funcionan sin ella). Pasos: portal.azure.com → App Registration → Personalización de marca → Verificar dominio del editor → añadir `tufacturaia.com` (registro DNS TXT) → MPN si lo pide. ~1-7 días hábiles. No bloquea uso.

- **[OPCIONAL MANU] Subir tier OpenAI (TPM 30k tope) para el bot WhatsApp** — _verificado 2026-06-22: ya NO es problema activo — 0 errores de rate-limit en 30 días_ (`bot_error_log`: 45 errores en 14d, ninguno 429/TPM). La mitigación (prompt −38% + memoria 10→8) lo resolvió en la práctica. Subir a Tier 2 (450k TPM) en platform.openai.com sigue siendo la solución de raíz, pero ya no urge.
<!-- RESUELTO 2026-06-22 (verificado por screenshot): Supabase Auth URL Config OK — Site URL = https://app.tufacturaia.com + 5 redirect URLs (app.tufacturaia.com/**, /invitacion, /invitacion?org=*, /api/auth/callback, /api/auth/callback?type=*). NO se añade localhost:3000 a un proyecto PROD (superficie de ataque innecesaria; el wildcard de prod ya cubre, y el reset usa admin.generateLink con URL propia). Cerrado. -->

- **[IMPORTANTE PSD2 — pre-cutover live] Compliance legal con Dani antes de abrir conexión bancaria al público** _(verificado 2026-06-22: la integración TÉCNICA está COMPLETA y probada — Tink + TrueLayer, 24 consents en 3 orgs internas, todos revoked/pending/expired → NO abierto al público. El bloqueo es 100% legal, lo de abajo)_. **2026-07-01: se suma Salt Edge como 3er provider** (PR #610 draft, ver NEXT) — el mismo gate legal (DPA/Privacy Notice/DPIA por encargado del tratamiento) aplica también a Salt Edge antes de exponerlo a usuarios:
    - Revisar `docs/decisions/ADR-002-psd2-data-retention.md` y archivar (base legal retención 7 años Ley 58/2003 + Ley 11/2021 + Código Comercio art 30). Sin esto, frente a inspección AEPD o derecho al olvido GDPR art 17 vamos a defender ad hoc.
    - **Privacy Notice** del producto debe mencionar: (a) "Procesamos datos bancarios PSD2 via Tink AB (Visa, Suecia) y TrueLayer Limited (UK + EU passport) como encargados del tratamiento art 28 GDPR"; (b) "Retención 7 años por obligación fiscal"; (c) "Email para ejercer derechos: privacy@tufacturaia.com (endpoint art 15 disponible en /api/conciliacion/banks/[id]/access-report)". **Sin esto el consentimiento al tratamiento está viciado**.
    - **DPA art 28 GDPR** firmado con Tink AB (template estándar suyo, ~10 min). Lo mismo TrueLayer.
    - **T&C de TuFacturaIA** mencionan "banca abierta PSD2" + "solo lectura, no operativa".
    - **DPIA art 35 GDPR** (Data Protection Impact Assessment) — obligatoria para "tratamiento sistemático a gran escala de datos financieros sensibles". Plantilla AEPD, 4-8h redacción. Sin esto, infracción objetiva GDPR (multa hasta 10M€ o 2% facturación). Necesario antes de >100 orgs con banca conectada.
    - **Pentest** del flow PSD2 antes de cutover live (~3-5k€ outsourced o 2-3d con OWASP ZAP / Burp si in-house).
    - **Solicitar app live Tink** en console.tink.com (proceso self-service standard tier; pueden pedir info corporativa + Privacy Notice + DPA firmado).
    - **App live TrueLayer** si la mantenemos como fallback (más farragoso que Tink, pueden pedir review humana de la app — 3-5 días).
- **[ACCIÓN — exigible YA, NO aplazada] Declaración Responsable de fabricante de SIF (RD 1007/2023, art 13)** — _verificado en sede AEAT + Orden HAC/1177/2024 art 3, 2026-06-22_. Aplazamiento RDL 15/2025 = solo USUARIOS, NO fabricante. Autocertificación (no se presenta a AEAT): in-app visible por versión + PDF descargable. Decir "adaptado al RD 1007/2023", nunca "homologado". **Auditoría de código hecha (2 agentes)**: de los 2 "bloqueantes" detectados, (1) **registro de eventos + firma de registros NO aplican** porque somos solo-Verifactu (Orden art 3 exime arts 8/9 y 6.c/d/14); (2) **inalterabilidad (art 8.2.a) cubierta con mig `376_facturas_inalterabilidad_verifactu.sql`** (trigger BEFORE UPDATE, congela contenido fiscal en cadena + huella tras aceptación; **PR #449**, **pendiente merge + `supabase db push`**). **Antes de firmar, cerrar tareas técnicas (no bloqueantes legales):** ~~residuo 1 céntimo huella~~ → _auditado 2026-06-22: NO es bug vivo, ambas rutas (RPC atómico+091 / emit trigger 303:348) calculan con Σ; la rama NOOP es inalcanzable por el advisory lock_; quedan validación XSD oficial, smoke real contra entorno `pre` AEAT, confirmar encadenamiento por-serie vs por-obligado con asesor. Fragilidad Σ duplicado: RESUELTA — totales unificados en #869 (`lib/documents/totales.ts`) y worker/trigger VeriFactu en Fase 5 (`lib/verifactu/cuota.ts`, PR #947). Análisis referenciado: `docs/compliance/centro-fiscal-pre-beta/SIF-declaracion-responsable-analisis.md`. (P12 por org y USO de Verifactu sí diferidos a 2027.)
- **[Centro Fiscal IA — externos legales pre-beta]** acción Manu, NO dev:
    - **FNMT TS@ vía CERES** — plantilla email lista en `docs/runbooks/fnmt-tsa-email-solicitud-convenio.md`. Convenio bilateral 2-6 semanas. Modulo Centro Fiscal IA funciona SIN sellar (wizard + cálculo + cuadres + PDF + libro IVA OK), solo "Marcar presentada" falla. Backblaze B2 WORM ya activado 2026-05-22.
    - **Seguro RC Tech E&O 1M€** — Hiscox/AXA/Generali, ~600-1.200€/año. Bloqueante GA Centro Fiscal IA.
    - **Abogado IT** — Términos B2B + B2C + DPA art 28 + Política privacidad + Runbook brecha RGPD art 33-34, 3-5k€. Bloqueante primer beta-tester.
    - **ZDR (Zero Data Retention)** OpenAI + Anthropic por ticket — gratis con DPF SCCs. Bloqueante primer beta-tester.
    - ~~**Stripe price IDs** crear~~ — _verificado 2026-06-22: el add-on **Centro Fiscal IA 14,90€/mes + 154,80€/año YA existe** en Stripe LIVE_ (`prod_UcjNQFyEkZ84eM`). Planes 19/49/99 + anual también live. Cubierto.
    - **Auditoría externa pre-beta** REAF 3k + abogado IT 4k + pentest 5k = 10-15k€ total.
<!-- Dani: DNS tufacturaia.com DNS-only resuelto 2026-05-16, SSL OK en app. y n8n. -->
- **Dani + Gonzalo**: subir certificado P12 por org en Ajustes → VERIFACTU. Sin él no se envían facturas a AEAT y aparece error en dashboard
<!-- Meta plantilla otp_facturaia APPROVED 2026-05-16 (language es_ES). Bloqueo cerrado. -->

## Decisiones pendientes (producto)

- **Ofrecer skin "Cristal" a usuarios** — QA contraste/APCA en pantallas densas OK (2026-07-02, oscuro+claro); queda solo la decisión de negocio de exponerlo.

- ✅ **Cutover cola OCR — RESUELTO (2026-07-08)**: ejecutado por #802 (no #800) — mig 449 aplicada a prod + cron `ocr-dispatcher` dado de alta y verificado. Queda solo el smoke (ver Smoke) y cerrar el PR #800 duplicado. Detalle en NOW.

- **Paso 2 del auto-OCR: calidad de extracción vs fricción de aprobación (2026-07-08)** — backfill de `facturas_ok` y bajar umbral 3→2 **descartados por datos** (dry-run: madura 0 proveedores; solo 3 con ≥2 recibidas limpias, 31 limpias en total). Raíz real: **86% de recibidas en `sin_aprobar`** (230/268) — no se aprueban, nada madura, nada va a verde. Dos vías: (1) **calidad de extracción** (endurecer prompt `ocr-process` + evaluar upgrade modelo vision; ~20% docs con num/fecha/total vacíos) — recomendada, útil independientemente del volumen de aprobación; (2) **fricción de aprobación** (por qué 230 recibidas sin aprobar) — es la raíz del auto-OCR inerte, pero producto/UX, no un knob. Ver [[gate-agentico-que-no-dispara-suele-estar-inanido-no-mal-calibrado]].

- **ClaveRegimen VeriFACTU pre-cutover** — hoy SIEMPRE '01' (el read-path de settings nunca funcionó); el dato existe en `settings.regimen_iva` ('recargo_equivalencia', no códigos AEAT). Decidir fuente canónica + mapeo antes del cutover o una org en criterio de caja enviará '07' como '01'. Ver gotchas.md §VeriFACTU (PR #648).
- **Auditoría de acciones admin GLOBALES** — nunca han quedado registradas (`audit_log.org_id` NOT NULL, insert fallaba en silencio; hoy early-return). Opciones: org_id nullable vs delegar a `logSystemAction`→`admin_audit_log` (sink ya existente). Ver gotchas.md §Auth.
- **Reempaquetado planes — casi cerrado** (act. 2026-06-26): mig 399 (#509) + Fase 2B (#513) aplicadas a prod ya cubrieron ~~grandfathering~~ ✅ (PASO 1, override `source='grandfathered'`), ~~Starter canónico 14€~~ ✅ (PASO 0, reconciliado desde 19), ~~sidebar candado~~ ✅ (#513). **Queda vivo**: (1) **crear prices Stripe live de Plus** (mensual 29 / anual 278,40) + rotar env `STRIPE_PRICE_ID_PLUS_*` — **acción Manu**, sin esto el tier Plus no es comprable (Starter 14 ya creado); (2) verificar que `stock` en beta no se cobra como add-on de pago (mig 399 PASO 6 tocó incoherencias de add-ons — confirmar). Detalle: [[facturaia-reempaquetado-planes]]

- **WhatsApp en starter — confirmar pricing** — desde 2026-06-01 starter incluye `generador_voz`+`presupuestos` con cuota `whatsapp_docs_mes`=30/mes (antes pro-only). Confirmar que es la intención comercial o revertir el toggle en `/admin/plans`.
- **Bot WhatsApp case-2 (cambiar org a media conversación, reprocesar sin repetir)** — deferido 2026-06-01: la versión que reejecutaba el agente 2× en una misma ejecución n8n no es production-grade. Rehacer reusando la última propuesta SIN re-correr el agente, con smoke. Hoy "no en la otra" cambia bien pero pide repetir.
- **Cliente live vs congelado** — hoy snapshot fiscal al crear factura (datos cliente embebidos). Si editas cliente, PDFs viejos conservan datos antiguos (legalmente correcto, confunde UX). ¿Botón "Re-emitir con datos actuales" o congelado siempre? → cuando se decida, ADR en `decisions/`
- **Feature flags experimentales globales** — debatido 2026-05-11, descartado por YAGNI (`org_features` por org ya cubre piloto/experimentos). Si en el futuro aparece caso real de "flag global para TODAS las orgs", usar `system_config` key-value sin UI dedicada hasta tener 3+ flags. NO construir abstracción especulativa
- **Cuenta Stripe — Agentesia Madrid SL vs entidad propia TuFacturaIA SL** — pendiente decisión jurídica antes de crear price IDs y abrir cobros reales. Si TuFacturaIA tiene CIF propio que firma con clientes finales, debe tener cuenta Stripe propia. Modo Test inicialmente. Bloquea P1.3 próxima sesión.
- **Pricing definitivo de Cashflow IA v2** — copy `descripcionLarga` ya refleja v2 completa (eventos + IVA + RETA + payment-pattern + bullet IA + pill crítico) pero el `precioMensual` del catálogo no se ha tocado desde v1 (19€ Conciliación). Revisar si Cashflow IA mantiene el mismo precio o sube a 24€ vs Holded Premium 30€. Ver análisis producto en plan de sesión 2026-05-23.
- **Modelo 130/IRPF en `incluir_fiscal` (Cashflow IA v3 opcional)** — hoy solo IVA 303. Diferido por análisis producto-GTM (la mayoría en directa simplificada tributa al final del año, no trimestre). Re-evaluar si clientes lo piden.
- **Fix contador de tab en `conciliacion-view.tsx` — SQL/RPC dedicado vs dejarlo** (2026-07-06) — mismo bug que ingesta/facturas/presupuestos (PRs #770/#771/#772) pero la clasificación de estado (`estadoDe()`) depende de metadata hidratada de varias tablas (sugerencias IA, anticipos, transferencias internas), no de un filtro de columna simple. Un conteo global correcto exige replicar esa lógica en una función SQL/RPC — migración nueva, más riesgo (conciliación bancaria). Ver [[contador-por-tab-derivado-de-datos-paginados-colapsa-a-0]].

## Índice de áreas (abrir solo el que toca)

| Área | Detalle / estado (vault) | Doc técnico (repo) |
|---|---|---|
| Conciliación bancaria | **[[facturaia-conciliacion-v4-plan]]** (plan maestro vigente) · [[facturaia-conciliacion-roadmap]] · [[facturaia-conciliacion-copiloto-spec]] | `docs/architecture/conciliacion-copiloto-spec.md` (parcialmente caducado) · `conciliacion-referencia-holded.md` (⚠️ su tabla de mapeo es de jun-26, NO fiable: verificar features contra el componente) |
| Stock / Inventario | [[facturaia-modulo-stock]] | `docs/architecture/modules-system.md` |
| Centro Fiscal IA | [[facturaia-centro-fiscal-ia]] | `docs/architecture/centro-fiscal-*.md` |
| Multiempresa / billing | — | `docs/architecture/multiempresa-billing.md` |
| Cobros / recordatorios | — | `docs/architecture/cobros-runbook.md` |
| API v1 ↔ portal | [[facturaia-integracion-api-v1-portal]] | `docs/architecture/gotchas.md` §API v1 |
| Integración Holded | [[facturaia-holded-integracion]] | `docs/architecture/holded-runbook.md` |
| Voz / WhatsApp | [[facturaia-bot-conversational-redesign]] · [[facturaia-arquitectura-multi-whatsapp-numbers]] | `docs/architecture/gotchas.md` §Voz |
| Copiloto IA — siguiente nivel | [[facturaia-copiloto-siguiente-nivel]] (roadmap tiers + diagnóstico adopción) | `docs/architecture/copiloto-modelo-escalado.md` |
| Emails / comunicaciones | [[facturaia-comunicaciones-emails]] | — |
| Cashflow | [[facturaia-cashflow-chart-saldo]] | — |
| Open Banking PSD2 | [[facturaia-open-banking-psd2]] | — |
| Multidivisa recibidas | [[facturaia-multidivisa-recibidas]] | — |
| Drive sync | [[facturaia-drive-sync-architecture]] | — |
| Onboarding / migración dominio | [[facturaia-migracion-tufacturaia-com]] | `docs/onboarding/PROGRESS.md` |
| Marketing/Growth (admin interno) | — | `issues/prd-marketing-growth.md` |
| Arquitectura general | — | `docs/architecture/ARCHITECTURE.md` · `dependency-map.md` |
| Changelog completo | [[facturaia-historico-detallado]] | — |

---

> ⬇️ **Lo de abajo NO se lee al arrancar.** Detalle bajo demanda: NEXT, Smoke, WIP, backlog, seguridad, specs y referencias.

---

## Smoke tests pendientes

- 🔴 **Caja y cobro + Personalizar (mig 549-551, PRs #1176/#1183-#1186, 2026-07-23)** — smoke BD/QA E2E ya verde (ver histórico). Queda UI real: (1) bulk "Marcar cobrada" >20 seleccionadas con 1 no elegible → progreso + "omitidas"; (2) revertir pago de recibida → deja de contar en cashflow; (3) import OCR emitida "ya cobrada" → fecha_cobro = min(fecha, hoy).
- 🔴 **Toast sobre modales (#1107, 2026-07-21)** — Obras→Unidades de obra (ciclo de componente) e Inventario→ajuste stock: toast debe salir delante del modal. Ver [[zindex-capa-overlay-orden-portal]].
- 🔴 **Sidebar rail plegado (#1010, 2026-07-19)** — desktop ≥1024px claro+oscuro: alineación logo/iconos, pestaña expandir visible, tooltips por icono.
- 🔴 **Módulo Obras — kit de listas paralelo mergeado a mano (`b34eb6b6`, 2026-07-18)** — verificar sin regresión en las 7 vistas (contactos/delegaciones/materiales/obras-list/presupuestos/unidades+sidebar) tras resolución de conflicto de 2 PRs paralelos. Ver [[triaje-seguro-ramas-worktrees-sesiones-paralelas]].
- 🔴 **Obras polish transversal (#1000, 2026-07-18)** — buscadores sin recortar, `EstadoPill` en Instaladores/Categorías/Materiales/Unidades, confirmación al cambiar horas en Tipos M.O. (+ guard servidor 403 `forbidden_horas`), "Mostrar dados de baja".
- 🔴 **OCR `auto_categorizar` (#990, 2026-07-18)** — toggle OFF→factura sin categoría; con ≥50 decisiones OCR resueltas, tarjeta "Precisión IA" muestra % real.
- 🔴 **Agente de cobros — WhatsApp real E2E (2026-07-18)** — único smoke no verificable sin envío real: recordatorio de vencida por plantilla Meta, quiet hours + opt-out + cuota + anti-duplicado del cron.
- 🔴 **Registro/onboarding standalone (#984, 2026-07-17)** — `/registro` columna única, wizard claro por defecto, validación en vivo — QA local OK, falta confirmar en prod.
- 🔴 **IMAP genérico (#956+, mig 466/469, 2026-07-16/17)** — cliente conecta buzón real, recibe correo con PDF, confirma ingesta + pausa/desconexión aislada.
- 🔴 **Multi-cuenta email (#937+, mig 464, 2026-07-16)** — reconectar 2ª cuenta real (OAuth interactivo) y confirmar sondeo/desconexión.
- 🔴 **PWA móvil (PR0-PR6, 2026-07-15)** — Safari iOS hardware real: home nativo, safe-area, teclado/tabbar, sheet "+", tabs Facturas/Documentos, instalación standalone. Emulado ya OK.
- 🔴 **UI polish #882-886 (2026-07-14)** — modo selección en `/clientes` tarjetas, skeleton sin flash en `/emitidas`/`/recibidas` carga fría.
- 🔴 **Crons fiscal-avisos/stock-alarmas-email (2026-07-14)** — confirmar disparo tras cambio TZ, ahora 08:00/08:05 Madrid (06:00/06:05 UTC), no 08:00 UTC.
- 🔴 **Centro Fiscal post-deploy (#825, 2026-07-13)** — `/fiscal/2026/180/<T>`, `/347`, `/303` sin error 42703; 180 pide situación del inmueble.
- 🔴 **Totales de editor con redondeo por línea (#871, 2026-07-13)** — editar borrador con subtotal no-exacto → emitir → pantalla=BD=XML VeriFactu al céntimo.
- 🟠 **Copiloto gpt-5.2 (#836+, 2026-07-11)** — 2-3 mensajes reales en prod; reversible con env `COPILOTO_OPENAI_MODEL=gpt-4o`. Búsqueda fuzzy con typo/orden cambiado.
- 🟠 **OCR no-factura (#834, 2026-07-11)** — reenviar un DNI por email → `estado='revisar'` (nunca `error`), sin alerta ALTA falsa. Ver [[llm-responde-200-no-json-no-es-fallo-pipeline]].
- 🟠 **QA loop 8 fixes (#826-833, 2026-07-11)** — 2 de mayor riesgo: badge notificaciones no cae a 0 con >50 notifs; `Idempotency-Key` compartida entre 2 duplicar-factura no cruza destinos.
- 🔴 **Retirada n8n WhatsApp — 4 smokes reales al número prod** — STOP→opt-out, extracto por foto→importa movimientos, "factúrame X" vía copiloto, cobro-confirmar por cliente final. Si OK → Fase 6 (dead-code, mapeado).
- 🟠 **ia-ops diagnóstico shadow superadmin (#798, 2026-07-09)** — funnel/motivos/maduración con datos reales, no pude QAearlo (usuario e2e no es superadmin).
- 🟠 **Cola OCR dispatcher (#802)** — lote grande drena en <1-2min, `ingesta-stale-sweep` ya no aparece huérfano.
- 🔴 **Marketing/Growth infra (#799/#801, 2026-07-08)** — `/admin/marketing/limites` persiste, `/admin/marketing/auditoria` carga vacía sin mutaciones.
- 🔴 **WhatsApp lote de fotos (#796/#797, 2026-07-08)** — sin "Cambiar empresa" con 1 sola membership, sin doble procesado.
- 🔴 **Polish Ajustes (#684)** / **Polish Auditoría (#688)** — sin QA visual en prod light/dark/skin Cristal. #688 además: extraer patrón `isInitialLoad` compartido (duplicado en 3 componentes) + `PaginationBar` no deshabilita su Select durante loading.
- 🔴 **Export/bot QA — 3 restantes** — modal "Facturas incluidas" del 303 abre factura; menús canónicos Gestoría en prod; Pre303 HITL con XLSX AEAT real.
- 🔴 **Empresa/nombre + dropdowns + footer factura (#665-671, 2026-07-03)** — footer del modal completo, descarga con nombre correcto de documento (solo verificado en local).
- **Conciliar "Eliminar movimiento" (#663)** / **"Asignar cliente" en emitida (#659)** — deploy Dokploy confirmado, falta smoke `app.tufacturaia.com`.
- 🟡 **Drawer notificaciones — badge "Fiscal N" vs render** (2026-07-02, bajo impacto, sin confirmar) — suma de tabs cuadra, probable límite de paginación del drawer.
- 🟡 **`/login` botón submit programático** (2026-07-05, bajo impacto) — solo responde a Enter, no a click programático.
- 🔴 **Presupuestos propios en OCR — validar con doc real** — clasificación `doc_type='presupuesto'` no probada con documento real (solo sintético, inconsistente). El intento real (2026-07-23, `ELPHIS. 23022026 30 días.pdf`) ni siquiera llegó a OCR — lo bloqueó el bug de nombre NFD (ver línea siguiente); repetir tras el fix.
- 🔴 **Upload con tilde/ñ tras fix NFD (PR #1191, 2026-07-23)** — re-subir `ELPHIS. 23022026 30 días.pdf` desde la bandeja de ingesta en prod y confirmar que ya no da "Error del servidor". Ver [[supabase-storage-rechaza-key-nfd-mac-chrome]].
- **Búsqueda/filtros Clientes/Proveedores** (`77ba6322`) — falta smoke en prod tras deploy.
- **PR #609/#608 — soporte + recurrente pill + conciliar modal glass + tour** (2026-06-30) — numeración `#N` en tickets, tour 4 pasos, `/soporte` sin 500.
- **WhatsApp**: `consultar_vencimientos` sin resultados da alternativa; `factura.por_vencer` cron encola evento; voz multi-org no repite nota; multi-org S3-S5 (crearPresupuesto/Proforma/anularFactura/convertirPresupuesto/cambiarEmpresa) paridad con web.
- **Emitidas Borja Galván** (org `50a3cfbf`) — borrador serie A numera `A2026-0001` sin error.
- **Email hola@tufacturaia.com** — deploy + smoke prod + logo real en `template_config.logoUrl`.
- **Slack** (tras activar app+envs) — aviso push, `/factura resumen|cobros|cobrada`, deep-link vincular.
- **MCP connect E2E cliente real** (claude.ai/Cursor) — tools visibles, revocar → 401 también en `/api/v1/*`.
- **Calendario selector de capas** (#382) — QA visual ya hecho en local.
- 🔴 **Fiscal #517 auditoría enforcement canales — BLOQUEADO** — sin fixture (ninguna org suspended/expired en prod).
- 🟠 **Cuadres fiscales (#835) — 2 follow-ups** — smoke "Resolver aquí" del 390; bug pre-existente hub `/fiscal/{ejercicio}` redirige a `/` para no-superadmin. Ver [[gating-de-modulo-duplicado-por-pagina-centralizar-en-helper]].
- 🟠 **OCR auto-orientación + scroll-fade (#905, 2026-07-15)** — doc torcido llega derecho; scroll-fade en chips de filtros.
- 🔴 **Responder tickets** — Abba BORME (re-escanear 2 docs), bgchivite `1762f07e`.
- 🔴 **UX feedback smoke** — #528 toast al guardar, #529 modal confirmación cambio de precio.

**Pendientes tras deploy Dokploy** (front en main, requieren hardware/datos reales, backlog largo — sin fecha de revisión):
Tarjeta expandible móvil emitidas/recibidas · pills listado docs móvil · toasts repetidos Bandeja IA · control de stock desde catálogo · numeración preview sin huecos · tabla facturas móvil auto-ajuste · registro branded+onboarding · auditoría APIs #518 (roles/scopes) · ingesta USD+parpadeo+stock servicios · integraciones lista glass · calendario capas · "Resolver con Claude" resumen cliente · modal ticket ancho completo · panel admin móvil · presupuestos Bearer app iOS · conciliación editar signo/importe · conciliación motor migs 265-269 · extractos PDF flujo web · Outlook/M365 · overflow móvil hardware real · consistencia glass Ajustes · recover-password 2ª mitad · 2FA E2E · multidivisa recibidas · Stripe checkout E2E real · WA multi-org crear cliente/proveedor.

## NEXT (próximas 2 semanas)

- **Rendimiento Ola 1-3 (BD+Realtime) CERRADO en prod, smokes confirmados por Manuel (2026-07-21/22)**: auditoría diagnóstico-primero (pg_stat_statements + advisors). (539) índices FK Obras → `DELETE obras_presupuestos` 36s→ms; (540) `get_org_usage` sargable, era 22% del tiempo de BD, 567→105ms; (541) RLS initplan 319 políticas → `bare=0`/`row_fn=0` en 370 pol.; (542) RPC `resumen_negocio_agg` en SQL; (543/544, opción B) Realtime → polling, `wal->>`=0% en reposo (antes ~26%). Re-medición `pg_stat_statements` con tráfico real: hecha, resultado bueno.
- **Ola 4 FRONTEND — 6 rutas RSC en prod (camino A, rollout ruta a ruta)**: `/obras` (#1129), `/obras/presupuestos` (#1130), `/obras/pedidos` (#1133), `/clientes` (#1135), `/obras/materiales` (#1136), `/inventario` (#1137). #1137 además **extrajo a fuente única** `src/lib/stock/productos-db.ts` (`listProductosPaginado`) el modo paginado de `GET /api/stock/productos` — endpoint y server-fetch llaman a la misma función. Fix aparte #1134: `usePaginationParams` remontaba tabs/toolbar enteros al cambiar de tab (ahora `history.replaceState`, no `router.replace`).
- **`conciliacion` BLOQUEADA por BD, no por UI**: el RPC `conciliacion_estado_counts` (mig 435, `SECURITY INVOKER`) no tiene `p_org_id` — depende 100% de RLS, y el admin client de RSC la bypasa (contaría todas las orgs). Requiere migración SQL antes de poder migrarla a RSC. Ver [[supabase-rpc-security-invoker-sin-org-id-bloquea-admin-client]].
- **Camino B (Cache Components) E0-E3 MERGEADO a prod — PR #1140 (2026-07-22)**: `cacheComponents:true` + route handlers con `connection()` antes del try + layouts en `<Suspense>` (cascarón+async) + 19 `loading.tsx` + auditoría `<Activity>` (hook `useCloseOnRouteHide`, QA visual de Manuel PASADA). Build verde 492/492. Caveat UX a validar en prod: hard-load muestra un instante el fallback vacío del layout. **Incidente el mismo día (#1140→#1142)**: se mergeó con un crash real sin arreglar (visor PDF bajo `<Activity>`, dos sesiones paralelas sin coordinarse); detectado en QA post-merge y arreglado con hotfix mismo día, verificado con repro determinista. Cerrado, en `Stack/incidents.md`. Ver [[next16-cache-components-migracion-por-etapas]] · [[nextjs-activity-no-resetea-estado-libreria-externa-con-recurso-imperativo]].
- **E4 (`use cache` per-org) — MERGEADO a main y en prod (2026-07-23)**: los 6 PRs (`#1143` Conciliación · `#1144` Clientes · `#1145` Obras Materiales+Pedidos · `#1147` Dashboard · `#1149` Presupuestos · `#1150` Emitidas/Recibidas), QA manual con agent-browser en los 6 dominios (Clientes 484→545€, Conciliación 5→4 sugerencias, Presupuestos 2→1/0→1, Dashboard 2073€/6→1032€/5, Obras 6,68→5,97€, Emitidas vencidas 8→7 — todo en carga fresca). **Inventario deliberadamente SIN cachear** (correcto, verificado): sin choke-point de invalidación fiable. **`/fia-cierre` sobre el merge completo encontró 1 bloqueante real** (`seed/route.ts`: el reseed de superadmin sobre org real borra facturas/clientes/presupuestos pero solo invalidaba presupuestos) + 5 avisos reales (toast falso en marcar-estado individual de Presupuestos; 3 crons con invalidación fuera de try/catch, riesgo de falso-positivo de cron; 2 comentarios desactualizados) — **todo corregido el mismo día en PR #1178 (mergeado)**. **23-jul, otra sesión (PR #1177) detectó 305 tests rotos** (`Invariant: static generation store missing in revalidateTag`) achacándolo al merge de E4 — diagnosticado y cerrado en PR #1181 (mergeado): es un artefacto de entorno de test (los handlers se invocan directo sin el runtime de Next que monta el store), NO un bug de producción — confirmado con worktree limpio que los 89 fallos restantes tras el mock son idénticos, mismo conteo exacto, a los que YA fallaban en el commit previo a E4. **Sin PRs abiertos de esta línea.** Ver [[nextjs16-revalidatetag-exige-segundo-argumento-cachelife-profile]] · [[git-commit-sin-add-previo-commitea-index-viejo-no-working-tree]] · [[nextjs-revalidatetag-en-tests-unitarios-exige-mock-next-cache]].
- Receta RSC + estado detallado en repo `issues/CONTINUAR-perf-frontend.md` (2 correcciones el 22-jul tras encontrar el apunte falso de B). Memoria `project_facturaia_perf_ola1`. Ver [[next16-rsc-algo-mejor-navegacion-instantanea-cache-components-coste]] · [[nextjs-rsc-seed-inicial-guard-contra-estado-solo-cliente]] · [[in-select-func-fuerza-seq-scan-usar-any-array-initplan]] · [[supabase-realtime-postgres-changes-filtrar-por-tenant-y-delete-replica-identity]].
- **Obras-IA — PLAN COMPLETO listo para lanzar en otra sesión (2026-07-20)**: programa que ataca los 3 puntos donde la IA supera a WAPI (2ª pasada del manual `~/Downloads/WAPI_MANUAL_COMPLETO.md`): (1) descripción comercial autogenerada en el import, (2) clasificador de Tipo de M.O. (~745), (3) presupuesto de obra conversacional multi-turno (chat+WhatsApp+voz). **14 issues** `issues/obras-060..073` (Fase A import G5 · Fase B conversacional), **PRD** `issues/prd-obras-ia.md`, **super-prompt multiagente** `docs/architecture/obras/SUPER-PROMPT-obras-ia.md`, **prompt de continuación** `issues/CONTINUAR-obras-ia.md` (con el bloque de arranque para pegar). Tablero: https://claude.ai/code/artifact/338fd85f-33d0-4316-9b84-c8c3a8cedda3. **Decisiones HITL ya resueltas** (Manuel): enriquecimiento diferido fire-and-forget + backfill, auto-disparo por import + guard de concurrencia (imports FRECUENTES, no anuales); modelo por *tier* (no hardcode) → `LLM_PROVIDER=openai` ⇒ `fast=gpt-4o-mini` + `jsonMode` para enriquecimiento, Fase B ya en el runner `gpt-5.2`; umbral clasificador 0.55; anti-alucinación (IDs del pool BD) + sin pgvector (pg_trgm+LLM); trazabilidad de origen (humano/ia), nunca pisar humano. Reutiliza `generarBorradorPresupuestoIA` (obras-016) + registro de tools del copiloto — no reinventa. **Nada commiteado aún** (PRD/issues/super-prompt/tablero en working tree de main). Arranca con el prompt de `CONTINUAR-obras-ia.md`.
- **Landing captación `/verifactu` (lead magnet) + captación de leads + panel admin + email con diseño — EN PROD (PRs #997/#1002, mig 512, 2026-07-18)**: test interactivo "¿tus facturas cumplen VeriFactu?" (5 preguntas → nivel A/B/C con iconos SVG → factura VeriFactu simulada por WhatsApp) público sin sesión (glass real, Filson/Switzer, mesh, claro/oscuro). Endpoint `POST /api/leads` (zod + rate-limit + `insertLead`), tabla `marketing_leads` (RLS service-role), aviso por email con plantilla estándar (`lead_nuevo`, `wrapBase`) + panel `/admin/leads` (tabla + filtro + modal, en sidebar Negocio). `/fia-cierre` cazó 1 bloqueante (emojis Unicode→SVG, inviolable Frontend) + 10 avisos, todos corregidos. Smoke prod E2E verde (insert+borrado). Origen: respuesta al ebook de captación de Holded. Cajón de piezas reutilizables en repo `marketing/lead-magnets/`. **Opcional pendiente**: env `MARKETING_LEADS_NOTIFY_EMAILS` en Dokploy (hoy avisos → info@agentesia.madrid). **Ideas futuras** (no empezadas): email de diagnóstico al propio lead, ebook propio con ángulo OCR/WhatsApp, banner in-app "ya cumples VeriFactu" para retención, canal asesorías.
- **Copy anti-slop de cara al cliente — PR #996 (2026-07-18, pendiente merge)**: barrido quirúrgico del em-dash como tell de IA en marketing (lead-magnet VeriFactu, añadido a git), emails (`email_copy` 19 kinds + snapshots), copy in-app (~24 strings/15 archivos) y `manual-usuario.md` (~64). Guardarraíl: `docs/architecture/copy-humano.md` + routing + inviolable en CLAUDE.md. Gates verdes. **Pendiente: (a) merge #996; (b) de-slop de `manual-admin.md`** (interno; lancé subagente pero NO llegó a escribir — 268 rayas siguen; rehacer). Ver [[copy-espanol-raya-tell-ia-dominante]].
- **TuFacturaIA — AUDITAR cobertura de tools MCP/copiloto/v1** — matriz capacidad × superficie (MCP `services/mcp-server/` + `tools-manifest.ts` · copiloto WA/web · API v1 · acciones web) por dominio (facturas/presupuestos/clientes/stock/cobros/conciliación/fiscal/empresa). Detectar tools ausentes/duplicadas/huérfanas y divergencias de permisos (role-matrix TS vs SQL), respetando fronteras deliberadas (emitir/anular = deep-link, no tool; scope `*:issue` no representable — gotchas §MCP). Método: agentes en paralelo (1/superficie) → síntesis. Entregable: matriz + gaps priorizados, sin implementar. Prompt completo en la sesión 2026-07-14.
- **TuFacturaIA — follow-up totales (NO bloqueante, tras #869)** — unificar el redondeo del `subtotal` POR LÍNEA persistido: `buildLineasFactura` (create-document/update-presupuesto) lo guarda sin redondear (NUMERIC 14,6) mientras `facturas/[id]/route.ts:191` lo redondea a 2 dec; el worker VeriFACTU lee ese subtotal. No descuadra hoy (el redondeo por grupo lo absorbe) pero blinda edición de borradores. PR dedicado. Ver [[totales-multilinea-redondeo-por-grupo-de-iva-una-sede]].
- **Copiloto IA — roadmap "siguiente nivel" MERGEADO (2026-07-13)**: Tier 0 (telemetría #847, feedback 👍/👎 #852, panel calidad #867, eval:mine #859) + Tier 1 (multimodal foto→gasto #864, motor proactivo #865) — 6 PRs en main + deploy, mig 458 aplicada, cron `copiloto-insights` creado en Dokploy (08:00 Madrid), manual actualizado #868. QA: multimodal y feedback verificados E2E en prod; cron `copiloto-insights` verificado (runManually→success, 08:00 Madrid). Auditoría de crons: 41 schedules OK, TZ de cashflow-alerts/fiscal-avisos/stock-alarmas corregida a Madrid. Detalle y decisiones en [[facturaia-copiloto-siguiente-nivel]]. Learnings previos: [[evals-de-agente-descartan-features-que-parecian-necesarias]] · [[gpt-4o-era-el-modelo-real-en-prod-sin-saberlo]].
- 🟠 **Switch de cuadres roto — FIX en PR #810 (abierto 2026-07-09, pendiente merge)**: el `switch (cuadre.tipo)` del drawer y `defaultBuildHref` comparaban por nombre semántico pero `cuadre.tipo` siempre es `C-NN` ⇒ todo caía al genérico, resolvers inline = código muerto. PR #810 arregla: `accionInline()` acepta código+semántico, cablea C-01 (emitir) y C-02 (verifactu→`/api/verifactu/retry`, la ruta anterior no existía), borra ResolverProveedorNif/ClienteVies (recibían ids de FACTURA pero pegaban a rutas de proveedor/cliente inexistentes) → C-06/C-07 quedan en genérico. QA antes/después con harness local mock (dev). lint/tc/build/302+4 tests ✓. **Follow-up (otro PR, requiere migración)**: la RPC SQL `fiscal_calcular_cuadres` (mig 150) emite nombres semánticos para C-01..C-04 y el TS códigos `C-NN`; `persist.ts` no deduplica → posible cuadre duplicado. Alinear RPC a `C-NN`.
- 🟠 **Smoke visual drawer cuadres (#809 + #810) pendiente**: la org sandbox no tiene módulo fiscal (redirige `/`); QA visual real del drawer requiere org con fiscal habilitado + declaración con cuadres. Verificado por comportamiento vía harness mock, no en el entorno fiscal real.
- **Auditoría `audit_log` — Bloque 4 pendiente (baja prioridad)**: Bloques 1-3 CERRADOS 2026-07-04 (7 PRs: #685/#690/#691/#693/#694/#696/#698, ver [[facturaia-historico-detallado]]). Queda Bloque 4: cobros (opt-out/send-now/orchestrator), facturas recurrentes, remesas SEPA, settings menores (email-config/features/module-suggestions/whatsapp/avatar), inventario/importar — todos con `logAgentAction` explícito (decisión ya tomada, mismo patrón). Detalle en [[facturaia-auditoria-huecos-restantes]].
- **Follow-ups export gestoría (audit 2026-07-03, PRs aparte)**: export en vista anual `/fiscal/[ejercicio]` (extraer `ExportGestoriaMenu`) · descarga para el asesor en el share link (token-scoped, análisis de seguridad) · fetch+blob en las 4 descargas del header del detalle (en /informes ya está) · ¿export con declaración en borrador? · espejo del deep-link en MCP `consultar_iva_trimestral` (copiloto ya hecho en #657). v1.5 completa speceada en [[facturaia-export-gestoria-v15]].
- **Follow-ups review #648 (deuda menor, no urgente)**: helper `asEmitida()` fail-loud para el invariante num/huella (6 sitios con `?? ''`); consolidar resolver template_config en `buildTemplateConfig()` (update-presupuesto no inyecta logo_url); helper `contenidoJson()` para MessageContent (4 copias); delegar audit global a logSystemAction.
- **Deuda arquitectural — 5 PRs abiertos (NO merge)** (2026-07-02): auditoría multi-agente (54 hallazgos, 47 catalogados). #643 ronda 1 (7 fixes) · #644 ronda 2 (dead-code, dedup, `database.types.ts`+`gen:types`, 19 `as any`) · #645 drawer piloto (8 submodales extraídos 2563→2086 LOC) + glass legible (glass-bg-strong) + pulido; QA visual + contraste Playwright 13-17:1 AA OK · #646 IRPF retenciones reales en modelo 130 (era hardcoded 0; **revisión fiscal de abonos pendiente**) · #647 signing quita fallback de clave (footgun). Tipar cliente Supabase global = migración aparte (300+ errores), ver [[supabase-tipar-admin-client-global-cascada-300-errores]]. **Pendiente: revisar + mergear los 5.**

- ✅ **Reorg split + auditoría NOTES.md** — CERRADA (2026-07-05). [[facturaia-historico-detallado]] · [[split-verbatim-checker-parts-por-basename]] · [[gh-pr-merge-no-confirma-verificar-state-merged]]

- **Salt Edge — 3er provider PSD2 (PR #610 draft, NO mergear)** (2026-07-01, re-revisado 2026-07-05): `SaltedgeProvider` implementado y code-reviewed (5 agentes, 7 hallazgos, 5 corregidos) detrás del contrato `Psd2Provider` existente — cero rutas nuevas. **Bloqueado por**: (1) **bug real de fuga cross-tenant en `exchangeCallback`** (`src/lib/conciliacion/psd2/saltedge.ts:1014-1053`) — no valida que el `connection_id` del callback pertenezca al `customer_id` de la org que inició el flujo (Salt Edge no usa OAuth, a diferencia de Tink/TrueLayer); sin fix, una org podría traerse cuentas/movimientos bancarios de otra, ver [[psd2-callback-connection-id-sin-oauth-debe-validar-dueno]]; (2) colisión de numeración: migración `423_saltedge_provider.sql` choca con `423_catalogo_servicios_created_at.sql` ya en main (main va por 431) → renumerar a 432+ al reabrir; (3) aprobación de Test access en el dashboard de Salt Edge (pendiente, sin ETA) → validar el flujo real una vez la tengas; (4) decisión de producto — exponer Salt Edge en el picker de banco (`bank-picker-modal.tsx`, hoy hardcodeado a Tink) o dejarlo solo disponible vía API. Ver [[saltedge-integracion-multi-provider-contrato-existente]].

- **Deepening tools copiloto (candidato #1) — núcleo CERRADO en main**: #536 schema único (Zod→JSON derivado), #538 port `CopilotoStore` (5 tools destructive), #539 email (8 tools en total). Pendiente: smoke API real del copiloto (bloqueado por saldo Anthropic en la key); duplicar/convertir/editar quedaron fuera a propósito → familia crear-documento (ver entrada "Refactor `documents`" abajo). Roadmap [[facturaia-arquitectura-candidatos-deepening]].

- **Arreglar 14 tests rojos `pending-action/execute` (regresión #517)** — el mock de `getOrgBilling` no implementa `.single()` en la cadena (`admin.from().select().eq().single`), introducido por el gate de billing de #517. Reproducible en `origin/main` limpio. No es de #518. Actualizar el mock-admin del test.

- **Reempaquetado de planes — acción Manu pendiente**: crear price IDs Plus en Stripe live (mensual 29 / anual 278,40) + rotar env `STRIPE_PRICE_ID_PLUS_*` (gotcha PEM → panel UI). Sin esto el tier Plus no es comprable. Resto en prod. Spec: [[facturaia-reempaquetado-planes]]

- **Refactor `documents` (deep module) — Fase 2 pendiente** (fase 1.1-1.2 ✅ en main, #463). Bloqueada por ratificar con gestoría el criterio NIF=A ([[nif-receptor-obligatorio-solo-3-supuestos-verifactu]]) → luego migrar voz/copiloto al gate único + salto `prepare/commit`+ports (con plan). RFC `issues/rfc-unificar-creacion-documentos.md`.

- **SEPA devoluciones — pendiente QA + golden-file** (Fase 1 ✅ en main + mig 385 en prod, #470). Falta: (1) QA del flujo en localhost/deploy (recargar /remesas, subir retorno); (2) ⛔ golden-file pain.002 REAL de BBVA (Dani) → `tests/fixtures/sepa/retorno-bbva-real.xml`. C19.14 diferido. [[sepa-pain008-remesa-adeudo]]

- **Fiscal etapa 2 — calculadores** (captura 016/019/021/025 + país/casilla 59 ya en prod, PRs #164/#166):
  - ~~017 (modelo 349): calculador~~ ✅ **HECHO 2026-06-10** — calculador #172 + IA #173 + mig 250 en prod. Fast-follows pendientes: export fichero posicional AEAT, drill-down por registro, captura auto-intracom OCR. [[facturaia-historico-detallado]]
  - **SPEC 349 VERIFICADO (BOE Orden EHA/769/2010 + mod. HAC/174/2020; AEAT manuales)** — NO re-sourcear:
    - **Claves (Anexo):** `E`=entregas intracom exentas (no triangular) · `A`=adquisiciones intracom sujetas · `T`=entregas subsiguientes a adquisición exenta (triangular) · `S`=prestaciones intracom servicios · `I`=adquisiciones intracom servicios · `M`=entregas subsiguientes a importación exenta (art 27.12 LIVA) · `H`=como M por representante fiscal (art 86.3) · `R`/`D`/`C`=consigna (transferencias/devoluciones/sustituciones, HAC/174/2020). (El enum 016 solo tiene entrega_bienes/adquisicion_bienes/prestacion_servicios/adquisicion_servicios → mapear/extender.)
    - **Periodicidad (Art 10.2):** mensual por defecto; **trimestral si entregas de bienes + prestaciones de servicios a consignar ≤ 50.000 € (IVA excl.)** ni en el trimestre de referencia ni en cada uno de los **4 trimestres anteriores** (ventana 5T).
    - **Plazo:** mensual 1-20 mes siguiente (julio→agosto+1-20 sep; diciembre 1-30 ene); trimestral 1-20 mes siguiente (4T→1-30 ene).
  - **018** (347), **020** (111/190), **022** (115/180), **023/024** (presentación 3b) — siguen BLOQUEADOS por sourcing del diseño oficial de cada modelo. 3b gated por base legal del conducto + seguro RC (D-013).

- **Onboarding invitación — cabos tras auditoría 2026-05-27** (código base ya en prod, `3fd402e`): (1) **subir TTL del enlace Supabase** 3600→86400 (panel Auth → Email OTP Expiration; manual o PAT) — mitiga "tardó >1h"; (2) **rediseño token-landing anti-prefetch** (fase 2) — único fix definitivo del pre-consumo por escáneres de email, ver [[magic-link-un-solo-uso-lo-preconsumen-escaneres-email]]; (3) **rotar `SUPABASE_SERVICE_ROLE_KEY`** (se vio en transcript del 27/05, exposición local); ~~(4) robustez "password puesta a mano"~~ ✅ **CERRADO 2026-06-09** — `layout-server.tsx` redirige a `/invitacion` si hay membresías `invitado` sin ninguna `activo`. Commit `07e8c92`.
- **[DEUDA recover-password silencioso]** (2026-06-04) — el endpoint `/api/auth/recover-password` devuelve 200 anti-enumeración SIEMPRE, tragándose fallos de envío reales (así estuvo roto el reset sin que saltara nada, ver [[facturaia-historico-detallado]] 2026-06-04). Hacer visible el fallo interno (alerta/log estructurado/metric) **sin** romper la anti-enumeración hacia el usuario. Mismo patrón aplicable a cualquier endpoint con respuesta genérica por seguridad.
- **Centro fiscal IA (módulo Pro add-on)** — análisis completo + plan congelado 2026-05-19 tras revisión de 4 agentes (arquitectura/legal/producto/research AEAT). Cubre 303/390/130/111/115/180/190/347/349 + libro IVA XLSX + export asesor A3/Holded. v1 cuadre+export (NO presenta), v3 presenta como colaborador social RD 1065/2007 (gestión convenio AEAT a arrancar en paralelo). Naming "Centro fiscal IA" en v1, "Agente fiscal" reservado v3. Add-on comprable sobre Pro (`addon_purchasable=true`, ~9 €/mes a validar). **Mig nueva** (número que toque al arrancar, repo en 104 hoy) crea `fiscal_declaraciones` + `fiscal_declaracion_snapshot` (céntimos enteros, no float — anti-drift IVA mixto) + `fiscal_plazos` (seed 2026 con festivos) + `fiscal_avisos`, RLS por org+rol contable/admin/propietario, calculador versionado `src/lib/fiscal/calculadores/v1/...` periodificado por COALESCE(fecha_operacion, fecha) (mig 087), RPC con `pg_advisory_xact_lock` anti-race A-fiscal análoga al cierre. **Sprint 1 incluye WORM Object Lock** (S3 Compliance o Backblaze B2; Supabase Storage NO sirve, service-role puede borrar) **+ sello tiempo eIDAS FNMT** (~0,01-0,05€/sello, valor probatorio art 42 UE 910/2014). 6 sprints + beta privada 2 trimestres + auditoría fiscal externa ~2k€ pre-GA. **Decisiones pendientes al arrancar**: precio exacto, proveedor WORM, proveedor sello, asesoría auditora. Spec completa: [[facturaia-centro-fiscal-ia]]
- **Migración a `tufacturaia.com` — pendientes menores** — F1+F2+F3 completos (2026-05-13), SSL Let's Encrypt OK desde 2026-05-16, 7 crons configurados, mig 074 aplicada, smoke OTP cubierto via Phone State Guard v2 (exec 165+169). **Pendiente sin urgencia**: `tufacturaia.com` raíz responde 404 (falta redirect a `app.tufacturaia.com`), `www.tufacturaia.com` sin cubrir en cert (decidir si se añade). Spec: [[facturaia-migracion-tufacturaia-com]]
- **Multi-org selector para bot WhatsApp (resolución determinista user→org)** — detectado 2026-05-18 00:30 durante smoke E2E post-fix URLs. Hoy `Buscar Org Voz` resuelve `phone → user_id → org_members[0]` sin orden ni filtro de feature, retorna una org arbitraria → bot revienta si esa org no tiene el feature requerido (`generador_voz`, etc.). Caso real: user `m.delmonte.p@gmail.com` apuntando a `Empresa TEST` (sin voz) en vez de `agentesia.madrid` (con voz). **Diseño limpio**: regla "1 phone → 1 user único → N orgs". Pasos: (1) extender `/api/internal/whatsapp/resolve-context` para devolver `orgs[]` con `{id, nombre, plan, features_activos}` filtradas por features candidatos; (2) `Buscar Org Voz` cambia a HTTP node llamando ese endpoint; (3) si `len(orgs_con_feature)==1` continúa; (4) si `len>=2` el bot envía mensaje WhatsApp con botones (max 3) "¿Para qué empresa? 1. X, 2. Y, 3. Z" + estado conversacional nuevo (`chat_session.pending_org_selection`); (5) reply del user resuelve `chat_session.selected_org_id` con TTL N min para no preguntar cada mensaje; (6) si `len==0` responde "ninguna de tus empresas tiene este servicio". **Soluciona también el feature gating por plan**: Starter sin voz + Enterprise con voz convive limpio. Ojo si user tiene >3 orgs candidatas: WhatsApp solo permite 3 botones, fallback a lista numerada por texto. ~2-3 días dev + n8n + tests. Riesgo medio (toca el flujo más crítico del bot). **Pre-requisito barato**: limpieza duplicados `profiles.phone` legacy (UNIQUE parcial solo cubre `phone_e164 WHERE verified`, el legacy `phone` permite duplicados → arreglar in-place caso a caso). Spec: pendiente de crear en `knowledge/projects/agentesia/facturaia-bot-multi-org-selector.md`.
- **Sistema comunicaciones email (Resend)** — 5 PRs secuenciales: mailer core + auth via Supabase SMTP, outbox unificado + 8 plantillas, notifications multi-canal, módulo + UI cliente, admin UI + custom domain + 3 IA. Spec: [[facturaia-comunicaciones-emails]]. Bloqueado por setup Resend manual (cuenta + 2 dominios + DNS + Supabase SMTP + Dokploy env). ~3 semanas total
- **Stripe en activación add-ons** — hoy CTA "+XX€/mes" redirige a `/settings?tab=plan` sin cobro. Conectar checkout para que toggle = compra. Conciliación 19€ y Anti-fraude 9€ ya seedeados
- **Manuales actualizar Bloque 1** — `manual-usuario.md` y `manual-admin.md` describen flujo viejo: 2 botones generar (no 3), no mencionan anular ni tab Abonos ni nuevos pills/badges
- **Abono ligado a factura real** — nueva tool n8n `consultar_facturas_recientes` para que el agente confirme "¿te refieres a F2026-0042 a Tecnocloud por 121€?" antes de generar abono. Liga vía `factura_origen_id`. ~1 día
- **Post-confirm "añadir al catálogo"** — tras crear documento, si hubo productos marcados con ✨ (no estaban en catálogo), enviar mensaje extra con botones "Sí, añadir / No". Endpoint nuevo `/api/voice/learn-product`. ~1 día
- **Cobrador inteligente** — job nocturno detecta facturas vencidas y envía WhatsApp personalizado al cliente del emisor sin intervención. Reutiliza agente WhatsApp existente. Config por org: días de gracia, tono, canales. ~1-2 días. **Impacto alto** — mayor dolor de autónomos/PYMEs
- **Alertas anomalías facturas recibidas** — al ingestar factura de proveedor conocido, comparar importe contra histórico. Si desviación >X% → "Esta factura de Vodafone es 40% más alta que la media". Coste: query histórico en pipeline OCR. ~medio día
- **OCR conversacional bidireccional** — cuando OCR no encuentra campo crítico (NIF, total, fecha), agente pregunta por WhatsApp en vez de dejar fila incompleta. Reutiliza agente conversacional. ~1 día
- **Auto-categorización de gastos** — clasificar facturas recibidas leyendo histórico del proveedor: "facturas de Vodafone siempre van a Telefonía". Pedir confirmación solo cuando hay duda. ~1-2 días
- **Centralizar OCR en un solo workflow n8n** — hoy hay 2 paths: receptor v2 (inline OCR para WhatsApp) y zf2 OCR Factura (HTTP webhook para `/ingesta` upload + email polling). Refactor: que `/api/upload` y `process-attachments` llamen a un único endpoint o consoliden la lógica OCR en un sub-workflow compartido. Reduce deuda + futuras rotaciones de key se hacen en 1 sitio. ~medio día. Apuntado tras incidente 2026-05-12 (key revocada rompió zf2 silente mientras receptor v2 seguía OK)
- **🔴 Telegram Bot — implementar canal real, prioridad alta** (detectado 2026-07-01 en auditoría UX `/agentes#canales-ingesta`): la tarjeta "Telegram Bot" prometía "+ Configurar bot" pero no hay backend detrás (sin bot, sin endpoint) — guardar ahí no hacía nada, parecía funcionar y no funcionaba. Se marcó como "Próximamente" en la UI mientras se construye de verdad. Falta: bot Telegram (webhook), alta como provider en la plataforma de integraciones (`src/lib/integrations/providers/`) con su propio flujo de ingesta análogo a Gmail/iCloud. Ver `agentes-view.tsx` (`CHANNELS`, id `'telegram'`, `comingSoon: true`).
- **Per-org email domains (Resend Fase 2)** — cada org verifica su propio dominio en Resend para que emails salgan desde `@<su-dominio.com>` con DKIM real del cliente (no `via TuFacturaIA`). Stack: tabla `org_email_domains` (RLS por org) + 4 endpoints API + UI `/settings/emails` con form + DNS records copiables + cron daily re-verificar pending + lógica envío con fallback a `facturaia.com`. ~4-5 días puros. Feature del plan Pro/Enterprise (justifica add-on). Diferido hasta que un cliente lo pida explícitamente — aditivo, no rompe nada migrando después. Spec base: [[facturaia-comunicaciones-emails]]
- **Diagnóstico inteligente rechazos AEAT** — hoy errores AEAT llegan como código técnico. Mejorar para que IA explique en lenguaje llano qué falló y cómo corregir, en el modal de la factura. Reutiliza `ai-validate.ts`. ~horas
- **Cashflow IA v3 — cerrar bucle copiloto v2 web** — registrar las 3 tools voz (`addCashflowExpense` destructive, `getCashflowForecast` read-only, `proposeCobroNudge` read-only) en `src/lib/copiloto/registry.ts` + `types.ts` + system prompt v_bump. Endpoints internos+voz ya operativos desde v2. ~1-2 días.
- **Cashflow IA v3 — features IA segundo nivel** — `simulate_cashflow_what_if` tool read-only ("qué pasa si gasto 3000€ en abril" recalcula `buildCashflowData` con array hipotético sin persistir); `detect_risk_month` + `suggest_invoice_pull` (sugerir adelantar cobro con descuento pronto pago 2% al cliente lentísimo del peor mes); `suggest_recurrente_shift` (detectar concentración >N% en un mes y proponer mover día_del_mes/renegociar). Reutiliza calculadores ya escritos. ~3-4 días.
- **Cashflow IA v3 — detalle por cliente en gráfica + drill-down día** — copy del catálogo (`descripcionLarga`) ya promete "próximas versiones". Sparkline por mes con breakdown clientes + drill-down día clickable. ~2-3 días.
- **Cashflow IA v3 — alerta delta semanal forecast_deteriora** — cron `cashflow-alerts` compara forecast hoy vs hace 7d; si peor mes empeoró >20%, notif distinta (`forecast_deteriora`) además de `caja_baja`. Reusa infra cron existente. ~horas.
- **Cashflow IA v3 — RETA detectado desde movimientos bancarios** — módulo Conciliación ve cargo recurrente "TGSS"/"Tesorería General SS" → autopopular `cuota_autonomos` con el importe detectado. Mata friction completa. ~medio día.
- **🟢 Cashflow IA v3 — detección automática de proveedores recurrentes desde facturas recibidas** (QUICK WIN ALTA IMPACTO, descubierto en audit 2026-05-23): si Vodafone/Endesa/Adobe/etc aparecen 3+ meses consecutivos como factura recibida con importe similar (CV < 0.2), proponer al usuario "¿añadir como gasto recurrente?". Backend: cron diario `cashflow-suggest-recurring` o trigger en `/cashflow` que agrupe `facturas WHERE tipo='recibida' AND fecha >= now() - interval '6 months' GROUP BY proveedor_id HAVING count >= 3` + match contra `gastos_recurrentes` activos del mismo proveedor para no duplicar + tabla `cashflow_recurrent_suggestions` con `accepted_at`/`dismissed_at`. UI: card inline en `/cashflow` "Detectamos 4 facturas de DEMO-Vodafone en últimos 4 meses (40€/mes mediana). [Crear recurrente] [Ignorar]". 1-click crea `gasto_recurrente` con importe = mediana últimos 3 meses + día_del_mes = mediana día factura + frecuencia='mensual'. Plus banner en `/recibidas` row del proveedor "Este proveedor parece recurrente". ~2-3 días. **Impacto medible**: para autónomo con 5-10 proveedores recurrentes, el forecast jul/ago subestima gastos 200-500€/mes — esta feature lo soluciona y mata la friction del setup manual. Es lo que más diferencia hace vs la competencia (Anfix/Quipu/Holded NO lo tienen).
- **Anexo II posicional 303 byte-exact** — PR dedicado, mirror MIT `github.com/hokus15/ArrendaToolsModelo303` disponible (YAML 2.426 líneas), XLSX oficial `DR303e26v101.xlsx` localizado. Cierra promesa "puedes presentar telemáticamente" en v3 Centro Fiscal. ~3-5 días.
- **API v1 — UI interna `descripcion` en formularios crear/editar factura+presupuesto** — backend ya soporta `descripcion` por línea (PR #49) pero la UI de `/generar` no la expone. Añadir `<textarea>` opcional debajo del input de concepto en `generar-view.tsx` + modales edit factura/presupuesto. Máx 2000 chars, contador visual al pasar 1500. ~medio día. Coherencia: si entra por API y se ve en PDF, debe poder editarse en UI también.
- **API v1 — `descripcion` en webhook payload `factura.created`/`presupuesto.created`** — actualmente el payload (mig 025) solo incluye `id, num, tipo_documento, total, estado, fecha` sin líneas. Si Borja u otro consumidor quiere `descripcion` post-creación sin tener que hacer GET, añadir `lineas[]` al payload + bump versión consumer. **NO incluído en PR #49** (Borja no lo pidió). ~horas.
- **Conciliación #44 — PR-A2.1 parser num factura en descripción banco** — regex `\b[ABFP]\d{4}-\d{4}\b` sobre `mb.descripcion`, lookup en `facturas.num` de la org, boost score sugerencia +0.2 + motivo `match_num_en_descripcion`. Caso real detectado en audit 2026-05-20: mov mencionaba A2026-0006 pero sugerencia apuntaba a A2026-0003 (importe igual). Confidence='alta' al match correcto con parser. ~horas. Detalle: [[facturaia-conciliacion-roadmap]] §Fase siguiente.
- **Conciliación #46 — F4 detección reembolso bidireccional** — F4 actual dispara solo desde mov negativo. Si devolución entra ANTES del cobro (import histórico desordenado, duplicate-import), F4 no dispara. Fix: trigger simétrico desde positivo nuevo → buscar mov negativo pendiente con es_devolucion=null y marcar preventiva. ~horas-1día. Edge case, no urgente. Detalle: [[facturaia-conciliacion-roadmap]] §Fase siguiente + §Deuda técnica.
- **Versionado de defaults módulos** — cambiar default global no debe sorprender a orgs sin override (snapshot del valor previo o aviso visible)
- **Log de cambios de config por org** — audit trail granular para reconstruir "quién cambió qué config de qué org cuándo"


## WIP (sesiones en curso, branches sin mergear)

- 🟠 **Importar facturas externas (emitidas/recibidas por OCR) — rama `feat/importar-facturas-externas`, NO mergeada (13+ commits, gates verdes)** — subir facturas emitidas en otro SIF como **registro espejo sin VeriFactu** (ADR-038) + subida de recibidas desde su pestaña + WhatsApp. HECHO y testeado (~96 tests): mig **470** (`registro_externo`/`direccion` + triggers huella con early-exit + RPC), núcleo `importarEmitidaExterna` + guards de anulación, OCR dual-mode, `/api/upload` dual + toggle `importar_emitidas`, modal `ImportarFacturaModal` con dropzone "Border Beam glass" ([[css-border-beam-conic-property-playbackrate]]) + botones en ambas pestañas (QA visual claro/oscuro con artifact), endpoint `registrar-emitida`, regresión 303. **Orden de despliegue INVIOLABLE**: aplicar mig 470 a prod ANTES de mergear el código que lee esas columnas (si no, rompe prod). PENDIENTE: aplicar+validar mig 470 (branch/CI; 469 la ocupó IMAP), F5-UI (panel revisión/badge "Importada"/banner), F6 WhatsApp (smoke con payload real = Manu), F7 (marca en exports + manuales), reconciliar solape con `feat/proveedor-dedup-ocr` en `ocr-process/route.ts`. Estado exacto + prompt de continuación multi-agente → memoria de agente `project_importar_facturas_externas` + PRD `docs/prd/importar-facturas-externas.md`.

- ✅ **Auditoría UX Ley de Jakob** — EN PROD (2026-07-14, PR #880 + manual #881). ListToolbar/BulkBar/modales unificados; sidebar mantiene Opción B (nav visible por defecto). [[migrar-submodal-a-modal-choca-con-escape-del-contenedor]]

- ✅ **Sidebar flotante glass + `.reveal` + secciones plegables + skeleton Clientes** — EN PROD desde 2026-07-07 (`fff933e6`+#880). Decisión: Opción B (nav desplegado, sin TTL). [[skeleton-debe-calcar-el-layout-real-para-no-hacer-snap]] · [[transform-o-filter-en-ancestro-rompe-hijos-position-absolute]] · [[glass-flotante-necesita-fondo-detras-liquid-glass-portable]] · [[full-bleed-flex-item-margin-negativo-stretch]]
- **🟢 Plan consolidación WhatsApp (2026-07-04) — #700/#701/#702 TODOS MERGEADOS** (corrección conversacional + detección/resolución proveedor, migs 430/431). wa-fase3 (cobro Stripe Connect) sigue abajo, bloqueado en acción Manu.
  - **wa-fase3 (Stripe Connect, test mode)** — rama local `feat/wa-fase3-cobro-stripe` (worktree `.claude/worktrees/wa-fase3`), **sin PR aún**:
    - **001 onboarding — COMPLETO** (commit `ebc3c219`): mig 432 `cobro_stripe_connect` + feature `cobro_stripe` (opt-in, sin pricing) + lib `stripe-connect.ts` (reusa `stripe-rest`, NO SDK) + endpoints connect/status + webhook `/api/internal/stripe-connect-webhook` (secret propio) + verificador de firma extraído a `stripe-signature.ts` (compartido con billing) + UI Ajustes›Cobros. lint+typecheck+build verdes, 30 tests.
    - **002 payment links — COMPLETO** (core `84e4edc5` + integración `b0796f7c`): mig 433 `cobro_links` + `create-payment-link.ts` (Price+PaymentLink en cuenta conectada, importe pendiente, reusa link activo) + `stripe-rest` con `Stripe-Account`. Integrado en email factura (CTA "Pagar con tarjeta" en template `factura_enviada` vía param opcional retrocompatible, web/v1 sin cambio), recordatorio (anexa al body) y resumen WhatsApp tras emitir. 243 tests, build verde. **PENDIENTE antes de merge (user-visible)**: QA localhost + manuales usuario/admin.
    - **003** gated por captura de payload real webhook Stripe test.
    - **Smoke pendiente**: crear cuenta Connect real test + onboarding→activo + emitir factura→link real. **Pausar antes de crear cuenta Connect real.**
  - **g5a-005 decomisión n8n**: audit CERRADO (0 tráfico real). Código: `fallback_n8n_agent` no existe (endpoint copiloto ya responde in-house). **Backup + plan quirúrgico listos** → [[facturaia-g5a-005-plan-decomision-n8n]] (backup en `n8n-backups/facturaia/...pre-g5a005-2026-07-04.json`). Quitar Agente Facturador + 16 toolCode + modelo + memoria; texto = relay puro al copiloto. **IRREVERSIBLE, requiere OK + hacerlo con skill `n8n-workflow-surgical-edit`.**
- **🟢 Informes de analítica (089-092) — CERRADO, en prod, mig 296 aplicada.** 4 informes en `/informes` (Ventas, Gastos, Objetivos, Actividad) con par realizado/pendiente (Ventas: "Pendiente de emitir" borradores · Gastos: "Pendiente de aprobar" sin_aprobar), margen, objetivos con sugerencia IA + widget, actividad acotada por rol. Tool copiloto `getInformeVentas`. Auditado multi-agente + Playwright (read+write) verde. Abonos excluidos en informes/dashboard/copiloto (303 sí los incluye). Detalle completo → [[facturaia-historico-detallado]] §2026-06-16. **Pendiente opcional**: smoke UI ventana "Pendiente de emitir" cuando haya borradores (cubierto por test).
- **🟡 Crear factura/gasto desde un movimiento (086-088) — A+B+C en prod, patch n8n aplicado, smoke WhatsApp en curso.** Ticket 2e79bd95 p2. PRD `issues/prd-conciliacion-crear-doc-desde-movimiento.md`. A+B cerrados. **088 slice C (WhatsApp, HITL) — SERVER en prod `51fa9c6b` 2026-06-16**: mig **297** aplicada (`wpa_action_type` += `crear_documento_desde_movimiento`), `crear-recibida.ts` fuente única (refactor de `/api/recibidas`), `buscar-movimiento-para-doc.ts`, `POST /api/internal/whatsapp/buscar-movimiento` (selector multi-org, gating, discriminadores none/multiple/needs_cliente/confirm/org_pendiente/upsell), branch en `pending-action/execute` (reclama pending delete-then-create + crea vía crearRecibidaConcilia/createDocument + concilia). **Smoke BD verde** `node scripts/smoke-088-buscar-movimiento.mjs` (7/7, mig 297 + filtrado). Tests unit+branch verdes. Manuales actualizados. **Patch n8n APLICADO** (`d2fcc81b`, PUT versionId `73cd3141`, receptor `pqSWkDIHqmSVHotB` — NO `zYcHHa8jWXB6dY5i` obsoleto; backup `ops/n8n-backups/20260616-0201-...json`). Aditivo, verificado: active, 169 nodos, conexiones intactas. Arquitectura `tipo`-emit (el agente emite `tipo:'crear_doc_desde_movimiento'`, rama en `Parsear y Calcular Totales` llama `buscar-movimiento` por HMAC + construye `wa_full`, nodo `Enviar WA Crear Doc` con credencial WA lo envía; botón `crear_doc_confirmar:<org_id>` → clon `Ejecutar Crear Doc Pendiente` → execute). Script `ops/n8n-patches/apply-crear-doc-desde-movimiento.py` (idempotente). **SMOKE WhatsApp real EN CURSO** (Borja, 2026-06-16): **gasto end-to-end VERDE** (botones → crea recibida + concilia). Bugs cazados/arreglados en caliente: (1) `d135d00e` el AI Agent emite `cliente:{id:null,…}` y el Zod `.optional()` no aceptaba `null` → 400 → "No pude buscar"; schema ahora null-tolerante. (2) `bfec64ea` UX: el mensaje de gasto incluye el concepto («PAGO A MANUEL…»); resolución de cliente de factura **por el nombre dicho** (ILIKE, lista corta filtrada — no vuelca 1000 clientes; si no existe pide su NIF), prompt n8n actualizado (live versionId `da5334e2`: tap `crear_doc_cli:<mov>:<cli>` + continuación NIF). (3) `1670a275` `texto` ya no filtra el movimiento cuando hay importe ("factura de 200 de <cliente no en el concepto>" antes daba none). **SMOKE E2E SERVER VERDE** (firmado HMAC vs prod sandbox): discriminadores multiple/none/cliente_ambiguo/cliente_no_existe/confirm + **factura buscar→confirm→execute → emitida A2026-0124 cobrada+conciliada** + gasto recibida+conciliada (docs limpiados). **Falta solo (opcional, server ya probado)**: tap-cliente→emisión por WhatsApp + **regresión** facturación normal/OCR/cobros (parser compartido). Datos de prueba en sandbox (2× −121 manuel, +200 manuel). Rollback: PUT del backup. **A/B**: smoke UI navegador VERDE (PR #269, fix céntimo + tolerancia).
  - **Modelo del bot (decidido 2026-06-16)**: híbrido — **conversación abierta para LEER** (tools `consultar_*`, ya parcial) + **acciones CONFIRMADAS para ESCRIBIR** (emitir/registrar vía botones; un LLM nunca escribe documentos fiscales sin confirmación). 088 es una acción más. El **asistente conversacional AMPLIO** ("ChatGPT acotado a TuFacturaIA+org sobre TODO el dato: fiscal, conciliación, informes, cashflow, alertas") es iniciativa propia → `Asistente IA multi-canal` [[facturaia-bloque-4-agent-query-spec]]. Acordado: plan numerado del asistente amplio tras cerrar 088 (opción 3).
- **`feat/factura-detail-split-modal`** (rama) — incluye el sprint **Seguridad/2FA** SIN commit. Bloqueador: aplicar mig 196 a prod va con el merge a main. (smoke 2FA en `Smoke tests pendientes`)
- **Rediseño UI/UX glass — 100% MERGEADO a main/prod** (PRs #170-195: 4 familias —botones V2, KPI losa, modales C2, tabs/filtros riel C1— + superficies app-wide + APCA 30/30 + toolbar + summary, coherente light+dark). Estado vivo en `docs/design/ESTADO.md`. Changelog PR-a-PR → [[facturaia-historico-detallado]].
- **Beta-feedback (PRD 001-008) completo en prod — 2 gaps menores abiertos**: (1) faltan tests de `POST /api/feedback` + `checkFeedbackRateLimit` (issue 003); (2) el badge no se reactiva tras resolución (`PATCH /api/admin/feedback/[id]` no pone `visto_por_user=false`; el email sí sale).
- **Pendiente menor (glass / CI):**
  1. **Barra-resumen** `.summary-hero` (facturas/presupuestos) sigue plana (vive en `globals.css`) — glassificar en PR corto si molesta.
  2. **Smoke prod** `app.tufacturaia.com` light+dark.
  3. **Billing Actions** ([[project-github-actions-billing-blocked]]): regenerar baselines visuales + verificar CI cuando se arregle.

- **Worktrees rescatables — TODOS resueltos 2026-07-13/14:**
  - `fix/gestor-externo-ux-gating` (role-gating gestor_externo/solo_lectura) → **rescatado deduplicado en PR #885 (MERGEADO)**: facturas/presupuestos/row-menu ya los cubría main (#880, causaban redeclaración de `orgRole`); porté solo los huecos que faltaban (conciliación, settings deep-link, onboarding welcome). QA localhost admin verde. Worktree+rama borrados.
  - `fix/informes-selector-periodo` → mergeado por sesión paralela (#873).
  - `agent-a80ef300` (disparo OCR→callOcrProcess) → superseded por la cola OCR #802 (`call-ocr-process.ts` ya en main), borrado.
  - `feat/saltedge-psd2-ais` (worktree `saltedge-psd2`) → PR #610 DRAFT, bloqueado gate legal PSD2. Conservar.
- **Ramas locales retomables (triaje 2026-06-12, regla 2 semanas)** — el resto se borró tras verificar merged/superseded:
  - `feat/fiscal-etapa2-captura` (6 commits, 06-10) — issues 019 (retención IRPF + IA OCR) y 021 (arrendamiento + ref catastral + OCR). Parte de Fiscal Etapa 2 (016-025).
  - `feat/glass-tokens` (5 commits, 06-10) + `stash@{1}` — glass overlays globales olas 1-2 + mesh + topbar scroll. **Revisar solapamiento** con el porteo glass ya mergeado; probablemente parcialmente superseded. **Decidir aquí (no parchear suelto):** `.int-row` (lista integraciones, #387) es glass sobre fondo de datos, lo que `docs/design/03-fundamentos.md` excluye (máx 3 superficies glass) y le faltan los guards mandatados (`@supports`+fallback sólido, `prefers-reduced-transparency`). Heredado de `.int-card` (06-11), no nuevo. Opciones: quitar glass de listas o añadir guards centrales en `.glass`.
  - `stash@{0}` glass-tokens-leakage-cleanup (sobre feat/fiscal-aeat).
  - `docs/design-research` (3 commits, 06-10, worktree `~/facturaia-design`) — guía bloques 3-13 + research fundamentos.
  - `feat/agentes-restyle-emitidas` (4 commits, 05-31) — cabecera estado automatización + medidor cobertura en /agentes (feature). Re-evaluar tras glass.
  - `fix/wa-boost-v1-audit-cashflow` (3 commits, 05-27) — fixes cashflow WhatsApp: doble conteo saldo, abonos en cobros, rate-limit, horizon_days. **Bugs reales sin mergear — priorizar.**
  - `stash@{2}` WIP Stripe multidivisa (sobre feat/saas-fase2-stripe-completo).
  - `test/stock-e2e` (worktree `~/facturaia-stock`) — entorno E2E stock, credenciales en `.env.local`/`.env.test` (backup 1Password).

## LATER (backlog)

- **Deepening arquitectónico — candidatos 1/3/4** — #2 (find-or-create) cerrado. Pendientes: #1 tools copiloto (45 shallow, ~1.3-1.5k LOC plomería — siguiente recomendado), #3 seam emisión voz↔web (núcleo, RFC previo a revisar), #4 lógica en route handlers (incremental). Roadmap + estado: [[facturaia-arquitectura-candidatos-deepening]]
- **Backends módulos pendientes** — Fiscal **promovido a NEXT con spec completa [[facturaia-centro-fiscal-ia]]**. Pendientes aún en LATER: Firma eIDAS, Cashflow IA forecast. ~21 opciones config con badge Próximamente
- **Conexión bancaria automática PSD2 (GoCardless BAD)** — paso 4 del roadmap, tras Norma 43 + trigger emitidas. Tabla `bank_consents` cifrada, endpoints connect/callback/sync, cron 4-6h, dedupe por `provider_transaction_id`, notif `bank_consent_expiring` 7d antes. Empezar con sandbox + 1 banco real (BBVA o Santander). 1-2 semanas. Reutiliza `movimientos_bancarios` y triggers 061 tal cual. Spec: [[facturaia-open-banking-psd2]]
- **Pasarelas de cobro (Stripe Invoice + Redsys)** — módulo aparte, no conciliación. Webhook `invoice.paid` → match por `metadata.factura_id` → estado cobrada sin pasar por extracto. Cablear cuando Stripe checkout para add-ons esté en NEXT. Spec: [[facturaia-open-banking-psd2]]
- **Factura de anticipo con IVA propio** — hoy solo existe "anticipo a cuenta" sin documento fiscal (`276_conciliacion_anticipos.sql`, en prod). Spec completa del mecanismo correcto (línea negativa con IVA congelado, no tocar `total` sin `base`) + bug real destapado en `anular-factura.ts` (`Math.abs()` rompería la reversión del abono) + bloqueante sin resolver (tramo IVA negativo en VeriFactu, verificar con AEAT/asesor). Spec: [[facturaia-anticipo-factura-propia]]
- **Asistente IA multi-canal** ⭐ **(opción 3 — EN MARCHA 2026-06-16, Fase 1 = LECTURA)**. **HALLAZGO CLAVE**: el asistente "ChatGPT sobre la org" YA EXISTE como **Copiloto v2** (`src/lib/copiloto/` — `runCopilotoTurn`, tool-use Anthropic nativo, registry + 10 tools: getKPIs, getDeudaPorCliente, searchFacturas, searchMovimientos, getInformeVentas, getCliente, explainMovimiento, analyzeMovimientoPattern, compararEmpresas, composeCobroMessage). Cubre cobros/KPIs/facturas/movimientos/informes. **PERO solo enchufado a la WEB** (`/api/copiloto/message`, withApiAuth sesión). El **WhatsApp** usa el bot de intención antiguo de n8n (`consultar_*` toolCode), limitado. → **Fase 1 NO es construir tools (existen): es el PUENTE WhatsApp → Copiloto v2**. **PUENTE SERVER HECHO + desplegado `f6d4bc5e` (DORMIDO)**: `POST /api/internal/whatsapp/copiloto` (service-auth) envuelve `runCopilotoTurn` — resuelve org (selector multi-org) + usuario por teléfono, gate `copiloto` + quota `copiloto_tokens_mes`, hilo de memoria propio (`titulo='WhatsApp'` en copiloto_threads), devuelve texto. Cero duplicación de tools. No cambia WhatsApp hasta enrutar n8n. **SIGUIENTE (con smoke, contigo)**: ruteo en el receptor `pqSWkDIHqmSVHotB` — preguntas → este endpoint y relay; acciones (crear/emitir/registrar, incl. 088) → bot de intención + botones. Acciones de escritura del copiloto (Sprint B, `destructive:true` en `types.ts`) seguirán necesitando confirmación por botones. Modelo híbrido leer-abierto / escribir-confirmado. Spec vieja: [[facturaia-bloque-4-agent-query-spec]].
- **Google OAuth email por org** — cada org envía desde su email vía Gmail API
- **Canales Ingesta + Plan/Facturación** (spec 2026-04-24) — rediseño canales sin toggles, página planes reales con método pago e historial
- **Conciliación bancaria IA** (spec 2026-04-21) — 5 tablas, pipeline Claude 2 fases, UI aprobación por lotes
- **Cleanup mockup AgentesiaLab** — plan SQL elaborado. Org `ea201784-...`
- **Multi-WhatsApp números por org** — Tecnocloud necesita su phone_number_id Meta. Spec: [[facturaia-arquitectura-multi-whatsapp-numbers]]
- **App iOS sin Swift** — Expo Go / React Native, mínima para consultar facturas recibidas/emitidas/bandeja IA. Gestión compleja → desktop. Valorar dificultad vs nativa Swift
- **Control de stock**
- **Enlace contable** para programas de contabilidad
- **Entrada y salida de caja**
- **Liquidaciones de facturas recibidas**
- **Facturación recurrente con SEPA**
- **Dashboard financiero avanzado** con predicciones cashflow basadas en patrones históricos
- **Auditar resto de Settings (bug lectura impersonando)** — empresa+plantillas ✅ (#460/#462). Revisar email-settings y cualquier sección con `createClient()`/`getOrgId()` → 0 filas o org del superadmin + smoke logo impersonando. Ver [[settings-leen-con-createclient-getorgid-se-rompen-impersonando]]
- **PR #376 Floating UI (popovers)** — 10 componentes a `useAnchoredMenu`, verde, QA 8/10. Pendiente: merge (CI billing) + QA visual `bank-connections-card` y `header-acciones` (sin datos sandbox). Rama `fix/team-member-menu-posicion-derecha`. Ver [[react-hooks-refs-falso-positivo-floating-ui]] · ADR-033
- **Fase unificación fields** — Drawers ✅ (#374). Falta: `<Field>`/clase única por tokens (`.form-input`/`.set-input`/`.adm-select`/`.input` divergen) + revisar copiloto. Smoke 2 drawers fiscal en prod. Ver [[controles-form-nativos-no-estilables-construir-componente]] · [[zindex-capa-overlay-orden-portal]]
- **Corregir ID receptor en routing `CLAUDE.md`** (1 línea) — `zYcHHa8jWXB6dY5i` → `pqSWkDIHqmSVHotB` (404; ya corregido en `gotchas.md` #368, falta la tabla de routing)
- **Stock plegado en Pro (mig 399) — smoke polish #475** — salió de beta, se incluye en Profesional (ya NO add-on 12,90€). Pendiente: smoke polish (badge coste/ud + "Por cajas") + confirmar gating en prod tras deploy #509. Ver [[facturaia-modulo-stock]]
- **Seed org test `stock-fase-d.spec.ts`** — crear org `7d9a2cfe-...` + "producto test" + bandeja `e2e00002` + factura `e2e00001` para que el gate spec pase P1-P6
- **Smoke E2E cobro empresa extra (F3b)** — código en prod (#144). Requiere pago real/Stripe test clock: comprar empresa extra → subscription item quantity + proración + webhook `billing_accounts.empresas_extra`. Hoy 409 `no_active_subscription` (complimentary). Ver [[stripe-subscription-item-resolver-por-price-no-por-indice]]
- **Multidivisa Recibidas — queda S4 + WA cutover + ISP** — S1-S3.5 ✅ en prod (mig 187). Pendiente: S4 Copiloto (`revisarBandejaMoneda` + notif FX>5% + badge) · cutover SSOT WhatsApp `pqSWkDIHqmSVHotB` · ISP intracom keywords · auditoría cross-PR 3 agentes. [[facturaia-multidivisa-recibidas]] · ADR-024
- **Cerrar bucle catálogo productos/servicios** — código en `origin/main` ✅. Falta: cableado n8n (`consultar_catalogo` + write-back `/api/internal/voice/catalogo/guardar`), mig 172 `copiloto_usage_increment` a prod, manuales
- **Fix locators spec `conciliacion-vinculacion`** — comparte bug `table[aria-label]` (está en `<div role=region>`) + `isVisible()` no espera → `[aria-label=…]` + `waitFor`. Ver [[playwright-isvisible-ignora-timeout-usar-waitfor]]
- **Setting catalog `bancario_umbral_anomalia`** (~5min) — añadir al `MODULES_CATALOG.conciliacion.settings` (~L258 catalog.ts). Sin esto el cron usa default `0.5` hardcoded
- **3 envs `.env.test` smoke middleware A/B** (~5min) — `E2E_SUPERADMIN_TEST_ORG_ID`, `E2E_UNVERIFIED_EMAIL/PASSWORD`. Commit cafc8dc
- **Coherencia `manual-usuario` vs `manual-admin` §31 Equipo** — etiqueta "Inactivo" vs `revocado/expirado`; "revocar invitación" (⋯) no en admin; "transferencia de propiedad" sin documentar. Decidir si UI existe y unificar
- **Decidir VerifACTU worker Dokploy** (hoy Disabled) — si queda off, eliminar entrada; si on, Run Now + verificar pendiente_envio→aceptada
- **OTP — patch n8n delivery status** (~5min) — key nueva en `n8n.tufacturaia.com/Settings/API` + `python3 ops/n8n-patches/apply-delivery-status.py`
- **Quitar dominio viejo `facturaia.agentesia.world`** — repo limpio; queda infra externa (DNS IONOS + entrada stack Dokploy)
- **Daily Briefing trigger** — `/daily` listo; falta cron Dokploy o trigger automático que escriba `00-home/daily-briefing.md`
- **Proteger columnas regulatorias con trigger guard** — billing+verifactu ya blindados (migs 321/162). Falta replicar para `regimen_iva`/`nif`/`iae`/`verifactu_num_instalacion` (hoy `org_member_update` deja UPDATE a cualquier miembro). Copiar trigger mig 321

---

## Ideas crudas / inbox

_(volcado sin filtrar — pasan a NEXT/LATER si maduran, o se descartan en poda quincenal)_

- ~~Calendario como agenda de trabajo~~ **HECHO 2026-07-17** — las obras afloran en `/calendario` como capa de rango. Ver §Módulo Obras.
- **Email_log Fase A** (recomendado próxima sesión, ~30min) — tabla `email_log` con insert en `send-factura`/`send-presupuesto`/`notify-quota`. Endpoint `/api/admin/config/email` devuelve último envío + counts 24h/7d. Sin webhook Resend de bounces todavía (Fase B siguiente). Hoy panel Email muestra "No configurado" + "Sin tracking en v1" — bloquea visibilidad cuando se configure SMTP
- **AEAT health derivado de últimas N facturas** (~5min) — cambiar `/api/admin/config/fiscal` para que el semáforo del panel se calcule desde las últimas 5-10 facturas enviadas (todas fallaron = rojo, alguna falla = ámbar). Sin ping a AEAT (lento + bloqueable). Más reactivo que tasa absoluta 30d
- **Exportar matriz planes a CSV** (~15min) — botón en `/admin/plans` que devuelve CSV plan↔feature para mandar a Borja o preparar pricing page. Alternativa al comparador visual (descartado por baja utilidad)
- **Test e2e Playwright middleware activo** (~1-2h, P0 antes de la próxima refactor del middleware) — disparado por incidente 2026-05-20 (middleware off 3 semanas tras rename `middleware.ts → proxy.ts`). Test: arranca server, hace request a `/?impersonate=ea201784-...` (con sesión superadmin mock), verifica que la cookie `impersonate_org` se set en la respuesta; navega después a `/` sin query param, verifica que la cookie se borra. Si alguien revierte o rompe el middleware otra vez, este test cae en CI. Coste casi cero, prevención fortísima. Considerar también un smoke test similar para `phone-verified-gate` (request a `/dashboard` con user sin teléfono verificado → debe redirect a `/verificar-telefono`).
- **Auditoría general "lo crítico está cubierto por capa N" tras restore middleware** (~1h, medio P1) — el incidente reveló que el rename del middleware no se notó porque otras capas (admin layout, dashboard layout) cubrían lo más visible. Listar conscientemente qué responsabilidades viven en cada capa (middleware vs layout vs handler vs RLS) y dónde una sola capa basta vs cuándo hay defensa en profundidad. Salida: una matriz en `Stack/facturaia.md` o `decisions/ADR-NNN-defense-in-depth-layers.md`. Evita el "no sabíamos qué se rompió" del próximo refactor crítico.
- **Banner "Estás impersonando X" más visible** (~30min, P2 UX) — actualmente el `ImpersonateBanner` es pequeño arriba del dashboard. Tras el incidente queda claro que es estado fácil de olvidar. Considerar: (a) hacerlo sticky en topbar, no solo arriba del contenido; (b) cambiar fondo del topbar a un naranja/ámbar suave mientras esté activa; (c) registrar evento `admin_impersonation_active >24h` para que aparezca alerta en el panel admin (¿se quedó pegada?). Bajo riesgo, mejora UX considerable.
- **Refactorizar `extractError` en el portal a un helper compartido** (~1h, P2 calidad) — el patrón "TuFacturaIA me devolvió `{error:{type,message,param,request_id}}` y quiero `message`" lo arreglé hoy en 3 sitios distintos (`auto-emit.ts`, `pdf/[shadowId]/route.ts` portal + el handler manual fetch). Extraer a `lib/facturaia/extract-error.ts` con tests + reemplazar las 3 copias. Evita drift entre handlers.
- **Test e2e PDF binario vs JSON{url}** (~1h, P2 robustez) — el cambio de TuFacturaIA (PDF binario en lugar de JSON{url}) se detectó por user en producción, no en CI. Añadir test e2e en portal que (a) genera factura mock; (b) hace GET `/api/facturaia/pdf/[shadowId]`; (c) asserta `Content-Type: application/pdf` + bytes empiezan con `%PDF-`. Hoy estamos protegidos por el bypass de openapi-fetch, pero si alguien vuelve a usar `fia.GET('/facturas/{id}/pdf')` directamente, este test lo caza.
- **Migrar el resto de consumidores de `impersonate_org` a un helper común** (~2h, P2 calidad) — hoy hay 3 sitios que leen la cookie con override semantics (`with-api-auth.ts`, `render-pdf/route.ts`, `(dashboard)/layout-server.tsx`) y 1 con union semantics nueva (`/api/documents/file` post PR #52). Extraer un helper `resolveAccessContext({ ownership: 'strict-override' | 'union' })` que documente la decisión y reduzca riesgo de divergencia. No urgente pero ayuda a futuros lectores.
- **Bot WhatsApp rediseño conversacional** — promovido a [[facturaia-bot-conversational-redesign]] (spec completa 2026-05-21). Bug "Hola → 🧾 con campos vacíos" + detección first-time + tono colega contable + Phase 2 Interactive Lists + sticky-aware. 3 PRs incrementales (PR-Bot-1/2/3), ~12-16h totales. Requiere LLM desbloqueado (`/admin/llm` hoy con 429 OpenAI + key inválida Anthropic).
- **Chat conversacional in-app (tipo ChatGPT/Claude) para TuFacturaIA** (planteado 2026-07-07, sin arrancar) — copiloto embebido en la app, no WhatsApp. Recomendación pactada: Fase 1 solo lectura (consultas cashflow/facturas/IVA reusando tools de lectura de `services/mcp-server/`) vía Claude API server-side (`POST /api/chat` con `withApiAuth`), tablas nuevas `chat_conversations`/`chat_messages` con RLS, widget flotante + página `/chat` dedicada. Fase 2 añade acciones de escritura con confirmación explícita y gating por `OrgRole`. Fase 3 proactivo (alertas/sugerencias). Nada implementado — retomar con estas 3 fases si se prioriza.
- ~~P0 FISCAL — PDF abono muestra importes POSITIVOS~~ ✅ **PR #64 MERGEADO + SMOKE VERDE 2026-05-21**.
- ~~P1 UX — Conciliación: vinculación factura↔movimiento INVISIBLE~~ ✅ **PR #65 MERGED 2026-05-21**.
- ~~UX `/emitidas` — «Anular y emitir abono» desde menú 3-puntitos~~ ✅ **HECHO 2026-05-21**.
- ~~Bug ruido — `GrandfatherBanner` llama `/api/profile/me` (404)~~ ✅ **HECHO 2026-05-21**.
- ~~🔐 Rotar OpenAI key + fix typo Anthropic~~ ✅ **HECHO 2026-05-21**.
- ~~Bug latente — `memberships_count` no llega a `v2-calc-totals` en path texto~~ ✅ **HECHO 2026-05-22**.
- **Vista móvil nativa + PWA COMPLETA EN PROD (2026-07-15, #895/#900/#899 + #901-#904)** — PR0-PR6 mergeados a main. Home nativo (hero cobro = KPI desktop, KPIs, vencimientos, ingesta) + shell (`useIsMobile`: initial server + matchMedia; header back explícito; tabbar) + PWA instalable + **sheet "+"** (Escanear cámara→/api/upload, Crear factura, Presupuesto→`/generar?doc=presupuesto`, Nuevo cliente→`/clientes?nuevo=1`; gating feature+`readOnly` billing) + **tab Facturas** nativa (card list agrupada por mes + segmented emitidas/recibidas, paridad counts/totales vía FacturasView) + **tab Documentos** (/ingesta cámara+progreso OCR, doc-preview reutilizado como overlay full-screen con `body.nav-locked`) + **menú "Más"** overlay (12 tiles gateados por feature+disponibilidad, tema, logout). Patrón: cada vista móvil es presentacional; el componente desktop sigue siendo dueño de datos/filtros/modales (bifurcación por `isMobile`). Bug de overflow en ingesta cazado en QA (grid `1fr` no encoge con título nowrap → `minmax(0,1fr)`, ver [[grid-1fr-no-encoge-con-contenido-nowrap-usar-minmax-0-1fr]]). Manual de usuario actualizado (#906, sección "En el móvil"). **SEGUNDA OLA COMPLETA EN PROD (2026-07-15/16, #907/#910/#911/#916/#917/#921 + #928/#929/#930)**: glass consistente + PWA/SW/offline + push server-side (VAPID puestas por Manu en Dokploy, `sendPushForNotification` enganchado a `notify()` solo warning|critical, gap real cerrado — el opt-in no activaba `channel_push`) + manual escaneo multipágina + gestos (swipe+undo, pull-to-refresh, scroll infinito, bifurcado sin tocar desktop). Smoke visual completo iPhone 14 claro/oscuro con Artifact. Auditoría perf (Lighthouse+throttling, build prod): LCP 5.1s/CLS 0/TBT 10ms, sin margen claro para optimizar. Umbral de blur validado con 2 fotos reales de Manu (nítida 768.8 / borrosa 58.6 de varianza Laplaciano — el 80 actual clasifica bien ambas, sin cambio de código). **PUSH VAPID CERRADO 2026-07-16**: PR #949 (root cause: `NEXT_PUBLIC_VAPID_PUBLIC_KEY` no se declaraba como build-arg en Dockerfile/docker-compose.yml) mergeado, desplegado, verificado por Manu en prod real — banner y push funcionan. Único pendiente real: smoke de push en iOS instalado (opcional, cosmético). Learnings: [[agent-browser-set-device-antes-de-open-para-ssr-mobile]] · [[cpu-contencion-multisesion-falso-positivo-ui-atascada]] · [[theme-en-localstorage-sin-cookie-espejo-causa-hydration-mismatch]] (preexistente, no corregido) · [[grid-1fr-no-encoge-con-contenido-nowrap-usar-minmax-0-1fr]] · [[gh-pr-merge-delete-branch-cierra-pr-apilado-dependiente]] · [[pwa-manifest-e-iconos-gateados-por-middleware-matcher-next]] · [[supabase-realtime-canal-nombre-unico-por-instancia-de-hook]]. Specs: [[facturaia-vista-movil-nativa]] · [[facturaia-vista-movil-segunda-ola]].
- _(añadir ideas aquí según surjan)_

## Seguridad

- **Middleware auth: fail-open/fail-closed si `getUser()` lanza (PR #1131, 2026-07-21) — MERGEADO.** Detectado auditando el proyecto contra un post genérico de terceros sobre error-handling en middleware Supabase (único gap real de 4 temas; RLS y N+1 ya estaban resueltos, y el fix de reconexión Realtime quedó obsoleto por el cutover paralelo de Ola 3). Ver [[supabase-ssr-middleware-getuser-sin-trycatch-tumba-rutas-publicas]].
- **Fuga secrets Dokploy (2026-07-03) — Capa 1 CERRADA, rotación pendiente** — `compose.one`/`schedule.one`/`application.one` de Dokploy vuelcan el `env` COMPLETO en plano; el 07-03 un agente lo volcó a chat (service role, Anthropic/OpenAI, WhatsApp, Resend, `WEBHOOK_SIGNING_KEY`, claves de cifrado, TrueLayer/Tink, B2, N8N). **Capa 1 (guardarraíl) hecha+deploy verificado** (PR #870): hook global bloquea las llamadas crudas + wrapper `dokploy-safe.sh` redacta; fixes de código (api-error, admin/config) y de skills (inventory/fincas). **Capa 2 (rotación) PENDIENTE** — alcance decidido: pragmático (3P + HMAC propios + claves kid `_v2`), SIN tocar `VERIFACTU_ENCRYPTION_KEY`/`WEBHOOK_SIGNING_KEY` (solo re-cifrado); empezar por Supabase→Stripe→Meta. **Capa 3 (secrets fuera del `compose.env`)** diseñada, sin arrancar. Runbook completo + orden en repo `ops/security/README.md`. Fuga fue a transcript privado, no público. Ver [[dokploy-api-get-schedule-list-expone-env-completo-secrets]].
- **Code Quality (CodeQL) activo (16/06)** — baseline triado: 23 findings Python = scripts desechables n8n-patches (arreglados PR #322), 9 JS fiabilidad = 0 bugs reales (dismiss UI), 8 dead-stores defensivos (dismiss). Sin deuda crítica oculta. Trial 30d → confirmar licencia. Ver [[github-code-quality-triage]]

### Auditoría de cifrado — DESPLEGADA en prod 2026-07-19 (#998 + #1011)
Contrastada con NIST SP 800-38D/57, OWASP (Crypto/Secrets/Key/Password), RGPD art.32+AEPD, PSD2 EBA RTS, RD 1007/2023. Base ya sólida (AES-256-GCM+kid en integraciones/PSD2, hashing correcto, RLS 100%); el problema era **consistencia** (IBAN en claro incoherente vs `bank_consents` cifrado) + claves en env. El PR se **dividió** por composición (aditivo vs cutover destructivo).
- **#998 (aditivo, zero-downtime) — DESPLEGADO:** A1 `src/lib/crypto/pii.ts` (AES-256-GCM kid + blind index HMAC scoped por org), A4 cifrado `.p12` VeriFactu + IV 12B, A8 cache-control PDFs, A9/A10, B5-f1; A5 mig **519** (columnas `*_enc`/`*_bidx`/`cuenta_mask`+`cuentas_bidx`), A6 dual-write IBAN/cuenta (degrada a plaintext-only si falta clave). Smoke A6 en prod verde (alta cliente API v1 → `iban_enc` descifra con la clave de 1Password).
- **#1011 (hash tokens fiscal/phone) — DESPLEGADO como EXPAND zero-downtime:** A2 mig **521** (fiscal_share_tokens → id PK + token_hash único + token_suffix; CONSERVA `token` nullable + trigger que lo rellena; enlaces show-once, revoke por id) · A3 mig **520** (phone_change_revoke_tokens→token_hash vía trigger, RPC `change_phone_revoke` por hash; ya zero-dt porque la app solo toca token vía RPC). **DROP `token` fiscal diferido a B2.** Triggers verificados en prod. Ver [[cutover-token-hash-zero-downtime-expand-contract]].
- **Claves:** `PII_BANK_ENCRYPTION_KEY`+`PII_BANK_INDEX_KEY` (64-hex distintas) en Dokploy + 1Password (vault FacturAIA, item `kknqs4zua…`). Perder la ENCRYPTION_KEY = IBAN irrecuperables.
- **Decisiones:** B4 **ADR-010 Aceptado** (env+hardening+kid, no Vault). B6 **mantener TTL 7d**.
- **Registros migs 519/520/521 RECONCILIADOS** en `schema_migrations` (insert version NNN, workaround CLI-bloqueado). **Verificado**: smoke A6 + E2E A2 fiscal + triggers A2/A3 en prod + auditoría adversarial sin hallazgos.
- **3 bugs destapados por el E2E A2, todos arreglados+desplegados:** **#1020** (enlace fiscal usaba `new URL(req.url).origin` → `0.0.0.0` interno tras el proxy → `resolvePublicBaseUrl`; verificado host `app.tufacturaia.com`). **#1022** (500 en token inválido/revocado: `ErrorScreen` server llamaba `buttonClassName` client). **#1025 (sistémico)**: 7 páginas gated (cashflow, fiscal ×3, inventario, recurrentes, remesas) daban **500 en su rama upsell** por el mismo patrón → orgs SIN el módulo (conversión) veían crash; latente porque las orgs de test tienen todo activo. Fix de raíz: `buttonClassName`→`button-class.ts` sin `'use client'` + re-export (0 consumidores client tocados). Verificado 500→200 en dev. Ver [[server-component-no-puede-llamar-funcion-use-client]].
- **Pendiente:** A3 E2E de sesión (cambio tel→email revoke; no automatizable, mecanismo ya verificado en BD/RPC) · Gated orden estricto: B1 backfill prod → A7 matching por bidx → B2 cutover (**+ DROP `token` fiscal**).
- Learnings: [[cutover-token-hash-zero-downtime-expand-contract]] · [[server-component-no-puede-llamar-funcion-use-client]] · [[cifrado-columna-dual-write-degrada]] · [[blind-index-scoped-por-tenant]] · [[supabase-pooler-caido-aplicar-ddl-via-mcp]] · [[aplicar-migracion-por-psql-y-registrar-version-cuando-el-cli-supabase-esta-bloqueado]].

### Auditoría de seguridad 2026-06-04 (6 agentes + red team activo) — ✅ CERRADA en prod
C1 crítico (RPCs SECURITY DEFINER expuestas), SSRF/CSRF/headers (PR #131), outbox anti-spoofing (PR #133/mig217), fuga RLS `billing_accounts` (mig 218) — todo resuelto y validado contra prod. [[supabase-rpc-security-definer-execute-public]] · [[facturaia-historico-detallado]]

### Auditoría de seguridad 2026-06-19 (red team 4 agentes) — ✅ CERRADA en prod (main `31f134f1` + mig 336)
HIGH bypass de pago en `PATCH /api/settings/features` + deps HIGH (nodemailer/undici) + MED `/api/seed`+search_path — todo corregido y verificado; residuales (HIBP sin Supabase Pro, extension_in_public) documentados como wontfix/no explotables. [[endpoint-toggle-feature-debe-gatear-enable-por-plan-o-compra]] · [[import-runtime-dep-no-declarada-solo-transitiva]] · [[supabase-advisor-trigger-functions-definer-son-ruido]] · [[facturaia-historico-detallado]]

### Migración crons → HMAC v2 (2026-06-05) — ✅ COMPLETA (2026-06-09)
24 schedules Dokploy migrados a `sign-call.sh` v2, Drive Sync y WhatsApp Receptor v2 incluidos, `SIGNING_LEGACY_UNTIL` cerrado. Huecos destapados en auditoría 2026-07-03 (agentic-gate-sweep/slack-dispatcher/verifactu-process sin schedule real) ya arreglados. [[dokploy-cron-docker-exec-no-hereda-env-de-app-env]] · [[cron-health-desconocido-para-cron-sin-ningun-run]] · [[facturaia-historico-detallado]]

### Deudas P0/P1 con threshold claro (priorizadas)

🔴 **P0 — ANTES de primer cliente facturando con dinero real**:
- ~~OTP teléfono al onboarding WhatsApp~~ — **HECHO 2026-05-16**. E2E verificado en prod.

🟠 **P1 — ANTES de cliente B2B que pida certificación ISO/ENS o auditoría AEPD nivel ISO**:
- **Encryption-at-rest del disco VPS** (~3-6h + ventana downtime). LUKS o dm-crypt del filesystem del `185.47.13.170`. Sin esto, robo físico del disco expone todas las BDs (postgres-n8n + envs). Hoy probabilidad muy baja en VPS managed pero auditor lo pedirá.

🟠 **P1 — Refactor del workflow para eliminar `$env.X`**:
- Hoy `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` con threat model documentado (security audit 2026-05-16). Aceptable hoy, bloqueante si: (a) se contrata dev externo con acceso a panel n8n, (b) n8n pasa a multi-user con roles. Plan: esperar n8n 2.21+ con fix del bug `toolHttpRequest`, o migrar a `toolWorkflow` con 4 sub-workflows (~6-10h).

🟡 **P2 — Hardening del perímetro n8n**:
- IP allowlist en Traefik para `n8n.tufacturaia.com` (solo IP del admin + crons internos). ~1h.
- Rotar `N8N_BASIC_AUTH_PASSWORD` a 32+ chars custom (no auto-gen Dokploy). ~5min.
- Desactivar `N8N_PUBLIC_API_ENABLED` si no se usa externo. Verificar.

### Hardening diferido (sin prioridad bloqueante)

- Logs estructurados en endpoints (correlation IDs)
- CSP headers más estrictos
- Métricas de uso de API IA (consumo OpenAI/Anthropic por org)
- Pen-test anual cuando se llegue a >100 clientes
- Rotar `SUPABASE_SERVICE_ROLE_KEY` — se vio su valor en el transcript de la sesión Claude del 2026-05-27 (al leer el env vía API Dokploy). Exposición local, no a terceros; riesgo bajo pero es service-role (salta RLS). Decidir rotar vs aceptar.
---

## Specs detalladas (subfiles)

- [[facturaia-historico-detallado]] — **📦 archivo histórico**: detalle de sprints cerrados, log "Progreso en vivo" mayo 2026, smokes verificados, historial de deploys (podado del hub el 2026-05-31)

- [[facturaia-comunicaciones-emails]] — sistema emails Resend + auth Supabase, 5 PRs, reutiliza outbox/notifications/ai_module_suggestions
- [[facturaia-integracion-api-v1-portal]] — webhooks HMAC, outbox, Stripe-style sync con agency-portal
- [[facturaia-test-tanda-2-3]] — tests de tanda 2 y 3
- [[facturaia-arquitectura-multi-whatsapp-numbers]] — multi-tenant WhatsApp por org
- [[facturaia-bloque-4-agent-query-spec]] — copiloto IA multi-canal
- [[facturaia-copiloto-siguiente-nivel]] — **🚀 Copiloto siguiente nivel** — roadmap por tiers (0 medición · 1 proactividad+multimodal · 2 gestorías+fiscal) + diagnóstico adopción prod 2026-07-12 (uso real ≈ 0, telemetría a medio cablear)
- [[facturaia-open-banking-psd2]] — conexión bancaria PSD2 (legado, sustituido por spec abajo)
- [[facturaia-conciliacion-roadmap]] — **🗺️ Conciliación · Roadmap maestro** (subfile canónico para cualquier sesión nueva — estado, fases hechas, pendiente, siguiente, deferred, decisiones, deuda)
- [[facturaia-conciliacion-copiloto-spec]] — **Conciliación bancaria + Copiloto v2 con tool-use** (spec definitiva, Sprint A MVP + Sprint B condicional). Documento autoritativo en repo: `docs/architecture/conciliacion-copiloto-spec.md`
- [[facturaia-prompt-auditoria-dashboard]] — prompt reutilizable: auditoría dashboard (exactitud + mejoras + arquitectura + benchmarking Holded/Quaderno con foco IA)
- [[facturaia-cashflow-chart-saldo]] — chart `/cashflow` unificado (saldo arriba + flujo abajo, estilo TradingView). ✅ hecho en commits `d810e31` + `26cb553` (2026-05-26)
- [[facturaia-centro-fiscal-ia]] — **Centro fiscal IA (módulo Pro add-on)** — spec completa congelada 2026-05-19. 303/390/130/111/115/180/190/347/349 + libro IVA + export asesor. v1 cuadre+export, v3 colaborador social AEAT. Calculador versionado, snapshot céntimos enteros, WORM + sello eIDAS, advisory lock anti-race
- [[facturaia-drive-sync-architecture]] — **Drive sync de PDFs a Google Drive del cliente** — backbone (mig 177-179) + worker outbox con SKIP LOCKED + subcarpetas año/tipo/mes lazy + cache JSONB + cascade revoke + naming rico. PRs #85/#87/#88 mergeados 2026-05-29. Cron n8n inactivo pendiente FACTURAIA_SERVICE_KEY. Ver [[ADR-025-drive-sync-outbox-vs-fire-and-forget]]

## Stack técnico

- **Frontend**: Next 16 (App Router), Tailwind, primitives Button/Card/Input/Modal en `components/ui/`
- **Backend**: Next API routes + Supabase (Postgres, RLS obligatoria por tabla)
- **OCR**: OpenAI Vision (`gpt-4o-mini`), fallback Anthropic Claude Vision
- **Voz**: n8n workflows + Retell (números WhatsApp por tenant)
- **Pagos**: Stripe (checkout pendiente cablear a add-ons)
- **Fiscal**: Verifactu (QR en PDFs, AEAT integrado)
- **Auth/RLS**: superadmin impersona vía `?org_id=` query param, no cookie
- **Tests**: Vitest (175+ unit/integration) + Playwright E2E (`tests/e2e/`: setup, smoke, explorer). Pre-release: `npm run e2e:explorer` y revisar `report.json`

## Reglas de proyecto (resumen)

Ver `facturaia/CLAUDE.md` (en repo) para versión completa.
- `lint` + `typecheck` + `build` limpios pre-commit
- Toda tabla nueva = `ENABLE ROW LEVEL SECURITY` + políticas en la misma migración
- Migration `ADD COLUMN NOT NULL` = `ADD nullable → UPDATE backfill → SET NOT NULL` en transacción
- Endpoint nuevo = auth + rate limit + validación input siempre
- Cambios Zod API público = `openapi.json` en mismo commit (clientes openapi-typescript no leen `refine`)
- UI con badge "implementado" = grep que el backend lee el valor (anti regresión silenciosa)
- **Merge a main exige 1 aprobación (ruleset del repo) y auto-merge está desactivado** — `gh pr merge --merge` falla con "base branch policy prohibits the merge"; `--auto` falla con "Auto merge is not allowed". Sin aprobación solo queda esperar review o `--admin` (bypass, pedir OK explícito antes de usarlo).

---

## Manuales y docs

| Doc | Link | Estado |
|---|---|---|
| Manual usuario | [GitHub](https://github.com/AgentesIA-MAdrid/facturaia/blob/main/docs/manuals/manual-usuario.md) | Bloque 1 desactualizado (3 botones, abonos, pills) |
| Manual admin | [GitHub](https://github.com/AgentesIA-MAdrid/facturaia/blob/main/docs/manuals/manual-admin.md) | Bloque 1 desactualizado |
| Módulos producto | [docs/MODULOS-PRODUCTO.md](https://github.com/AgentesIA-MAdrid/facturaia/blob/main/docs/MODULOS-PRODUCTO.md) | Vivo |
| CLAUDE.md proyecto | `facturaia/CLAUDE.md` | Vivo |

## Workflows n8n (backup real en `knowledge/projects/agentesia/n8n-backups/facturaia/` de ESTE vault — git-tracked, push a GitHub, excluido de búsquedas vía `.ignore`; espejo local de acceso rápido en `~/facturaia/ops/n8n-backups/`, gitignored ahí adrede)

| Workflow | ID | Función |
|---|---|---|
| OCR Factura | `zf2la2N2YBXKQNKk` | Vision → estructurado |
| WhatsApp Receptor v2 | `pqSWkDIHqmSVHotB` | Ingesta + intención + AI Agent con tools (consultar_*, registrar_gasto_rapido, crear_cliente). Antes era `zYcHHa8jWXB6dY5i` (obsoleto). |
| WhatsApp Verify | `LlBeZfa2P6mg13wc` | Verificación Meta |
| Voice Process | `KVk45rJARQk0EKaF` | Procesar audio |
| Voice Confirm | `5Q3pmag3XnIQOkQV` | Confirmación voz |
| Voice Correct | `Q1WB42g8GtyX3qnp` | Corrección voz |
| Email Polling | `dqvzeyeifTcNv50u` | Ingesta email |

---

## Links rápidos

- [GitHub repo](https://github.com/AgentesIA-MAdrid/facturaia)
- [App producción](https://facturaia.agentesia.world)
- [Dokploy](https://dokploy.agentesia.world)
- [Slack canvas Panel TuFacturaIA](https://agentesialab.slack.com/docs/T0ARXF0V31S/F0AV38CHYSJ) (`F0AV38CHYSJ`)
- [Canal Slack](https://agentesialab.slack.com/archives/pro-facturaia) (#pro-facturaia)
- WhatsApp público: `+34 919 93 26 18`

## Credenciales (refs en memoria, NO secrets aquí)

- Supabase TuFacturaIA → memoria `supabase-facturaia.md`
- Anthropic API key → memoria `anthropic-api-key.md`
- OpenAI API key → memoria `openai-api-key.md`
- WhatsApp config → memoria `whatsapp-facturaia.md`
- Gemini (backup) → memoria `gemini-api-key.md`
- **SSH Dokploy nuevo**: `root@185.47.13.170 -p 5251` (clave id_ed25519 ya autorizada). Dokploy viejo era `.166`.
- **n8n.tufacturaia.com API key** (JWT) → memoria `reference_n8n_api_key.md` (rotada 2026-05-17).
- **MCP AS — keypair ES256** → 1Password vault FacturAIA, item "MCP ES256 — AS signing keypair (mcp.tufacturaia.com)" (privada PKCS8 = `MCP_SIGNING_PRIVATE_KEY`, solo servicio; pública SPKI = `MCP_USER_TOKEN_PUBLIC_KEY`, servicio+app). Password de `mcp-redis` → env del compose `mcp-server` en Dokploy.

---

