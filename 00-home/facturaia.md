---
title: facturaia
date: 2026-05-10
tags: [cliente, facturaia, hub]
---

# TuFacturaIA — Hub maestro

App SaaS de facturación con IA (OCR, agente WhatsApp, voz, recomendador). Multi-tenant. **Este archivo es el ÚNICO punto de entrada al proyecto.** Todo lo nuevo (idea, pendiente, smoke test, decisión, doc) entra aquí o se enlaza desde aquí.

## Cómo usar este hub

**Modelo de 2 niveles — no leer todo, leer lo que toca:**
- **Arranque de sesión (siempre, barato ~6-8K tokens)**: leer solo este bloque superior — `Estado` + `NOW` + `Bloqueos` + `Decisiones pendientes` + `Índice de áreas`. Es la foto del proyecto.
- **Durante (bajo demanda)**: abrir el subfile del área en la que trabajas (ver `Índice de áreas`) o la sección inferior concreta (NEXT / Smoke / WIP). NO leer el hub entero ni el histórico salvo que se pida.
- **Cierre (`/obsidian-1`)**: lo cerrado (✅ / ~~tachado~~) **se mueve a [[facturaia-historico-detallado]]**, no se acumula aquí. Entradas del hub = 1-2 líneas mirando hacia delante. Sección que pasa de ~30 líneas → extraer a subfile.

---

## Estado actual

- **Prod**: `app.tufacturaia.com` + `n8n.tufacturaia.com` (Dokploy `185.47.13.170`, SSL Let's Encrypt OK). BD Supabase `lahqlyaxvobqjgdiftag`. (dominio viejo `facturaia.agentesia.world` caído/obsoleto)
- **Stack**: Next 16 · Supabase · n8n · OpenAI Vision · Anthropic Claude
- **Planes**: Starter 19 / Pro 49 / Enterprise 99 €/mes (+IVA 21%, anual −20%) + add-on Centro Fiscal IA 14,90. **Stripe LIVE** desde 2026-06-01.
- **Migraciones**: prod = main; última aplicada **319** (`feedback_action_tokens`, 2026-06-17; 317 `feedback_ai_job_events` + 318 `dashboard_top_clientes` también ese día). Secuenciales `NNN_`, sin timestamps.
- **Tests**: Vitest 1648/1648 verde · E2E smoke verde (cashflow v2 · conciliación Fase 2 · onboarding).
- **Orgs prod**: AgentesiaLab · tecnocloud · Borja Galván · AgenteIA PRUEBA (todas `billing_status=active` complimentary).
- _Snapshot detallado anterior y changelog completo → [[facturaia-historico-detallado]]._

---

## NOW (trabajo activo)

- 🟢 **MCP server remoto (OAuth) — skeleton del servicio en pie, falta OAuth real.** Worktree `feat/mcp-server`. TDD+commit: 041 + 044 (v1 user-aware) + 043 (mig **327** tablas OAuth) + 042 (skeleton transport Streamable HTTP + `whoami` en `services/mcp-server/`, 2639 tests verde). **Siguiente: issue 045** (AS core con `@jmondi/oauth2-server`). Pendiente ops: aplicar mig 327 al mergear · HITL `whoami` con cliente de header fijo (conector Claude.ai remoto exige OAuth → 048). Detalle [[facturaia-mcp-server]].
- 🟢 **"Resolver con Claude" Fase 4 — COMPLETA en prod 2026-06-17.** Tandas 2+3 (#333/#338, migs 317+319): Q1 progreso en vivo, Q7/Q8 loop Continuar/Reintentar, Q5 botón email→token→`/resolver-con-claude`. **Smoke real Q1/Q5 VERDE** (timeline completo preparando→…→`pr_abierto`; el botón del email consumió token + encoló). **Bug Q5 cazado+arreglado**: el acuse 303 caía en `0.0.0.0` (`new URL(req.url)` = bind interno tras proxy) → `buildAppUrl()` (#352). **Q6 réplicas: ESCALADO a 2 LIVE** (host ya 16 GiB+swap; `RUNNER_REPLICAS=2` en compose `ticket-runner` — ojo: la env va en ESE compose, no en el de la app). **Tanda 4 — "instrucciones para Claude antes de resolver" LIVE** (#356, mig **324**): cuadro de comentario en email+panel → mensaje `internal` (no visible al usuario) + `is_ai` (etiqueta la salida de Claude); el claim lo mete en el prompt. **Residual:** validar 2 builds concurrentes (encolar 2-3 tickets a la vez) + ver Claude en directo (`docker logs -f`). Detalle [[facturaia-resolver-con-claude]].
- 🟢 **CI Visual Regression endurecida 2026-06-17** (#358 pre-warm + #361 `domcontentloaded` + #363 baselines + #366 trigger acotado): se acabaron los rojos falsos en cada PR con CSS. Causa triple: trigger `src/**/*.css` demasiado amplio, `waitUntil:'load'` cuelga en el dev server (RSC) y baselines stale. Baselines regenerados en contenedor jammy local (Actions bloqueado por billing). Ver [[regresion-visual-ci-acotar-trigger-y-baselines-en-contenedor]].
- 🔥 **Incidente host Dokploy 2026-06-17 — RESUELTO**: el host de **8 GiB sin swap** sufrió OOM durante un build del runner → containerd/runc *wedged* (`runc create ... PID from pipe: EOF` en todo contenedor nuevo) → control-plane de Dokploy caído ~7h + autoDeploy muerto. Fix: **reboot del VPS** + **`pg_resetwal`** de la BD interna de Dokploy (WAL corrupto) + deploy manual del app (recuperó el backlog). **Swap 4 GiB AÑADIDO** (`/swapfile` en `/etc/fstab`) + host a **16 GB RAM** → causa raíz cerrada (prevención). **Detección**: dead-man's-switch externo (GitHub Action `scheduler-watchdog`, PR #345) que avisa por email si el scheduler vuelve a morir (el monitor in-house moría con él → 7-9h de silencio). El blackout además destapó 3 crons que nunca disparaban (schedules Dokploy mal dados de alta) → arreglados. Detalle en `Stack/incidents.md` + `[[facturaia-historico-detallado]]` (Monitorización in-house endurecida). SSH al host: **puerto 5251**.
- 🟢 **Perf merge-train — #310/#311/#312 MERGEADOS a main 2026-06-17** (review humano, sin override): #310 cron check-vencimientos UPDATE set-based, #312 memoizar facturas-view + code-split recharts en ficha-producto, #311 dashboard top-clientes agregado en BD (RPC). #311 traía su migración como `313_` (colisión, main ya iba por 316) → **renumerada a `318_dashboard_top_clientes_rpc.sql`** + aplicada a prod (única pendiente; resto hasta 317 ya estaba). #317 (Dependabot form-data) autocerrado por #329. **Smoke prod pendiente** ↓. Descartados (no seguros): #326 (quality roja), #271/#229 (conflicto+CI roja), una feature en curso de sesión paralela (sin PR abierto entonces). [Nota: #337 NO era esa feature — ese número lo tomó después el PR de seguridad CodeQL, mergeado a main `2e821288`.]

- ✅ **Debouncing unificado + race-guards — CERRADO+DEPLOY 2026-06-17** (PR #340, merge `55ee725e`, deploy `done` 09:20, smoke prod verde). 6 debounces ad-hoc → 2 hooks (`useDebouncedCallback`/`useDebouncedValue`); 2 bugs topbar (cleanup unmount + race de orden) + guard de cancelación en fetches async de efecto que re-corren con deps de usuario (auditoría/calendario/agentes/`/generar` autocomplete + 6 sitios BAJO). Baseline E2E vs `origin/main` = **0 regresiones** (11 fallos idénticos = ambientales). Verificado en `app.tufacturaia.com` (topbar + autocomplete). → histórico próxima poda. Learning [[e2e-baseline-contra-main-antes-de-culpar-a-tu-rama]].
- ✅ **Emisión WEB de facturas — CERRADO 2026-06-17.** El CHECK `facturas_fuente_check` rechazaba `fuente='web'` (mig 258 añadió `mobile`, olvidó `web`) → 500 prod-wide desde 12/06 (WhatsApp/api/móvil OK). Fix: mig **313** añade `'web'` (#314 mergeado+prod; build gate saltado por SQL puro). Smoke real web → **201** verde + fila `fuente='web'`. → histórico próxima poda.
- 🟢 **Stock por lotes/partidas — integridad + UI (PR #307 MERGEADO + mig 312 aplicada a prod + deploy `280a0e72`, 2026-06-16).** Fase A (bugs reales, validados en PG17 local): anular **repone el lote** (`anular-factura.ts` propaga `lote_id`), ajuste/merma **por partida** (`ajustar_lote_manual`; el ajuste agregado queda bloqueado en productos con lotes), **código de partida de compra único por documento** (no fusiona), `recompute_lote`/`detect_lote_drift` + **fix latente**: recompute/drift excluyen reservas (mig 291 las sumaba → corrompían stock al reconciliar). Fase B (UI): EstadoPill de partida (Activa/Caduca pronto/Caducada/Agotada/Inactiva) + señal de caducidad ≤30d, selector de partida en fila propia (€ simple), inputs tokenizados, selector de partida en el modal de Ajuste. Endurecido + re-revisado por agentes (3+3). **Smoke prod: merma por partida LIVE ✅** (100→97, invariante OK); anular-repone-lote validado en PG17 (ya demostrable live tras cerrar el bug de emisión web 17/06). ADR-031 + learnings en vault. **Diferido a otro PR (mejoras, no bugs):** editar/archivar partida (PATCH/DELETE), trazabilidad inversa "ventas de un lote", FEFO autopick en /generar, captura del código de lote real al aprobar compra, proveedor/notas en el alta. Datos demo en sandbox ("Prueba heavy": LOTE-DEMO-01/CADUCA-PRONTO/CADUCADO); ⚠️ en el smoke anulé por error la vieja A2026-0127 (sandbox, inocuo).
- ✅ **Asistente WhatsApp Fase 1 (ruteo a Copiloto v2) — CERRADO 2026-06-16.** Receptor `pqSWkDIHqmSVHotB` parcheado: las PREGUNTAS de datos (deuda/ventas/conciliación/KPIs/comparativas) → puente `/api/internal/whatsapp/copiloto`; las acciones siguen en el bot de intención. Smoke real verde (datos cuadran, trampa 2019 no alucina). Scope: cashflow caja/runway sigue inline (no se redirigió, evita 2 cerebros — convergencia = Fase 2). Patches en repo (#273). **De paso, 2 bugs 088 que el smoke destapó (arreglados + desplegados):** (a) selecciones de lista `crear_doc_mov:`/`crear_doc_cli:` se descartaban en silencio → reclasificadas a texto en `Parsear Mensaje`; (b) homónimos sin diferenciador → NIF en la fila (#272 server + n8n). **Pendiente menor:** confirmar visualmente en WhatsApp que la fila muestra `NIF …` (verificado a nivel de datos: el endpoint ya devuelve nif distinto por cliente).
- ✅ **Bug copiloto anti-alucinación feature-gated — RESUELTO 2026-06-16 (#280).** `compararEmpresas` con `multiempresa` OFF hacía que el LLM improvisara "solo tienes una empresa" (mentira). Fix en `runSingleTool` (`humanizeToolError`): `feature_unavailable: <slug>` → mensaje honesto con nombre del módulo + "no inventes datos". Punto único, aplica web+WhatsApp. Test unitario (4 casos). Ver [[tool-feature-gated-error-llm-alucina-dato-devolver-mensaje-honesto]].
- 🟢 **Asistente WhatsApp Fase 2 — piloto "marcar cobrada" LIVE 2026-06-16 (issue 117 COMPLETO).** Todo el raíl de confirmación en prod: tabla `copiloto_pending_action` (mig **307**, single-use atómico + TTL 30min + una activa por hilo) · puente propose/confirm (consumo atómico, idempotencia doble-tap "ya hecho", caducó/reemplazada, gate de rol `propietario|admin|contable`) · tool `marcarCobrada` (preview/commit, fuente única `src/lib/documents/marcar-cobrada.ts` compartida con v1) · patch n8n receptor `pqSWkDIHqmSVHotB` activo (+3 nodos: `Es Botones Copiloto?`/`Enviar Botones Copiloto`/`Confirmar Copiloto`, rama `accion_copiloto`, `copiloto_confirm:` en Procesar Boton). PRs **#288** (feat) + **#290** (manuales usuario §8.7 + admin §11.9). **Smoke server-side prod VERDE** (propose→confirm→idempotencia→no-encuentro; factura A2026-0044 cobrada, pending_action `consumed`, audit `via=copiloto`). Diseño grill-me + PRD `issues/prd-fase2-copiloto-acciones.md`. **Smoke WhatsApp 16/06**: camino feliz + cancelar OK; destapó 2 bugs **ya arreglados+desplegados (#292)**: (a) OpenAI 400 por `assistant content:null` al replayar turnos de propuesta con `tool_calls` huérfanos → runner no emite ese mensaje + no se persisten huérfanos; (b) el LLM reusaba una factura de un turno previo → prompt **v4** "usa la del último mensaje" + hilo limpio. **Residual:** re-test tap físico WhatsApp (ya limpio; sandbox **+34617314938**) + migrar resto de acciones (gasto/cliente → emitir/anular al final) reusando el raíl. Ver [[smoke-endpoint-interno-prod-firma-hmac-prod-secret]].
- 🟢 **Fase 2 Copiloto — web converge, 109/110/115 LIVE + revert/historial arreglados (#295 mergeado+deploy 16/06).** **109** `registrarGasto` + **110** `crearCliente`/`crearProveedor` + **115** tarjeta de confirmación en el drawer web; raíl propose/confirm en fuente única `lib/copiloto/pending-action.ts` (web+WhatsApp comparten protocolo y textos). Smoke WhatsApp+web **verde** (gasto propuesta→Confirmar→registrado; revert; historial). **Flip n8n del gasto suelto LIVE** (receptor `pqSWkDIHqmSVHotB`; 4 ediciones prompt "Agente Facturador"; **088 intacto**; backup `n8n-backups/facturaia/pqSWkDIHqmSVHotB_pre-flip-gasto_2026-06-16.json`). **Fixes de cobro (mig 311 en prod):** "Revertir cobro" reparado (columna `cobrada_via` faltaba) + cobro por Copiloto ahora visible en Historial (audita `entidad='facturas'`). **111/112/113/114 COMPLETOS y mergeados a `main` 2026-06-17** (#309 + #318 + #319; #315 cerrado por quirk de rama, su código fue en #318): `enviarEmailFactura` (vía `sendFacturaByEmail`), `emitirFactura` (vía `createDocument`, IVA 4/10/21, IRPF opcional como Holded), `convertirPresupuesto` (RPC `convertir_presupuesto_a_factura`+render), `anularFactura` (vía `anularFactura()`, abono serie B, motivo R4 default). Todos: raíl preview/commit + gate rol + idempotencia + audit en Historial + **NIF P0** en los fiscales. `system-prompt` v10, **18 tools** en `registry`, **168 tests verde** + smokes preview en vivo (email; emitir 181,50€ exacto; bloqueo NIF al convertir). Manuales usuario+admin actualizados (en #318). **El set de acciones fiscales de Fase 2 está CERRADO** (114 era la última). Mergeado con `--admin` (rama protegida, sin revisor) tras verificar MERGEABLE sin conflictos. **Deploy a prod VERIFICADO ✅ 2026-06-17** (Dokploy compose `tufacturaia-app`: deploys `80ef7fed`+`e14a36d3` `done`; main hasta `930015c8` desplegado) → los 4 tools ya **LIVE en el Copiloto WEB**. **Smoke web (runner real `runCopilotoTurn`+`executeTool`) VERDE 2026-06-17** en sandbox: emitir A2026-0128 181,50€, email a `delivered@resend.dev`, anular→abono B2026-0005, convertir P2026-0006→A2026-0129, + bloqueo NIF. **Destapó P0 prod** (el preview smoke previo nunca llegaba al commit): `emitirFactura` emite con `fuente='copiloto'` y el CHECK `facturas_fuente_check` (mig 313) no lo admitía → 500 en TODA emisión por Copiloto (LIVE desde el deploy de hoy). **Fix mig 315 (PR #323 mergeado + aplicada a prod + CHECK verificado en vivo)**, solo-schema, sin redeploy. **Flips n8n a WhatsApp** (receptor `pqSWkDIHqmSVHotB`, patches `ops/n8n-patches/apply-flip-*-copiloto.py`, backups con timestamp, revert=restaurar prompt): **`enviarEmailFactura` LIVE + smoke WhatsApp tap VERDE ✅**; **`anularFactura` LIVE** (NO aditivo: añade (d) + reescribe PRECEDENCIA, deja mapping abono/`=== ABONOS ===` dormidos como red de seguridad). El smoke de anular destapó un **bug del runner Copiloto**: persistía cada propuesta destructiva como **texto del assistant** → con el hilo WhatsApp cargado de "Voy a [acción]…" el LLM **narraba en vez de llamar al tool** (sin botones; anular colgado). El agente n8n emitía bien `accion_copiloto` (verificado exec 6264 — el bug NO era del flip). **Fix #331 mergeado+desplegado** (`buildPersistedAssistantContent`: propuesta = ciclo tool_use→tool_result sintético, fuente única web+puente; `PendingConfirmation` gana `id`; 172 tests) + hilo WhatsApp sandbox purgado + learning [[persistir-propuesta-destructiva-como-texto-entrena-al-llm-a-narrar]]. Artefactos (patches+smoke) en PR #326. **`convertirPresupuesto` LIVE + smoke WhatsApp ✅** (P2026-0007→A2026-0130; negativo NIF ✅). **PDF como documento WhatsApp (parity bot viejo) LIVE + smoke ✅** (PR #335 puente devuelve `pdf_url` firmada + PR #326 4 nodos n8n `Tiene PDF Copiloto?→Descargar/Subir/Enviar Documento`; emitir/convertir/anular mandan el PDF de factura/abono al chat). **Fix #339 mergeado (autodeploy):** (a) warning Verifactu **condicional** a `verifactu_activo` (antes "entra en Verifactu" siempre, falso en sandbox); (b) hardening del #331 para acciones **blocked** (el NIF-block se persistía como texto → el LLM **fabricaba** bloqueos, inventó «Cliente Test Android»; ahora `tool_result is_error`). **✅ flip `emitirFactura` LIVE + smoke VERDE 2026-06-17** (opción A scope v1: cliente con NIF + IVA 4/10/21 + IRPF; exento/intracom/moneda → "desde la web"; bot viejo `GENERAR DOCUMENTO` dormido como red, presupuesto/proforma intactos; patch `apply-flip-emitir-copiloto.py`). **El set fiscal completo está flipado a WhatsApp.** **NEXT — 116 decomiso bot intención: NO arrancado, scope sin definir.** El "Agente Facturador" NO es decomisable entero: sigue siendo el ROUTER + dueño de presupuesto/proforma (no flipados, sin tool Copiloto), crear_doc_desde_movimiento, cashflow inline, cambio_org y small_talk. "Decomiso" = retirar las ramas YA muertas (factura/abono/convertir en `GENERAR DOCUMENTO`/`ABONOS`/`CONVERTIR_PRESUPUESTO`), pero esas son la **red de seguridad de revert** que acabamos de usar → retirarlas justo tras el 1er smoke es prematuro. Recomendado: dejar bakear los flips, luego decomiso medido con plan + OK. **Plan completo: [[facturaia-decomiso-bot-intencion-116]]** — hallazgo clave: los nodos de emisión son COMPARTIDOS con presupuesto/proforma (no flipados) → 116 está BLOQUEADO por flipar presupuesto/proforma (Fase 3); el código "muerto" es solo inerte en el prompt. Siguiente paso real: tools `crearPresupuesto`/`crearProforma`. **OJO multi-org:** sandbox y AgentesiaLab comparten el teléfono +34617314938 → nums de presupuesto colisionan (P2026-0008 existe en ambas). **E2E_PASSWORD ROTADO** (refrescar 1Password vault FacturAIA). _(hitos cerrados → mover a histórico en próxima poda — este NOW está enorme.)_
- 🟢 **Revertir cobro/pago en la ficha (modelo Holded) — LIVE (#293).** Cerró el gap: no había acción UI para deshacer un cobro marcado a mano/por copiloto (solo el enlace WA). Botón "Revertir cobro" en sección Cobro (solo cobros DIRECTOS, rol propietario/admin/contable); si la factura está conciliada con el banco → no sale el botón, hay que **desvincular** en Conciliación (estado derivado de `mfa`; server responde `409 conciliada`). Reusa el endpoint canónico generalizando su modo-sesión + guarda de conciliación. Manual usuario actualizado.
- 🟢 **088 crear factura/gasto desde movimiento — CERRADO** (server E2E verde + gasto WhatsApp verde). Opcional pendiente tuyo: tap-cliente→emitir por WhatsApp + regresión (factura normal/OCR/cobro). Datos de prueba en sandbox (2× −121 manuel, +200 manuel) — bórralos cuando quieras desde `/conciliacion`.
- 🔴 **Smoke UI conciliación en sandbox** (tras PR #259/#261) — revisar `/conciliacion`: contraste del drawer, sugerencia visible en el drawer del movimiento, tabla sin scroll horizontal (móvil+web), banner legible. Si chirría → pedir 3-4 variantes numeradas. Pasos en `Smoke tests pendientes`.
- **Registro branded + onboarding** (PR #256/#258 en main `84277b13`) — **falta deploy Dokploy**; tras deploy, smoke registro → confirmación Resend → perfil → fiscal → canales → dashboard. Pasos en `Smoke tests pendientes`.
- **Stock / Inventario** — Fases A-D + 1 + 2 + 3 IA completas en prod. **Siguiente: activar `disponibilidad='disponible'`** + smoke prod con feature activa. Detalle [[facturaia-modulo-stock]].
- _Hitos recién cerrados (onboarding unificado 021-030, conciliación Fase 2, modelo 349, multiempresa) → [[facturaia-historico-detallado]]._
## Bloqueos / esperando a terceros

- **[ACCIÓN MANU 2026-06-17] Billing GitHub Actions re-bloqueado** — desde ~13:00 todos los runs fallan a 0 pasos ("payments failed / spending limit"). Repo privado → Actions de pago; se agotó cupo/límite. CI ya optimizado para no derrochar (#367 CI/Visual solo en PR + concurrency; #365 visual determinista). Regularizar pago / subir spending limit en Settings → Billing org `AgentesIA-MAdrid`. Hasta entonces no se valida/mergea nada que dependa de CI.
- **[PENDIENTE MANU] Publisher Verification Azure — Microsoft 365** — sin esto, usuarios de tenants M365 corporativos ven aviso "app no verificada del editor" al conectar Outlook. Pasos: (1) ir a portal.azure.com → App Registration TuFacturaIA → Personalización de marca → Verificar dominio del editor; (2) añadir `tufacturaia.com` como dominio verificado (requiere añadir un registro DNS TXT que Azure proporciona); (3) asociar cuenta Microsoft Partner Network (MPN) si se pide — normalmente con dominio verificado basta para "Verified publisher". Tiempo: ~1-7 días hábiles. Outlook.com personal funciona sin esto. Para activarlo falta un **App Registration en Azure**, y `info@agentesialabs.com` es **cuenta Microsoft personal sin directorio Entra propio** → Azure no deja crear app ni tenant desde ella. El alta de Azure Free se atascó en la **verificación de tarjeta** (la tarjeta no la aceptó; el VAT/CIF SÍ funcionó usando el formato `ESB27602085` con prefijo `ES`). **Para retomar**: (1) completar alta Azure Free con **tarjeta de crédito real** (no débito/prepago, no cobra, solo verifica) → genera Default Directory; (2) App Registration: multi-tenant + cuentas personales, redirect `https://app.tufacturaia.com/api/integrations/microsoft-365/callback`, permisos Graph delegados `Mail.Read User.Read openid email offline_access`, crear client secret; (3) migración `UPDATE integration_providers SET coming_soon=false, scopes=array['openid','email','offline_access','User.Read','Mail.Read'], capabilities=array['read_email'] WHERE slug='microsoft-365'`; (4) `MICROSOFT_CLIENT_ID`/`MICROSOFT_CLIENT_SECRET` en Dokploy (recrear contenedor); (5) smoke (validar query Graph `$filter`+`$orderby`). Pendiente producción: **Publisher Verification** (dominio `tufacturaia.com`) para que clientes de empresa M365 no vean aviso "app no verificada".

- **[ACCIÓN MANU 2026-06-01] Subir tier OpenAI (TPM 30k tope) para el bot WhatsApp** — cada llamada del agente pide ~10-13k tokens; con TPM 30.000 (Tier 1) solo caben ~2-3/min → rate-limit en ráfagas. Mitigado con prompt −38% + memoria 10→8, pero la solución de raíz es subir a Tier 2 (450k TPM) añadiendo saldo/historial en platform.openai.com/settings/organization/limits.
- **[ACCIÓN MANU 2026-05-29] `FACTURAIA_SERVICE_KEY` discrepancia local vs prod** — Smoke directo a `/api/internal/drive-sync-dispatcher` (y a `/api/internal/webhook-dispatcher`) devuelve **401** con la `FACTURAIA_SERVICE_KEY` del `.env.local`. Hipótesis: la key fue rotada en prod y `.env.local` quedó con valor obsoleto, O la env var NO está seteada en el compose de prod (verificación bloqueada por classifier de credential exploration). **Implicación**: NINGÚN cron interno con `withCronTracking + requireServiceKey` funciona (incluye drive-sync, webhook-dispatcher, conciliación, cashflow-alerts...). El email-poll funciona porque usa `SUPABASE_SERVICE_ROLE_KEY` legacy. **Fix en próxima sesión** (ver [[facturaia-historico-detallado]] P0 paso 1): verificar valor en Dokploy panel `compose/tufacturaia-app` env, actualizar `.env.local` o crear nueva con `openssl rand -hex 32` añadiéndola a app + n8n con mismo valor + Deploy. **Sin esto, el workflow n8n `TwDd1OkMqr3nhSso` (Drive Sync) NO puede activarse**.

- **[ACCIÓN MANU 2026-05-27] `N8N_WEBHOOK_URL` falta en Dokploy prod** — La env var no está seteada en el contenedor `tufacturaia-app`. Sin ella, `POST /api/upload` y el cron de email ingestion crean la bandeja pero **nunca disparan OCR**: entrada queda `procesando/progreso:0` indefinidamente (incidente PDF de hoy). Commit `68cf674` añade `console.error` para que sea visible en logs. **Fix**: Dokploy → compose `tufacturaia-app` → env → añadir `N8N_WEBHOOK_URL=https://n8n.tufacturaia.com` → Save → Deploy.


- **[Acción Manu] Configurar Supabase Auth URL Configuration** — Dashboard → Authentication → URL Configuration: Site URL = `https://app.tufacturaia.com`, Redirect URLs add `https://app.tufacturaia.com/**` + `http://localhost:3000/**`. Defense in depth aunque el flujo password reset ya no use plantillas Supabase (genera link propio con `admin.generateLink`). Sin esto, si algún flujo nativo llama a `resetPasswordForEmail` desde otro punto, el enlace cae a Site URL actual = `0.0.0.0:3000`. (2026-05-26)

- **[IMPORTANTE PSD2 — pre-cutover live] Compliance legal con Dani antes de abrir conexión bancaria al público**:
    - Revisar `docs/decisions/ADR-002-psd2-data-retention.md` y archivar (base legal retención 7 años Ley 58/2003 + Ley 11/2021 + Código Comercio art 30). Sin esto, frente a inspección AEPD o derecho al olvido GDPR art 17 vamos a defender ad hoc.
    - **Privacy Notice** del producto debe mencionar: (a) "Procesamos datos bancarios PSD2 via Tink AB (Visa, Suecia) y TrueLayer Limited (UK + EU passport) como encargados del tratamiento art 28 GDPR"; (b) "Retención 7 años por obligación fiscal"; (c) "Email para ejercer derechos: privacy@tufacturaia.com (endpoint art 15 disponible en /api/conciliacion/banks/[id]/access-report)". **Sin esto el consentimiento al tratamiento está viciado**.
    - **DPA art 28 GDPR** firmado con Tink AB (template estándar suyo, ~10 min). Lo mismo TrueLayer.
    - **T&C de TuFacturaIA** mencionan "banca abierta PSD2" + "solo lectura, no operativa".
    - **DPIA art 35 GDPR** (Data Protection Impact Assessment) — obligatoria para "tratamiento sistemático a gran escala de datos financieros sensibles". Plantilla AEPD, 4-8h redacción. Sin esto, infracción objetiva GDPR (multa hasta 10M€ o 2% facturación). Necesario antes de >100 orgs con banca conectada.
    - **Pentest** del flow PSD2 antes de cutover live (~3-5k€ outsourced o 2-3d con OWASP ZAP / Burp si in-house).
    - **Solicitar app live Tink** en console.tink.com (proceso self-service standard tier; pueden pedir info corporativa + Privacy Notice + DPA firmado).
    - **App live TrueLayer** si la mantenemos como fallback (más farragoso que Tink, pueden pedir review humana de la app — 3-5 días).
- **[IMPORTANTE] Declaración Responsable AEAT como fabricante de SIF** — AgentesIA debe presentar este trámite administrativo para que TuFacturaIA opere legalmente en producción con VERI*FACTU. SIF = Sistema Informático de Facturación (RD 1007/2023). Sin esto el sistema no está homologado. Confirmar con Dani+Gonzalo si ya está o iniciar trámite. **Nota**: Verifactu aplazado a 2027 por RDL 15/2025 (IS 01-ene-2027, IRPF/IRNR-EP 01-jul-2027) — durante 2026 voluntario. Aún así la declaración SIF conviene tenerla antes para no llegar tarde.
- **[Centro Fiscal IA — externos legales pre-beta]** acción Manu, NO dev:
    - **FNMT TS@ vía CERES** — plantilla email lista en `docs/runbooks/fnmt-tsa-email-solicitud-convenio.md`. Convenio bilateral 2-6 semanas. Modulo Centro Fiscal IA funciona SIN sellar (wizard + cálculo + cuadres + PDF + libro IVA OK), solo "Marcar presentada" falla. Backblaze B2 WORM ya activado 2026-05-22.
    - **Seguro RC Tech E&O 1M€** — Hiscox/AXA/Generali, ~600-1.200€/año. Bloqueante GA Centro Fiscal IA.
    - **Abogado IT** — Términos B2B + B2C + DPA art 28 + Política privacidad + Runbook brecha RGPD art 33-34, 3-5k€. Bloqueante primer beta-tester.
    - **ZDR (Zero Data Retention)** OpenAI + Anthropic por ticket — gratis con DPF SCCs. Bloqueante primer beta-tester.
    - **Stripe price IDs** crear: Starter 12€ · Pro 23€ · Enterprise 49€ · Fiscal addon 14,90€/mes · 149€/año (scope B según ADR-018).
    - **Auditoría externa pre-beta** REAF 3k + abogado IT 4k + pentest 5k = 10-15k€ total.
<!-- Dani: DNS tufacturaia.com DNS-only resuelto 2026-05-16, SSL OK en app. y n8n. -->
- **Dani + Gonzalo**: subir certificado P12 por org en Ajustes → VERIFACTU. Sin él no se envían facturas a AEAT y aparece error en dashboard
- **Borja**: review/merge agency-portal PR #54, #55, #56
<!-- Meta plantilla otp_facturaia APPROVED 2026-05-16 (language es_ES). Bloqueo cerrado. -->

## Decisiones pendientes (producto)

- **WhatsApp en starter — confirmar pricing** — desde 2026-06-01 starter incluye `generador_voz`+`presupuestos` con cuota `whatsapp_docs_mes`=30/mes (antes pro-only). Confirmar que es la intención comercial o revertir el toggle en `/admin/plans`.
- **Bot WhatsApp case-2 (cambiar org a media conversación, reprocesar sin repetir)** — deferido 2026-06-01: la versión que reejecutaba el agente 2× en una misma ejecución n8n no es production-grade. Rehacer reusando la última propuesta SIN re-correr el agente, con smoke. Hoy "no en la otra" cambia bien pero pide repetir.
- **Cliente live vs congelado** — hoy snapshot fiscal al crear factura (datos cliente embebidos). Si editas cliente, PDFs viejos conservan datos antiguos (legalmente correcto, confunde UX). ¿Botón "Re-emitir con datos actuales" o congelado siempre? → cuando se decida, ADR en `decisions/`
- **Feature flags experimentales globales** — debatido 2026-05-11, descartado por YAGNI (`org_features` por org ya cubre piloto/experimentos). Si en el futuro aparece caso real de "flag global para TODAS las orgs", usar `system_config` key-value sin UI dedicada hasta tener 3+ flags. NO construir abstracción especulativa
- **Cuenta Stripe — Agentesia Madrid SL vs entidad propia TuFacturaIA SL** — pendiente decisión jurídica antes de crear price IDs y abrir cobros reales. Si TuFacturaIA tiene CIF propio que firma con clientes finales, debe tener cuenta Stripe propia. Modo Test inicialmente. Bloquea P1.3 próxima sesión.
- **Pricing definitivo de Cashflow IA v2** — copy `descripcionLarga` ya refleja v2 completa (eventos + IVA + RETA + payment-pattern + bullet IA + pill crítico) pero el `precioMensual` del catálogo no se ha tocado desde v1 (19€ Conciliación). Revisar si Cashflow IA mantiene el mismo precio o sube a 24€ vs Holded Premium 30€. Ver análisis producto en plan de sesión 2026-05-23.
- **Modelo 130/IRPF en `incluir_fiscal` (Cashflow IA v3 opcional)** — hoy solo IVA 303. Diferido por análisis producto-GTM (la mayoría en directa simplificada tributa al final del año, no trimestre). Re-evaluar si clientes lo piden.

## Índice de áreas (abrir solo el que toca)

| Área | Detalle / estado (vault) | Doc técnico (repo) |
|---|---|---|
| Conciliación bancaria | [[facturaia-conciliacion-roadmap]] · [[facturaia-conciliacion-copiloto-spec]] | `docs/architecture/gotchas.md` §OCR |
| Stock / Inventario | [[facturaia-modulo-stock]] | `docs/architecture/modules-system.md` |
| Centro Fiscal IA | [[facturaia-centro-fiscal-ia]] | `docs/architecture/centro-fiscal-*.md` |
| Multiempresa / billing | — | `docs/architecture/multiempresa-billing.md` |
| Cobros / recordatorios | — | `docs/architecture/cobros-runbook.md` |
| API v1 ↔ portal | [[facturaia-integracion-api-v1-portal]] | `docs/architecture/gotchas.md` §API v1 |
| Voz / WhatsApp | [[facturaia-bot-conversational-redesign]] · [[facturaia-arquitectura-multi-whatsapp-numbers]] | `docs/architecture/gotchas.md` §Voz |
| Emails / comunicaciones | [[facturaia-comunicaciones-emails]] | — |
| Cashflow | [[facturaia-cashflow-chart-saldo]] | — |
| Open Banking PSD2 | [[facturaia-open-banking-psd2]] | — |
| Multidivisa recibidas | [[facturaia-multidivisa-recibidas]] | — |
| Drive sync | [[facturaia-drive-sync-architecture]] | — |
| Onboarding / migración dominio | [[facturaia-migracion-tufacturaia-com]] | `docs/onboarding/PROGRESS.md` |
| Arquitectura general | — | `docs/architecture/ARCHITECTURE.md` · `dependency-map.md` |
| Changelog completo | [[facturaia-historico-detallado]] | — |

---

> ⬇️ **Lo de abajo NO se lee al arrancar.** Detalle bajo demanda: NEXT, Smoke, WIP, backlog, seguridad, specs y referencias.

---
## NEXT (próximas 2 semanas)

- **Auditoría seguridad 4 dims — ✅ MERGEADO + cerrado (#341, main `b7faef38`)** — núcleo sólido (auth/RLS/webhooks OK). Fixes en prod: RBAC `settings/features`+`disconnect` (admin-only, manual-admin §7.1 actualizado), IP confiable en rate-limit (helper `lib/http/client-ip.ts`), reconexión Redis. Sin follow-ups: el `SIGNING_LEGACY_UNTIL` que el audit marcó INCIERTO **ya estaba cerrado desde 2026-06-09** (ver "Migración crons → HMAC v2" más abajo) — el INCIERTO venía de que el agente no veía el env de prod. Ver [[traefik-dokploy-client-ip-x-real-ip-o-ultimo-xff]].

- **✅ Pentest profundo (16 hallazgos) — CERRADO + desplegado 2026-06-17** — auditoría caja-blanca 10 vectores + verificación adversaria. **5 PRs mergeados a main + deploy `done`** (migs 320-323 en prod): **#342** (RLS/RPC: `merge_cliente` callable por anon, `org_features` bypass monetización, +5), **#343** (bypass de pago: gate billing en `withApiAuth`+v1, cablea `requireWriteAccess` que era código muerto), **#346** (atomicidad rate-limit TOCTOU + idempotency carrera, mig 322), **#347** (RBAC `module-suggestions` + anti-farming trial fiscal), **#351** (follow-up: gate billing a nivel RLS `is_billing_readonly`+`user_can_write_in_org` para writes client-side que el gate API no cubría; mig 323). #6/#7 por #341. **Balance: 15 corregidos + #15 (OTP email cap) riesgo ACEPTADO con rationale** (ya acotado 3/h por user; un cap global rompería el fallback email = autogol). **Smokes verdes**: #343 (navegador), #346 (API real: 201+409, 1 factura), #347 (BD determinista). Patrón reincidente (3ª vez): [[postgres-revoke-public-no-elimina-grants-individuales]] · [[defensa-cableada-vs-codigo-muerto]]. → histórico próxima poda.

- **🟢 Factura simplificada (ticket) 094-099 — CERRADO 2026-06-16** — epic mergeado a main (TDD por issue), migs **303/304** en prod (renumeradas desde 302/303 por colisión con `302_feedback`), smoke E2E Playwright verde (wizard → ticket serie S → conciliado+cobrado). Residual: (a) **smoke F2 real** con org `verifactu_activo=true` (sandbox lo tiene en false → quedó `no_aplica`); (b) verificar **redeploy Dokploy** del fix `fuente` (commit `2e4285f0`: `emitir-ticket` daba 500 con `source='web'`). ADR-030 · [[smoke-insert-directo-no-ejerce-el-motor-real]].

- **"Resolver con Claude" (feedback)** — ✅ issues 100-108 + **Fase 4 Tanda 1** en prod (2026-06-16): prompt enrutado por área, badge "Claude trabajando" en la lista, en_revisión al iniciar, "Relanzar con Claude". **NEXT: Fase 4 Tanda 2** (progreso en vivo + loop "Continuar") **y Tanda 3** (email-action + réplicas runner para paralelo). Roadmap + cómo retomar en [[facturaia-resolver-con-claude]] §Fase 4.

- **Fiscal etapa 2 — calculadores** (captura 016/019/021/025 + país/casilla 59 ya en prod, PRs #164/#166):
  - ~~**▶ 017 (modelo 349): calculador**~~ ✅ **HECHO 2026-06-10** — calculador #172 (`460dd2e`) + vista de detalle + IA #173 (`cfd604e`) + **mig 250 EN PROD** (enum 4→10 claves, seed plazos). Ver Progreso. **Fast-follows pendientes**: (1) **export fichero posicional oficial AEAT** (HITL sandbox, como 390 — falta sourcear posiciones byte-a-byte del Anexo); (2) **drill-down por registro** (requiere enriquecer el snapshot con clave/nif por fila); (3) **captura auto-intracom por OCR + detección de operaciones UE olvidadas** (fase 2 — hoy el pipeline OCR no extrae la clave por línea).
  - **SPEC 349 VERIFICADO (BOE Orden EHA/769/2010 + mod. HAC/174/2020; AEAT manuales)** — NO re-sourcear:
    - **Claves (Anexo):** `E`=entregas intracom exentas (no triangular) · `A`=adquisiciones intracom sujetas · `T`=entregas subsiguientes a adquisición exenta (triangular) · `S`=prestaciones intracom servicios · `I`=adquisiciones intracom servicios · `M`=entregas subsiguientes a importación exenta (art 27.12 LIVA) · `H`=como M por representante fiscal (art 86.3) · `R`/`D`/`C`=consigna (transferencias/devoluciones/sustituciones, HAC/174/2020). (El enum 016 solo tiene entrega_bienes/adquisicion_bienes/prestacion_servicios/adquisicion_servicios → mapear/extender.)
    - **Periodicidad (Art 10.2):** mensual por defecto; **trimestral si entregas de bienes + prestaciones de servicios a consignar ≤ 50.000 € (IVA excl.)** ni en el trimestre de referencia ni en cada uno de los **4 trimestres anteriores** (ventana 5T).
    - **Plazo:** mensual 1-20 mes siguiente (julio→agosto+1-20 sep; diciembre 1-30 ene); trimestral 1-20 mes siguiente (4T→1-30 ene).
  - **018** (347), **020** (111/190), **022** (115/180), **023/024** (presentación 3b) — siguen BLOQUEADOS por sourcing del diseño oficial de cada modelo. 3b gated por base legal del conducto + seguro RC (D-013).

- **Onboarding invitación — cabos tras auditoría 2026-05-27** (código base ya en prod, `3fd402e`): (1) **subir TTL del enlace Supabase** 3600→86400 (panel Auth → Email OTP Expiration; manual o PAT) — mitiga "tardó >1h"; (2) **rediseño token-landing anti-prefetch** (fase 2) — único fix definitivo del pre-consumo por escáneres de email, ver [[magic-link-un-solo-uso-lo-preconsumen-escaneres-email]]; (3) **rotar `SUPABASE_SERVICE_ROLE_KEY`** (se vio en transcript del 27/05, exposición local); ~~(4) robustez "password puesta a mano"~~ ✅ **CERRADO 2026-06-09** — `layout-server.tsx` redirige a `/invitacion` si hay membresías `invitado` sin ninguna `activo`. Commit `07e8c92`.
- **[DEUDA recover-password silencioso]** (2026-06-04) — el endpoint `/api/auth/recover-password` devuelve 200 anti-enumeración SIEMPRE, tragándose fallos de envío reales (así estuvo roto el reset sin que saltara nada, ver [[facturaia-historico-detallado]] 2026-06-04). Hacer visible el fallo interno (alerta/log estructurado/metric) **sin** romper la anti-enumeración hacia el usuario. Mismo patrón aplicable a cualquier endpoint con respuesta genérica por seguridad.
- **Centro fiscal IA (módulo Pro add-on)** — análisis completo + plan congelado 2026-05-19 tras revisión de 4 agentes (arquitectura/legal/producto/research AEAT). Cubre 303/390/130/111/115/180/190/347/349 + libro IVA XLSX + export asesor A3/Holded. v1 cuadre+export (NO presenta), v3 presenta como colaborador social RD 1065/2007 (gestión convenio AEAT a arrancar en paralelo). Naming "Centro fiscal IA" en v1, "Agente fiscal" reservado v3. Add-on comprable sobre Pro (`addon_purchasable=true`, ~9 €/mes a validar). **Mig nueva** (número que toque al arrancar, repo en 104 hoy) crea `fiscal_declaraciones` + `fiscal_declaracion_snapshot` (céntimos enteros, no float — anti-drift IVA mixto) + `fiscal_plazos` (seed 2026 con festivos) + `fiscal_avisos`, RLS por org+rol contable/admin/propietario, calculador versionado `src/lib/fiscal/calculadores/v1/...` periodificado por COALESCE(fecha_operacion, fecha) (mig 087), RPC con `pg_advisory_xact_lock` anti-race A-fiscal análoga al cierre. **Sprint 1 incluye WORM Object Lock** (S3 Compliance o Backblaze B2; Supabase Storage NO sirve, service-role puede borrar) **+ sello tiempo eIDAS FNMT** (~0,01-0,05€/sello, valor probatorio art 42 UE 910/2014). 6 sprints + beta privada 2 trimestres + auditoría fiscal externa ~2k€ pre-GA. **Decisiones pendientes al arrancar**: precio exacto, proveedor WORM, proveedor sello, asesoría auditora. Spec completa: [[facturaia-centro-fiscal-ia]]
- **Migración a `tufacturaia.com` — pendientes menores** — F1+F2+F3 completos (2026-05-13), SSL Let's Encrypt OK desde 2026-05-16, 7 crons configurados, mig 074 aplicada, smoke OTP cubierto via Phone State Guard v2 (exec 165+169). **Pendiente sin urgencia**: `tufacturaia.com` raíz responde 404 (falta redirect a `app.tufacturaia.com`), `www.tufacturaia.com` sin cubrir en cert (decidir si se añade). Spec: [[facturaia-migracion-tufacturaia-com]]
- **Multi-org selector para bot WhatsApp (resolución determinista user→org)** — detectado 2026-05-18 00:30 durante smoke E2E post-fix URLs. Hoy `Buscar Org Voz` resuelve `phone → user_id → org_members[0]` sin orden ni filtro de feature, retorna una org arbitraria → bot revienta si esa org no tiene el feature requerido (`generador_voz`, etc.). Caso real: user `m.delmonte.p@gmail.com` apuntando a `Empresa TEST` (sin voz) en vez de `agentesia.madrid` (con voz). **Diseño limpio**: regla "1 phone → 1 user único → N orgs". Pasos: (1) extender `/api/internal/whatsapp/resolve-context` para devolver `orgs[]` con `{id, nombre, plan, features_activos}` filtradas por features candidatos; (2) `Buscar Org Voz` cambia a HTTP node llamando ese endpoint; (3) si `len(orgs_con_feature)==1` continúa; (4) si `len>=2` el bot envía mensaje WhatsApp con botones (max 3) "¿Para qué empresa? 1. X, 2. Y, 3. Z" + estado conversacional nuevo (`chat_session.pending_org_selection`); (5) reply del user resuelve `chat_session.selected_org_id` con TTL N min para no preguntar cada mensaje; (6) si `len==0` responde "ninguna de tus empresas tiene este servicio". **Soluciona también el feature gating por plan**: Starter sin voz + Enterprise con voz convive limpio. Ojo si user tiene >3 orgs candidatas: WhatsApp solo permite 3 botones, fallback a lista numerada por texto. ~2-3 días dev + n8n + tests. Riesgo medio (toca el flujo más crítico del bot). **Pre-requisito barato**: limpieza duplicados `profiles.phone` legacy (UNIQUE parcial solo cubre `phone_e164 WHERE verified`, el legacy `phone` permite duplicados → arreglar in-place caso a caso). Spec: pendiente de crear en `knowledge/projects/agentesia/facturaia-bot-multi-org-selector.md`.
- **Sistema comunicaciones email (Resend)** — 5 PRs secuenciales: mailer core + auth via Supabase SMTP, outbox unificado + 8 plantillas, notifications multi-canal, módulo + UI cliente, admin UI + custom domain + 3 IA. Spec: [[facturaia-comunicaciones-emails]]. Bloqueado por setup Resend manual (cuenta + 2 dominios + DNS + Supabase SMTP + Dokploy env). ~3 semanas total
- **Stripe en activación add-ons** — hoy CTA "+XX€/mes" redirige a `/settings?tab=plan` sin cobro. Conectar checkout para que toggle = compra. Conciliación 19€ y Anti-fraude 9€ ya seedeados
- **Manuales actualizar Bloque 1** — `manual-usuario.md` y `manual-admin.md` describen flujo viejo: 2 botones generar (no 3), no mencionan anular ni tab Abonos ni nuevos pills/badges
- **Abono ligado a factura real** — nueva tool n8n `consultar_facturas_recientes` para que el agente confirme "¿te refieres a F2026-0042 a Tecnocloud por 121€?" antes de generar abono. Liga vía `factura_origen_id`. ~1 día
- **Post-confirm "añadir al catálogo"** — tras crear documento, si hubo productos marcados con ✨ (no estaban en catálogo), enviar mensaje extra con botones "Sí, añadir / No". Endpoint nuevo `/api/voice/learn-product`. ~1 día
- **Cobrador inteligente** — job nocturno detecta facturas vencidas y envía WhatsApp personalizado al cliente del emisor sin intervención. Reutiliza agente WhatsApp existente. Config por org: días de gracia, tono, canales. ~1-2 días. **Impacto alto** — mayor dolor de autónomos/PYMEs
- **Alertas anomalías facturas recibidas** — al ingestar factura de proveedor conocido, comparar importe contra histórico. Si desviación >X% → "Esta factura de Vodafone es 40% más alta que la media". Coste: query histórico en pipeline OCR. ~medio día
- **OCR conversacional bidireccional** — cuando OCR no encuentra campo crítico (NIF, total, fecha), agente pregunta por WhatsApp en vez de dejar fila incompleta. Reutiliza agente conversacional. ~1 día
- **Auto-categorización de gastos** — clasificar facturas recibidas leyendo histórico del proveedor: "facturas de Vodafone siempre van a Telefonía". Pedir confirmación solo cuando hay duda. ~1-2 días
- **Centralizar OCR en un solo workflow n8n** — hoy hay 2 paths: receptor v2 (inline OCR para WhatsApp) y zf2 OCR Factura (HTTP webhook para `/ingesta` upload + email polling). Refactor: que `/api/upload` y `process-attachments` llamen a un único endpoint o consoliden la lógica OCR en un sub-workflow compartido. Reduce deuda + futuras rotaciones de key se hacen en 1 sitio. ~medio día. Apuntado tras incidente 2026-05-12 (key revocada rompió zf2 silente mientras receptor v2 seguía OK)
- **Per-org email domains (Resend Fase 2)** — cada org verifica su propio dominio en Resend para que emails salgan desde `@<su-dominio.com>` con DKIM real del cliente (no `via TuFacturaIA`). Stack: tabla `org_email_domains` (RLS por org) + 4 endpoints API + UI `/settings/emails` con form + DNS records copiables + cron daily re-verificar pending + lógica envío con fallback a `facturaia.com`. ~4-5 días puros. Feature del plan Pro/Enterprise (justifica add-on). Diferido hasta que un cliente lo pida explícitamente — aditivo, no rompe nada migrando después. Spec base: [[facturaia-comunicaciones-emails]]
- **Diagnóstico inteligente rechazos AEAT** — hoy errores AEAT llegan como código técnico. Mejorar para que IA explique en lenguaje llano qué falló y cómo corregir, en el modal de la factura. Reutiliza `ai-validate.ts`. ~horas
- **Cashflow IA v3 — cerrar bucle copiloto v2 web** — registrar las 3 tools voz (`addCashflowExpense` destructive, `getCashflowForecast` read-only, `proposeCobroNudge` read-only) en `src/lib/copiloto/registry.ts` + `types.ts` + system prompt v_bump. Endpoints internos+voz ya operativos desde v2. ~1-2 días.
- **Cashflow IA v3 — features IA segundo nivel** — `simulate_cashflow_what_if` tool read-only ("qué pasa si gasto 3000€ en abril" recalcula `buildCashflowData` con array hipotético sin persistir); `detect_risk_month` + `suggest_invoice_pull` (sugerir adelantar cobro con descuento pronto pago 2% al cliente lentísimo del peor mes); `suggest_recurrente_shift` (detectar concentración >N% en un mes y proponer mover día_del_mes/renegociar). Reutiliza calculadores ya escritos. ~3-4 días.
- **Cashflow IA v3 — detalle por cliente en gráfica + drill-down día** — copy del catálogo (`descripcionLarga`) ya promete "próximas versiones". Sparkline por mes con breakdown clientes + drill-down día clickable. ~2-3 días.
- **Cashflow IA v3 — alerta delta semanal forecast_deteriora** — cron `cashflow-alerts` compara forecast hoy vs hace 7d; si peor mes empeoró >20%, notif distinta (`forecast_deteriora`) además de `caja_baja`. Reusa infra cron existente. ~horas.
- **Cashflow IA v3 — RETA detectado desde movimientos bancarios** — módulo Conciliación ve cargo recurrente "TGSS"/"Tesorería General SS" → autopopular `cuota_autonomos` con el importe detectado. Mata friction completa. ~medio día.
- **🟢 Cashflow IA v3 — detección automática de proveedores recurrentes desde facturas recibidas** (QUICK WIN ALTA IMPACTO, descubierto en audit 2026-05-23): si Vodafone/Endesa/Adobe/etc aparecen 3+ meses consecutivos como factura recibida con importe similar (CV < 0.2), proponer al usuario "¿añadir como gasto recurrente?". Backend: cron diario `cashflow-suggest-recurring` o trigger en `/cashflow` que agrupe `facturas WHERE tipo='recibida' AND fecha >= now() - interval '6 months' GROUP BY proveedor_id HAVING count >= 3` + match contra `gastos_recurrentes` activos del mismo proveedor para no duplicar + tabla `cashflow_recurrent_suggestions` con `accepted_at`/`dismissed_at`. UI: card inline en `/cashflow` "Detectamos 4 facturas de DEMO-Vodafone en últimos 4 meses (40€/mes mediana). [Crear recurrente] [Ignorar]". 1-click crea `gasto_recurrente` con importe = mediana últimos 3 meses + día_del_mes = mediana día factura + frecuencia='mensual'. Plus banner en `/recibidas` row del proveedor "Este proveedor parece recurrente". ~2-3 días. **Impacto medible**: para autónomo con 5-10 proveedores recurrentes, el forecast jul/ago subestima gastos 200-500€/mes — esta feature lo soluciona y mata la friction del setup manual. Es lo que más diferencia hace vs la competencia (Anfix/Quipu/Holded NO lo tienen).
- **Anexo II posicional 303 byte-exact** — PR dedicado, mirror MIT `github.com/hokus15/ArrendaToolsModelo303` disponible (YAML 2.426 líneas), XLSX oficial `DR303e26v101.xlsx` localizado. Cierra promesa "puedes presentar telemáticamente" en v3 Centro Fiscal. ~3-5 días.
- **API v1 — UI interna `descripcion` en formularios crear/editar factura+presupuesto** — backend ya soporta `descripcion` por línea (PR #49) pero la UI de `/generar` no la expone. Añadir `<textarea>` opcional debajo del input de concepto en `generar-view.tsx` + modales edit factura/presupuesto. Máx 2000 chars, contador visual al pasar 1500. ~medio día. Coherencia: si entra por API y se ve en PDF, debe poder editarse en UI también.
- **API v1 — `descripcion` en webhook payload `factura.created`/`presupuesto.created`** — actualmente el payload (mig 025) solo incluye `id, num, tipo_documento, total, estado, fecha` sin líneas. Si Borja u otro consumidor quiere `descripcion` post-creación sin tener que hacer GET, añadir `lineas[]` al payload + bump versión consumer. **NO incluído en PR #49** (Borja no lo pidió). ~horas.
- **Conciliación #44 — PR-A2.1 parser num factura en descripción banco** — regex `\b[ABFP]\d{4}-\d{4}\b` sobre `mb.descripcion`, lookup en `facturas.num` de la org, boost score sugerencia +0.2 + motivo `match_num_en_descripcion`. Caso real detectado en audit 2026-05-20: mov mencionaba A2026-0006 pero sugerencia apuntaba a A2026-0003 (importe igual). Confidence='alta' al match correcto con parser. ~horas. Detalle: [[facturaia-conciliacion-roadmap]] §Fase siguiente.
- **Conciliación #46 — F4 detección reembolso bidireccional** — F4 actual dispara solo desde mov negativo. Si devolución entra ANTES del cobro (import histórico desordenado, duplicate-import), F4 no dispara. Fix: trigger simétrico desde positivo nuevo → buscar mov negativo pendiente con es_devolucion=null y marcar preventiva. ~horas-1día. Edge case, no urgente. Detalle: [[facturaia-conciliacion-roadmap]] §Fase siguiente + §Deuda técnica.
- **Versionado de defaults módulos** — cambiar default global no debe sorprender a orgs sin override (snapshot del valor previo o aviso visible)
- **Log de cambios de config por org** — audit trail granular para reconstruir "quién cambió qué config de qué org cuándo"

## Smoke tests pendientes
- **🟡 PR #371 "Resolver con Claude" — resumen al cliente + prompt editable (NO mergeado).** Diagnóstico técnico de Claude pasa a mensaje **interno** (nunca al cliente); resumen llano = **borrador** con botón "Publicar al cliente" (cola de aprobación, avisa por email); "Ver/editar prompt" → `prompt_override`; todas las capturas al prompt; cliente arrastra imágenes (drag&drop). Runner `ops/ticket-runner` actualizado al contrato (`docs/architecture/feedback-runner-contract.md`). Mig **326**. **Tras merge:** `supabase db push --linked` desde main limpio + **redeploy compose `ticket-runner`** (el contrato viejo `screenshot` singular deja de cuadrar con el claim) + smoke del panel "Resumen para el cliente" con un job real. Patrón: [[llm-runner-output-cliente-bloques-y-aprobacion]].
- **🟡 Modal detalle ticket `/admin/feedback` ancho completo (#362 en main 2026-06-17)** — tras deploy Dokploy: superadmin → `/admin/feedback` → abrir ticket → en desktop 2 columnas 50/50 (izq: descripción+capturas+conversación; der: Resolver con Claude+estado+notas), colapsa a 1 columna ≤760px. Build+typecheck local verde; merge con `--admin` (CI roja por billing). Conflicto con main resuelto preservando render enriquecido `Claude`/`· interno` + campo "Instrucciones para Claude".
- **🟡 Panel admin móvil (#357 en main 2026-06-17, merge `3b5c6f5e`)** — a 390px recorrer `/admin` → orgs / ficha org / system / plans / features: hamburguesa abre/cierra el drawer, navegación cierra el menú, sin cortes ni descuadres ni scroll horizontal indeseado. Solo build verde, NO probado en navegador. (Merge con `--admin`: el único check rojo era el baseline stale de visual-regression `/design-system`, página que no usa `admin.css`.)
- **✅ Pentest #343/#346/#347 — smokes VERDES 2026-06-17** (→ histórico próxima poda): (a) #343 bypass pago verificado en navegador (crear factura→402, PDF/lectura OK; change-plan exento pero bloqueado por su propia lógica de negocio = correcto); (b) #346 idempotency verificado por API real (2 POST paralelos misma key → 201+409, 1 sola factura A2026-0133, fila idempotencia `completed`, factura test borrada); (c) #347 trial farming verificado por BD determinista (BEGIN/ROLLBACK: 2ª org misma billing_account + trial usado en la 1ª → query del fix detecta sibling = 409; control negativo sin trial = 0 = no bloquea). Sandbox dejada en `active`.
- ✅ **"Resolver con Claude" Fase 4 (Q1+Q5) — smoke real VERDE 2026-06-17.** Timeline completo en vivo (preparando→…→`pr_abierto`) + botón del email (consumió token + encoló). Destapó y arregló el bug del acuse 303 a `0.0.0.0` (#352). → histórico próxima poda. **Residual de Q6 (no smoke):** validar 2 builds concurrentes encolando 2-3 tickets a la vez.
- **✅ Perf #310/#311/#312 — SMOKE PROD VERDE 2026-06-17** (Playwright org Sandbox, login iOS vía generateLink+verifyOtp + cookie ssr): (a) `/dashboard` **llamó RPC `dashboard_top_clientes`→200** (mig 318), widget Top clientes con nombres reales, 0 errores consola → #311 desplegado+live (no agregación vieja); (b) `/emitidas` 34 filas, 0 errores → #312 facturas-view OK; (c) `/inventario/<id>` chart recharts renderizó (code-split lazy), 0 errores → #312 ficha-producto OK. Además RPC verificada a nivel BD (RPC-as-user == agregación manual por-org, RLS scopea sin fuga). **(d) #310 check-vencimientos**: lógica set-based correcta + dry-run 0 transiciones hoy / 11 vencidas activas (notify idempotente); **confirmación final = cron nocturno 02:30 UTC** (no disparé el cron global de clientes a deshora).
- **✅ Copiloto Fase 2 fiscal (111-114) — SMOKE WEB VERDE 2026-06-17** (runner real `runCopilotoTurn`+`executeTool` contra BD prod sandbox, script `~/facturaia-copiloto-fase2/scripts/smoke-fase2-fiscales.ts`): los 4 tools rutean+commitean OK (emitir A2026-0128 181,50€ `fuente='copiloto'`, email→`delivered@resend.dev`, anular→abono B2026-0005, convertir P2026-0006→A2026-0129) + bloqueo NIF. **Cazó el P0** `fuente='copiloto'` → **mig 315**. _Pendiente solo el tap físico WhatsApp por acción (abajo)._
- **🟡 Copiloto Fase 2 — SMOKE WhatsApp del tap, por acción (sandbox +34617314938)** — orden = orden de flip. **(c) enviarEmailFactura: ✅ VERDE**. **(d) anularFactura: ✅ VERDE** (anuló A2026-0129→abono B2026-0006; negativo "anula el abono B…"→"no se pueden anular abonos"). **(e) convertirPresupuesto: ✅ VERDE** (P2026-0007→A2026-0130 363€; negativo P2026-0004 sin NIF→bloqueo). **PDF como documento WhatsApp (parity bot viejo): ✅ VERDE** (PR #335 puente devuelve `pdf_url` + PR #326 4 nodos n8n `Tiene PDF Copiloto?→Descargar/Subir/Enviar Documento`; convertir→PDF de la factura confirmado en el chat por Manu). **(a) emitirFactura: ✅ FLIP APLICADO LIVE 2026-06-17** (opción A scope v1; patch `apply-flip-emitir-copiloto.py` rama `feat/copiloto-flip-emitir` commit `9e833c1e`, backup `pqSWkDIHqmSVHotB-pre-flip-emitir-20260617-091134.json`; añade (f) + reescribe PRECEDENCIA, `GENERAR DOCUMENTO` dormido para factura, presupuesto/proforma intactos) — **✅ SMOKE VERDE (tap físico confirmado por Manu 2026-06-17)**: factura emitida por WhatsApp OK. Negativo extra: IVA exento/intracom/moneda → "desde la web". **El set fiscal completo (cobrada/gasto/email/anular/convertir/emitir) está LIVE en WhatsApp vía Copiloto.** Esto también confirma #339 desplegado (el warning Verifactu condicional es #339). En todas: idempotencia doble-tap + rol `solo_lectura`→sin permiso. Fix #331/#339 evitan contaminación del hilo (propuestas y blocked); si reaparece narración/alucinación sin botones → purgar `copiloto_mensajes` del hilo WhatsApp del sandbox. Login UI: sesión service-role (`generateLink`+`verifyOtp`, `m.delmonte.p@agentesia.madrid`) — **`E2E_PASSWORD` ROTADO** (refrescar 1Password vault FacturAIA). **OJO multi-org**: +34617314938 está en sandbox Y AgentesiaLab → nums de presupuesto colisionan; al smokear, confirma que el bot resuelve al sandbox.
- **🟡 Copiloto Fase 2 — RE-TEST tap físico WhatsApp "marcar cobrada"** (server-side verde; bugs del 1er smoke arreglados #292) — desde sandbox **+34617314938**: (1) "marca cobrada la factura A2026-00XX" → resumen determinista + botones ✅/✖️; (2) ✅ → "Hecho… cobrada (X €)" y queda `cobrada`; (3) ✅ otra vez → "Eso ya lo había hecho"; (4) pedir 0047 tras 0058 → debe proponer **0047** (era el bug, ya con prompt v4); (5) `solo_lectura` → "sin permiso"; (6) >30 min → "caducó". Para resetear: ahora hay botón **"Revertir cobro"** en la ficha (#293). Script server-side: `/tmp/smoke-copiloto-confirm.py` (secreto PROD).
- **🟢 UX polish ficha factura (apuntado en smoke 16/06)** — (a) header del drawer con más contraste + icono ✉️ más grande/claro; (b) decidir si la pill "Enviada" es redundante con el icono ✉️ (son cosas distintas: estado vs email-enviado). No bloqueante.
- **🟢 `/recibidas` — menú de fila fuera de pantalla + "Descartar" no eliminaba (ticket Pescados Chivite `e79afd3e`, main `d41111b1` 2026-06-16, DEPLOY DOKPLOY done 16:39 dentro de `73be86a4`)** — (1) el menú "..." de la última columna se salía del viewport (estaba anclado por la izquierda; ahora `right`); (2) "Descartar" en recibida `sin_aprobar` marcaba `disputada` en vez de borrar → ahora abre el confirm de Eliminar (`DELETE /api/facturas/[id]` con cascada FK bandeja+blob). **Smoke LOCAL verde** (Playwright, 2 recibidas sembradas en sandbox + verificado en BD: menú dentro del viewport, sin_aprobar→borrada físicamente, activa→`disputada`; seeds y spec temporal limpiados). Sin migración (push `--no-verify`: ratchet señalaba WIP ajeno de csv-import). **Pendiente acción usuario**: (a) confirmar en prod `app.tufacturaia.com` con Pescados Chivite; (b) limpiar a mano el duplicado `disputada` 95015083 (ya fuera del limbo OCR, "Descartar" no lo borra). **Hallazgo adjunto sin arreglar**: el menú puede desbordar por *abajo* en filas cercanas al fondo (overflow vertical, preexistente) — candidato a fix aparte (clamp `top` vs `innerHeight`).
- **🟢 Toasts repetidos en Bandeja IA (PR #276 en main `3ba5532a`, falta deploy Dokploy)** — ticket Gonzalo Riera (Abba): al volver a `/ingesta` reaparecían toasts "no se pudo extraer datos". Smoke tras deploy: entrar en Bandeja IA con docs ya en `revisar` sin datos → salir a otra página → volver → NO deben salir toasts. Sin migración.
- **🟡 Facturas con varios albaranes — OCR completo + escape hatch (PR #271 `632fdc09`, ticket Pescados Chivite, falta deploy Dokploy)** — bug: factura-resumen con 7 albaranes dentro (Mariscos Wildomar, base 4.828,85) → OCR extraía solo las 5 líneas del 1er albarán → con módulo Stock activo, aprobar daba `422 descuadre_lineas` sin salida. Fix: REGLA 11.1 del prompt (recorrer todos los albaranes/páginas, excluir subtotales `Total HT BL`) + `max_tokens` 1024→4096; escape hatch `{omitir_stock:true}` en `/api/recibidas/[id]/aprobar`; reintentar OCR ahora admite `listo`/`revisar` + botón en barra de acciones. Smoke prod tras deploy: (1) re-disparar OCR del item bandeja `1801eec3-5578-4f2b-8d7e-d107cffcf765` (botón Reintentar OCR) → debe extraer ~18 líneas y Σ ≈ 4.828,85; si `gpt-4o-mini` sigue sin coger todas, subir a `gpt-4o` para docs largos; (2) en una factura con descuadre real → "Aprobar sin stock" archiva sin mover inventario. Build verificado local (push `--no-verify`, sin migración).
  - **🟢 Update 2026-06-16 (PR #291 `38da664d` MERGEADO + DEPLOY Dokploy done 19:16, ticket Chivite `6aa9ab6a`)** — enfoque definitivo del escape hatch: **líneas editables en la bandeja** (cantidad/precio/descripción como inputs + añadir/eliminar línea, persisten en `datos_extraidos.lineas`, footer Σlíneas vs base en vivo) + **comparador PDF** (pestaña en el drawer que despliega el documento al lado) + glass uniforme + selects con chevron; REGLA 11 prompt reforzada (recorrer páginas/albaranes + autochequeo Σ). Conflictos del drawer con #297 resueltos fusionando. Smoke RPC prod (BEGIN/ROLLBACK) verde: líneas reales→`descuadre_lineas|2477.70|4828.85`, cuadrando→`ok`. **⚠️ #271 NO está en main** (ni mergeado, no solo "sin deploy"): su `{omitir_stock:true}`, REGLA 11.1 (excluir subtotales `Total HT BL`) y **`max_tokens` 1024→4096 NO están en prod**. **Causa raíz del truncamiento sigue viva**: `ocr-process/route.ts:463` en main = `max_tokens: 1024` → en facturas largas (3 pág / ~18 líneas) la respuesta del LLM se corta y no devuelve todas las líneas, aunque la REGLA 11 lo pida. Mi edición manual es la red, pero **follow-up recomendado: subir `max_tokens` a 4096 (+ portar exclusión de subtotales de #271)** o mergear #271 conviviendo con #291. **Smoke prod usuario**: en Chivite, abrir la factura, editar/añadir líneas hasta cuadrar → aprobar; comparar con el PDF vía la pestaña del drawer.
  - **🟢 Update 2026-06-16 (#303 + #313 MERGEADOS+DEPLOY done) — CERRADO salvo smoke visual** — #303 hizo el follow-up: `max_tokens` 1024→4096 + REGLA 11 excluye subtotales `Total HT BL` → el OCR ya lee las **18 líneas** y cuadra solo (verificado en :3003). #313: pulido del drawer (portal que cubre la topbar, comparador PDF que carga vía cache-buster, glass coherente claro/oscuro, líneas como tarjetas, tipografía a escala) + **modal "Nuevo producto" migrado al Modal canónico** (porteado → sale sobre el drawer; antes quedaba detrás). 8 agentes (5 diagnóstico + 3 adversariales) + /polish + skill emil-design-eng; validado en localhost por el usuario antes de prod. Migs 311/312 (PRs ajenos) ya aplicadas en prod. **Pendiente: solo smoke visual del usuario en `app.tufacturaia.com`.** Learnings: [[max-tokens-es-tope-no-consumo-trunca-json-ocr-largo]] · [[dos-react-pdf-document-misma-url-nocache-onloaderror]].
- **🟡 Activar control de stock desde el catálogo (main `1b13d0b1` + manuales `1d3cbc77`, 2026-06-16, falta deploy front Dokploy)** — cierra el callejón sin salida: el botón "Activar el control de stock en tus productos" del empty state de Inventario lleva a `/settings?tab=catalogo` pero el form del catálogo no exponía cómo activarlo (solo el modal al facturar). Ahora en el detalle de un producto con **Tipo = Producto** aparece toggle **"Controlar stock de este producto"** + stock mínimo/coste (y stock inicial solo en alta). Smoke prod (`app.tufacturaia.com`, org con módulo stock): (1) Ajustes › Catálogo → producto Tipo=Producto → activar toggle → guardar → aparece en `/inventario`; (2) en alta nueva con stock inicial → ver apertura en historial; (3) producto existente: la nota enlaza a Inventario para ajustar unidades. Smoke local ya verde (SQL apertura+PATCH, navegador login real). Sin migración.
- **🟡 Numeración preview (PR #268 en main + mig 299 aplicada a prod 2026-06-16, falta deploy front Dokploy)** — tras deploy: abrir "Nueva factura" en `/generar` 2-3× sin guardar → el nº de preview NO debe avanzar; luego emitir una → toma el correlativo correcto sin huecos. Ver [[postgres-split-tx-counter-burn-serie-numeracion]].
- **🟡 `POST /api/presupuestos` con sesión Bearer para app iOS (PR #265 de Borja, squash `b4be740` en main + DEPLOY DOKPLOY done 2026-06-16)** — espejo de sesión del `POST /api/v1/presupuestos` (la app iOS crea presupuestos/proformas con el JWT del usuario, sin API key). Smoke: desde la app iOS (`CrearPresupuestoView`) crear un presupuesto → 201 con doc serie P + PDF + audit (`created_via='web'`, `source='mobile'` etiqueta canal); verificar rol (comercial OK, contable 403). Nota menor: validación devuelve **400** (convención v1), no 422 como `/api/facturas` — pendiente decidir si alinear.
- **🟢 Crear gasto/factura desde un movimiento (086/087) — CERRADO + EN PROD** (PR #269 `74d5e781`, deploy 2026-06-16) — el "céntimo residual (99,99 vs 100)" eran 2 cosas: drawer con tolerancia `0.005` hardcoded vs lista (`TOL_CONCILIACION`=0,01)/BD → botones fantasma de resto; y base redondeada antes de tiempo → gasto 99,99 ≠ total. Fix: tolerancia única + `exactBase` (gasto = total exacto). **Smoke BD + UI navegador + POST-DEPLOY prod VERDE ambos flujos** (sesión user test sin password: generateLink+cookie SSR, ver [[supabase-mint-access-token-sin-password-via-generate-link]]). Ver [[sembrar-base-desde-total-con-iva-pierde-centimo]]. _(candidato a mover a histórico en próximo /obsidian-1)_
- **🟢 Conciliación — editar signo/importe (ticket 2e79bd95, prod `fe2367e3` 2026-06-15)** — API ya smoke'd real y verde (PATCH flip signo + guard 409 + 422/404). Falta navegador: (1) `/conciliacion` → Importar extracto (PDF/foto) → en el preview corregir signo ingreso/gasto + importe/fecha/concepto de una fila → Importar → entra con el valor corregido; (2) detalle de un movimiento NO conciliado → "Editar movimiento" → cambiar signo → guarda y se refleja; (3) movimiento conciliado → importe/fecha bloqueados con explicación, concepto sí editable.
- **🔴 Revisar UI conciliación en sandbox — "no se ve muy bien" (tras #259/#261, 2026-06-15)** — chequeo visual fino de `/conciliacion`: (1) **drawer del movimiento** — ¿contraste/azul OK tras quitar el sheen + velo 38%? ¿la caja de sugerencia (factura+razones+Asignar) se ve bien?; (2) **tabla** — ¿filas/columnas cuadran sin scroll horizontal en **móvil y web**? ¿el chip de motivo + badge cuarentena no rompen el layout?; (3) banner "N movimientos con coincidencias" legible. Si algo chirría, pedir a Claude **3-4 variantes numeradas** del panel/tabla. Probablemente queden ajustes de pulido (spacing, jerarquía, densidad) — no quedó 100% redondo.
- **Registro por email branded + onboarding completo (PRs #256 email-Resend + #258 icono, en main `84277b13` — FALTA DEPLOY DOKPLOY)** — tras deploy: (1) incógnito `/registro` con `m.delmonte.p+clienteN@gmail.com` → debe llegar **rápido** el email de confirmación **por Resend desde @tufacturaia.com** (plantilla branded "Ya casi estás", tono cercano), NO el de Supabase; verificar en `email_log` `kind='signup_confirmation' status=delivered provider=resend`. (2) Clic en el enlace → `/onboarding/perfil` → fiscal → canales → `/dashboard?welcome=1`. (3) Reintento con el mismo email sin confirmar → **reenvía** (no se queda mudo). (4) `/recuperar-contrasena` muestra icono **sobre** (no teléfono). Nota: email de recuperación YA verificado funcionando (Resend, delivered 4s) el 2026-06-15; lo único que iba por Supabase era el de registro. Limpiar user+org de prueba al final. Ver [[supabase-bypassear-plantilla-auth-con-admin-generatelink]].
- **Conciliación motor reescrito — DEPLOY + smoke prod (2026-06-13, migs 265-269 SOLO LOCAL)** — antes de smoke: `supabase db push --linked` desde main limpio (`git log origin/main..HEAD` vacío) para aplicar 265-269. Smoke en sandbox: (1) importar extracto → entra mov → si hay factura con score ≥80 única se auto-concilia (pill `Auto`), si <80 o gemelas → sugerencia con **barómetro** visible; (2) confirmar sugerencia → factura cobrada, mov conciliado; (3) `POST /movimientos/[id]/asignar` 1 mov → 2 facturas (cuando exista UI: modal bilateral); (4) parcial → factura `parcial` + Progreso %; (5) intentar asignar > capacidad del mov → 409 `movimiento_sin_capacidad`; (6) importes con **céntimos** en toda la vista. Tests integración (no prod): `npm run test:integration` con colima+Supabase local arriba.
- **Extractos PDF — flujo web** (#218 mergeado; WhatsApp ✅ smoke verde 2026-06-12, ver [[facturaia-historico-detallado]]) — flujo navegador `/conciliacion` → Importar extracto: (1) arrastrar PDF de extracto real → "Extrayendo con IA…" → preview → desmarcar una fila → "Importar seleccionados (N)" → movimientos en tabla, la desmarcada NO; (2) re-subir el mismo PDF → duplicados omitidos; (3) subir una factura PDF → error claro "parece una factura, súbela a la bandeja"; (4) CSV sigue igual (sin preview); (5) foto JPG de extracto. Cuenta quota: cada extract consume `ocr_mes`.
- ✅ **Extractos — import multi-archivo (PR #289 MERGEADO `73be86a4` + deploy Dokploy done + smoke prod VERDE 2026-06-16)** — `/conciliacion` Importar extracto acepta **varios archivos a la vez** con barra de progreso, **preview combinado agrupado por archivo**, un confirm por archivo, errores aislados, éxito con contadores sumados. Rate limits 3→10/5→10/5→10. Smoke E2E real Playwright contra prod: 2 PDF a la vez → "Procesando 1 de 2" → preview con 5 movs + 2 cabeceras de archivo → confirm → **5 importados, verificado en BD** (cleanup OK). Mover a histórico en próximo /obsidian-1.
- **Impersonación superadmin persiste (2026-06-12, en main `6a9afb9b` — ojo: el fix viajó dentro del commit de NIF duplicados de la sesión paralela)** — entrar como superadmin → "Ver como esta org" → navegar varias páginas del dashboard y esperar >1 min con los banners admin cargados → el banner de impersonación debe persistir (antes los fetches background a `/api/admin/*` borraban la cookie en segundos). Salir vía banner → vuelve a org real. Cookie ahora sliding TTL 1h (se renueva navegando, caduca tras 1h inactivo).
- **OCR error + Reintentar + alertas superadmin (2026-06-12, en prod)** — (1) subir foto que el OCR no pueda procesar → la ficha pasa a "Error al procesar" (ya no zombi en "Procesando") y aparece botón primario "Reintentar OCR" que re-procesa el doc ya guardado (vuelve a "procesando", resultado por realtime). (2) Al fallar, llega email de incidencia a `info@agentesialabs.com` + banner fijo superadmin en cualquier org + entrada en `/admin/alerts`. (3) Tras éxito, la incidencia OCR se auto-resuelve. Migs 260 (`system_alerts`) + 261 (`estado='error'`) en prod.
- ✅ **Cron `ingesta-stale-sweep` — cierra ingestas zombi en `procesando` (2026-06-16, en prod)** — Complementa el fix OCR error/Reintentar del 2026-06-12: aquél marca `error` solo si `ocr-process` se ejecuta; éste barre los que **nunca llegan** (n8n caído, contenedor muerto a mitad, `OPENAI_API_KEY` ausente cortando antes — justo el zombi `procesando/90%` de la nota `N8N_WEBHOOK_URL` de arriba). Cron `*/30`, marca `error` las filas en `procesando` con `created_at > 30 min` (espejo de `email-outbox-zombie-sweep`). Extra en `ocr-process`: el early-return de `OPENAI_API_KEY` ausente ahora también marca `error` + `system_alert` global (dedupe único) — antes zombificaba en silencio a **toda** org si caía la key. PR **#302** → main (`79a6502c`), CI verde, **sin migración** (261 ya admite `error`; sweep por `created_at` porque la tabla no tiene `updated_at`). Cron dado de alta en Dokploy **vía API** (`schedule.create`, scheduleId `xEsjmVB5LDnBn0DU2M4uQ`, enabled, mismo patrón `sign-call.sh` que los otros 24) + verificado verde en `/admin/system` (run manual 239ms HTTP 200). Método API documentado en memory `facturaia-dokploy-crons-api`.
- **Cuotas por tier — issue 008 CERRADO COMPLETO 2026-06-12 (incl. WhatsApp)** — web 402 · API v1 402 · copiloto 429 · usage endpoint · WhatsApp: mensaje neutro al 16º (override 15) + factura por voz bloqueada 402 con mensaje real. El smoke WhatsApp destapó 3 bugs del patch n8n issue 003 (IF salidas invertidas, jsonBody sin stringify, neverError tragándose el 402) → 3 PUTs (final v`7ccd6dd1`) + apéndice warn80 re-adjuntado en parser (sin smoke en vivo: cap real 300 no salta) + PR #217 (panel admin pintaba uso 0 en keys no consultadas). Overrides sandbox borrados. Residual: primera semana vigilar burbuja `/admin/alerts` por falsos positivos del collector.
- **Notifs fiscal 130 falsas + drawer UI (PR #214)** (✅ main `ea3e4cc8` + deploy Dokploy done 2026-06-12 11:42; ✅ **cron verificado en prod** vía run manual `schedule.runManually` 09:50 UTC: `success`, 130 con salientes=0 — falsos positivos eliminados; drift restante "1 nueva" es real, abono B2026-0001) — queda acción usuario: (1) marcar leídas/descartar las notifs fiscal viejas; (2) abrir y recalcular borradores 130 2T/3T/4T (limpia `requires_recalc` y refresca snapshot → cron se calla); (3) check visual drawer: X 28px desktop sin focus ring, chips con fade derecho. Nota: 1 solo aviso `fiscal_avisos` para 7 recalculadas = índice único `uniq_fiscal_avisos_decl_tipo` (by design).
- **Perf auth dedupe — ticket cashflow `1762f07e`** (✅ main `0bf7a0c` PR #209 2026-06-12, deploy Dokploy done 2026-06-12 11:42 junto a #197/#214) — smoke: (1) `/cashflow` con sesión normal carga visiblemente más rápido (los 5 GETs `/api/internal/cashflow/*` pasan de ~7 a ~5 round-trips, 3→1 llamadas GoTrue); (2) regresión auth: login normal, superadmin con impersonación (`?org_id=` y cookie), rol `solo_lectura` recibe 403 en writes, y app iOS (Bearer) sigue autenticando; (3) responder al usuario del ticket (bgchivite) por el hilo de feedback.
- **Tickets feedback — banner + hilo + prompt Claude** (✅ main `0b64502` 2026-06-12, mig 254 EN PROD) — (1) crear ticket desde sandbox con usuario normal → como superadmin debe aparecer **banner azul "1 ticket nuevo"** en cualquier página (espera ≤60s) → X lo oculta → otro ticket nuevo lo reaparece; (2) en `/admin/feedback` abrir detalle → **Conversación** → responder → al usuario le llega email "Te hemos respondido" + badge en botón feedback + ticket pasa a `en_revision` (banner desaparece); (3) como usuario: Mis tickets → expandir → contestar → email de aviso a Manu; (4) botón **"Copiar prompt para Claude"** copia plantilla con descripción + conversación.
- **Robustez LLM PR #197 (tras merge+deploy)** — Copiloto (valida bump a Sonnet 4.6 + tools): (1) "¿cuánto he facturado este mes?" → cuadra con dashboard; (2) "¿qué clientes me deben dinero?" → sin cobradas; (3) "busca las facturas de <cliente> de este año"; (4) "redáctame recordatorio de cobro para <num pendiente>" → datos exactos sin inventar; (5) "¿qué es este movimiento de <importe>?" → explain+pattern; (6) "compara mis dos empresas" en org SIN multiempresa → mensaje UX en español, no `feature_unavailable:` crudo; (7) trampa "¿cuánto facturé en 2019?" → admite que no hay datos. OCR (valida path Zod+audit): (8) factura PDF por WhatsApp → "Listo para revisar" con prov/nif/total/moneda; (9) ticket SIN NIF → solo anomalía NIF; (10) factura en USD → `confianza_moneda` poblada; (11) SQL: `ocr_extraction_audit` post-deploy sin `llm_json_parse_error` (si aparece, ahora es visible — esa es la mejora).
- **Modelo 349 vista + IA** (✅ main PRs #172/#173/#174 + migs 250/251 en prod, deploy done 2026-06-10) — org con facturas `es_intracom` marcadas + clave + contraparte con `nif_iva`: ir a `/fiscal/2026/349/<periodo>` → (1) tabla de **registros** (NIF-IVA·país·clave+leyenda·base·nº ops), NO casillas vacías; (2) **badge periodicidad** mensual/trimestral con el porqué (o "sin determinar — recalcula" si snapshot viejo); (3) chip ámbar "VIES sin validar" en operadores no validados; (4) **"Explícame esto"** genera texto que cita los operadores/claves reales (no inventa) y cachea; (5) banner "fichero oficial pendiente" + botones PDF/TXT ocultos; (6) factura intracom SIN clave → cuadre crítico `C-349-falta-clave`. Post-audit añadido: cuadre `atencion` si un registro queda con base negativa. **Hechos ya por agente**: NIF-IVA normalización E2E + baseline API 200. **Smoke visual hecho por usuario 2026-06-10** (3 hallazgos → corregidos en PR #177 `7304fe3`). ~~**Residual**: 403 feature-gate~~ ✅ **cerrado por tests 2026-06-11** — E2E prod inviable limpio (sandbox tiene feature `fiscal` y quitarla mutaría `plan_features` reales; única cuenta no-superadmin `m.delmonte.p@gmail.com` sin credenciales). Verificado en código (`with-api-auth.ts` §2.6 gate con "superadmin saltea") + `src/lib/api/__tests__/feature-gate.test.ts` 8/8 verde.
- **Fiscal etapa 2 captura** (✅ merged main PR #164 `e4a4156`, 2026-06-10; migs 246/247/248 en prod) — ✅ **(1) VIES 2026-06-11**: `IE6388047V` → "NIF-IVA válido en VIES · GOOGLE IRELAND LIMITED · 11/06/2026", persiste tras recargar (BD `vies_validado=t`, `pais=IE`); `DE123456789` → "no válido", cancelado sin guardar. ✅ **(2) intracom 2026-06-11**: `/generar` checkbox → selector clave → guardar borrador → BD `es_intracom=t` + `clave=prestacion_servicios` → reabrir (Editar) → checkbox + "Prestación de servicios UE" persisten; borrador smoke limpiado vía BD (el Eliminar de UI no completó — ver hallazgos sesión). **Pendiente**: (3) **OCR retención/arrendamiento**: subir a bandeja una factura recibida con retención IRPF y otra de alquiler con ref catastral → el OCR sugiere `tipo_retencion` / `es_arrendamiento`+`referencia_catastral` con "Sugerido por IA" + confianza → override manual → aprobar → persiste en `facturas`. (4) helper plazos: `npx vitest run src/lib/fiscal/__tests__/plazos.test.ts` (16 verde).
- **Fiscal Fase 1** (✅ merged main PR #163, 2026-06-10; migs 241-245 en prod; deploy `76e2070`) — ✅ **(1) perfil round-trip 2026-06-11**: cambiar `estimacion_irpf` → guardar → persiste en BD+UI y los flags NO mostrados quedan intactos (fix del bug crítico confirmado); revertido a `directa_normal`. ✅ **(3) "Explícame esto" 2026-06-11**: 303 2T grounded — cita casilla 08 = 1064,20€ y facturas reales (Borja 1210€ / Sansee 1041,58€ / abono −605€) + disclaimer asesor. ✅ **(4) cron 2026-06-11** validado vía tabla `cron_runs` (Run Now cross-org denegado por permisos): `fiscal-generar-borradores` success 09:14 dokploy `{orgs_evaluadas:2, plazos_vigentes:6, borradores_creados:12, errores:0}`. **Pendiente HITL Manu**: (2) fichero oficial 303 → subir al **sandbox AEAT Pre303** → debe importar sin reformatear. **Observación sin investigar**: el 303 sandbox muestra importes en casillas 07/08 "Recargo equivalencia" con `recargo_equivalencia=f` en perfil.
- **Overflow móvil en dispositivo físico** (PR #161, 2026-06-10) — abrir en TU móvil real `/generar`, `/emitidas`, `/recibidas` (y de paso `/presupuestos`): la página NO debe scrollear en horizontal ni verse "encogida/descuadrada"; el formulario de Nueva factura ocupa el ancho; las tablas de listas scrollean DENTRO de su tarjeta. Validado a 390/360px con emulación; falta confirmar en hardware real (era la queja original).
- **Outlook / Microsoft 365** — `app.tufacturaia.com/settings?tab=integraciones` → card "Microsoft 365" visible (sin "Próximamente") → Conectar → pantalla login Microsoft muestra "TuFacturaIA" → autorizar → vuelve a settings con estado conectado → "Revisar ahora" → mandar email con PDF a la cuenta conectada → comprobar bandeja ingesta.
- **Consistencia glass Ajustes (PRs #193-196, en main 2026-06-11)** — `/settings` integraciones (riel C1 + cards losa) · plan (cards losa) · módulos (toggles + badges T3 Gota). Verificado en LOCAL; **integraciones/plan/módulos en prod aún sin smoke**.

_Pasos detallados de smokes ya verificados → [[facturaia-historico-detallado]]. Aquí solo los pendientes reales:_


- **Recover-password E2E** (2026-06-04) — ✅ **mitad crítica 2026-06-11**: `/recuperar-contrasena` (sesión anónima) → "Revisa tu email" → fila `email_log` `kind=password_reset` `status=delivered` en 4s (el INSERT que moría por CHECK ya funciona). **Pendiente** (a propósito, para no cambiar la pwd real): link `/api/auth/callback?type=recovery` → `/nueva-contrasena` → cambia pwd.
- **Gonzalo `.madrid` → WhatsApp** (2026-06-04) — escribir desde `34609779229` al bot → enruta a **Abba** sin selector multi-org → emite/consulta OK. (Bloqueado solo de validación; config verificada.)
- **2FA E2E** (nuevo) — activar TOTP (QR Google Authenticator→código→guardar recovery) · logout→login pide código · recovery code desactiva factor · enforcement org (require_2fa→user sin 2FA bloqueado tras 7d) · inactividad cierra sesión.
- **Modal factura** — lightbox de imagen (subir scan .jpg/.png a bandeja → preview con zoom) + envío real de email desde el modal.
- **WA multi-org** (PR #118) — crear cliente/proveedor por WhatsApp con 2 orgs → selector interactivo.
- **WA multi-org documentos** (2026-06-01, P0 fix + case-1) — teléfono con 2+ orgs: "hazme factura a X de 100€" → bot pregunta empresa (lista) → eliges → propone **sin pedir repetir**. Borrar sticky web_switch antes: `delete from whatsapp_org_selection where source='web_switch'`. Verificar cuota: starter bloquea al doc 31 del mes (`quota_exceeded`).
- **Multidivisa** — ✅ **emitidas USD 2026-06-11**: `/generar` MONEDA USD (tipo de cambio autocompletado 0,866626) + 1000€ → emitida A2026-0104 sandbox 1210,00 US$ ≈ 1048,62 €; PDF con totales en US$ + bloque "EQUIVALENCIA EN EUROS" citando RD 1619/2012 art. 6.1.j; BD `moneda=USD`, `tipo_cambio=0.86662622`. **Pendiente**: recibidas USD vía email/WA real (wire OCR moneda).
- **WA Boost conversacional** — "¿cómo voy de caja?" / "¿puedo permitirme 1.500€?" / "apunta 40€ gasolina".
- **Stripe checkout E2E real** (live, 2026-06-01) — `Settings → Facturación` → activar p.ej. Pro mensual → checkout muestra **49€ + 21% IVA**, pide **NIF + dirección**, **trial 14d con tarjeta** → pagar (no cobra hasta día 15) → al volver banner activo + webhook `checkout.session.completed` activa org (`billing_status=active`, `base_stripe_subscription_id` poblado). Cancelar antes del día 14 en Customer Portal. Smoke Tax API ya verde.
- **UI polish V1/V2** (PR #125/#126, deploy 2026-06-01) — dark mode en `settings/verifactu` + `calendario` (colores legibles) · 7 rutas con throttling → skeleton no blank (agentes/calendario/cashflow/conciliacion+reglas/generar/informes) · confirm dialogs producto (clientes archivar/eliminar, series descartar, fiscal apagar Verifactu, webhooks editar/borrar) · empty-state Resumen con 0 facturas + 1 doc en bandeja → variante "Tienes N esperando revisión" (PR #124) · admin/email-logs reenvío → toast sileo.

## WIP (sesiones en curso, branches sin mergear)

- **🟢 Informes de analítica (089-092) — CERRADO, en prod, mig 296 aplicada.** 4 informes en `/informes` (Ventas, Gastos, Objetivos, Actividad) con par realizado/pendiente (Ventas: "Pendiente de emitir" borradores · Gastos: "Pendiente de aprobar" sin_aprobar), margen, objetivos con sugerencia IA + widget, actividad acotada por rol. Tool copiloto `getInformeVentas`. Auditado multi-agente + Playwright (read+write) verde. Abonos excluidos en informes/dashboard/copiloto (303 sí los incluye). Detalle completo → [[facturaia-historico-detallado]] §2026-06-16. **Pendiente opcional**: smoke UI ventana "Pendiente de emitir" cuando haya borradores (cubierto por test).
- **🟡 Crear factura/gasto desde un movimiento (086-088) — A+B+C en prod, patch n8n aplicado, smoke WhatsApp en curso.** Ticket 2e79bd95 p2. PRD `issues/prd-conciliacion-crear-doc-desde-movimiento.md`. A+B cerrados. **088 slice C (WhatsApp, HITL) — SERVER en prod `51fa9c6b` 2026-06-16**: mig **297** aplicada (`wpa_action_type` += `crear_documento_desde_movimiento`), `crear-recibida.ts` fuente única (refactor de `/api/recibidas`), `buscar-movimiento-para-doc.ts`, `POST /api/internal/whatsapp/buscar-movimiento` (selector multi-org, gating, discriminadores none/multiple/needs_cliente/confirm/org_pendiente/upsell), branch en `pending-action/execute` (reclama pending delete-then-create + crea vía crearRecibidaConcilia/createDocument + concilia). **Smoke BD verde** `node scripts/smoke-088-buscar-movimiento.mjs` (7/7, mig 297 + filtrado). Tests unit+branch verdes. Manuales actualizados. **Patch n8n APLICADO** (`d2fcc81b`, PUT versionId `73cd3141`, receptor `pqSWkDIHqmSVHotB` — NO `zYcHHa8jWXB6dY5i` obsoleto; backup `ops/n8n-backups/20260616-0201-...json`). Aditivo, verificado: active, 169 nodos, conexiones intactas. Arquitectura `tipo`-emit (el agente emite `tipo:'crear_doc_desde_movimiento'`, rama en `Parsear y Calcular Totales` llama `buscar-movimiento` por HMAC + construye `wa_full`, nodo `Enviar WA Crear Doc` con credencial WA lo envía; botón `crear_doc_confirmar:<org_id>` → clon `Ejecutar Crear Doc Pendiente` → execute). Script `ops/n8n-patches/apply-crear-doc-desde-movimiento.py` (idempotente). **SMOKE WhatsApp real EN CURSO** (Borja, 2026-06-16): **gasto end-to-end VERDE** (botones → crea recibida + concilia). Bugs cazados/arreglados en caliente: (1) `d135d00e` el AI Agent emite `cliente:{id:null,…}` y el Zod `.optional()` no aceptaba `null` → 400 → "No pude buscar"; schema ahora null-tolerante. (2) `bfec64ea` UX: el mensaje de gasto incluye el concepto («PAGO A MANUEL…»); resolución de cliente de factura **por el nombre dicho** (ILIKE, lista corta filtrada — no vuelca 1000 clientes; si no existe pide su NIF), prompt n8n actualizado (live versionId `da5334e2`: tap `crear_doc_cli:<mov>:<cli>` + continuación NIF). (3) `1670a275` `texto` ya no filtra el movimiento cuando hay importe ("factura de 200 de <cliente no en el concepto>" antes daba none). **SMOKE E2E SERVER VERDE** (firmado HMAC vs prod sandbox): discriminadores multiple/none/cliente_ambiguo/cliente_no_existe/confirm + **factura buscar→confirm→execute → emitida A2026-0124 cobrada+conciliada** + gasto recibida+conciliada (docs limpiados). **Falta solo (opcional, server ya probado)**: tap-cliente→emisión por WhatsApp + **regresión** facturación normal/OCR/cobros (parser compartido). Datos de prueba en sandbox (2× −121 manuel, +200 manuel). Rollback: PUT del backup. **A/B**: smoke UI navegador VERDE (PR #269, fix céntimo + tolerancia).
  - **Modelo del bot (decidido 2026-06-16)**: híbrido — **conversación abierta para LEER** (tools `consultar_*`, ya parcial) + **acciones CONFIRMADAS para ESCRIBIR** (emitir/registrar vía botones; un LLM nunca escribe documentos fiscales sin confirmación). 088 es una acción más. El **asistente conversacional AMPLIO** ("ChatGPT acotado a TuFacturaIA+org sobre TODO el dato: fiscal, conciliación, informes, cashflow, alertas") es iniciativa propia → `Asistente IA multi-canal` [[facturaia-bloque-4-agent-query-spec]]. Acordado: plan numerado del asistente amplio tras cerrar 088 (opción 3).
- **`feat/factura-detail-split-modal`** (rama) — incluye el sprint **Seguridad/2FA** SIN commit. Bloqueador: aplicar mig 196 a prod va con el merge a main. (smoke 2FA en `Smoke tests pendientes`)
- **Rediseño UI/UX glass — 100% MERGEADO a main/prod** (PRs #170-195: 4 familias —botones V2, KPI losa, modales C2, tabs/filtros riel C1— + superficies app-wide + APCA 30/30 + toolbar + summary, coherente light+dark). Estado vivo en `docs/design/ESTADO.md`. Changelog PR-a-PR → [[facturaia-historico-detallado]].
- **Beta-feedback (PRD 001-008) completo en prod — 2 gaps menores abiertos**: (1) faltan tests de `POST /api/feedback` + `checkFeedbackRateLimit` (issue 003); (2) el badge no se reactiva tras resolución (`PATCH /api/admin/feedback/[id]` no pone `visto_por_user=false`; el email sí sale).
- **Pendiente menor (glass / CI):**
  1. **Barra-resumen** `.summary-hero` (facturas/presupuestos) sigue plana (vive en `globals.css`) — glassificar en PR corto si molesta.
  2. **Smoke prod** `app.tufacturaia.com` light+dark.
  3. **Billing Actions** ([[project-github-actions-billing-blocked]]): regenerar baselines visuales + verificar CI cuando se arregle.

- **Ramas locales retomables (triaje 2026-06-12, regla 2 semanas)** — el resto se borró tras verificar merged/superseded:
  - `feat/fiscal-etapa2-captura` (6 commits, 06-10) — issues 019 (retención IRPF + IA OCR) y 021 (arrendamiento + ref catastral + OCR). Parte de Fiscal Etapa 2 (016-025).
  - `feat/glass-tokens` (5 commits, 06-10) + `stash@{1}` — glass overlays globales olas 1-2 + mesh + topbar scroll. **Revisar solapamiento** con el porteo glass ya mergeado; probablemente parcialmente superseded.
  - `stash@{0}` glass-tokens-leakage-cleanup (sobre feat/fiscal-aeat).
  - `docs/design-research` (3 commits, 06-10, worktree `~/facturaia-design`) — guía bloques 3-13 + research fundamentos.
  - `feat/agentes-restyle-emitidas` (4 commits, 05-31) — cabecera estado automatización + medidor cobertura en /agentes (feature). Re-evaluar tras glass.
  - `fix/wa-boost-v1-audit-cashflow` (3 commits, 05-27) — fixes cashflow WhatsApp: doble conteo saldo, abonos en cobros, rate-limit, horizon_days. **Bugs reales sin mergear — priorizar.**
  - `stash@{2}` WIP Stripe multidivisa (sobre feat/saas-fase2-stripe-completo).
  - `test/stock-e2e` (worktree `~/facturaia-stock`) — entorno E2E stock, credenciales en `.env.local`/`.env.test` (backup 1Password).

## LATER (backlog)

- **Backends módulos pendientes** — Fiscal **promovido a NEXT con spec completa [[facturaia-centro-fiscal-ia]]**. Pendientes aún en LATER: Firma eIDAS, Cashflow IA forecast. ~21 opciones config con badge Próximamente
- **Conexión bancaria automática PSD2 (GoCardless BAD)** — paso 4 del roadmap, tras Norma 43 + trigger emitidas. Tabla `bank_consents` cifrada, endpoints connect/callback/sync, cron 4-6h, dedupe por `provider_transaction_id`, notif `bank_consent_expiring` 7d antes. Empezar con sandbox + 1 banco real (BBVA o Santander). 1-2 semanas. Reutiliza `movimientos_bancarios` y triggers 061 tal cual. Spec: [[facturaia-open-banking-psd2]]
- **Pasarelas de cobro (Stripe Invoice + Redsys)** — módulo aparte, no conciliación. Webhook `invoice.paid` → match por `metadata.factura_id` → estado cobrada sin pasar por extracto. Cablear cuando Stripe checkout para add-ons esté en NEXT. Spec: [[facturaia-open-banking-psd2]]
- **Asistente IA multi-canal** ⭐ **(opción 3 — EN MARCHA 2026-06-16, Fase 1 = LECTURA)**. **HALLAZGO CLAVE**: el asistente "ChatGPT sobre la org" YA EXISTE como **Copiloto v2** (`src/lib/copiloto/` — `runCopilotoTurn`, tool-use Anthropic nativo, registry + 10 tools: getKPIs, getDeudaPorCliente, searchFacturas, searchMovimientos, getInformeVentas, getCliente, explainMovimiento, analyzeMovimientoPattern, compararEmpresas, composeCobroMessage). Cubre cobros/KPIs/facturas/movimientos/informes. **PERO solo enchufado a la WEB** (`/api/copiloto/message`, withApiAuth sesión). El **WhatsApp** usa el bot de intención antiguo de n8n (`consultar_*` toolCode), limitado. → **Fase 1 NO es construir tools (existen): es el PUENTE WhatsApp → Copiloto v2**. **PUENTE SERVER HECHO + desplegado `f6d4bc5e` (DORMIDO)**: `POST /api/internal/whatsapp/copiloto` (service-auth) envuelve `runCopilotoTurn` — resuelve org (selector multi-org) + usuario por teléfono, gate `copiloto` + quota `copiloto_tokens_mes`, hilo de memoria propio (`titulo='WhatsApp'` en copiloto_threads), devuelve texto. Cero duplicación de tools. No cambia WhatsApp hasta enrutar n8n. **SIGUIENTE (con smoke, contigo)**: ruteo en el receptor `pqSWkDIHqmSVHotB` — preguntas → este endpoint y relay; acciones (crear/emitir/registrar, incl. 088) → bot de intención + botones. Acciones de escritura del copiloto (Sprint B, `destructive:true` en `types.ts`) seguirán necesitando confirmación por botones. Modelo híbrido leer-abierto / escribir-confirmado. Spec vieja: [[facturaia-bloque-4-agent-query-spec]].
- **Google OAuth email por org** — cada org envía desde su email vía Gmail API
- **Canales Ingesta + Plan/Facturación** (spec 2026-04-24) — rediseño canales sin toggles, página planes reales con método pago e historial
- **Conciliación bancaria IA** (spec 2026-04-21) — 5 tablas, pipeline Claude 2 fases, UI aprobación por lotes
- **Cleanup mockup AgentesiaLab** — plan SQL elaborado. Org `ea201784-...`
- **Multi-WhatsApp números por org** — Tecnocloud necesita su phone_number_id Meta. Spec: [[facturaia-arquitectura-multi-whatsapp-numbers]]
- **Exportación contable** — formato compatible con gestorías (A3, Sage, etc.)
- **App iOS sin Swift** — Expo Go / React Native, mínima para consultar facturas recibidas/emitidas/bandeja IA. Gestión compleja → desktop. Valorar dificultad vs nativa Swift
- **Control de stock**
- **Enlace contable** para programas de contabilidad
- **Entrada y salida de caja**
- **Liquidaciones de facturas recibidas**
- **Facturación recurrente con SEPA**
- **Dashboard financiero avanzado** con predicciones cashflow basadas en patrones históricos

---

## Ideas crudas / inbox

_(volcado sin filtrar — pasan a NEXT/LATER si maduran, o se descartan en poda quincenal)_

- **Email_log Fase A** (recomendado próxima sesión, ~30min) — tabla `email_log` con insert en `send-factura`/`send-presupuesto`/`notify-quota`. Endpoint `/api/admin/config/email` devuelve último envío + counts 24h/7d. Sin webhook Resend de bounces todavía (Fase B siguiente). Hoy panel Email muestra "No configurado" + "Sin tracking en v1" — bloquea visibilidad cuando se configure SMTP
- **AEAT health derivado de últimas N facturas** (~5min) — cambiar `/api/admin/config/fiscal` para que el semáforo del panel se calcule desde las últimas 5-10 facturas enviadas (todas fallaron = rojo, alguna falla = ámbar). Sin ping a AEAT (lento + bloqueable). Más reactivo que tasa absoluta 30d
- **Exportar matriz planes a CSV** (~15min) — botón en `/admin/plans` que devuelve CSV plan↔feature para mandar a Borja o preparar pricing page. Alternativa al comparador visual (descartado por baja utilidad)
- **Test e2e Playwright middleware activo** (~1-2h, P0 antes de la próxima refactor del middleware) — disparado por incidente 2026-05-20 (middleware off 3 semanas tras rename `middleware.ts → proxy.ts`). Test: arranca server, hace request a `/?impersonate=ea201784-...` (con sesión superadmin mock), verifica que la cookie `impersonate_org` se set en la respuesta; navega después a `/` sin query param, verifica que la cookie se borra. Si alguien revierte o rompe el middleware otra vez, este test cae en CI. Coste casi cero, prevención fortísima. Considerar también un smoke test similar para `phone-verified-gate` (request a `/dashboard` con user sin teléfono verificado → debe redirect a `/verificar-telefono`).
- **Auditoría general "lo crítico está cubierto por capa N" tras restore middleware** (~1h, medio P1) — el incidente reveló que el rename del middleware no se notó porque otras capas (admin layout, dashboard layout) cubrían lo más visible. Listar conscientemente qué responsabilidades viven en cada capa (middleware vs layout vs handler vs RLS) y dónde una sola capa basta vs cuándo hay defensa en profundidad. Salida: una matriz en `Stack/facturaia.md` o `decisions/ADR-NNN-defense-in-depth-layers.md`. Evita el "no sabíamos qué se rompió" del próximo refactor crítico.
- **Banner "Estás impersonando X" más visible** (~30min, P2 UX) — actualmente el `ImpersonateBanner` es pequeño arriba del dashboard. Tras el incidente queda claro que es estado fácil de olvidar. Considerar: (a) hacerlo sticky en topbar, no solo arriba del contenido; (b) cambiar fondo del topbar a un naranja/ámbar suave mientras esté activa; (c) registrar evento `admin_impersonation_active >24h` para que aparezca alerta en el panel admin (¿se quedó pegada?). Bajo riesgo, mejora UX considerable.
- **Refactorizar `extractError` en el portal a un helper compartido** (~1h, P2 calidad) — el patrón "TuFacturaIA me devolvió `{error:{type,message,param,request_id}}` y quiero `message`" lo arreglé hoy en 3 sitios distintos (`auto-emit.ts`, `pdf/[shadowId]/route.ts` portal + el handler manual fetch). Extraer a `lib/facturaia/extract-error.ts` con tests + reemplazar las 3 copias. Evita drift entre handlers.
- **Test e2e PDF binario vs JSON{url}** (~1h, P2 robustez) — el cambio de TuFacturaIA (PDF binario en lugar de JSON{url}) se detectó por user en producción, no en CI. Añadir test e2e en portal que (a) genera factura mock; (b) hace GET `/api/facturaia/pdf/[shadowId]`; (c) asserta `Content-Type: application/pdf` + bytes empiezan con `%PDF-`. Hoy estamos protegidos por el bypass de openapi-fetch, pero si alguien vuelve a usar `fia.GET('/facturas/{id}/pdf')` directamente, este test lo caza.
- **Migrar el resto de consumidores de `impersonate_org` a un helper común** (~2h, P2 calidad) — hoy hay 3 sitios que leen la cookie con override semantics (`with-api-auth.ts`, `render-pdf/route.ts`, `(dashboard)/layout-server.tsx`) y 1 con union semantics nueva (`/api/documents/file` post PR #52). Extraer un helper `resolveAccessContext({ ownership: 'strict-override' | 'union' })` que documente la decisión y reduzca riesgo de divergencia. No urgente pero ayuda a futuros lectores.
- **Bot WhatsApp rediseño conversacional** — promovido a [[facturaia-bot-conversational-redesign]] (spec completa 2026-05-21). Bug "Hola → 🧾 con campos vacíos" + detección first-time + tono colega contable + Phase 2 Interactive Lists + sticky-aware. 3 PRs incrementales (PR-Bot-1/2/3), ~12-16h totales. Requiere LLM desbloqueado (`/admin/llm` hoy con 429 OpenAI + key inválida Anthropic).
- ~~**🔴 P0 FISCAL — PDF abono muestra importes POSITIVOS en lugar de negativos**~~ ✅ **PR #64 MERGEADO + SMOKE POST-DEPLOY VERDE 2026-05-21** (commit `d84f10d` en main). Smoke: anulada A2026-0014 (605€) → abono nuevo generado con código del fix → PDF renderiza importes negativos correctos. Único pendiente menor: decisión regen masivo PDFs históricos abonos pre-fix (en sandbox solo B2026-0001 con PDF antiguo).
- ~~**🔴 P1 UX — Conciliación: vinculación factura↔movimiento INVISIBLE en la UI**~~ ✅ **PR #65 MERGED 2026-05-21 12:05 UTC** (verificado 2026-05-22 10:25 via `gh pr view 65`, branch `feat/conciliacion-vinculacion-visible`). 10 archivos, +906/-33. Mig 131 helper SQL `desvincular_asignacion` con orden crítico last_revert_at-first (evita loop trigger infinito descubierto durante implementación) — **función verificada en prod 2026-05-22 10:25** (PostgREST hint confirma signature `(p_asignacion_id, p_razon, p_user_id)`). Endpoint `POST /api/conciliacion/asignaciones/[id]/desvincular` role-gate propietario/admin/contable, drawer rediseñado (border-left ok, SVG link, factura prominente, botón desvincular + modal confirm), microtexto match en tabla `/conciliacion` y listados `/emitidas` `/recibidas`, modal detalle factura con bloque "Cobro/Pago registrado" leyendo N:N para pagos parciales. **Auditado pre-código con 3 agentes paralelos** (backend/UX/arquitectura) — 3 P0 reales aplicados (loop trigger, leak roadmap, native confirm). Smoke SQL verde. Docs gotchas §Conciliación añadido en commit `f871232` main. Smoke UI Playwright `tests/e2e/smoke/conciliacion-vinculacion.spec.ts` listo (Progreso 2026-05-22 00:35), pendiente ejecución headless con storageState.
- ~~**UX `/emitidas` — acción "Anular y emitir abono" desde menú 3-puntitos de la fila**~~ ✅ **HECHO 2026-05-21 21:00** (commit `270d110` en main, componente reutilizable `anular-confirm-modal.tsx`). Smoke UI pendiente post-Dokploy redeploy. Ver Progreso 21:00.
- ~~**Bug ruido — `GrandfatherBanner` llama `/api/profile/me` (404)**~~ ✅ **HECHO 2026-05-21** (commit `aa6b27b` en main). Refactor server-side: props `phoneVerifiedAt`+`isSuperadmin` desde layout-server (no fetch client).
- ~~**🔐 Rotar OpenAI key + fix typo Anthropic**~~ ✅ **HECHO 2026-05-21 19:30**. OpenAI rotada (Dokploy app + n8n credentials), Anthropic key nueva añadida en Dokploy nuevo (typo del viejo no aplicaba). Smoke `/admin/llm` con ambos providers verde. Desbloquea PR-A3.1. Ver Progreso 2026-05-21 19:30.
- ~~**Bug latente — `memberships_count` no llega a `v2-calc-totals` en path texto**~~ ✅ **HECHO 2026-05-22 00:35** (workflow `pqSWkDIHqmSVHotB` patcheado vía REST API, no commit en repo — todo el patch vive en n8n). Causa raíz confirmada: nodos jsCode `Resolver Chat State Texto` y `Resolver Chat State Voz` hacían `return [{ json: { ...parsed, org_id, org_nombre, ... } }]` con `parsed = $('Parsear Mensaje').first().json` (payload Meta crudo sin memberships_count). El campo vivía en `$('Buscar Org Texto').first().json` pero solo se extraía `org_id`/`org_nombre`. Fix: añadidos `memberships_count: org?.memberships_count || 1` + `candidate_orgs: org?.candidate_orgs || null` en los 3 return paths (early-guard, try-success, catch-error) de ambos nodos. Marker idempotente `// PR_BOT_MEMBERSHIPS_FIX`. Verificado en execution 746: `Resolver Chat State Texto` → `memberships_count: 2 ✓`, propaga hasta `Preparar Input Agente`. Ver Progreso 2026-05-22 00:35.
- _(añadir ideas aquí según surjan)_

## Seguridad

- **Code Quality (CodeQL) activo (16/06)** — baseline triado: 23 findings Python = scripts desechables n8n-patches (arreglados PR #322), 9 JS fiabilidad = 0 bugs reales (dismiss UI), 8 dead-stores defensivos (dismiss). Sin deuda crítica oculta. Trial 30d → confirmar licencia. Ver [[github-code-quality-triage]]

### Auditoría de seguridad 2026-06-04 (6 agentes + red team activo)
- **C1 (crítico) RESUELTO en prod** — RPCs SECURITY DEFINER ejecutables por anon/authenticated vía PostgREST con anon key público (bypass de pago `change_billing_status`, IDOR, DoS numeración, abuso OTP). Mig 213 REVOKE EXECUTE FROM PUBLIC/anon/authenticated en verbos admin-only. Validado desde fuera: anon→401, authenticated→403. Ver [[supabase-rpc-security-definer-execute-public]].
- **PR #131 mergeado + DESPLEGADO en prod** (SSRF render-pdf+webhooks via `src/lib/security/ssrf.ts`, security headers anti-clickjacking en `next.config.ts`, signout CSRF, voice/generate guard phone↔org, periodo/file.name/email-outbox/idempotency). Headers live verificados (`x-frame-options: DENY`, CSP frame-ancestors, HSTS, nosniff; `x-powered-by` eliminado).
- **PR #133 mig 217 — MEDIO cerrado** — `enqueue_outbox_event` revocado de authenticated (anti-spoofing webhook cross-tenant; tenía args → invocable por PostgREST). Aplicado a prod (vía SQL+schema_migrations porque el CLI no conecta al pooler), smoke anon→401.
- **Fuga RLS `billing_accounts` (multiempresa F1) DETECTADA Y RESUELTA en prod 2026-06-04** — la policy SELECT (mig 215) usaba `user_billing_account_ids()` (todas las orgs-membresía activas), exponiendo plan/`stripe_customer_id`/`payment_failed_*` de orgs que `organizations` oculta (RLS = `id=get_user_org_id`, solo la activa). Fix **mig 218** (`billing_accounts_rls_solo_org_activa`): la cuenta visible es solo la de la org activa, heredando la visibilidad de `organizations`. Validado contra prod con usuario real miembro de 2 orgs: **fuga=0**. Aplicado a prod + PR #134. Detectado al validar RLS antes del deploy del código F2.
- **Confirmado seguro (red team):** RLS multi-tenant sin fuga cross-org, superficie sin-auth (v1/admin/internal/voice bloquean), billing sin bypass, XSS escapado por React, escalada admin imposible para no-superadmin.
- **Config dashboard (acción user):** bucket `logos` listing off ✅ HECHO; leaked-password protection ⚠️ requiere Supabase Pro (plan FREE no lo permite) — mientras, min password length subido a 8 + requisitos fuertes ✅; cuenta E2E (`.env.test`) sin rol superadmin de plataforma (pendiente, higiene).

### Migración crons → HMAC v2 (2026-06-05) — ✅ COMPLETA (2026-06-09)
Motivo: el legacy `x-service-key` es un secreto estático sin anti-replay; cerrarlo (`SIGNING_LEGACY_UNTIL`) requiere que NINGÚN caller lo use.
- **24 schedules Dokploy → `sign-call.sh` v2** (firma HMAC con node leyendo `/app/.env`; openssl NO está en node:alpine, node sí en /usr/local/bin). PRs #135/#141. 16+ validados (success en cron_runs), 0 errores. Bug corregido: `enrich-batch` firmaba `{}` → quitar body (endpoint firma sobre body vacío).
- **Drive Sync** movido de n8n (`TwDd1OkMqr3nhSso`, solo timer, **desactivado**) → cron Dokploy v2 + catalogado en `CRON_REGISTRY` con watchdog (antes no se monitorizaba). PR #149.
- **Catálogo reconciliado** (PR #147): añadidos fiscal-avisos, fiscal-recalcular-borrador, cron-watchdog, conciliacion-sugerencias-run (el panel solo veía 20/24). Duplicado `sugerencias-run` borrado en Dokploy. Filtros panel admin crons: PR #142.
- **WhatsApp Receptor v2 HMAC v2** (2026-06-09): 6 nodos migrados (`Hash Session`, `crear_cliente`, `crear_proveedor`, `Obtener Orgs Para Crear`, `Ejecutar Creacion Pendiente`, `Log Bot Error → Error Agente`). Smoke WA OK.
- **`SIGNING_LEGACY_UNTIL=2026-06-01T00:00:00Z`** (pasada) aplicado vía API Dokploy 2026-06-09. Legacy cerrado definitivamente. Deuda pendiente: Voice* inactivos (no bloquean). Ver [[dokploy-cron-docker-exec-no-hereda-env-de-app-env]].

### Deudas P0/P1 con threshold claro (priorizadas)

🔴 **P0 — ANTES de primer cliente facturando con dinero real**:
- ~~**OTP teléfono al onboarding WhatsApp**~~ — **HECHO 2026-05-16 19:55**. E2E verificado en prod, WhatsApp llega con wamid + cost 3¢, verify funcional. Ver `Progreso en vivo` y `Histórico de hitos`.

🟠 **P1 — ANTES de cliente B2B que pida certificación ISO/ENS o auditoría AEPD nivel ISO**:
- **Encryption-at-rest del disco VPS** (~3-6h + ventana downtime). LUKS o dm-crypt del filesystem del `185.47.13.170`. Sin esto, robo físico del disco expone todas las BDs (postgres-n8n + envs). Hoy probabilidad muy baja en VPS managed pero auditor lo pedirá.

🟠 **P1 — Refactor del workflow para eliminar `$env.X`**:
- Hoy `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` con threat model documentado (security audit 2026-05-16). Aceptable hoy, bloqueante si: (a) se contrata dev externo con acceso a panel n8n, (b) n8n pasa a multi-user con roles. Plan: esperar n8n 2.21+ con fix del bug `toolHttpRequest`, o migrar a `toolWorkflow` con 4 sub-workflows (~6-10h).

🟡 **P2 — Hardening del perímetro n8n**:
- IP allowlist en Traefik para `n8n.tufacturaia.com` (solo IP del admin + crons internos). ~1h.
- Rotar `N8N_BASIC_AUTH_PASSWORD` a 32+ chars custom (no auto-gen Dokploy). ~5min.
- Desactivar `N8N_PUBLIC_API_ENABLED` si no se usa externo. Verificar.

### Hardening diferido (sin prioridad bloqueante)

- Logs estructurados en endpoints (correlation IDs)
- CSP headers más estrictos
- Métricas de uso de API IA (consumo OpenAI/Anthropic por org)
- Pen-test anual cuando se llegue a >100 clientes
- Rotar `SUPABASE_SERVICE_ROLE_KEY` — se vio su valor en el transcript de la sesión Claude del 2026-05-27 (al leer el env vía API Dokploy). Exposición local, no a terceros; riesgo bajo pero es service-role (salta RLS). Decidir rotar vs aceptar.

---

## Specs detalladas (subfiles)

- [[facturaia-historico-detallado]] — **📦 archivo histórico**: detalle de sprints cerrados, log "Progreso en vivo" mayo 2026, smokes verificados, historial de deploys (podado del hub el 2026-05-31)

- [[facturaia-comunicaciones-emails]] — sistema emails Resend + auth Supabase, 5 PRs, reutiliza outbox/notifications/ai_module_suggestions
- [[facturaia-integracion-api-v1-portal]] — webhooks HMAC, outbox, Stripe-style sync con agency-portal
- [[facturaia-test-tanda-2-3]] — tests de tanda 2 y 3
- [[facturaia-arquitectura-multi-whatsapp-numbers]] — multi-tenant WhatsApp por org
- [[facturaia-bloque-4-agent-query-spec]] — copiloto IA multi-canal
- [[facturaia-open-banking-psd2]] — conexión bancaria PSD2 (legado, sustituido por spec abajo)
- [[facturaia-conciliacion-roadmap]] — **🗺️ Conciliación · Roadmap maestro** (subfile canónico para cualquier sesión nueva — estado, fases hechas, pendiente, siguiente, deferred, decisiones, deuda)
- [[facturaia-conciliacion-copiloto-spec]] — **Conciliación bancaria + Copiloto v2 con tool-use** (spec definitiva, Sprint A MVP + Sprint B condicional). Documento autoritativo en repo: `docs/architecture/conciliacion-copiloto-spec.md`
- [[facturaia-prompt-auditoria-dashboard]] — prompt reutilizable: auditoría dashboard (exactitud + mejoras + arquitectura + benchmarking Holded/Quaderno con foco IA)
- [[facturaia-cashflow-chart-saldo]] — chart `/cashflow` unificado (saldo arriba + flujo abajo, estilo TradingView). ✅ hecho en commits `d810e31` + `26cb553` (2026-05-26)
- [[facturaia-centro-fiscal-ia]] — **Centro fiscal IA (módulo Pro add-on)** — spec completa congelada 2026-05-19. 303/390/130/111/115/180/190/347/349 + libro IVA + export asesor. v1 cuadre+export, v3 colaborador social AEAT. Calculador versionado, snapshot céntimos enteros, WORM + sello eIDAS, advisory lock anti-race
- [[facturaia-drive-sync-architecture]] — **Drive sync de PDFs a Google Drive del cliente** — backbone (mig 177-179) + worker outbox con SKIP LOCKED + subcarpetas año/tipo/mes lazy + cache JSONB + cascade revoke + naming rico. PRs #85/#87/#88 mergeados 2026-05-29. Cron n8n inactivo pendiente FACTURAIA_SERVICE_KEY. Ver [[ADR-025-drive-sync-outbox-vs-fire-and-forget]]

## Stack técnico

- **Frontend**: Next 16 (App Router), Tailwind, primitives Button/Card/Input/Modal en `components/ui/`
- **Backend**: Next API routes + Supabase (Postgres, RLS obligatoria por tabla)
- **OCR**: OpenAI Vision (`gpt-4o-mini`), fallback Anthropic Claude Vision
- **Voz**: n8n workflows + Retell (números WhatsApp por tenant)
- **Pagos**: Stripe (checkout pendiente cablear a add-ons)
- **Fiscal**: Verifactu (QR en PDFs, AEAT integrado)
- **Auth/RLS**: superadmin impersona vía `?org_id=` query param, no cookie
- **Tests**: Vitest (175+ unit/integration) + Playwright E2E (`tests/e2e/`: setup, smoke, explorer). Pre-release: `npm run e2e:explorer` y revisar `report.json`

## Reglas de proyecto (resumen)

Ver `facturaia/CLAUDE.md` (en repo) para versión completa.
- `lint` + `typecheck` + `build` limpios pre-commit
- Toda tabla nueva = `ENABLE ROW LEVEL SECURITY` + políticas en la misma migración
- Migration `ADD COLUMN NOT NULL` = `ADD nullable → UPDATE backfill → SET NOT NULL` en transacción
- Endpoint nuevo = auth + rate limit + validación input siempre
- Cambios Zod API público = `openapi.json` en mismo commit (clientes openapi-typescript no leen `refine`)
- UI con badge "implementado" = grep que el backend lee el valor (anti regresión silenciosa)

---

## Manuales y docs

| Doc | Link | Estado |
|---|---|---|
| Manual usuario | [GitHub](https://github.com/AgentesIA-MAdrid/facturaia/blob/main/docs/manuals/manual-usuario.md) | Bloque 1 desactualizado (3 botones, abonos, pills) |
| Manual admin | [GitHub](https://github.com/AgentesIA-MAdrid/facturaia/blob/main/docs/manuals/manual-admin.md) | Bloque 1 desactualizado |
| Módulos producto | [docs/MODULOS-PRODUCTO.md](https://github.com/AgentesIA-MAdrid/facturaia/blob/main/docs/MODULOS-PRODUCTO.md) | Vivo |
| CLAUDE.md proyecto | `facturaia/CLAUDE.md` | Vivo |

## Workflows n8n (backups en `knowledge/projects/agentesia/n8n-backups/facturaia/`)

| Workflow | ID | Función |
|---|---|---|
| OCR Factura | `zf2la2N2YBXKQNKk` | Vision → estructurado |
| WhatsApp Receptor v2 | `pqSWkDIHqmSVHotB` | Ingesta + intención + AI Agent con tools (consultar_*, registrar_gasto_rapido, crear_cliente). Antes era `zYcHHa8jWXB6dY5i` (obsoleto). |
| WhatsApp Verify | `LlBeZfa2P6mg13wc` | Verificación Meta |
| Voice Process | `KVk45rJARQk0EKaF` | Procesar audio |
| Voice Confirm | `5Q3pmag3XnIQOkQV` | Confirmación voz |
| Voice Correct | `Q1WB42g8GtyX3qnp` | Corrección voz |
| Email Polling | `dqvzeyeifTcNv50u` | Ingesta email |

---

## Links rápidos

- [GitHub repo](https://github.com/AgentesIA-MAdrid/facturaia)
- [App producción](https://facturaia.agentesia.world)
- [Dokploy](https://dokploy.agentesia.world)
- [Slack canvas Panel TuFacturaIA](https://agentesialab.slack.com/docs/T0ARXF0V31S/F0AV38CHYSJ) (`F0AV38CHYSJ`)
- [Canal Slack](https://agentesialab.slack.com/archives/pro-facturaia) (#pro-facturaia)
- WhatsApp público: `+34 919 93 26 18`

## Credenciales (refs en memoria, NO secrets aquí)

- Supabase TuFacturaIA → memoria `supabase-facturaia.md`
- Anthropic API key → memoria `anthropic-api-key.md`
- OpenAI API key → memoria `openai-api-key.md`
- WhatsApp config → memoria `whatsapp-facturaia.md`
- Gemini (backup) → memoria `gemini-api-key.md`
- **SSH Dokploy nuevo**: `root@185.47.13.170 -p 5251` (clave id_ed25519 ya autorizada). Dokploy viejo era `.166`.
- **n8n.tufacturaia.com API key** (JWT) → memoria `reference_n8n_api_key.md` (rotada 2026-05-17).

---

