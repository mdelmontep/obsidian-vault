---
title: facturaia
date: 2026-05-10
tags: [cliente, facturaia, hub]
---

# FacturaIA — Hub maestro

App SaaS de facturación con IA (OCR, agente WhatsApp, voz, recomendador). Multi-tenant. **Este archivo es el ÚNICO punto de entrada al proyecto.** Todo lo nuevo (idea, pendiente, smoke test, decisión, doc) entra aquí o se enlaza desde aquí.

## Cómo usar este hub

- **Al empezar sesión FacturaIA**: leer este archivo entero. Es la foto del proyecto.
- **Durante**: añadir cualquier cosa nueva en su sección. No esperar al final.
- **Al cerrar sesión**: `/obsidian-1` propondrá actualizar las secciones tocadas + mover lo cerrado a `## Histórico`.
- **Reglas**: 1-2 líneas por entrada. Si necesita más, link a subfile en `knowledge/projects/agentesia/facturaia-*.md`.

---

## Estado actual

- **Producción**: ~~facturaia.agentesia.world~~ (**CAÍDO 404 — confirmado 2026-05-18**, sigue en DNS/Dokploy pero no responde) + **app.tufacturaia.com** y **n8n.tufacturaia.com** (Dokploy nuevo `185.47.13.170`, SSL Let's Encrypt OK desde 2026-05-16, **misma BD Supabase `lahqlyaxvobqjgdiftag` que servía el dominio antiguo — la "migración" fue solo cambio de dominio**) · spec [[facturaia-migracion-tufacturaia-com]]
- **Stack**: Next 16 + Supabase (proyecto `lahqlyaxvobqjgdiftag`) + n8n + OpenAI Vision + Anthropic Claude
- **Plan comercial**: Starter / Pro / Enterprise + add-ons (Conciliación 19€, Anti-fraude 9€, +21 con badge Próximamente)
- **Orgs activas en prod**: AgentesiaLab (Enterprise, **reseteada a 0 el 2026-05-18** — ver Histórico), tecnocloud (Enterprise), Borja Galván (Enterprise), AgenteIA PRUEBA (Starter)
- **Última deploy mayor**: 2026-05-20 (maratón 10 PRs Equipo + Multi-org + Bot WhatsApp + Hardening: PRs #54 a #63, migs 120-127 aplicadas, 649/649 vitest, 15 audits paralelos pasados, 0 vulnerabilidades; ver entrada NOW "Multi-org real + Equipo SaaS-pro + Bot WhatsApp" y Progreso 2026-05-20 22:00) — anterior: 2026-05-20 (PR #49 API v1 líneas `descripcion` opcional + mig 119), 2026-05-19 (PR #48 Verifactu toggle unify + guard rol + audit)
- **Tests Vitest**: 275+ pasando (HMAC, cursor, idempotency, SERIE_BY_TIPO, IVA, rate-limit, createDocument, anularFactura, canAnular, descripcion propagation)

---

## NOW (trabajo activo)

- ~~**🚀 Multi-org real + Equipo SaaS-pro + Bot WhatsApp multi-org + Hardening**~~ ✅ **HECHO 2026-05-20** (10 PRs #54-#63, mig 120-127). Detalle:
    - **PR-1 #54 `9f0be19`**: Multi-org real. Mig 120 `profiles.active_org_id` UUID FK org `ON DELETE SET NULL` + policy `profiles_update` con WITH CHECK validando membresía activa (anti escalation Supabase JS directo). Mig 121: reescribe `get_user_org_id()` (antes `LIMIT 1` sin orden → arbitrario en multi-org), nueva `current_org_role()`, **matriz canónica `user_can_write_in_org(p_org_id, p_resource)`** con CASE hardcoded (single source of truth). Recreadas policies INSERT/UPDATE/DELETE de 8 tablas core (facturas/clientes/proveedores/presupuestos/lineas/bandeja/gastos/series). Helper TS compartido `src/lib/auth/active-org.ts` con writeback determinista. `getOrgId` + `getSessionActor` ambos usan el mismo path. Frontend: `OrgRoleProvider` + `useOrgRole` + matriz espejo TS en `src/lib/auth/role-matrix.ts`. Gating UI en sidebar, generar-view, clientes-view, factura-detail-modal. **Por qué**: la web ya tenía switcher (mig 120 commits previos) pero RLS y endpoints resolvían org arbitraria — un user multi-org veía/editaba datos de la org equivocada vía Supabase JS directo. Auditoría 4 agentes paralelos detectó este bug latente y 4 bloqueantes secundarios (todos cerrados).
    - **PR-3a #55 `2f83175` + hotfix #56 `a578ee3`**: Backend Equipo refundido. Mig 123 (`team_members_audit_fields`): columnas `invited_by_user_id`, `revoked_at`, `revoked_by_user_id`, `last_invite_sent_at`, `expires_at` en `org_members` + CHECK estado (activo/invitado/revocado/expirado/pausa) + matriz canónica extendida con resource `'team'` (propietario+admin) + 2 índices parciales. Mig 124: backfill `expires_at` para invitaciones pre-existentes (sin esto cron las ignoraba). Emails Resend branded (`src/lib/auth/team-invite-email.ts`, `team-added-existing-user-email.ts`) sustituyendo Supabase Auth SMTP genérico. POST /api/team/members reescrito con ORDEN ESTRICTO contra race trigger `handle_new_user`: createUser → INSERT membership → generateLink → sendEmail → rollback `deleteUser+delete membership` si email falla. PATCH bloquea admin-no-toca-admin. DELETE soft (`estado='revocado'` + `revoked_at`) + `admin.auth.admin.signOut(target.user_id, 'global')` para invalidar refresh tokens + limpieza sticky bot. Nuevo POST `/api/team/members/[id]/resend` con cooldown 60s server-side + recovery `user_exists_retry`. Nuevo cron `/api/internal/team/expire-invites` diario (Dokploy 03:15) que pasa invitaciones >30d a `estado='expirado'` + signOut a users huérfanos. Audit log en `admin_audit_log` con prefijo `team.*`. Notif kinds in-app `team.member_added/role_changed/removed/invite_resent`. force_reactivate solo propietario (anti admin-malicioso reactivando un user que el propietario eliminó). **Por qué**: el módulo Equipo era esqueleto; faltaba lifecycle real, emails branded, defensa contra resurrecciones silenciosas y race en createUser.
    - **PR-3b #57 `9961296` + hotfix #58 `d9a091a`**: Frontend Equipo refundido. `src/components/settings/team/` con 8 archivos: orquestador `team-members-list` (2 secciones separadas Activos/Pendientes patrón Notion/Linear), `team-member-row` con avatar inicial + badges Tú/Propietario + dot estado + chip rol, `team-member-menu` (portal 3-puntos con keyboard nav completa Escape/↑↓/Home/End + scroll-close + touchstart iOS), `invite-member-modal` con validación email inline + selector rol con micro-descripción + AbortController + confirm "¿Reactivar?" si actor es propietario, `resend-invite-button` con cooldown 60s en localStorage (clamp anti auto-DoS + try/catch Safari modo privado), `hooks/use-team-members.ts` con AbortController + generation counter + optimistic updates + rollback + BroadcastChannel multi-tab sync. Sustituye `SetUsuarios` viejo de settings-view.tsx (~300 líneas eliminadas). Editor inline de teléfono propio retirado del tab Equipo (vive ya en MiCuentaSection con OTP completo). **Por qué**: la UI vieja tenía `<select>` inline peligroso para cambiar rol (sin confirm), no diferenciaba estados, no permitía reenviar invitaciones, mezclaba edición propia con gestión ajena.
    - **PR-2 #59 `6bc7bcc`**: Bot WhatsApp multi-org. Mig 125 `whatsapp_org_selection` (sticky session 6h sliding por user, PK por user_id, source enum: `whatsapp_selector`/`web_switch`/`active_org_sync`). `whatsapp-resolve.ts` refactor con helper `resolveSenderOrgForUser` jerárquico: N=1 → directo · N≥2 + sticky viva válida → usa + refresh TTL · N≥2 + `active_org_id` válido → usa + upsert sticky `active_org_sync` · sin nada → bucket nuevo `multi_org_pendiente` con `candidate_orgs[]` para que workflow n8n envíe lista WhatsApp interactiva. Fix paralelo del mismo bug `.limit(1)` en `resolvePhoneChangeMatch`. POST `/api/internal/whatsapp/select-org` service-key + valida membresía activa antes del upsert. Sync web→bot en `switch-org` con TTL 24h (vs default 6h) para que elección explícita no se pise por sync automático. **Por qué**: el comentario autoconfeso en línea 92 decía "Limitación conocida (multi-org): tomamos la primera. Roadmap doc" — un user multi-org escribiendo al bot caía en una org arbitraria, no la que había elegido en web.
    - **PR-4 (3 sub-PRs #60/#61/#62/#63)**: Hardening post-audit global cross-PR. **#60 `6fd510a`** cierra 2 bloqueantes detectados por agente E2E: loops `/login ↔ /dashboard` cuando user revocado/expirado tiene JWT vivo (access token sigue válido ~1h tras signOut, solo se invalidan refresh) — nueva página `/sin-acceso` con mensaje + logout limpio + middleware exempt + layout-server redirige ahí. Cron `expire-invites` ahora también `signOut('global')` users huérfanos. Telemetría `[whatsapp-resolve] multi_pending` para detectar si workflow n8n no procesa. **#61 `143aacd`** mig 126: REVOKE/GRANT explícito en `get_user_org_id` + índice `(org_id, invited_at DESC)` para listado Equipo + CHECK secundario `revoked_at ⟺ estado terminal`. TTL sticky web_switch a 24h. DELETE team limpia sticky de la org de la que sale. `invitacion/page.tsx` order by invited_at + filter estado. **#62 `2e0519a`** mig 127: extiende matriz canónica con 2 recursos nuevos (`'categoria'` propietario+admin, `'catalogo'` propietario+admin+contable+comercial) y aplica `'module_config'` a 6 tablas que estaban con UPDATE/DELETE abierto (`categorias`, `agentes_config`, `agentes_ia`, `catalogo_servicios`, `modulos_activos`, `workflows_n8n`). Espejo TS sincronizado + tests vitest. **#63 `7c3c262`**: UX polish (localStorage hardening try/catch+clamp en resend-button, useMemo split active/pending, AbortController modal invite, scroll-close menu) + cron `/api/internal/whatsapp/purge-stale-selections` diario 03:45 borra entradas con `expires_at < now() - 30d` (registrado en CRON_REGISTRY) + docs CSRF en gotchas.md.
    - **Audits ejecutados** (15 agentes paralelos en 5 rondas): PR-1 (3) → 4 bloqueantes cerrados · PR-3a (3) → 5 bloqueantes cerrados · PR-3b (3) → 4 bloqueantes cerrados · Global cross-PR (3) → 2 bloqueantes cerrados (los loops). **Total ~15 bloqueantes cerrados, ~40 NICE_TO_HAVE documentados, 0 vulnerabilidades reales confirmadas**.
    - **Smoke tests pendientes en prod** (Dokploy redeploy post commit `7c3c262`):
        - Switcher org: ver dropdown con membresías, cambiar entre ellas, BroadcastChannel sincroniza tabs.
        - Equipo: invitar email nuevo (recibe Resend branded), reenviar invitación con cooldown UI 60s visible, cambiar rol con submenu "Cambiar rol → submenu con descripciones", eliminar miembro (recibe email transparencia), reactivar miembro previamente eliminado (confirm "¿Reactivar?" solo si actor es propietario).
        - Multi-org bot: con cuenta de 2 orgs, escribir al bot → si nunca eligió → `multi_pending` (workflow n8n debería responder lista; si no está cableado, ver log `[whatsapp-resolve] multi_pending` en Dokploy).
        - `/sin-acceso`: crear usuario test con 1 sola membresía, eliminarla via DELETE team. Su próxima request → `/sin-acceso` (no loop). Click logout → /login limpio.
    - **Pendientes externos al código** (acción manual del operador, NO commit):
        - **Workflow n8n `pqSWkDIHqmSVHotB`**: añadir branch ante `reason='multi_org_pendiente'` del endpoint `/api/internal/whatsapp/resolve-context` → enviar WhatsApp list message con `candidate_orgs` → al recibir reply → POST a `/api/internal/whatsapp/select-org` con `{ phone, selected_org_id }` → re-invocar resolve-context. Sin esto el bot multi-org no funciona end-to-end (el backend está listo pero el frontend del bot es n8n). Telemetría `[whatsapp-resolve] multi_pending` lo detectará en logs Dokploy.
        - ✅ **Dokploy crons nuevos creados 2026-05-20**: `team-expire-invites` (`15 3 * * *`) + `whatsapp-purge-stale-selections` (`45 3 * * *`). Pegar comandos SIN `| jq .` (Alpine runner no lo trae). Runbook `docs/runbooks/team-and-whatsapp-crons.md`. Pendiente smoke E2E (crear invitación con `expires_at < now()` + fila stale `whatsapp_org_selection` y validar `summary.expired`/`deleted ≥ 1`).
        - **Manual admin** (`docs/manuals/manual-admin.md`): actualizar con flow rediseñado de Equipo (modal invite + reenvío + reactivación propietario-only + menú ⋯). El usuario lo recibió actualizado pero falta el admin.
    - **NICE_TO_HAVE remanentes (futuro, no bloqueantes)**:
        - Refetch del rol activo on `team.role_changed` notif (BroadcastChannel intra-org) — hoy el rol actualizado solo se ve tras router.refresh manual.
        - Email "tu invitación expiró" tras cron `team-expire-invites` (hoy el cron pasa a estado='expirado' silenciosamente).
        - Tests E2E vitest endpoints team (POST/PATCH/DELETE/resend con mocks Supabase Auth Admin + canActorDoAction) — proyecto grande por la complejidad de mocks.
        - Retirar `'pausa'` del CHECK constraint si BD prod no tiene filas legacy con ese valor (auditar primero: `SELECT COUNT(*) FROM org_members WHERE estado='pausa'`).
        - Logging audit `signOut_failed` cuando `admin.auth.admin.signOut` lanza (hoy solo console.warn).
        - Snapshot del email del invitador en `admin_audit_log.payload` al INSERT membership (defensa contra `ON DELETE SET NULL` perdiendo la referencia si el invitador se va).
        - Bulk actions en tab Equipo (seleccionar múltiples miembros, cambiar rol/eliminar a la vez).
        - Filtros + búsqueda en tab Equipo (cuando una org tenga >20 miembros).
        - Drawer "Auditoría per-member" reutilizando `/settings?tab=auditoria` con filtro `target_key LIKE 'org_member:%:userX'`.
        - Validar `actionLink.origin` contra allowlist Supabase URL (defense extra anti hipotético link manipulado).
        - Subject CRLF sanitize en emails (header injection SMTP — no real con Resend pero defensa en fallback).
        - Rate-limit por `target_user_id` en `/resend` (anti-DoS al destinatario; ya hay cooldown 60s pero podría afilarse).
        - Eliminar legacy `'pausa'` del union TS y union enum BD si se confirma 0 filas legacy.
        - Watchdog "cron nunca registró un run" (hoy `cron-zombies` solo detecta runs viejos, no ausencia total).
- ~~**🔴 P0 BUG — Crear factura con nombre cliente nuevo NO crea cliente**~~ ✅ **HECHO 2026-05-20 (commit `34129ac`)** — modal "Crear cliente nuevo" estilo Holded pre-guardar. Helper `ensureClienteId()` con Promise + modal en `generar-view.tsx`. Si user escribe nombre sin seleccionar sugerencia, antes de INSERT factura abre modal con form NIF/email/dirección/CP opcionales + nota mod 347 (>3000€ NIF obligatorio). Confirmar → INSERT cliente → continúa submit. Cancelar → aborta. Smoke pendiente post-deploy: crear factura con nombre nuevo "Test Cliente" → ver modal → confirmar → factura insertada con cliente_id válido + cliente "Test Cliente" en `/clientes`.

- ~~**Agente de Cobros — debug end-to-end + mejoras**~~ ✅ **HECHO 2026-05-20 (commit `bf9e515`, mig 118 aplicada)** — pasada quirúrgica E2E sobre todo el módulo Cobros con 8 sub-agentes paralelos. **P0 fixes** (bloqueantes Meta prod): `sanitizeTemplateParam` en `send-whatsapp.ts` evita 132xxx por vars vacías o con `\n`/`\t`; opt-out manual + cobros-stop ahora filtran por `canal` (antes cancelaban queued de canal cruzado); render hardening control chars. **Mejoras**: eliminado toggle `parar_si_paga` (BD lo ignoraba), refactor `isClientOptedOut` → `{optedOut, checkFailed}` + métrica nueva `opt_out_check_errors` en summary del cron + `module_event` `cobros_opt_out_check_failed` con cliente_id+canal+error truncado, nuevo `POST /api/cobros/opt-out/revoke` + migration 118 con RPC `cliente_is_opted_out` reescrita patrón append-only-with-revoke-events, aging con `?sort=urgency|amount|vto` (default urgency = vencidas primero ordenadas vto ASC + total DESC) + selector UI + count exacto para `truncated`. **Smoke E2E VERDE** 2026-05-20 12:00 en Sandbox: aging sort UI ✓, toggle retirado ✓, ciclo opt-out → RPC true → revoke → RPC false ✓, audit log 3 filas append-only ✓, cron run con `opt_out_check_errors: 0` visible en `cron_runs.summary` ✓. **Pendiente**: send-now real con WhatsApp requiere plantilla Meta aprobada + envs `META_WHATSAPP_*` + factura vencida real (sanitización cubierta por 6 unit tests). **Hole observabilidad menor**: cron sale por `candidates.length===0` sin sumar a ningún contador `orgs_skipped_*` — apuntar `orgs_skipped_sin_candidatas` en mejora futura.


- **🟢 EN CURSO 2026-05-19 — Conciliación bancaria (módulo IA) + Copiloto v2 con tool-use** para `app.tufacturaia.com`. **🗺️ Roadmap maestro: [[facturaia-conciliacion-roadmap]]** (estado · fases hechas · pendiente commit · fase siguiente · bloque 5 strategic · decisiones arquitectónicas · deuda técnica). Spec original repo: `docs/architecture/conciliacion-copiloto-spec.md`. Subfile resumen: [[facturaia-conciliacion-copiloto-spec]].
    - ✅ **PR-A1 mergeado (commit f56a0b3)** — mig 105 + parser CSV/MT940/OFX + endpoint import + drawer básico + sidebar + EstadoPill extendido + 45 tests Vitest. Fix post: ON CONFLICT con índice parcial → filter-pre-insert + INSERT con retry SQLSTATE 23505. Modal cierre prematuro → onImported NO cierra (deja al user ver card éxito).
    - ✅ **PR-A1.5 mergeado** — mig 106 (metadata + N:N + soft delete) + endpoints CRUD + drawer detail completo + helper inter-módulo (`src/lib/conciliacion/inter-module.ts`). Arquitectura inter-módulo: campos puente en `movimiento_metadata`, event bus `module_events`, helper `getMovimientoFullContext`. Cualquier módulo IA puede ahora escribir/leer cross-module sin acoplamiento.
    - ✅ **PR-A1.6 + A1.6.1 mergeado 2026-05-19 (commits `d0f33f4` → `73a12c7` → `4a210e5`)** — IA enriquecimiento movimientos bancarios funcional en prod. Mig 107 + 107b + 108 aplicadas (Supabase). Endpoint `/api/internal/conciliacion/enrich-batch` + cron Dokploy `*/10 * * * *` activos. **5 features**: auto-categorización con gate signo server-side (importe>0 → solo categorías tipo=ingreso/neutro, gate detectó 6 sign_mismatches en pruebas y los descartó), detección determinista pre-LLM de transferencias internas, enriquecimiento concepto bancario, redact NIF/IBAN/email pre-envío, retry/backoff 429/5xx en `callLlm`. **Prompt evolucionado v1→v5** tras 5 agentes auditando (correctness + prompt + SRE + UX + competidores SaaS + behavioral trust): v2 regla signo + few-shots palabras-trampa NOMINA+/ALQUILER+/IVA+, v3 lista proveedores ES (Vodafone/Endesa/AEAT/SS/Amazon/SaaS/Marketing), v4 sección INGRESOS explícita (COBRO FRA → Ventas y servicios), v5 categorías agrupadas por tipo en prompt → modelo solo "ve" categorías compatibles con signo. **Cap coste $0.50/día/org** con pre-reserva + tracking en `conciliacion_enrich_runs` (tokens, cost_usd, sign_mismatches, error_text). Kill switch `AI_ENRICH_DISABLED=1`. Watchdog cron-zombies `*/5 * * * *` libera locks `cron_runs` huérfanos >20min. **Resultado smoke 18 movs**: 14 ia_auto, 3 ia_sugerencia, 1 manual, 0 null. Coste total $0.012. **UI desplegada**: banner CTA "X por revisar [Confirmar todas]" en cabecera, badge "Revisar" en filas ia_sugerencia, drawer con botones Confirmar/Descartar inline, concepto enriquecido visible (solo si difiere del crudo), transferencias internas atenuadas (opacity 0.6 + icono ↔ + toggle "Mostrar/ocultar") con default ON (UX agent: no romper modelo mental "¿dónde están mis 5000€?"), onboarding modal first-run (localStorage v1) **sin mencionar IA/GPT** (operational transparency). Endpoint `/api/conciliacion/movimientos/bulk-confirm` pasa ia_sugerencia/ia_auto → manual sin tocar categoría. P1 diferidos a PR-A1.7: SKIP LOCKED + query rotada por org + tiktoken real + cap global plataforma + uso real columna `ia_claim_at`.
    - ✅ **PR-A2 + auditoría 4 agentes + Bloques 1-4 + smoke F4 reembolso VERDE end-to-end 2026-05-20 00:00** — sugerencias deterministas + N:N + mig 111/112/113/114 aplicadas + UI accesible + smoke validado con 3 queries SQL. Detalle completo en entrada Progreso 2026-05-20. Commits pendientes (`89b3dee`, `34129ac`, `318b37d`) **pusheados a main 2026-05-20**.
    - ✅ **#41 P0 BUG cliente nuevo en /generar** (commit `34129ac`) — modal "Crear cliente nuevo" estilo Holded pre-INSERT factura. Smoke pendiente post-deploy.
    - 🟢 **#44 PR-A2.1 parser num descripción bancaria** (commit `318b37d` — mig 115 escrita) — boost score=1.0 confidence='alta' si `mb.descripcion` contiene literal `A2026-XXXX` y matchea factura.num del org. Aplica bidireccional (mov→factura y factura→mov). **Pendiente aplicar mig 115 en Supabase Studio + smoke**.
    - 🟢 **#46 F4 bidireccional** (commit `e55e5fd` — mig 116 escrita) — `detect_reembolso_for_cobro` cubre orden inverso. Reusa predicado neutralización mig 114 anti-loop.
    - ✅ **Auditoría 3 agentes post #41/#44/#46 → 6 fixes** (commits `e50b359`/`f0c67f4`/`eab27dc`) — verificado ~60% FP filtrados. **Reales aplicados**: (P0) Promise deadlock modal cliente nuevo con useEffect cleanup; (P0) mig 117 filtro N:N en `detect_reembolso_for_cobro` (`movimiento_factura_asignacion` tipo='pago' activa); (P0) mig 117 guard revert en `auto_mark_cobrada_on_bank_match` (cobrada→pendiente skipea re-match); (P1) mig 117 backfill payload `cobro_devuelto` con `direccion='devolucion_busca_cobro'`; (P0) `ocr/anomaly.ts` filtro deleted_at; (P1) `metrics/route.ts` filtro deleted_at. **FP descartados verificados**: CHECK constraint naming (autonombre canónico OK), `enrich-batch`/`transfer-pairs`/`inter-module` ya filtran, `created_via`/`created_by_user_id` en clientes (no existen), ESC handler modal (consistente con resto), doble-click cliente (disabled cubre), `mm.es_devolucion` extra (predicado actual ya cubre). **Pendiente aplicar mig 115/116/117 en orden en Supabase Studio + smokes**.
    - **🟢 EN CURSO 2026-05-19 — PR-A2 sugerencias deterministas factura↔movimiento**: mig 109 escrita (`compute_sugerencias_for_movimiento` + `compute_sugerencias_for_factura` con tolerancia 2× del config, triggers `trg_zz_sugerencias_after_*` que corren post mig 103 auto-match, double-write trigger `mirror_factura_match_to_nn` espejea `cobrada/pagada_con_movimiento_id` → `movimiento_factura_asignacion`, backfill 1:1 existentes → N:N). **Decisión**: mantener columna legacy `facturas.cobrada_con_movimiento_id` 6 meses (spec original, expand-contract migration: triggers double-write hasta migrar readers). Pendiente: endpoint `/api/conciliacion/sugerencias/run` (cron + manual), UI tabs colapsables (Por revisar / Movimientos / Historial), panel sugerencias en drawer movimiento + drawer factura, aplicar mig 109 en Supabase Studio, cron Dokploy.
    - 🟢 **PR-A3 Copiloto v2 — backend + UI + multi-turno desplegado 2026-05-20** (commits `51e9979` → `51dac1b` → `723da99` → `724bca6` → `380110c`). 4 tools core: getKPIs, searchFacturas, getDeudaPorCliente, composeCobroMessage. Runner dual-provider (Anthropic/OpenAI tool-use según `/admin/llm`). Endpoints `/api/copiloto/message`, `/threads`, `/threads/[id]`. Drawer v2 + tool result cards. **Smoke 1er turno** VERDE: "¿Cuánto facturé este mes?" → getKPIs → 1.431€, 2386/38 tokens, provider=openai. **Bugfix `380110c`** — 400 "tool_call_ids did not have response messages" en 2º turno: route persiste `tool_calls`+`tool_results` juntos en fila `rol='assistant'` pero runner los leía de filas `rol='tool'` inexistentes → fix: emit `role:'tool'` por call_id tras assistant; skip huérfanos para threads viejos. **Smoke multi-turno** VERDE tras fix (3 turnos consecutivos OK). Ver [[llm-tool-use-multi-turno-replay-tool-calls-y-results-emparejados]]. **🚫 BLOQUEADO 2026-05-20**: OpenAI key 429 quota exceeded en prod; Anthropic key inválida desde incidente PR-A1.6. Desbloquear con (a) recargar saldo OpenAI o (b) rotar Anthropic key + switch en `/admin/llm`.
    - **PR-A3.1 NEXT** (tras desbloqueo LLM): 3 tools de movimientos (`suggest_note`, `explain_movimiento`, `analyze_movimiento`) para contexto conciliación.
    - **PR-B-anomalias NEXT**: extender módulo Detector de Anomalías a scope bancario (duplicados borrosos + gasto anómalo) — escribe en `movimiento_metadata.anomalia_score`.
    - **PR-B-cobros NEXT**: extender Agente de Cobros con predicción retraso por cliente — escribe en `movimiento_metadata.prediccion_cobro_dias`.
    - **PR-B-tesoreria NEXT**: widget IA resumen mensual en Previsión de tesorería.
    - **Decisiones clave**: (a) Dokploy cron (NO n8n) para crons del módulo, mismo patrón que verifactu-process; (b) TrueLayer Data API como proveedor PSD2 AISP (pivot 2026-05-19: GoCardless BAD cerró signups nuevos); (c) cliente PSD2 abstraído tras interfaz `Psd2Provider` para swap futuro a Tink; (d) movimientos_bancarios = inmutable (datos del banco), capa editable en `movimiento_metadata` con campos puente cross-module; (e) N:N `movimiento_factura_asignacion` reemplaza 1:1 progresivamente (backfill en PR-A2). Mig última verificada: 106.
- ~~**🔴 BUG — Convertir presupuesto a factura por WhatsApp no funciona**~~ ✅ **RESUELTO 2026-05-18** — refactor state machine conversacional desplegado (ver Histórico). Flow E2E "Convierte presupuesto Manuel → el N → qué tiene → confirma" funciona.
- ~~**🔴 P0 inmediato — rotar OpenAI key expuesta en repo**~~ ✅ **HECHO 2026-05-12** — verificado que prod n8n ya usaba `$env.OPENAI_API_KEY` (hardening previo 2026-05-06, doc `docs/security/ocr-n8n-hardening-applied.md`). Key vieja revocada en OpenAI, nueva añadida en env Dokploy n8n + restart, OCR smoke-test funcional. Eliminados snapshots stale: `n8n/workflows/whatsapp-receptor-v2.json`, `whatsapp-receptor-v1-backup.json`, `whatsapp-receptor.json` (contenían literal pre-hardening). Repo grep limpio. **Pendiente**: commit del repo limpio + decisión sobre `git filter-repo` (key ya revocada, riesgo bajo)
- ~~**Bloque 1 — generar-view form manual**~~ ✅ **HECHO 2026-05-07** — commit `820684b`. SERIE_POR_TIPO corregido (proforma F, abono solo via anulación), tipo_documento en INSERTs, rama proforma a presupuestos, botón "Anular y emitir abono" con selector R1-R5, `GET /api/render-pdf?tipo=&id=`, `/api/email/send` acepta `presupuesto_id`.
- ~~**Cumplimiento fiscal: referencia en abonos**~~ ✅ **HECHO 2026-05-18** — commit `501a593`. PDF abono muestra bloque "Factura rectificada nº X fecha Y — art 15.3 RD 1619/2012" en los 4 templates + link clicable a factura origen en modal detalle.
- ~~**Tests proformas + abonos rectificativos**~~ ✅ **HECHO 2026-05-18** — 97 tests Vitest nuevos, 2 bugs fiscales corregidos. Commit `0e94de4`. Ver Histórico.
- ~~**🔵 Smoke 5 puntos 2026-05-07**~~ ✅ **CERRADO 2026-05-18** — smoke automatizado (`5ed2dc8`+`dbde6a6`) + auditoría cruzada 3 agentes + 9 fixes (C1/C2/I2-I7+mig 091) en `c83d50b`/`7097797`/`0865e89`. Checklist humano en `docs/runbooks/sprint3-smoke-checklist.md`. Mig 089 + 091 aplicadas en prod. Ver Histórico 2026-05-18 21:30.
- ~~**Mobile responsive resto**~~ ✅ **HECHO 2026-05-18** — FASE 2 polish (`1a95e47` mobile + `f7b4624` docs) cubre formularios <480px, admin sistémico, settings/informes/calendario, iOS anti-zoom, a11y dvh/inert/focus trap. Audit cruzada 4 auditores + 5 críticos+8 importantes arreglados. Ver Histórico 2026-05-18 19:30.
- ~~**Archivar sub-workflows voz antiguos en n8n**~~ ✅ **HECHO 2026-05-18** — `DpNxjOAzMMgORzrI` (Voice Confirm), `gLWHHd9kRqphbtWJ` (Voice Correct), `V5fsUJbm0srqktQH` (Voice Process) archivados vía `POST /api/v1/workflows/:id/archive` en `n8n.tufacturaia.com`. 0 referencias desde 6 workflows activos (OCR, Error Handler, Verify, Chat Retention, Email Polling, Receptor v2 `pqSWkDIHqmSVHotB`).
- ~~**agency-portal PRs #54 #55 #56**~~ ✅ **MERGEADOS 2026-05-13** — #54 quotes actions, #55 actor passthrough, #56 unificación facturas. Verificado vía `gh pr view`. Pendiente derivado (mover a NEXT si aplica): regen `types.gen.ts` post-#54 + quitar 3 `as never` + migration `quotes.converted_facturaia_factura_id` populate.

## Smoke tests pendientes

- **🔵 Multi-org + Equipo + Bot (post commit `7c3c262`, Dokploy redeploy 2026-05-20)** — escenarios completos:
    - **Switcher org web**: dropdown muestra membresías reales, click cambia activa, otras pestañas refrescan vía BroadcastChannel, NIF cambia, `/clientes` y `/emitidas` muestran datos de la nueva org. Múltiples switches consecutivos sin spinner colgado.
    - **Equipo invitar (rol no-existente)**: `/settings?tab=equipo` → "Invitar miembro" → email nuevo + rol comercial → modal cierra → toast "Invitación enviada" → aparece en Pendientes. Email Resend branded llega con CTA + expiración 30d visible. Audit log: `SELECT * FROM admin_audit_log WHERE action='team.invite' ORDER BY created_at DESC LIMIT 1`.
    - **Equipo invitar (rol existente otra org)**: invitar email que YA tiene cuenta FacturaIA pero NO es miembro → toast "Se ha añadido a X. Recibirá un email informativo". Email transparencia llega. Notif in-app `team.member_added` al destinatario.
    - **Reenviar invitación**: en row pendiente → ⋯ → "Reenviar invitación" → spinner → check "Enviada" → cooldown visible "Reenviar (59s)" decrementa. Reintento dentro de cooldown → 429 con mensaje claro.
    - **Cambiar rol con submenu**: ⋯ → "Cambiar rol" → submenu con 5 opciones + descripción. Cambiar a "Solo lectura" pide confirmación. Cambio aplicado optimista + audit + notif al miembro afectado.
    - **Admin no toca admin**: con actor `admin`, ⋯ de otro `admin` → items "Cambiar rol" y "Eliminar" disabled con tooltip "Solo el propietario puede modificar admins".
    - **Eliminar miembro (soft delete + signOut)**: ⋯ → "Eliminar del equipo" → confirm → row desaparece. En BD: `org_members.estado='revocado'`, `revoked_at NOT NULL`. JWT del miembro revocado: en otra pestaña sigue válido ~1h pero `/dashboard` redirige a `/sin-acceso` (en lugar de loop).
    - **Reactivar miembro eliminado (solo propietario)**: invitar mismo email previamente revocado → si actor es admin → 403 con mensaje claro. Si actor es propietario → confirm "¿Reactivar?" → tras OK → upsert estado=activo, audit `team.reactivated`, notif transparencia.
    - **/sin-acceso**: crear user test con 1 sola membresía → DELETE team. Próximo request del user revocado → ve página `/sin-acceso` con mensaje + botón "Cerrar sesión" → click logout → /login limpio (no loop).
    - **Cron `team-expire-invites`**: configurar en Dokploy + Run Now. Crear invitación con `expires_at < now()` manualmente para validar:
        - `UPDATE org_members SET expires_at = now() - interval '1 hour' WHERE id='<X>'`. Run cron → `summary.expired ≥ 1` + `summary.signed_out ≥ 1` (si era su única membresía). Verificar `estado='expirado'` en BD.
    - **Bot WhatsApp multi-org**: con user de 2 orgs sin sticky previa, escribir al bot. Sin workflow n8n cableado todavía → el bot devolverá copy genérico de "no entendí" PERO log Dokploy mostrará `[whatsapp-resolve] multi_pending { user_id: ..., candidates: 2 }`. Cuando se cablee workflow: enviar mensaje → bot responde lista interactiva → user elige → POST `/api/internal/whatsapp/select-org` → siguiente mensaje resuelve a esa org.
    - **Sync web→bot**: hacer switch en web → escribir al bot inmediatamente → debe resolver a la nueva org (sticky con source='web_switch', TTL 24h).
    - **Cron `whatsapp-purge-stale-selections`**: configurar + Run Now. Crear fila `whatsapp_org_selection` con `expires_at < now() - 31d` manualmente → Run cron → `summary.deleted ≥ 1`.
    - **CHECK constraint mig 126 `revoked_at ⟺ estado`**: intentar `UPDATE org_members SET estado='activo' WHERE id='<revocado>'` directo en Studio → debe fallar con CHECK violation (porque revoked_at sigue NOT NULL).
    - **RLS rol-aware tablas extra (mig 127)**: con user `solo_lectura`, intentar `supabase.from('agentes_config').update(...)` desde browser DevTools → falla con RLS violation. Mismo con `solo_lectura` intentando insertar en `catalogo_servicios` → falla. Con `comercial` insertando en `catalogo_servicios` → OK (porque comercial está en la matriz para `catalogo`).
- **✅ Smokes migs 115/116/117 ejecutados en sandbox** 2026-05-20 — 116 VERDE directo, 115 VERDE camino feliz (caso donde mig 103 cobra la cercana y mig 115 sugiere la lejana mencionada literalmente), 117 ROJO inicial → FIX vía **mig 128 `last_revert_at` persistente** (escrita + aplicada). Smoke 117 v3 VERDE post-mig 128. Fixtures movidas a `docs/runbooks/smokes/conciliacion/`. Detalle en Progreso 21:30.
- **🔵 Mig 115 caso ambiguo "mig 103 elige mal"** — pendiente: el smoke actual solo cubre el camino donde la "cercana" gana auto-match. Falta el caso adverso: mismas 2 facturas pero el +mov con fecha EQUIDISTANTE o ANTERIOR a la "lejana" → mig 103 podría escoger mal y mig 115 debería corregir vía sugerencia parser para la mencionada. Re-ejecutar tras commit + push.
- **✅ Modal "Crear cliente nuevo"** 2026-05-20 — `/generar` con nombre nuevo abre modal, ESC cierra, INSERT cliente OK, factura con cliente_id NOT NULL.

- **🔵 Conciliación post mig 111-114 + Bloques 1-4 (deploy pendiente — archivos sin commit aún)**: tras `git push` y Dokploy redeploy, re-ejecutar en prod: (a) import CSV con +100€ que case con factura emitida pendiente → factura `cobrada` con `fecha_cobro = mb.fecha` (NO CURRENT_DATE); (b) import CSV con -100€ que case con factura recibida pendiente → factura `pagada` con `fecha_pago = mb.fecha` (asimetría F5 fixed); (c) import devolución -X€ tras cobro +X€ existente → factura emitida vuelve a `pendiente`, mov +X€ NEUTRALIZADO (no se re-matchea con factura), mov -X€ marcado `es_devolucion=true`, evento `cobro_devuelto` registrado; (d) borrar mov vía soft-delete SQL → ya no aparece en lista UI (filtro `deleted_at IS NULL` añadido); (e) confirmar sugerencia para factura `anulada` → 409 `factura_no_conciliable`; (f) intentar PATCH movimiento con `categoria_id` de otra org → 400/403; (g) cambiar tolerancia/ventana en config conciliación → sugerencias pending borradas automáticamente (trigger `invalidate_sugerencias_on_config_change`); (h) anular factura emitida cobrada → mfa N:N desvinculada + `cobrada_con_movimiento_id=NULL`. Smoke local end-to-end VERDE 2026-05-20 con factura A2026-0009 (ver Progreso).
- **🔵 PR-A2 sugerencias deterministas (post mig 109 + deploy)**: (a) aplicar mig 109 en Supabase Studio (transaccional, OK); (b) importar CSV con factura+movimiento mismo importe → ver sugerencia en `conciliacion_sugerencias` con status='pending', score≥0.8, confidence='alta'; (c) factura con importe ligeramente distinto (±15€ con tolerancia 10€) → sugerencia con confidence='baja', origen='trigger_cercano'; (d) sugerencia ya emparejada en otro mov → NO crear duplicada; (e) verificar backfill: `SELECT COUNT(*) FROM movimiento_factura_asignacion` debe ≈ count facturas con cobrada_con_movimiento_id NOT NULL; (f) crear nueva asignación 1:1 → verificar fila N:N espejo automática.
- **🔵 Cron enrich-batch + watchdog (deploy 2026-05-19 PR-A1.6)**: cron `*/10 * * * *` corriendo sin errores en Dokploy. `SELECT error_text, sign_mismatches FROM conciliacion_enrich_runs ORDER BY created_at DESC LIMIT 10` → mayoría error_text NULL, sign_mismatches casi siempre 0 con prompt v5. Watchdog cron `*/5 * * * *` corriendo y devolviendo `cleaned: 0` (no zombies). Si crece sign_mismatches >20% en un run → revisar prompt.
- **🔵 Borradores Holded-style (deploy post-commit 2026-05-19)** — flujo completo: (a) Guardar borrador desde `/generar` → aparece en tab "Borradores" sin número; (b) `SELECT num FROM facturas WHERE estado='borrador'` → confirmar que es NULL (no `''`); (c) abrir modal del borrador → banner "Sin número asignado" visible + botón "Emitir factura"; (d) emitir → lista se actualiza con número real y estado `pendiente` (NO `anulada`); (e) editar borrador + guardar → num sigue NULL en BD; (f) enviar email con borrador → debe rechazar con error "Los borradores deben emitirse antes".
- **🔵 State machine bot WhatsApp (deploy 2026-05-18)** — flujos largos: (a) 2 listados consecutivos sin colisión de `last_listado`; (b) TTL `awaiting_confirmation` 90s expira limpio sin reabrir `last_doc` viejo; (c) cron `chat-state-purge` diario borra solo expiradas >24h; (d) doble-tap "Confirmar" → 1 sola factura via `whatsapp_message_processed`; (e) ruta voz (audio) además de texto. Ver [[ADR-002-bot-state-machine-postgres]].
- **🔵 Deploy 2026-05-17 23:00 (Phone State Guard v2 + panel Auditoría) — 5 commits push a main, esperando Dokploy redeploy**:
  1. ✅ **n8n Phone State Guard v2**: ya activo en prod via PUT API durante la sesión. Verificado live exec 165 (verified) + exec 169 (pending_change_old con copy literal en WhatsApp).
  2. **Panel auditoría tras redeploy app FacturaIA**: abrir `/settings?tab=auditoria` y verificar (a) la fila "Quién" muestra "Tú" en eventos OTP propios; (b) filas Agente · Voz con caller resuelto muestran el nombre del humano; (c) filtros nuevos por categoría funcionan (Documentos / Mi cuenta / Equipo / Agentes IA); (d) click en una fila la expande con detalle completo, copy buttons UUID, ESC cierra; (e) móvil <720px → cards apiladas; (f) sin tooltips nativos invisibles.
  3. **API key resolve**: si tienes acciones via API v1 en el log, verificar que aparece el `name` de la api_key (no "API" genérico).
  4. **Email fallback**: si hay users sin `full_name`, verificar que muestra parte local del email capitalizada (`Manuel`) NO email crudo.
  5. **Multi-WhatsApp** (cuando exista): el detalle expandido debe mostrar `Número receptor` cuando hay `detalles.receiver_phone_id` populado por `voice/generate`.
  6. ✅ **Smoke OTP `whatsapp_delivery_log`** (heredado de migración): cubierto indirectamente por exec 165 + 169 del Phone State Guard que atraviesan el mismo pipeline Meta+delivery.
- **🟢 Deploy 2026-05-16 (OTP teléfono onboarding) — FUNCIONAL EN PROD ✅**:
  1. ✅ Plantilla Meta `otp_facturaia` APPROVED (language `es_ES` no `es`). Healthcheck `/admin/system?tab=otp` OK.
  2. ✅ E2E real verificado 17:53 UTC con cuenta `m.delmonte.p@gmail.com`: start whatsapp → wamid → verify_ok 22s después, cost 3¢.
  3. **PENDIENTE**: Smoke test cambio teléfono con re-auth password (no probado todavía, pero código deployado).
  4. **PENDIENTE**: Trigger fallback email forzando `provider_degraded_until`. Ya verificado *indirectamente* (cuando Meta fallaba con 132001 el email Resend rescató — UUID en provider_message_id).
  5. **PENDIENTE**: Banner grandfathering con user pre-deploy + verificar bloqueo D+15.
  6. **PENDIENTE**: Superadmin impersonando — verificar bypass.
  7. ✅ Stats admin reflejan envíos reales: phone_otp_audit con start (channel=whatsapp, cost=3) + verify_ok.
  8. ✅ **HECHO 2026-05-17 01:00**: Webhook delivery n8n parche aplicado al workflow `pqSWkDIHqmSVHotB` (marker `delivery_status_patch_v1` verificado). Envs `FACTURAIA_SERVICE_KEY` + `APP_URL=https://app.tufacturaia.com` activas en container n8n (tras varias iteraciones porque Dokploy duplicó `FACTURAIA_SERVICE_KEY` en lugar de añadir APP_URL al pegar el compose — fix manual). Pendiente smoke real: enviar OTP → ver rows `status=delivered` en tabla `whatsapp_delivery_log`.
  9. **PENDIENTE**: Lockout 15min (5 OTPs malos consecutivos).
- **🟢 Deploy 2026-05-12 (módulo Cobros completo) — commit `b48d9cb` ya pusheado y desplegado**:
  1. ✅ Cron `cobros-reminders` en Dokploy con shell=bash + Command sin wrap manual + comillas DOBLES `-H "x-service-key: $FACTURAIA_SERVICE_KEY"` — Run Now devuelve HTTP 200 + JSON con `summary` (verificado 2026-05-12 23:20: orgs_scanned=1, orgs_skipped_quiet_hours=1 → comportamiento correcto, ventana 21:00-08:00 default)
  2. **PENDIENTE — Meta**: solicitar plantilla utility `cobros_recordatorio_v1` en Meta Business Manager con variables `cliente_nombre, numero, importe_eur, fecha_vencimiento, dias_vencida, link_pago, firma`. Aprobación humana Meta 24-72h. Luego registrar en `/admin/config?tab=cobros-pricing` con form de plantilla Meta
  3. **PENDIENTE — envs Dokploy**: añadir `META_WHATSAPP_PHONE_NUMBER_ID` (del número WABA +34 919 93 26 18) + `META_WHATSAPP_ACCESS_TOKEN` (long-lived, 60d o System User Token)
  4. **PENDIENTE — n8n env**: confirmar que workflow `zYcHHa8jWXB6dY5i` tiene env `FACTURAIA_SERVICE_KEY` (debe coincidir con backend) para que el branch STOP del receptor v2 pueda hacer POST a `/api/internal/cobros-stop`
  5. **End-to-end real con cliente test** (cuando 1-4 OK): UPDATE `clientes` para validar telefono propio de prueba → poner `cobros.hora_envio` a la hora actual → forzar Run Now → recibir WhatsApp en móvil propio con plantilla aprobada → responder STOP → verificar opt-out en BD + notif al dueño
  6. **UI verifica**:
     - `/agentes` → Agente de cobros → tab Configuración: form 6 keys con preview en vivo, sin section cards pesadas, footer balanceado (helper text + botón)
     - `/emitidas?view=aging`: switcher Tabla/Aging, KPI strip + 5 buckets + tabla agrupada con icono teléfono verde/rojo (sin pulse) tooltip nativo
     - Click "Enviar ahora" → Modal con selector canal WhatsApp/Email (email Próximamente disabled)
     - Drawer campana: chips filter single-select + menú ⋯ por notif con Snooze (1h/esta noche/mañana/lunes/semana) + Descartar + footer "Preferencias y horas silenciosas"
     - `/settings?tab=notificaciones`: sección "Mis preferencias" arriba con quiet hours personales + días semana + canales + digest opt-in (disabled hasta Resend)
     - `/admin/system?tab=cobros`: KPIs cross-org + top orgs + Pausar con frase tipeada `PAUSAR COBROS <ORG>`
     - `/admin/config?tab=cobros-pricing`: tabla editable plan_quotas + form Meta template
  7. **Hydration**: confirmar `/settings?tab=notificaciones` no muestra error de hydration mismatch (anterior bug arreglado con `useSearchParams` + Suspense)
- **🔴 Deploy 2026-05-11 (reorganización admin)** — tras push + Deploy Dokploy:
  1. **Sidebar admin** en 3 secciones (NEGOCIO / PRODUCTO IA / SISTEMA). "Sugerencias IA" aparece como "Recomendador". "Tareas programadas", "Alertas", "Storage" desaparecen del nivel raíz (ahora en "Salud")
  2. **`/admin/system`** abre con 4 tabs (Tareas programadas · Almacenamiento · Alertas · Webhooks). `?tab=` actualiza URL al cambiar. Cada tab carga su panel
  3. **Redirects**: `/admin/storage`, `/admin/alerts`, `/admin/system/crons` redirigen a `/admin/system?tab=...`. No 404
  4. **`/admin/config`** abre con 5 tabs (Entorno · Email · Fiscal · Mantenimiento · WhatsApp). Tab Entorno muestra envs ✓/✗ por categoría + commit/uptime/Node/Next
  5. **Tab Email** muestra "No configurado" (warning rojo) hasta que `SMTP_HOST` esté seteada en Dokploy
  6. **Tab Fiscal** lista cola Verifactu + tasa éxito 30d + certificados (caducados/7d/30d). NO hace ping AEAT
  7. **Tab Mantenimiento** locked por defecto. Click "Desbloquear" → modal contraseña → unlock. Editar banner: texto + severidad + caducidad → Guardar
  8. **Banner global**: tras guardar banner activo, aparece banda arriba en `/dashboard` (y todas las rutas). Refresh persiste. Click X dismissa per-session
  9. **Kill switch**: en modal de confirmación, sin escribir "ACTIVAR MANTENIMIENTO" el botón está disabled. Tras escribir + activar → indicador rojo en `/admin/system` header + sección con datos quién/cuándo/expira
  10. **Kill switch ON, usuario no-superadmin** intenta crear factura → 503 "Sistema en modo mantenimiento". Mismo superadmin sigue pudiendo. Lectura siempre OK
  11. **Kill switch ON, superadmin** entra a `/admin/config` y `/api/admin/config/maintenance` siguen accesibles (recovery path) → desactivar OK
  12. **Features** `/admin/features`: cada row muestra chip "N planes" antes de Core/switch. Click expande mini-lista con plan-nombre · plan-id
  13. **Audit log** `admin_audit_log` tiene row tras toggle banner / activar / desactivar kill switch. Campos: `actor_user_id`, `action`, `old_value`, `new_value`
- **🔴 IMPORTANTE — Deploy 2026-05-11 (módulos IA + observabilidad crons)** — tras redeploy con los últimos commits (`5255547` + `d4b7566` + `d43f80d` + `6442182`):
  1. **Cron `cashflow-alerts`** — Run Now en Dokploy → debe responder ✅ (era ❌ por endpoint 404 antes del push). Verificar row `status=success` en tabla `cron_runs` y en `/admin/system/crons` semáforo verde
  2. **Cron `storage-quota-check`** — Run Now → era ❌ con `invalid warn_mb` por env vacía. Tras fix `parseMb` (commit `5255547`) debe pasar a ✅. Verificar en `/admin/system/crons`
  3. **Panel `/admin/system/crons`** en producción — los 6 crons listados con datos, semáforo correcto por cada uno (verde si corrió OK en su ventana, ámbar si excede 80% del intervalo, rojo si falló o lleva más del max). VerifACTU como gris/sin datos hasta que se active
  4. **Tooltip de info** en cada card — hover sobre icono "i" muestra popover OPACO (no transparente) con título + descripción + bloque azul "Ejemplo real". Verificar contraste en dark mode y light mode
  5. **Modal de detalle del cron** — click en card abre modal con bloque "Ejemplo real" antes de "¿Qué pasa si falla?" + tabla de últimas 10 ejecuciones
  6. **OCR `auto_categorizar` trigger BD** — desactivar toggle en `/settings/modulos` para una org con OCR; subir factura recibida; verificar que `facturas.categoria` queda NULL aunque el LLM/n8n haya enviado categoría
  7. **Conciliación `auto_marcar_cobradas` trigger BD** — con factura recibida en `sin_aprobar`/`pendiente` y movimiento bancario coincidente (mismo importe ±tolerancia, ±ventana_días), verificar transición automática a `estado='pagada'` + row en `module_events` tipo `factura_pagada_auto`
  8. **Copiloto gate `orgHasFeature('copiloto')`** — con org Starter sin add-on activo: botón "Preguntar a IA" del topbar debe OCULTARSE; intentar abrir vía `window.dispatchEvent('open-ai-assistant')` desde consola NO debe abrir el chat; `POST /api/ai-assistant` debe devolver 403. Activar override `org_features` para esa org y verificar que vuelve a funcionar
  9. **Copiloto `idioma` + `guardar_historial`** — cambiar idioma a `en` en `/settings/modulos`, mandar pregunta, verificar respuesta en inglés. Con `guardar_historial=true`, verificar rows en `copiloto_conversaciones` (user + assistant del mismo `thread_id`). Con `guardar_historial=false`, no debe persistir nada
  10. **Cashflow `umbral_alerta_caja`** — configurar umbral alto (ej 50000€), forzar Run Now del cron `cashflow-alerts`, verificar notif `caja_baja` en campanita de org de control + row en `module_events`
  11. **ModuloConfig defaults** — abrir modal de cualquier módulo con `org_module_config` vacío para esa org → toggles default-true (`auto_categorizar`, `auto_marcar_cobradas`, `incluir_recurrentes`, `guardar_historial`) deben mostrarse como ✅ "Activado" (NO "Desactivado" como antes del fix)
  12. **Error handling save module config** — desconectar red, intentar guardar config → toast rojo de error (NO falso "✓ Guardado")
  13. **Notif `cron_failed_<name>`** — provocar fallo intencional en un cron (ej desconfigurar URL en Dokploy temporalmente) → verificar notif en campanita de la org de control + entrada roja en panel `/admin/system/crons`
- **Deploy 2026-05-07 (post)**:
  1. Anular factura emitida → ver abono ligado en banner rojo del modal + tab "Abonos" con count correcto
  2. Crear presupuesto/proforma desde `/generar` (serie P/F + Ver PDF sin 404)
  3. Probar 3er botón "Emitir como pendiente" (Verifactu sí, email no, estado=`pendiente`)
  4. Dashboard KPIs no inflados con borradores
  5. Menú 3 puntitos sin Editar/Eliminar en factura emitida, sin Anular en abono
- **Bloque 1 manual post-deploy** (`820684b`):
  1. Crear factura/presupuesto/proforma desde `/generar` y verificar serie A/P/F + `tipo_documento` correcto en BD
  2. Anular factura emitida con motivo R1-R5 → abono serie B con líneas en negativo + factura origen anulada + remisión AEAT si VeriFACTU
  3. "Ver PDF" en presupuestos sin 404
  4. Enviar presupuesto/proforma por email con PDF adjunto
- **VeriFACTU cron — verificar frecuencia** — confirmar en Dokploy que cron de `/api/verifactu/process` corre cada 1min (no cada hora). Si fuera 1h, incumple normativa AEAT (remisión no puede ser diferida). Solo config, sin código
- **PR #47 prod** — verificar entries en `audit_log` tras marcar cobrada / reenviar email / anular / DELETE vía API v1
- **agency-portal PR #56 post-merge** — badge fiscal en row, redirect 301 `/agency/facturaia/*`, botones según estado/origen, modal motivo R1-R5 obligatorio, doble-click sin duplicar (Idempotency-Key)

## WIP (sesiones en curso, branches sin mergear)

_(añadir aquí ramas activas con propósito y bloqueador si lo hay)_

## Progreso en vivo

Log cronológico de cada cosa que se trabaja. **Antes de empezar** algo nuevo, Claude busca aquí (+ NOW + Histórico) para detectar conflictos, solapes o trabajo previo. **Al avanzar/cerrar** algo, Claude añade entrada.

Formato: `YYYY-MM-DD HH:MM · estado · qué · ref (commit/PR/file)`. Estados: `[empezado]` / `[en progreso]` / `[bloqueado: razón]` / `[hecho]` / `[descartado: razón]`.

Reglas para el motor de conflictos:
- Idea nueva → grep en este log + NOW + LATER + Histórico ANTES de añadir. Si match → avisar "ya existe / ya se hizo / ya está en progreso por X" y proponer fusionar.
- Si dos cosas tocan el mismo archivo/feature → flagear solape y preguntar prioridad.
- Si lleva >7 días `[en progreso]` sin update → marcar `[stale]` en poda quincenal.
- Cuando una entrada llegue a `[hecho]` y sea hito relevante → mover a `## Histórico de hitos` con fecha + 1 línea.

<!-- nuevas entradas debajo, lo más reciente arriba -->
- 2026-05-20 21:30 · `[hecho]` · **Smokes conciliación 115/116/117 ejecutados en sandbox + descubierto bug fiscal real en guard revert de mig 117 + fix vía mig 128**. Pre-flight verde 2026-05-20 mañana → ejecución sandbox con factura A2026-0009 (100€) y A2026-0015/0016 (242€ creadas por user via UI `/generar`). **Smoke 116 F4 invertido** VERDE: import -100€ 22/05 → mov queda sin clasificar; import +100€ 21/05 → mig 103 cobra A2026-0009 → trigger `trg_zzzz_reembolso_bidireccional` dispara `detect_reembolso_for_cobro` → encuentra -mov posterior → marca `es_devolucion=true` + revierte factura a pendiente + emite evento `cobro_devuelto` con `direccion='cobro_busca_devolucion'`. **Smoke 115 parser_num_descripcion** VERDE: 2 facturas 242€ mismo importe ambiguas (cercana 20/05 + lejana 10/05); +mov 242€ 20/05 con descripcion `Pago factura A2026-0016 cliente test` → mig 103 auto-match cobra A2026-0015 (cercana por fecha) y mig 115 emite sugerencia `parser_num_descripcion` score=1.0 confidence='alta' para A2026-0016 (lejana, mencionada literalmente). **Smoke 117 guard revert** ROJO inicial — bug fiscal descubierto: tras revertir factura a pendiente por devolución, un `+mov_C` posterior del mismo importe ±tolerancia re-cobra silenciosamente. **Causa raíz doble**: (a) guard de mig 117 en `auto_mark_cobrada_on_bank_match` solo cubre la transición `OLD.estado='cobrada'→NEW='pendiente'` (per-transición); (b) el chain `mirror_factura_match_to_nn → trg_zzz_mfa_recompute → recompute_factura_estado` hace OTRO UPDATE con `OLD.estado='pendiente'` que dispara el mismo trigger pero ya sin guard → re-encuentra mov_A libre y re-cobra (evidencia: 2 eventos `factura_cobrada_auto` al mismo timestamp + N:N con fila desvinculada por mirror + fila nueva creada en mismo instante por re-match). **Fix mig 128** (escrita + aplicada en sandbox vía user en Studio): columna persistente `facturas.last_revert_at TIMESTAMPTZ` seteada por `detect_reembolso_for_cobro` (mig 117) y `detect_reembolso_movimiento` (mig 112) en el UPDATE de revert; filtro `last_revert_at IS NULL OR < NOW()-INTERVAL '30 days'` añadido en 4 funciones de auto-match: `auto_mark_cobradas_on_movimiento_emitidas` (mig 114, ON mov INSERT), `auto_mark_pagadas_on_movimiento` (mig 112), `auto_mark_cobrada_on_bank_match` (mig 117, ON facturas UPDATE — la que se nos había pasado y cerró el chain recompute), `auto_mark_pagada_on_bank_match` (mig 111, simetría recibidas). Smoke 117 v3 VERDE post-mig 128: factura sobrevive a +mov_C casual mismo importe en ventana. Ventana 30 días — user puede casar manual via UI (escribe directo `cobrada_con_movimiento_id`, bypass trigger). **Fixtures persistentes** `docs/runbooks/smokes/conciliacion/` (3 CSVs + postcheck SQL + README) — antes los CSVs estaban en `~/Desktop` y se perdieron entre sesiones. **Hallazgo en `Stack/incidents.md`**. **Side learning**: psql directo contra pooler Supabase falla por password en `.env.local` rotado — service-role REST funciona; mig se aplica vía Studio. **Aprendizaje meta multi-trigger**: cuando un guard de un trigger no funciona, mapear el chain entero (INSERT mov → mirror → recompute → re-fire UPDATE trigger), no asumir que UN trigger es responsable de toda la cadena. **Pendiente próxima sesión**: smoke 115 caso ambiguo limpio (el smoke actual encontró el camino feliz porque la "cercana" ganó mig 103 — falta validar el caso donde mig 103 escoge mal y el parser corrige); commit + push mig 128.
- 2026-05-20 16:30 · `[hecho]` · **Setup Supabase CLI + reconciliación tracking de migraciones con prod**. Hasta hoy las migs se aplicaban a mano por Studio → `supabase_migrations.schema_migrations` sólo trackeaba 001-058. Pasos: (1) `brew install supabase/tap/supabase` → CLI v2.100.1 en `/usr/local/bin/supabase`; (2) `supabase login` (token via Terminal real, no TTY en Claude Code); (3) `supabase link --project-ref lahqlyaxvobqjgdiftag`; (4) probes SQL contra prod para confirmar qué migs estaban realmente aplicadas (115-120 sí, 121 inicialmente no — luego user reportó que sí); (5) `supabase migration repair --linked --status applied 060…120 122 121` (56+2 migs marcadas applied como solo-metadata, no re-ejecuta SQL); (6) **rename `107b_conciliacion_ia_enrich_indexes.sql` → `122_conciliacion_ia_enrich_indexes.sql`** porque el CLI requiere prefijo `<num>_name.sql` puramente numérico y skipeaba el 107b. SQL es idempotente (`CREATE INDEX IF NOT EXISTS`), fresh setups lo aplican post-107 sin efectos. 2 comments referenciales actualizados: `supabase/migrations/107_conciliacion_ia_enrich.sql:15` y `src/lib/conciliacion/transfer-pairs.ts:12`. **Estado final**: `supabase db push --dry-run` ⇒ "Remote database is up to date". Commit `b6d05bf` en main. **Gotcha aprendido**: el rename intermedio a `1071_*` rompió el parser del CLI (colisión visual con `107` al ordenar), `db push --dry-run` decía "Remote migration versions not found in local migrations directory" → solución usar número >121 sin colisión.
- 2026-05-20 15:30 · `[hecho]` · **Sesión cascada de fixes "no se ve el PDF" → desenrosca 5 capas de bug en agency-portal + FacturaIA**. Reportado por user "al crear factura sale `no se puede crear una factura en facturaia [object Object]`". Cada fix reveló el siguiente bug debajo. **6 PRs portal (#68, #69, #70, #71, descripción ya en #49) + 4 PRs FacturaIA (#50, #51, #52, #53) + 1 commit oportunístico de agente paralelo (`520ea1b`)**. Resumen capa a capa:
    - **Capa 1 — `[object Object]` opaco** (PR portal #68): `extractError` en `auto-emit.ts` hacía `String(r.error.error)` y cuando FacturaIA devuelve `error` como objeto colapsaba a `"[object Object]"`. Fix: serializa con `JSON.stringify` + maneja string y objeto.
    - **Capa 2 — JSON crudo feo** (PR portal #69): tras desbloquear, salía `HTTP 422: {"error":{"type":"validation_error","message":"NIF del receptor obligatorio…",…}}`. Fix: surface `message` directamente del shape estándar `error.message`.
    - **Causa raíz factura**: NIF del receptor faltaba (cliente sin NIF rellenado). FacturaIA rechazaba por RD 1619/2012 art 6.1.d. **No es bug del portal, es validación correcta**. User añade NIF y emite OK.
    - **Capa 3 — PDF "Internal error"** (PR portal #70): tras emitir, abrir PDF devolvía `{"error":"Internal error"}` sin contexto. Mismo patrón que #68: endpoint `/api/facturaia/pdf/[shadowId]` tragaba el error en un catch genérico. Fix: surface body + log `remoteId`+status para diagnóstico.
    - **Capa 4 — Parser JSON falla con binario** (PR portal #71): tras surface, salía `"Internal error: Unexpected token '%', \"%PDF-1.4\\n%\"... is not valid JSON"`. **FacturaIA cambió** `GET /facturas/{id}/pdf` y `/presupuestos/{id}/pdf` para devolver **PDF binario directo** (Content-Type: application/pdf) en lugar de JSON `{url}`. `openapi-fetch` parseaba todo como JSON y reventaba. Fix: bypass openapi-fetch con `fetch` manual + negociación por Content-Type (binary → stream con `Content-Disposition: inline`; json → 302 redirect retro).
    - **Capa 5 — PDF sin `descripcion`** (PR FacturaIA #50): tras ver PDF, user nota que `descripcion` NO aparece bajo el concepto. La columna estaba en DB (mig 119), la API la aceptaba y persistía, `get-document.ts` la devolvía — **las 4 plantillas de `invoice-pdf.tsx` (PdfTech/Corp/Freelance/Creativa) solo imprimían `concepto`**, nunca `descripcion`. Fix: render condicional debajo en 8pt gris + `descripcion?: string \| null` añadido a `LineaFactura`. Datos retroactivos: facturas creadas hoy (post commit `34129ac`) ya tienen `descripcion` en BD → al re-pedir PDF sale; facturas pre-commit tienen NULL → nada que renderizar.
    - **Capa 6 — `Forbidden` al previsualizar PDF** (PR FacturaIA #51 + #52): user reporta `{"error":"Forbidden"}` al abrir PDF desde "Facturas emitidas". **Causa raíz que llevaba 3 semanas latente**: commit `508517d` (27-abr) renombró `src/middleware.ts → src/proxy.ts` junto a un fix de `logAgentAction`. Next.js solo invoca archivos llamados `middleware.ts`, así que el rename **desactivó toda la cadena de middleware durante 3 semanas** sin que nadie lo notara: refresh sesión Supabase, cleanup automático de cookie `impersonate_org`, gate phone-verified, redirect /login. Algunas estaban cubiertas por capas alternativas (admin layout RSC + handlers + dashboard layout para login), pero **cleanup de cookie `impersonate_org`: SIN otra capa** → cookie pegada → `resolveAllowedOrgIds` en `/api/documents/file` devolvía SOLO la org impersonada (override semantics) → superadmin pierde acceso a sus propios PDFs.
    - **Fix triple capa 6** profesionalizado: (a) PR #51 revierte el rename — 1 línea efectiva, restaura toda la cadena; (b) PR #52 cambia `resolveAllowedOrgIds` de override → **union semantics**: la org impersonada se AÑADE al set de orgs propias, no lo reemplaza. SOLO en `/api/documents/file` (autorización pura de bucket); NO se toca `with-api-auth`/`layout-server`/`render-pdf` donde "verlo como ellos" sigue siendo la semántica correcta; (c) commit `520ea1b` añade endpoint dedicado `POST /api/admin/exit-impersonation` que borra la cookie explícitamente + banner ImpersonateBanner con efecto inmediato (antes solo navegaba a /admin/orgs dependiendo del middleware como side-effect); (d) PR #53 test de regresión: extrae `buildAllowedOrgIds` como función pura exportada + 7 tests unitarios cubriendo override-vs-union, deduplicación, cookie no-UUID, superadmin sin membresías, etc.
    - **Diagnóstico clave que destrabó capa 6**: query a `org_members` confirmó user como propietario activo de `ea201784-…` → descartado problema de membresía → query a `profiles` confirmó `is_superadmin=true` → match exacto con la rama de impersonación de `resolveAllowedOrgIds` → encontrado rename del middleware vía `git log --diff-filter=R`.
    - **Documentación**: entrada en `Stack/incidents.md` con el detalle del middleware-off-3-semanas. **Borja revisará** (mensaje enviado en Slack DM): los cambios FacturaIA, especialmente el revert del middleware (#51) por ser el que más riesgo de regresión tiene.
    - **Lección operativa**: un rename de archivo crítico por convención de framework (`middleware.ts`, `route.ts`, `page.tsx`, `layout.tsx`) **debe llevar un test de smoke** que verifique que la cadena está activa. Un build limpio no detecta que Next.js no invoca tu archivo. Apuntar como mejora futura → tests e2e Playwright que verifiquen middleware activo (ej: cookie `impersonate_org` se borra en navegación sin query param).
    - Commits/PRs: portal #68 (`b…`), portal #69, portal #70, portal #71, FacturaIA #50 (PDF descripcion render), FacturaIA #51 (restore middleware, revert 508517d), FacturaIA #52 (union semantics), FacturaIA #53 (tests), commit `520ea1b` (exit endpoint + banner — bundled junto a switcher fix por agente paralelo en main local).

- 2026-05-20 13:00 · `[hecho]` · **PR #49 API v1 — `descripcion` opcional en líneas factura/presupuesto + mig 119 aplicada + smoke E2E prod VERDE** (merge `8b65e2a`). Disparado por Borja (agency-portal) tras separar líneas en `concepto` (corto, requerido) y `descripcion` (largo opcional, hasta 2000 chars). Auditado con **3 agentes paralelos** antes de codificar (backend/BD/security · PDF/typography · contratos API/webhook). **Hallazgo crítico**: la API estaba devolviendo `400 unrecognized_keys` ante `descripcion` por `.strict()` en Zod — Borja creía que se "ignoraba silenciosamente" pero estaba comiendo 400s. **Mig 119**: `ALTER TABLE lineas_factura ADD COLUMN descripcion TEXT CHECK len<=2000` + recreados RPC `create_factura_with_lineas` (094) y `convertir_presupuesto_a_factura` (088) propagando descripcion del JSONB. Verifactu huella NO incluye conceptos/descripciones (verificado mig 091:122-128) → cero impacto cadena AEAT. **Zod**: `lineaSchema` exportado para reuso (consolidé duplicado pre-existente en PATCH presupuesto), transform normaliza `\r\n→\n` y strip C0/C1 control chars (deja `\n` y `\t`). **createDocument + updatePresupuesto**: propagan a RPC factura, JSONB presupuesto y templateData PDF (3 sitios + 3 sitios respectivamente). **4 plantillas PDF** (tech/corp/freelance/creativa): render condicional debajo del concepto con `whiteSpace:pre-wrap` + `wordBreak:break-word`, tokens propios por plantilla, manteniendo fallback split-`\n` legacy del concepto para PDFs viejos. **GET endpoints**: nuevo `LineaDto` + `DocumentDto.lineas[]` poblado en singular GET (factura: JOIN `lineas_factura`; presupuesto: lee JSONB); listings devuelven `lineas: null` para mantener query barata. **OpenAPI**: `Linea.descripcion` + nuevo schema `LineaResponse` + `DocumentResult.lineas[]`. **Tests**: 3 nuevos vitest (RPC factura propaga, null por defecto, JSONB presupuesto propaga). Lint+typecheck+build verdes en CI GitHub (2m58s quality + 3m22s integration). **Mig 119 aplicada en Supabase Studio** 2026-05-20 12:30 (user confirma `descripcion text` + ambos RPCs vigentes). **Detectado overload viejo 2-arg de `convertir_presupuesto_a_factura`** (deuda pre-existente, no la creó este PR — leftover pre-mig 082) — borrar en PR aparte tras grep callers. **Smoke E2E VERDE en prod** 2026-05-20 13:00 con API key `fia_test_OmHC9nbsqKGN7-N2Pg8_YXGAJEf4JEXbN1TbbYo3aS0`: POST `/api/v1/facturas` con `descripcion: "Setup WhatsApp + voz + kommo\nSesión kick-off + 2 revisiones"` → factura `A2026-0014` creada (id `e139fc2a-7887-464a-8461-ca833bdc06b7`, `verifactu_estado: no_aplica` → org del test key sin Verifactu activo, no anular); GET devuelve `lineas[0].descripcion` con `\n` literal preservado byte-exacto; PDF descargado 61KB renderiza descripcion debajo del concepto en gris pequeño multilinea (verificado visualmente). **Fuera de scope (deferred a PRs separados)**: (a) UI interna textareas en formularios crear/editar factura+presupuesto en `/generar` (no lo pidió Borja); (b) borrar overload 2-arg de `convertir_presupuesto_a_factura` (deuda); (c) añadir `descripcion` al webhook payload `factura.created`/`presupuesto.created` — actualmente NO incluyen líneas tampoco; (d) descartado tocar `invoice-pdf.tsx` por ser código muerto (ningún import real, solo mencionado en comentario de `layout.tsx`). **Aprendizaje destacado**: la auditoría de 3 agentes paralelos descubrió 3 P0 que mi plan inicial no contemplaba — (1) Zod `.strict()` ya rechazaba, no toleraba; (2) RPCs SQL hay que recrearlos enteros (no patch parcial); (3) `DocumentDto` no devolvía `lineas` (Borja necesitaba poder leerlas). El patrón "3 agentes con expertise dispar antes de codificar plan grande" sigue valiendo su peso en oro. **Acción Borja pendiente**: regenerar tipos con `npm run gen:facturaia` en `agency-portal` + quitar TODOs cast en `agency-invoices/service.ts` y `quotes/service.ts`. Avisarle también que **rote Idempotency-Keys** si va a reintentar requests previos al deploy con `descripcion` añadida (hash body cambia → 422 idempotency_conflict). Ver [[facturaia-pr49-lineas-descripcion]] para spec completa.
- 2026-05-20 00:00 · `[hecho]` · **Conciliación — auditoría 4 agentes post-A2 + Bloques 1-4 fixes + smoke F4 reembolso VERDE end-to-end** — auditoría paralela 4 agentes (security multi-tenant / fiscal AEAT / E2E backend / UX frontend) post PR-A2. **Bloque 1 quick wins (commits ya en main)**: (S1) `/api/conciliacion/categorias` POST valida `parent_id` pertenece a org pre-INSERT; (S2) `MetadataPatchSchema` narrow `categoria_source: z.literal('manual')` (impide forjar `ia_auto` desde cliente); (S3) PATCH movimientos valida `categoria_id` + `transferencia_pareja_id` pertenecen a org; (S4) confirm sugerencia valida UUID path + rechaza factura con `estado NOT IN (pendiente|enviada|vencida|parcial|sin_aprobar)` con 409 `factura_no_conciliable`; (S5) `sanitizeClienteName` aplica `redactPII` + escape «««/»»» en prompt enrich-batch; (E1) `decodeText` strip BOM 0xFEFF + dedup CSV sin external_id usa `.is('descripcion', null)` para NULL real (`.eq('','')` no matcheaba); (FE1/2/4) modal sugerencias refactor a `<Modal/>` accesible con focus-trap+ESC+aria-labelledby, `busyAny` previene clicks paralelos cross-cards, dictionary `src/lib/conciliacion/error-labels.ts` traduce códigos backend a copy usuario, responsive KPIs grid `auto-fit minmax(180px,1fr)` + media query <520px stack vertical sug-card-body. **Bloque 2 mig 111 — F1+F5+F-rev+F3 (aplicada)**: drop UNIQUE legacy `uniq_facturas_cobrada/pagada_movimiento` (incompatible con N:N), reescribe 4 triggers de mig 103/061 para `fecha_cobro/fecha_pago = mb.fecha` (NO `CURRENT_DATE` — fiscal AEAT criterio caja), trigger A recibidas SETEA `fecha_pago` (asimetría histórica fixed), `recompute_factura_estado(factura_id)` lee Σ N:N con tolerancia 0.5%/0.10€ → decide pendiente/parcial/cobrada/pagada, `trg_zzz_mfa_recompute` AFTER INSERT/UPDATE/DELETE en mfa, `invalidate_sugerencias_on_config_change` borra pending sugs si cambia tolerancia/ventana. **Bloque 3 mig 112 — F4 detect_reembolso (aplicada)**: columnas `movimiento_metadata.es_devolucion BOOLEAN` + `devolucion_de_movimiento_id UUID` con FK + COMMENTs, función `detect_reembolso_movimiento(mov_id)` busca cobro positivo previo en ventana ±dias conciliado a factura emitida cobrada del mismo importe ±tol — si encuentra: revierte factura a pendiente + INSERT/UPDATE mov_metadata es_devolucion=true + INSERT module_events `cobro_devuelto`, llamada desde `auto_mark_pagadas_on_movimiento` con return temprano antes de buscar factura recibida (evita marcar reembolso como pago erróneo de gasto). **Mig 113 — hotfix `facturas.fecha_pago` (aplicada)**: yo había referenciado `facturas.fecha_pago` en mig 111 sin verificar que existía. Solo existía `fecha_cobro` (mig 034) para emitidas — `fecha_pago` para recibidas asumida pero NUNCA creada. Síntoma user-facing: import CSV → `Error al guardar movimientos con referencia: column "fecha_pago" does not exist` con `imported:0`. Fix: `ADD COLUMN fecha_pago DATE` + backfill desde `pagada_con_movimiento_id` → `mb.fecha` + index `(org_id, fecha_pago) WHERE fecha_pago IS NOT NULL`. **Aprendizaje**: tocar triggers que asumen columnas existentes → verificar con `information_schema.columns` antes de escribir mig. **Mig 114 — hotfix stack overflow reembolso (aplicada)**: tras mig 112, importar mov -100€ devolución → `stack depth limit exceeded`. Cadena recursiva: detect_reembolso UPDATE facturas estado=pendiente → trg_auto_cobrada_on_factura (mig 103) re-matchea con +100€ original (sigue libre) → UPDATE estado=cobrada + cobrada_con_movimiento_id → trg_mirror INSERT mfa → trg_zzz_mfa_recompute → recompute UPDATE facturas estado/fecha/cobrada/pagada → trg_auto_cobrada otra vez → loop. Fix: (a) `pg_trigger_depth() > 4 → RETURN NULL` guard en detect_reembolso; (b) **predicado neutralización** en auto_mark_cobrada (UPDATE-side) + auto_mark_cobradas_on_movimiento_emitidas (INSERT-side): `NOT EXISTS (SELECT 1 FROM movimiento_metadata mm2 WHERE mm2.devolucion_de_movimiento_id = mb.id)` — un mov positivo cuya devolución posterior ya está registrada queda excluido del pool de candidatos; (c) en `auto_mark_cobrada_on_bank_match` también `fecha_cobro = (SELECT fecha::date FROM mb WHERE id=v_mov_id)` (F1 retroactivo a este trigger), (d) `auto_mark_cobradas_on_movimiento_emitidas` skip si mov entrante ya tiene es_devolucion=true (defensa segundo orden). **Aprendizaje**: ciclo trigger emerge cuando reverter estado deja "candidato libre" que otro trigger inmediatamente re-asocia — la solución no es solo `pg_trigger_depth` (cura síntoma) sino marcar el candidato como "consumido" para que el trigger competidor lo excluya (cura causa). **Bloque 4 UI (commit en main pendiente)**: `conciliacion-view.tsx:117-121` no filtraba `deleted_at` (comentario decía "defensivo por mig 106 no aplicada", pero llevaba semanas aplicada) → movimientos soft-deleted vía SQL seguían apareciendo en lista. Fix: añadir `.is('deleted_at', null)` a query principal + KPI pendientes; log diagnóstico en `/api/conciliacion/import` cuando `parsed.movimientos.length === 0` (warnings de parser se perdían en UI). **Smoke F4 reembolso end-to-end VERDE (verificado user con 3 queries SQL)**: import `smoke_reembolso_1_cobro.csv` (+100€ 2026-05-19) → factura `A2026-0009` queda `cobrada` con `fecha_cobro='2026-05-19'` (NOT CURRENT_DATE) + `cobrada_con_movimiento_id=7d185103...`. Después import `smoke_reembolso_2b_devolucion.csv` (-100€ 2026-05-20) → factura revertida a `pendiente` + `fecha_cobro=NULL` + `cobrada_con_movimiento_id=NULL`; mov +100€ queda `es_devolucion=false` (correcto — es el original) pero NEUTRALIZADO via predicado mig 114 (otro mov referencia su id en `devolucion_de_movimiento_id`); mov -100€ queda `es_devolucion=true` + `devolucion_de_movimiento_id` apuntando al +100€; evento `cobro_devuelto` registrado en `module_events` con resumen completo ("Devolución detectada: factura A2026-0009 revertida a pendiente, cobro original 100€ del 19/05, devolución 100€ del 20/05"). Sin stack overflow. **CSVs smoke** en `~/Desktop/`: `smoke_reembolso_1_cobro.csv` (`19/05/2026,COBRO TEST REEMBOLSO 100EUR,100.00,ES01,SMOKE-COBRO-1`), `smoke_reembolso_2_devolucion.csv` (`20/05/2026,...,-100.00,ES01,SMOKE-DEVOL-2`), `smoke_reembolso_2b_devolucion.csv` (con ref nueva tras dedup blocking). **Limitación F4 conocida (deuda task #46)**: detección solo dirección negativo→busca-positivo. Si devolución entra ANTES del cobro original (poco probable pero posible si import histórico desordenado), F4 no dispara. Fix bidireccional: trigger también desde positivo→buscar-negativo-pendiente-marcar-es_devolucion. **Pendiente commit + push + deploy** de los archivos sin commitear: `src/components/conciliacion/conciliacion-view.tsx` (filtro deleted_at), `src/app/api/conciliacion/import/route.ts` (log warn), `src/app/api/conciliacion/sugerencias/[id]/confirm/route.ts`, `src/lib/conciliacion/error-labels.ts` (nuevo), `src/lib/conciliacion/ai-enrich.ts`, `src/lib/conciliacion/import-parser.ts`, `src/app/api/conciliacion/categorias/route.ts`, `src/app/api/conciliacion/movimientos/[id]/route.ts`, `src/lib/documents/anular-factura.ts`, `src/app/globals.css`, `supabase/migrations/111_*.sql`, `supabase/migrations/112_*.sql`, `supabase/migrations/113_*.sql`, `supabase/migrations/114_*.sql`. **Decisión próxima sesión**: 3 candidatos — (1) **#41 P0 BUG factura sin cliente cuando nombre nuevo** (urgente fiscal — recomendado, corta cashflow por cliente, cobros recordatorios, mod 347); (2) **#44 PR-A2.1 parser num factura en descripción banco** (matches `A2026-XXXX` literal en `mb.descripcion` → boost score sugerencia; encontrado caso real: mov mencionaba A2026-0006 y sugerencia apuntaba a A2026-0003 con badge REVISAR correcto pero parser num daría confidence='alta' al match correcto); (3) **#46 F4 bidireccional** (caso edge — devolución antes que cobro). Anterior recomendación: 1 → 2 → 3. **Bloque 5 strategic diferido** (no urgente): N:1 matching real para pagos parciales/agrupados (cliente paga 250€ por 2 facturas 100+150), split-view full-width estilo Gmail/Linear (canvas vs modal), inline edit + j/k keyboard shortcuts, RPC atómica `confirmar_sugerencia` (S7 audit), FE3 bulk-confirm preview modal con checkbox individual, FE5 `<tr role="button">` → estructura semántica, columna `currency` en movimientos (multi-divisa). **Sprint A3 NEXT después de #41/#44**: Copiloto v2 con tools.
- 2026-05-19 · `[en progreso]` · **PR-A2 sugerencias deterministas factura↔movimiento + N:N sync — mig 109 escrita** — siguiente paso tras cerrar PR-A1.6 IA enriquecimiento. Mig 109 (`109_conciliacion_sugerencias_y_nn.sql`) implementa 6 piezas: (1) función `compute_sugerencias_for_movimiento(mov_id)` que busca facturas con tolerancia 2× del config, score lineal 1.0→0.5, confidence alta/media/baja según diferencia vs tolerancia estricta, evita duplicados ya emparejados (`already_matched`), `DELETE pending` previo + INSERT en `conciliacion_sugerencias` con `expires_at +30d`; (2) función simétrica `compute_sugerencias_for_factura(fact_id)`; (3) triggers `trg_zz_sugerencias_after_mov` AFTER INSERT movimientos + `trg_zz_sugerencias_after_fact` AFTER INSERT/UPDATE facturas (prefijo `zz` para orden alfabético post mig 103 auto-match); (4) double-write trigger `mirror_factura_match_to_nn` AFTER UPDATE de `cobrada/pagada_con_movimiento_id` → upsert/desvincula en `movimiento_factura_asignacion` (N:N expand-contract, columnas legacy mantienen 6 meses por spec hasta migrar readers); (5) backfill: `INSERT ... ON CONFLICT DO NOTHING` desde existing `cobrada/pagada_con_movimiento_id` → filas N:N tipo `cobro`/`pago` metodo `automatico`; (6) asserts. Decisión arquitectónica: expand-contract migration (Add new → Double-write → Migrate readers → Backfill → Contract). Drop de columna legacy va en mig futura 3-6 meses cuando todos los readers (dashboard, drawer, cashflow, /api/conciliacion/movimientos/[id]) lean de N:N. **Pendiente**: aplicar mig 109 en Supabase Studio, endpoint `/api/conciliacion/sugerencias/run` (cron Dokploy `*/30 * * * *` + disparo manual desde botón "Buscar sugerencias"), UI tabs colapsables Por revisar/Movimientos/Historial, panel sugerencias en drawer (lista chips factura con Asignar/Descartar/Buscar otra). Hub `00-home/facturaia.md` actualizado con entrada NOW + smoke pendiente.
- 2026-05-19 · `[hecho]` · **PR-A1.6 + A1.6.1 IA enriquecimiento movimientos bancarios — desplegado y funcional** — 5 commits a `origin/main` (`d0f33f4 6794225 8d85c6c fbd3987 b87020e 7558ac9 1afa7d8 1cba0de a410632 73a12c7 1fdf9ab 4a210e5`). 3 mig aplicadas en prod (107, 107b, 108). Endpoint `/api/internal/conciliacion/enrich-batch` con `withCronTracking` + `withApiAuth` x-service-key, kill switch `AI_ENRICH_DISABLED=1`, cap coste $0.50/día/org con pre-reserva, `p-limit(5)` orgs paralelas, gate signo server-side (importe>0 → solo categorías ingreso/neutro), whitelist UUID anti-alucinación, dedup resultados LLM, ia_prompt_version filtro reproceso, kill switch zombies vía mig 108 `cleanup_cron_zombies()` + watchdog `/api/internal/system/cron-watchdog` cron `*/5 * * * *`. **5 agentes auditaron** (correctness + prompt + SRE + UX + behavioral trust + competidores SaaS) y produjeron prompt v5: regla 8 INVIOLABLE signo + orden razonamiento (signo→filtrar→leer) + few-shots palabras-trampa (NOMINA+/ALQUILER+/IVA+) + lista proveedores ES (Vodafone/Endesa/AEAT/SS/Amazon/SaaS/Marketing) + sección INGRESOS explícita + categorías agrupadas por tipo en prompt (modelo solo ve compatibles con signo). **Bugs encontrados y arreglados durante smoke**: (a) embed PostgREST ambiguo por 2 FKs entre `movimientos_bancarios` y `movimiento_metadata` (mov_id + transferencia_pareja_id) → refactor a 2 queries separadas; (b) Anthropic key inválida en prod → switch a OpenAI via `/admin/llm`; (c) cache provider 60s necesita recreate container Dokploy para invalidar; (d) maxTokens viejo (200+80*movs) en call real pese a fix en pre-reserva → unificado; (e) `response_format json_object` OpenAI + finishReason detection; (f) LLM devolvía arrays parciales (jsonMode cierra JSON válido con menos items) → batch 20→10 + log partial; (g) gate signo descartaba nueva pero NO limpiaba categoría IA legacy → fix con UPDATE explícito a NULL si source!=manual. **Resultado smoke 18 movs**: 14 ia_auto, 3 ia_sugerencia (Starbucks/Mercadona/Autónomo Colab — ambiguos legítimos), 1 manual, 0 null, 0 errores, $0.012 total. NOMINA EMPRESA SL +2500 correctamente clasificado a Ventas y servicios (no Nóminas pese a palabra-trampa). **UI desplegada**: CTA "X por revisar [Confirmar todas]" warn, badge "Revisar" en sugerencias, drawer botones Confirmar/Descartar inline, concepto enriquecido visible (solo si difiere del crudo tras normalizar), transferencias internas atenuadas opacity 0.6 + icono ↔ + toggle "Mostrar/ocultar" default ON, onboarding modal first-run (localStorage) **sin mencionar IA/GPT** (operational transparency). Endpoint `/api/conciliacion/movimientos/bulk-confirm` POST pasa ia_sugerencia/ia_auto → manual. **Métricas IA al usuario** (decisión 3 agentes): NUNCA mostrar %accuracy (negativity bias → paranoia), coste $$$, modelo/provider, prompt_version, "powered by GPT-4". Solo count trabajo pendiente accionable. Esas métricas guardadas para `/admin/ia-ops` interno (futuro). **P1 diferidos a PR-A1.7**: SKIP LOCKED claim atómico (lock global cron_runs cubre el caso actual), query rotada por org (round-robin para orgs grandes que monopolizan los 5000 candidatos globales), tiktoken real (chars/3.5 in + chars/3 out subestima ~10% pero acceptable), cap global plataforma además del por-org, uso real columna `ia_claim_at`. **Tests**: 31 vitest verdes (transfer-pairs anti-injection + signo edge + parse + redact PII + estimate tokens + dedup). Lint+typecheck+build limpios. Coste deploys deploy Dokploy: ~2 min/push.
- 2026-05-19 · `[hecho]` · **Auditoría completa + 13 hallazgos cerrados (commits 2bbc538 + 5cb857d) — 4 agentes verifican VERDE** — 2 rondas de auditoría paralela 4 agentes (security multi-tenant / fiscal AEAT / backend integridad / frontend). Round 1 detectó 16 hallazgos reales tras filtrar ~50% FP. **P0 fixed (5)**: (a) `/api/email/send` BOLA cross-tenant — `org_id` del body sin validar contra sesión → exfil PDFs + abuso SMTP → migrado a `withApiAuth` + Zod no-strict (tolera org_id legacy de 3 callers) + rate-limit 30; (b) mig 103 dedup conciliación — mismo movimiento bancario podía marcar 'cobrada'/'pagada' N facturas distintas con `LIMIT 1` arbitrario → columnas `cobrada/pagada_con_movimiento_id` con UNIQUE partial + `NOT EXISTS` en SELECTs + `ORDER BY ABS(importe-total) ASC, ABS(fecha-mb.fecha) ASC`. Aplicado a mig 061 (recibidas) y mig 101 (emitidas) por simetría; (c) mig 103 skip `OLD.estado='borrador'` en trigger A — fresh emission no puede haber sido cobrada antes → evita match falso con ingreso casual ±30d; (d) mig 102 trigger emit recalcula `cuotaTotal` con `Σ ROUND(cuota_grupo,2)` leyendo `lineas_factura` (no `total-base`) + handle abonos R1-R5 según motivo (no F1 hardcoded) → IVA mixto ya no rompe huella↔XML; (e) `process/route.ts` carga `num+fecha` de factura anterior y pasa a `buildRegistroAlta` → `<RegistroAnterior>` con NumSerie+Fecha pobladas (antes vacías → rechazo XSD AEAT garantizado en cadena ≥2). **P1 fixed (3)**: (f) `emitirBorrador._generateAndStorePdf` devuelve `string|undefined` + `EmitirBorradorResult.pdfError` expone fallos PDF al caller; (g) `/api/verifactu/validate` migrado a `withApiAuth` + rate-limit 20 (cada call gasta tokens LLM); (h) `exportCSV` con RFC 4180 (escapa comas, comillas, saltos). **P2 fixed (3)**: (i) `/api/verifactu/retry` UPDATE con `.eq('org_id', orgId).eq('verifactu_estado','rechazada')` defense-in-depth + race intra-org cerrada; (j) mig 102 CHECK constraint `facturas_num_required_if_not_borrador`; (k) `saveEdit` guard de estado (solo borrador/sin_aprobar editable). **Round 2 verificación** detectó **1 P0 nuevo introducido por mi propio fix** + 2 P1+P2: (l) **mig 104 hotfix** — CHECK de mig 102 aplicaba a TODA `facturas` → recibidas con OCR fallido (num=NULL) no podían aprobarse → restringido a `tipo='emitida'`; (m) `process/route.ts` añade `continue` con `console.error` si cadena rota (prev factura no encontrada) — antes enviaba XML vacío al AEAT; (n) modal `handleEmitir` propaga `pdfError + emailWarning` vía `onAnulada({warning})` → parent muestra toast info (antes silenciaba avisos cuando emit desde modal de detalle). **Round 3 verificación final** 4 agentes devuelven `VERDE — sin hallazgos críticos ni medios` por unanimidad. **Migs aplicadas en prod 2026-05-19** (verificadas por user via SQL `pg_proc.prosrc` + `pg_constraint.condef` + `information_schema.columns`): 102 (trigger emit Σ + abonos + CHECK), 103 (dedup conciliación emitidas+recibidas — columnas `cobrada/pagada_con_movimiento_id` + UNIQUE partial + funciones reescritas), 104 (hotfix CHECK solo emitidas — tras intento inicial fallido por copy-paste del texto explicativo junto al SQL, re-aplicada pelada OK). Lint+typecheck+build verdes. **Falsos positivos descartados Round 1** (~50%): bugs frontend con mitigación existente (canAnular bloquea anular borrador, optimistic update correcto tras emit), prompt injection en diagnose-aeat (errorText viene de BD AEAT no del usuario, delimitadores OK), conciliación cross-tenant (todas las funciones filtran org_id), SQL injection (cliente Supabase tipado), SSRF logoUrl (mitigado por `--no-sandbox`+container). **Falsos positivos Round 2** (~30%): rounding negativo .005 edge case (no nuevo, también en mig 091/093), race intra-org retry (mitigado por triple-eq), AbortController en handleDiagnosticar (warning React, no crash). **Notas menores no-bloqueantes diferibles**: `pdfError` no se loguea en `logAgentAction.detalles`, mig 104 NOT VALID sin `VALIDATE CONSTRAINT` posterior, prefijo asimétrico de warning toast (`PDF:` no prefijado pero `Email:` sí).
- 2026-05-19 · `[hecho]` · **Quick wins post-borradores Holded** — commit `6175be3`. (1) **Trigger simétrico emitidas→cobrada** (mig 101 original, reemplazada por 103 en mismo día): cuando entra movimiento bancario positivo casa con factura emitida pendiente → marca `cobrada` + `fecha_cobro`. Cubre los dos sentidos (factura preexiste vs movimiento preexiste). Reutiliza feature `conciliacion` + config `auto_marcar_cobradas`. (2) **Diagnóstico inteligente rechazos AEAT** — endpoint `POST /api/facturas/[id]/diagnosticar-aeat` lee `verifactu_error` y llama LLM (`callLlm({tier:'fast', maxTokens:300, temperature:0})`) con prompt + delimitadores anti-injection (errorText truncado 1000ch). UI: modal añade botón "¿Qué significa este error?" cuando `verifactu_estado='rechazada'` → muestra explicación inline.
- 2026-05-19 · `[hecho]` · **Borradores Holded-style (num=NULL) — implementación completa + audit + 5 bug fixes** — Decisión: en lugar de eliminar borradores (P0 anterior), se adopta modelo Holded donde `facturas.num` es NULL hasta la emisión. Número se asigna atómicamente al emitir via RPC `emit_factura_borrador` con `pg_advisory_xact_lock`. **BD**: mig 099 (num nullable, trigger BEFORE INSERT skip borrador, nuevo trigger BEFORE UPDATE con WHEN clause, RPC emit_factura_borrador SECURITY DEFINER) + mig 100 (`NULLIF(p_factura->>'num','')` en `create_factura_with_lineas` para que JSON `"num":null` inserte NULL real). Ambas verificadas en prod. **Código** (10 archivos): `Factura.num: string|null` en types.ts, `createDocument` soporta `estado:'borrador'` sin asignar num, `emitir-borrador.ts` llama RPC + genera PDF + email opcional, `facturas-view.tsx` tab borradores + `docKey` helper null-safe + `onAnulada` con update parcial, `factura-detail-modal.tsx` banner borrador con botón emitir, `generar-view.tsx` botón "Guardar borrador", email bloqueado para borradores. **4-agent audit + 5 bugs fijados**: (1) NULLIF num en RPC (mig 100), (2) WHEN clause en trigger emit, (3) `onAnulada` pasaba `estado='anulada'` tras emitir → ahora pasa `{estado,num}` reales del response, (4) skip PDF cuando idempotent=true, (5) `saveEdit` guardaba `num:''` en lugar de `null`. Typecheck+build limpios.
- 2026-05-19 00:30 · `[hecho]` · **Cierre auditoría Sprint 3 al 100% — 5 pendientes diferibles + RPC atómico + 4 migraciones nuevas** — sesión extendida tras la entrada 21:30 que cerraba 9/9 hallazgos iniciales. Auditoría cruzada e2e por 4 agentes paralelos adicionales (A-fiscal AEAT, A-security multi-tenant, A-code-quality, A-prod-readiness/infra) sobre los commits del Sprint 3 ya en main: detectó **6 críticos + 15 importantes**; filtrado FP ~50% dejó **3 bloqueantes prod cutover + 5 pendientes diferibles**. **Bloqueantes** cerrados en `f43cf66` (mig 093 motivo NULL→R4 unificado triggers BD vs worker + EXCEPTION handler trigger 091 con RAISE WARNING NOOP por sucesor) y `e7144d3` (mig 094 RPC `create_factura_with_lineas` SECURITY DEFINER que envuelve INSERT facturas + INSERT lineas en una transacción única; `pg_advisory_xact_lock` del BEFORE INSERT se sostiene hasta DESPUÉS del AFTER INSERT recalculando huella con Σ → cierra race A-fiscal #2; DELETE manual de compensación eliminado porque Postgres rollbackea atómicamente). **Pendientes diferibles** cerrados en `529e467`: mig 096 `verifactu_skip_count` + worker counter anti-loop tras MAX_SKIPS=10 marca rechazada + `notifyFireAndForget kind='verifactu_factura_huerfana'` severity critical; mig 097 `CREATE UNIQUE INDEX CONCURRENTLY uniq_abono_por_factura_origen` partial sobre `(factura_origen_id) WHERE tipo_documento='abono'` blindando race concurrente anularFactura a nivel BD; ai-validate log estructurado con propiedades primitivas seguras (name/status/code/message.slice(200)) + cache `cacheGet` clone defensivo `issues.map({...i})`; anularFactura refactor con helpers `mapActor`/`loadExistingAbono`/`buildIdempotentResult` + audit `factura.anular.idempotent_hit` para distinguir doble-click vs primer-anular + try/catch race resolver que re-lee abono ganador en SQLSTATE 23505 con audit `race_resolved:true`. **Mig 095 independiente** (PR #48 `188f519` autoría AgentesIAMadrid bot 2026-05-18 23:53 UTC) creada por **otra sesión Claude paralela** en branch `fix/verifactu-toggle-unify`: unifica fuente única VeriFACTU en `organizations.verifactu_activo` eliminando JSON `settings.fiscal.verifactu` huérfano + guard rol + audit. **FP descartados auditoría**: A-fiscal #1 formato decimal (`NUMERIC(12,2)::TEXT` preserva escala Postgres → siempre "21.00", coincide con `.toFixed(2)`), A-security #3 trigger SECURITY DEFINER + RLS lineas (policy `lineas_insert` mig 002:58 bloquea cross-org), backfill huellas pre-089 (no aplica sin abonos AEAT prod). **Smoke pre-097**: `SELECT factura_origen_id, COUNT(*) FROM facturas WHERE tipo_documento='abono' AND factura_origen_id IS NOT NULL GROUP BY 1 HAVING COUNT(*)>1` → 0 rows confirmado user → CREATE INDEX CONCURRENTLY seguro. **Tests**: anular-factura.test.ts 36/36 con 2 nuevos race tests, create-document.test.ts 104/104 con mocks unificados rpc.mockResolvedValueOnce (sustituye queueResponse facturas+lineas separados), xml.test.ts 36/36. Lint+typecheck+build+e2e:smoke verde global. **Migs aplicadas en prod por user**: 089/091/093/094/095/096/097. **Cadena fiscal completa post-Sprint 3 audit (orden aplicación)**: 085 advisory_xact_lock huella, 086 facturas DELETE blindado aceptadas, 087 fiscal compliance Desglose XSD obligatorio, 088 convertir presupuesto exencion_codigo, 089 TipoFactura real F1/R1-R5, 091 huella post-líneas (sucesor NOOP defensivo), 093 motivo NULL→R4 + EXCEPTION handler, 094 RPC atómico create_factura_with_lineas, 095 settings unify (sesión paralela), 096 verifactu_skip_count counter, 097 UNIQUE abono partial. **Otras tareas sesión**: archivado workflows voz v1 en n8n.tufacturaia.com vía POST /api/v1/workflows/:id/archive (DpNxjOAzMMgORzrI Voice Confirm, gLWHHd9kRqphbtWJ Voice Correct, V5fsUJbm0srqktQH Voice Process — 0 referencias activas verificadas), limpieza NOW hub (movidos a histórico: Bloque 1 generar-view, Cumplimiento abonos, Tests Vitest, Mobile resto, Smoke 5 puntos, Sub-workflows voz, agency-portal PRs #54-56 verificados MERGED 2026-05-13 vía gh pr view, migración tufacturaia.com a NEXT). **Side incidents proceso**: (1) 2 sesiones Claude paralelas mismo repo causaron colisiones git en 3 ocasiones (commits cayeron en branch ajena necesitando cherry-pick a main; colisión naming migrations 092 → renumeradas mías a 093/094/096/097 evitando colisión con paralela 095); (2) commit `d360731` mobile polish FASE 2 (11 archivos UI) desapareció tras hook git, re-commiteado como `1a95e47` con mismos archivos. **Aprendizaje meta**: con sesiones paralelas, verificar `git branch --show-current` + `ls supabase/migrations/0XX*` antes de commit/Write + `git log --all` para detectar colisiones de naming antes de elegir número. **4 learnings vault**: [[verifactu-rpc-atomico-cierra-race-transacciones-rest-separadas]], [[unique-index-concurrently-parcial-para-idempotencia-bd]], [[llm-error-categorizar-no-exponer-crudo-a-logs]], [[claude-code-sesiones-paralelas-mismo-repo-colisiones-git]]. **NEXT recomendado próxima sesión**: P0 Eliminar concepto "factura borrador" (RD 1619/2012, ADR + refactor 2-3d), Cobrador inteligente (job nocturno WhatsApp ~1-2d alto impacto), Admin observabilidad bot WhatsApp cross-org (~2d tabla bot_error_log), Diagnóstico inteligente rechazos AEAT (~horas reusa ai-validate), Trigger simétrico emitidas→cobrada (~horas mig copia 061), Multi-org selector bot WhatsApp (~2-3d toca flujo más crítico), Manuales actualizar Bloque 1.
- 2026-05-18 21:30 · `[hecho]` · **Sprint 3 audit — 9/9 hallazgos cerrados** — tras auditoría cruzada (3 agentes, entrada 20:30) y aprobación del user, se aplicaron los 7 fixes pendientes en 3 commits: `c83d50b` (C1 mig 089 huella TipoFactura real F1/R1-R5, C2 pre-flight Supabase service-role valida `is_test=true`+`verifactu_entorno='pre'` en `auth.setup.ts`, I3 `requireRole:['propietario','admin','contable']` en `/api/facturas/[id]/anular` web); `7097797` (I4 guard idempotente `anularFactura` por `factura_origen_id` independiente del estado origen → +1 test 34/34 verde, I5 worker `verifactu/process` SKIP factura sin líneas con `continue` sin incrementar reintentos, I6 `cuotaTotal = Σ round(grupo.cuota, 2)` post-agrupación excluyendo exentos en lugar de `total-base`, I7 `xml.test.ts` 14→36 tests cubriendo TipoRectificativa I/S + FacturasRectificadas + b717a09 exencion abono + L8A/L9/L10 + escape XML rectificada); `0865e89` (I2 `escapeXml(p.numSerie)` en 3 ocurrencias de NumSerieFactura raíz de `xml.ts` —alta/baja/encadenamiento—, mig 091 trigger AFTER INSERT `lineas_factura` recalcula huella con `Σ` real solo si no hay sucesor en cadena con doble guarda anti-race). Migraciones 089 + 091 aplicadas en Supabase prod por user. `.env.test` con `E2E_ORG_NAME`+`SUPABASE_URL`+`SUPABASE_SERVICE_ROLE_KEY` configurado. Lint+typecheck+build+vitest+e2e:smoke verdes. **Pendientes residuales (no bloqueantes)**: ninguno fiscal — los gaps documentados quedan como deuda monitorizable (huella divergente si race trigger 091 vs sucesor en mismo tick, único escenario teórico no cubierto por NOOP defensivo). **Checklist humano** `docs/runbooks/sprint3-smoke-checklist.md` (commit `9e70284`) cubre validación visual de PDFs binarios + KPI dashboard + menú 3 puntitos que el code audit no demuestra. Item NOW "Smoke 5 puntos 2026-05-07" → marcado CERRADO con link a este histórico.
- 2026-05-18 20:30 · `[hecho]` · **Auditoría cruzada Sprint 3 cumplimiento fiscal + checklist humano** — sesión paralela había implementado smoke automatizado (`5ed2dc8 dbde6a6 0e94de4 501a593 b717a09 fc5738e`); esta sesión ejecutó FASE 0 (3 agentes A0.1/A0.2/A0.3: cobertura code audit ~55%, GO condicional sandbox `verifactu_entorno='pre'`, E2E Playwright parcial — puntos 1/4/5 no cubiertos auto) + FASE 3 auditoría cruzada (3 agentes A3.1 security multi-tenant, A3.2 fiscal AEAT, A3.3 PDFs regresión). **7 hallazgos reales** post-filtro ~50% FP: **🔴 C1 BLOQUEANTE pre-cutover prod** — trigger huella `085_verifactu_huella_lock.sql:75` hashea con literal `TipoFactura=F1` mientras `xml.ts:197` envía R1-R5 reales para abonos → AEAT rechazaría todo abono al subir a prod (mig nueva). **🔴 C2** smoke `sprint3-compliance.spec.ts` corre contra `app.tufacturaia.com` sin assert `verifactu_entorno='pre'`. **🟠 I3** `/api/facturas/[id]/anular` web sin `requireRole`. **🟠 I4** `anularFactura` guard idempotente roto si UPDATE origen falla → doble abono. **🟠 I5** race trigger antes de líneas insertadas → fallback desglose vacío. **🟠 I6** `cuotaTotal = total - base` divergente vs Σ CuotaRepercutida (redondeo IVA mixto). **🟠 I7** `xml.test.ts` 14 tests (hub decía 28). PDFs (A3.3) sin críticos. **Acciones**: `docs/runbooks/sprint3-smoke-checklist.md` con 5 puntos validación humana (commit `<pendiente>`); 7 hallazgos en `Stack/incidents.md`. Regla "no fix inline durante smoke" — hallazgos al user para triage. **Side incident**: commit `d360731` mobile polish desaparecido tras reset sesión paralela; re-commiteado como `1a95e47`. Item NOW "Smoke 5 puntos 2026-05-07" permanece abierto hasta ejecución humana del checklist + decisión user sobre C1-I7.
- 2026-05-18 19:30 · `[hecho]` · **FASE 2 mobile polish + audit cruzada 4 auditores** — commit `d360731` (desaparecido tras reset sesión paralela; re-commiteado `1a95e47`). PR #5 cubrió críticos; FASE 2 cierra formularios <480px, admin sistémico, settings/informes/calendario, iOS anti-zoom y a11y profunda. **5 agentes paralelos** (forms /generar, admin sistémico, settings+informes+calendario+ingesta, iOS zoom 16px transversal, a11y+dvh+safe-area) + **1 auditor visual** + **4 auditores cruzados** post-impl (CSS cascade/bugs, a11y profundo WCAG, atajos/profesionalidad, cross-browser/iOS) → **5 críticos + 8 importantes detectados** y arreglados. **Críticos fix**: (1) `voz-variables-form.tsx:790` `min(vh,dvh)` atómico falla Safari <15.4 — movido a clase `.voz-variables-preview` con cascada doble declaración segura; (2) `inert` en `<main>` + `aria-hidden` complementario fallback iOS 15.0-15.4; (3) restore-focus en `requestAnimationFrame` (race WebKit con inert activo); (4) `Sidebar` mobile con **focus trap real Tab/Shift+Tab** + focus inicial al X + `aria-modal`; (5) **5 wrappers tabla** con `role="region" tabIndex={0} aria-label` WCAG 2.1.1 + clase reusable `.set-table-wrap` (dedupe inline-style ×5). **Importantes fix**: eliminado `.api-keys-input` muerto + `--muted2-text` huérfano (política limpia), patches `inbox-col`/`set-side` integrados in-place (no override al final), `.sort-select` outline-offset `-2px → 1px`, toast item `role="status" aria-atomic`, `WebkitOverflowScrolling:'touch'` legacy removido. **Reglas confirmadas**: proyecto NO usa Tailwind (CSS puro + variables + media queries), body Inter no Cabinet Grotesk, breakpoint canónico mobile=640px. **No tocado**: `.ab-primary !important` legacy (2274), `notifications-drawer` `aria-controls`, `layout/icon.tsx`. Smoke E2E + lint + typecheck + build verdes. **Gaps declarados** (no atajos): migración masiva `--muted2`→AA sprint dedicado, `set-table`→cards <480px (overflow-x con region/tabIndex es WCAG-válido). **Docs**: `docs/architecture/gotchas.md` §Frontend con 7 reglas nuevas (vh→dvh cascada doble, inert+aria-hidden complementarios, restore-focus rAF, tablas region/tabIndex/aria-label, iOS 16px anti-zoom, overscroll-behavior modal body, btn-primary legacy no tocar), `manual-usuario.md` §10 "Uso en móvil", `manual-admin.md` §30.10 mejoras admin mobile. **4 learnings vault**: [[mobile-vh-dvh-cascade-vs-min-atomic-safari-15]], [[mobile-inert-plus-aria-hidden-ios-15-fallback]], [[mobile-restore-focus-after-inert-needs-raf]], [[mobile-table-scroll-x-needs-region-tabindex-wcag-2-1-1]].
- 2026-05-18 14:30 · `[hecho]` · **Sesión rediseño toolbar `/emitidas` `/recibidas` + a11y notificaciones + docs** — 3 commits a `origin/main`: (1) `13d42fe` rediseño toolbar — `Filters` con prop `sort` integrada (select + dir toggle), doc-tabs ARIA `role=radiogroup` + `role=radio` + `aria-checked` + roving tabindex + flechas izq/der/Home/End (NO `aria-pressed` según W3C APG), `filter-pill.active`/`doc-tab.active` con gradient brand→brand-fg + sombra multi-capa `color-mix(in srgb, var(--brand) 25%, transparent)` + inset highlight, hover crafted con bg tint y count vira brand, `sort-control` 36/40px con `focus-within` ring, mobile <767px filter-pills scroll horizontal con fade hint Komarov (var `--scroll-shadow` con override `:root[data-theme="dark"]`), `filters-actions` wrapper agrupa sort+Más filtros+Exportar (reemplaza `filters-spacer`), `scroll-padding-inline: 16px`, easing project-wide `cubic-bezier(0.32,0.72,0,1)`. **4 agentes audit** (visual drift/a11y/responsive/code-quality) + **4 agentes research** (W3C APG/MDN/M3/Komarov) para validar rechazos por intuición — descubierto **3 errores propios corregidos**: aria-pressed→radiogroup (era subóptimo per APG), dark mode shadow (rgba negro invisible), scroll-padding-inline (chip enfocado pegado al borde). (2) `056ff94` fix a11y notifications drawer — focus trap Tab/Shift+Tab con `drawerRef` + `querySelectorAll` focusables, `aria-controls="notif-drawer"` en bell, `aria-labelledby="notif-drawer-title"` + `id` apuntando al h2, live region `role="status" aria-live="polite"` para Realtime, ref type `HTMLElement` correcto. **4 agentes audit** (security/correctness/a11y/tests) — 0 vulns multi-tenant (RLS+withApiAuth+effectiveOrgId todo OK), `notify_upsert` atomic ON CONFLICT, ~50% falsos positivos descartados con razón (incl. claim Agent2 P0 falso: `unread-count` snooze null OK por `return acc` línea 61). (3) `93b5403` docs — manual-usuario §Filtros con sort+iconos pestañas+mobile, manual-admin §22 con `NON_SILENCEABLE_KINDS` completa (7 kinds + prefijo `cron_failed_*`) y subsección a11y drawer, `gotchas.md` §Frontend con 4 reglas (radiogroup vs aria-pressed APG, scroll shadows dark mode, `color-mix()` tematizable, drawer modal a11y). **Vault**: 4 learnings + `hot.md` + `CLAUDE.md` global regla "rechazos por intuición sobre specs → validar fuente oficial". Smoke E2E **12 passed en 55.3s** cero regresiones. **Aprendizaje meta**: mi intuición acertó 5/8 audits pero falló en spec ARIA y dark mode CSS — validar con W3C APG/MDN/M3 antes de descartar propuestas en áreas spec-driven.
- 2026-05-17 23:00 · `[hecho]` · **Sesión Phone State Guard v2 + panel Auditoría unificado** — 5 commits pusheados a `origin/main`: (1) `0c06e36` docs slim CLAUDE.md + extract gotchas.md (~80 líneas vs 250); (2) `f8936a0` Phone State Guard v2 reescrito como Code node (no httpRequest — fix de raíz tras debate 3 agentes: HTTP node reemplaza item rompiendo downstream Hash Session; Code propaga `{ ...parsed, phone_change_match, phone_change_meta }` via spread, patrón idéntico Buscar Org Voz). Anti-enum byte-idéntico para `unknown` y `pending_change_new`. Fail-open con `phone_change_check_error` truncado 120 chars. Aplicado a prod n8n.tufacturaia.com workflow `pqSWkDIHqmSVHotB` (77 nodos). E2E live verificado: exec 165 verified OK, exec 169 pending_change_old con copy literal en WhatsApp, caso unknown probado estructural. Runbook `docs/runbooks/phone-state-guard-v2-deploy.md` 462 líneas. Validador `validate-phone-state-guard-v2.py` 33 checks. (3) `d9cad05` endpoint `/api/settings/auditoria` que mergea audit_log+phone_otp_audit con masking server-side; (4) `2548259` UI polish completo tras debate con 4 agentes (contable/forense/UX/backend) — "Quién"=persona NUNCA agente IA, "Tú" cuando viewer es actor, fallback email→local capitalizado, "Sin identificar" para histórico, expand row inline con chevron animado + ARIA + ESC, número humano del documento protagonista (FAC-2026/0042), filtros por categoría server-side (Documentos/Mi cuenta/Equipo/Agentes IA), filtrado de UUIDs internos de diffs, teléfono inline con canal. 63/63 vitest verdes. (5) `c982876` docs manuales — §29 Phone State Guard + §30 panel auditoría unificado en admin, §7 reescrita + §1 ampliada en usuario. **Hallazgos colaterales**: (a) regex maskPhoneE164 duplicada con bug greedy `\d{1,3}` → eliminada, ahora re-exporta canónica de `phone-mask.ts`; (b) FACTURAIA_SERVICE_KEY del backend desincronizada con credencial n8n tras rotación — re-sincronizada; (c) AI Agent responde "no tengo id de presupuesto" ante texto sin sentido — apuntado para revisar prompting (no del guard). **Iteración B diferida**: impersonación visible en feed cliente, lookup proveedor email entrante, export CSV gateado por rol, backfill caller_* histórico descartado por riesgo legal. **Smoke prod pendiente**: tras redeploy automático Dokploy del app FacturaIA, validar `/settings?tab=auditoria` con feed real + expand row funcional + filtros categoría.
- 2026-05-17 · `[en progreso]` · **Mi cuenta + cambio de teléfono — 5 ramas paralelas** — feature self-service para sustituir `phone_e164` post-verificación sin pasar por soporte. Decisiones cerradas: email read-only v1, re-auth password + OTP al nuevo número, sesiones revoke "others" tras confirm, email "No fui yo" con revoke 24h, notif cross-org in-app a propietarios por defecto (email opt-in), bot WhatsApp bloqueado en AMBOS números mid-change, rate-limit 3 starts/24h/user + cool-down 5min + 72h post-cambio confirmado. Migration `077_phone_change.sql` añade columnas pending + tabla `phone_change_revoke_tokens` + 5 RPCs. Endpoints `/api/auth/phone/change/{start,verify,resend,cancel,status}` + `GET /api/auth/revoke-change`. UI `/settings?tab=mi-cuenta` + banner ámbar persistente + 2 modales. n8n receptor v2 patcheado con `Phone State Guard` + `Reply Phone Locked` (script `ops/n8n-patches/apply-phone-change-guard.py`). Cron `phone-change-sweep` cada 1min (pg_cron, NO Dokploy) expira pendings + limpia tokens. Anti-enumeration `/api/internal/whatsapp/resolve-context` con campo `match` y body byte-idéntico para `unknown` vs `pending_change_new`. **Branch docs `feat/phone-change-docs`**: manual-usuario sección "Cambiar tu teléfono móvil", manual-admin §28 (28.1-28.11 con threat model T1-T8 + métricas + recovery + edge cases), ARCHITECTURE.md §5.9, onboarding-flow.md §3 reescrita con diagrama Mermaid completo (estados/transiciones/revoke), `docs/legal/phone-change-privacy-note.md` (retención 6 años AEAT, base legal 6.1.b+c RGPD). Commits de las 5 ramas se mergearán al cerrar la feature. Smoke tests pendientes post-merge: E2E cambio happy path, cancel mid-change, revoke 24h vía email, lockout 5 fallos, race attacker-vs-user, bloqueo bot WA en ambos números, notif cross-org a propietarios.
- 2026-05-16 20:30 · `[hecho]` · **OTP polish — 4 agentes review + 2 batches fixes** — 4 agentes paralelos auditaron (critique 23/40, audit técnico 13/20, baseline anti-slop 6.5/10, impeccable design 6.5/10) y se aplicaron las P0/P1 más altas: (1) email OTP rewrite — subject Stripe pattern con código (+2-3x CTR), HTML profesional con DOCTYPE/viewport/color-scheme dark mode, brand color #3D7BF5, footer legal CAN-SPAM/RGPD con razón social + soporte, pre-header invisible Gmail; (2) UX meta-row 6-CTAs competidoras → 2 niveles (primary: Reenviar + Email-si-aplica; secondary: Cambiar · Salir); (3) 'Recibir por email' oculto cuando channel=email (antes indistinguible); (4) toast email con 'revisa spam y promociones' (Marta-test crítico); (5) mapStartError default accionable; (6) grandfather banner 3-niveles emocionales (info ≥7d brand-blue → warn 3-6d ámbar → danger <3d rojo) con copy con utilidad no solo amenaza, color-mix tokens sin hex sueltos, transition .35s, contraste AA; (7) aria-live duplicado con role=alert resuelto; (8) tap targets meta-row 44px WCAG 2.5.5; (9) countdown tabular-nums + min-width 13ch sin layout shift; (10) botón Verificar min-width 168px sin shift texto+spinner; (11) endpoint público /api/auth/phone/check-available con rate-limit 10/min/IP fail-open; (12) /registro onBlur avisa si móvil ya verificado por otra cuenta — antes el user descubría phone_taken tras signup+email; (13) modal admin OTP migrado a componente <Modal> compartido (focus trap + ESC + return-focus + aria-modal); (14) CLAUDE.md doc tipografía corregida (Cabinet Grotesk real, no Filson como decía). **Commits**: `835ae00` (batch 1) + `1a3fec9` (batch 2). Build/typecheck/lint limpios. Deploy automático en marcha. **Pendientes opcionales** (próxima sesión, no bloqueantes): retirar SVGs inline → Icon registry, inline styles a clases (~12 occurencias), 6-slot OTP input (vs single con letter-spacing 0.55em), sparkline admin con eje Y + tooltip, ADR sobre Filson vs Cabinet final.
- 2026-05-16 19:55 · `[hecho]` · **OTP teléfono onboarding WhatsApp — funcional end-to-end en prod** — 3 agentes paralelos (backend / frontend / docs+n8n) más 5 agentes de review previo (security, backend, legal, UX, SRE). Smoke E2E real verificado 17:53 UTC: `start` channel=whatsapp + `wamid.HBgLMzQ2MTczMTQ5...` + cost=3¢ + `verify_ok` 22s después. Plantilla Meta `otp_facturaia` AUTHENTICATION con botón COPY_CODE expiración 10min, hard-cap 30 €/mes editable desde `/admin/system?tab=otp` con notif al 80%, fallback Resend automático cuando Meta falla, grandfathering D+15 (`GRANDFATHER_DEADLINE=2026-05-31`), TTL 10min, cooldown 60s, 3 starts/h user, 5 starts/h phone, 5 intentos, lockout 15min, solo +34 MVP, pepper en `system_config` cifrado con `CREDENTIAL_ENCRYPTION_KEY` NO en env, re-auth password para cambio post-verificado. **Bugs detectados y arreglados en la sesión**: (1) build-time crash en `openapi.json/route.ts` por `throw` en module-load sin `NEXT_PUBLIC_APP_URL`; (2) middleware `rewrite` vs `redirect` confundía URL bar; (3) página verificar arrancaba en stage `code` aunque no hubiera verificación abierta — añadida query `phone_verifications` server-side; (4) sin endpoint signout — creado `/api/auth/signout`; (5) signout usaba `request.nextUrl.origin` que devuelve `http://0.0.0.0:3000` del container — usar `NEXT_PUBLIC_APP_URL`; (6) compose `docker-compose.yml` no pasaba envs `META_*` ni `RESEND_API_KEY` al container — añadidas; (7) BuildKit cache de Dokploy ignoraba cambios src/ aunque `.deploy-marker` cambiara — fix con bump `package.json` 0.1.0→0.1.1 invalida cadena entera; (8) **bug crítico Meta**: plantilla aprobada con language `es_ES` ("Spanish SPA") pero código enviaba `es` → error 132001 → fallback email permanente. Default cambiado a `es_ES` en código + compose fallback. **Bug observabilidad arreglado**: dispatchOtp reseteaba `providerError` a null al éxito del fallback email → perdíamos visibilidad del fallo Meta; ahora `primaryChannelError` se conserva en audit aunque email rescate. **Panel admin Cobros unificado**: stats + pricing en `/admin/system?tab=cobros` (antes en 2 sitios). **Refactor `agentesia.world` → `app.tufacturaia.com`** en README + .env.example. **Docs**: `docs/manuals/manual-admin.md` sección 26.0-26.10 completa, `docs/manuals/manual-usuario.md` sección "Verificar tu teléfono", `docs/architecture/ARCHITECTURE.md` §5.8 + tablas nuevas, `docs/architecture/onboarding-flow.md` (3 diagramas Mermaid), `docs/legal/privacy-otp-update.md` (texto público política privacidad), `docs/architecture/n8n-data-layer.md` sección "Delivery status webhook". **Patch n8n**: `ops/n8n-patches/apply-delivery-status.py` idempotente — **no ejecutado todavía**, requiere API key nueva del n8n.tufacturaia.com (la vieja del agentesia.world devuelve 401). **Reglas nuevas en CLAUDE.md proyecto**: allowlist `OTP_BYPASS_ENDPOINTS` en `withApiAuth`, plantilla AUTHENTICATION no UTILITY, pepper en system_config no env, retención `phone_otp_audit` 6 años AEAT. **Memorias guardadas**: Resend Pro montado (no re-proponer), Meta template language `es_ES` (no `es`).
- 2026-05-16 12:50 · `[hecho]` · **Backup externo Wasabi configurado** — Dani confirmó que todos los Dokploys están conectados a Wasabi (S3-compatible). Backup nativo Dokploy del postgres-n8n activo apuntando a Wasabi via la pestaña Backups del proyecto n8n-prod. Sin necesidad de credentials manuales (Dokploy las inyecta del S3 Destination compartido). Cierra el TODO bloqueante de "sync externo de backups". Triple protección: (1) local age-cifrado en VPS, (2) Wasabi server-side AES-256 via Dokploy, (3) sin SPOF — si VPS revienta queda Wasabi, si Wasabi cae queda local cifrado, si filtra bucket el atacante no tiene la clave age para descifrar. Docs actualizadas: `docs/architecture/n8n-data-layer.md` sección "Capas de backup actuales", `manual-admin.md` 25.6.
- 2026-05-16 14:00 · `[descartado: bugs n8n LangChain 2.x]` · **Intento refactor receptor v2 → rollback** — tras 8h de trabajo intentando eliminar `$env.X` del workflow productivo, se detectaron bugs latentes de `@n8n/n8n-nodes-langchain.toolHttpRequest` typeVersion 1.1: cuando el LLM invoca varios tools secuencialmente con `jsonBody` con expressions, n8n cae al fallback runNode() y dispara "supplyData but no execute method" (afecta al SEGUNDO tool invocado, el primero funciona). **Rollback aplicado al productivo** (71 nodos pre-refactor, sigue funcional). Endpoints internos creados SIGUEN VIVOS para uso futuro: resolve-context, ingesta, ocr-process, voice/catalogo|clientes|facturas|presupuestos|presupuesto-detalle, chat-session-hash. **Decisión**: mantener flag `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` con justificación security audit (delta marginal vs RCE real). Reintentar refactor cuando n8n 2.21+ corrija el bug toolHttpRequest, o migrar a toolWorkflow con 4 sub-workflows. Backup checkpoints del intento en `knowledge/projects/agentesia/n8n-backups/facturaia/refactor-clon-checkpoint/` y `cutover-20260516-1306/`.
- 2026-05-16 12:35 · `[en progreso 50%]` · **Refactor receptor v2: eliminar `$env.SUPABASE_SERVICE_ROLE_KEY` del workflow** — siguiendo regla canónica CLAUDE.md "workflows consultan API v1, NUNCA query directa". **7 endpoints internos creados y desplegados en prod** (`/api/internal/whatsapp/resolve-context`, `/whatsapp/ingesta`, `/voice/catalogo`, `/voice/clientes`, `/voice/facturas`, `/voice/presupuestos`, `/voice/presupuesto-detalle`) — todos con `requireServiceKey` timing-safe, Zod estricta, rate-limit per-org, 404 cross-tenant anti-revelación. Commit `85784a7`. **Clon `WcOl1yKsROdYircp` (68 nodos) refactorizado al 50%**: 4 HTTP nodes con $env → cred, Buscar Org Voz/Texto + Check Feature + Preparar Input Correccion → endpoint resolve-context, cadena ingesta (4 nodos) consolidada en 1 HTTP al endpoint ingesta. **Pendiente próxima sesión (~6-8h)**: extender endpoint presupuesto-detalle para resumen_whatsapp, 4 toolCode → HTTP Request Tool, dividir Parsear y Calcular Totales + Preparar Datos Generar, refactor Disparar OCR, smoke E2E 6 escenarios, cutover atómico, quitar SUPABASE_SERVICE_ROLE_KEY + N8N_BLOCK_ENV_ACCESS_IN_NODE del compose. **Productivo intacto** durante todo el refactor. Backup checkpoint del clon en `knowledge/projects/agentesia/n8n-backups/facturaia/refactor-clon-checkpoint/clone-state-20260516-1232.json`. Docs: `docs/architecture/n8n-data-layer.md` sección "Refactor en curso". Decisión arquitectónica documentada con security audit threat model (delta marginal de B vs A en n8n Community single-admin, pero se completa por purismo correcto).
- 2026-05-15 18:00 · `[hecho]` · **Migración n8n viejo → nuevo + capa de datos profesional** — n8n.tufacturaia.com operativo en `185.47.13.170` con `n8nio/n8n:2.20.9`. Cutover completo: 8 workflows migrados (5 activos: Receptor v2 `pqSWkDIHqmSVHotB`, Verify, OCR, Email Polling, Error Handler; 3 Voice inactivos sin Redis). Webhook path renombrado `whatsapp-facturaia` → `whatsapp-tufacturaia`. App Meta nueva creada, número `+34 919 93 26 18` (WABA Facturaia `1236628691608937`) conectado via System User token. **Capa de datos pro**: BD postgres-n8n dedicada (red interna, no Supabase). Schema con `CHAR(64)`, índice compuesto, autovacuum tuned, tabla audit `_chat_history_cleanup_log`, tabla `_n8n_data_meta` version 1. **Hash sha256(salt+telefono)** en session_id — workflow patcheado para no guardar teléfonos planos (`N8N_CHAT_SESSION_SALT` en env Dokploy). **Retención automática** workflow `FYaTVufEl6HXALro` diario 3am Madrid, DELETE en lotes 10k + VACUUM. **Backups cifrados age** diario 02:30 Madrid `/root/backups/n8n/*.sql.gz.age`, rotación 7d, test restore E2E validado. **TODOs explícitos para escala** documentados: sync externo backups (BLOQUEANTE >5 clientes prod), endpoint alerta fallos, encryption-at-rest disco. Docs: `docs/architecture/n8n-data-layer.md` (nuevo) + sección 25 en `manual-admin.md`. Clave privada age en `~/.config/age/n8n-backup.txt` + password manager.
- 2026-05-17 18:30 · `[hecho]` · **Feature completa "cambio de teléfono post-verificación"** — 6 ramas + fixes round 2 mergeadas a main (commits `4758074` backend → `1599312` n8n-patch → `cbc3da4` frontend → `7b81677` fix cross-org email → `d5cf631` tests + vitest config → `c302179` docs + `f9dd2ad` reconciliación §28.12 → `b4b527f` fix Mi cuenta query). Migrations 077 + 078 aplicadas. 5 agentes audit (security/contract/n8n/migration + 1 fix) + 5 implementación + 5 round 2 fixes = 15 agentes total. **Smoke real OK**: AgentesiaLab verificó +34617314938 vía flujo /verificar-telefono nuevo (stage='send' con botón explícito + fix wraparound + mapeo completo error codes). **Pendientes apuntados como NEXT**: (a) n8n Phone State Guard patch falla — el workflow real no tiene "Resolve Context" HTTP node (refactor descartado), rediseño necesario; (b) panel auditoría extender con phone_otp_audit + actor_phone/owner. Mitigación sin guard: OTP 10min TTL + banner cancel cubre ventana de riesgo. **6 fixes UX detectados y arreglados durante smoke**: (1) handleStartChange redirect a /verificar-telefono si phone no verificado; (2) verify-client stage='send' UI dedicada con botón explícito (antes mostraba input OTP sin enviar); (3) `<strong>` con phone enmascarado `white-space: nowrap`; (4) doSend pasa phoneE164 explícito; (5) mapStartError mapea 13 codes; (6) Mi cuenta query profiles por user_id (PK), no id. Lección: cada agente paralelo asume contratos plausibles pero falsos — los auditors contractual round 2 atraparon 3 BLOCKERs antes del merge, pero quedaron 2 bugs (PK profiles + asunción Resolve Context) que solo el smoke real destapó.
- 2026-05-17 01:30 · `[hecho]` · **Crons en Dokploy nuevo + feature Run Now en panel admin** — 7 crons recreados en proyecto `tufacturaiaprod-tufacturaiaapp` (app.tufacturaia.com): webhook-dispatcher (`* * * * *`), check-vencimientos (`30 2 * * *`), cashflow-alerts (`30 7 * * *`), storage-quota-check (`0 4 * * 1`), run-module-suggestions (`0 5 * * *`), cobros-reminders (`*/15 * * * *`), verifactu-process (`*/1 * * * *` Disabled). **Drift detectado vs registry código**: el Dokploy viejo tenía 4 schedules distintos al `CRON_REGISTRY` canónico de `src/lib/cron/registry.ts` — corregidos al pasar al nuevo (regla: registry código es source of truth, no captura del viejo). 6 enabled, verifactu disabled hasta cutover prod. **Feature nueva — Run Now en `/admin/system?tab=crons`**: endpoint `/api/admin/crons/run` con requireAdmin que importa el handler dinámicamente (`await import('@/app/api/...')`) y lo invoca con Request sintético + `x-service-key`. **NO** hace fetch a sí mismo (anti-patrón documentado — falla DNS Docker interno; cometido y corregido en commit `591fd9b`). Botón ▶ por card con spinner + toast verde/rojo. Commits `c48c5cf` + `591fd9b`. **Aclaración importante**: tabla `cron_runs` Supabase es source of truth — captura disparos de Dokploy auto + Dokploy manual + admin Run Now. Dokploy log solo ve los suyos. **Learnings persistidos** en vault: [[dokploy-paste-compose-corruption]] + [[docker-compose-env-not-recreate]] + CLAUDE.md global 2 reglas Operativa (verify secret sin filtrar + cambios env 3 capas).
- 2026-05-17 01:00 · `[hecho]` · **Patch n8n delivery-status aplicado y env cableado** — script `ops/n8n-patches/apply-delivery-status.py` ejecutado con éxito sobre `pqSWkDIHqmSVHotB` (backup pre-patch `ops/n8n-backups/20260517-002928-receptor-pre-delivery-patch.json`, marker `delivery_status_patch_v1` verificado, 74 nodos). Envs cableados en container n8n (`185.47.13.170` puerto SSH `5251`): `FACTURAIA_SERVICE_KEY` ya estaba, `APP_URL=https://app.tufacturaia.com` añadido. **Lección aprendida — Dokploy compose Save bug**: al pegar el YAML completo en la pestaña Compose, Dokploy duplicó `FACTURAIA_SERVICE_KEY=${FACTURAIA_SERVICE_KEY}` en línea 30 en lugar de poner `APP_URL=${APP_URL}`. Tras varios Save/Deploy/Reload sin éxito (container con `APP_URL` vacío), tuve que (a) inspeccionar compose en disco (`/etc/dokploy/compose/n8nprod-n8nprod-mcqocs/code/docker-compose.yml`), (b) detectar la línea mal regenerada, (c) pedir al user reemplazarla manualmente, (d) Save + Deploy de nuevo, (e) `docker compose up -d --force-recreate n8n` por SSH para forzar recreate (Dokploy Deploy no recrea container si solo cambia un valor de variable referenciada). **Aviso seguridad**: una iteración intermedia filtró el valor de `FACTURAIA_SERVICE_KEY` en el chat (template Go demasiado permisivo) — rotación recomendada tras smoke final. **Pendiente smoke real**: enviar OTP a número propio → revisar rows con `status=delivered` en `whatsapp_delivery_log` Supabase.
- 2026-05-16 22:25 · `[hecho]` · **SSL tufacturaia.com desbloqueado** — Dani puso DNS-only en Cloudflare. Let's Encrypt emitió certs OK en `app.tufacturaia.com` (R13) y `n8n.tufacturaia.com` (R13). `/api/health` verde sobre HTTPS. **Pendiente menor**: root `tufacturaia.com` da 404 (falta redirect a `app.`), `www.tufacturaia.com` SSL verify error 20 (cert no cubre www) — ambos diferidos. Desbloquea: aplicar patch n8n delivery-status (necesita API key nueva del n8n.tufacturaia.com via Settings → API).
- 2026-05-13 · `[hecho 2026-05-16]` · **F3 — servidor nuevo tufacturaia.com operativo** — `185.47.13.170` Dokploy, proyecto `tufacturaia-prod`, servicio `facturaia-app` Docker Compose desde repo `AgentesIA-MAdrid/facturaia` branch `main`. App verde, `/api/health` → `{status:"ok", supabase.ok:true, latency_ms:110}`. SSL emitido tras DNS-only de Dani (ver entrada 2026-05-16 22:25).
- 2026-05-13 · `[hecho]` · **F1+F2 — refactor main + compose Redis + rate-limit Redis + n8n compose** — commits `7aabe38` (healthcheck, cron locks migration 074, CRON_BASE_URL, /api/health, middleware isServiceRoute) + `b7a8fbc` (rate-limit.ts async con ioredis + fallback in-memory, docker-compose Redis service, healthcheck curl, log rotation) + `bcb5743` (fix healthcheck wget→curl, depends_on no-blocking, ops/docker-compose-n8n.yml con healthcheck+pruning+memoria). Lint/typecheck/build limpios.

- 2026-05-12 · `[hecho]` · **P0 secundaria detectada y parchada — OCR Factura workflow tenía la misma key revocada hardcoded** — descubierto al hacer backup de los 7 workflows n8n (Fase 0.3): el workflow `zf2la2N2YBXKQNKk` (FacturaIA - OCR Factura) usaba la OpenAI key revocada esta mañana en literal hardcoded en el nodo "OpenAI Vision OCR". Tras la rotación de la mañana, **OCR vía `/ingesta` (drag&drop UI) y email polling estaban ROTOS silentes** (401 OpenAI) — pero el smoke test inicial pasó por WhatsApp/receptor v2 que tiene OCR inline propio con env var, así que no se detectó. Fix: script `ops/n8n-patches/apply-ocr-factura-env-key.py` idempotente. Cambia Authorization header de `Bearer sk-proj-...` literal a `Bearer {{$env.OPENAI_API_KEY}}` (expresión n8n). Aplicado vía PUT API n8n, verificado con grep marker post-PUT (cero literales sk-*). Backup pre-patch en `~/.facturaia-migration-secrets/2026-05-12-pre-flip/` (fuera del repo, chmod 600). **Pendiente smoke test usuario**: subir factura por `/ingesta` y verificar datos extraídos
- 2026-05-12 · `[hecho]` · **Fase 0.3 backups workflows n8n** — 6 de 7 workflows FacturaIA backupeados en `ops/migration/snapshot-2026-05-12-pre-flip/n8n-workflows/` (sin secretos). El 7º (`zf2la2N2YBXKQNKk` con literal sk-*) movido a `~/.facturaia-migration-secrets/` fuera del repo. CSVs de tablas BD saltados por redundancia con Supabase PITR
- 2026-05-12 · `[hecho]` · **Fase 0.5 auditoría URLs BD prod limpia** — 7 queries SQL ejecutadas en Supabase contra `module_metadata`, `system_config`, `webhook_endpoints`, `voice_prompt_versions`, `voice_chat_bindings`, `notifications`, `organizations.settings`. **Todas devuelven 0 rows**. Cero URLs hardcoded a `facturaia.agentesia.world` o `facturaia.com` en BD prod. No hay UPDATEs SQL a aplicar pre-flip. Fase 0.5 cerrada sin acción
- 2026-05-12 · `[hecho]` · **Fase 0.8 pre-flight checks** — lint + typecheck + tests Vitest + build los 4 verdes en `main`. Repo estable y listo para refactor de Fase 1
- 2026-05-12 · `[hecho]` · **Resend custom domain operativo + Supabase Custom SMTP cableado** — `tufacturaia.com` y `auth.tufacturaia.com` verificados en Resend EU (Frankfurt). **Plan Pro 20€/mes activado** (necesario para 2+ dominios; cubre 50.000 emails/mes). DNS añadido en IONOS: DKIM `resend._domainkey` + MX `send` (feedback-smtp.eu-west-1.amazonses.com prio 10) + SPF `send` tanto en root como auth subdomain. 2 smoke tests OK: desde `noreply@tufacturaia.com` (ID `fa41f746-382d-49d3-aafe-b1ba71966418`) y desde `noreply@auth.tufacturaia.com` (ID `a9bc870c-4814-42ba-9623-555183fce985`). 2 API keys creadas con scope restringido: `facturaia-app-transactional` (root domain) y `facturaia-supabase-auth` (auth subdomain). Supabase Auth → Custom SMTP cableado a `smtp.resend.com:465` con key auth. Magic link test funcional (llega desde `noreply@auth.tufacturaia.com` con DKIM válido). Block 0.7 de Fase 0 cerrado
- 2026-05-12 · `[bug-detectado]` · **Convertir presupuesto a factura por WhatsApp roto** — usuario reporta flujo end-to-end no funciona. Histórico: 2 fixes previos 2026-05-10 (totales 0€ + matching difuso typos Whisper). Diagnóstico pendiente: revisar execution logs n8n `zYcHHa8jWXB6dY5i`, grep marker prompt, curl directo a `/api/voice/convert-presupuesto`. Bloqueante Bloque 1
- 2026-05-12 · `[hecho]` · **Caos engineer audit + fixes finales módulo Cobros** — commit `671b890` pusheado y desplegado. Agente con mindset "humano adversarial intentando romper" encontró 13 issues, 8 reales aplicados. **🔴 2 críticos**: (1) regex STOP n8n era case-sensitive sin tildes → "stop"/"baja"/"NO MÁS" no matcheaban → RGPD breach silente. Patch script actualizado con clases `[Ss][Tt]...` + tilde `[ÁA]` + idempotent re-apply (detecta regex viejo y actualiza in-place). Aplicado al workflow productivo `zYcHHa8jWXB6dY5i` con marker re-verificado. (2) Doble envío real por `network_error` post-Meta-delivery — `failed` no bloqueaba UNIQUE → retry cron a los 15min → cliente recibía 2x. Fix migration 073: trigger BEFORE INSERT rechaza retry mismo (factura, nivel) durante 6h tras failed lanzando 23505 → orchestrator lo trata como `skipped:duplicate`. **🟠 4 altos**: (3) Admin stats panel mentía sobre pausadas tras migration 071 cambió shape boolean→objeto — helper `isPaused` con doble shape support; (4) Lost update concurrente pause-org (dos admins simultáneos perdían pausa) — migration 072 con RPCs `cobros_pause_org_set/clear` atómicas usando jsonb_set + operador `-`; (5) DELETE validate-phone dejaba telefono_e164 stale → re-validación usaba número viejo del cliente equivocado — DELETE limpia ambos campos, POST siempre recanonicaliza desde `telefono`; (6) Aging endpoint sin límite temporal en recordatorios — crecía linealmente, fix `.gte(attempted_at, 90d ago)`. **🟡 2 medios**: (7) Race opt-out vs send mid-flight — re-check `isClientOptedOut` justo antes del fetch Meta + marca como opted_out si llega tarde; (8) Bucket aging día 0 (vto=hoy) — alineado con cron `vto<CURRENT_DATE`: ahora `diasVencida <= 0` va a "por_vencer". **Falsos positivos descartados**: dedupe_key sin org_id (verificado manualmente: notify_upsert sí usa UNIQUE(org_id, dedupe_key)), regex escapeValue bytes 0x00-0x1F invisibles (correctos), quiet hours start==end (no security). 2 migrations nuevas aplicadas (072+073). 256 tests + typecheck + lint + build limpios.
- 2026-05-12 · `[hecho]` · **Módulo Agente de cobros completo end-to-end** — commit `b48d9cb` pusheado y desplegado en prod. Backend: 6 migrations 065-070 (cobros_recordatorios append-only con UNIQUE idempotency, trigger cancel-on-terminal, SQL function `cobros_pending_for_org` SECURITY INVOKER triple-check, notification_preferences + quiet hours org/user con tabla y dos funciones SQL, clientes_opt_out RGPD append-only + telefono_e164 + telefono_validado_at + CHECK config validation, cobros_envios_billing + cobros_plan_quotas editable desde admin con seed Starter=0/Pro=500/Enterprise=2000). Lib TS: 9 plantillas hardcoded (3 tonos × 3 niveles, RD 1619/2012 auditable) con render single-pass anti-recursión, E.164 canonicalizer con default ES, Meta Graph wrapper (template/text mode con fallback) + send-email TODO Resend gated, quota check con membership inside, opt-out check con DEFAULT-deny en error, orquestador con race condition handling (re-leer status tras INSERT, ON CONFLICT 23505 manejado como `skipped:duplicate`). Endpoints: cron `/api/internal/cobros-reminders` con withCronTracking + ventana hora_envio ±30min + quiet hours operativas + batch cap 500/run, manual `/api/cobros/send-now` con rate limit 5/min + 30/día + role check + cross-tenant guard + 404 (no 403) para no revelar existencia, webhook `/api/internal/cobros-stop` n8n-friendly + sin enumerar count cross-org, opt-out manual RGPD, preferences GET/PUT, snooze con IDOR guard, phone validation. Admin: `/api/admin/cobros/{stats,pause-org,quotas}` todos con audit log + frase tipeada "PAUSAR COBROS <ORG>" + auto-expira 7d. Workflow n8n receptor v2 parcheado con branch IF "Es STOP?" + HTTP "Notificar STOP a FacturaIA" + "Confirmar STOP al Cliente" — marker `cobros_stop_marker_v1` verificado post-PUT. UI: tab Configuración con preview en vivo (overrideTab para 'cobros' en ModuloDetalleModal) — steppers limpios "día/días", segmented control tono (suave/profesional/firme), Aging view en `/emitidas?view=aging` con KPI strip + 5 buckets segmented + tabla agrupada por cliente con details/summary + cards mobile, icono teléfono estático rojo/verde sin pulse con tooltip nativo, Modal "Enviar ahora" rediseñado con resumen factura + canal selector WhatsApp/Email (email Próximamente Resend, disabled) + tono + preview burbuja + Enviar. Drawer notif: chips filter single-select por categoría con contadores + menú ⋯ por notif con Snooze (1h/esta noche/mañana/lunes/semana) + Descartar + footer link `/settings?tab=notificaciones` + useSyncExternalStore singleton para tick estable sin loops infinitos. Settings: nueva sección "Mis preferencias" con quiet hours personales + días semana toggleable + canales con switches iOS + digest opt-in placeholder. Admin `/admin/system?tab=cobros` con stats cross-org + top 10 orgs + acciones pausar con frase tipeada. Admin `/admin/config?tab=cobros-pricing` con tabla editable plan_quotas + addon_quotas + Meta template config (name/language/body_variable_keys). Auditoría: 6 agentes paralelos (2 rondas planning UX/design × 1 backend auditor) → 1 P0 + 3 P1 + 2 P2 aplicados + falsos positivos descartados con razón. 40 tests Vitest nuevos (256 total). Migrations aplicadas a prod desde local con psql. **Conexiones verificadas end-to-end**: catalog keys ↔ validate_cobros_config SQL ↔ orchestrator ↔ UI; admin pause-org → system_config.cobros_org_paused → cobros_check_quota → cron salta; admin quotas → cobros_plan_quotas → cobros_check_quota lee tabla; workflow n8n STOP → /api/internal/cobros-stop → clientes_opt_out → orchestrator skip; phone validation UI → endpoint → BD; isServiceRoute middleware cubre /api/internal/*. **Patrones aprendidos**: useSyncExternalStore con `getSnapshot` debe devolver valor cacheado (no `Date.now()` directo) o loop infinito; Dokploy cron requiere shell `bash` + Command sin `bash -c` wrap + comillas DOBLES alrededor del header `-H "x-service-key: $VAR"`; SettingsView con `useState(getter)` + `if (typeof window === 'undefined')` causa hydration mismatch → usar `useSearchParams` + Suspense wrapper. **Mockup data seedeado** en AgentesiaLab (3 facturas con vto pasado para buckets aging, 2 clientes telefono_validado_at, 1 cobros_recordatorio sent, 3 module_events, 3 notifs variadas)
- 2026-05-12 · `[hecho]` · **P0 OpenAI key rotada** — verificado vía API n8n que prod ya usaba env var (`$env.OPENAI_API_KEY` en nodo "Disparar OCR" del workflow `zYcHHa8jWXB6dY5i`, hardening 2026-05-06). Key vieja revocada, nueva en env Dokploy n8n + restart, OCR smoke OK. Borrados 3 snapshots stale del repo: `whatsapp-receptor-v2.json`, `whatsapp-receptor-v1-backup.json`, `whatsapp-receptor.json`. Backups recientes en `ops/n8n-backups/` ya estaban limpios. Repo grep cero hits secrets. Desbloquea Fase 1 migración tufacturaia.com
- 2026-05-11 · `[empezado]` · **Plan migración tufacturaia.com + Dokploy nuevo** — 3 agentes paralelos auditaron infra/integraciones/seguridad-escalabilidad. 1 agente crítico segundo pase descubrió 17 gaps no obvios. P0 detectado: OpenAI key literal en `whatsapp-receptor-v2.json` (repo público). Plan 9 fases en [[facturaia-migracion-tufacturaia-com]] cubre: rotación P0, refactor en main pre-migración (fallbacks hardcoded, CRON_BASE_URL, cron locks distribuidos, healthcheck real, rate-limit Redis), diseño compose (n8n separado, Redis, pdf-renderer en red interna, GitHub auto-deploy), provisión Dokploy aislada, staging E2E + caos, flip aditivo con 13 pasos ordenados (incluye UPDATE SQL en BD para URLs hardcoded, CORS Storage, Meta/Retell/Google dashboards, desactivar schedules viejos), redirect 301 mes 1-3, apagado viejo mes 3+, hardening continuo. **Pendiente OK Manuel** en 4 decisiones de arquitectura
- 2026-05-11 · `[empezado]` · **Integraciones bancarias — análisis y spec** — confirmado estado actual: tabla `movimientos_bancarios` (043) + triggers bidireccionales 061 + módulo Conciliación con config schema. Gap = cero ingesta (tabla vacía en prod). Spec [[facturaia-open-banking-psd2]] reescrita con 3 opciones (Norma 43 / GoCardless BAD PSD2 / pasarelas cobro) + roadmap 6 fases + riesgos compliance/cifrado consent. Decisiones cerradas: empezar por N43 (coste 0, 80% valor), GoCardless BAD como primer agregador (no Tink/Plaid), pasarelas cobro como módulo aparte. Añadidos a NEXT: Norma 43 import + trigger simétrico emitidas→cobrada. LATER expandido: PSD2 + pasarelas
- 2026-05-11 · `[hecho]` · **Reorganización admin — sidebar 3 secciones + `/admin/system` tabs + `/admin/config` tabs + banner global + kill switch read-only** — 4 agentes paralelos (backend/frontend/security/product) debatieron plan, sintetizado y aplicado. Migración 064 `admin_audit_log` (RLS service-only) + reuso `system_config` con keys `maintenance_banner` y `read_only_mode`. Endpoints nuevos: `/api/admin/system/webhooks`, `/api/admin/config/{env-check,email,fiscal,maintenance}`, `/api/system/banner` (público). Kill switch en `with-api-auth.ts` y `handler.ts` v1 con bypass por rol superadmin + prefijos `/api/admin/system/*` y `/api/admin/config/*` (recovery path). Banner global en root layout, poll 60s + on-focus, dismiss per-session. UI: tabs componente accesible (`src/components/admin/ui/tabs.tsx`), 6 paneles config + 1 webhooks panel, redirects en next.config (`/admin/storage` → `/admin/system?tab=storage` y similar). Cross-link Features ↔ Planes con chip "N planes". 5 bugs detectados en autorevisión (columnas BD: `status_code` vs `http_status`, `vence_at` vs `expires_at`, `verifactu_enviada_at` vs `verifactu_fecha_envio`, `plans.id` es código no `plans.codigo`, regex range escapada mal). React 19 purity: `Date.now()` durante render → derivar booleano server-side. Lint/typecheck/build OK. Migración aplicada en prod. **Pendiente push + deploy + smoke tests**. **Diferidos por debate**: email_log Fase A (recomendado SÍ próxima sesión), AEAT health derivado de últimas N facturas (SÍ ~5min), comparador planes visual (NO, valor bajo), feature flags globales (NO, YAGNI)
- 2026-05-11 · `[hecho]` · **Observabilidad de crons + panel admin** — tabla `cron_runs` (063), `withCronTracking` wrapper en 6 crons, panel `/admin/system/crons` con semáforo + tooltips + ejemplos reales por cron. Notif `cron_failed_<name>` a campanita de org de control si falla. Registry con descripción human-friendly. Commits `9892e4c` + `1b14779` + `d4b7566` + `d43f80d`
- 2026-05-11 · `[hecho]` · **Bug destapado y arreglado** — `storage-quota-check` fallaba semanal en silencio por env `STORAGE_QUOTA_WARN_MB=""` → `Number("")=0` ≤ 0 → throw. Fix con helper `parseMb` (fallback robusto null/empty/non-numeric). Destapado al primer run de `cron_runs`. Commit `5255547`
- 2026-05-11 · `[hecho]` · **Módulos IA mejorados — OCR + Conciliación via triggers BD** — migrations 060/061: `respect_ocr_auto_categorizar` (BEFORE INSERT/UPDATE facturas) + `auto_mark_pagada_on_bank_match` (AFTER INSERT/UPDATE facturas + AFTER INSERT movimientos_bancarios). Triggers cubren todos los callers (n8n, ingesta-view, API v1, voice). Catálogo: `auto_categorizar` + `auto_marcar_cobradas` `implemented: true`. Commit `33d5c7c`
- 2026-05-11 · `[hecho]` · **Cashflow IA opts + cron Opción B** — `buildCashflowData` acepta `{horizonteDias, incluirRecurrentes}`, cableado en cashflow-view/dashboard-view/impersonate. Nuevo cron `/api/internal/cashflow-alerts` (Dokploy `30 7 * * *`, chunks 10 + Promise.all, dedupe 24h, gate orgHasFeature). Commit `164dd9a`
- 2026-05-11 · `[hecho]` · **Copiloto — gate + idioma + persistencia historial** — gate `orgHasFeature('copiloto')` en `/api/ai-assistant` (cierra fuga billing preexistente), Topbar oculta botón sin feature, `AIAssistantHost` gatekeep evento `open-ai-assistant`. System prompt en `{idioma}`. Tabla `copiloto_conversaciones` (062, RLS por user_id) + thread_id desde cliente. Commit `19ee00f`
- 2026-05-11 · `[hecho]` · **ModuloConfig UI fixes** — toggles respetan `f.default` cuando config vacío (afectaba auto_categorizar, auto_marcar_cobradas, incluir_recurrentes, guardar_historial — se mostraban "Desactivado" la primera vez). Error handling con toasts en `save()`. Commit `3fefc3a`
- 2026-05-11 · `[hecho]` · **6 agentes paralelos auditaron cambios** (2 rondas × 3 expertise: security/backend/frontend) — 1 P0 + 3 P1 + 2 P2 reales aplicados, ~50% falsos positivos descartados con razón documentada
- 2026-05-10 · `[empezado]` · **Sistema comunicaciones email — Resend** — Spec consolidada tras review de 5 agentes paralelos (codebase, security, architecture, UX product, UI consistency). 5 PRs secuenciales reemplazan plan original 60d. Reutiliza `outbox_events` + `notifications` + `ai_module_suggestions` (NO duplica). Auth via Supabase + Custom SMTP Resend en dominio separado `auth.facturaia.agentesia.world`. Spec completa: [[facturaia-comunicaciones-emails]]. **Pendiente: Manuel hace setup Resend (2 dominios + Cloudflare DNS + Supabase SMTP + Dokploy env) antes de PR 1**
- 2026-05-10 · `[hecho]` · Hub maestro FacturaIA creado con NOW/Smoke/WIP/NEXT/LATER/Ideas/Decisiones/Stack/Manuales/Specs/Workflows/Histórico · commit `922ccea` vault
- 2026-05-10 · `[hecho]` · Sección Progreso en vivo añadida al hub para tracking de conflictos · commit pendiente vault
- 2026-05-10 · `[hecho]` · Hub poblado desde Slack canvas Panel FacturaIA — Bloque 1 generar-view, smoke tests Bloque 1, mobile responsive, sub-workflows voz a archivar, sección Seguridad (3 prio + 3 hardening), 9 features IA en NEXT, 8 features producto en LATER, suite E2E Playwright al histórico · commit pendiente vault
- 2026-05-10 · `[hecho]` · Manuales actualizados con UX real del módulo voz — manual-usuario sección "Convertir presupuesto" reescrita con 4 escenarios (1 match / candidatos numerados / fallback típos Whisper / ya facturado) + manual-admin sección 23.12 con root cause + fix técnico. Commit `4e98d76`
- 2026-05-10 · `[hecho]` · **Fix UX matching presupuestos** (E2E real reveló): resumen 0€ por iterar `data.lineas` que el agente no devuelve para `convertir_presupuesto` → branch en `Parsear y Calcular Totales` con lookup HTTP a Supabase. Y system prompt mejorado: matching permisivo (acentos/diminutivos/typos Whisper), lista candidatos numerados en >1 match, fallback "últimos 5 abiertos" en 0 matches, memoria conversacional para que el usuario responda "1"/"2". Commits `5cf48a3` + `6d41b9b`. Backups en ops/n8n-backups/
- 2026-05-10 · `[hecho]` · **F1+F2 voz + híbrido + workflow patched + verificación exhaustiva** — 12 commits: endpoints find/convert + UI variables/avanzado + métricas drill-down + admin /admin/voice + binding chat_id↔org_id + pg_cron purge + endpoint internal voice-system-prompt + endpoint voice-chat-bindings + workflow patched (consultar_presupuestos toolCode + branch convert post-confirm) + 5 bugs hardening (PGRST203, feature_gate, iva_pct min/max, invalid_json 400, ocr-audit FK 404). 101 tests E2E reales contra Supabase prod, 60/60 unit, lint/typecheck/build OK. **Pendiente push + deploy + activar generador_voz por org + phone en profiles antes de E2E con Meta**

---

## NEXT (próximas 2 semanas)

- **Multi-org real para usuarios con varias membresías** — detectado 2026-05-20 durante smoke cobros. Mi user (`m.delmonte.p@agentesia.madrid`) es propietario de `FacturaIA Sandbox` (is_test=true) Y `AgentesiaLab SL` (is_test=false) con `org_members.estado='activo'` en ambas. Bug arquitectónico en `src/lib/auth/session.ts:16-22`: `getSessionActor` hace `.from('org_members').eq('user_id', user.id).limit(1).single()` sin orden ni concepto de "org activa" → Postgres devuelve siempre la misma arbitrariamente (sandbox en mi caso), y no hay UI switcher porque no hay backend para soportarlo. La impersonación superadmin (`?org_id=`) es read-only por diseño (middleware borra cookie en POST/PATCH). **Plan**: (1) columna `profiles.active_org_id` o tabla `user_active_org`; (2) endpoint `POST /api/auth/switch-org` que valida membresía activa + setea `active_org_id`; (3) `getSessionActor` lee `active_org_id` y cae a la primera membresía si NULL; (4) componente `OrgSwitcher` en `src/components/layout/sidebar.tsx` listando memberships con check en la activa; (5) tests E2E del switch. ~1-2 días dev. Workaround temporal mientras: pausar membresía no deseada con `UPDATE org_members SET estado='pausa'`. Spec: pendiente.
- **Centro fiscal IA (módulo Pro add-on)** — análisis completo + plan congelado 2026-05-19 tras revisión de 4 agentes (arquitectura/legal/producto/research AEAT). Cubre 303/390/130/111/115/180/190/347/349 + libro IVA XLSX + export asesor A3/Holded. v1 cuadre+export (NO presenta), v3 presenta como colaborador social RD 1065/2007 (gestión convenio AEAT a arrancar en paralelo). Naming "Centro fiscal IA" en v1, "Agente fiscal" reservado v3. Add-on comprable sobre Pro (`addon_purchasable=true`, ~9 €/mes a validar). **Mig nueva** (número que toque al arrancar, repo en 104 hoy) crea `fiscal_declaraciones` + `fiscal_declaracion_snapshot` (céntimos enteros, no float — anti-drift IVA mixto) + `fiscal_plazos` (seed 2026 con festivos) + `fiscal_avisos`, RLS por org+rol contable/admin/propietario, calculador versionado `src/lib/fiscal/calculadores/v1/...` periodificado por COALESCE(fecha_operacion, fecha) (mig 087), RPC con `pg_advisory_xact_lock` anti-race A-fiscal análoga al cierre. **Sprint 1 incluye WORM Object Lock** (S3 Compliance o Backblaze B2; Supabase Storage NO sirve, service-role puede borrar) **+ sello tiempo eIDAS FNMT** (~0,01-0,05€/sello, valor probatorio art 42 UE 910/2014). 6 sprints + beta privada 2 trimestres + auditoría fiscal externa ~2k€ pre-GA. **Decisiones pendientes al arrancar**: precio exacto, proveedor WORM, proveedor sello, asesoría auditora. Spec completa: [[facturaia-centro-fiscal-ia]]
- **Migración a `tufacturaia.com` — pendientes menores** — F1+F2+F3 completos (2026-05-13), SSL Let's Encrypt OK desde 2026-05-16, 7 crons configurados, mig 074 aplicada, smoke OTP cubierto via Phone State Guard v2 (exec 165+169). **Pendiente sin urgencia**: `tufacturaia.com` raíz responde 404 (falta redirect a `app.tufacturaia.com`), `www.tufacturaia.com` sin cubrir en cert (decidir si se añade). Spec: [[facturaia-migracion-tufacturaia-com]]
- ~~**n8n Phone State Guard — rediseñar patch para workflow real**~~ ✅ **HECHO 2026-05-17** (commits `0c06e36` docs + `f8936a0` feat). Patch reescrito anclando en `Parsear Mensaje` (no en el "Resolve Context" inexistente). Phone State Check implementado como **Code node** (no httpRequest) tras revisión de 3 agentes con perspectivas distintas (técnica n8n / seguridad / arquitectura): el HTTP node reemplaza el item y rompe downstream que lee `$json.X` de Parsear — el Code node devuelve `{ ...parsed, phone_change_match, phone_change_meta }` propagando todos los campos via spread (zero hardcode, future-proof). Patrón idéntico a `Buscar Org Voz`. Anti-enum: `pending_change_new` y `unknown` reciben copy byte-idéntico (mismo string literal en ternario), solo `pending_change_old` distinto. Fail-open por diseño con auditabilidad: en error devuelve `phone_change_check_error` truncado 120 chars (sin headers/key/url). E2E live OK: caso A verified (exec 165), caso B pending_change_old (exec 169 con copy literal en WhatsApp), caso C unknown probado estructural. Runbook: [[docs/runbooks/phone-state-guard-v2-deploy.md]].
- ~~**Auditoría panel /settings?tab=auditoria — extender**~~ ✅ **HECHO 2026-05-17** (3 commits: `d9cad05` base + `2548259` polish UI + `c982876` docs manuales). Nuevo endpoint `GET /api/settings/auditoria` que mergea `audit_log` (scoped org_id) + `phone_otp_audit` (scoped user_id propio), con masking server-side reusando `src/lib/auth/phone-mask.ts` (single source of truth). **Modelo mental unificado tras debate con 4 agentes** (contable/forense/UX/backend): el "Quién" SIEMPRE es persona humana, NUNCA el agente IA. "Tú" cuando viewer es el actor (OTP, web, o agente disparado por mí). Nombre completo si `full_name`, si no parte local del email capitalizada (nunca email crudo). API key resuelve a `api_keys.name`. Agente con caller_user_id → nombre del humano. Agente sin info → "Sin identificar" (gris). UI completa: tabla desktop 4 cols / cards apiladas <720px, **expand row inline** al click (chevron animado, aria-expanded, ESC cierra, copy UUID con feedback), número humano del documento (FAC-2026/0042) protagonista del detalle, filtros nuevos por categoría server-side (Documentos/Mi cuenta/Equipo/Agentes IA), filtrado de UUIDs internos en diffs, keys humanizadas, teléfono inline con "vía Voz · +34 6** *** 89 · agente IA". 63/63 vitest verdes. Lint/typecheck/build limpios. Manuales actualizados (§30 admin + §7 reescrita usuario). **Iteración B diferida**: impersonación visible en feed cliente, lookup nombre proveedor por email entrante, export CSV gateado por rol, backfill caller_* histórico descartado por riesgo legal.
- ~~⭐ WhatsApp convertir presupuesto a factura por intención~~ ✅ **HECHO 2026-05-10** (commits + workflow patched, falta push/deploy + E2E real con Meta)
- ~~⭐ Admin módulo Voz/WhatsApp~~ ✅ **HECHO 2026-05-10** (`/admin/voice` con tabla overrides + métricas drill-down + sistema híbrido prompt variables/avanzado)
- **Admin observabilidad errores bot WhatsApp cross-org** — detectado 2026-05-18 01:00 durante smoke conversión presupuesto→factura. Hoy cuando el bot responde "Hubo un error al generar...", "Esta función no está activa en tu plan", "No tengo el ID del presupuesto..." etc., el dueño de la org NO ve nada en su panel; el superadmin solo si entra a n8n executions y descifra el JSON. Necesitamos panel `/admin/system?tab=bot-errors` (o tab nuevo en `/admin/system?tab=cobros` ampliado a "Bot WhatsApp") que: (1) liste **fallos del bot por org** con tipo de error categorizado (feature_missing, no_match, validation_error, backend_error, llm_error); (2) detalle con: org, user_id resuelto, phone enmascarado, timestamp, transcripción Whisper si era audio, tool calls del agente, response del backend, mensaje enviado al user final; (3) filtros: tipo, org, periodo; (4) acción "marcar revisado" para descartar errores ya analizados; (5) diferencia "problema real" (feature_missing en plan que sí debería tenerlo, backend caído) vs "uso equivocado del cliente" (phone no registrado, intent no soportada). **Fuente de datos**: actualmente NO se persiste — el bot solo envía respuesta WhatsApp pero no logea estructurado. Necesitamos tabla nueva `bot_error_log` (org_id, user_id, phone_masked, error_kind, error_payload jsonb, raw_transcription, ts, reviewed_at, reviewed_by) poblada por (a) endpoint nuevo `/api/internal/bot-error-log` que llaman los nodos n8n `Responder *` antes de enviar el mensaje al user, o (b) post-hoc desde n8n executions API parseando logs cada N minutos. Opción (a) más directa, opción (b) cubre histórico sin tocar workflow. Mejor: ambas — (a) para nuevos, (b) backfill 7d inicial. ~2 días dev + migration + UI + endpoint + cableado n8n. **Valor alto operativa**: hoy operamos a ciegas. Spec: pendiente.
- ~~**🔴 P0 — Eliminar concepto "factura borrador"**~~ ✅ **DECISIÓN TOMADA 2026-05-19**: en lugar de eliminar, se adopta modelo **Holded-style** (num=NULL hasta emisión). Implementado y en prod. Ver Progreso 2026-05-19. — feedback Manu 2026-05-18 smoke A. En España una factura ya emitida es factura fiscal (RD 1619/2012): tiene número correlativo + fecha de emisión + obligación tributaria. El estado `borrador` para facturas es invención de producto, no fiscal — confunde y permite estados "limbo" que no deberían existir. **Decisión**: facturas NACEN en estado emitido. Lo que hoy es "borrador" desaparece. La revisión previa se hace ANTES de pulsar "Emitir factura" (en el form, no creando una row en BD). Para presupuestos sí sigue habiendo borrador (no son docs fiscales). **Alcance amplio**: (a) RPC `convertir_presupuesto_a_factura` mig 082 — cambiar INSERT factura `estado='borrador'` → `estado='pendiente'` (o el estado emitido canónico); (b) `src/lib/documents/create-document.ts` — todas las creaciones (manual + voz + email + API + OCR) emiten directo; (c) `src/lib/documents/emitir-borrador.ts` + endpoint `/api/v1/facturas/[id]/emitir` → eliminados o marcados deprecated; (d) `src/components/generar/generar-view.tsx` 3 botones ("Borrador / Emitir / Emitir pendiente") → 1 botón "Emitir factura" + confirmación clara; (e) UI permisos por estado — eliminar/editar SOLO mediante "Anular" (RD 1619/2012); (f) Verifactu cron — facturas emitidas se disparan inmediato a AEAT (org con verifactu_activo); (g) audit_log + Verifactu trigger BEFORE INSERT siguen igual; (h) migración de datos: facturas históricas en estado=borrador → análisis manual (borrar las que nunca se emitieron, emitir las que sí). **Pre-trabajo**: relevamiento de TODOS los callsites que hoy crean borradores + plan de migración de filas históricas. ~2-3 días dev + tests + migración + manuales. Riesgo medio (toca cero-trust del modelo de dominio). **Decisión arquitectónica documentada en ADR pendiente** `decisions/ADR-NNN-no-borrador-facturas.md`.
- ~~**Verifactu: validación asistida AEAT falla "No se pudo ejecutar la validación asistida"**~~ ✅ **HECHO 2026-05-18** — commit `e452b4f`. (1) Cache LRU por hash(payload) sha256 truncado 16ch, max 200 entries, TTL 5min éxito / 30s error transitorio / nunca auth_error → reintentos idénticos no martillean al provider. (2) `categorizeLlmError()` bucketiza en kinds (timeout / rate_limit / auth_error / provider_unreachable / unknown). (3) `errorIssueFor()` mensajes específicos por kind con sugerencia accionable; el user sabe si esperar 30s, avisar soporte, o que provider está caído. Todos los mensajes recuerdan "la emisión sigue siendo posible sin la pre-validación". Hardening anti-injection y cálculo determinista de coherencia base×IVA intactos. Lint+typecheck+build verdes.
- ~~**Bot WhatsApp — listar TODOS los candidatos cuando hay varios**~~ ✅ **HECHO 2026-05-18** — refactor state machine bot (10+ commits ese día: `d966556` state machine, `1195bf6` persist last_listado cuando ambiguous, `78c809c` slot resolver determinista, `3d7bd27` robustness v5 con slot resolver 8 patrones + tool fallback + prompt blindado, `2a9692f` handler nif_required). System prompt actual en `pqSWkDIHqmSVHotB` (actualizado 2026-05-18 14:06) tiene CASO 2 exhaustivo: "N>=2 presupuestos matchean → SIEMPRE lista TODOS (máx 5) → NUNCA elijas tú el más reciente sin preguntar" + CASO 4 con resolución por número usando memoria conversacional + REGLA EXTENDIDA `candidates` raw para persistencia + 2 ejemplos (mismo cliente / clientes distintos) + v4 persist `previous_state.resolved_slot`.
- **Multi-org selector para bot WhatsApp (resolución determinista user→org)** — detectado 2026-05-18 00:30 durante smoke E2E post-fix URLs. Hoy `Buscar Org Voz` resuelve `phone → user_id → org_members[0]` sin orden ni filtro de feature, retorna una org arbitraria → bot revienta si esa org no tiene el feature requerido (`generador_voz`, etc.). Caso real: user `m.delmonte.p@gmail.com` apuntando a `Empresa TEST` (sin voz) en vez de `agentesia.madrid` (con voz). **Diseño limpio**: regla "1 phone → 1 user único → N orgs". Pasos: (1) extender `/api/internal/whatsapp/resolve-context` para devolver `orgs[]` con `{id, nombre, plan, features_activos}` filtradas por features candidatos; (2) `Buscar Org Voz` cambia a HTTP node llamando ese endpoint; (3) si `len(orgs_con_feature)==1` continúa; (4) si `len>=2` el bot envía mensaje WhatsApp con botones (max 3) "¿Para qué empresa? 1. X, 2. Y, 3. Z" + estado conversacional nuevo (`chat_session.pending_org_selection`); (5) reply del user resuelve `chat_session.selected_org_id` con TTL N min para no preguntar cada mensaje; (6) si `len==0` responde "ninguna de tus empresas tiene este servicio". **Soluciona también el feature gating por plan**: Starter sin voz + Enterprise con voz convive limpio. Ojo si user tiene >3 orgs candidatas: WhatsApp solo permite 3 botones, fallback a lista numerada por texto. ~2-3 días dev + n8n + tests. Riesgo medio (toca el flujo más crítico del bot). **Pre-requisito barato**: limpieza duplicados `profiles.phone` legacy (UNIQUE parcial solo cubre `phone_e164 WHERE verified`, el legacy `phone` permite duplicados → arreglar in-place caso a caso). Spec: pendiente de crear en `knowledge/projects/agentesia/facturaia-bot-multi-org-selector.md`.
- **Admin Email Logs unificado (tabla `email_outbox` propia + webhooks Resend)** — detectado 2026-05-17 23:30 durante diagnóstico signup que no llegó a crear user (`auth.users` sin row). Necesitamos visibilidad operativa: ver qué emails se han disparado, su estado (sent/delivered/bounced/complained/opened), poder reenviar 1-click desde admin, correlacionar fallos. **Diseño**: (1) tabla `email_outbox` (id, kind, recipient, subject, status, resend_id, error, created_at, delivered_at) con RLS service-role + view admin cross-org; (2) wrapper `src/lib/email/send.ts` que INSERT primero, llama Resend, actualiza `resend_id` y `status='sent'`; (3) endpoint webhook Resend `/api/internal/resend-webhook` con HMAC verify que actualiza status según evento (`email.delivered|bounced|complained|opened|clicked`); (4) UI `/admin/system?tab=email-logs` con filtros (kind, status, periodo, recipient), columnas básicas + drawer detalle con headers/payload, botón "Reenviar" que dispara nuevo send con `parent_outbox_id`. **Cubre emails de app FacturaIA** (OTP fallback, notif colaboradores phone change, factura enviada, recordatorios cobros, etc.). **NO cubre Supabase Auth emails** (signup confirm, reset password) — esos van directo Supabase→Resend sin pasar por nuestro backend; para esos correlacionar con Resend dashboard externo o, mejor v2, suscribirse a webhooks Resend tenant-wide y enriquecer outbox post-hoc por `recipient + timestamp`. ~1-2 días puros. Apunta también `email_event_log` audit append-only 6 años (cumplimiento RGPD trazabilidad comunicaciones). Spec base reutilizable: [[facturaia-comunicaciones-emails]] §outbox. **Importante para diagnóstico operativa**, no diferir.
- **Sistema comunicaciones email (Resend)** — 5 PRs secuenciales: mailer core + auth via Supabase SMTP, outbox unificado + 8 plantillas, notifications multi-canal, módulo + UI cliente, admin UI + custom domain + 3 IA. Spec: [[facturaia-comunicaciones-emails]]. Bloqueado por setup Resend manual (cuenta + 2 dominios + DNS + Supabase SMTP + Dokploy env). ~3 semanas total
- **Stripe en activación add-ons** — hoy CTA "+XX€/mes" redirige a `/settings?tab=plan` sin cobro. Conectar checkout para que toggle = compra. Conciliación 19€ y Anti-fraude 9€ ya seedeados
- ~~**Cobros backend**~~ ✅ **HECHO 2026-05-12** (commit `b48d9cb`, endpoint cron operativo en prod, falta plantilla Meta aprobada + envs META_WHATSAPP_* para envíos reales)
- **Manuales actualizar Bloque 1** — `manual-usuario.md` y `manual-admin.md` describen flujo viejo: 2 botones generar (no 3), no mencionan anular ni tab Abonos ni nuevos pills/badges
- **Abono ligado a factura real** — nueva tool n8n `consultar_facturas_recientes` para que el agente confirme "¿te refieres a F2026-0042 a Tecnocloud por 121€?" antes de generar abono. Liga vía `factura_origen_id`. ~1 día
- **Post-confirm "añadir al catálogo"** — tras crear documento, si hubo productos marcados con ✨ (no estaban en catálogo), enviar mensaje extra con botones "Sí, añadir / No". Endpoint nuevo `/api/voice/learn-product`. ~1 día
- **Cobrador inteligente** — job nocturno detecta facturas vencidas y envía WhatsApp personalizado al cliente del emisor sin intervención. Reutiliza agente WhatsApp existente. Config por org: días de gracia, tono, canales. ~1-2 días. **Impacto alto** — mayor dolor de autónomos/PYMEs
- **Alertas anomalías facturas recibidas** — al ingestar factura de proveedor conocido, comparar importe contra histórico. Si desviación >X% → "Esta factura de Vodafone es 40% más alta que la media". Coste: query histórico en pipeline OCR. ~medio día
- **OCR conversacional bidireccional** — cuando OCR no encuentra campo crítico (NIF, total, fecha), agente pregunta por WhatsApp en vez de dejar fila incompleta. Reutiliza agente conversacional. ~1 día
- **Auto-categorización de gastos** — clasificar facturas recibidas leyendo histórico del proveedor: "facturas de Vodafone siempre van a Telefonía". Pedir confirmación solo cuando hay duda. ~1-2 días
- **Centralizar OCR en un solo workflow n8n** — hoy hay 2 paths: receptor v2 (inline OCR para WhatsApp) y zf2 OCR Factura (HTTP webhook para `/ingesta` upload + email polling). Refactor: que `/api/upload` y `process-attachments` llamen a un único endpoint o consoliden la lógica OCR en un sub-workflow compartido. Reduce deuda + futuras rotaciones de key se hacen en 1 sitio. ~medio día. Apuntado tras incidente 2026-05-12 (key revocada rompió zf2 silente mientras receptor v2 seguía OK)
- **Per-org email domains (Resend Fase 2)** — cada org verifica su propio dominio en Resend para que emails salgan desde `@<su-dominio.com>` con DKIM real del cliente (no `via FacturaIA`). Stack: tabla `org_email_domains` (RLS por org) + 4 endpoints API + UI `/settings/emails` con form + DNS records copiables + cron daily re-verificar pending + lógica envío con fallback a `facturaia.com`. ~4-5 días puros. Feature del plan Pro/Enterprise (justifica add-on). Diferido hasta que un cliente lo pida explícitamente — aditivo, no rompe nada migrando después. Spec base: [[facturaia-comunicaciones-emails]]
- **Diagnóstico inteligente rechazos AEAT** — hoy errores AEAT llegan como código técnico. Mejorar para que IA explique en lenguaje llano qué falló y cómo corregir, en el modal de la factura. Reutiliza `ai-validate.ts`. ~horas
- **Predicción de cashflow** — con historial de facturas + patrones de cobro por cliente (quién paga 30d, quién 90d), proyectar cashflow del próximo mes con desglose confirmado vs en riesgo. Datos ya en BD. ~2-3 días
- ~~**Import Norma 43 / CSV bancario para conciliación**~~ → **INCLUIDO en PR-A1 ampliado (CSV/MT940/OFX)** — ver NOW + [[facturaia-conciliacion-copiloto-spec]]. Mantenemos N43 como formato adicional opcional al parser. Trigger 061 reescrito en mig 103 para crear sugerencias en vez de tocar `facturas.estado` directo.
- ~~**Trigger simétrico emitidas → cobrada**~~ ✅ **HECHO 2026-05-19** — mig 101 ya en prod (auto_mark_cobradas_on_movimiento_emitidas). Será refactorizado en mig 103 del nuevo plan para unificar path por RPC `confirmar_sugerencia`. Ver [[facturaia-conciliacion-copiloto-spec]] §19.
- **API v1 — UI interna `descripcion` en formularios crear/editar factura+presupuesto** — backend ya soporta `descripcion` por línea (PR #49) pero la UI de `/generar` no la expone. Añadir `<textarea>` opcional debajo del input de concepto en `generar-view.tsx` + modales edit factura/presupuesto. Máx 2000 chars, contador visual al pasar 1500. ~medio día. Coherencia: si entra por API y se ve en PDF, debe poder editarse en UI también.
- **API v1 — `descripcion` en webhook payload `factura.created`/`presupuesto.created`** — actualmente el payload (mig 025) solo incluye `id, num, tipo_documento, total, estado, fecha` sin líneas. Si Borja u otro consumidor quiere `descripcion` post-creación sin tener que hacer GET, añadir `lineas[]` al payload + bump versión consumer. **NO incluído en PR #49** (Borja no lo pidió). ~horas.
- **Limpiar overload viejo 2-arg de `convertir_presupuesto_a_factura`** — deuda pre-existente detectada durante smoke mig 119 (2026-05-20): hay 2 firmas convivientes en BD (`(uuid, uuid)` y `(uuid, uuid, text, uuid, uuid)`). Código solo llama a la de 5 args. Pre-grep callers + `DROP FUNCTION public.convertir_presupuesto_a_factura(uuid, uuid)`. Trivial, ~minutos.
- **Conciliación #44 — PR-A2.1 parser num factura en descripción banco** — regex `\b[ABFP]\d{4}-\d{4}\b` sobre `mb.descripcion`, lookup en `facturas.num` de la org, boost score sugerencia +0.2 + motivo `match_num_en_descripcion`. Caso real detectado en audit 2026-05-20: mov mencionaba A2026-0006 pero sugerencia apuntaba a A2026-0003 (importe igual). Confidence='alta' al match correcto con parser. ~horas. Detalle: [[facturaia-conciliacion-roadmap]] §Fase siguiente.
- **Conciliación #46 — F4 detección reembolso bidireccional** — F4 actual dispara solo desde mov negativo. Si devolución entra ANTES del cobro (import histórico desordenado, duplicate-import), F4 no dispara. Fix: trigger simétrico desde positivo nuevo → buscar mov negativo pendiente con es_devolucion=null y marcar preventiva. ~horas-1día. Edge case, no urgente. Detalle: [[facturaia-conciliacion-roadmap]] §Fase siguiente + §Deuda técnica.
- **Versionado de defaults módulos** — cambiar default global no debe sorprender a orgs sin override (snapshot del valor previo o aviso visible)
- **Log de cambios de config por org** — audit trail granular para reconstruir "quién cambió qué config de qué org cuándo"

## LATER (backlog)

- **Backends módulos pendientes** — Fiscal **promovido a NEXT con spec completa [[facturaia-centro-fiscal-ia]]**. Pendientes aún en LATER: Firma eIDAS, Cashflow IA forecast. ~21 opciones config con badge Próximamente
- **Conexión bancaria automática PSD2 (GoCardless BAD)** — paso 4 del roadmap, tras Norma 43 + trigger emitidas. Tabla `bank_consents` cifrada, endpoints connect/callback/sync, cron 4-6h, dedupe por `provider_transaction_id`, notif `bank_consent_expiring` 7d antes. Empezar con sandbox + 1 banco real (BBVA o Santander). 1-2 semanas. Reutiliza `movimientos_bancarios` y triggers 061 tal cual. Spec: [[facturaia-open-banking-psd2]]
- **Pasarelas de cobro (Stripe Invoice + Redsys)** — módulo aparte, no conciliación. Webhook `invoice.paid` → match por `metadata.factura_id` → estado cobrada sin pasar por extracto. Cablear cuando Stripe checkout para add-ons esté en NEXT. Spec: [[facturaia-open-banking-psd2]]
- **Asistente IA multi-canal** — copiloto WhatsApp para consultas (vencidas, resúmenes, cobrador, predicción cashflow, auto-categorización, alertas, presupuestos por contexto, informe fiscal). Spec: [[facturaia-bloque-4-agent-query-spec]]
- **Google OAuth email por org** — cada org envía desde su email vía Gmail API
- **Canales Ingesta + Plan/Facturación** (spec 2026-04-24) — rediseño canales sin toggles, página planes reales con método pago e historial
- **Conciliación bancaria IA** (spec 2026-04-21) — 5 tablas, pipeline Claude 2 fases, UI aprobación por lotes
- **Cleanup mockup AgentesiaLab** — plan SQL elaborado. Org `ea201784-...`
- **Multi-WhatsApp números por org** — Tecnocloud necesita su phone_number_id Meta. Spec: [[facturaia-arquitectura-multi-whatsapp-numbers]]
- **Exportación contable** — formato compatible con gestorías (A3, Sage, etc.)
- **App iOS sin Swift** — Expo Go / React Native, mínima para consultar facturas recibidas/emitidas/bandeja IA. Gestión compleja → desktop. Valorar dificultad vs nativa Swift
- **Control de stock**
- **Enlace contable** para programas de contabilidad
- **Entrada y salida de caja**
- **Liquidaciones de facturas recibidas**
- **Facturación recurrente con SEPA**
- **Dashboard financiero avanzado** con predicciones cashflow basadas en patrones históricos

---

## Ideas crudas / inbox

_(volcado sin filtrar — pasan a NEXT/LATER si maduran, o se descartan en poda quincenal)_

- **Email_log Fase A** (recomendado próxima sesión, ~30min) — tabla `email_log` con insert en `send-factura`/`send-presupuesto`/`notify-quota`. Endpoint `/api/admin/config/email` devuelve último envío + counts 24h/7d. Sin webhook Resend de bounces todavía (Fase B siguiente). Hoy panel Email muestra "No configurado" + "Sin tracking en v1" — bloquea visibilidad cuando se configure SMTP
- **AEAT health derivado de últimas N facturas** (~5min) — cambiar `/api/admin/config/fiscal` para que el semáforo del panel se calcule desde las últimas 5-10 facturas enviadas (todas fallaron = rojo, alguna falla = ámbar). Sin ping a AEAT (lento + bloqueable). Más reactivo que tasa absoluta 30d
- **Exportar matriz planes a CSV** (~15min) — botón en `/admin/plans` que devuelve CSV plan↔feature para mandar a Borja o preparar pricing page. Alternativa al comparador visual (descartado por baja utilidad)
- **Test e2e Playwright middleware activo** (~1-2h, P0 antes de la próxima refactor del middleware) — disparado por incidente 2026-05-20 (middleware off 3 semanas tras rename `middleware.ts → proxy.ts`). Test: arranca server, hace request a `/?impersonate=ea201784-...` (con sesión superadmin mock), verifica que la cookie `impersonate_org` se set en la respuesta; navega después a `/` sin query param, verifica que la cookie se borra. Si alguien revierte o rompe el middleware otra vez, este test cae en CI. Coste casi cero, prevención fortísima. Considerar también un smoke test similar para `phone-verified-gate` (request a `/dashboard` con user sin teléfono verificado → debe redirect a `/verificar-telefono`).
- **Auditoría general "lo crítico está cubierto por capa N" tras restore middleware** (~1h, medio P1) — el incidente reveló que el rename del middleware no se notó porque otras capas (admin layout, dashboard layout) cubrían lo más visible. Listar conscientemente qué responsabilidades viven en cada capa (middleware vs layout vs handler vs RLS) y dónde una sola capa basta vs cuándo hay defensa en profundidad. Salida: una matriz en `Stack/facturaia.md` o `decisions/ADR-NNN-defense-in-depth-layers.md`. Evita el "no sabíamos qué se rompió" del próximo refactor crítico.
- **Banner "Estás impersonando X" más visible** (~30min, P2 UX) — actualmente el `ImpersonateBanner` es pequeño arriba del dashboard. Tras el incidente queda claro que es estado fácil de olvidar. Considerar: (a) hacerlo sticky en topbar, no solo arriba del contenido; (b) cambiar fondo del topbar a un naranja/ámbar suave mientras esté activa; (c) registrar evento `admin_impersonation_active >24h` para que aparezca alerta en el panel admin (¿se quedó pegada?). Bajo riesgo, mejora UX considerable.
- **Refactorizar `extractError` en el portal a un helper compartido** (~1h, P2 calidad) — el patrón "FacturaIA me devolvió `{error:{type,message,param,request_id}}` y quiero `message`" lo arreglé hoy en 3 sitios distintos (`auto-emit.ts`, `pdf/[shadowId]/route.ts` portal + el handler manual fetch). Extraer a `lib/facturaia/extract-error.ts` con tests + reemplazar las 3 copias. Evita drift entre handlers.
- **Test e2e PDF binario vs JSON{url}** (~1h, P2 robustez) — el cambio de FacturaIA (PDF binario en lugar de JSON{url}) se detectó por user en producción, no en CI. Añadir test e2e en portal que (a) genera factura mock; (b) hace GET `/api/facturaia/pdf/[shadowId]`; (c) asserta `Content-Type: application/pdf` + bytes empiezan con `%PDF-`. Hoy estamos protegidos por el bypass de openapi-fetch, pero si alguien vuelve a usar `fia.GET('/facturas/{id}/pdf')` directamente, este test lo caza.
- **Migrar el resto de consumidores de `impersonate_org` a un helper común** (~2h, P2 calidad) — hoy hay 3 sitios que leen la cookie con override semantics (`with-api-auth.ts`, `render-pdf/route.ts`, `(dashboard)/layout-server.tsx`) y 1 con union semantics nueva (`/api/documents/file` post PR #52). Extraer un helper `resolveAccessContext({ ownership: 'strict-override' | 'union' })` que documente la decisión y reduzca riesgo de divergencia. No urgente pero ayuda a futuros lectores.
- _(añadir ideas aquí según surjan)_

## Decisiones cerradas (referencia)

- **2026-05-12 · Supabase Cloud se mantiene durante migración tufacturaia.com** — debatido self-host Supabase. Argumento user: "mejor ahora que con 300 usuarios reales". Contra-argumento: estamos en medio (4 orgs reales + agency-portal integrado), self-host añade 7 servicios a operar (Postgres + GoTrue + PostgREST + Storage + Realtime + Studio + Kong), coste real ~30€/mes VPS + backup ~5€/mes ≈ Supabase Pro (~25€/mes) sin contar tiempo, y triplicaría el riesgo de la migración actual. Re-evaluable en 6-12 meses con plan dedicado. Acción opcional: montar Supabase self-host en sandbox aparte un fin de semana para probarlo sin tocar prod, decidir con datos reales

## Decisiones pendientes (producto)

- **Cliente live vs congelado** — hoy snapshot fiscal al crear factura (datos cliente embebidos). Si editas cliente, PDFs viejos conservan datos antiguos (legalmente correcto, confunde UX). ¿Botón "Re-emitir con datos actuales" o congelado siempre? → cuando se decida, ADR en `decisions/`
- **Feature flags experimentales globales** — debatido 2026-05-11, descartado por YAGNI (`org_features` por org ya cubre piloto/experimentos). Si en el futuro aparece caso real de "flag global para TODAS las orgs", usar `system_config` key-value sin UI dedicada hasta tener 3+ flags. NO construir abstracción especulativa

## Bloqueos / esperando a terceros

- **[IMPORTANTE] Declaración Responsable AEAT como fabricante de SIF** — AgentesIA debe presentar este trámite administrativo para que FacturaIA opere legalmente en producción con VERI*FACTU. SIF = Sistema Informático de Facturación (RD 1007/2023). Sin esto el sistema no está homologado. Confirmar con Dani+Gonzalo si ya está o iniciar trámite
<!-- Dani: DNS tufacturaia.com DNS-only resuelto 2026-05-16, SSL OK en app. y n8n. -->
- **Dani + Gonzalo**: subir certificado P12 por org en Ajustes → VERIFACTU. Sin él no se envían facturas a AEAT y aparece error en dashboard
- **Borja**: review/merge agency-portal PR #54, #55, #56
<!-- Meta plantilla otp_facturaia APPROVED 2026-05-16 (language es_ES). Bloqueo cerrado. -->

## Seguridad

### Deudas P0/P1 con threshold claro (priorizadas)

🔴 **P0 — ANTES de primer cliente facturando con dinero real**:
- ~~**OTP teléfono al onboarding WhatsApp**~~ — **HECHO 2026-05-16 19:55**. E2E verificado en prod, WhatsApp llega con wamid + cost 3¢, verify funcional. Ver `Progreso en vivo` y `Histórico de hitos`.

🟠 **P1 — ANTES de cliente B2B que pida certificación ISO/ENS o auditoría AEPD nivel ISO**:
- **Encryption-at-rest del disco VPS** (~3-6h + ventana downtime). LUKS o dm-crypt del filesystem del `185.47.13.170`. Sin esto, robo físico del disco expone todas las BDs (postgres-n8n + envs). Hoy probabilidad muy baja en VPS managed pero auditor lo pedirá.

🟠 **P1 — Refactor del workflow para eliminar `$env.X`**:
- Hoy `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` con threat model documentado (security audit 2026-05-16). Aceptable hoy, bloqueante si: (a) se contrata dev externo con acceso a panel n8n, (b) n8n pasa a multi-user con roles. Plan: esperar n8n 2.21+ con fix del bug `toolHttpRequest`, o migrar a `toolWorkflow` con 4 sub-workflows (~6-10h).

🟡 **P2 — Hardening del perímetro n8n**:
- IP allowlist en Traefik para `n8n.tufacturaia.com` (solo IP del admin + crons internos). ~1h.
- Rotar `N8N_BASIC_AUTH_PASSWORD` a 32+ chars custom (no auto-gen Dokploy). ~5min.
- Desactivar `N8N_PUBLIC_API_ENABLED` si no se usa externo. Verificar.

### Hardening diferido (sin prioridad bloqueante)

- Logs estructurados en endpoints (correlation IDs)
- CSP headers más estrictos
- Métricas de uso de API IA (consumo OpenAI/Anthropic por org)
- Pen-test anual cuando se llegue a >100 clientes

---

## Stack técnico

- **Frontend**: Next 16 (App Router), Tailwind, primitives Button/Card/Input/Modal en `components/ui/`
- **Backend**: Next API routes + Supabase (Postgres, RLS obligatoria por tabla)
- **OCR**: OpenAI Vision (`gpt-4o-mini`), fallback Anthropic Claude Vision
- **Voz**: n8n workflows + Retell (números WhatsApp por tenant)
- **Pagos**: Stripe (checkout pendiente cablear a add-ons)
- **Fiscal**: Verifactu (QR en PDFs, AEAT integrado)
- **Auth/RLS**: superadmin impersona vía `?org_id=` query param, no cookie
- **Tests**: Vitest (175+ unit/integration) + Playwright E2E (`tests/e2e/`: setup, smoke, explorer). Pre-release: `npm run e2e:explorer` y revisar `report.json`

## Reglas de proyecto (resumen)

Ver `facturaia/CLAUDE.md` (en repo) para versión completa.
- `lint` + `typecheck` + `build` limpios pre-commit
- Toda tabla nueva = `ENABLE ROW LEVEL SECURITY` + políticas en la misma migración
- Migration `ADD COLUMN NOT NULL` = `ADD nullable → UPDATE backfill → SET NOT NULL` en transacción
- Endpoint nuevo = auth + rate limit + validación input siempre
- Cambios Zod API público = `openapi.json` en mismo commit (clientes openapi-typescript no leen `refine`)
- UI con badge "implementado" = grep que el backend lee el valor (anti regresión silenciosa)

---

## Manuales y docs

| Doc | Link | Estado |
|---|---|---|
| Manual usuario | [GitHub](https://github.com/AgentesIA-MAdrid/facturaia/blob/main/docs/manuals/manual-usuario.md) | Bloque 1 desactualizado (3 botones, abonos, pills) |
| Manual admin | [GitHub](https://github.com/AgentesIA-MAdrid/facturaia/blob/main/docs/manuals/manual-admin.md) | Bloque 1 desactualizado |
| Módulos producto | [docs/MODULOS-PRODUCTO.md](https://github.com/AgentesIA-MAdrid/facturaia/blob/main/docs/MODULOS-PRODUCTO.md) | Vivo |
| CLAUDE.md proyecto | `facturaia/CLAUDE.md` | Vivo |

## Specs detalladas (subfiles)

- [[facturaia-comunicaciones-emails]] — sistema emails Resend + auth Supabase, 5 PRs, reutiliza outbox/notifications/ai_module_suggestions
- [[facturaia-integracion-api-v1-portal]] — webhooks HMAC, outbox, Stripe-style sync con agency-portal
- [[facturaia-test-tanda-2-3]] — tests de tanda 2 y 3
- [[facturaia-arquitectura-multi-whatsapp-numbers]] — multi-tenant WhatsApp por org
- [[facturaia-bloque-4-agent-query-spec]] — copiloto IA multi-canal
- [[facturaia-open-banking-psd2]] — conexión bancaria PSD2 (legado, sustituido por spec abajo)
- [[facturaia-conciliacion-roadmap]] — **🗺️ Conciliación · Roadmap maestro** (subfile canónico para cualquier sesión nueva — estado, fases hechas, pendiente, siguiente, deferred, decisiones, deuda)
- [[facturaia-conciliacion-copiloto-spec]] — **Conciliación bancaria + Copiloto v2 con tool-use** (spec definitiva, Sprint A MVP + Sprint B condicional). Documento autoritativo en repo: `docs/architecture/conciliacion-copiloto-spec.md`
- [[facturaia-prompt-auditoria-dashboard]] — prompt reutilizable: auditoría dashboard (exactitud + mejoras + arquitectura + benchmarking Holded/Quaderno con foco IA)
- [[facturaia-centro-fiscal-ia]] — **Centro fiscal IA (módulo Pro add-on)** — spec completa congelada 2026-05-19. 303/390/130/111/115/180/190/347/349 + libro IVA + export asesor. v1 cuadre+export, v3 colaborador social AEAT. Calculador versionado, snapshot céntimos enteros, WORM + sello eIDAS, advisory lock anti-race

## Workflows n8n (backups en `knowledge/projects/agentesia/n8n-backups/facturaia/`)

| Workflow | ID | Función |
|---|---|---|
| OCR Factura | `zf2la2N2YBXKQNKk` | Vision → estructurado |
| WhatsApp Receptor v2 | `zYcHHa8jWXB6dY5i` | Ingesta + intención |
| WhatsApp Verify | `LlBeZfa2P6mg13wc` | Verificación Meta |
| Voice Process | `KVk45rJARQk0EKaF` | Procesar audio |
| Voice Confirm | `5Q3pmag3XnIQOkQV` | Confirmación voz |
| Voice Correct | `Q1WB42g8GtyX3qnp` | Corrección voz |
| Email Polling | `dqvzeyeifTcNv50u` | Ingesta email |

---

## Links rápidos

- [GitHub repo](https://github.com/AgentesIA-MAdrid/facturaia)
- [App producción](https://facturaia.agentesia.world)
- [Dokploy](https://dokploy.agentesia.world)
- [Slack canvas Panel FacturaIA](https://agentesialab.slack.com/docs/T0ARXF0V31S/F0AV38CHYSJ) (`F0AV38CHYSJ`)
- [Canal Slack](https://agentesialab.slack.com/archives/pro-facturaia) (#pro-facturaia)
- WhatsApp público: `+34 919 93 26 18`

## Credenciales (refs en memoria, NO secrets aquí)

- Supabase FacturaIA → memoria `supabase-facturaia.md`
- Anthropic API key → memoria `anthropic-api-key.md`
- OpenAI API key → memoria `openai-api-key.md`
- WhatsApp config → memoria `whatsapp-facturaia.md`
- Gemini (backup) → memoria `gemini-api-key.md`
- **SSH Dokploy nuevo**: `root@185.47.13.170 -p 5251` (clave id_ed25519 ya autorizada). Dokploy viejo era `.166`.
- **n8n.tufacturaia.com API key** (JWT) → memoria `reference_n8n_api_key.md` (rotada 2026-05-17).

---

## Histórico de hitos

- **2026-05-20 (Cascada de 5 capas de bug "no se ve el PDF" — destapa middleware-off-3-semanas)** — Reporte user: `no se puede crear una factura en facturaia [object Object]`. Cada fix descubrió una capa más abajo. Total **10 PRs (6 portal + 4 FacturaIA) + commit `520ea1b` agente paralelo**. Capas: (1) `[object Object]` por `String(r.error.error)` con objeto anidado — PR portal #68; (2) HTTP body crudo poco legible → surface `error.message` directamente — #69; (3) PDF "Internal error" sin contexto → surface body real — #70; (4) **FacturaIA cambió `/pdf` para devolver binario** (Content-Type: application/pdf) en vez de JSON `{url}` → openapi-fetch parseaba el binario y reventaba "Unexpected token '%', \"%PDF-1.4\"..." → bypass openapi-fetch con `fetch` manual + Content-Type negotiation — #71; (5) PDF de FacturaIA NO renderizaba `descripcion` aunque la BD la tenía (mig 119) → las 4 plantillas `invoice-pdf.tsx` solo imprimían concepto → render condicional debajo en gris 8pt — FacturaIA #50. **Capa 6 destapa incidente major**: `Forbidden` al previsualizar PDF como superadmin → causa raíz commit `508517d` (27-abr) renombró `src/middleware.ts → src/proxy.ts` por un fix de audit. Next.js solo invoca archivos `middleware.ts` → toda la cadena llevaba **3 semanas apagada** sin que nadie lo notara (refresh sesión Supabase, cleanup cookie `impersonate_org`, gate phone-verified, login redirect). Capas alternativas cubrían admin layout + dashboard login, pero cleanup de `impersonate_org` quedaba sin guard → cookie pegada → `resolveAllowedOrgIds` con override semantics devolvía SOLO la org impersonada → superadmin pierde acceso a sus propios PDFs. Fix triple: (a) revert rename — FacturaIA #51; (b) `resolveAllowedOrgIds` cambia a union semantics (org impersonada se AÑADE, no reemplaza) SOLO en `/api/documents/file` — FacturaIA #52; (c) endpoint dedicado `POST /api/admin/exit-impersonation` + banner con efecto inmediato — commit `520ea1b`; (d) test de regresión `buildAllowedOrgIds` 7 casos — FacturaIA #53. Documentado en `Stack/incidents.md`. **Lección operativa nueva**: rename de archivos críticos por convención de framework (`middleware.ts`, `route.ts`, `page.tsx`, `layout.tsx`) debe tener test de smoke que verifique que la cadena está activa — un build limpio no detecta que Next.js no invoca tu archivo. Mensaje review enviado a Borja Galván por Slack DM con foco en el revert del middleware. **Causa raíz original de la factura**: NIF del receptor faltaba en el cliente → FacturaIA rechazaba por RD 1619/2012 art 6.1.d. No es bug del portal, es validación correcta — user añade NIF y emite OK. Ver [[middleware-rename-disables-everything]] y entrada Progreso 2026-05-20 15:30.
- **2026-05-19 (Toggle Verifactu — PR #48 mergeado, mig 095 aplicada)** — commit `188f519`. Auditoría previa con 3 agentes paralelos (UI/UX + estructura + seguridad) que destaparon: (a) **vulnerabilidad CRITICAL** policy `org_member_update` permite UPDATE a cualquier miembro sin distinguir rol → cualquier user autenticado podía apagar `verifactu_activo`/`entorno_verifactu` desde DevTools (incumplimiento Ley Antifraude); (b) **bug latente** `fiscal.verifactu !== false` reactivaba silenciosamente Verifactu cuando la columna era `false` pero el JSON no tenía la clave; (c) duplicación columna ↔ JSON huérfana (worker, triggers huella, gating, PDFs, voz ya leían solo la columna); (d) `handleSave` con `select('id').limit(1).single()` sin filtrar por org. Fix integral: API route `PUT /api/settings/verifactu` con `requireRole:['propietario','admin']`+Zod+rate limit, trigger BEFORE `guard_verifactu_settings` + trigger AFTER `audit_verifactu_settings_change` (captura IP/UA via `current_setting('request.headers')`), UI lee de la columna + `confirm()` al apagar + error visible. Mig 095 idempotente (UPDATE limpia JSON + 2 triggers). Workflow PR limpio: worktree desde `origin/main` para evitar arrastrar 6 commits locales no pusheados de sprint pre-cutover. **Pendientes**: Fase 3 UX (dirty pill, toggle disabled sin cert, warning con facturas en cola) + Fase 4 a11y (`SettingsToggle` → `role=switch`+teclado afecta todos los toggles). **3 patrones nuevos**: ver [[Stack/facturaia]] "Toggle Verifactu" + [[Stack/hot]] facturaia-toggle-verifactu sección.
- **2026-05-18 (Limpieza cuenta a 0 AgentesIA Lab SL + reapuntado dominio)** — descubrimiento: portal `agency-portal` tenía `FACTURAIA_API_URL=https://facturaia.agentesia.world` (dominio antiguo) → ese dominio **devuelve 404** desde la migración a `app.tufacturaia.com`. Cliente HTTP del portal lleva semanas fallando silenciosamente todas las llamadas portal→FacturaIA. Fix: cambiar env var a `https://app.tufacturaia.com` (misma API key funciona; misma BD Supabase — la "migración" fue solo cambio de dominio/branding). Limpieza completa de la org en FacturaIA (102 facturas en `pendiente_envio` ninguna a AEAT → seguro borrar, 23 presupuestos, 31 clientes, 51 bandeja, 1 recordatorio, contadores series a 0) + 75 sombras `facturaia_documents` borradas en portal. Replicado desde portal: P2026-0001..0007 (7 presupuestos PRES-2026-0003..0009) + A2026-0001 (factura FACT-2026-0001 a Tecnocloud emitida sin NIF como particular, decisión consciente). `api_idempotency` tenía 3 entries stale apuntando a IDs borrados → limpiado antes del re-push para evitar cached responses obsoletas. Verifactu desactivado vía SQL durante el push de la factura y reactivado al final (el toggle UI no persistía por el bug — arreglado al día siguiente en PR #48). Ver [[Stack/facturaia]] "Reset cuenta a 0".
- **2026-05-18 (Bloque 1 tests Vitest + 2 bugs fiscales)** — commit `0e94de4`. 97 tests nuevos en `src/lib/documents/__tests__/` (272+ totales): createDocument (19+13+13+5), anularFactura (32), canAnular (14). Fixtures compartidas con mock Supabase chainable fail-fast por defecto. **2 bugs fiscales corregidos**: (1) validación `exencion_codigo` ⇔ `iva_pct=0` corría DESPUÉS del INSERT facturas → si fallaba dejaba factura huérfana con `num` consumido del RPC → movida antes del RPC/INSERT; (2) SELECT organizations corría DESPUÉS del RPC → si org no existe `num` quedaba quemado → movido antes del RPC. Fix server-side `anularFactura` rechaza `estado='borrador'`. `canAnular` extraída a función pura. ADR-001 en repo documenta huecos serie por errores residuales post-RPC (deuda técnica aceptada). 21 agentes paralelos en 5 rondas. Cero as-any/ts-ignore/skips. Lint+typecheck+build+469 tests suite verdes. **Patrones**: [[validar-input-antes-de-rpc-atomico-e-insert]] + [[mock-supabase-fail-fast-default-en-tests-vitest]].
- **2026-05-18 (sesión cumplimiento fiscal full)** Sprint 3 fiscal completo + Sprint 2 PDFs + Sprint 1 críticos AEAT + UI Sprint 3. **C4 bloqueante AEAT descubierto** en audit pre-prod por agente A-verifactu (5 agentes paralelos especializados): XML enviado sin `<Desglose>` → XSD rechaza en prod (entorno `pre` no validaba estricto). 4 migrations nuevas (085 huella race-lock `pg_advisory_xact_lock` por (org,serie), 086 RLS DELETE blindado, 087 fecha_operacion DATE + lineas_factura.exencion_codigo + CHECKs, 088 RPC convertir copia exencion_codigo). xml.ts emite `DetalleDesglose` agrupado por (iva_pct, exencion_codigo) con catálogos AEAT literales L8A (`01-20`), L9 (S1/S2/N1/N2), L10 (E1-E6) — throw si vacío/>12, XOR Calificacion/Exenta. NIF receptor obligatorio si cliente.empresa en createDocument + voice/generate + convert-presupuesto. Bot WhatsApp con handlers cascada IF nif_required + exencion_required con mensajes específicos al user. PDFs blindados: `<NormaExencionNotice/>` (cita literal art 20/21/22/23-24/25 + Directiva 2006/112/CE por código) + huella SHA-256 truncada + frase "Factura verificable en la sede electrónica de la AEAT" + disclaimer proforma "SIN VALIDEZ FISCAL". UI `/generar`: disclosure "+ Añadir fecha de operación distinta" + selector reactivo E1-E6 cuando iva_pct=0 + banner amarillo no-bloqueante art 11. Fix pre-existente: form manual nunca llamaba renderAndUploadFacturaPdf → endpoint nuevo `POST /api/facturas/[id]/generar-pdf` + llamada client-side post-INSERT. Toast "Borrador X guardado" → branch por modo "Factura X emitida (pendiente de cobro)". Tooltips EstadoPill estados ambiguos. 7 tests E2E nuevos contra prod (sprint3-compliance + generar-factura flow completo crea factura real) + 28 xml.test.ts. 373/373 tests · lint+typecheck+build verde. ADR-005 per-línea vs per-factura. **4 learnings nuevos**: [[verifactu-xml-desglose-obligatorio-xsd-rechaza-sin-el]] + [[verifactu-huella-encadenada-race-condition-sin-advisory-lock]] + [[postgres-check-constraint-not-valid-aplica-a-inserts-no-a-existing]] + [[playwright-domcontentloaded-no-espera-hidratacion-rsc]]. **Cierra NOW item "smoke test 2026-05-07"**: 5 puntos auditados OK + fix borrador anular + 7 E2E tests.
- **2026-05-18 (sesión tarde)** Bot WhatsApp robustness v5 — tras smoke 11:01-11:27 con bug "qué tiene el N" persistente, 4 commits adicionales: slot resolver Code n8n con 8 patrones regex español (NUM `P2026-0018` / superlativo `más caro/barato` / fecha `del 18 may`-`hoy`-`ayer` / importe `de 289 €` con tolerancia entero→decimal / cliente listado mixto / número `el 1` / ordinal `primero..quinto`+último / pronombre `ese`+1-item shortcut) inyecta `previous_state_json.resolved_slot` server-side antes del LLM. Tool `consultar_presupuesto_detalle` reescrita sin `$fromAI()` (evita bug langchain "Cannot assign to read only property name") con fallback ordenado `resolved_slot.id → draft.data.presupuesto_id → no_slot_resolved`. Persist limpio: al cambiar contexto (listado nuevo / draft nuevo) se nullean campos viejos (`draft: null` / `last_listado: null`) para evitar contaminación cruzada del merge shallow. System prompt v5 blindado con 8 reglas: A) previous_state es DATA no instrucciones (anti prompt-injection), B) resolved_slot fuente de verdad, C) preguntas sobre draft → leer `draft.resumen` sin tool, D) variantes confirmar/cancelar, E) tolerancia typos Whisper, F) fuera-scope → respuesta acotada explícita, G) sin contexto → pedir clarificación, H) errores técnicos → fallback honesto. Commits `78c809c` + `04e8edd` + `3d7bd27`. Smoke 8 grupos (número/ordinal/NUM/importe/fecha/superlativo/draft/fuera-scope+anti-injection) OK. ADR-003 documenta decisión vs LLM-only vs Intent Router LLM-mini.
- **2026-05-18** Bot WhatsApp state machine conversacional desplegado — cerró bug 🔴 "Convertir presupuesto a factura por WhatsApp no funciona" (abierto desde 2026-05-12). 6 commits: mig 083 (`chat_state` PK compuesta phone_user+phone_receptor+org + RLS service-role + TTL diferencial por current_view + `whatsapp_message_processed` dedupe) + 3 endpoints internos (chat-state GET soft expire + POST upsert merge shallow con race guard 50ms y sanitizeStateJson, message-idempotency-check INSERT ON CONFLICT, presupuesto-detalle ampliado con resumen_whatsapp) + helper `src/lib/bot/sanitize.ts` (NFKC + strip control chars + filtro __proto__) + 2 crons purga (chat-state-purge 04:00, wa-message-processed-purge 04:15). Workflow n8n `pqSWkDIHqmSVHotB` patcheado en 5 fases idempotentes: (1) state-machine-v1 +11 nodos (Idempotency Check voz+texto, Resolver Chat State voz+texto, Persistir Chat State doc+resumen, tool consultar_presupuesto_detalle, Preparar Input Agente con prefix `<previous_state>...</previous_state>` en chatInput); (2) persist-listado (few-shot agente devuelve `candidates: [{id, num, ...}]` en ambiguous + Parsear y Calcular Totales hace POST chat-state antes del error); (3) phone-sesion-fix (normalización `+` E.164 en 5 Code nodes + nuevo Resolver Chat State Sesion entre Preparar Input Correccion y Preparar Input Agente — rama "sesión voz activa" no pasaba por resolvers principales); (4) urlsearchparams-fix (encodeURIComponent + concat manual en 3 Code nodes — sandbox n8n no expone URLSearchParams); (5) httpnode-hardening (POST Delivery Status responseFormat→text + neverError, Hash Session retry x3 — sobreviven ventanas redeploy Traefik 404). Wire admin: cron-runs/run loadHandler con cases nuevos + test guard `registry-wired.test.ts`. Smoke real E2E OK ("Convierte presupuesto Manuel → el 1 → confirma → factura A2026-0059 generada con PDF + caption correctos"). **Patrones aprendidos** 3 learnings nuevos: [[n8n-code-sandbox-no-tiene-urlsearchparams]] + [[n8n-sessionid-sin-plus-vs-endpoint-e164]] + [[n8n-http-responseformat-json-rompe-con-404-traefik]]. ADR: [[ADR-002-bot-state-machine-postgres]]. Incidente en [[incidents]].
- **2026-05-16** OTP teléfono onboarding WhatsApp en prod — 5 agentes review (security/backend/legal/UX/SRE) + 3 agentes implementación paralelos. Backend (migrations 075-076 + 11 endpoints + RPCs atómicas + cap 30€/mes + audit append-only 6 años AEAT), Frontend (página verificar + admin tab + banner grandfathering + signout), Docs (manual-admin §26, manual-usuario, ARCHITECTURE §5.8, onboarding-flow Mermaid, privacy-otp-update, n8n delivery patch). Plantilla Meta `otp_facturaia` AUTHENTICATION con COPY_CODE, fallback Resend automático. E2E verificado en prod 17:53 UTC: wamid + cost 3¢ + verify_ok. 8 bugs detectados y arreglados en sesión testing real: (a) `openapi.json` throw build-time sin NEXT_PUBLIC_APP_URL; (b) middleware rewrite confunde URL → redirect; (c) página verifica arrancaba en code aunque no había verification abierta; (d) /api/auth/signout faltaba; (e) signout request.nextUrl.origin = http://0.0.0.0:3000 container → NEXT_PUBLIC_APP_URL; (f) compose no pasaba envs META_* + RESEND_API_KEY al container; (g) BuildKit Dokploy cache invencible aunque Dockerfile ARG CACHEBUST + .deploy-marker — bump package.json 0.1.0→0.1.1 finalmente lo rompió; (h) **plantilla Meta APPROVED como `es_ES` (Spanish SPA) pero código enviaba `es` → error 132001 → fallback email permanente**. Panel admin Cobros unificado (stats + pricing en `/admin/system?tab=cobros`). Refactor `agentesia.world` → `app.tufacturaia.com` en README/.env.example. **Patrones aprendidos** documentados como memorias: Resend Pro montado (no re-proponer), Meta template language `es_ES`. **Patch n8n delivery status pendiente** (API key nueva del n8n.tufacturaia.com).
- **2026-05-12** Módulo Agente de cobros end-to-end + caos engineer audit (commits `b48d9cb` + `ad5d8a4` + `671b890`) — backend 8 migrations (065-073) + 9 libs TS + 11 endpoints + workflow n8n parcheado dos veces (init STOP detection + regex case-insensitive con tildes), UI 4 componentes Cobros + drawer notif rediseñado con chips/snooze + settings preferencias personales + 2 admin panels + Aging view en /emitidas. Tres auditorías independientes (6 agentes planning paralelos + 1 backend static + 1 caos engineer adversarial) → 8 fixes reales aplicados con ~50% falsos positivos descartados con razón. 256 tests. Migrations aplicadas prod. Mockup data seedeado. Endpoint cron operativo en Dokploy `*/15 * * * *`. **Pendiente externo**: aprobar plantilla Meta `cobros_recordatorio_v1` (24-72h Meta review) + envs `META_WHATSAPP_PHONE_NUMBER_ID` + `META_WHATSAPP_ACCESS_TOKEN` en Dokploy → entonces envíos reales. **Patrones aprendidos** (10 nuevos para CLAUDE.md): (a) useSyncExternalStore getSnapshot devolver valor cacheado, NO `Date.now()` directo → "Maximum update depth"; (b) Dokploy cron Command sin wrap + comillas DOBLES alrededor de `-H "x-service-key: $VAR"` (simples NO expanden); (c) SettingsView con useState getter + window check → hydration mismatch → useSearchParams + Suspense; (d) 9 plantillas hardcoded RD 1619/2012 auditable git, cuotas+template Meta SÍ editables admin; (e) Lost update concurrente JSONB → usar jsonb_set/operator `-` SQL atomic en RPCs, no read-modify-write TS; (f) Auditoría con DOS perspectivas: static analyzer + adversarial pen-test descubren cosas distintas; (g) Falsos positivos típicos auditor: regex con bytes invisibles que parecen `[ --]` pero son control chars, dedupe_key con kind+date sin entender que el helper usa (org_id, dedupe_key) UNIQUE; (h) DELETE de validation flag debe limpiar TODOS los campos derivados (no solo el flag), o re-validación usa valores stale; (i) Aging/dashboard queries deben tener límite temporal explícito en JOIN a tablas append-only (recordatorios, audit, etc.) o crecen linealmente con histórico; (j) Race opt-out vs send mid-flight: defensa segunda haciendo re-check ANTES del fetch externo, no solo en INSERT inicial; (k) Network error post-delivery Meta es CRÍTICO: necesita cool-down BD para evitar retry → doble envío real; (l) n8n IF regex node es case-sensitive por defecto — usar clases [Ss][Tt]... explícitas + cubrir tildes con [ÁA]
- **2026-05-11** Reorganización admin (sidebar + Salud + Config + mantenimiento) — 4 agentes paralelos planificaron (backend/frontend/security/product), síntesis aplicada en 1 sesión. Migración 064 `admin_audit_log` + reuso `system_config` para `maintenance_banner` y `read_only_mode`. 7 endpoints nuevos (5 admin + 1 público banner + features extendido). 14 componentes UI nuevos (tabs accesible + paneles config/system + banner global). Kill switch en `with-api-auth.ts` + `handler.ts` v1 con bypass superadmin + recovery path. Redirects en next.config para zero-breakage (URLs viejas siguen sirviendo). Cross-link Features ↔ Planes. 5 bugs columnas BD pillados en autorevisión antes de runtime. Lint/typecheck/build OK. Migración aplicada en prod (`psql $SUPABASE_DB_URL -f`). **Patrones aprendidos**: (a) 4-agente paralelo con expertise dispares produce planes convergentes; (b) importar páginas Next.js existentes como panel-components inside tabs view = zero breakage refactor pattern; (c) reutilizar tabla key-value existente (system_config) en lugar de crear singleton para configs relacionadas; (d) admin destructive actions requieren re-auth + typed phrase + auto-expire + bypass recovery path; (e) grep schemas BD antes de escribir endpoints — 5 mismatches detectados; (f) `Date.now()` durante render bloquea react-hooks/purity en React 19 strict; (g) Next 15 redirects vs imports = capas independientes (URL redirect no rompe import). **Diferidos**: email_log Fase A (recomendado SÍ próxima), AEAT health derivado (SÍ ~5min), comparador planes y feature flags globales (NO)
- **2026-05-11** Sesión grande módulos IA + observabilidad — (1) Módulos IA mejorados: OCR `auto_categorizar` + Conciliación `auto_marcar_cobradas` via triggers BD (migrations 060/061 cubren todos los callers n8n/manual/API v1/voice); Cashflow IA `{horizonteDias, incluirRecurrentes, umbral_alerta_caja}` con cron `/api/internal/cashflow-alerts` Opción B (chunks 10 + dedupe 24h + gate orgHasFeature); Copiloto `idioma` + `guardar_historial` con tabla `copiloto_conversaciones` (062) + gate `orgHasFeature('copiloto')` cerrando fuga billing preexistente + Topbar oculta botón + `AIAssistantHost` gatekeep evento global; ModuloConfig fix defaults `value !== undefined ? value : default` + error handling. (2) Observabilidad crons: tabla `cron_runs` (063), `withCronTracking` wrapper aplicado a 6 crons (webhook-dispatcher, check-vencimientos, cashflow-alerts, storage-quota-check, run-module-suggestions, verifactu-process — éste con auth custom `SUPABASE_SERVICE_ROLE_KEY`), panel `/admin/system/crons` con semáforo + tooltips con ejemplos reales + notif `cron_failed_<name>` a campanita. (3) Bug destapado: storage-quota-check fallaba semanal por env vacía → fix `parseMb`. (4) Auditoría: 6 agentes paralelos (2 rondas × 3 expertise) → 1 P0 + 3 P1 + 2 P2 aplicados, ~50% falsos positivos descartados con razón. Commits: `33d5c7c` + `164dd9a` + `19ee00f` + `3fefc3a` + `9892e4c` + `1b14779` + `5255547` + `d4b7566` + `d43f80d` + `6442182` (CLAUDE.md vault). 4 migrations aplicadas en prod. ADR-001 documenta decisión cron observability. **Patrones aprendidos** 6 learnings nuevos + 1 update + 9 reglas CLAUDE.md global + sección Vault Obsidian en CLAUDE.md proyecto
- **2026-05-10** Primera prueba E2E real WhatsApp del módulo voz — Borja envió audio "convierte el presupuesto a factura" → bot detectó intención correctamente y mostró botones, pero descubiertos 2 bugs UX en producción: (1) resumen con totales 0€ por iterar `data.lineas` que el agente no devuelve para `convertir_presupuesto` (fix con lookup HTTP a Supabase REST en `Parsear y Calcular Totales`); (2) matching estricto con dictado por voz fallaba ("manvel"≠"Manuel") y bot pedía "número exacto" en bucle (fix con system prompt: matching permisivo + 4 casos: 1 match / >1 candidatos numerados / 0 matches con últimos 5 abiertos / memoria conversacional para responder "1"/"2"). Commits `5cf48a3` + `6d41b9b` + `4e98d76` (manuales). Backups en ops/n8n-backups/. **Patrones aprendidos** documentados como 3 learnings nuevos: lista candidatos numerados + memoria conversacional vs error genérico (LLM ambiguity), parchear downstream o lookup recurso real al extender contrato JSON-out, verificar persistencia tras PUT con grep marker único (idempotency check defectuoso reportó OK sin aplicar). Pendiente: que Borja reintente la prueba con bot ya parcheado en producción
- **2026-05-10** Módulo Voz F1+F2 completo — convertir presupuesto por intención + admin completo. 12 commits a main: endpoints `/api/voice/find-presupuesto` + `convert-presupuesto` + `/api/internal/{voice-system-prompt,voice-chat-bindings}` + `/api/modules/generador_voz/{prompt,history,restore,preview,playground}` + `/admin/voice` (tabla overrides). 6 migraciones (053 voice_invocations, 054 voice_idempotency PK compuesta, 055 voice_chat_bindings, 056 voice_prompt_versions con RLS por org, 057 grants/purge, 058 pg_cron 03:00 UTC). Sistema híbrido del prompt: 12 variables tipadas + modo Avanzado override libre, template canónico HEADER/FOOTER inviolables. UI tab Configuración custom + tab Avanzado banner rojo + Playground con tokens/coste/historial + WAI-ARIA tabs completo. 5 bugs hardening detectados E2E: PGRST203 RPC overload (todas las conversiones rotas en prod), feature_gate ausente en find/convert, iva_pct sin .min(0).max(100), invalid_json devolvía 422 (debe 400), ocr-audit FK violation devolvía 500. Workflow `zYcHHa8jWXB6dY5i` patcheado vía API n8n: 64→67 nodos, +consultar_presupuestos toolCode + Es Conversion? IF + Convertir Presupuesto HTTP post-confirm. Backup en `ops/n8n-backups/`. **101 tests E2E reales contra Supabase prod (0 fail), 60/60 unit, lint/typecheck/build OK**. Pendiente push + deploy + activar generador_voz en orgs + phone en profiles antes de E2E real con Meta WhatsApp
- **2026-05-08** Suite E2E Playwright (`tests/e2e/`) — 3 proyectos: setup (login → storageState), smoke (4 paths críticos) y explorer (bot recorre app, rellena forms, clica tabs/filas/modales, captura console errors + HTTP 4xx/5xx + crashes). Output a `tests/e2e/report.json`. Comandos: `npm run e2e:smoke|e2e:explorer|e2e:report`. 2 bugs UI detectados y fixeados (commit `394a7ff`): (a) `/emitidas` y `/recibidas` 404 silenciosos por falta de guard `logo_url='not_found'` en `facturas-view.tsx:842,1082` (estaba en otros 4 sitios — bug por inconsistencia entre componentes); (b) `/ingesta` con 0 canales apuntaba a `/admin/connectors` (404), redirigida a `/settings?tab=integraciones`. CI verde tras fix `vitest.config.ts` (excluir `tests/e2e/**`). Re-run explorer post-deploy: 0 issues vs 8 antes. **Patrón aprendido**: cuando un guard `algo === 'sentinel_string'` se replica entre componentes, verificar TODOS los puntos de uso con grep
- **2026-05-08** PR #47 mergeado — audit log en marcar-cobrada/reenviar-email/anular/DELETE + idempotency en DELETE
- **2026-05-07** Sesión grande — 31 commits a main: 175 tests, withApiAuth en 19 endpoints, UI primitives, 14 modales role=dialog, FacturaMeta badges, tabs Facturas/Abonos, fix bug fiscal informes IVA, fix EstadoPill MAP, 3er botón "Emitir como pendiente", menú condicional por estado, CI integration job, RLS migration_037, .env.example expandido, migrations 047/048/049, deploy verde, 5 learnings + 5 reglas CLAUDE.md
- **2026-05-07** Voz WhatsApp en producción — 10 puntos verificados (e2e orgs, OCR receptor v2, presupuestos, textos WA, 403 toggles, modal series, duplicados BD, response limpio)
- **2026-05-07** Presupuestos/Proformas/Abonos completos — emisión funcional desde form manual + voz
- **2026-05-07** Secrets rotados — `fia_live_…` API key + `whsec_fia_…` webhook secret
- **2026-05-06** Sistema módulos premium completo — catálogo + add-ons + recomendador IA con 2 evaluators reales + activity feed + métricas + 137 tests Vitest
- **2026-05-06** Cron Dokploy recomendador IA configurado y validado (1 sugerencia generada para AgentesiaLab)
- **2026-05-02** Integración API v1 ↔ agency-portal — webhooks HMAC, outbox, Stripe-style sync, 7 PRs apilados
- **2026-04-28** Presupuestos/proformas/abonos voz + form manual; modal series builder; validación formato; invitaciones equipo; fix workflow voz abono; cleanup mockup AgentesiaLab
- **2026-04-27** VeriFACTU integración completa (QR en PDFs, AEAT)
- **2026-04-26** Billing banner + feature-locked rediseñados; datos fiscales completos clientes/proveedores; simplificación workflow voz WhatsApp
- **2026-04-25** Plantillas factura PDF pixel-perfect; system_config + admin config; security hardening; admin limits/storage fix; generador facturas voz desplegado; rediseño formulario nueva factura + email
- **2026-04-21** WhatsApp ingesta multi-tenant + Admin Panel + Feature Flags
