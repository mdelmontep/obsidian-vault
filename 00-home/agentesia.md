---
title: agentesia
date: 2026-07-22
updated: 2026-09-04
tags: [cliente, agentesia]
---

# Agentesia (interno)

La empresa. agency-portal + ticketing chatbot + integración con TuFacturaIA + Slack canvases.

## Estado

- **agency-portal** (Borja como reviewer) — Next.js, integra con TuFacturaIA via API v1 (HMAC webhooks + outbox + Stripe-style sync)
- **`/agency/time`** — dashboard de horas del equipo (Dani/Borja/Manu): ingesta vía hooks locales de Claude Code (`ops/claude-time-tracker`), límites de uso 5h/semanal (+ countdown de reset real, 25-jul, ver hito 0), síntesis IA (gpt-4o-mini) de qué está haciendo cada uno en "Trabajando ahora" (árbol persona→sesión, agrupado por `claudeSessionId` desde PR #166 — antes agrupaba por proyecto y fusionaba sesiones paralelas del mismo proyecto en una sola fila; la fusión de DURACIÓN sigue siendo por sesión, no se pierde). Gráficas "Trabajo del mes" (técnico/proyecto) rediseñadas 25-jul: barras (no líneas) para todas las métricas, panel lateral con total REAL del periodo (no una suma que crece sin techo), 6 rangos (Hoy/Ayer/7 días/Este mes/30 días/Personalizado) × vela 5min-7días — ver hito 0. El nombre de proyecto se deriva del cwd en el hook: normalizado en la ingesta (mismo repo con carpetas distintas por máquina) y, desde 25-jul, los **git worktrees resuelven al repo principal** en vez de a la rama. "Activo ahora" = última actividad hace <15 min (`ACTIVITY_GAP_MS`), y sin heartbeat un turno más largo que eso no aparece.
- **Chatbot propio** — dos workflows vivos en `n8n.agentesia.madrid`: **`89B9QN23hOHDq6oP` "ChatBOT mejorado"** (WhatsApp vía Chatwoot, 63 nodos: captación + soporte/tickets) y **`G8vbSYm0SY3zi912` "Web Chat Agentesia"** (widget de la landing, síncrono, 12 nodos, NO pasa por Chatwoot). `enVlCyi7McKfwkRQ` está **apagado desde abril** pese a que la doc vieja lo señalaba como el vivo. Los dos escriben en el mismo Sheet `Registro de Leads AGENTESIA` distinguidos por `Channel`. Widget en el repo `agentesia-web` → `src/pages/ChatbotPage.tsx:21`. API key n8n en 1Password `N8N API Key - AgentesIA (Claude)` (vault Agentesia)
- **agentesia-web** — la landing pública (`agentesialabs.com`), Vite + react-ssg (NO Next). Gates: `npm run lint && npx tsc --noEmit && npm test && npm run build`; el H1 se comprueba sobre el `dist/index.html` prerenderizado, no sobre el DOM del dev. ⚠️ **`mdelmontep` NO tiene push aquí** — se contribuye por fork+PR y mergea `notcapi`. Ver [[permiso-de-github-es-por-repo-no-por-pertenecer-a-la-org]]
- **Slack workspace** activo, canvases por proyecto

## Próximos hitos

0. **agency-portal — `/agency/time` CERRADO EN PROD (25-jul): worktrees, ventana de 15 min y hook con fuente única** — #174 (countdown + rediseño), **#176** (`2aef17d`, worktrees) y **#177** (`f4db182`, countdown en días+horas a partir de 24h: "5 días 12h" en vez de "132h 23m") mergeados y desplegados; #175 cerrado por duplicado. El fix de fondo: `resolveProject()` devolvía la RAMA en cualquier worktree (`.git` es fichero con `gitdir:`, no directorio), así que 5 sesiones del mismo repo salían como 3 proyectos; `ACTIVE_WINDOW_MS = ACTIVITY_GAP_MS` mata la franja ciega de 10-15 min; y `ops/feedback-runner` tenía una **copia del hook ya divergida** (sin el bridge de `resets_at`) → borrada, el Dockerfile copia el canónico con build context en `ops/`, y el clon del runner pasa a llamarse como el repo (`/home/runner/agency-portal`) para que sus horas caigan bajo `agency-portal` en vez de un proyecto inventado por job. **Verificado en prod, no asumido**: migración `member_rate_limits_resets_at` ya aplicada (columnas con valores), backfill de 10 filas, deploy del portal `2aef17d` Done, build del runner con las rutas COPY nuevas (`claude-time-tracker/claude-time-hook.mjs`) y `[runner] arrancado (stub=false…)` clonando en `/home/runner/agency-portal`, y "Trabajando ahora" mostrando las **5 sesiones bajo `facturaia`**. Aprendizaje operativo: el compose del runner **no** estaba pegado en el panel — el servicio tiene Provider=GitHub con Autodeploy, así que Dokploy lee el `docker-compose.yml` del repo (ver [[Stack/docker-infra]]); no hubo que tocar nada en Dokploy. Ver [[git-worktree-dotgit-es-fichero-basename-devuelve-la-rama]] · [[presencia-por-eventos-discretos-ventana-mayor-o-igual-al-gap-y-heartbeat]]. Queda solo: (a) que Borja/Dani/Manuela corran `./install.sh` para tener el hook nuevo en su máquina (les afecta si usan worktrees; el countdown además necesita `jq`, y el statusLine no se dispara en `claude -p` headless), (b) opcional: heartbeat `PostToolUse` throttleado para que un turno de >15 min no desaparezca del panel. **(b) HECHO por Borja (26-jul, #181/#182/#183)**: latidos `SubagentStop`+`PostToolUse` (el trabajo autónomo se contaba como 1 min), backfill desde los transcripts JSONL y badge "hook antiguo" en el dashboard. **Hook reinstalado en la máquina de Manu (26-jul)** — al hacerlo se destapó lo del env, ver hito 1.
1. **agency-portal — la separación `manu`/`info` de `TIME_TRACKER_KEYS` NUNCA se aplicó; la nota decía que sí (26-jul)** — al reinstalar el hook, la key de 1Password daba 401 contra `/api/internal/time-ingest`. Medido: la key vieja responde `{"ok":true,"member":"manu"}`, o sea que ni se añadió la key nueva ni se renombró la vieja a `info`. El env editado el 22-jul de madrugada no llegó al contenedor que corre, y `app.agentesialabs.com` **sí** resuelve a `185.47.13.166`, el host de la nota — así que el panel era el correcto y el cambio se revirtió o nunca se guardó. **Decisión: se queda UNA sola key, member `manu`** (la separación no era el mecanismo de las 2 cuentas Claude; eso es `accountKeys` en `~/.agentesia-tracker.json`, que la config de Manu nunca ha tenido). Corregidos el item de 1Password y la entrada del 23-jul de este hub, que documentaban un estado inexistente. **Pendiente**: averiguar por qué esa edición de env no persistió, antes del próximo cambio que sí importe. Ver [[verificar-deploy-de-env-por-comportamiento-no-por-contenedor-recreado]]. **Actualización 04-ago**: la separación por CUENTA (no la de `manu`/`info` descartada aquí) sí se hizo, con el mecanismo correcto — ver hito 8.
2. **agency-portal — Dani: hook time-tracker no reporta pese a reinstalar** (NOW, 22-jul) — última actividad real de Dani congelada desde hace 45+ min pese a abrir sesión nueva y escribir prompts. Se le pasaron 3 comandos de diagnóstico (config `~/.agentesia-tracker.json`, hook registrado en `~/.claude/settings.json`, test manual POST a `/api/internal/time-ingest`) — pendiente que los corra y comparta la salida (sobre todo el `HTTP_STATUS` del test manual).
3. **agency-portal — CI (Actions) bloqueado por billing, confirmado también aquí** (recurrente, mismo org que TuFacturaIA) — jobs mueren en 2-4s con "recent account payments have failed". Mismo patrón que TuFacturaIA: verificar local (`lint`+`typecheck`+`test`+`build`) antes de cada merge, `gh pr merge --merge` normal (no hace falta `--admin` aquí, a diferencia de TuFacturaIA). Ver [[github-actions-org-private-free-tier-2000-min]].
4. **agency-portal — n8n router consulta Supabase como source of truth** (NEXT, arrastrado) — TTL 30d en `aia_ob:{phone}` es tirita. Endpoint `/api/onboarding/is-active-by-phone` + nodo HTTP en `ChatBOT mejorado` antes del IF Activo + Redis a caché con re-seed. Ver [[Stack/n8n]] · [[ADR-004-tool-calling-vs-json-schema-en-extraccion-onboarding]]

5. **Chatbot propio — cerraba ventas sin crear la cita: arreglado en prod, SIN verificar en vivo** (NOW, 4-sep) — la primera ejecución real tras el fix del Sheets (Orconsa, 11:41-11:44) destapó lo gordo: el prompt mandaba llamar a una tool `Agendar` que **no existe** (la real es `Reservar`) y las 3 tools de Google iban con `descriptionType: auto`. El bot cerró la demo, prometió invitación por Teams a `luis@orconsa.es` y **no creó nada**; el Slack sí salió, y duplicado (la regla anti-repetición estaba solo en el bot web desde abril). Tres PUT aplicados y verificados (63 nodos, activo, sin drift): 4 nombres fantasma corregidos, `descriptionType: manual` en las de Google, `Reservar` limpio del taller mecánico del que estaba copiado + `attendees` + `sendUpdates: all` + Meet, y el bloque de precios alineado con la web (fuera las cifras de puesta en marcha, Starter a 1 canal a elegir, web incluida en Pro, plan A medida con señales para que lo ofrezca él). **Decisión**: el evento vive SIEMPRE en Google Calendar y lo que cambia es el enlace — Meet por defecto, Teams bajo demanda pidiéndole a Graph solo el `joinWebUrl`; descartado crear el evento en Outlook, que partiría la agenda en dos. **Tuyo:** `Reservar` no se ha ejecutado NUNCA en la vida del workflow — esa credencial de Google solo ha LEÍDO ese calendario; escribir al bot desde el móvil y confirmar que el evento se crea, que la invitación llega y que el Slack sale una sola vez. Backup `PRE-fix-tools-20260904-1305.json` en el scratchpad de la sesión. Ver [[tool-description-generica-no-fuerza-ejecucion-de-tool-critica]]
   - Arrastrado de antes (31-jul): (a) `Es FIN` sin cablear y 12 conversaciones abiertas sin cerrar; (b) `Web Chat Agentesia` sin una ejecución desde el 19-jul, sin diagnosticar; (c) el `x-webhook-secret` del toolCode `Crear Ticket` sigue hardcodeado en el `jsCode` → a `$env`. Ver [[if-con-ambas-ramas-al-mismo-nodo-no-hace-nada]]

6. ✅ **agentesia-web — el H1 sin "en Madrid": PR #94 MERGEADO (03-ago, `89473be`)** — `titleSeed` pasó a `"PYMEs"`, la señal local se quedó en el eyebrow, `seo.ts`, `seoPages.ts` y el `LocalBusiness` de `schemaOrg.ts`. De paso quedó claro que el tecleo del hero no lo mató reduced-motion sino el #89 (a11y/SEO), que sacó la animación fuera del heading. Ver [[animar-el-texto-de-un-heading-indexable-lo-sirve-a-medias]]

8. **agency-portal — badge "en uso ahora" en cuentas de Claude compartidas: PR #206 mergeado+desplegado (04-ago), PR #209 abierto** — el equipo comparte una cuenta ("agentesia", login `info@agentesia.madrid`) cuando se agota la personal; sus % de límites se mezclaban con los de quien la usara en cada momento. #206: cabecera `x-tracker-reporter-key` (key personal de quien reporta, nunca para autenticar) + `accountKeys` en `~/.agentesia-tracker.json` de Manu/Dani/Borja mapeando el `accountUuid` de "agentesia" (`53a26b18-812b-4239-bf26-cac46b9c9a13`) a su member propio en `TIME_TRACKER_KEYS` — mergeado, desplegado, migración aplicada, verificado por Borja (200 en key personal y en la compartida). Al preguntarse "¿y si son 2 a la vez?" salió que el diseño (`used_by_member`/`used_by_at`, columnas escalares) era last-writer-wins: dos personas usando la cuenta a la vez se pisaban en el badge. **#209 (abierto, pendiente review+deploy+migración de Borja)** lo sustituye por tabla `member_rate_limit_reporters` (fila por reportero) — el badge pasa a listar a TODOS los activos en los últimos 15 min. Ver [[atribucion-quien-usa-x-ahora-columna-escalar-pierde-bajo-concurrencia]].
9. **agentesia-web — el giro a "desarrollo a medida", en dos PRs esperando a `notcapi` (03-ago)** — ya no queda nada sin subir: el `main` local divergido se deshizo en rama propia.
   - **PR #97** (`feat/visuales-precios-voz`, worktree `~/wt-web-visual`) — los 4 commits que solo vivían en el `main` local: capturas de cliente a 4:3 con pills en rejilla y `min-h` en la tarjeta, fotografía nueva en las tarjetas de servicio, precios en anual por defecto con cabecera de altura fija, y los cinco sectores reales con audio de llamadas de Retell. Rebase limpio sobre `origin/main`. **Lleva un 5º commit cherry-pickeado a propósito**: el fix que quita los nombres de clientes de los audios vivía en la OTRA rama, y sin él este PR publica nombre y apellidos reales. Listo para mergear.
   - **PR #99** (`feat/foco-desarrollo-a-medida`, DRAFT hasta que entre el #97) — los 16 commits del worktree: `/servicios/software-a-medida`, `/servicios/tufacturaia` con capturas reales de la app, hero con ilustración SVG interactiva, sección de redes, `/resultados` y `/casos-de-uso`. **Las dos regresiones anunciadas eran reales y están resueltas**: el titular volvía a teclearse dentro del `<h1>` (lo que cerró el #89) y las stats a mano resucitaban el "+35% Más conversión" que el #91 retiró por no tener respaldo. Ahora el H1 es estable ("Desarrollamos herramientas a medida"), los ejemplos rotan fuera del heading con `aria-hidden` y las tres cifras vuelven a `PROOF`. Integrado con **merge, no rebase** (16 commits y `HeroSection.tsx` reescrito 5 veces → un solo punto de resolución); backup previo en `backup/foco-a-medida-prerebase`. Gates: **vitest 76/76**, lint 0 errores, build SSG con las tres rutas prerenderizadas.
   - **Tuyo**: horquilla de precio de "A medida", créditos de Higgsfield para la foto que falta, desplegar `tufacturaia.com` (sigue en **404**) y decidir si el titular recupera el tecleo — si sí, hay que decidir antes qué pasa con `heading-text-integrity`. Las capturas (desktop fullPage + móvil de las 3 rutas) se arrastran a mano a un comentario del #99: `gh` no las sube.
   - Ver [[rama-que-reescribe-el-mismo-fichero-varias-veces-se-integra-con-merge]] · [[al-partir-una-pila-en-prs-el-fix-tiene-que-viajar-con-lo-que-lo-causa]] · [[los-tests-rojos-que-hereda-un-merge-se-clasifican-uno-a-uno]] · [[rama-nueva-desde-un-main-local-sin-fetch-revierte-trabajo-ajeno]] · [[los-audios-de-llamadas-reales-llevan-nombres-de-clientes]] · [[capturar-un-saas-para-la-web-publica-exige-mirar-la-organizacion-activa]]

10. **agency-portal — Flota IA: centro de control de agentes voz+chat — FASE 1 EN PROD (26-ago)** — PRD `issues/prd-flota-ia-fase-1.md`, doc maestro artifact `cbac2f09` (fases 2-5, decisiones D1-D11). Reutiliza el pipeline Retell existente (webhook + `sync-retell`). Todo mergeado: #269-#284 (fase 1) y la tanda de cierre **#297-#303**; `main` en `d116daf`. Migraciones aplicadas hasta `…180000`; **no reaplicar `…140000` tras la `…160000`** (dos sobrecargas de la RPC → ingesta a `error`). Estado medido en prod: RPC de 22 args, 9 conexiones en 4 canales (4 en `silent`), 487 en cola de juez, 1.976 ingestas en 48 h con 2 errores.
    **Pendiente, todo de campo**: verificar los 5 schedules de Dokploy (el "✅" del panel no prueba nada), recuperar 3 interacciones sin transcript —están en **dos** clientes: `only=` acepta lista con comas—, reapuntar el webhook post-call de Retell (su firma llevaba rota desde el 22-jun rechazando el 100 %, invisible porque desde el 5-may todo entra por el cron), provocar una interacción real de Kommo (no manda teléfono → sin interlocutor), y mover `SLACK_FLEET_WEBHOOK_URL` al conector de `/agency/admin`. **El CI ya no está rojo**: el check `verify` salió SUCCESS en las cuatro PRs del 1-sep (#598-#601), así que la facturación de Actions se desbloqueó en algún momento entre el 17-ago y hoy.
    Learnings: [[una-verificacion-que-nadie-ejerce-puede-llevar-meses-rota]], [[un-check-que-muere-en-segundos-sin-ejecutar-pasos-es-la-plataforma]], [[el-emisor-que-vive-fuera-del-repo-se-ancla-versionandolo-y-ejecutandolo-en-ci]], [[una-fila-de-auditoria-por-corrida-en-multi-tenant-es-invisible-para-el-resto]], [[la-frescura-del-evento-solo-decide-revivir-nunca-limpiar-el-error]], [[una-fila-de-log-por-evento-y-pasada-con-cron-de-ventana-crece-a-miles-al-dia]], [[verificar-una-migracion-por-columnas-del-constraint-y-en-transaccion]], [[el-umbral-de-un-retry-sale-de-medir-prod-no-de-estimarlo]], [[un-filtro-de-ambito-en-singular-cubre-medio-lote-y-devuelve-ok]], [[dos-prs-que-anaden-seccion-al-mismo-doc-conflictan-siempre-apilalos]].
    **Fase 2, el juez — ENCENDIDO Y JUZGANDO EN PROD (31-ago)**. Las 46 PRs mergeadas (**0 abiertas**, `main` en `cf9631d`), las migraciones aplicadas, los 3 schedules activos y el proveedor puesto en **OpenAI** (`gpt-4o-mini`) desde `/agency/agents/judge`. `fleet-judge` tiene topes de 5 $/tanda y 10 $/día, y su drenaje efectivo medido el 1-sep es de **50 jobs por tanda**. Primera cosecha real: 182 evaluaciones y 134 hallazgos; el peor caso que estima el tope sobreestima ~5× el coste realizado. **El 1-sep se reseteó todo y se arrancó de cero** con el juez ya arreglado (ver abajo).
    - **Qué está aplicado se pregunta al CATÁLOGO**, no a `supabase_migrations.schema_migrations`, que en el self-hosted no existe: `pg_proc` / `pg_constraint` / `information_schema`.
    - **033-bis, el mismo día: los hallazgos no agrupaban nada** — 131 títulos distintos de 134, y 133 con una sola ocurrencia; el proposer de fase 3 (exige 3) no habría disparado jamás. Arreglado con taxonomía de código cerrado (#597), la paginación a 100/500 (#596) y la migración `20260831150000` (`occurrence_count` cuenta solo evaluaciones **vigentes**), aplicada y verificada en catálogo. → [[agrupar-por-hash-de-la-prosa-de-un-llm-no-agrupa-nada]]
    - **El reproceso aterrizó y luego se tiró entero, a propósito (1-sep)** — los 6 lotes cerraron en 645 evaluaciones y 195 hallazgos. Con los tres bugs de abajo ya arreglados se decidió **empezar de cero** en vez de parchear: borradas en una transacción `agent_finding_occurrences` (404) → `agent_findings` (195) → `agent_evaluations` (645) → `agent_evaluation_jobs` (826), conservando `fleet_judge_runs` (25) y sin tocar interacciones ni transcripts. Con eso mueren los tres pendientes que este hub arrastraba (reencolar el prompt viejo, el `UPDATE occurrence_count = 0`, los 28 fallos de cita): no queda nada viejo que reparar. Reencolados **628** con el prompt nuevo, drenaje de **50 por tanda**; primer lote de 50 en vuelo por 0,06 $. Quedan ~12 tandas para vaciar la cola — subir `max_jobs_per_run` en `/agency/agents/judge` si urge.
    - 🔴 **Tres bugs del juez, encontrados por los avisos de Slack de madrugada y arreglados el 1-sep** (#598, #599, #600, todos en `main`, migración `20260901120000` aplicada y verificada en catálogo):
      1. **Tandas fantasma**: el claim que no reclama nada devolvía una fila de nulos (tipo compuesto de PostgREST), el runner se la creía y encolaba y drenaba **sin acotar por agencia**, 9-10 veces por tick y sin dejar fila en `fleet_judge_runs`. 27-30 fallos por tick.
      2. **Huérfanos falsos**: los jobs cuyo transcript purgó el cron salían de `jobRefs` pero su línea de resultado sí venía → 22 avisos de «no correspondían a ningún job», cero desajustes reales.
      3. **`prompt_version` mentía**: se sellaba en la RECOGIDA con la constante compilada, así que un despliegue entre envío y recogida atribuía los veredictos a un prompt que no los generó (34 filas afectadas el 31-ago). Ahora se sella en el SUBMIT; comprobado en prod: 50/50 jobs enviados con su versión.
      Verificado tras desplegar: dos tandas seguidas con `failures: []` y `finished_at`. → [[funcion-que-devuelve-tipo-compuesto-con-return-null-llega-como-fila-de-nulos]] · [[un-discriminador-en-memoria-no-sobrevive-a-la-pasada-siguiente]] · [[un-canal-de-avisos-deduplicado-no-mide-la-frecuencia-del-fallo]] · [[sella-en-cada-fila-la-version-del-codigo-que-la-escribio]]
    - **El prompt no enseñaba a citar como el código valida (#599)** — le faltaba el mínimo de 12 caracteres y su excepción (vale una cita más corta **si es el turno entero**), y en voz casi la mitad de los turnos no llega a 40 caracteres: un hallazgo que solo se sostiene en un turno corto no tenía forma válida de citarse. De ahí salían los 28 fallos de verificación de cita. Presentaba además su regla anti-cita como si fuera el umbral del validador, que en realidad son dos ramas (12 contra `quote` declarada, 40 contra el transcript).
    - **Un `verify` verde no prueba nada si el stub miente** (#601) — un flake de la suite que salía según el reloj resultó ser un stub que no aplicaba el filtro por gravedad que sí hace la consulta real. → [[un-stub-que-no-aplica-el-filtro-de-la-consulta-real-prueba-la-coincidencia]]
    - **La puerta del art. 28.2 por cliente se retiró (28-ago)** — el contrato ya autoriza al subencargado. Cae con ella lo que este hub daba por tuyo: **no hay anexo de Anthropic que firmar** ni 7 clientes que marcar. → [[ADR-060-la-autorizacion-del-subencargado-la-da-el-contrato-no-una-puerta-por-cliente|el contrato autoriza, no una puerta por cliente]]
    Learnings de fase 2: [[columna-de-purga-sin-cron-es-retencion-aparente]] · [[un-gate-que-solo-mide-lo-aplicado-puntua-al-humano]] · [[postgrest-sdk-or-no-compara-columnas]] · [[ADR-059-la-cadencia-del-runner-vive-en-la-bd-no-en-el-cron|la cadencia del runner vive en la BD]] · [[el-trabajo-no-critico-dentro-del-try-hace-que-el-emisor-reintente]] · [[tipo-de-bd-a-mano-sin-relationships-tipa-toda-fila-como-never]] · [[una-suscripcion-realtime-impide-revocar-el-select-de-esa-tabla]].

## Infraestructura — el monitor del portal (`/agency/infrastructure`)

El alta de un servidor **solo se hace desde el formulario del portal**: el único punto de entrada es el Server Action `createServerAction` (`src/lib/dokploy/actions.ts`), y bajo `src/app/api` lo único de Dokploy es el cron `internal/collect-dokploy-metrics`. No hay API de alta, así que no se puede scriptear.

- Clave del monitor: **`~/.ssh/dokploy_portal_monitor`** (ed25519, sin passphrase, una sola para todos los hosts). La genera y autoriza `~/.claude/skills/dokploy-backup-monitor/scripts/monitor-onboard.sh <alias-ssh>`, que además deja la privada en el portapapeles. Creada el 07-ago; antes no existía.
- **No pegar la clave root del host** (la de parchear/reiniciar): el portal solo lee métricas.
- **Pendiente**: DOKPLOYMANU (`185.99.186.76:5251`) ya tiene la pública autorizada y verificada, falta crearlo en el formulario. → [[tecnocloud]]
- Un n8n de Agentesia vive en ese host de Tecnocloud y se cayó en su reboot del 07-ago → [[contenedor-que-no-vuelve-tras-reboot-dos-causas-que-se-confunden]]

## Bloqueos / esperando a terceros

- `notcapi`: mergear **PR #97** de `agentesia-web` (listo) y luego sacar de draft y mergear el **#99** — en ese orden, el #99 depende del #97 (hito 7)
- Dani: correr diagnóstico del hook de time-tracker (ver hito 2)
- GitHub org: subir spending limit / esperar reset de cupo Actions (ver hito 3)

## Links rápidos

- [Github org](https://github.com/AgentesIA-MAdrid)
- [agency-portal](https://github.com/AgentesIA-MAdrid/agency-portal)
- Slack #pro-facturaia, #pro-agentesia
- Canvas Panel TuFacturaIA: F0AV38CHYSJ

## Histórico de hitos

- 2026-07-31: **el chatbot llevaba meses sin guardar un solo lead en la hoja y confirmaba igual** — auditoría sobre las 13 conversaciones reales de Chatwoot, no sobre el prompt en abstracto. `Registro Sheets` del de WhatsApp tenía `columns.value = {}` → error en cada captación, tragado por el AI Agent, ejecución en `success`. Arreglado con las 11 columnas del gemelo web (teléfono desde `chatId`, no del modelo). Prompt web corregido: `Channel (web)`, `urgencia` con valor del enum real, y repregunta obligatoria del sector cuando el cliente responde "negocio propio" (caso Alba, conv 43). `agentesia.world` → `agentesialabs.com` en los dos prompts. Borradas 5 etiquetas residuales de Ecobox en Chatwoot (0 conversaciones las usaban). **Sin verificar todavía**: no ha entrado tráfico desde el fix. Otros huecos medidos y no arreglados: el bot descubre sin cerrar nunca (conv 41, lead CUSTOM perdido tras 5 turnos), promete abrir webs que no puede leer (conv 34) y se va de tema (conv 33, 4 horas hablando de viajes). Ver [[error-de-tool-de-ai-agent-no-marca-la-ejecucion-como-fallida]] · [[if-con-ambas-ramas-al-mismo-nodo-no-hace-nada]]
- 2026-05-02: integración API v1 ↔ agency-portal viva en prod (7 PRs apilados + 7 fixes auditoría)
- 2026-06-16: onboarding WhatsApp caído — cuenta OpenAI sin saldo (`429 insufficient_quota`) → webhook 500 silencioso, bot mudo. Diagnóstico vía ejecuciones n8n. Recargado el saldo (revive sin deploy) + PR #86 para que no muera en silencio. Feature secretaría virtual HGH (PR #87) lista y validada con dry-run.
- 2026-06-22: Pizarra/board (PR #91) + secretaría virtual HGH + fixes de prefill fiscal + extracción onboarding, todo mergeado y cerrado (verificado vía `gh pr view` — el hub llevaba semanas desfasado listándolos como pendientes).
- 2026-07-22: módulo **time-tracking** completo en prod — dashboard + ingesta (#156), dominio prod correcto (#157), límites de uso 5h/semanal (#158), síntesis IA gpt-4o-mini del bloque de trabajo (#160), auto-refresco + filtros (#161), árbol "Trabajando ahora" por persona + fix de solape en cálculo de horas cuando hay sesiones concurrentes del mismo proyecto (#163). CI de Actions confirmado bloqueado por billing (mismo org que TuFacturaIA) — merges verificados en local. Ver [[merged-duration-intervalos-solapados-time-tracking]] · [[claude-code-hooks-no-se-recargan-en-sesion-abierta]].
- 2026-07-22 (tarde): **PR #165 mergeado** (Borja) — dos gráficas nuevas en `/agency/time` (técnico y proyecto) con selector estilo trading (rango/intervalo/métrica desacoplados, intervalo filtrado a 2-60 buckets legibles), coste estimado por proyecto (18,75€/h provisional), tiempo/coste **acumulado** en vez de valor puntual por bucket, leyenda propia clicable. Fix real: al agrupar por proyecto se fusionaba solape entre técnicos DISTINTOS trabajando en paralelo (topaba el bucket como si fuera 1 persona) — corregido sub-agrupando por `member` antes de fusionar. Normalizado el nombre de proyecto en el origen (`service.ts`): cada técnico lo derivaba del nombre de su carpeta local ("AGH Iberica"/"agh-iberica"/"agh_iberica" eran 3 valores), con backfill del histórico ya aplicado en BD. Ver [[metrica-topada-por-bucket-debe-mostrarse-acumulada]] · [[identificador-de-carpeta-local-diverge-entre-maquinas-normalizar-en-ingesta]] · [[merged-duration-intervalos-solapados-time-tracking]].
- 2026-07-22 (noche): **PR #166 abierto** (Manu) — "Trabajando ahora" agrupaba por `(member, project)` y fusionaba en una fila sesiones de Claude Code paralelas del mismo proyecto (Manu con varias sesiones de FacturaIA solo veía una); `buildActiveTree` ahora agrupa por `claudeSessionId`. Aparte, confirmado y arreglado el motivo de que Manu no saliera en "Límites de uso de Claude": su `statusline-command.sh` calculaba el %5h/7d pero nunca lo escribía en `~/.claude/statusline-rate-limits.json` (diagnóstico correcto de Borja, aplicado). Ver [[agrupar-vista-en-vivo-por-clave-de-negocio-colapsa-sesiones-paralelas]] · Stack `claude-code-gotchas.md`.
- 2026-07-25 (tarde): **`/agency/time` — worktrees agrupados bajo su repo + ventana de "activo ahora" alineada** (commit `ce396ec`, en PR #176) — con 5 sesiones abiertas en facturaia el panel solo mostraba 3: `resolveProject()` cortaba en el `.git` del worktree (es un FICHERO con `gitdir:`, no un directorio) y devolvía la rama como nombre de proyecto. Diagnóstico contra `work_sessions` de prod, no por inspección de código: las 2 "perdidas" existían como `modia-003-escape-stack` e `importe-cobrable-retenciones`. Fix en el hook + 10 filas históricas renombradas en prod + `ACTIVE_WINDOW_MS = ACTIVITY_GAP_MS` (franja ciega 10-15 min en la que el bloque seguía extendiéndose pero la sesión no se pintaba). Verificado: 5/5 sesiones bajo `facturaia`. Ver [[git-worktree-dotgit-es-fichero-basename-devuelve-la-rama]] · [[presencia-por-eventos-discretos-ventana-mayor-o-igual-al-gap-y-heartbeat]].
- 2026-07-25: **`/agency/time` — countdown de reset + rediseño de "Trabajo del mes" (mergeado en main vía PR #174)** — `rate_limits.resets_at` solo llega en el input del `statusLine` de Claude Code (no en los hooks estándar); nuevo `statusline-rate-limits-bridge.sh` se instala como `statusLine` (encadenado con el que ya hubiera) y puentea `pct`+`resets_at` al fichero que ya lee `claude-time-hook.mjs`. Gráficas de trabajo por técnico/proyecto rediseñadas a petición de Manu tras 4 iteraciones con mockups en Artifact: de línea acumulada (crecía sin techo) a barras por bucket + panel lateral con el total REAL del periodo (fix real de bug: sumar conteos por bucket para el total daba "18 técnicos" con 3 en el equipo — nueva función `periodTotalsByKey`, no reutiliza la suma de buckets para conteos discretos). 6 rangos (Hoy/Ayer/7 días/Este mes/30 días/Personalizado) × vela 5min-7días, métrica Horas/Coste/Sesiones/Proyectos-Técnicos. Verificado con tests dirigidos (no solo unit tests) contra el escenario real que preguntó Manu: sesiones paralelas del mismo técnico en proyectos distintos SÍ fusionan a tiempo real (no duplican); el mismo hueco repartido entre proyectos/técnicos DISTINTOS correctamente NO fusiona (cada uno se lleva su tiempo real). Ver [[sumar-conteos-por-bucket-sobrecuenta-vs-total-real-del-periodo]].
- 2026-07-23 (madrugada): **`TIME_TRACKER_KEYS` de agency-portal reestructurado y desplegado** — Manu opera 2 cuentas Claude (info + personal), cada una con cupo propio; en vez de tocar esquema, key nueva mintada como member **`manu`** = cuenta personal, y la key vieja de Manu renombrada a member **`info`** = cuenta que ya venía usando. Separación deliberada: el histórico previo bajo `member=manu` queda tal cual; desde este deploy la key vieja reporta como `info`, la nueva como `manu`. Aplicado por API (`application.one` de Dokploy vía wrapper `dokploy-safe.sh` que redacta el env; el guard global bloqueó el intento de leer el env crudo o de sacar credenciales de la BD interna de Dokploy — correcto, el usuario pegó el env final él mismo) + Deploy verificado (contenedor recreado, logs limpios). Key guardada en 1Password `Dani/Borja/Manu` → "Time Tracker Key · manu (cuenta personal, agency-portal)". Ver [[cuota-por-cuenta-externa-sin-deteccion-mint-credential-separada]]. **⚠️ CORRECCIÓN 2026-07-26: este cambio NUNCA llegó al contenedor que corre.** Medido con `curl` al endpoint de ingesta: la key nueva da 401 y la vieja devuelve `{"member":"manu"}`, o sea ni alta de la nueva ni renombrado de la vieja a `info`. "Contenedor recreado + logs limpios" no prueba el contenido del env — de ahí que se diera por bueno durante 3 días. Decisión 26-jul: una sola key, member `manu`. Ver hito 1 y [[verificar-deploy-de-env-por-comportamiento-no-por-contenedor-recreado]].
