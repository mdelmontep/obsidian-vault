---
title: Incidentes resueltos — log cronológico
date: 2026-05-10
source: claude-code-session
tags: [incidents, postmortem, ops]
---

# Incidents log

Una línea por incidente. Formato: `YYYY-MM-DD · proyecto · síntoma → causa → fix (commit/PR)`. Si el aprendizaje universal ya está en CLAUDE.md, dejarlo aquí solo como pointer.

Para incidentes con análisis largo (>1 línea de causa), crear nota separada en `knowledge/incidents/<slug>.md` y enlazarla aquí con `[[wikilink]]`.

Junio 2026 y anteriores → [[incidents-archive-2026-06]] (82 entradas, archivadas el 27-jul).

## Reglas
- **Cronológico inverso** (lo más reciente arriba).
- **Sin secrets**, sin nombres de cliente sensibles. Proyecto = nombre interno (Simarro, TuFacturaIA, agency-portal…).
- Si el fix ya está en CLAUDE.md como anti-patrón, marcar con `→ CLAUDE.md` para evitar duplicar.
- Si pasa de 80 entradas, archivar las >6 meses a `incidents-archive-YYYY.md`.

## 2026

<!-- añade nuevas entradas aquí debajo -->
- 2026-08-03 · cryptobruj-bot · el bot pasó 32 h en `live` en vez de paper (451 ops, PF 0,58, **−1.613,33 USDT**, detectado el 01-ago) → la regla "NUNCA TRADING_MODE=live sin OK" existía en el CLAUDE.md y como casilla de la checklist pre-deploy: prosa que se lee, no guarda que bloquee. Y el modo efectivo ni siquiera está en el repo, lo pone el panel de Dokploy → hook `PreToolUse` `.claude/hooks/live-guard.sh` con dos ramas (local lee `.env`/compose; remoto exige haber confirmado el panel), **fail-closed** cuando el modo es indeterminado, y salida deliberada solo si el token va EN EL COMANDO — leerlo del entorno heredado permitía desarmarlo desde `settings.json` o `.zshrc`, permanente e invisible. Suite `live-guard.test.sh`, 76 casos: **~40 son bypasses que CUATRO rondas adversariales abrieron de verdad** —15 la 1ª, 7 la 2ª, 4 el grafo de auditoría, 10 la 4ª—, cada ronda contra la versión ya endurecida y sin dejarle ver la suite. Entre ellos, cuatro que introduje yo endureciendo: `sed` con `N` en BSD se comía el comando entero; `while read` sin `|| [ -n "$seg" ]` salía sin evaluar nada; un heredoc dentro de `$()` no parsea en el bash 3.2 de macOS; y aplanar los saltos de línea a ESPACIO fundía dos comandos en un segmento, con lo que `cat README.md\npython -m src.main` quedaba exento entero. **Una sola ronda adversarial no basta**: "la suite pasa" solo significa que pasa los casos que se te ocurrieron. **Y auditando la guarda salió algo peor que el incidente original**: `POST /test-order` abría una posición REAL mirando solo si había claves, sin consultar `TRADING_MODE` —la bifurcación paper/live vivía en `place_order`, que ese camino no usa—, y `uvicorn src.api:app` levantaba la API sin pasar por `main()`, saltándose cualquier comprobación de arranque. Cerrado con tres capas independientes: hook → gate G5 contra el bot vivo → `Settings.assert_live_authorized()` en `main()` **y** al importar `src.api`, con `LIVE_CONFIRMED` obligado a igualar `live_strategy` (atar la llave al contenido la hace caducar al cambiar lo que opera; un `=1` en un panel no caduca nunca). Ver [[claude-code-harness]]
- 2026-08-03 · tucrmia/tecnocloud · `autoDeploy` no había disparado NUNCA y el webhook de GitHub daba 500 → el panel servía la cadena TLS **sin el intermedio** (`curl` en macOS lo salvaba y engañaba) → añadido el intermedio al certificado del panel por `certificates.update` (parcial, sin que viajara la clave privada) + recarga de Traefik, que es lo que lo materializó. Ver [[cadena-tls-incompleta-curl-en-macos-la-salva-y-engana]] · [[dokploy-guarda-en-su-bd-y-no-toca-el-disco]]
- 2026-07-30 · TuFacturaIA · Chivite no podía emitir una factura, "No se pudo emitir la factura" tras cada reintento y cada relogin (ticket #117) → el `raise sobreventa_lote` del motor de partidas (migs 308/312/388) no estaba mapeado en `emitirBorrador` y salía por el 500 mudo, así que la causa (falta stock en la partida) solo vivía en los logs → 409 `stock_insuficiente` nombrando partida o producto (#1381). El cliente se apañó guardando y reabriendo el borrador, que reasigna partida por FEFO; no entró género → [[workaround-que-le-funciono-al-cliente-no-es-la-explicacion-de-la-causa]]















## Archivo
Incidentes de **mayo 2026 y anteriores** movidos a [[incidents-archive-2026]] (2026-07-13). Aquí solo lo reciente (jun–jul).

- 2026-07-25 · FacturaIA · ningún modal de la app abría en LOCAL durante días, sin error en consola (el foco volvía al botón: era el modal cerrándose solo al abrirse) → `useCloseOnRouteHide` cerraba desde el cleanup de un `useLayoutEffect`, y el desmontaje sintético de StrictMode lo disparaba al montar; llegó en `d35b70a5` (#1140), **cuyo propio título decía "[WIP · no mergear sin QA]" y se mergeó igual** → aplazar la decisión un microtask, PR #1210. Proceso: un PR que se autodeclara no-mergeable no debería poder mergearse. Ver [[cerrar-un-overlay-desde-el-cleanup-de-un-efecto-lo-rompe-en-strictmode]].
- 2026-07-25 · FacturaIA · ficheros SEPA con tilde/`&` en el nombre del cliente o del acreedor los rechaza el banco tras subirlos → nunca se saneó al juego de caracteres EPC217-08 (escapar XML no lo cubre) y faltaba el límite de 70 en `Cdtr/Nm`/`Dbtr/Nm` → `toSepaText` en pain.008 y pain.001 + límite, PR #1201. Ver [[escapar-xml-no-es-sanear-al-charset-del-protocolo]].
- 2026-07-22 · TuFacturaIA · cliente manda ticket por WhatsApp, OCR no lo reconoce y el bot se queda mudo (cliente sin saber que falló); reintento manual funcionó pero registró `forma_pago="Efectivo"` sin que el ticket lo dijera → 2 causas: `noLegibleComoFactura` no notificaba por WhatsApp en ningún exit temprano del pipeline, y el prompt no tenía regla anti-invención para `forma_pago` (a diferencia de NIF/IRPF/ref. catastral). Fix PR #1156 (aviso WhatsApp + REGLA 17). Ver [[ocr-prompt-campo-sin-regla-anti-invencion-se-alucina]] · [[pipeline-async-solo-notifica-camino-feliz-deja-fallo-silencioso]].
- 2026-07-18 · TuFacturaIA · sub-menú de Obras en el sidebar: opciones visibles pero **no clicables** (reportado sobre prod, deploy ya al día → no era caché) → regresión #993: el grupo desplegable envuelve sus sub-enlaces en `.nav-sublist`, pero el colapso animado (`.nav-collapse` grid 0fr) solo definía `overflow:hidden/min-height:0` sobre `.nav-list` → con el grupo cerrado (estado inicial en `/`) los sub-items se desbordaban visibles Y quedaban `inert` → fix #995 (extender colapso a `.nav-sublist` + anular su margen al cerrar). Diagnóstico con repro de navegador (grupo cerrado 65px→0px). Ver [[colapso-grid-0fr-child-divergente-queda-inert-visible]].
- 2026-07-03 · TuFacturaIA · alertas `/admin/system?tab=alerts` "OCR de facturas en fallo" (×2 sandbox, ×6 org real "Borja Galván") → email transaccional de Salt Edge con logos/iconos embebidos vía `cid:` sin `Content-Disposition` colado como si fueran facturas (ver ADENDA [[ingesta-email-imagenes-inline-firma-tratadas-como-adjuntos-facturables]]) → gpt-4o rechaza correctamente, pero cada rechazo generaba 502+alerta+fila zombi. Fix PR #675 (cross-check Content-ID vs `cid:` en HTML + tamaño mínimo 15KB, 3 providers). 15 filas basura (`bandeja_ingesta`+`facturas`, estado `sin_aprobar` sin datos) limpiadas en BD tras nullificar FK. De paso: org huérfana "Empresa Test Screenshot" (0 miembros/datos) borrada + timezone del cron `agentic-gate-sweep` corregido a `Europe/Madrid` (estaba en `None`/UTC).
- 2026-07-03 · TuFacturaIA · alerta "Cron en fallo: gate de acierto agéntico" → auditoría reveló 2 crons del `CRON_REGISTRY` (`agentic-gate-sweep`, `slack-dispatcher`) sin schedule real en Dokploy nunca creado (0 runs históricos, sin alerta por [[cron-health-desconocido-para-cron-sin-ningun-run]]) + `verifactu-process` (cron fiscal más crítico) deshabilitado desde el 16-may sin que nadie lo notara (0 orgs con Veri*Factu activo aún, sin impacto real). Al re-habilitarlo lo migré por error a HMAC v2 pese a tener auth custom ya documentada → repetido, ver [[dokploy-cron-docker-exec-no-hereda-env-de-app-env]] ADENDA 3. Fix: 2 schedules nuevos creados + verifactu-process restaurado a su comando original + limpiados 3 schedules duplicados (`mcp-dcr-cleanup` x2, `copiloto-recordatorios-batch` x1, `verifactu-process` x1 accidental de esta misma sesión).
- 2026-07-01 · TuFacturaIA · cambios propios (`types.ts`, `ingesta-view.tsx`) mezclados sin querer en un commit ajeno (`68a0f25c`) ya pusheado a main → sesión paralela en el mismo checkout sin worktree hizo `git add -A` sobre mis cambios sin commitear. Sin daño funcional (build/tests verdes); refuerza el inviolable ya documentado de `git worktree add` por sesión paralela en CLAUDE.md.
- 2026-07-01 · TuFacturaIA · Ticket rápido (feature nueva) en prod quedaba `estado='pendiente'` sin marcar cobrado → contaminaba KPIs "por cobrar"/cashflow/aging/vencidas con dinero ya cobrado en efectivo/Bizum. Fix same-day: `marcarFacturaCobrada` tras `createDocument`. Ver [[documento-dinero-ya-cobrado-debe-marcarse-cobrada-explicito]].
- 2026-07-02 · TuFacturaIA · worker VeriFACTU no-op silencioso desde 2026-05-18 → el select central pedía `factura_origen_num` (columna solo de la vista `facturas_con_autor`) sobre la tabla `facturas` → 42703 tragado, cron reportando `{ok:true, procesadas:0}`; 0 orgs activas aún, pero el cutover habría enviado nada a AEAT sin alerta. Destapado al tipar el cliente (PR #648); fix: columna fuera del select + fallback muerto eliminado. Ver [[supabase-select-columna-inexistente-falla-query-entera-42703]].
- 2026-07-02 · TuFacturaIA · misma clase 42703 silencioso en 9 sitios más (PR #648): emails siempre con branding fallback (`organizations.web` inexistente), duplicar presupuesto v1 siempre 404 (`forma_pago`), `is_admin` al crear org nunca concedido (upsert a `profiles.id`, PK es `user_id`), fallback PDF email nunca renderizó (`facturas.lineas`), export fiscal sin NIF (`nombre_fiscal`), getURLPDF presupuestos roto (`documento_url`), acciones admin globales sin auditar (`audit_log.org_id` NOT NULL), /api/health sin reportar modo lectura (shape `enabled` vs `active`).
- 2026-07-02 · TuFacturaIA · el worker VeriFACTU tenía una SEGUNDA capa muerta: `/api/verifactu/process` no estaba en `isServiceRoute` → middleware 307 a /login antes del handler, y el `curl -sf` del cron trataba el 307 como éxito → 0 runs en cron_runs en toda la historia. Fix 1 línea PR #649; smoke verde (primer run: success, procesadas:0).
- 2026-07-03 · TuFacturaIA · Libro IVA XLSX 500 permanente en prod (misma clase 42703, destapado diseñando export contable) → select a columnas x100 inexistentes en `lineas_factura` (cast local anulaba typecheck) → loader compartido derivando de subtotal×iva_pct como el motor fiscal (PR #653). Audit 3-agentes añadió: paginación max-rows, chunking .in(), periodificación devengo, trial fiscal server-side. Ver [[supabase-select-columna-inexistente-falla-query-entera-42703]] · [[postgrest-max-rows-trunca-silencioso-in-revienta-url]].
- 2026-07-03 · TuFacturaIA · cliente real (org Abba) no podía emitir factura a empresa con NIF ya relleno → `validation_error` genérico → fix a medias en el 1er intento: PR #665/#666 arreglaron que la card/factura mostraran `empresa` en vez del contacto (autocomplete de cliente nunca mandaba `empresa` al backend), pero el payload de "cliente ya existente" solo llevaba `{id, nombre, empresa}` — sin `nif`, que antes tampoco viajaba pero el bug estaba enmascarado porque `assertNifReceptor` nunca se disparaba (sin `empresa` nunca detectaba "hay empresa sin NIF"). Al arreglar `empresa`, la validación se activó por primera vez y expuso el hueco del NIF. Fix PR #667: la rama "cliente existente" manda los mismos campos que "cliente libre". De paso, el toast mostraba el CÓDIGO `validation_error` crudo en vez del mensaje explicativo (leía `error` en vez de `message` de la respuesta). Ver [[payload-parcial-cliente-existente-oculta-bug-hasta-activar-validacion-relacionada]].
- 2026-07-03 · TuFacturaIA · /informes en prod: rangos de trimestre corridos un día (widget IVA/comparativa/CSVs incluían el último día del trimestre anterior), columna IVA=total−base (mal con IRPF), cards muertas 'Modelo 303 XML'/'Enviar al gestor' → `new Date().toISOString()` en local + CSV a mano → PR #657 (rangoIsoDelPeriodo + cards conectadas al export contable). Ver [[new-date-toisostring-desplaza-un-dia-fuera-de-utc]].
- 2026-07-03 · TuFacturaIA · pooler Supabase (`aws-*.pooler.supabase.com:5432`) inalcanzable a mitad de `supabase db push` (timeout a las 3 IPs) → mig 426 aplicada vía MCP `execute_sql` (API HTTPS sigue viva) + `gen:types --linked`; columnas en prod pero pendientes de registrar en `schema_migrations` con `db push` al recuperar (no-op por IF NOT EXISTS). Ver [[supabase-pooler-caido-aplicar-ddl-via-mcp]].
- 2026-07-03 · TuFacturaIA · bot WhatsApp 'amnésico' y con cifra/trimestre erróneos (3 capas): orden del intent multi-org (#662) + `void` sobre builders supabase = writes nunca ejecutados (#676, ver [[supabase-builders-lazy-void-nunca-ejecuta]]) + hilo con 100 mensajes anclando la respuesta vieja (reset + regla 'datos frescos mandan' #681, ver [[llm-hilo-envenenado-ancla-tools-frescos-mandan]]). Además: céntimos-string en consultarIvaTrimestral y v1 (#676), estimación en vivo (#678), trimestre actual + formato es-ES (#679), respuesta dual a-presentar/en-curso (#680). Verificado por el endpoint interno real: respuesta dual correcta.
- 2026-07-03 · TuFacturaIA · plantilla custom: nombre de empresa bajo el logo seguía saliendo pese a toggle en `false` en BD → `sanitizeBlockOpts('cabecera')` reconstruye el objeto por allowlist y corre en CADA render (no solo al guardar); `mostrarNombre` no estaba en la lista → se descartaba siempre. Fix + tests de regresión. De paso, bug real independiente: `clientes/proveedores.empresa` (razón social) no se seleccionaba en 5 sitios (listas, modal, asignar-cliente, generación PDF), así que siempre salía el contacto en vez de la empresa aunque las plantillas ya preferían `empresa`. Ver [[sanitizador-allowlist-reconstruye-objeto-descarta-campos-nuevos]] · [[campo-opcional-en-tipo-compartido-no-implica-seleccionado-en-todos-los-selects]].
- 2026-07-03 · TuFacturaIA/Dokploy · fuga de secrets de prod en chat: `compose.one` llamado solo para revisar si un deploy había terminado devolvió el `env` COMPLETO del compose (service role key, API keys LLM, tokens WhatsApp, claves de cifrado, etc.). Sin daño explotado conocido; usuario decidió revisar y rotar por su cuenta. Ver [[docker-infra]] (gotcha `compose.one`/`schedule.one`).
- 2026-07-04 · TuFacturaIA · `supabase db push` (mig 428 datos-backups) abortó: la BD prod tenía 2 migraciones con versión TIMESTAMP (`20260703141924/141936` = 426/427, el `name` lo delata) que una sesión paralela aplicó ese día, no en el repo → CLI bloquea. Reconciliado con `migration repair --status reverted <timestamps>` + `--status applied 426 427` (SQL ya en prod, idempotente) → `db push` aplicó solo la 428. Anti-patrón timestamp-migration ya en CLAUDE.md del proyecto. Ver [[extender-database-supabase-interface-never-anyclient]].
- 2026-07-04 · TuFacturaIA · docs del roadmap G5 (ADR + `g5-capa-a-plan.md`) daban por pendiente la retirada del Agente Facturador n8n y la Capa B (voz/OCR) → auditoría en vivo (API n8n `/executions` + git log + lectura de `system-prompt.ts`/`registry.ts`/webhook route) mostró que el cutover del webhook Meta→Next.js y la consolidación de voz+OCR YA estaban en `main` (commits `8dc30888`/`dd07fef7`/`8dff6a90`); solo queda huérfano el nodo `Agente Facturador` + 16 toolCode en n8n (issue `g5a-005` reabierto con gate de auditoría de tráfico real antes de tocarlo). Refuerza: verificar contra código/API en vivo antes de planificar sobre docs propios, aunque el usuario diga "contexto ya verificado". Ver [[integracion-en-jsonb-tabla-generica-pierde-observabilidad]] (mismo espíritu de verificación).
- 2026-07-04 · TuFacturaIA · casi-incidente: intento de auditar ejecuciones reales de n8n con `op read` timeout de autorización → pegué el JWT del API key literal en el comando bash como fallback → el clasificador de seguridad de Claude Code lo bloqueó correctamente antes de ejecutar nada dañino, pero quedó expuesto en el comando/transcript de la sesión. Sin explotación conocida. Fix: nunca hardcodear el secreto ni siquiera para un `curl` de solo lectura; parar y pedir reautorización de `op` al usuario. Ver [[op-read-secreto-nunca-en-comando-bash-ni-desde-memoria]].
- 2026-07-04 · TuFacturaIA · RECURRENCIA falso positivo cron: email incidencia ALTA "Cron en fallo: Limpieza conectores MCP (DCR)" con TODOS los runs `success`. `mcp-dcr-cleanup` (purga auto-sanable, borra 0 filas) se saltó UN disparo diario (único cron que falló ese día); umbral 36h (<2×24h) lo cruzó a las 12:00 UTC. El fix #393 (2×intervalo, 2026-06-19) no lo cubría. Fix PR #703: `CronMeta.criticidad:'housekeeping'` → collector emite 'media' (sin email) para mantenimiento auto-sanable + umbral→48h + `expresion` registry corregida (50 4→0 0). Autorresuelto tras run manual (schedule.runManually). Ver [[cron-mantenimiento-auto-sanable-no-debe-paginar-severidad-por-criticidad]].
- 2026-07-04 · TuFacturaIA · login con mitad derecha en blanco en iPad portrait (768px exacto): media queries MOBILE AUTH (`max-width:768px`) y TABLET AUTH (`min-width:768px`) se solapaban en ese punto exacto — rejilla pasaba a 2 columnas pero `.auth-left` seguía oculto por la regla mobile. Detectado y arreglado end-to-end por el runner "Resolver con Claude" (ticket `9edebeee` → PR #704, ciclo 3, sin bugs en revisión manual). Fix: límite mobile a `max-width:767px`. Ver [[facturaia]] §NOW/NEXT y `Stack/frontend-css-mobile.md` §Mobile Auth.
- 2026-07-05 · TuFacturaIA · sidebar de `/settings` desplegado verticalmente en iPad portrait (768-1100px) tapaba el contenido — ticket de soporte del usuario. Causa: el modo compacto (barra horizontal de chips) solo activaba bajo 767px, mientras que el layout de 2 columnas se rompía ya a 1100px; la franja intermedia se quedaba sin columna fija Y sin el tratamiento compacto. Fix PR #754: unificado a un solo breakpoint 1100px. Mismo espíritu que el incidente de login del día anterior (breakpoints inconsistentes en dos zonas del mismo componente). Ver `Stack/frontend-css-mobile.md`.
- 2026-07-05 · TuFacturaIA · bot WhatsApp subía imagen/PDF siempre a la misma org (AgentesiaLab SL) ignorando la org elegida segundos antes por texto (Sandbox) — smoke manual del usuario lo detectó. Causa: `processMedia` resolvía por `phone_number_id` (número receptor Meta) en vez de por remitente; con 2 orgs compartiendo el mismo número personal, solo la que tenía `phone_number_id` real wireado ganaba siempre. Fix PR #764: `from_phone` primero (respeta sticky), fallback a `phone_number_id` solo si el remitente no es miembro de ninguna org (preserva el flujo de proveedor externo, wa-fase2-001). De paso, selector de empresa unificado a lista interactiva en texto y documento (antes 3 botones vs. lista). Ver [[whatsapp-multi-tenant-matching-debe-ser-por-numero-remitente-no-por-phone-number-id]].
- 2026-07-05 · TuFacturaIA · PR #705 (cobro Stripe) rompió `migrated-tools-gate.test.ts` en main — `createOrGetPaymentLink(createAdminClient(), ...)` reintrodujo `createAdminClient` en 2 tools ya migradas al patrón port (`enviarEmailFactura.ts`/`enviarRecordatorioPago.ts`), justo el seam que el gate prohíbe. Ningún caller reutilizaba el client para nada más. Fix PR #765: `createOrGetPaymentLink` crea su propio admin client internamente (mismo patrón que `sendFacturaByEmail`). Suite completa 469/469 tras el fix. Ver [[helper-di-client-mover-dentro-si-ningun-caller-lo-reutiliza]].
- 2026-07-05 · TuFacturaIA · reverificando el fix #764 en real: el selector multi-org funcionó bien, pero tras elegir empresa el bot se quedó completamente mudo (ni éxito ni error). Confirmado por BD (`whatsapp_pending_intent` borrado solo tras descarga exitosa) que el flujo llegó hasta el envío final; logs del contenedor sin ninguna traza después de ese punto. Causa: `postToGraph` (transporte base de todos los envíos WhatsApp) nunca logueaba sus fallos, y ~35 call sites del webhook usan `void sendText(...)` sin mirar el resultado — un fallo real de Meta (token/rate-limit/restricción) quedaba 100% invisible. Fix PR #768: loguear errorCode/errorText en el único punto de transporte compartido (sin PII). No resuelve la causa de fondo del fallo de Meta en sí — solo lo hace diagnosticable la próxima vez. Ver [[fire-and-forget-resultado-descartado-esconde-fallo-loguear-en-transporte]].
- 2026-07-05 · TuFacturaIA · siguiendo la investigación del bot mudo: pregunta del usuario ("¿si spamean el botón 10000 veces siempre responde?") destapó una 2ª causa latente de silencio — el fetch del webhook a `/api/internal/whatsapp/copiloto` no comprobaba `res.status`; bajo el rate-limit interno (429, ~60-120 req/min anti-bucle) el body de error caía en la rama genérica `copiloData.error` (solo log, sin respuesta). Fix PR #769: mensaje de cortesía ("espera unos segundos") si el status es 429, en los 2 puntos que llaman al copiloto (texto y audio). Recurrencia del patrón ya documentado — ver [[internal-fetch-res-ok-silencioso]].
- 2026-07-07 · TuFacturaIA · 4 emails incidencia ALTA "Cron en fallo" (slack-dispatcher, data-export-runner, drive-sync-dispatcher, conciliacion-enrich-batch) SIN fallo real → día de blips Cloudflare/Supabase (520/522) tumbó 1 run de cada cron y el siguiente se recuperó solo (auto-resueltas 10-20 min; 99.9% éxito 24h; 7 días previos con 0 fallos). Causa: `computeHealth` paginaba ALTA ante 1 sola observación mala (1 `error`, o 1 `running` colgado antes de que el watchdog lo reapee como zombie). Fix PR #790: exigir ≥2 fallos consecutivos para 'rojo'/email; 1 blip → 'ambar' (panel, sin email). Ver [[cron-alta-frecuencia-exigir-2-fallos-consecutivos-antes-de-paginar]].
- 2026-07-07 · TuFacturaIA · pérdida real de datos en cuenta de cliente (Dani, Holded — no un trial vacío) durante el smoke E2E de la integración: al activar push+pull de contactos, el pull importó 409 contactos reales y el trigger de BD los re-encoló para push de inmediato; el push hacía `PUT` del contacto completo con solo el subconjunto de campos que mapea FacturaIA → Holded reemplazó el objeto entero, perdiendo `iban`/`swift`/`trade_name`/`client_record`/`supplier_record`/idioma/moneda en los 409 contactos. Dani confirmó después que los datos no se habían perdido realmente, pero la decisión ya estaba tomada: eliminar el push por completo (mig 445, PR #792), pull-only para siempre. Ver [[put-objeto-completo-borra-campos-no-mapeados]] · [[facturaia-holded-integracion]].
- 2026-07-09 · TuFacturaIA · el deploy #802 (cola OCR) retiró el endpoint `ingesta-stale-sweep` pero su schedule Dokploy quedó enabled → 404 cada 30 min, invisible en `cron_runs` (un 404 no crea fila; la fila se escribe dentro del endpoint). Detectado consultando la tabla `schedule` de la BD interna de Dokploy por SSH read-only. Fix: `schedule.delete` del huérfano + del `mcp-dcr-cleanup` duplicado muerto (42→40 schedules). Ver [[cron-endpoint-retirado-deja-schedule-externo-huerfano]].
- 2026-07-10 · TuFacturaIA · Supabase (prod) superó la cuota de **egress** → proyecto restringido → prod devolvía `404 page not found` de Traefik (no la app), login roto y TODOS los crons/OCR en fallo con la misma línea `exceed_egress_quota`. El contenedor Docker seguía `(healthy)` (engaña: es otro healthcheck). Probable acelerador: QA pesado del día (lotes de PDFs + previews + capturas). Fix: subir Supabase a Pro; al restaurarse, Traefik re-registró la ruta solo. Ver [[supabase-egress-restringido-tumba-app-via-healthcheck-traefik-404]].
- 2026-07-10 · TuFacturaIA · el cutover del receptor WhatsApp n8n→app (workflow `pqSWkDIHqmSVHotB` desactivado ~4-jul) dejó 2 flujos huérfanos que estuvieron rotos en prod sin avisar: (1) **opt-out de cobros** — cliente que responde «STOP» a un recordatorio seguía recibiéndolos (n8n detectaba STOP y llamaba `cobros-stop`; el webhook nuevo no lo replicaba) → compliance/spam; fix #814. (2) **extractos/justificantes por foto** — se clasificaban y descartaban en silencio esperando el rebote n8n→`movimiento-ingesta`; con n8n fuera se perdían; fix #816 (mig 451). Detectado auditando el nodo n8n retirado contra el nuevo receptor (paridad de flujos), no por un error visible. Lección: un cutover de topología exige auditar CADA rama del componente retirado, no solo el path principal. Ver `project_facturaia_retirada_n8n` (memoria agente).
- 2026-07-11 · TuFacturaIA · DNIs reenviados por email caían en el OCR → gpt-4o rehúsa el documento de identidad (200 con `"I'm sorry,"`/`"Lo siento,"` no-JSON) → `JSON.parse` fallaba → 502 → el worker reintentaba ×3 → estado `error` + alerta ALTA "OCR de facturas en fallo" (FALSA: saltó en 3 orgs reales — Borja Galván, FacturaIA Sandbox, Laserys). Fix #834: un 200 no-parseable → revisión manual sin reintento ni alerta; retry+alerta reservado al fallo real de API (non-2xx). El conector de email ingesta cualquier adjunto (no filtra por asunto) — correcto: clasificar en el OCR, no por keyword. Ver [[llm-responde-200-no-json-no-es-fallo-pipeline]].
- 2026-07-13 · TuFacturaIA · `copiloto-insights` (cron nuevo, PR #865) no disparó: el schedule Dokploy tipo compose se creó SIN `serviceName` → `runManually` 500 "invalid container name" y el trigger no ejecutaba → 0 filas en `cron_runs` → invisible al watchdog (un cron que NUNCA corrió = `desconocido`, no alerta — mismo blind spot que el huérfano del 07-09 pero por el otro lado). Fix: recrear con `serviceName=facturaia`+`shellType=bash`+`timezone` en el CREATE (el `update` no re-registra el trigger vivo). De paso corregida la TZ de cashflow-alerts/fiscal-avisos/stock-alarmas-email (corrían en UTC = 2h tarde) → Europe/Madrid. Ver [[dokploy-compose-schedule-requiere-servicename]]. **Corolario (misma tarde/noche):** ese `schedule.update` de TZ NO re-registró el trigger vivo → `fiscal-avisos`+`stock-alarmas-email` PERDIERON el disparo del 13 → 2 emails ALTA del health-sweep a las 22:00 CEST (cuadra: último éxito 12 08:00 UTC + max_intervalo 36h). Relanzados a mano vía `ops/cron/sign-call.sh` (HMAC) contra prod (stock: 1 email real/5 productos; fiscal: sin pérdida, hitos plazo_7d ya existían). Regla: tras CUALQUIER `schedule.update`, relanzar a mano (`runManually`/sign-call) o se pierde el disparo del día. **Cierre 2026-07-14:** verificado en `cron_runs` que `fiscal-avisos`+`stock-alarmas-email` dispararon SOLOS el 14 (06:00/06:05 UTC = 08:00/08:05 Madrid) ya con TZ Europe/Madrid → triggers sanos, el cambio de TZ solo costó el disparo de ese día. Incidente cerrado.
- 2026-07-13 · TuFacturaIA · la API de Dokploy (`schedule.one`/`compose.one`/`application.one`) vuelca el `compose.env` completo en plano → el 07-03 un agente lo volcó a chat (service role, Stripe, WhatsApp, Resend, claves de cifrado, TrueLayer/Tink, B2, N8N) → guardarraíl DURO: hook global PreToolUse (Bash+WebFetch) que bloquea las llamadas crudas + wrapper redactor `dokploy-safe.sh` (PR #870, Capa 1 cerrada+deploy verificado). Rotación de secrets (Capa 2) y sacar secrets del env (Capa 3) pendientes. Ver [[dokploy-api-get-schedule-list-expone-env-completo-secrets]].
- 2026-07-13 · TuFacturaIA · al aprobar una recibida a mano (web, individual o masiva) el **nº de factura no se copiaba** de la ingesta: el cliente leía `d.num` pero la clave canónica de `bandeja_ingesta.datos_extraidos` es `num_factura` → `undefined` silencioso (JSONB sin tipo TS). El flujo servidor (n8n/ocr-process) sí usaba `num_factura`. Hallado auditando el shape durante un QA de rendimiento, no reportado. Fix #875: helper único `buildApprovalUpdates`. De paso: aprobación masiva de N×2 round-trips en serie → batch+pool+progreso. Ver [[accion-masiva-cliente-n-round-trips-serie-cuelga-usar-batch-y-pool]].
- 2026-07-14 · TuFacturaIA · "Aprobar seleccionadas" en recibidas dejaba filas **sin aprobar en silencio** (y "seleccionar todo" no deseleccionaba) → la clave de selección era `docKey = num ?? id`, pero los números de recibidas se repiten (mismo `num` de proveedores distintos, scans dup) → `data.find(docKey===key)` resolvía solo la 1ª fila por número y `toggleAll` (`size===length`) nunca limpiaba. Hallado en QA del usuario (screenshots). Fix #888: `docKey = f.id`. Ver [[clave-seleccion-fila-debe-ser-id-unico-no-campo-de-negocio]].
- 2026-07-14 · TuFacturaIA · escaneos (PDF) subidos del revés → OCR devolvía "Sin nombre / 0 € / nº = nombre de archivo" (gpt-4o no lee un PDF a 180°). Fix #888: auto-orientación (reintento rotado en fallos, pdf-lib para PDF + sharp para imagen) + rotar manual + toggle admin. **Verificado en prod (Alpine)**: PDF fallido → `Tamme Air Conditioners · 254,21 €` persistido. Ver [[openai-vision-lee-pdf-del-reves-rotando-con-pdf-lib]] · [[dep-nativa-import-dinamico-defensivo-en-ruta-api]].
- 2026-07-15 · TuFacturaIA · Holded importó 409 contactos (clientes+proveedores) **todos a `clientes`**, 0 a `proveedores` → el pull decidía por `type` e ignoraba `client_record`/`supplier_record` (la señal real de rol en Holded v2; un contacto puede ser ambos). Fix #908: `contactRoles()` + ruteo por rol + `entity_type` `contact_client`/`contact_supplier` (mig 462) + script de reconciliación idempotente. Ver [[holded-v2-contacto-rol-por-record-no-por-type]].
- 2026-07-15 · TuFacturaIA · pull Holded daba 4 errores/ciclo `insert proveedores duplicate key` → `PROVEEDOR_COLS` pedía `updated_at` (col que `proveedores` NO tiene) → 42703 → `findByNif` tragaba el error (null) → INSERT en vez de link → choque unique `lower(nif)`. Fix #918 (quitar col + throw). El síntoma estaba a 3 saltos de la causa; ~1h perdida persiguiendo el deploy (deployments[]=done pero mismo-commit redeploy = no-op). Ver [[column-list-drift-clientes-proveedores-select-inexistente-42703]] · [[dokploy-redeploy-mismo-commit-es-no-op-cambiar-sha-fuerza-recreacion]].
- 2026-07-19 · TuFacturaIA · 7 páginas de módulos gated (cashflow, fiscal ×3, inventario, recurrentes, remesas) devolvían **HTTP 500 en su rama UPSELL** — las orgs SIN el módulo (las de conversión) veían un crash en vez del EmptyState "activar módulo". Causa: `buttonClassName` (función pura) vivía en el módulo `'use client'` `button.tsx`; llamarla desde el Server Component revienta en runtime (Next 16) y el `build` no lo caza. Latente semanas: las orgs de test tienen todos los módulos → happy path, nunca renderizan la rama upsell. Destapado por el smoke E2E del enlace fiscal (misma raíz, PR #1022). Fix #1025: extraer `buttonClassName` a `button-class.ts` sin `'use client'` + re-export (0 consumidores client tocados). Ver [[server-component-no-puede-llamar-funcion-use-client]].
- 2026-07-19 · TuFacturaIA · editar un material no guardaba (y fijar la duración del kill-switch de mantenimiento fallaba) → `<input number>` con `min`/`step` incompatibles = `stepMismatch` → `checkValidity()` false → `requestSubmit()` no enviaba, sin error visible. Fix `step="any"` (#1030 materiales, #1034 admin). Ver [[html-stepmismatch-min-step-bloquea-submit]].
- 2026-07-20 · TuFacturaIA · **Obras: catálogo entero valorado a 0 € de material** en prod. Motor de precio: `PFVC_min = COALESCE(MIN(PFVC entre proveedores), 0)` → material sin proveedor enlazado (todos tras importar Telematel) se vendía solo por mano de obra; accesorio de tarifa 213,97 € salía a 0,00 €. El "=0" era salvaguarda del código, no regla de negocio. Fix #1100 (mig 530): sin proveedor, coste = base×(1+margen); recalculado el catálogo. Ver [[coalesce-agregado-0-anti-null-puede-ser-regla-negocio-erronea]].
- 2026-07-20 · TuFacturaIA · main con **typecheck rojo enmascarado** (2º del día, misma clase): #1091 (drag-scroll) mergeó con un `@ts-expect-error` muerto en el test — el fix estaba en el working tree pero NO en el commit antes del `push`. Gates solo-PR + `--admin` no revalidaron. Fix #1094. Lección: tras editar post-commit, `git add` + verificar `git status` limpio ANTES de push. Ver [[actions-sin-billing-hooks-locales-unico-gate]].
- 2026-07-20 · TuFacturaIA · main quedó con **lint rojo enmascarado**: #1086 (overhaul materiales) mergeó con 2 `setState`-en-effect (`react-hooks/set-state-in-effect`) que los gates solo-PR + merge `--admin` no revalidaron. Como el pre-commit hook lintea TODO el repo, bloqueaba cualquier commit ajeno (destapado al tunear el botón «Ask»). Fix #1087: `eslint-disable-next-line` justificado (loaders async legítimos). Patrón: `--admin` + gates solo-PR → main puede ir rojo y frenar al siguiente. Ver [[actions-sin-billing-hooks-locales-unico-gate]].
- 2026-07-20 · TuFacturaIA · Unidad de Obra con una sub-unidad sin materiales mostraba **precio 0 falso** → el array `lineas` de `obras_uo_calcular` traía `importe: null` en ese grupo (sin COALESCE por-línea; el total sí lo llevaba) y el parse Zod `z.number()` estricto rechazaba TODO el objeto y zerificaba el precio → mig 532 (COALESCE por línea) + parse `nullable→0`. Llegó a prod en #1102; lo cazó `/fia-cierre` (no el QA en vivo, que solo probó líneas con material); fix #1105 verificado en vivo. Ver [[agregado-coalesce-total-vs-null-por-grupo-parse-estricto]].
- 2026-07-22 · TuFacturaIA · #1140 (Cache Components E0-E3) se mergeó a main con un **crash real sin arreglar**: bajo `<Activity>`, ocultar `/emitidas` con el visor PDF abierto y volver → `Cannot read properties of null (reading 'sendWithPromise')` inmediato (react-pdf pinta sobre un `PDFPageProxy` cuyo transporte ya fue destruido al ocultar la ruta; el estado React no se resetea, solo se limpia el efecto interno de pdf.js). Causa raíz de fondo: dos sesiones paralelas trabajando el mismo tema (Cache Components) sin coordinarse — una mergeó #1140 sin saber que la otra ya tenía el bug detectado. Detectado en QA de regresión DESPUÉS del merge, ya en prod unas horas. Fix mismo día (#1142, HOTFIX): `PdfRecoveryBoundary` (error boundary + remount por `key={gen}`, tope 1 reintento). Verificado con repro determinista 2×/2× antes/después, auditoría propia confirmó que ningún otro componente usa un recurso imperativo similar (Worker/canvas/WebSocket) vulnerable al mismo patrón. Ver [[nextjs-activity-no-resetea-estado-libreria-externa-con-recurso-imperativo]].
- 2026-07-22 · Centro Elphis · lead que pidió callback ("me podéis llamar") no generó **ningún aviso interno** (ni WA ni email) al equipo → el guard anti-doble-deal en Clientify (`idempotency_log`, TTL 30 días por contacto) cortaba toda la cadena de notificación, no solo la creación del deal duplicado; y `should_notify` dependía de que el deal fuera nuevo (`was_created`), así que la rama "deal ya existe" tampoco avisaba nunca. Además `pause_bot` (handoff a humano en chat) no llamaba a nada. Fix en `registrar-lead` (dedup de notificación separado, 60min, de la idempotencia del deal) + wiring `pause_bot`→`registrar-lead`. Desplegado y verificado con ejecuciones reales de n8n. Ver [[idempotencia-de-entidad-no-debe-gatear-notificacion-side-effect]]
- 2026-07-23 · Centro Elphis · **Claude (yo) volqué el `env` completo del stack n8n en crudo** vía `curl .../api/compose.one` en vez de `~/.claude/bin/dokploy-safe.sh`, exactamente el anti-patrón ya documentado en [[dokploy-api-get-schedule-list-expone-env-completo-secrets]] — quedaron en el transcript de la sesión `POSTGRES_PASSWORD`, `N8N_ENCRYPTION_KEY`, `POSTGRES_AUX_PASSWORD`, `RETELL_WEBHOOK_SECRET`, `CHATWOOT_WEBHOOK_SECRET` y `META_APP_SECRET` en texto plano, para verificar (legítimamente) si el HMAC de Meta estaba activo. El objetivo de la verificación era válido; el medio no debió ser `curl` directo. Acción: tratar esos secrets como expuestos y rotarlos (no solo `META_APP_SECRET`, que ya estaba en la lista).
- 2026-07-23 · TuFacturaIA · **cualquier archivo con tilde/ñ/diéresis subido desde Mac fallaba SIEMPRE** en la bandeja de ingesta ("Error del servidor", determinista) → `file.name` llega en Unicode NFD (macOS/Chrome), Supabase Storage rechaza esa key con 400 y el handler lo traducía a 500 genérico. Detectado a raíz de un PDF real ("ELPHIS. 23022026 30 días.pdf") que el usuario reportó como "no me acepta este presupuesto". Fix (PR #1191, mergeado): helper compartido `sanitizeStorageBaseName` (NFC + strip no-ASCII) aplicado en `/api/upload` y en adjuntos de email (este último, además, no saneaba nada — sin protección de path traversal). Ver [[supabase-storage-rechaza-key-nfd-mac-chrome]]. Pendiente: smoke real en prod (re-subir el PDF).
- 2026-07-21 · FacturaIA · `supabase db push` de Fase 2 Obras aborta "remote migration versions not found in local" → nº migración 519 DUPLICADO (orphan `519_obras_materiales_trazabilidad_ia` idéntico al 524, colado en #1094) → borrar el orphan; NO correr el `migration repair --reverted` que sugiere el CLI (corrompe historial). Ver [[db-push-remote-versions-not-found-es-checkout-stale-o-num-duplicado-no-repair]]
- 2026-07-21 · Centro Elphis · bot WA se quedaba mudo tras el usuario dar el nombre + nombre vacío en Clientify + "Timed out reading data" en Chatwoot → `chatwoot-event` retenía el lock de conversación ~15s (la extracción CRM con LLM corría dentro del lock, tras responder) y el 2º mensaje en ráfaga caía en `locked_busy` y se DESCARTABA → liberar lock+responder antes de la extracción (que pasa a correr fuera del lock) + reintento en `locked_busy` + 2º upsert de ids. Desplegado y verificado E2E. Ver [[lock-conversacion-liberar-tras-responder-no-tras-trabajo-post]]
- 2026-07-19 · Centro Elphis · mensaje WA de Alba tardó 87 min en llegar al bot → NO era el stack (n8n "Up 3 days", Traefik "Up 2 months", sin redeploy, webhook OK): retención/reintento de entrega del webhook por Meta (número recién migrado a Cloud API el 17-jul). Externo, no reproducible. Diagnóstico de uptime vía API Dokploy sin SSH. Ver [[dokploy-api-docker-getcontainers-estado-sin-ssh]]
- 2026-07-23 · TuFacturaIA · runner "Resolver con Claude" fallaba TODO job con exit 1 y stderr de solo-warnings ("Ignoring 23 permissions.allow entries… workspace has not been trusted") → CLI reciente de Claude Code rechaza `--dangerously-skip-permissions` sin trust aceptado (Dockerfile instala latest en cada rebuild) + el callback reportaba `stderr||stdout` ocultando el error fatal → entrypoint siembra `hasTrustDialogAccepted` en `~/.claude.json` + reportar ambos streams (PR #1182). Ver [[claude-headless-skip-permissions-requiere-workspace-confiado]]
- 2026-07-23 · TuFacturaIA · gráfico "Caja y cobro" no cuadraba con el listado (junio 980€ vs 2.060€ facturado) → 1.372/1.396 cobradas y 8/10 pagadas en prod con fecha de caja NULL (3 escritores marcaban estado sin fecha: menú/bulk del listado con `.update({estado})` directo, RPC import OCR, seed) y el cashflow base caja las imputaba al mes del VENCIMIENTO → PR #1176 + mig 549: escritores por endpoint canónico, trigger de invariante, backfill audit-first, KPI Cobrado a base caja, query compartida paginada en 7 consumidores. Ver [[estado-de-dinero-sin-fecha-de-caja-obligatoria-rompe-en-silencio]]
- 2026-07-23 · TuFacturaIA · import de extracto bancario real de un cliente perdía 8-9 movimientos silenciosamente (comisiones de divisa repetidas el mismo día, mismo importe/concepto, saldo runtime distinto) → el dedup sin `external_id` comprobaba "¿existe ALGUNA fila igual?" dentro del mismo bucle que inserta, así que la 2ª/3ª comisión idéntica se autodetectaba como duplicado de la 1ª recién insertada → fix: agrupar por clave y contar cuántas ya hay en BD, insertar solo la diferencia (PR #1190). De paso, cabecera real "F. Valor"/"F. Operativa" (espaciado distinto al alias) no se detectaba como fila de cabecera → normalizar igual en ambos matchers (PR #1189) y soporte real de `.xls` legado vía SheetJS con CVEs parcheadas desde su CDN propio (PR #1187/#1188). Ver [[hash-dedup-necesita-indice-secuencial-para-movimientos-repetidos]] · [[matching-texto-header-debe-normalizar-igual-en-ambos-lados]] · [[sheetjs-xlsx-npm-abandonado-cves-usar-cdn-propio-version-fijada]]
- 2026-07-25 · TuFacturaIA · movimientos de gasto conciliados al céntimo se mostraban "Parcial" y el motor seguía sugiriendo sobre ellos → `mirror_factura_match_to_nn` insertaba `importe_aplicado` con el signo del movimiento (negativo en pagos) y 3 consumidores sumaban sin `ABS`; además `compute_sugerencias_for_movimiento` no comprobaba la capacidad del propio movimiento (9 sugerencias fantasma) → migs 555/556: magnitud canónica + `CHECK (>0)` NOT VALID + backfill con predicado réplica de los guards, y techo de capacidad con la misma tolerancia que el badge. 22 movimientos corregidos, `parciales` 22→0. Ver [[columna-cuyo-signo-duplica-otra-columna-divergira]]
- 2026-07-25 · TuFacturaIA · 16 facturas de 6 orgs con importes en dólares etiquetados como euros (~14 % de error) y el motor de conciliación casando contra el importe en divisa → `ocr-process` guardaba moneda y tipo del BCE solo en `bandeja_ingesta`, nunca en `facturas` → mig 557 + escritura en el OCR (solo con tipo real del BCE; `manual_requerido` no cabe en el CHECK y una paridad 1:1 inventada contamina `total_eur`). De paso, 9 agregaciones sumaban `total` crudo mezclando divisas. Ver [[agregacion-monetaria-multidivisa-siempre-sobre-columna-eur]]
- 2026-07-25 · TuFacturaIA · `supabase migration list` daba 353 migraciones "pendientes" que estaban aplicadas → 2 migraciones aplicadas a mano el 23-jul con timestamp desalinean el emparejado del CLI desde esa versión ('2026…' ordena entre '202' y '203'); una de ellas (`editar_importe_asignacion`) no existía en NINGÚN fichero del repo → `migration repair --status applied 552` + `--status reverted <timestamps>` y recuperar la función al repo desde `schema_migrations.statements` (mig 561, PR #1205). Ver [[migracion-aplicada-fuera-de-historial-supabase]]
- 2026-07-25 · TuFacturaIA · 412 de 426 recibidas aprobadas sin `lineas_factura` → el calculador del 303 las saltaba con `continue`, o sea 136.503,94 € de base sin aportar IVA soportado en 4 orgs; causa: el listado de `/recibidas` aprobaba con `update` directo desde el navegador, saltándose el endpoint que inserta líneas → fallback de cabecera en el adapter fiscal con cuota contrastada contra el total + `POST /api/recibidas/aprobar-lote` y las dos rutas del componente al servidor (PRs #1203/#1206). Ver [[derivar-cuota-iva-de-cabecera-contrastar-con-el-total]]
- 2026-07-25 · TuFacturaIA · los recordatorios de cobro reclamaban 67,50 € de más en 2 facturas vivas → `facturas.total` es el importe fiscal (base+IVA) pero el PDF resta el IRPF desde siempre, y cobros leía `total`; el "Importe total a pagar" del PDF además ignoraba el IRPF cuando había también retención de garantía (imprimía 1.149,50 debajo de un Total de 1.060,00) → migs 559/560: columnas generadas `importe_cobrable` (divisa del documento) e `importe_cobrable_eur`, solo emitidas, con `floor(v+0.5)` para cuadrar con el 111 en abonos; el PDF pasa a imprimir los importes persistidos. Smoke verde sobre 2.003 facturas. Sigue con el bruto el adeudo SEPA y el Payment Link de Stripe. Ver [[importe-fiscal-no-es-importe-a-cobrar-retenciones]]
- 2026-07-25 · TuFacturaIA · una declaración fiscal en `cuadrado` cuyas facturas cambian queda con cifras obsoletas y NADIE avisa → el cron `fiscal-recalcular-borrador` solo carga estado `borrador`, así que no marca `requires_recalc` en las revisadas → parche manual con el mismo patrón del cron + `issues/fiscal-002` para decidir si incluirlas y/o bloquear la presentación mientras el flag esté activo. Destapado con 68.931,82 € de base entrando en un 303 ya cuadrado.
- 2026-07-25 · agency-portal · `/agency/time` mostraba 3 de 5 sesiones de Claude Code abiertas en el mismo repo → `resolveProject()` del hook detecta la raíz con `existsSync('.git')`, pero en un git worktree `.git` es un FICHERO (`gitdir: <repo>/.git/worktrees/<rama>`): cortaba ahí y reportaba la RAMA como proyecto (`modia-003-escape-stack`, `importe-cobrable-retenciones` en vez de `facturaia`); de paso `ACTIVE_WINDOW_MS` (10 min) era menor que `ACTIVITY_GAP_MS` (15 min), franja ciega con bloque abierto y sesión invisible → parseo del `gitdir:` al repo principal + ventana = gap (commit `8beaa3a`, PR #175) + backfill de 10 filas en prod. Diagnóstico contra `work_sessions` real, no por lectura de código. Ver [[git-worktree-dotgit-es-fichero-basename-devuelve-la-rama]] · [[presencia-por-eventos-discretos-ventana-mayor-o-igual-al-gap-y-heartbeat]]
- 2026-07-25 · TuFacturaIA · dos asignaciones bancarias parecían un doble pago a la misma factura recibida → eran pagos a DOS COPIAS del mismo documento: 62 grupos `(org,num)` duplicados por reingesta del mismo PDF (143 facturas, 5 orgs), y la medición se corrigió el 26/07: 17 de esos "duplicados" eran números iguales de proveedores DISTINTOS (no son duplicados), y el daño real no eran 4,38 € del doble pago bancario sino **532,20 € de IVA soportado deducido dos veces** (7.393,88 € de base, 3 orgs) → `issues/ingesta-001`. Sin unicidad de `(org,proveedor,num)` ni dedup por hash en la ingesta.
- 2026-07-26 · TuFacturaIA · CUATRO RPC abortaban con `forbidden` 42501 y sus acciones eran imposibles desde la app → `user_can_write_in_org` resuelve el rol por `auth.uid()`, y los endpoints las llaman con `createAdminClient()` (service role key, sin usuario) → NULL → FALSE. Rotas: `fiscal_marcar_cuadrada` y `fiscal_marcar_presentada` (preexistentes: "marcar cuadrada"/"marcar presentada" no funcionaban, encaja con 0 declaraciones con `presentada_en` en prod) y `fiscal_reabrir` + `recibida_eliminar` (nacidas rotas ese mismo día copiando el patrón). Test concluyente sin efectos: llamar con un argumento que falle DESPUÉS del guard (hash inválido) → devolvía 'forbidden' en vez de 'hash_mismatch'. Fix mig 569: `IF auth.uid() IS NOT NULL AND NOT ...`; autoriza el endpoint vía `withApiAuth({requireWrite})` y el límite es el REVOKE. **Tercera ocurrencia con la lección ya escrita 2 veces en el vault → check en `.githooks/pre-push`** (PR #1231, #1232). Ver [[rpc-rls-authuid-vacio-en-service-role]]
- 2026-07-26 · TuFacturaIA · el cron `fiscal-recalcular-borrador` marcaba `requires_recalc` y BORRABA los cuadres de 3 declaraciones cada noche, 5 noches seguidas con `recalculadas: 3` idéntico → el snapshot del 303 guarda emitidas Y recibidas pero el diff solo consultaba `tipo='emitida'`, así que cada recibida salía como "saliente"; peor, `fiscal_avisos` deduplica por `(declaracion_id, tipo)`, así que el falso positivo quemó el único hueco de aviso y una deriva REAL posterior no habría notificado nunca → universo a fuente única en `src/lib/fiscal/server/universo.ts`, compartido por motor y cron (PR #1219). Tras el fix: `salientes=0` en todas. Ver [[universo-de-datos-reimplementado-en-dos-sitios-divergge]]
- 2026-07-26 · TuFacturaIA · `recibida_eliminar` (mig 567) nació inservible en la práctica: bloqueaba si la factura estaba en CUALQUIER snapshot de declaración, copiando el `RESTRICT` de la FK que no distingue estados, y el cron `fiscal-generar-borradores` (03:50) mete las recibidas en un snapshot cada noche → cualquier duplicado dejaba de poder borrarse en <1 día, y recalcular lo volvía a meter (bloqueo circular). Lo destapó el guard de verificación de la propia limpieza de datos, que abortó sin tocar nada. Fix mig 568: de un `borrador` se retira del snapshot y se marca `requires_recalc`; solo bloquea si la declaración ya no es borrador. Ver [[fk-restrict-no-sirve-como-regla-de-negocio-no-distingue-estados]]
- 2026-07-26 · TuFacturaIA · **producción caída ~1 h** (`app.tufacturaia.com` → 404 de Traefik, `mcp` igual; n8n intacto) → la instancia Supabase (compute **NANO**, CPU compartida) agotó su crédito: leer UNA fila de `pg_settings` llegó a tardar **11,5 s**, 97 `statement timeout`, y el contenedor dejó de pasar el health check, así que Swarm lo retiró y Traefik se quedó sin ruta (404 y no 502 = no hay router, no hay backend enfermo). Descartados por evidencia: disco (1 GB de 8), incidente de plataforma (status all operational) y permisos (los 325 `permission denied for get_user_org_id` eran 288 ruido de mi propio dev server consultando como `anon`). Causa real: la BD gastaba su CPU en mantenimiento propio — `get_org_usage` 216.511 llamadas/4.081 s como MAYOR consumidor (colector de cuotas del health-sweep cada 10 min), el registro de pg_cron a 199 ms por insert con 96.114 filas sin purgar nunca, `purge_old_cron_runs` sin programar desde la mig 063, y el poll del ticket-runner cada 15 s. Fix: NANO→MICRO (2 cores, ya pagado, +0,00 €) + mig 570 y PR #1233 (cadencias, purgas, `omitirCostososEn`). ~800 s de CPU/día recortados. **El dead-man's-switch externo (NotCaído) NO avisó** y debía: hay que verificar por qué. Detonante: mis propias agregaciones forenses de tabla completa esa tarde. Ver [[vigilar-cuesta-cpu-y-puede-costar-mas-que-trabajar]] · [[monitor-en-la-misma-infra-no-detecta-su-propia-muerte]]
- 2026-07-27 · TuFacturaIA · el gate de cierre de la sesión anterior destapó que **el arreglo de la deriva fiscal rompió el modelo 130** y que la acción que ese arreglo añadía **nunca funcionó desde la app** → (1) `universo.ts` declaraba emitidas+recibidas también para el 130, cuyo snapshot solo guarda emitidas, así que TODAS las recibidas del ejercicio salían como "nuevas": la primera noche tras el despliegue (04:15) borró los cuadres de 6 declaraciones de 3 orgs (264 "nuevas" sobre 271 recibidas vivas en una de ellas) y quemó el hueco de aviso; antes del PR el cron miraba solo `tipo='emitida'` y para el 130 acertaba por casualidad. (2) El recheck de la mig 566 exigía recibidas en ese mismo snapshot parcial → un 130 con un gasto aprobado no se podía marcar presentado, sin salida (mig 571: se exige un tipo solo si el snapshot lo representa). (3) "Volver a abrir y recalcular" devolvía **400 siempre**: `POST` sin body contra una ruta con `parseBody` sin `bodyOptional`, y los tests no lo veían porque mockean `withApiAuth` inyectando `body: {}`. (4) El recorte de CPU del health-sweep daba por "recuperadas" las alertas de cuota que acababa de omitir, reemailándolas cada hora. PR #1234, 7.290 tests verdes. Ver [[el-universo-comparable-es-lo-que-se-persiste-no-lo-que-se-carga]] · [[mockear-el-wrapper-de-auth-esconde-el-400-que-ve-el-usuario]] · [[lo-que-un-barrido-omite-no-puede-darse-por-recuperado]]
- 2026-07-26 · TuFacturaIA · el panel `/admin` mostraba **12 incidencias ALTAS "Precio desincronizado BD↔Stripe (missing)"**, una por cada fila `active` de `plan_prices`, y no había ni un desajuste: el `.env.local` no tenía `STRIPE_SECRET_KEY`, `stripeGet` devuelve `ok:false` sin llamar a Stripe y `checkPriceCoherence` etiquetaba cualquier `!res.ok` como `missing`. Verificado luego contra Stripe real: los 12 precios existen, activos, cuadran al céntimo. Lo peligroso no era el ruido sino la acción que induce (recrear precios que sí existen → duplicados en Stripe y suscripciones apuntando a prices archivados); en prod lo dispara igual una clave rotada (401) o un 429. Delator: fallaban TODAS las filas con el MISMO `kind`. Fix PR #1238: `missing` exige `404 + resource_missing`, lo no verificable va en `unavailable` → UNA alerta media, y el smoke falla explícito en vez de leer "0 desajustes". **Daño propio en el mismo episodio**: verifiqué si una restricted key era read-only haciendo un `POST /v1/prices` que devolvió 200 y creó producto+price reales en la cuenta AgentesIA (archivados; el price quedó pendiente de archivar). Ver [[fallo-de-credencial-no-es-dato-ausente-en-un-check-de-coherencia]] · [[no-verificar-una-clave-read-only-escribiendo-con-ella]]
- 2026-07-27 · TuFacturaIA · secuela invisible del corte del 26: **cinco dispatchers de cola estuvieron 2 h 41 min parados y el sistema no emitió NI UN aviso** (cero filas en `notifications` entre las 04:15 del 26 y el día siguiente, comprobado). Se colgaron a las 18:08-18:09 con la base sin responder, el UNIQUE parcial `uniq_cron_in_flight` hizo que cada minuto siguiente skipease **sin escribir fila** (así que no había fallos que contar), y el `cron-watchdog` — que tampoco pudo correr entre 18:05 y 20:20 — los liberó a las 20:50: 9 zombies de golpe. El corte real fue ~1 h; el resto fue lock puro con la base ya sana. No era un bug del sweep: `computeHealth` da 'ambar' al `running` colgado sobre histórico sano y otra vez 'ambar' tras el reapeo (racha=1), nunca rojo. Fix PR #1241: el reaper emite `cron_failed_lote_congelado` al liberar ≥3 locks en una pasada, umbral sacado de la distribución de 8.653 pasadas (0×8.614, 1×23 rutina de deploy, 2×1, 9×1 el incidente) → un aviso en toda la historia y el correcto. Smoke real en prod con 3 locks de nombre ficticio: reapeados, notif `critical` visible en la campanita, restos borrados y 0 locks colgados. Casi meto un fix inventado antes: la media de 8 s de esos crons era p50 de 200 ms secuestrado por el `max` de 2 h 42 min. Ver [[parada-de-flota-solo-se-ve-en-la-simultaneidad]]
- 2026-07-27 · TuFacturaIA · "Marcar cuadrada" y "Marcar presentada" tardaban **125 s** en fallar con el hash obsoleto (el caso para el que existe el guard) y acababan en `upstream request timeout` → NO era contención de locks (la mig 572 se hizo con esa hipótesis y no cambió nada): son las dos únicas RPC que lanzan `ERRCODE 40001`, que PostgREST **reintenta** por significar conflicto transitorio; siendo el fallo determinista, reintentaba hasta agotar el gateway. Caso de control: el mismo endpoint con un id inexistente respondía en 266 ms (`P0002`), y `fiscal_reabrir`/`recibida_eliminar`, que no usan 40001, en 73 ms → mig 573 pasa `hash_mismatch`/`snapshot_outdated` a `P0001` conservando el texto: **125.135 ms → 145 ms** medido en prod. De paso, el hueco fiscal deja de salir como `INTERNAL: FISCAL_GAP: …` y sale como 422 nombrando la factura culpable (`issues/fiscal-004`). Ver [[error-determinista-con-sqlstate-40001-provoca-reintentos-infinitos]]
- 2026-07-27 · TuFacturaIA · **`origin/main` rebobinada 40+ commits** por un `git push --force-with-lease` lanzado desde el checkout principal (parado en `22bf05ec`/#1204) mientras el remoto iba por `cdf41473`: el lease compara contra tu `refs/remotes/origin/main` LOCAL, no contra el remoto, así que sin `fetch` previo no protege nada → `+ cdf41473...22bf05ec main -> main (forced update)`. Se perdieron la tanda fiscal (#1238-#1246), el paso middleware→proxy (#1239) y la fase 1 de módulos IA (#1221), con las **migraciones 566-573 que YA estaban aplicadas en la BD de prod**: un deploy en esa ventana habría sido un rollback contra una BD migrada. Encima `gh pr merge 1248` aplastó la fase 2 sobre la base rebobinada (57 ficheros de diferencia). Recuperado reconstruyendo `cdf41473 + squash(rama rebasada)` desde el worktree, que estaba intacto, y verificando por ÁRBOL (`rev-parse HEAD^{tree}` idéntico al que pasó el gate), no por log. El push de recuperación lo bloqueó además el `pre-push`, que no distingue migraciones nuevas de las que ya arrastra el rango → `--no-verify` documentado. Ver [[force-with-lease-sin-fetch-no-protege-nada]]
- 2026-07-27 · TuFacturaIA · P2026-0019 mostraba **−123,20 € de IVA** en el listado → `compose.ts` persistía en presupuestos la base BRUTA (3.850) mientras el `total` ya llevaba el descuento global del 20 % (3.726,80), y media docena de consumidores derivan la cuota como `total − base`; con descuentos ≳17,4 % al 21 % sale negativa. El total y el PDF eran correctos desde el día uno: lo falso era el desglose. Peor que el número feo: `convertir_presupuesto_a_factura` copiaba base bruta + total descontado + el % a la cabecera y las líneas al bruto, así que la factura resultante declaraba un desglose VeriFACTU con base y cuota **mayores que su propio total** (ya había pasado: A2026-0163). Fix migs 575/576 (base NETA + backfill de los 3 documentos afectados + reparto en la conversión) y PR #1250. **Secuela el mismo día**: #1250 no cerró el bug que decía cerrar — el INSERT de creación seguía leyendo `totales.base`, así que cada presupuesto NUEVO nacía descuadrado; destapado creando un documento de control en prod tras el deploy, arreglado en #1252. Ver [[base-persistida-debe-ser-la-imponible-o-total-menos-base-miente]] · [[cambiar-la-semantica-de-una-columna-el-compositor-no-es-el-punto-de-persistencia]] · [[ADR-042-base-imponible-neta-en-todos-los-tipos]]
- 2026-07-27 · TuFacturaIA · **producción llevaba 88 días sobre Node 20 (EOL 30-abr) y 117 sobre Alpine 3.20 (EOL 1-abr)**, sin un solo aviso: Dependabot mira CVEs y versiones, no fechas de fin de soporte, y su propia propuesta (#1164) llevaba a `node:24-alpine3.20`, un tag congelado desde mayo de 2025. Detectado revisando esa PR a mano. Fix #1257: Node 24 LTS + Alpine 3.23 en las tres imágenes, `@types/node` a la línea 24 (no 26: los tipos van DETRÁS del runtime) y `node-version` de los 4 workflows. El salto de Chromium 131→149 se validó A/B con las dos imágenes construidas (páginas, texto extraído y píxeles idénticos salvo 195 px de antialiasing en un logo) antes de mergear, no después. Secuela: el MCP se quedó en Node 20 porque su `deploy-mcp.yml` está muerto con Actions → desplegado a mano por `compose.deploy`. Ver [[dependabot-no-avisa-de-eol-de-runtime]] · [[validar-cambio-de-motor-de-render-con-ab-de-misma-imagen]]
- 2026-07-27 · TuFacturaIA · **dos podredumbres de 2 meses en el camino que genera todas las facturas**, encontradas el mismo día y ninguna por un test: (1) la vista previa de PDF enseñaba 150 px del A4 y el resto en blanco desde el 25-may — `flex:1` en un iframe cuyo padre no es flex, así que caía a su alto por defecto (fix #1261: altura explícita + modal `xl`); (2) el microservicio `pdf-renderer` devolvía 500 en TODOS los renders desde el #951 (16-jul) porque una plantilla empezó a importar un CSS Module que tsx no puede parsear — no se notó porque **nadie llamaba a ese servicio**: se desplegaba en cada deploy y el PDF se genera en proceso (retirado entero en #1262, junto con `/render-invoice`, que era público sin sesión). La suite tiene 7.541 tests y ninguno arranca Chromium. Ver [[camino-critico-sin-smoke-se-pudre-meses]] · [[iframe-con-flex-1-en-contenedor-no-flex-cae-a-150px]]

- 2026-07-27 · TuFacturaIA · **el vencimiento de una factura recibida ya aprobada no se podía corregir por ningún sitio** (ticket de soporte de IET, la org de Natalia): el OCR no lo cogió, el usuario lo escribió en el panel de `/ingesta` y la factura seguía sin él. `bandeja_ingesta.datos_extraidos` solo se copia a `facturas` **en** la aprobación, así que editarlo después guardaba en el JSONB y nada más; el editor de `/recibidas` está gateado a `sin_aprobar` y su rama no-editable descartaba los campos fiscales remachando con un toast de éxito ("Datos del proveedor actualizados"); y `PATCH /api/facturas/[id]` es emitida-only. Fix: endpoint `POST /api/facturas/[id]/vencimiento` (recibidas en cualquier estado; el `vto` de una recibida no es fiscal, en el 303 y el libro manda `fecha`), el panel de la bandeja pasa a mostrar el `vto` de la FACTURA y a bloquear el resto con motivo, y la ficha de la recibida gana la fila Vencimiento (Añadir/Editar). #1260/#1264/#1265/#1272/#1273. Ver [[staging-deja-de-ser-fuente-de-verdad-tras-el-commit-y-editarlo-no-cambia-nada]] · [[editor-inline-que-compara-contra-el-valor-mostrado-encalla-al-reescribir-lo-mismo]]

- 2026-07-27 · TuFacturaIA · **el grafo de dependencias llevaba desde el 7-may describiendo un repo que ya no existía** (300 módulos commiteados vs 3.140 reales) y su documento recomendaba un cambio que habría abierto una **fuga cross-org**: unificar `render-pdf` con el helper canónico `effectiveOrgId`, que devolvía el `?org_id=` sin validar apoyándose en que el caller ya hubiera hecho `requireAdmin()` — pero el GET de `render-pdf` solo exige sesión y luego filtra con `createAdminClient()`, que salta RLS. La copia local que el mapa mandaba borrar era la correcta. Resuelto al revés: el gate `isSuperadmin()` se mueve DENTRO del helper (#1274). En el mismo barrido: ciclo `notifications/push` cerrado, 30+ inversiones de capa enderezadas, 9 huérfanos borrados, y un rol desconocido en `org_members` que daba 500 por TypeError en `MATRIX[role]` pasa a 403 (#1288). Ver [[helper-de-auth-que-asume-validacion-del-caller]] · [[madge-ciclo-import-dinamico-no-rompe-arista]]

- 2026-07-28 · TuFacturaIA · **main quedó en rojo por un cron a medio cablear, y nadie lo vio porque CI lleva roja desde el 17-jun**: el #1290 metió `runtime-eol-check` en `CRON_REGISTRY` y su ruta interna pero no el `case` en `loadHandler`, así que el botón "Run Now" de `/admin` devolvía "handler not wired". El repo YA tenía el guard que lo caza (`registry-wired.test.ts`) y fallaba — pero `ci.yml` en main viene en `failure` desde junio por el billing de Actions, de modo que el único gate real es correrlo en local. Fix en #1292. Aprendizaje del incidente: con CI apagada, "los tests pasan" solo vale si alguien los corrió; el registro doble (registry + despachador) necesita su test Y que el test se ejecute.

- 2026-07-28 · TuFacturaIA · **dos sesiones atacaron la misma suite smoke en paralelo y duplicaron el trabajo**: mi rama `fix/e2e-smoke-cierre` y el #1263 (`fix/e2e-smoke-verde`) diagnosticaron por separado los mismos dos hallazgos (los locators muertos de `conciliacion-vinculacion` y la herencia de `storageState` en `proxy-active`). Coste real: horas de análisis repetido, y por poco un test verde ajeno roto — mi prefijo `sr-only` "Vinculado con" entra en `textContent` y habría reventado su aserción `^<num> · DD/MM/YYYY$` si hubiera mergeado sin auditar el solape. Además la contención de CPU (3 sesiones, load 86) invalidó dos tandas de 19 min. Antídoto: antes de arrancar un issue, `gh pr list --search "<área>" --state all` y `git log origin/main --oneline -20`; el fichero del issue no sabe que otro ya lo está cerrando.

- 2026-07-28 · TuFacturaIA · **el catálogo de crons y los schedules reales llevaban meses divergiendo, y un cron del catálogo no se había ejecutado NUNCA**: al ir a dar de alta `runtime-eol-check` (el #1290 lo dejó en `CRON_REGISTRY` sin `schedule.create`, o sea el vigilante de EOL tampoco existía) comparé las 43 entradas contra los 43 schedules de Dokploy — coincidían en número y discrepaban en cuatro sitios. El grave: `obras-reservas-reconciliar`, la red de seguridad de las reservas de facturación de obra, con **cero runs desde su despliegue**. Invisible por diseño: sin runs `computeHealth` da `'desconocido'` y el colector solo miraba `'rojo'`. El hueco estaba **documentado en `gotchas.md`** con un "auditar de vez en cuando" y mordió igual. Los otros tres: `copiloto-recordatorios-batch` corriendo a diario desde el 29-jun sin entrada en el catálogo (sin panel ni vigilancia), `mcp-dcr-cleanup` pasado a semanal el 3-jul con el umbral aún en 48 h (rojo cada martes estando sano) y `module-events-purge` con la expresión desviada 30 min. Fix #1300: incidencia `cron_nunca_ejecutado` ALTA que se resuelve sola al primer run, tres derivas corregidas y el gotcha del `timezone` por schedule (`NULL`=UTC vs `Europe/Madrid`, conviven) documentado — deducir el cron de las horas de `cron_runs` sin mirar la zona fue lo que causó la corrección errónea anterior. Ambos crons creados y verificados con run real; `obras-reservas-reconciliar` devolvió `derivas_detectadas: 0`, así que los meses sin correr no dejaron daño. Ver [[estar-en-el-catalogo-de-crons-no-es-estar-programado]]

- 2026-07-28 · Clínica Zen · **la reserva por voz llevaba meses sin enviar NADA al terminar, y los recordatorios mandaban la plantilla equivocada de madrugada**: feedback de una llamada real (`call_a398cee3465ea4472f9a9464ba3`) destapó tres cosas independientes. (1) La rama de voz de `RN0wl8RaRmwLpnfQ` terminaba en Calendar+Kommo+Postgres sin tocar `Build Emails HTML` → cero correos, ni al paciente ni el interno; y los dos nodos `WA Confirmación Cita A/B` existían con CERO conexiones, así que el "te llegará la confirmación al WhatsApp" que promete el agente no lo enviaba nadie. Fix: `Pendiente de Asignar A/B` → `Build Emails HTML` (origen elegido con `isExecuted`), IF `¿Hay email de paciente?` antes del correo al paciente (por voz no se pide dirección), WA reconectado, `onError: continueRegularOutput` en los emisores. (2) `PJBMjLLE0vNJjZH8`: el Switch `Tipo de recordatorio` tiene las conditions sin bloque `options`, así que el item `"4h"` sale por la rama 0 (la de 24h) — probado en el `runData` (rama 0: 1 item, rama 1: 0) — y todos los avisos de 4 horas iban con `bot_id` 63810 en vez de 63808. (3) El mismo workflow marca `staticData.enviados` ANTES de enviar: el 400 de `salesbot/run` de las 06:30 dejó a un paciente sin recordatorio y la clave puesta, sin reintento posible. Y esas 06:30 no eran un fallo: es el aviso de 4h de una cita de 10:30, con un scanner que corre 24/7 sin ventana horaria. (2) y (3) quedan ABIERTOS. La queja original ("no se escucha nítido") no se reprodujo en la grabación: agente −19,8 dBFS, 0 muestras saturadas, y el llamante 5 dB por debajo; la causa candidata es `ambient_sound: call-center`, que se mezcla después de grabar.  **Ampliación mismo día, RECTIFICADA el 29-jul**: creí haber descubierto que el SMTP de la clínica (Paginalia, `mail.clinicazen.es:465`) aceptaba con `250 queued` y no entregaba fuera. **Era falso y el error fue de método**: el rebote de la sonda existía, tardó 7 min y cayó en la carpeta **Spam** del buzón; yo lo busqué a los 2 min y solo en INBOX. Gmail había respondido `550-5.1.1 does not exist`, o sea la salida funciona. La causa real de que los correos no aparecieran es **reputación**: cabeceras de un envío propio dan `dmarc=pass (p=QUARANTINE)`, `dkim=pass header.d=clinicazen.es`, `spf=pass` — autenticación impecable, pero `clinicazen.es` casi no envía correo y sale por IP compartida, así que Google lo aparca en Spam. Impacto acotado: el aviso interno va a `citas@clinicazen.es`, entrega local (4 s), sin pasar por Google. Ver [[smtp-acepta-con-250-queued-y-no-entrega-fuera]] · Ver [[canal-nuevo-en-workflow-no-hereda-los-side-effects-de-la-rama-original]] · [[n8n-switch-conditions-sin-options-enruta-todo-por-la-primera-salida]] · [[marcar-enviado-antes-de-enviar-pierde-el-mensaje-sin-reintento]] · [[recordatorio-relativo-sin-ventana-horaria-escribe-de-madrugada]] · [[retell-ambient-sound-no-esta-en-la-grabacion-auditar-por-config]] · [[clinica-zen]]

- 2026-07-28 · TuFacturaIA · el ticket #86 preguntaba **por qué el total del PDF no cuadra con el de la interfaz** en una factura con retención, y la respuesta era que los dos números son correctos (`A2026-0002`: 1.215,00 fiscal firmado en VeriFactu, 1.144,71 lo que transfiere el cliente): lo que faltaba era enseñar el segundo. La ficha ya lo desglosaba, el LISTADO no, que es donde miraba el cliente → "A cobrar" bajo el total en escritorio y móvil (#1309), y el #1296 en paralelo renombró la fila de la ficha a "Total (base + IVA)". **Lo que destapó investigarlo**: el editor rápido del listado calculaba el total restando el IRPF y lo persistía en `facturas.total` **también en emitidas**, donde es el importe fiscal bruto → `importe_cobrable` (columna generada) habría restado la retención dos veces. La mig 559 documenta esa fórmula neta como deliberada **y señala esa misma línea por número**, pero solo vale para recibidas: el comentario llevaba meses dando coartada al bug. Cero filas dañadas (4 emitidas con IRPF, todas con el bruto correcto; 0 borradores con IRPF; las 2 anómalas son recibidas) y sin riesgo fiscal, porque solo se edita un borrador y `compose.ts` recalcula al emitir abortando si base+cuota ≠ total. Criterio movido a `totalDesdeEdicion()` con 18 tests y las 4 emitidas reales como fixtures. De rebote, el manual de usuario describía el comportamiento CON el bug y el de admin se contradecía consigo mismo. Ver [[comentario-que-declara-una-formula-deliberada-solo-cubre-su-mitad]] · [[antes-de-tocar-un-ticket-mira-si-otra-sesion-ya-lo-esta-cerrando]]

- 2026-07-28 · TuFacturaIA · **un cliente podía leer por PostgREST los mensajes INTERNOS de su propio ticket de soporte, incluidas las instrucciones que le pasábamos a Claude**. La política de `feedback_ticket_messages` filtraba por dueño del ticket pero no nombraba la columna `internal`, y el endpoint sí filtraba (`includeInternal: false`) — o sea, la app estaba bien y la BD no, que es justo lo que no se ve revisando el código de la ruta. Reproducido en prod con la sesión real del usuario E2E antes de tocar nada: un mensaje `internal=true` volvía en la respuesta. De rebote, `feedback_tickets` tenía SELECT de tabla para `authenticated`, así que `notas_internas` y `admin_visto_at` también eran legibles: RLS decide filas, nunca columnas. Fix mig 582 (#1323): `internal = false` en el `using`, `revoke select` de tabla y `grant select (…)` por columnas, más índice único parcial de job vivo por ticket. Verificado después contra el catálogo: la política lleva el predicado, `authenticated` ya no tiene `r` de tabla y las dos columnas privadas quedan sin ACL. El único consumidor con sesión de usuario (`feedback-button.tsx`) pide `id` filtrando por `user_id`/`visto_por_user`, las tres en la lista. Ver [[rls-filtra-filas-no-columnas-y-la-politica-debe-nombrar-la-columna-privada]] · [[verificar-grants-por-columna-con-pg-attribute-attacl-no-con-information-schema]]

- 2026-07-28 · TuFacturaIA · **`change_phone_confirm` llevaba meses rota en producción y nadie lo notó**: la RPC declara `SET search_path = public, pg_temp` y llama `gen_random_bytes` sin cualificar, pero `pgcrypto` vive en `extensions` → `42883: function does not exist`, o sea que el cambio de teléfono fallaba siempre. Invisible desde que la verificación de teléfono se difirió a la activación de WhatsApp (refactor de mayo), así que el camino casi no se usaba. Es la única función del esquema `public` con ese patrón. Salió buscando otra cosa: auditando por qué el repo no podía reconstruir la BD desde cero. Fix mig 581 (`public, extensions, pg_temp`), aplicada y verificada ejecutando en prod el bloque que antes daba 42883. **Dos trampas en el camino, las dos por no ejecutar**: la migración reventaba con `malformed array literal` (`array || 'literal'` sin cast), y sus números 578/579 ya estaban ocupados en prod por otras ramas → un `db push` los habría dado por aplicados EN SILENCIO dejando el bug vivo. Ver [[plpgsql-array-mas-literal-sin-cast-se-resuelve-como-array]] · [[colision-de-numero-de-migracion-hace-que-db-push-la-salte-en-silencio]]
- 2026-07-29 · TuFacturaIA · una org creada por script no podía emitir (500 genérico) → el seed de series estaba partido entre un trigger (B/F/S) y el onboarding (A/R/P/T), y le faltaba la A → mig 589: el trigger de alta siembra las 7 y backfill. La mig 402 ya lo había parcheado sin cerrar el origen. Ver [[seed-partido-entre-trigger-y-onboarding-deja-filas-a-medias]]
- 2026-07-29 · TuFacturaIA · cambiar de empresa dejaba la app pintando datos de la ANTERIOR (visto en Ajustes → Auditoría: mismas filas antes y después; el sidebar sí cambiaba) → el switcher hacía `router.refresh()`, que invalida el árbol RSC pero no remonta los client components, así que los ~106 que cargan con `fetch` en un `useEffect` de montaje conservaban su `useState`. Parecía fuga cross-org y no lo era: el endpoint filtraba bien por `orgId`. Fix #1334: recarga dura del documento en el switch y en el listener de `BroadcastChannel`. Ver [[router-refresh-no-remonta-los-client-components-datos-de-la-org-anterior]]

- 2026-07-29 · Clínica Zen · **los recordatorios de WhatsApp no habían funcionado NUNCA, y el historial de n8n solo duraba 7 horas**: tres causas independientes, todas cerradas el mismo día. (1) `salesbot/run` mandaba `entity_type` como **string** `"2"`; Kommo exige entero y responde `400 Invalid field: entity_type` — probado con una sonda a un `entity_id` inexistente, que distingue las dos capas de validación (`403 Entity not found` = tipo OK, `400` = tipo mal). Los otros 13 nodos `salesbot/run` del sistema usaban entero y sí funcionaban. Confirmado en la GUI de Kommo: los bots `63810` y `63808` tenían **0 lanzamientos históricos**. (2) `EXECUTIONS_DATA_MAX_AGE=7` **dentro del `composeFile`** (no en el Environment): la variable va en **horas**, así que el historial duraba 7 h y era imposible ver un fallo del día anterior — el 400 se cazó por horas. Corregido a `336` + `PRUNE_MAX_COUNT=10000` por la API de Dokploy y desplegado (13 s de corte). (3) **Yo introduje un doble envío el 28-jul** al conectar los nodos huérfanos `WA Confirmación Cita A/B`: el salesbot `63814` ya se disparaba solo con el trigger "lead movido a la etapa pENDIENTE DE ASIGNAR" y llevaba 104 lanzamientos. Revertido. El contador del bot solo se ve en la GUI. Ver [[kommo-salesbot-run-entity-type-debe-ser-entero-no-string]] · [[n8n-executions-data-max-age-va-en-horas-no-en-dias]] · [[nodo-huerfano-puede-estar-desconectado-porque-otro-mecanismo-ya-lo-cubre]] · [[clinica-zen]]
- 2026-07-29 · TuFacturaIA · **tercera ronda del mismo PDF: el OCR seguía guardando el código de cliente como nº de factura** (ticket 89d8a652, IET). Factura de mayorista eléctrico con cabecera en tabla (`Fecha | Factura 624214649 | Cliente 00743 | C.I.F. | Agente | Página`): el modelo devolvía `00743`. Las dos rondas anteriores (83c8028d) se cerraron escribiendo la REGLA 19 en el prompt, con el `00743` citado literalmente dentro, y falló las dos veces. La causa raíz no era la instrucción que faltaba: el pipeline no verificaba NADA de ese campo (`if (sanitized.num_factura) facturaUpdates.num = ...`). Fix #1342: el prompt pide las DOS columnas (`num_factura` + `num_cliente`) y `resolverNumFactura` las cruza; si coinciden no persiste nada y manda a revisión. De paso, la ecotasa RAEE (1,10 €, dentro de la base 4.683,36 €) pasa a campo propio y `reconciliarLineasEcotasa` añade su línea solo si el hueco Σlíneas↔base es exactamente ese importe. Ver [[dos-campos-confundibles-pide-los-dos-y-cruzalos-en-codigo]]
- 2026-07-29 · AGH Ibérica · **el alta de un cliente reventaba el turno entero cuando la cuenta ya existía en OTRA cartera** — visto EN VIVO en los logs de prod (`duplicate key value violates unique constraint "clients_tenant_name_uidx"`, turno muerto por el camino de #113 con copia genérica). Causa: dos alcances desalineados — el índice único es por **TENANT** y el dedup de `ClientIntake` miraba `list(tenant, owner)` con su propia `normalize()` de TS. Con varios comerciales tocando la misma cuenta (Dragados, McDonald's) es el caso corriente, no el raro. Fix #640/PR #641: `ClientStore.existsInTenantByName` (devuelve `boolean`, no la fila, para no leer cartera ajena) consultando con **la expresión literal del índice**, guard por encima de `confirmedNew` y `redundant` en vez de `decline` para conservar las hermanas del batch. **Lo sacó a la luz la instrumentación recién desplegada** (`errorClass=store_error` en la traza): el mismo fallo llevaba muriendo mudo. Ver [[guard-en-codigo-que-predice-un-indice-unico-de-sql-diverge]] · [[agh-iberica]]
- 2026-07-29 · TuFacturaIA · **una corrección de fecha en la bandeja se perdía sin decir nada** (ticket #104, Pescados Chivite, factura 10422P de DAORO). La fila "Fecha emisión" pintaba `dd/mm/aaaa` con `fmtDate` pero se editaba con un `<input type="text">` libre, y el guardado validaba ISO haciendo `return` mudo: el cliente tecleó `23/06/2026`, no saltó ni un toast, `datos_extraidos.fecha` se quedó en `2023-06-26` (verificado en prod: ni la factura ni la bandeja registraron el intento) y la aprobación propagó esa fecha a `facturas`. Y sin salida: tras aprobar, la fila pasa a `readOnly` en la bandeja y `/recibidas` pinta `fecha` como texto plano. Fix #1349: las dos filas de fecha pasan al `<DatePicker>` compartido (habla ISO dentro, `dd/mm/aaaa` fuera) y ninguna rama de guardado sale muda. Ver [[campo-que-muestra-un-formato-y-guarda-otro-descarta-la-edicion-en-silencio]]
- 2026-07-29 · TuFacturaIA · **el runner de tickets quemaba media hora y la tiraba entera** — 6 jobs muertos por `timeout: claude excedió 1800000ms`, sin dejar ni la salida de claude (el camino de `exit≠0` sí la reportaba; el de timeout no) y con el `finally` borrando el worktree. Causa: el prompt le ordenaba dos cosas irrealizables en ese contenedor, una captura de pantalla **sin navegador** y `npm run build` (OOM contra 3G) que el system prompt prohibía a la vez, así que la sesión las perseguía hasta que la mataba el reloj. Otros 6 fallos eran jobs SANOS matados por el watchdog: el latido solo existía dentro de la sesión de claude, no durante la copia de `node_modules`. Fix #1350: fuera las dos órdenes, cada instrucción en una sola capa, SIGTERM+rescate en PR draft con la cola del stdout, y latido de job abierto al entrar y cerrado en el `finally`. Autopsia sobre 91 jobs: de 34 fallidos, 11 eran una cuenta mal configurada, 7 cuota, 6 watchdog y solo 6 timeouts. Ver [[orden-imposible-en-su-entorno-el-agente-explora-hasta-que-lo-matan]] · [[latido-que-solo-cubre-el-tramo-interesante-deja-el-resto-a-merced-del-watchdog]] · [[mide-el-reparto-de-fallos-antes-de-arreglar-el-que-te-cuentan]]
- 2026-07-29 · TuFacturaIA · **el OCR lleva mandando facturas a otro ejercicio al leer el año de dos dígitos**: `23-06-26` (DD-MM-YY) se persistía como `2023-06-26` en vez de `2026-06-23`. Verificado contra los dos papeles (DAORO 10422P de Chivite y un justificante de AgentesiaLab, donde además leyó el día 23 como 29). Medido en prod: **9 de 109 recibidas de orgs reales (8,3%)** con la fecha desviada >180 días de su alta; 7 en `sin_aprobar` y 2 ya aprobadas, corregidas a mano con `audit_log` tras comprobar que no estaban en ningún snapshot fiscal. Impacto real: `fecha` manda en el 303 y el libro de recibidas, así que la factura sale del trimestre que le toca (492,80 € de base en Chivite cuyo IVA soportado no se deducía). Sin arreglar en el origen: falta desambiguar el año corto en el prompt y en código + sanity check antes de persistir. Ver [[ocr-lee-dd-mm-yy-como-yy-mm-dd-y-manda-la-factura-a-otro-ejercicio]]
- 2026-07-29 · AGH Ibérica · **la primera comercial nueva (Itziar) quedó 14 minutos bloqueada y seguía bloqueada al encontrarlo** — y su sesión entera destapó una cadena, no fallos sueltos: el lote de alta murió mudo contra el índice único de tenant (#640/#644), sus correcciones ortográficas no se entendieron (#648), «sigue con lo nuevo» —la frase que el propio copy sugiere— confirmó el lote viejo y tiró la lista nueva (#647), la propuesta le ofreció crear clientes que ya existían (#650) y, al borrar un cliente, el turno **se envenenó a sí mismo**: el puntero en memoria al cliente recién borrado violaba la FK, el catch respondía «¡Hecho!» sin limpiar el pending y el borrado idempotente lo repetía para siempre (#643). Nueve PRs a prod el mismo día. Ver [[un-puntero-durable-a-una-fila-borrada-convierte-un-fallo-en-bucle]] · [[guard-en-codigo-que-predice-un-indice-unico-de-sql-diverge]] · [[agh-iberica]]
- 2026-07-29 · AGH Ibérica · un dedup de reunión agendada **habría sido un no-op silencioso en producción**: comparaba `Date.parse(evento.start)` (hora de pared sin offset que Graph devuelve) contra un instante UTC, y el contenedor corre en UTC mientras el portátil de dev va en Madrid. Gate verde, 5 tests verdes, cazado por revisión adversarial antes de mergear (#585). Ver [[graph-devuelve-la-hora-del-evento-como-pared-sin-offset]] · [[el-gate-verde-no-sustituye-una-revision-adversarial-antes-de-mergear]]
- 2026-07-30 · TuFacturaIA · **tres tickets seguidos (112/113/114) dijeron "runner sin latido > 15 min" y el runner estaba sano**: el compose del `ticket-runner` tenía `autoDeploy` contra `main` con `watchPaths` VACÍO, así que cada merge recreaba sus contenedores y cortaba la sesión de Claude en marcha. Diagnóstico por horas, no por hipótesis: los latidos se pararon a las 21:43:56 y el deploy fue a las 21:44:38 UTC; `docker inspect` daba `OOMKilled: false`, `ExitCode: 0`, y de los 10 deploys del día solo 2 tocaban el runner. Sin handler de señal el job quedaba en `ejecutando` hasta que el watchdog lo cerraba 15 min después culpando al agente. Fix #1360: `watchPaths: ["ops/ticket-runner/**"]` en Dokploy + apagado grácil (mata el grupo de `claude`, cierra el job con el motivo real, deja de reclamar) con `stop_grace_period: 30s`, gate y copia de `node_modules` a asíncrono, y callback terminal con ~3 min de reintentos por los redeploys del app. El smoke con SIGTERM real destapó una carrera que no se veía leyendo: al morir el hijo, el flujo normal reportaba "claude salió con código null" y ganaba el compare-and-set, o sea el estado volvía a mentir → durante el apagado solo habla el handler. Ver [[autodeploy-sin-watchpaths-mata-el-trabajo-en-vuelo-del-worker]] · [[latido-que-solo-cubre-el-tramo-interesante-deja-el-resto-a-merced-del-watchdog]]
- 2026-07-30 · TuFacturaIA · **la ✕ del panel de alertas fingía cerrar una incidencia técnica**: la rama de acciones miraba `org_id` antes que `alert_id`, así que una entrada de `system_alerts` que ADEMÁS tenía org ofrecía "Descartar" (escribe en `admin_alert_dismissals`, tabla que su collector no consulta) y escondía "Resolver", el único cierre real. Toast de éxito, la fila desaparecía y volvía al recargar. De paso, el badge pintaba el slug interno con `text-transform: capitalize` → "System Alert:Ocr-Process" y "Emails Failed" en una UI en español. Fix #1366: `alertTypeLabel()` como fuente única de la etiqueta (badge + filtro + dashboard) y las acciones ramificando por `alert_id`. El smoke con navegador encontró un fallo del propio fix que el código no delataba: con etiqueta genérica, dos `system_alert:*` distintos daban dos opciones idénticas en el filtro. Ver [[dos-mecanismos-de-cierre-y-la-ui-ofrece-el-que-no-aplica]] · [[etiqueta-humana-de-un-slug-debe-seguir-siendo-unica-por-tipo]]
- 2026-07-30 · TuFacturaIA · **Remesas SEPA llevaba caída en producción y la suite lo veía sin decirlo**: la lista de facturas domiciliables casteaba la respuesta del endpoint (`as Tipo`, sin validar), así que el rename de `total` a `importe_cobrable` tumbó la página entera. Existía un E2E que la visitaba, pero **el crawler del explorer nunca fallaba por lo que encontraba** — escribía `report.json` y pasaba con haber visitado una página. Salió de la auditoría "lo que el usuario cree que guardó, ¿se guardó?" (ticket #104: `/ingesta` pintaba fechas en dd/mm/aaaa, las editaba con `<input type="text">` y el guardado hacía `return` en silencio si no casaban el formato ISO). 18 issues y 5 PRs a prod (#1361-#1364, #1367): parser de importes único con estado "ambiguo" explícito, 30 escrituras que respondían `ok` sin comprobar filas afectadas → `updateOneOrThrow`, ajustes que resolvían la empresa con `.limit(1)`, y seis guards de conformidad en CI. **Queda abierto**: los E2E no están enganchados a ningún workflow, así que el crawler ya falla pero nadie se entera. Ver [[update-que-afecta-cero-filas-no-devuelve-error-en-postgrest]] · [[crawler-que-escribe-informe-y-sale-cero-es-recolector-no-test]] · [[eslint-flat-config-el-ultimo-bloque-que-casa-reemplaza-el-array-de-la-regla]]
- 2026-07-30 · TuFacturaIA · **un importe escrito en español se corrompía y llegaba a una factura firmada, y el comentario del código decía que eso estaba resuelto**: `<NumberField>` usa `<input type="number">`, cuyo navegador acepta `1.234` como número válido, descarta la coma y concatena — teclear `1.234,56` guardaba **1,23 €**, sin aviso, sin `:invalid`, sin error. Reproducido de punta a punta en la sandbox: factura `A2026-0177` emitida por 1,23 € con su registro VeriFactu en `pre`. Estuve a punto de descartarlo porque el fichero llevaba un comentario afirmando que el caso estaba cubierto (`guardado-016`); no lo estaba, y esa frase es la razón de que sobreviviera a las revisiones anteriores. Salió de la auditoría funcional total (400 controles en 23 lotes con navegador, 599 capturas, cada cifra contrastada contra Postgres y no contra el toast): 4 P0, 14 P1, 26 issues y **7 PRs** (#1373-#1379) con test que falla antes y pasa después. El patrón nº1 con 6 casos fue "el guard existe en un camino y falta en el hermano" (`ajustar_lote_manual` sí bloquea stock negativo, `ajustar_stock_manual` no; Sidebar sí distingue `proximamente`, menú móvil no). 3 falsos positivos retirados con evidencia antes de reportarlos. Fix del P0 con listener nativo `beforeinput` (la prop `onBeforeInput` de React es un polyfill de composición IME, NO el evento nativo) reutilizando `parseNumeroEs`. Ver [[input-number-nativo-corrompe-importe-es]] · [[un-comentario-que-afirma-una-invariante-es-una-deuda-de-test]] · [[auditar-un-lado-de-par-simetrico-revisar-el-espejo]]
- 2026-07-31 · TuFacturaIA · **el euro salía dos veces en los PDFs de Obras que se envían fuera, y las notas de la factura perdían sus párrafos**: `fmt2` ya devuelve el símbolo y los dos generadores de PDF de Obras se lo concatenaban otra vez, así que un texto tipo con `@total@` imprimía `1.234,50 € €` en presupuestos y pedidos (2 usos malos de 135 — arreglo local, la función no se toca). En paralelo, 4 de 5 plantillas pintaban el bloque de notas sin `pre-wrap`: el usuario escribe en un `<textarea>` y salía párrafo corrido. Ese segundo llevaba meses congelado por un comentario que decía que **no se podía arreglar** «porque cambiaría documentos ya emitidos»; medirlo lo desmintió (76 facturas propias con notas, 0 con salto de línea). Cerrado con #1412 y #1413. El `/fia-cierre` encontró **dos defectos en el trabajo propio**: un caso de test satisfecho por la píldora TOTAL del propio PDF, y dos comentarios que quedaban mintiendo al mergear. El mismo fallo estaba en un tercer sitio y se cerró el mismo día con **#1415** (`obra-pedido-pdf.tsx:151` más los dos que pintan `obras_presupuestos.descripcion`): ahí `observaciones` era el ÚNICO bloque de texto libre del fichero sin `pre-wrap`, entre vecinos con idéntico padding, fontSize y lineHeight — ese contraste es lo que distingue un olvido de una decisión. Sus 34 casos en prod resultaron ser **todos de la org sandbox**: primero se reportaron como «34 pedidos reales» y comprobar de quién eran corrigió la severidad a la baja. Ver [[la-aguja-de-una-asercion-sobre-el-documento-entero-debe-ser-unica-de-la-feature]] · [[una-prohibicion-escrita-en-un-comentario-merece-medirse-antes-de-aceptarla]]
- 2026-07-31 · TuFacturaIA · **el receptor de WhatsApp no tenía antirreplay y dos orgs reales veían declaraciones fiscales que no les tocaban**: segunda tanda de la auditoría funcional, 8 PRs más a prod (#1398-#1411, total 21) y mig **600**. (1) La firma HMAC de Meta se calcula sobre el body y no cubre timestamp, así que un body capturado vale para siempre — y ni hace falta atacante, Meta reintenta agresivamente; por ese webhook entran altas de gasto por foto, turnos de copiloto que escriben y opt-outs de cobros. Candado por `message.id` contra una PK, reclamado antes de despachar. **Su primera versión lanzaba si la tabla no respondía → 500 → más reintentos**: el guard habría causado lo que evitaba. (2) Dos hojas del Centro Fiscal gateaban con `orgHasFeature` a secas en vez del gate unificado, así que con el add-on manual se veían casillas e importes por URL directa aunque el módulo estuviera `proximamente` (**2 orgs reales, una de cliente**); el issue solo nombraba una de las dos, la otra apareció grepeando los hermanos. (3) Aprobar un documento marcado `posible_duplicado` no pedía nada pese a que su docstring decía «fuerza revisión humana» — es el incidente `ingesta-001`, IVA soportado duplicado; ahora exige confirmación mostrando con qué factura choca, y queda auditado quién la dio. **Abierto y verificado**: `src/lib/email/` no tiene NI UN gate por `is_test`, así que una organización de pruebas manda email real al mundo, incluido el aviso de soporte a la dirección del equipo. Ver [[anti-replay-que-lanza-convierte-el-webhook-en-500-y-causa-mas-reintentos]] · [[el-guard-va-en-la-transicion-unica-que-comparten-todos-los-caminos]] · [[supabase-gen-types-numeric-override-bigint-string]]
- 2026-07-31 · Agentesia (chatbot propio) · **ningún lead de WhatsApp se guardaba en la hoja y el bot confirmaba igual**: el `googleSheetsTool` "Registro Sheets" del `ChatBOT mejorado` (`89B9QN23hOHDq6oP`) tenía `columns.value = {}` con `mappingMode: defineBelow`, así que lanzaba `"At least one value has to be added under 'Values to Send'"` en cada captación — pero el AI Agent se traga el error de sus tools y la ejecución sale `success`, de modo que el cliente recibía "Listo, el equipo te llama" con la hoja vacía. Solo sobrevivían los leads que además disparaban Slack. Salió de cruzar la conversación 43 de Chatwoot (Alba, 30-jul) contra las ejecuciones 2476-2505: la 2491 llamó a `Think` → `Registro Sheets` (error) → `Lead caliente` (ok). Fix: 11 columnas mapeadas con `$fromAI`, calcadas del gemelo web que sí funcionaba (mismo Sheet y misma credencial), con el **teléfono desde `chatId`** en vez del modelo. De paso, medido que el marcador `[FIN]` no se emite nunca (0/30) y que su `IF` lleva las dos ramas al mismo nodo → 12 conversaciones abiertas sin cerrar. Y la doc del vault señalaba como bot vivo `enVlCyi7McKfwkRQ`, apagado desde abril. Ver [[error-de-tool-de-ai-agent-no-marca-la-ejecucion-como-fallida]] · [[if-con-ambas-ramas-al-mismo-nodo-no-hace-nada]]
- 2026-07-31 · cryptobruj-bot · **todos los backtests del repo llevaban meses siendo optimistas por una constante que nadie verificó**: `FEE_TAKER = 0.0004` con el comentario «comisión taker típica de futuros». La tabla oficial de BingX dice **0,05%** — un 25% de subestimación que viajaba a cada número derivado (barrido de 480 configuraciones y banco de búsqueda incluidos) sin que nada petase. Corregirlo movió la estrategia base de −0,158R a −0,180R. Fix: constante única con **fuente y fecha de verificación en el comentario**; la señal de alarma era el adjetivo («típico»). En la misma sesión, renombrar una clave de un dict dejó `/strategies/{id}` devolviendo **500 para todas las estrategias** con 207 tests en verde: los tests probaban la función que la produce, nunca a quien la consume. Y la conciliación recién estrenada gritó «46 entradas fantasma» el primer día — era arranque en frío, no un bug, y una alarma que salta con todo el universo deja de leerse. Ver [[una-constante-tipica-sin-fuente-contamina-todo-lo-construido-encima]] · [[probar-el-productor-y-no-el-consumidor-deja-un-500-con-la-suite-verde]] · [[una-alarma-que-salta-el-primer-dia-deja-de-leerse]] · [[cryptobruj]]
- 2026-07-31 · TuFacturaIA · **una migración se aplicó sin efecto y el CLI dijo `Finished`**: la mig 601 (FEFO deja de auto-elegir partidas caducadas) declaró `aplicar_movimientos_lotes(uuid, boolean)` con un `p_revertir` inventado que ni aparecía en el cuerpo; la firma real es `(uuid)`. Con firma distinta `create or replace` **no reemplaza, crea una sobrecarga**: quedaron dos funciones, la vieja intacta y sin filtro y la nueva inalcanzable, porque todos los llamantes hacen `perform ...(p_factura_id)` con un argumento. `db push` imprimió `Finished` y `migration list` la dio por aplicada. Se detectó verificando `pg_proc` en vez del mensaje del CLI: **dos filas donde debía haber una**. Corregido con la mig 602 (`drop function` de la sobrecarga + replace sobre la firma real) y verificado: una sola función, con el filtro dentro. El learning ya estaba escrito desde mayo y no estaba en `hot.md`, así que no se tenía delante. Cierre de la auditoría funcional: **24 PRs**, 27 de 30 issues, y `qa-014` resuelto redirigiendo el destinatario (no bloqueando) tras medir que bloquear por organización habría apagado los avisos internos del equipo. Ver [[postgres-rpc-firma-identica-create-replace]] · [[senal-de-entorno-de-pruebas-describe-la-org-no-al-destinatario]] · [[guard-colgado-de-un-flag-calculado-una-vez-solo-cubre-ese-instante]]
- 2026-07-31 · TuFacturaIA · **el emparejado de ingesta llevaba enlazando facturas a la empresa equivocada**: la RPC `resolve_proveedor` buscaba por NIF y, si no encontraba, SEGUÍA por nombre exacto, alias y similitud difusa, así que un NIF informado que no casaba no vetaba nada. El alcance se midió DOS veces y la primera estaba mal: 92 filas sin agrupar por organización (mezclaba la sandbox); separando `is_test`, **8 en clientes reales y solo 2 son atribución a otra empresa** (IET, en libros, ninguna dentro de una declaración). Las otras 6: una es el mismo NIF con prefijo de país y cinco son NIF mal leídos sobre el proveedor correcto, todas en `sin_aprobar`. Eran **tres** implementaciones del mismo emparejado (RPC, bandeja y `ensureProveedor`), las tres prometiendo por comentario que el NIF era la identidad fuerte, ninguna cumpliéndolo (`byNif ?? byNombre` es el mismo fallo con match exacto). Amplificador: la bandeja escribía el nombre leído por OCR como **alias** del proveedor enlazado, con lo que un solo enlace erróneo se auto-perpetuaba y ningún NIF volvía a desmontarlo (hay proveedores con los nombres de dos empresas distintas guardados como alias). Fix mig 604 + candado redundante en el caller; **la reparación va aparte** (`qa-031`, script escrito y verificado en seco, sin ejecutar), porque toca la contabilidad de un cliente real. En la misma tanda: el guard de duplicados de `qa-022` cubría **1 de 14** casos porque colgaba de `review_reasons`, un flag que el OCR calcula una vez y no reevalúa. Ver [[un-identificador-que-no-casa-tiene-que-vetar-el-respaldo-por-nombre]] · [[guard-colgado-de-un-flag-calculado-una-vez-solo-cubre-ese-instante]] · [[proveedor-aliases-ocr-dedup]]
- 2026-07-31 · TuFacturaIA · **la captura de emails de organizaciones de prueba tenía tres puertas traseras, y dos eran justo las que se usan al montar una org de pruebas**: `qa-014` metió el gate dentro del wrapper `sendEmail`, pero las invitaciones de miembro las manda Supabase Auth con su propio SMTP (`inviteUserByEmail`), así que ni pasaban por el wrapper ni dejaban fila en `email_log`. Salida: en una org `is_test` no se invoca `inviteUserByEmail` sino `generateLink({type:'invite'})`, que crea el usuario igual pero DEVUELVE el enlace, y el correo sale por el wrapper. El criterio no se duplica: el helper decide el emisor y `captura-org-test.ts` sigue siendo el único sitio que decide el destinatario. Al tocarlo apareció un bug latente que tapaba un cast: `redirectTo` iba anidado bajo `options` cuando la firma lo quiere plano, así que se descartaba en silencio y el invitado volvía a la Site URL en vez de al callback de invitación; el `as Parameters<...>` era lo único que impedía que TypeScript lo dijera. El tercer camino (un `sendEmail` propio de la integración Gmail) estaba muerto y en otro fichero del que decía el issue: borrado con seis greps de por medio, incluido el contrato `Provider`, que solo pide `ping`. El cuarto (reenvío de verificación desde el navegador) queda EXENTO por escrito: el destinatario es la propia persona que se registra y todavía no hay organización. Guard nuevo que comprueba la LLAMADA y no la palabra, porque prohibir el identificador falla contra los comentarios que explican por qué ya no se usa. Ver [[senal-de-entorno-de-pruebas-describe-la-org-no-al-destinatario]] · [[un-comentario-que-afirma-una-invariante-es-una-deuda-de-test]]
- 2026-07-31 · TuFacturaIA · **reparadas en prod las dos facturas de IET que colgaban del proveedor equivocado** (314,84 € y 5.666,87 €, ambas de 2026-07-15 y ambas `pendiente`): pasan de GUARCONSA (A78875499) a COMERCIAL ELÉCT. ALCOBENDAS (B84753292) y SUM.ELECTRICOS P.DIRECCION (B81955411), creados con el NIF del documento; los dos alias ajenos de GUARCONSA retirados; dos filas en `audit_log` con el proveedor anterior; cero discrepancias restantes en esa organización. Ninguna había entrado en una declaración, así que no hubo que reabrir nada. **El primer intento falló ENTERO y no escribió nada, que es lo que tenía que pasar**: `audit_log.user_id` tiene FK contra `auth.users` y el UUID centinela `00000000-…` no existe ahí, así que al ir todo dentro de un bloque `do $$` la transacción revirtió completa; se comprobó que la reversión fue limpia (0 proveedores creados) ANTES de reintentar con `user_id=null` + `actor_type='system'`. Lección: meter la reparación entera en un `do $$` es lo que convirtió un error de FK en "no pasó nada" en vez de en "medio arreglo aplicado". Ver [[un-identificador-que-no-casa-tiene-que-vetar-el-respaldo-por-nombre]]
- 2026-07-31 · TuFacturaIA · **la suite E2E llevaba meses sin poder fallar por la razón correcta**: `E2E_BASE_URL` apuntaba a un puerto sin nada escuchando y Playwright no levanta el servidor (no hay `webServer` en la config), así que cada spec agotaba su timeout de navegación y la tanda acababa en "9 de 13 rojos" tras media hora. Eso se lee como "el código está roto", nadie miraba los rojos concretos, y el gate de cierre venía sacando la dimensión `smoke` en reservas en 3 de los últimos 8 cierres por esta causa. Los tres sitios que documentaban el puerto se contradecían. Fix: comprobación de entorno en el setup que aborta en el acto con un mensaje que dice qué levantar. Con eso, primera tanda concluyente en meses: 109 pasan / 8 fallan / 23 saltados; de los 8, dos son contención (pasan aislados) y uno es PREEXISTENTE, demostrado levantando un worktree del commit anterior a la sesión y viéndolo fallar igual (sin esa comparación se le habría colgado al cambio más reciente). De paso, el script de limpieza del residuo tampoco había corrido nunca: abortaba siempre por exigir una org que vive en el otro proyecto Supabase, y además llevaba un `\set ON_ERROR_STOP on` que hace fallar `supabase db query` con un 42601, o sea que el comando documentado en su propia cabecera no podía ejecutarlo. Ver [[un-checker-que-se-pone-rojo-por-la-razon-equivocada-es-peor-que-no-tenerlo]]
- 2026-08-01 · TuFacturaIA · **`tufacturaia.com` responde 404**: la landing pública está terminada y commiteada en el repo `tufacturaia-web` pero nunca se desplegó; `app.tufacturaia.com` sí responde 200 → cualquier enlace de marketing al dominio raíz manda al visitante a un error. Fix pendiente: desplegar. Detectado al enlazar el producto desde la web de la agencia.

- 2026-08-01 · TuFacturaIA · **reemplazar un número estaba roto en las 144 casillas de la app**, importes incluidos: `<input type="number">` no reporta selección (`selectionStart` siempre `null`), así que el interceptor de `beforeinput` de `NumberField` solo sabía añadir → seleccionar todo y teclear encima concatenaba (33+"7"=337). Regresión del 30/31-jul (#1374, #1398). Fix: nodo `text` mientras tiene el foco + `queueMicrotask` para ganar al restore de React. → [[input-type-number-no-reporta-seleccion-y-el-interceptor-solo-anade]]
- 2026-08-01 · TuFacturaIA · **las acciones globales de `/admin` no dejan traza desde siempre** (precios de plan, `plan_features`, catálogo de features, módulos, invitaciones): `logAdminAction` hace `if (!orgId) return` y esas llaman con `null`. Verificado sobre el histórico: `*price*`/`*plan*`/`*modulo*`/`*invite*` → 0 filas; `*feature*` → 542, todas con `org_id`. → [[helper-de-auditoria-con-early-return-deja-sin-traza-lo-global]]
- 2026-08-01 · TuFacturaIA · **`/calendario` se caía entera** —pantalla «This page couldn't load»— al abrir el modal de un día con una factura sin vencimiento: `f.vto.split()` sobre `null`. Basta UNA recibida sin vto (lo normal entrando por OCR; la sandbox tiene 8) para dejar el calendario inservible en esa org. Latente desde el 5-jul. El E2E lo reportaba como «no encuentro el botón Hoy» porque el primer clic desmontaba el árbol: síntoma a dos saltos de la causa. Raíz: `r.vto as string` sobre una columna `string | null`, o sea el compilador desactivado. → [[quitar-el-cast-enumera-los-consumidores-el-optional-chaining-tapa-uno]]
- 2026-08-01 · TuFacturaIA · **el patrón «audita antes de borrar y si no puedes, no borres» nunca podía dispararse**: `logSystemAction` se tragaba los fallos por su `catch` Y por no mirar el `{ error }` que devuelve supabase-js en lugar de lanzar, así que el `try/catch` del caller destructivo era código muerto y el borrado seguía igual. → [[supabase-js-no-lanza-en-insert-asi-que-el-try-catch-del-caller-es-codigo-muerto]]
- 2026-08-01 · TuFacturaIA · el despliegue habría dado por aplicada la migración de billing de #1445 y se la habría saltado en silencio (8 orgs, 5 de clientes reales, sin poder dar de alta 2ª empresa) → seis migraciones del bloque de obras se aplicaron a prod ANTES de mergear y ocuparon el hueco 607 que luego reclamó #1445 → renumeradas a 612-618 y ledger reparado con `migration repair`; lo paró el hook de pre-push, no una revisión. Ver [[aplicar-migraciones-a-prod-antes-del-merge-caduca-la-reserva-de-numero]]
- 2026-08-01 · TuFacturaIA · todos los campos numéricos de la app invertían los dígitos al teclear (1,2,3 → «321»), 67 ficheros, desde #1443 → reasignar `input.type` en un microtask tras cada pulsación resetea la selección a 0 y el interceptor inserta al principio → PR #1446; jsdom no lo reproducía, lo cazó un gate conduciendo el navegador. Ver [[jsdom-no-reproduce-el-reset-de-seleccion-al-cambiar-input-type]]
- 2026-08-01 · TuFacturaIA · **el primer intento de aplicar una migración a prod lo paró un CHECK que no leí**: escribía texto libre en `billing_accounts.complimentary_reason`, que solo admite `beta|cortesia|interno` → 23514, transacción abortada, prod intacta (verificado: mig sin registrar, mismas cuentas, mismas orgs sin cuenta). Nada lo cazó antes porque los asserts de la migración corren DESPUÉS del insert y la sandbox no sirve: el backfill apunta a UUID de prod y allí el bucle recorre cero filas, o sea que pasaría en verde sin insertar nada. El chequeo barato que sí funciona: leer los CHECK y las FK de la tabla y validar contra ellos los valores antes de aplicar.
- 2026-08-01 · TuFacturaIA · **una factura de proveedor que había dado entrada a material no se podía ni editar ni eliminar** (Chivite, ticket nº130): el bloqueo pedía «deshaz el movimiento de stock» y eso no existe en la app (el ledger solo crece; el ajuste manual inserta otra fila). El fix (mig 620) revierte el inventario y el coste medio → pero su primera versión bloqueaba la propia factura del ticket, porque el ajuste manual que el usuario había metido para salir del paso activaba el guard de saldo negativo. Ver [[el-parche-del-usuario-atascado-dispara-el-guard-del-arreglo]]
- 2026-08-01 · TuFacturaIA · **la vista previa y el «enviar prueba» de Ajustes → Emails llevaban rotos para TODOS los clientes**: `email-preview` y `email-test-send` vivían bajo `/api/admin/*` pidiendo `propietario|admin`, pero el proxy ya gatea ese prefijo por superadmin, así que su `requireRole` nunca se ejecutaba. El cliente recibía un 307 al dashboard y el `res.json()` del panel reventaba con ese HTML. Invisible porque quien lo probaba era superadmin. Lo clasifiqué yo como «severidad baja, sin fuga, solo rompe una invariante»; era un fallo funcional. Fix: movidas a `/api/settings/*`, que es la auth que de verdad tienen. → [[un-gate-en-el-proxy-vuelve-codigo-muerto-el-gate-del-handler]]
- 2026-08-01 · TuFacturaIA · **un gate de emisión correcto habría dejado sin facturar a 4 clientes reales**: exigir domicilio fiscal del emisor (RD 1619/2012) bloqueaba a 4 orgs reales (una con facturas ya emitidas) y a una sandbox con 1340. Medido ANTES de mergear; ya estaba rompiendo 2 tests del camino de voz sin que nadie relacionara el rojo. Decisión de producto: aviso recurrente en vez de bloqueo, asumiendo que se puede ignorar. → [[antes-de-mergear-una-validacion-que-bloquea-cuenta-a-quien-bloquea]]
- 2026-08-02 · TuFacturaIA · **`SUPERADMIN_EMAILS` daba acceso cross-org invisible, retirado (A-0)**: concedía superadmin sin fila en `profiles`, y ese booleano permitía en toda ruta con sesión operar contra cualquier org y saltarse el gate de escritura, sin salir en el panel de miembros ni en el log. Llevaba meses «pendiente» porque retirarlo a ciegas revoca en silencio a quien lo use: faltaba **leer la variable**, no arreglarla. Tenía dos emails, ambos ya con el flag en BD. La leían tres sitios, no uno. → [[privilegio-concedido-por-env-es-invisible-y-no-se-retira-a-ciegas]]
- 2026-08-01 · cryptobruj-bot · **ABIERTO** · el bot llevaba **32 h en `mode: live`** (no paper) con las 5 estrategias de la familia Cryptobruj —las medidas como perdedoras— operando: **451 operaciones, expectancy −0,244R, PF 0,58, −1.613,33 USDT** realizados, justo lo que predecía el backtest. Nadie lo puso en esta sesión y el checklist pre-deploy daba verde: comprueba que el compose NO hardcodea `TRADING_MODE` (correcto) y el `.env.example` dice `paper`, pero el valor real vive solo en el panel de Dokploy. Se descubrió por `curl /health` justo antes de desplegar. Despliegue abortado; sin tocar `/stop` (cerrar posiciones vivas es irreversible y es decisión del usuario). Pendiente: confirmar en BingX si es dinero real —`/config/live` dice `max_notional 10` pero hay posiciones de 1.632 USDT, no cuadra— y decidir si se paran las 5. → [[no-hardcodear-el-modo-lo-hace-inverificable-desde-el-repo]]
- 2026-08-02 · TuFacturaIA · **mergeé el código antes de aplicar su esquema y prod quedó llamando a una RPC inexistente**: `/api/obras/materiales/familias` en 500 `db_error`. Entre el merge y el `db push` se cayó el pooler de Supabase (`Connection timed out` por CLI y por psql, con GitHub y la app respondiendo bien: era la red de un túnel de viento, no Supabase). Alcance acotado —el resto de endpoints en 200— pero era la pantalla del encargo vivo de la clienta, y guardar un descuento habría fallado igual por las columnas de la 625. Mitigado revirtiendo los dos PRs (#1468, prod sana a los 60 s); repuesto (#1469) tras aplicar 624/625/626 por el SQL Editor con su registro en `schema_migrations` dentro de la misma transacción. Orden bueno: `db push` → `migration list` → merge. → [[aplicar-migraciones-a-prod-antes-del-merge-caduca-la-reserva-de-numero]]
- 2026-08-02 · TuFacturaIA · **el cron de retención de logs llevaba 3 días en `error` y nadie lo miraba**: `logs-retention-sweep` fallaba el 31-jul, 1-ago y 2-ago con `api_request_log_delete_failed: Bad Request` — el 400 del edge por meter 1.000 UUID en la URL del `DELETE ... .in('id', ids)` (500 pasan, 700 no). 15.006 filas sin purgar. Descubierto leyendo `cron_runs`, no por una alerta. Y mi primer fix (bajar el lote de SELECCIÓN de 5.000 a 1.000, para que el `break` del bucle funcionase) **no arreglaba esto**: un tope de lectura más pequeño no acorta la URL del borrado. Dos límites, dos arreglos. → [[postgrest-max-rows-trunca-silencioso-in-revienta-url]]
- 2026-08-02 · TuCRMIA · **fuga entre clientes en el esquema ya aplicado, encontrada por auditoría y sin datos reales de por medio**: `get_user_org_id()` se copió de TuFacturaIA con su forma y sin su guarda —`select active_org_id from profiles where id = auth.uid()`, sin comprobar membresía—. Como `active_org_id` es escribible por `authenticated` (hace falta para cambiar de organización), el helper devolvía «la organización que el usuario dice». Un ex-miembro o un invitado que no aceptó, que conoce el UUID ajeno porque la política de `org_members` enseña todas las membresías propias sin mirar el estado, hacía un PATCH sobre su propio perfil y leía censo con correos de invitación, equipos y ajustes de esa organización; después devolvía el valor y no quedaba rastro. Las políticas de `leads` se salvaban por accidente, porque además comprueban el rol. Cerrado en la migración 003 arreglando el helper, no política a política. Ahora tiene un test que se pone rojo sin el arreglo (`npm run db:replay`). → [[rls-multi-org-active-vs-membership]]
- 2026-08-03 · TuFacturaIA · **una alerta ALTA de «OCR de facturas en fallo» llevaba 4 días abierta y no describía lo que pasaba**: OpenAI había devuelto `400 invalid_value` ("The uploaded file could not be processed") para UN documento, y `extractOnce` lanzaba lo mismo que para «el modelo no existe» → el caller lo traducía todo a 502, el worker quemaba tres reintentos contra un 400 permanente y se paginaba por org por un fichero roto de un usuario. Encima no podía auto-resolverse: la fila de `bandeja_ingesta` ya no existía, así que nadie iba a volver a pasar OK por ese camino. Mis dos hipótesis apuntadas el 02-ago («residuo de los smokes de stock» o «502 reales de OpenAI») eran las dos falsas; salió de leer `system_alerts`, no de razonar. Fix #1492: `esDocumentoNoProcesable` reparte el 4xx y el camino «archivo» va a `revisar` sin reintento ni alerta. → [[4xx-que-culpa-al-archivo-no-es-caida-del-pipeline]] · [[alerta-que-se-resuelve-al-volver-a-funcionar-queda-huerfana-si-su-sujeto-desaparece]]
- 2026-08-03 · TuFacturaIA · **un origen de incidencias entero sin quien las cerrara, y el panel escondiendo las viejas sin decirlo**: `resolve-ia:job_fallido:*` se emitía y no había NI UNA llamada a `resolveSystemAlert` para esa clave en todo el repo → de sus 18 avisos en 7 semanas, los 15 cerrados se cerraron a mano y 3 seguían vivos con sus tickets ya en `resuelto`. Mi primer diagnóstico («falta un barrido de caducidad») era falso: la causa era falta de dueño, y un barrido por antigüedad habría cerrado avisos vivos. Salió de mirar el ciclo de vida por `source`, no el código. Para forzar el cron de cobros y comprobar el fix de correo sin esperar 15 h hubo que medir primero el radio: 17 facturas candidatas, **todas de la sandbox `is_test`**, cero orgs reales — un cron que reclama dinero no se dispara sin esa cuenta. Arreglado con los dos cierres reales (#1495) y, aparte, el tope de 100 del panel —que corta por `created_at desc`, o sea que se comía las MÁS ANTIGUAS— ahora lo dice (#1496). El guard que comprueba la regla destapó **8 emisores más igual de huérfanos**, ninguno con un solo disparo en su historia: nombrados en la lista de excepciones, no arreglados a ciegas. → [[un-guard-enumera-la-clase-que-la-regla-escrita-solo-documenta]] · [[alerta-que-se-resuelve-al-volver-a-funcionar-queda-huerfana-si-su-sujeto-desaparece]]
- 2026-08-03 · TuCRMIA · **el append-only de la evidencia lo tiraba un `truncate`, y el replay que debía cazarlo era ciego**: con la migración 013 aplicada, el replay en verde y el rojo del `revoke update, delete` demostrado contra un Postgres real, se consultó el ACL efectivo de producción en vez de leer las migraciones. `TRUNCATE` estaba concedido a `anon`, `authenticated` y `service_role` en las **doce** tablas — y `truncate` no pasa por RLS, así que sobre `leads` vaciaba las filas de todas las organizaciones de una línea. Causa: las migraciones revocaban los cuatro DML **enumerados** sobre el `grant all` que Supabase concede por defecto; los otros cuatro (`TRUNCATE`, `REFERENCES`, `TRIGGER`, `MAINTAIN`) seguían vivos. No era explotable por HTTP —PostgREST no expone `truncate` y ningún rol tenía `CREATE` en el esquema—, pero convertía en promesa lo que se llamaba garantía. Y el hallazgo de segundo orden es peor: `db:replay` no podía verlo porque su bootstrap no reproducía los privilegios por defecto del proveedor, así que las tablas nacían limpias ahí y sucias en producción. Migración 014 + bootstrap fiel leído de `pg_default_acl`, con el rojo demostrado quitando la migración. → [[truncate-salta-rls-y-sobrevive-al-revoke-de-update-y-delete]] · [[el-replay-que-arranca-mas-limpio-que-produccion-es-ciego]]
- 2026-08-03 · TuCRMIA · **el enlace de acceso llevaba a `http://0.0.0.0:3000`, y solo se veía desplegado**: el callback construía el destino con `new URL(request.url).origin`, que detrás de Traefik es el host interno del contenedor. En local es invisible porque los dos hosts coinciden. Un cliente habría gastado su enlace de un solo uso para acabar en una dirección que no existe. Arreglado con `Location` relativo, que resuelve el navegador contra el origen que ya usa — y no con `X-Forwarded-Host`, que la escribe quien esté delante. → [[request-url-detras-de-un-proxy-trae-el-host-interno-del-contenedor]]
- 2026-08-03 · TuFacturaIA · **yo tiré el runner de tickets con un `COPY` y el síntoma culpaba a la infraestructura**: el Dockerfile enumeraba sus `.mjs` uno a uno y el módulo nuevo (`reconciliar-prs.mjs`) entró al repo sin entrar en esa lista. El build **no falla**, la imagen se despliega, el contenedor arranca, el `import` revienta con `Cannot find module` y el proceso muere. Lo único visible desde fuera es que nadie reclama los jobs, y a los 60 min el watchdog los cierra con «nadie reclamó el job — runner caído o pausado», que suena a Dokploy o a cuota de Claude. **Dos tickets de cliente (138 y 139) se rescataron así** antes de que nadie abriera el Dockerfile. Reproducido con Docker antes de tocar nada (imagen de `main` → falta el fichero y el import falla; con `COPY *.mjs ./` → los 6 dentro y resuelve), arreglado con el glob y con un guard que compara los `.mjs` del directorio con lo que el Dockerfile copia — el fallo no fue escribir código, fue **empaquetarlo**. Al volver el runner, el reconciliador cerró de una vez los **63 jobs** que llevaban meses clavados en `pr_abierto`. → [[dockerfile-que-lista-modulos-uno-a-uno-mata-el-servicio-sin-fallar-el-build]]
- 2026-08-03 · TuCRMIA · **me cargué el límite por clave metiendo el límite por IP, y lo desplegué**: el contador anti-fuerza-bruta se aplicaba a TODAS las peticiones antes de autenticar, con el tope de §7.2 —60/min por IP— y eso dejaba **inalcanzable** el de 600/min por clave: una integración legítima detrás de un NAT se habría comido un 429 a las sesenta. Corregido el mismo día a contar **intentos fallidos**, que es lo que un control anti-fuerza-bruta cuenta: el tráfico bueno no lo toca nunca. Lo habría cazado `smoke:v1` en dos minutos —hace ~600 peticiones autenticadas contra el despliegue y habría muerto en la 60— pero se corrió DESPUÉS del push. La lección no es el bug, es el orden: **un cambio en el pipeline de una API pide el smoke antes de subir**, no después. Verificado el arreglo contra el despliegue: el 429 llega a las ~1100. → [[un-limite-por-ip-que-cuenta-todas-las-peticiones-anula-el-limite-por-credencial]]

- 2026-08-03 · TuCRMIA · **la aplicación entera se pintaba sin su sistema visual, desde el commit 1**: `tokens.css` declara los 86 tokens de color, radio, sombra y `--focus-ring` dentro de `:root[data-theme="light|dark"]` —viene de TuFacturaIA, donde el atributo sí se escribe— y aquí `layout.tsx` pintaba `<html lang="es">` a secas. Un selector que no casa no define nada, así que el navegador se quedaba con lo heredado y seguía: sin fondo, sin marca, y sin anillo de foco al tabular, que además es accesibilidad. Comprobado contra el servidor construido (`curl /login` → cero apariciones de `data-theme`), no deducido. Lo peor no es el atributo: `tokens:check` estaba **en verde y tenía razón** —comprobaba que los tokens estuvieran ESCRITOS, no ENCHUFADOS—, que es el mismo fallo que el límite de tasa construido y sin conectar. Arreglado con `data-theme="light"` fijo (el selector de tema es del 009 y necesita cookie espejo) y con una segunda mitad del gate: si todos los bloques que definen un token condicionan por un atributo, la app tiene que escribirlo. Lo encontró la lente `interfaz` de la auditoría, que corría por primera vez. → [[un-token-definido-bajo-un-selector-que-nadie-produce-no-existe]]
- 2026-08-03 · TuFacturaIA · **el abono de proveedor no se puede aprobar y el aviso manda a una puerta que no existe**: la recibida en negativo cae en el guard `importe_negativo` (422) de `aprobarRecibida`, correcto y con excepción para `tipo_documento='abono'` — pero ningún control de la app sabe poner ese campo en una recibida, así que la excepción es inalcanzable. El mensaje sí se pinta y es claro («Corrige la base y el total con "Editar"»), solo que **no se puede obedecer**: ni el modal ni la bandeja exponen base/total. Ticket 138 de Natalia; su 369R llevaba parada desde el 02-ago. Probado en el sandbox con navegador: puesto `tipo_documento='abono'`, el mismo botón da 200 y pasa a `pendiente` con el negativo intacto. → [[una-excepcion-de-validacion-sobre-un-campo-que-la-ui-no-sabe-poner-es-inalcanzable]]
- 2026-08-04 · Centro Elphis · **el bot (chat y voz) respondía preguntas de composición de sustancias y no avisaba al equipo con contactos previos**: sin regla en el prompt, un usuario preguntó si el tussi lleva heroína y el bot contestó (reportado por Alba); aparte, "ya tuve visita/ya hablé con vosotros" no disparaba el aviso al equipo — probado en vivo contra la API real, el LLM daba el texto correcto pero no llamaba a la tool en la mayoría de los casos, incluso con instrucción muy directiva. Fix: bloqueo de sustancias en ambos prompts (chat `router-ia` y voz, verificado 4/4 con intento de jailbreak) + pre-check determinista por regex en chat (nodo nuevo, mismo patrón que `Crisis pre-check`, 9/9 en pruebas) + edges de transición ampliados en voz (Retell Conversation Flow no tiene capa de código, así que ahí no hay garantía dura). → [[tool-description-generica-no-fuerza-ejecucion-de-tool-critica]]
- 2026-08-04 · AGH · una corrida de evals de ~12 $ tirada a la 3/3 → se lanzó con el prompt sin congelar y el defecto apareció DESPUÉS, leyendo las descripciones renderizadas (comprobación gratis hecha tras la de pago) → orden gratis→congelar→commitear→pagar, y ahora lo bloquea el hook `paid-measure-guard` (~8 $ perdidos)
- 2026-08-04 · TuCRMIA · **el despliegue automático llevaba 17 commits fallando en silencio, con `autoDeploy` recién arreglado y dado por fiable**: `package-lock.json` pinnaba `flat-cache@6.1.24`, un paquete que su autor retiró del registro npm después de fijarlo. `npm ci` en Dokploy fallaba desde ese commit; invisible en local porque `next build` de aquí nunca vuelve a bajar esa dependencia. Diagnosticado leyendo los logs REALES del build (`dokploy-safe.sh`), no adivinado. Arreglado regenerando el lockfile (`npm cache clean --force && rm -rf node_modules package-lock.json && npm install`) y verificado tres veces: `npm ci` limpio, `deploy:check --wait`, petición HTTP real contra el servidor desplegado. → [[lockfile-pinna-paquete-npm-retirado-del-registro-build-limpio-lo-revela]]
- 2026-08-05 · agh-iberica · un alta de cliente por voz falla y el usuario la re-dicta **4 veces seguidas** (traza 29-jul) → el guard de duplicado se consulta al PROPONER y nada lo repite al CONFIRMAR, así que el `23505` del índice único sale como genérico ciego («vuelve a pedírmelo») → traducir el error del índice a copia honesta + clase `conflict_duplicate` (PR #890). Ver [[guard-en-codigo-que-predice-un-indice-unico-de-sql-diverge]]
- 2026-08-05 · TuFacturaIA · el pooler de Supabase (`aws-0-eu-west-1`, puertos 5432/6543) dejó de responder ~1 h con la API REST viva (401, o sea sana) → `supabase db push` daba `LegacyDbConnectError` y sin migración aplicada no hay tipos generados, así que la fase se quedó bloqueada a medias → se esperó en vez de rodearlo por la Management API, que habría obligado a tocar `schema_migrations` a mano. Volvió solo. Si pasa: reintentar, y si el CLI se atasca en el rol temporal, `SUPABASE_DB_PASSWORD` desde 1Password.
- 2026-08-06 · agh-iberica · **bypass del HITL en voz: un «¿Vale?» del usuario EJECUTA el lote pendiente** (llamada real 05-ago 14:16 — dijo «¿Vale?» y el agente contestó «¡Hecho!» con dos escrituras) → `normBare()` borra los signos `¿?` **una línea antes** de consultar `BARE_CONFIRMS`, donde está `"vale"`; es simétrico («¿No?» cancela) y alcanza a `¿Correcto?`/`¿Perfecto?`/`¿Sí?`. **El modelo no puede cazarlo**: el atajo determinista existe justo para no consultarlo → issue #945. Los otros defectos de esa llamada PIERDEN dato dictado; este **escribe en el CRM sin confirmación**, que es un inviolable del proyecto.
- 2026-08-06 · agh-iberica · el saludo de todas las llamadas de un comercial decía **«Soy Aquí está, tu asistente»** (muletilla transcrita por el STT como nombre del asistente) → el guard que la rechaza entró el 27-jul y la fila se escribió el **21-jul**: endurecer la entrada no re-valida lo ya guardado, y el scanner que se dejó escrito **llevaba 9 días sin correrse** → `UPDATE` de una fila (alcance medido: 1 de 9). Ver [[un-guard-de-entrada-nuevo-no-revalida-lo-ya-guardado]]
