---
title: Incidentes resueltos — log cronológico
date: 2026-05-10
updated: 2026-09-11
source: claude-code-session
tags: [incidents, postmortem, ops]
---

# Incidents log

Una línea por incidente. Formato: `YYYY-MM-DD · proyecto · síntoma → causa → fix (commit/PR)`. Si el aprendizaje universal ya está en CLAUDE.md, dejarlo aquí solo como pointer.

Para incidentes con análisis largo (>1 línea de causa), crear nota separada en `knowledge/incidents/<slug>.md` y enlazarla aquí con `[[wikilink]]`.

## Reglas
- **Cronológico inverso** (lo más reciente arriba), y la entrada nueva va JUSTO debajo del marcador de abajo. Añadir al final del fichero es el error fácil: ahí vivía el bloque «Archivo», así que lo más nuevo acababa en el sitio de lo más viejo (pasó el 12-ago con cinco entradas).
- **Sin secrets**, sin nombres de cliente sensibles. Proyecto = nombre interno (Simarro, TuFacturaIA, agency-portal…).
- Si el fix ya está en CLAUDE.md como anti-patrón, marcar con `→ CLAUDE.md` para evitar duplicar.
- **Se archiva POR MES, no por antigüedad.** Al abrir mes nuevo, el anterior entero se mueve a `incidents-archive-YYYY-MM.md`. La regla vieja —«si pasa de 80 entradas, archivar las >6 meses»— **no podía dispararse nunca**: a este ritmo nada llega a los 6 meses, así que el fichero creció hasta 202 entradas con el tope escrito al lado. Una regla que no puede cumplirse no es una regla.

## 2026-09

<!-- añade nuevas entradas aquí debajo -->
- 2026-09-13 · simarro · **ningún email de n8n salía desde antes del 9-sep** (`535-5.7.8 Username and Password not accepted`, y la app password de 1Password fallaba también) → una app password de Google se revoca **sola** cuando se cambia la contraseña de la cuenta: no había nada roto en n8n, la clave guardada ya no existía para Google → clave nueva + `PATCH /api/v1/credentials/{id}` con el objeto `data` completo en las dos credenciales; verificado con envío real desde un nodo `emailSend`. Los 15 nodos afectados tenían `onError: continue`, por eso 4 días de correos perdidos sin una sola ejecución en rojo.
- 2026-09-12 · simarro · **la rama larga de cancelación borró 10 citas reales de 4 agendas en una ejecución** (disparada por un lead de prueba SIN `Event ID visita`) → los 9 nodos `Buscar <agente>` recibían el item de `Extraer Calendar Info`, que no lleva `Lead_id`: `query` salía vacía, `getAll` devolvía el calendario entero y `Eliminar <agente>` lo borraba uno a uno — bug latente desde siempre, invisible mientras los leads traían el dato → query desde `$('Resolver Lead Id Cancelacion')` + un Code `Filtrar <agente>` por `Lead ID: <id>` en la description + entrada extra al Merge para el caso "0 resultados". Restaurados desde la papelera de Google Calendar. Ver [[un-borrado-encadenado-a-una-busqueda-no-puede-confiar-en-que-el-filtro-llegue]] · [[un-guard-que-filtra-a-cero-deja-sin-ejecutar-el-resto-de-la-cadena]]
- 2026-09-12 · simarro · **el bot dijo al cliente "hubo un problema al cambiar la hora" y derivó a humano, con la cita sin mover en Kommo** → `Es cambio?` disparaba TAMBIÉN la rama de cancelación (borraba el evento del agente mientras se creaba otro en el calendario general), y `Verificar Cancelacion` sacaba el `leadId` solo parseando descripciones de eventos, así que por la ruta rápida quedaba `null` → IF en falso → sub-workflow sin salida → `did not return a response` → el AI Agent lo leyó como error y lo escribió → rama de cambio reescrita con resolución de calendario en cascada y conexión a cancelación eliminada.
- 2026-09-12 · simarro · **el cierre de la tarea de visita al cancelar nunca había funcionado, en silencio** → el nodo `n8n-nodes-kommo` de tareas ignora el filtro `task_type_id` y devuelve un item con `_embedded.tasks[]`, así que `$json.id` era `undefined` y se hacía PATCH a `/tasks/undefined` → `httpRequest` + Code que filtra `task_type_id === 2`. Ver [[kommo]]
- 2026-09-11 · facturaia/arnés · **el gate de pre-push en rojo 4 veces seguidas sin una sola aserción fallida** (ficheros de test midiendo 322.000-1.038.227 ms, `Failed to start forks worker`, y las VÍCTIMAS CAMBIAN entre corridas) → no era CPU: con carga 13,3 la suma de %CPU de todos los procesos era 419% de 1000%. Era memoria: 13 contenedores Docker vivos para un repo que necesita 6, un Chrome de agent-browser huérfano de 17h, y 29 días de uptime con 16,4M de pageouts → parar los 7 contenedores ajenos y cerrar el Chrome: carga 13,3 → 2,5, la suite de 1.038 s → 247 s, verde. Víctima rotatoria = infraestructura, nunca el test. Ver [[el-porcentaje-de-swap-no-discrimina-thrashing-los-swapouts-si]]
- 2026-09-11 · claude-harness · **un `git commit` encolado 1h46m y muerto por OOM sin commitear** → el troceador del hook que clasifica comandos pesados partía el CUERPO del heredoc del mensaje, y `chore(vitest):` dejaba un segmento pelado `vitest` → quitar las líneas del cuerpo del heredoc antes de trocear, dejando la que lo abre. Ver [[el-cuerpo-de-un-heredoc-no-son-comandos]]
- 2026-09-11 · laserys-las-rozas · **`recordatorios` fallando cada 30 min desde la tarde anterior** (23 ejecuciones, 2 avisos a Slack y 1 correo por pasada) → encendí `alwaysOutputData` en el getAll de Calendar sin tocar el Code node de abajo, que hacía `evento.start.dateTime` sin guarda desde el import: con la agenda vacía el nodo recibe un item `{}` y revienta → flag fuera + `if (!evento || !evento.start) continue;`. Pasada de las 08:30 en verde. Lo que convirtió «hoy no hay nada que recordar» en excepción fue mi propio endurecimiento del día anterior. Ver [[n8n-gcal-getall-empty-no-propaga-downstream]]
- 2026-09-11 · laserys-las-rozas · **cada incidencia llegaba dos veces a Slack** → el error handler posteaba en el canal y además reenviaba al bot central, que repostea y manda el correo; el reenvío tenía `onError: continueRegularOutput`, así que borrar el aviso local habría dejado el canal mudo si el bot central cae → reenvío a `continueErrorOutput` + `retryOnFail` y el Slack local colgado de su rama de error. Verificado provocando un error real: un solo mensaje en `#01-incidencias`. Ver [[al-quitar-un-aviso-duplicado-mira-como-falla-el-que-dejas]]
- 2026-09-11 · laserys-las-rozas · **dato de negocio, no incidencia**: el calendario del bot tiene 0 eventos en los próximos 120 días y 1 solo en los últimos 45, así que los recordatorios están sanos pero no tienen a quién avisar.
- 2026-09-11 · laserys-las-rozas · **el canal de WhatsApp de Kommo amanece sin número vinculado, y soporte dice que no fueron ellos** → verificado en Meta: WABA `2639880919800077` única del portfolio, número +34 919 93 52 14 Conectado/Alta, `phone_number_id` `1350959428096173`, Kommo socio con control total, **18 mensajes recibidos** del 14-ago al 10-sep (Meta sí recibe). El *Registro de actividad* de la WABA no tiene **ninguna** entrada del 11-sep — la última es del 10-sep 18:02 instalando su app `1022173854571346` → la desvinculación es interna de Kommo. Además, el ID que su ficha mostraba (`2603510696778315`) **no existe en el portfolio**, y mi nota del día anterior lo daba por el `phone_number_id` sin haberlo resuelto. Sin reconectar a propósito, para no destruir la evidencia del ticket. Ver [[un-id-que-ensena-un-saas-hay-que-resolverlo-contra-la-plataforma-que-lo-emite]] · [[el-log-de-auditoria-de-la-plataforma-duena-dice-de-que-lado-ocurrio-el-cambio]]
- 2026-09-11 · laserys-las-rozas · **cero mensajes entrantes de WhatsApp en Kommo desde el 9-jul** (y por tanto 0 ejecuciones del chatbot) → el lado Meta está entero y verificado —número `+34 919 93 52 14` Conectado/calidad Alta en la WABA `2639880919800077`, portfolio verificado, Kommo socio con control total sobre la WABA **y** sobre el número, su app `1022173854571346` instalada—, pero el registro de actividad de la WABA no registra ni el OAuth que rehicimos; y la ficha del canal en Kommo sigue diciendo `unknown` / «Límite de mensajes: Desconocido» tras reconectar → los `messages` de Meta no llegan a la app de Kommo para ese `phone_number_id`; solo Kommo lo ve desde su backend. Ticket abierto con las 6 capturas que pidieron. Sin resolver.
- 2026-09-10 · simarro · **una clienta recibió un WhatsApp automático por una visita que ya había pasado**: el nuevo `Calendario_a_Kommo` leía eventos desde `ahora-24h` y metía en el plan citas ya celebradas; al crear el lead en la etapa `Lead Caliente`, un **salesbot de Kommo** (no de n8n: por eso la auditoría de los 31 workflows no lo vio) escribió 1 s después. Fix: planificar solo eventos futuros, y quitar `Lead Caliente` como destino. La clienta respondió y Manu apagó el bot en esa conversación a mano. → [[el-inventario-de-automatismos-no-esta-solo-en-el-orquestador]] · [[dar-de-alta-con-fecha-pasada-despierta-los-automatismos-de-esa-fecha]]
- 2026-09-10 · simarro · **el aviso del nuevo cron iba a postear "sin novedades" cada 5 min** (288/día) en `#01-incidencias`, enterrando las incidencias reales; detectado en la primera pasada automática, antes de que se notara → el nodo `Aviso` devuelve 0 items cuando todo ha ido bien y el de Slack no llega a ejecutarse; habla solo si el fail-closed bloquea o si se planificaron N leads y salieron menos. → [[n8n-un-nodo-sin-items-de-entrada-no-corre-y-corta-la-cadena-hasta-el-aviso]]
- 2026-09-10 · facturaia · **lo cerrado como «no llegó» seguía siendo facturable**: con 26 recibidas, 25 facturadas y 1 diferencia cerrada, una factura tardía por esa unidad pasaba el guard (25+1 no supera 26) y dejaba facturado+cerrado = 27 sobre una entrega de 26 → candado **AL016** en el trigger del puente albarán↔factura (mig 884, #2693), 422 `sobre_lo_cerrado`; y la RPC de aprobación resta lo cerrado en su `least` para no tumbar la aprobación entera. Detectado auditando el ticket 169, sin daño en datos. → [[un-candado-fail-closed-nuevo-tumba-el-flujo-que-ya-pasaba]]
- 2026-09-10 · ecobox · **8 alertas «Bad request» en el Slack del cliente** → NO era producción: un bucle mío con `set -- $W` en zsh no partía la variable y mandaba una URL malformada a 8 webhooks. Cerrado reescribiendo el bucle en Python. Pero destapó un defecto real: la validación de horario colgaba **detrás** de dos llamadas a Google, así que una fecha imposible costaba dos peticiones y salía como 400 con alerta → puerta `Validar consulta` **delante** de Google, y las ventanas invertidas / `2026-04-31` / `10:60` devuelven 200 con motivo en vez de 500. Ver [[zsh-no-parte-por-palabras-y-el-bucle-de-medicion-itera-una-vez]]
- 2026-09-10 · ecobox · **el bug del offset `+02:00` seguía vivo en la creación del evento con la puerta en verde** → `Validar horario` normalizaba `preferred_date`, pero tres nodos rehacían el item desde `Edit Fields` y otros cuatro leían ese nodo directamente, así que Google recibía el crudo; y `sede='Majadahonda'` (la mayúscula que el LLM copia del prompt) hacía que la puerta mirase los festivos de un taller y Google escribiese en el calendario del otro — el 29-sep, festivo local en Las Rozas, se habría aceptado la cita. Los dos arreglados en el **punto de entrada único** (`Edit Fields`), no en los siete consumidores. Verificado en la ejecución 5356. Ver [[normalizar-en-un-nodo-intermedio-no-protege-a-quien-relee-el-de-entrada]]
- 2026-09-10 · ecobox · **el teléfono obsoleto `+34 636 521 315` estaba vivo de cara al cliente** → en la base de conocimiento de voz, en el prompt del bot de WhatsApp y en el botón de WhatsApp del **correo de confirmación**. Contrastado contra Meta (`display_phone_number` = `+34 910 05 48 13`, `verified_name` «Ecobox 360»), no de memoria. Corregido en los cuatro sitios y publicado (voz v16). De paso: Majadahonda no existía en la KB — el agente preguntaba a qué taller y luego daba la dirección de Las Rozas.
- 2026-09-10 · agency-portal · **dos escrituras reales en PRODUCCIÓN inspeccionando la Pizarra en `localhost:3000`** (un proyecto pasado a «Entregado» y otro archivado) → el `.env.local` del dev apunta al Supabase de prod, y el clic salió de coordenadas de un screenshot tomado ANTES de que la ventana cambiara de tamaño → restaurado por la UI (cambio de estado + «Ver archivados» → Restaurar), nunca por SQL, para que la reparación deje su propia fila; las 4 filas de `/agency/audit` no se pueden borrar. Regla: sobre un dev que apunta a prod, la inspección es solo lectura. Ver [[el-dev-server-local-con-env-de-prod-convierte-cada-clic-de-qa-en-escritura-real]] · [[la-ventana-del-navegador-de-inspeccion-no-reflowa-aunque-resize-diga-ok]]
- 2026-09-10 · laserys-las-rozas · **tres fallos silenciosos en la vía de voz, encontrados ejercitando los webhooks reales** → (1) `Reservar` devolvía **200 con cuerpo vacío** ante cualquier fecha no parseable (el nodo de fechas lanzaba), así que Sara no recibía nada y podía confirmar una cita inexistente; (2) reservaba **a 15 meses vista** porque el guard de año solo cubría la vía de chat, y la confirmación decía "miércoles 1 de diciembre" sin año; (3) **Google Calendar caído se leía como "agenda vacía"** en reserva y en disponibilidad — el generador de dobles reservas. Arreglados con `ok:false` + motivo (`fecha_no_entendida`, `demasiado_lejos`, `no_puedo_comprobar_agenda`, `agenda_no_disponible`), techo de 120 días y `Avisar fallo agenda` en Slack, porque `onError: continueRegularOutput` deja la ejecución en `success` y el Error Trigger no salta nunca. Batería de rechazo completa en verde, cero respuestas vacías. Ver [[un-nodo-de-log-con-onerror-continue-puede-no-haber-escrito-nunca]]
- 2026-09-10 · laserys-las-rozas · **los recordatorios se marcaban como enviados ANTES de enviarlos** → un fallo de WhatsApp dejaba el aviso por entregado y el paciente no recibía nada nunca. La marca pasa a un nodo posterior que solo marca lo que salió bien y avisa en Slack de lo que no; y la ventana del scanner sube de ±20 a ±35 min porque con cron de 30 solo había una pasada garantizada y el reintento era suerte. Verificado en un workflow espejo, tres pasadas. Ver [[ventana-de-dedupe-mas-corta-que-dos-pasadas-del-cron-hace-el-reintento-una-loteria]]
- 2026-09-10 · laserys-las-rozas · **el agente contradecía a su propio prompt con datos de hace cinco meses** (teléfonos, "financiación en cuotas mensuales", "consulta médica gratuita") → no era alucinación: salía de la KB de Retell, que inyecta un PDF de abril en **cada turno**, y de un segundo almacén pgvector que consulta `consultar_precios`. Corregidas **las dos** fuentes (KB reescrita y resubida como fichero; 5 chunks reescritos **y reembebidos** + 1 nuevo), y el prompt (v24-v26) enseñado a no leer un precio que no mencione lo preguntado — un umbral de similitud no vale, porque en este corpus "financiacion" puntúa 0,7883 y "que tiempo hace en Madrid" 0,8114. Ver [[la-kb-del-proveedor-y-tu-rag-son-dos-fuentes-y-hay-que-corregir-las-dos]] · [[recall-semantico-sin-umbral-es-confidently-wrong]]
- 2026-09-09 · simarro · **dos agentes `active=true` apuntando a calendarios de Google que ya no existen** (Javier Villalba, Mónica de Lope): la RPC `resolve_property_calendar` devolvía un `calendar_id` muerto, así que las visitas de sus viviendas se agendaban contra la nada → filas a inactivo (la RPC pasa a `source: fallback`, verificado) y, para que no vuelva a pasar, cron diario `Sync_agentes_calendar` (`SrCfPm1g7tXZ2T0r`, 07:15 Europe/Madrid) que da de alta y de baja contra `calendarList.list`, fail-closed y avisando a Slack `#01-incidencias`. → [[una-tabla-que-alimenta-un-matcher-debe-generarse-con-la-normalizacion-del-matcher]]
- 2026-09-09 · simarro · **el estreno de ese mismo sync escribió en real creyéndose en simulación**, con un plan de dar de alta las 6 agendas: el Set `dry` con `type: boolean` por expresión no evaluó, y `.first()` sobre una respuesta de PostgREST (un item por fila) hacía ver la tabla vacía. Lo paró la constraint `unique` de `agent_key`, no el código: **cero filas escritas**, verificado. → [[el-modo-simulacion-tiene-que-ser-un-valor-fijo-no-una-expresion]]
- 2026-09-09 · simarro · **SMTP caído y sin arreglo posible por API — ABIERTO**: la credencial `my0IFrzKpGNhCs8c` devuelve `535-5.7.8 BadCredentials` y la contraseña de aplicación guardada en 1Password está revocada también (comprobado creando una credencial limpia y probándola). Hace falta generar una app password nueva desde la cuenta de Google (2FA): la credencial `xtt0y0L6tTFOq9pd` ya está creada en n8n esperando el valor. Mientras tanto, los avisos de este sync van por Slack, no por correo.
- 2026-09-09 · clinica-zen · **las citas se creaban una hora antes de lo dicho al paciente ~34 días al año** → el offset de Madrid se deducía del número de mes (`mes >= 2 && mes <= 9`) en 4 nodos de `RN0wl8RaRmwLpnfQ`, y el DST entra el último domingo de marzo, no el día 1 → cálculo del último domingo real, con el umbral **en hora local** (02:00 en marzo, 03:00 en octubre), no contra el instante UTC. 20 casos verdes y 6 mutantes muertos; verificado 9/9 ejecutando el bloque YA DESPLEGADO en el motor de n8n, no en local. Ver [[offset-de-zona-horaria-por-numero-de-mes-desfasa-una-hora-las-citas]]
- 2026-09-09 · clinica-zen · **el CC a `info@zendental.es` no llegó nunca a la clínica** → `ccEmail` puesto en la RAÍZ del nodo `emailSend` 2.1 en vez de dentro de `options`: no da error, no avisa y manda sin copia → movido a `options`; probado contra el SMTP real comparando `accepted` (raíz: solo el destinatario; options: destinatario + copia). Ver [[ccemail-en-la-raiz-del-nodo-email-se-descarta-sin-dar-error]]
- 2026-09-09 · clinica-zen · **quien reservaba por teléfono no recibía ninguna confirmación escrita** → `Preparar Datos Voz` sale con `email=None` y `telefono=None` en las 6 reservas retenidas, y el `Send Confirmation Email1` que parecía su confirmación iba a `citas@clinicazen.es` (la clínica); el WhatsApp lo manda el salesbot `63814`, que solo lanzaban `WA Confirmación Cita A/B`, **sin padre y sin que nadie más los lanzara** en los 12 workflows → reconectados en paralelo a `Create an event Voz[0]`/`Voz2[0]` (2 conexiones, 0 nodos tocados, `onError: continueRegularOutput` ya puesto). Delatados por la posición: seguían alineados en la misma Y que su padre. Pendiente capturar email/teléfono en la llamada (Retell). Sin estrenar: 0 ejecuciones desde el PUT.
- 2026-09-09 · facturaia · **Borja completó el OAuth del conector MCP dos veces y el asistente seguía diciendo «not found»** → había pegado la **raíz** del dominio (`https://mcp.tufacturaia.com`) en vez del endpoint (`.../mcp`), y como todo el OAuth cuelga de la raíz el login va perfecto y el fallo no aparece hasta la primera llamada MCP, ya fuera de la pantalla; **lo mandaba el manual** → manual corregido, `hint` con el endpoint real en el 404 de la raíz, y discovery path-aware (RFC 9728 §3.1) para el sufijo `/mcp`. Firma diagnóstica: `consent_granted`+`token_issued` en verde y **cero filas en `api_request_log`**. PR #2662.
- 2026-09-09 · facturaia · **`codex mcp login` fallaba con «authorization endpoint origin does not match the authorization server origin without issuer-bound callbacks»** → nuestro AS está **partido** (consent en `app.`, `issuer`/`token` en `mcp.`) y no mandábamos el `iss` de RFC 9207, así que un cliente con la defensa anti-mix-up de RFC 9700 §4.4 no podía atar el `code` a su AS y se negaba, **antes del DCR** (de ahí cero clientes de Codex en `oauth_clients`; el primer diagnóstico, «es bug suyo», era erróneo en la causa) → `authorization_response_iss_parameter_supported: true` + `iss` en toda respuesta de `/authorize`, y de paso el puerto del loopback dejó de contar (RFC 8252 §7.3), que habría roto el segundo login igual. App desplegada primero, servicio después. Ver [[partir-el-authorization-server-oauth-obliga-a-mandar-el-iss]] · [[el-puerto-del-redirect-de-loopback-no-cuenta-rfc-8252]]
- 2026-09-09 · simarro · **"Monte Alegre" se ofrecía como "Monte Escorial" y con etiqueta de coincidencia exacta** → el guard fuzzy del nodo `Plan` (`5NRXALN9lBVE9fTs`) existía desde el 8-sep pero solo cubría la query libre, y la voz manda el sitio en `address_contains`/`municipality` (ejec. 12261/12263 y 12298) → mismo predicado `fuzzyStub` aplicado en las **tres** entradas. 19 fixtures reales: PROD 16/19, parcheado 19/19; 5 de 7 mutantes muertos (los 2 vivos son constantes preexistentes que ningún término del catálogo discrimina, reportados como tales). Ver [[project-simarro-buscador-20260907]]
- 2026-09-09 · simarro · **Ana decía "tengo dos" con tres viviendas en cartera** → el literal estaba fijo en `Format For Voice` (el payload ya traía `total_in_catalog: 3`) y, en otra llamada, el LLM aplicó al conteo el tope de "máximo 2 viviendas" del prompt → conteo calculado del payload en cardinales hablados y, con 3+, **pregunta de filtrado** (zona → tipo → presupuesto) en vez de recitar dos. 20 fixtures reales 20/20, 7 de 9 mutantes muertos. Ver [[el-numero-que-dice-el-agente-sale-del-payload-no-del-tope-de-presentacion]] · [[con-varios-resultados-la-pregunta-que-filtra-se-calcula-del-resultado]]
- 2026-09-09 · simarro · **Ana repetía la misma respuesta dos veces seguidas** (`call_924981bcc…`, 91 s) → "¿podría ver los dos?" disparó el edge "pide MÁS opciones" hacia `n_buscar`, el modelo soltó la respuesta entera mientras corría la función (60,7 s) y `n_presentar` se la hizo repetir al volver (74,1 s) → v33: regla global de que mientras corre una función solo hay frases de espera y no se repite al volver, caso "las dos/ambas" resuelto sin transitar, y el edge `e_pres_mas` acotado. Ver [[un-edge-abierto-manda-a-buscar-lo-que-ya-estaba-en-pantalla]] · [[retell-execution-message-description-no-comprometer-accion]]
- 2026-09-09 · simarro · **las reservas se guardaban en el calendario general aunque la vivienda dijera el agente** → `parseAgent` del sync (`nzxtGnblEFwwkofO`/`Prepare Items`) exigía los dos puntos pegados: casaba `Agente: X` pero **no** `Agente Inmobiliario: X`, la forma que usa Ramón en la mayoría; con `properties.agent = null` la RPC `resolve_property_calendar` devolvía `source: fallback`, que es una rama legítima → ni error ni alerta, y el bloqueante llevaba 4 meses en el hub atribuido a que Ramón no rellenaba el campo → regex ampliado a `/agent(?:e)?(?:\s+inmobiliari[oa])?\s*:\s*(...)/gi`, gate contra las 13 descripciones reales (4→12, 0 regresiones) y E2E tras correr el sync: 13/14 activas devuelven `source: agent`. Ver [[un-fallback-legitimo-hace-mudo-un-parser-incompleto]] · [[routing-citas-por-agente]]
- 2026-09-09 · centro-elphis · **la ficha de Clientify de quien llamaba se rellenaba con el nombre del paciente** (Ana llama por Carlos → contacto de Ana llamado «Carlos»; y si no daba el nombre del paciente, `Sin nombre`) → la descripción de `dv_nombre` en el flow decía «la persona que va a ser atendida», pero su consumidor (`Upsert contacto`) la escribe como nombre del contacto, que se identifica por el **teléfono de quien llama**: dos definiciones distintas del mismo campo a los dos lados del webhook → descripción reescrita a «quien está llamando», con instrucción explícita de devolver `Sin nombre` antes que el nombre del paciente (v45), y el sentinel normalizado a vacío en el origen del Code node para que `name_is_placeholder` lo vea. Medido: 3/3 aciertos en dos corridas, antes 0-1/3. Ver [[una-extraccion-inestable-entre-corridas-es-una-instruccion-ambigua-no-ruido-del-modelo]] · [[un-sentinel-de-ausencia-es-una-cadena-no-vacia-normalizalo-en-el-origen]]
- 2026-09-09 · centro-elphis · **a recepción le llegaban avisos de «Solicitud de ingreso residencial — URGENTE» de gente que solo pedía hablar con una persona** → la etiqueta del WhatsApp/email se derivaba del `destino` del enrutado, y `urgencia alta` arrastra `destino='ingreso'` sea cual sea la consulta → etiqueta derivada del `tipo_consulta` con tabla explícita y *fallback* al comportamiento viejo si el LLM inventa un tipo; 42 comprobaciones sobre el `jsCode` real del servidor y 3 mutantes con víctima. De paso, el dedup pasa de 60 min a alcance de llamada (`call_id`): quien cuelga y vuelve a llamar en la misma hora vuelve a avisar.
- 2026-09-09 · centro-elphis · **Laura ignoraba preguntas directas para seguir su guion** («¿qué tratamientos ofrecen?» → «¿qué te ha hecho llamar hoy?», idéntico en dos corridas) → el punto 3 de `intake` ordena dedicar un turno a entender «antes de pedirle nada» y sugiere de ejemplo esa frase; la regla que lo impedía existía, pero al final del punto 4 y **anclada al nombre**, así que no gobernaba ese paso → excepción metida dentro del punto 3 y regla final desanclada (v46). 13/13 en dos corridas, antes 12/13. Ver [[un-prompt-es-una-superficie-con-localidad-no-un-documento]] · [[un-juez-llm-acierta-en-que-caso-falla-y-se-equivoca-en-por-que]]
- 2026-09-08 · centro-elphis · un deal nuevo en el CRM por cada mensaje del mismo paciente → la clave de idempotencia era un candado que no guardaba el `deal_id`, y el `COALESCE` del `ON CONFLICT` no filtraba `{"deal_id":null}` por ser jsonb → paso 2.7 + `NULLIF` y ventana fija por episodio. Sale de ahí un fallo mayor sin arreglar: el bot manda `status:1` en todo PATCH y **reabre deals que recepción cerró**. → [[on-conflict-coalesce-no-filtra-un-jsonb-null-y-el-where-veta-el-update]]
- 2026-09-08 · clinica-zen · lead con cita cerrada vuelve a «Contestados» 8 s después → los 10 nodos `salesbot*` PATCHean `status_id` fijo junto al texto del mensaje → fuera el `status_id`, etapa escrita solo desde `Marcar Pendiente si Reserva*` con guarda de etapas avanzadas. [[el-nodo-que-envia-el-mensaje-no-debe-escribir-la-etapa-del-lead]]
- 2026-09-08 · facturaia · **todo `git push` desde un worktree corría el `pre-push` del checkout principal, 175 commits atrás** → `core.hooksPath` puesto como ruta ABSOLUTA (con valor relativo cada worktree corre el suyo; con absoluto, todos corren el del árbol apuntado, medido en repo de pruebas). Cuatro etapas ausentes —integración, maqueta de layout, pre-vuelo de base, núcleo-vs-dominio— y el log salía MÁS CORTO, que se lee como buenas noticias → `git config core.hooksPath .githooks`, más un caso en la SUITE que lo detecta (un hook no puede detectarse a sí mismo), guard de auto-ubicación en los dos hooks y una línea final que enumera las etapas que corrieron de verdad (#2610, PR #2622). Ver [[core-hookspath-absoluto-hace-que-todo-worktree-corra-el-hook-del-principal]]
- 2026-09-08 · centro-elphis · **el bot abría un deal por cada mensaje, y además duplicaba el que había creado recepción** → la clave `deal-open-<contact_id>` era un candado que no guardaba el `deal_id`, y `Check deal abierto` nunca preguntaba a Clientify si ese contacto ya tenía un deal → paso 2.7 + `NULLIF` contra `{"deal_id":null}` (el `COALESCE` no filtra un jsonb) + consulta a Clientify que **adopta** el deal ajeno; *fail closed* si Clientify no responde: no crea deal pero sí avisa. Verificado con tráfico real (3 pacientes, 7 ejecuciones, 3 deals) y 24 mutantes cazados. Ver [[on-conflict-coalesce-no-filtra-un-jsonb-null-y-el-where-veta-el-update]]
- 2026-09-08 · centro-elphis · **el validador de firma de Meta llevaba dos semanas puesto sin validar nada, y "activarlo" habría matado el WhatsApp entrante** → el nodo usaba `crypto.subtle` y en el sandbox del Code node (`N8N_RUNNERS_ENABLED=true`) `crypto` no existe: poner solo `META_APP_SECRET` daba `ReferenceError` → 500 a Meta → reescrito con `require('crypto')` + `timingSafeEqual`, desplegado en **observación** y despliegue en 4 pasos (`META_APP_SECRET` **y** `NODE_FUNCTION_ALLOW_BUILTIN=crypto` en la misma edición → verificar 100 % `hmac=ok` → `META_HMAC_ENFORCE=true`). Ver [[n8n-task-runner-sandbox-sin-url-global]]
- 2026-09-08 · clinica-zen · **el bot preguntaba «¿sigues ahí?» a conversaciones bien cerradas, y al arreglarlo dejó de mirar las abandonadas de verdad** → el cron decidía por el último mensaje del bot, sin mirar el ESTADO (cita en el campo `1864817`, status del lead, palancas manuales); el primer parche esquivó un CHECK constraint dejando de escribir la fila de descarte, y con `LIMIT 5` sin `ORDER BY` los descartes recirculaban cada 30 min copando el lote → gate de estado + CHECK ampliado a `skipped` + descarte persistido en los dos sitios (`INSERT` y `NOT EXISTS`) + `ORDER BY r.created_at ASC`. Verificado con las ejecuciones 13806 (escribe el descarte) y 13808 (cero filas). Ver [[un-descarte-que-no-se-persiste-recircula-y-con-limit-desplaza-a-los-reales]]
- 2026-09-08 · clinica-zen · **un paciente que cancelaba resucitaba a «Contestados» 37 s después, y una reserva se decidía por carrera entre dos workflows** → la lista `NO_PISAR` del guard del chatbot enumeraba los cierres positivos y no `143` («Perdido») ni `111224991`; y `Update leads` de `Leads entrantes` escribía Contestados en el mismo evento en que el guard escribía Pendiente de asignar → las dos etapas añadidas a `NO_PISAR` (4 nodos) y `Update leads` a `104115975`. Atribuido leyendo el `workflowData` de las ejecuciones 13682/13684/13697, no el workflow vivo. Ver [[al-centralizar-quien-escribe-el-estado-quedan-dos-huecos-tipicos]]
- 2026-09-07 · simarro · **las visitas de dos semanas al año se guardaban una hora antes en Kommo y en Calendar** → el offset de Madrid se deducía del número de mes (`mes >= 2 && mes <= 9`) y el DST entra el último domingo de marzo/octubre; replicado en 3 nodos de `iMoTKZWxYLymGuHF`, encontrado por `grep getMonth()` y no leyendo el nodo ya sospechoso → offset real por `Intl` con resolución en dos pasadas (115/115, incluidas la hora ambigua de octubre y la inexistente de marzo). Sin citas afectadas: las 6 ejecuciones de la ventana de retención eran de hoy, para el 8/9/10-sep. Ver [[offset-de-zona-horaria-por-numero-de-mes-desfasa-una-hora-las-citas]]
- 2026-09-07 · simarro · **se podían agendar visitas en sábado, fuera de horario o en una fecha pasada** → el recheck previo a reservar solo bloqueaba `motivo === 'slot_ocupado'` y `Calc_Disponibilidad` devuelve otros cuatro motivos, que caían por la rama que reserva → invertido a fail-closed (`libre !== true`) en los dos IF (voz y WhatsApp), con `libre` verificado en la raíz de 3 ejecuciones reales; de paso el mensaje ya no dice "justo se ha ocupado ese hueco" a quien pide un sábado. Ver [[un-guard-que-enumera-los-motivos-de-fallo-deja-pasar-los-que-no-enumera]]
- 2026-09-07 · simarro · **el agente de voz prometía una cita que no existía y colgaba** → el flow no tenía rama para `ok:false` de `Reservar` y, sobre todo, el `global_prompt` ordenaba añadir "un agente te contactará" tras CUALQUIER reserva → nodo `n_reserva_no_hecha` con 6 salidas + la línea global partida por resultado + edges reordenados; agente v30 publicado. Pendiente la llamada real de confirmación (la rama la evalúa un LLM). Ver [[una-rama-nueva-del-flow-no-manda-si-el-prompt-global-dice-lo-contrario]]
- 2026-09-07 · simarro · **quien pedía un garaje o una parcela recibía pisos, marcados como coincidencia exacta** → "garaje"/"trastero" solo existían como característica de vivienda, nunca como tipo de inmueble (en catálogo van como `property_type='otro'`) → tipado por contexto en el Router de `5NRXALN9lBVE9fTs` y mensaje honesto cuando no hay cartera; "piso con garaje" sigue siendo una vivienda con plaza. Gate 18/18. Ver [[una-palabra-puede-ser-caracteristica-o-el-inmueble-en-si]]
- 2026-09-05 · facturaia · el proveedor factura en unidades sueltas y el OCR asienta 144/192 donde el papel dice 6/4 envases → el inventario y el coste medio quedan contaminados desde agosto → reparado en una sola transacción y **en la fecha original de los apuntes** (19 rectificaciones, total de la factura sin cambio), más el guardarraíl de coste desviado que ahora para la factura antes de guardarla. Ver [[el-saldo-historico-se-deriva-hacia-atras-desde-el-actual]]
- 2026-09-05 · facturaia · Pescados Chivite recibe un SEGUNDO correo de «incidencia resuelta» → el ticket se había reabierto y el siguiente merge lo volvió a cerrar (el guard idempotente solo protege mientras siga cerrado) → se deja cerrado y lo pendiente pasa al issue #2538. Ver [[reabrir-lo-que-se-cerro-solo-vuelve-a-armar-el-cierre-automatico]]
- 2026-09-05 · facturaia · **un gasto de un cliente llevaba mes y medio sin importarse de FacturaDirecta y la sincronización se declaraba al día** → el cursor incremental persistía el máximo `modificationDate` de todo lo VISTO, así que los dos gastos rechazados fail-closed (línea sin código de impuesto) quedaron por detrás; el de las 12:44 se reintentaba cada cuarto de hora (13 fallos) y el de las 12:28, a 16,5 min de un solape de 10, ni una vez → cursor anclado al fallo más antiguo de la pasada, con excepción por truncamiento de páginas, en `seguirCursor` compartido por las cuatro entidades (#2518). El aviso además solo decía «falló en 1 elemento(s) de 4»: ahora nombra número, contraparte, fecha e importe. Ver [[un-cursor-incremental-que-avanza-sobre-lo-que-fallo-pierde-el-documento]]
- 2026-09-05 · facturaia · **todo embed de PostgREST entre `facturas` y `lineas_factura` empezó a devolver `300 PGRST201` en prod** (el PDF de rescate que recibe el cliente, el cargador fiscal, informes de ventas y dos herramientas del copiloto) → la mig 845 del abono parcial añadió una SEGUNDA FK entre esas dos tablas (`linea_origen_factura_id`), y con dos candidatas PostgREST se niega a elegir; no lo vio ningún gate porque los tipos generados en `main` seguían siendo los viejos y el typecheck pasaba → hints `!factura_id` en los 8 sitios (y `!factura_id!inner` en los dos de stock: **`!inner` no desambigua**), más un guard que deriva los pares ambiguos de las FK de `database.types.ts` en vez de llevar lista — con él aparecieron 2 sitios más, uno roto desde antes de la 845. La causa de fondo es el orden: los tipos se regeneraron después del `db push`, no antes del merge. Ver [[una-fk-nueva-hacia-una-tabla-ya-referenciada-rompe-los-embeds-de-postgrest]] · [[un-fake-que-decide-por-includes-del-select-degrada-en-silencio]] · [[gen-types-linked-no-db-url]]
- 2026-09-04 · facturaia · **el `db push` de la mig 827 dejó prod con el esquema nuevo y `schema_migrations` sin la fila** → su autoverificación buscaba `unidades_por_caja` en `pg_get_functiondef`, que devuelve el `CREATE` entero **con los comentarios**, así que abortó contra la prosa que documentaba por qué esa columna ya no está — y el `DO` va DESPUÉS del `COMMIT`, de modo que el DDL quedó confirmado igual → filtrado de comentarios con el mismo criterio que su candado TS gemelo, reaplicada y verificada por catálogo (#2485).
- 2026-09-04 · facturaia · **la aprobación de una recibida asentaba en el almacén un coste 22 veces menor que el habitual sin preguntar nada** (Chivite, ostras por caja leídas como unidad; contamina el precio medio y ya no lo corrige la siguiente factura) → guardarraíl en `aprobar_recibida_con_lineas` que consulta `stock_coste_desviado` y aborta con `coste_desviado_sin_confirmar` → 409 + diálogo en las tres superficies (#2486, mig 828, ADR-080). Medido retroactivamente sobre 12 meses de prod: dispara en **1 de 33** facturas, y es justo la del incidente. Ver [[un-guardarrail-mide-lo-que-asienta-el-consumidor-no-lo-que-trae-la-entrada]] · [[un-409-con-flag-de-confirmacion-que-nadie-manda-es-un-bloqueo-duro]]
- 2026-09-04 · centro-elphis · **una cancelación de Doctoralia procesada BIEN salió como error rojo en Slack, y con ella los otros 9 códigos de negocio desde el 27-ago** → n8n parte el `throw new Error('<codigo>: <detalle>')` de un Code node en `description` + `message`, y el handler buscaba el código con `startsWith` sobre `message`: severidad por defecto, sin explicación y con clave de dedup genérica (dos pacientes sin enlace en la misma hora = un solo aviso) → detección por igualdad contra `description` limpia y el prefijo de `message`, detalle aparte para la clave por caso; gate ampliado a 463 checks (10 códigos × 3 formas + falso positivo SQL) y 9 mutaciones con víctima; verificado E2E con un workflow temporal. El gate ya estaba en rojo desde el 27-ago y nadie lo corrió. Ver [[n8n-parte-el-mensaje-de-error-en-el-primer-dos-puntos]] · [[un-gate-cuyo-fuente-es-copia-de-lo-desplegado-caduca-y-nadie-lo-corre]]
- 2026-09-04 · agentesia (chatbot propio) · **el bot cerró una demo con un lead, prometió la invitación y no creó nada en el calendario** (Orconsa; el Slack sí llegó, y duplicado) → el system prompt mandaba llamar a una tool `Agendar` que no existe (la real es `Reservar`), y las 3 tools de Google iban con `descriptionType: auto`, mientras las 2 de Slack tenían descripción manual y por eso sí se ejecutaron; además `Reservar` estaba copiado de un taller mecánico (pedía matrícula y modelo) y sin `attendees` ni `conferenceData` → 4 nombres fantasma corregidos, `descriptionType: manual` en las tres de Google, `attendees` + `sendUpdates: all` + Meet, y portada la regla anti-repetición que el bot de la web tenía desde abril. `Reservar` no se había ejecutado NUNCA: la credencial de Google jamás ha escrito en ese calendario, pendiente de prueba en vivo. Ver [[tool-description-generica-no-fuerza-ejecucion-de-tool-critica]]
- 2026-09-03 · facturaia · Subagente (Sonnet, PR D1) aplicó la mig 811 en prod durante un «dry-run» `BEGIN; \i mig; ROLLBACK`: el COMMIT interno del fichero confirmó. Revertida a mano al cuerpo de la 555 y verificada por pg_proc. Causa: el brief no llevaba el sed que quita BEGIN/COMMIT. Regla: pegar el comando entero del dry-run. [[feedback_dryrun_prod_brief_lleva_el_sed_de_begin_commit]]
- 2026-09-03 · facturaia · **Chivite (ticket 169): las facturas de Wildomar no cruzaban con ningún albarán** → el OCR le había creado una ficha gemela (`nif='ESB20987657'` junto a la real `B20987657` con `nif_iva='ESB…'`) porque cada resolutor de proveedor tenía su criterio, y la casación busca por `proveedor_id` → un solo criterio (nif → `nif_iva` único, que corta en empate → nombre) en los cuatro resolutores: `resolve_proveedor` (migs 810/812), `elegirProveedorExistente`, `resolverProveedorPorNombreONif` y `aprobarItem` de `/ingesta`; el cuarto lo vio el gate de cierre y no el code review (#2424-#2436, ADR-064). Casación 4 → 14 pares.
- 2026-09-03 · clinica-zen · **reserva a las 13:00 encima de una cita existente y «miércoles completo» con la mañana libre** → la v67 hacía calcular al LLM la disponibilidad desde los eventos crudos del calendario, y aun con la lista cerrada (v68) gpt-4.1 aceptó una hora que no estaba en ella → huecos calculados en un Code node de n8n (horario, festivos, 48 h, solapes) + guard en `Reservar_crm` que relee el calendario y devuelve `hueco_ocupado` sin tocar Kommo (v70). Ver [[defensa-en-codigo-vs-prompt-llm-para-invariantes-de-dominio]]
- 2026-09-03 · centro-elphis · **una prueba de `registrar-lead` mandó un WhatsApp y un email reales a recepción con datos ficticios** → el flag `update_only` que gateaba el aviso no estaba declarado en el schema del trigger y n8n lo tiró en silencio; el constructor ejecutó sin comprobar el runData → schema corregido, y regla: no se ejecuta `registrar-lead` sin ver `update_only:true` salir de `Recibir inputs`, y toda prueba se cierra comprobando que `Enviar notif` no aparece en `runData`. Ver [[n8n-executeworkflowtrigger-schema-estricto-filtra-campos]]
- 2026-09-03 · centro-elphis · **dos fallos reales el día del despliegue: PATCH 400 «Pipeline stage does not belong the pipeline» (11666 + 10 reintentos) y `invalid input syntax for type bigint` en un lead de voz (11614)** → el POST de deals ignora `pipeline` y guarda el 54955; y el `conv_id` de voz es `postcall-…`, no numérico → todo PATCH con pipeline+etapa+estado+vencimiento, PATCH de consolidación al crear y gate `If conv_id numerico` antes de la caché. El deal real se reparó con replay del payload; dos agentes «failed» por 529 ya lo habían hecho. Ver [[clientify-post-deals-ignora-pipeline-y-un-patch-parcial-reevalua-etapa-y-vencimiento]] · [[un-agente-que-reporta-fallo-por-529-puede-haber-terminado-el-trabajo]]
- 2026-09-03 · facturaia · **`supabase db push` de la mig 818 (abono parcial) dejó prod a medias**: el `CREATE UNIQUE INDEX CONCURRENTLY` falló con SQLSTATE 25001 dentro del pipeline del CLI, con columnas y puente ya creados y la 818 sin registrar → swap del índice reescrito dentro de la transacción, dry-run con ROLLBACK, segundo push OK y verificado (índice válido, 25 filas de puente, 0 duplicados). → [[unique-index-concurrently-parcial-para-idempotencia-bd]]
- 2026-09-03 · facturaia · **dos facturas reales `cobrada` con 107,79 € de caja fantasma en el ledger de cobros** → el backfill de la mig 644 asentó el pendiente contra el total fiscal y desde la 770 el objetivo es el líquido (IRPF que ingresa el cliente); estado correcto, caja e informe de Cobros no → mig 819 (#2466): anula la fila del bruto con motivo, inserta el líquido, verificación auto-abortante; encontrado rederivando con un criterio independiente del predicado del backfill
- 2026-09-02 · facturaia · **el reintento escalado del OCR (#2385) nunca disparaba en prod**: gpt-5.2 rechaza `max_tokens` con 400 y el catch best-effort lo silenciaba — gate, tests mockeados y 25/25 evals en verde con la feature muerta; lo destapó el smoke real (1 fila de audit donde tocaban 2) → fix #2388: `max_completion_tokens` unificado en `buildOcrRequestBody` + el fallo deja anomalía `ocr_reintento_escalado` en el audit. Tras el fix, Guarconsa 48/48 líneas, Δ=0. Ver [[catch-best-effort-sin-senal-persistente-fallo-parece-no-disparo]].
- 2026-09-02 · facturaia · **el OCR creó un proveedor repetido por el prefijo ES del NIF y una recibida repetida de un albarán ya registrado, y el cliente compensó el stock a mano sin que lo viéramos**: le dijimos «te sumó 5 unidades» cuando ya las había devuelto el 1-ago → causa: el enlace por NIF es estrecho a propósito (ADR-064) pero el alta no preguntaba si existía otra ficha salvo prefijo, y el listado oculta `disputada` → fix: mig 796 retira el papel sin tocar el ledger (#2386), la ingesta para en revisión (#2382/#2393), corrección en el hilo del ticket.
- 2026-09-02 · facturaia · **anular una emitida dejó de reponer stock y solo lo vio el canario `stock_abono_sin_devolucion`** (5 min, un abono dañado: B2026-0006, 52 uds): la mig 792 añadió `and l.cantidad > 0` a los tres agregados de los motores y un abono es TODO negativo por construcción → excluido entero, el espejo del ledger (mig 762) nunca corría. Tres candados en verde: uno exigía la forma rota por regex, otro vigilaba el bucle que el filtro vaciaba antes, y el tercero casaba con un comentario del propio fichero. Fix mig 803 (#2404): filtro por SIGNO CANÓNICO del documento + reparación idempotente en la propia mig; verificado por mutación. Ver [[un-candado-que-fija-la-forma-literal-del-filtro-consagra-el-bug]].
- 2026-09-01 · agency-portal · **el juez de Flota IA llenaba Slack con 27-30 fallos por tick y ninguna tanda quedaba cerrada**: `claim_fleet_judge_run` está declarada `returns public.fleet_judge_runs` (tipo COMPUESTO) y su `return null` llega desde PostgREST como objeto con las 13 columnas a null — truthy, así que el `?? null` del runner no filtraba nada. El runner se creía una tanda real, no rompía el bucle y ese `agency_id: null` viajaba a `runFleetJudgeEnqueue`/`runFleetJudgeDrain`, que filtran con `if (options.agencyId)`: el null **se saltaba el filtro** y ambas corrían SIN acotar por agencia, 9-10 veces por tick, sin dejar fila en `fleet_judge_runs`. Ese vacío es por lo que llevaba días invisible. Fix #598 (guard por `id`, extraído a `isPostgrestCompositeRow` y compartido con `claim_next_feedback_ai_job`, que tenía el mismo latente). Verificado en prod tras desplegar: dos tandas seguidas con `failures: []` y `finished_at`. Ver [[funcion-que-devuelve-tipo-compuesto-con-return-null-llega-como-fila-de-nulos]] · [[un-canal-de-avisos-deduplicado-no-mide-la-frecuencia-del-fallo]].
- 2026-09-01 · agency-portal · **«N resultados no correspondían a ningún job de este lote» eran falsos, todos**: la purga de transcripts de las 04:30 UTC vació 34, y al recoger el lote esos jobs se marcaban `failed` y salían de `jobRefs` — pero sus líneas SÍ venían del proveedor, así que caían en `unknownCustomIds` y se denunciaban como huérfanos (22 avisos, 10+8+4, cero desajustes reales). Fix #598: se parten en descartados-por-purga y huérfanos de verdad, y solo estos últimos avisan. El discriminador tuvo que hacerse durable —releyendo el `last_error` por lote— porque un lote se recoge en varias pasadas. Ver [[un-discriminador-en-memoria-no-sobrevive-a-la-pasada-siguiente]].
- 2026-09-01 · facturaia · **el albarán que entra por el lector no podía decir qué producto es cada línea, y validarlo cerraba el documento vacío**: sin `catalogo_id` ni `material_id` el asiento salta la línea (el `JOIN catalogo_servicios` es INNER) pero el paso (c) la marca `asentada` igual y `validado` es terminal — el género no entra nunca en el inventario y no queda nada que corregir. En prod, **25 líneas en 7 albaranes de un cliente real** (Pescados Chivite), todos aún `abierto`. Fix #2365/#2367: `PATCH /api/albaranes/[id]/lineas/[lineaId]` para emparejar, selector en la ficha, y la mig 790 hace que `albaran_validar` **se niegue** (OB065) con puerta explícita `p_permitir_sin_producto` para portes y servicios. El predicado del guard es el estrecho (`catalogo_id IS NULL AND material_id IS NULL`): el ancho, que es el que usa el propio asiento, habría rechazado **9.567 albaranes ya validados** de Obras. Ver [[el-predicado-de-un-guard-se-mide-contra-el-historico-antes-de-escribirlo]].

## Archivo
- Agosto 2026 → [[incidents-archive-2026-08]] (182 entradas, archivadas el 10-sep)
- Julio 2026 → [[incidents-archive-2026-07]] (110 entradas, archivadas el 12-ago)
- Junio 2026 → [[incidents-archive-2026-06]] (82 entradas, archivadas el 27-jul)
- Mayo 2026 y anteriores → [[incidents-archive-2026]] (archivadas el 13-jul)
