---
title: Incidentes resueltos — log cronológico
date: 2026-05-10
source: claude-code-session
tags: [incidents, postmortem, ops]
---

# Incidents log

Una línea por incidente. Formato: `YYYY-MM-DD · proyecto · síntoma → causa → fix (commit/PR)`. Si el aprendizaje universal ya está en CLAUDE.md, dejarlo aquí solo como pointer.

Para incidentes con análisis largo (>1 línea de causa), crear nota separada en `knowledge/incidents/<slug>.md` y enlazarla aquí con `[[wikilink]]`.

## Reglas
- **Cronológico inverso** (lo más reciente arriba).
- **Sin secrets**, sin nombres de cliente sensibles. Proyecto = nombre interno (Simarro, TuFacturaIA, agency-portal…).
- Si el fix ya está en CLAUDE.md como anti-patrón, marcar con `→ CLAUDE.md` para evitar duplicar.
- Si pasa de 80 entradas, archivar las >6 meses a `incidents-archive-YYYY.md`.

## 2026

<!-- añade nuevas entradas aquí debajo -->

- 2026-07-22 · TuFacturaIA · cliente manda ticket por WhatsApp, OCR no lo reconoce y el bot se queda mudo (cliente sin saber que falló); reintento manual funcionó pero registró `forma_pago="Efectivo"` sin que el ticket lo dijera → 2 causas: `noLegibleComoFactura` no notificaba por WhatsApp en ningún exit temprano del pipeline, y el prompt no tenía regla anti-invención para `forma_pago` (a diferencia de NIF/IRPF/ref. catastral). Fix PR #1156 (aviso WhatsApp + REGLA 17). Ver [[ocr-prompt-campo-sin-regla-anti-invencion-se-alucina]] · [[pipeline-async-solo-notifica-camino-feliz-deja-fallo-silencioso]].

- 2026-07-18 · TuFacturaIA · sub-menú de Obras en el sidebar: opciones visibles pero **no clicables** (reportado sobre prod, deploy ya al día → no era caché) → regresión #993: el grupo desplegable envuelve sus sub-enlaces en `.nav-sublist`, pero el colapso animado (`.nav-collapse` grid 0fr) solo definía `overflow:hidden/min-height:0` sobre `.nav-list` → con el grupo cerrado (estado inicial en `/`) los sub-items se desbordaban visibles Y quedaban `inert` → fix #995 (extender colapso a `.nav-sublist` + anular su margen al cerrar). Diagnóstico con repro de navegador (grupo cerrado 65px→0px). Ver [[colapso-grid-0fr-child-divergente-queda-inert-visible]].

- 2026-07-03 · TuFacturaIA · alertas `/admin/system?tab=alerts` "OCR de facturas en fallo" (×2 sandbox, ×6 org real "Borja Galván") → email transaccional de Salt Edge con logos/iconos embebidos vía `cid:` sin `Content-Disposition` colado como si fueran facturas (ver ADENDA [[ingesta-email-imagenes-inline-firma-tratadas-como-adjuntos-facturables]]) → gpt-4o rechaza correctamente, pero cada rechazo generaba 502+alerta+fila zombi. Fix PR #675 (cross-check Content-ID vs `cid:` en HTML + tamaño mínimo 15KB, 3 providers). 15 filas basura (`bandeja_ingesta`+`facturas`, estado `sin_aprobar` sin datos) limpiadas en BD tras nullificar FK. De paso: org huérfana "Empresa Test Screenshot" (0 miembros/datos) borrada + timezone del cron `agentic-gate-sweep` corregido a `Europe/Madrid` (estaba en `None`/UTC).
- 2026-07-03 · TuFacturaIA · alerta "Cron en fallo: gate de acierto agéntico" → auditoría reveló 2 crons del `CRON_REGISTRY` (`agentic-gate-sweep`, `slack-dispatcher`) sin schedule real en Dokploy nunca creado (0 runs históricos, sin alerta por [[cron-health-desconocido-para-cron-sin-ningun-run]]) + `verifactu-process` (cron fiscal más crítico) deshabilitado desde el 16-may sin que nadie lo notara (0 orgs con Veri*Factu activo aún, sin impacto real). Al re-habilitarlo lo migré por error a HMAC v2 pese a tener auth custom ya documentada → repetido, ver [[dokploy-cron-docker-exec-no-hereda-env-de-app-env]] ADENDA 3. Fix: 2 schedules nuevos creados + verifactu-process restaurado a su comando original + limpiados 3 schedules duplicados (`mcp-dcr-cleanup` x2, `copiloto-recordatorios-batch` x1, `verifactu-process` x1 accidental de esta misma sesión).
- 2026-06-28 · TuFacturaIA · webhooks salientes acumulándose + watchdog de crons en fallo → el webhook-dispatcher murió mid-run (13:13 UTC) dejando lock zombi (`uniq_cron_in_flight`, `finished_at NULL`); el cron-watchdog que debía liberarlo falló en bucle porque `cleanup_cron_zombies()` escribía `status='zombie'` y el CHECK de `cron_runs.status` solo permitía running/success/error (bug latente, 1º zombi real en meses) → dispatcher bloqueado >1h, 0 webhooks perdidos (0 en cola). Fix: mig 412 añade 'zombie' al CHECK + mig 413 desacopla en la función la liberación del lock (sin tocar status) del marcado cosmético (best-effort, captura check_violation). PR #576 (merge --admin, Actions caído por billing). Ver [[update-atomico-no-acopla-liberacion-critica-con-metadata-cosmetica]].
- 2026-06-28 · TuFacturaIA · duplicar facturas/presupuestos + ingesta WhatsApp + aprobar-bandeja fallaban con "null value in column created_via" → la col es NOT NULL sin DEFAULT (mig 038) y 4 inserts directos la omitían (no pasaban por `createDocument`) → fix #567 (factura, llegó a main vía #568) + #571 (presup→'web', WA ingesta→'voice', bandeja→'ocr'). Audité TODOS los `.insert` a facturas/presupuestos; el resto delega en `createDocument`/RPC `convertir_presupuesto_a_factura`. Ver [[facturas-schema-iva_pct-created_via-gotcha]].
- 2026-06-27 · TuFacturaIA · mig `406` duplicada en main (`406_slack_provider_activar` #542 vs `406_eventos_vencimiento` #541, mergeados en paralelo el mismo día) → habría roto `db push` por versión repetida en `schema_migrations` → renumerada la de slack a `409` (#549, cacheado antes de aplicar). Numerar al MERGEAR, no al crear rama. → CLAUDE.md
- 2026-06-25 · Simarro · llamada outbound cuelga aunque el cliente sigue buscando → nodo `n_motivo` era terminal (único edge → despedida) + `n_start` leía "No, era cara" como abandono → abrir `n_motivo` a match/descubrir/callback + afinar condiciones de `n_start`. Ver [[conversation-flow-outbound-gotchas]]
- 2026-06-10 · TuFacturaIA · guardar preset del dashboard daba HTTP 500 → policy `profiles_update` recursiva (`42P17`, subselect a profiles dentro de su propia policy) + hook `use-dashboard-config` leía/escribía profiles con query sin filtro y columna `id` inexistente (PK es `user_id`) → fix hook a `user_id`/`maybeSingle` + función `SECURITY DEFINER` en la policy (mig 240, `bc3a79a`/`f588348`). Ver [[rls-policy-recursion-42P17-subquery-misma-tabla]]
- 2026-06-09 · TuFacturaIA · ia-ops paneles (Copiloto/OCR/WhatsApp/Crons) mostraban texto plano sin layout → CSS Module kebab-case (`.kpi-grid`) + Turbopack no convierte a camelCase → `s.kpiGrid` = `undefined` silencioso → renombrar todas las clases a camelCase directo. Ver [[css-module-camelcase-turbopack]]
- 2026-06-08 · TuFacturaIA · huecos en serie facturas (A2026-0001→0021→0028→0031) → `next_invoice_number_for_org` commitea contador TX1; si INSERT TX2 falla el número queda quemado → mig 232/233/234: contador resuelto dentro del RPC (`SELECT FOR UPDATE` + `UPDATE` + `INSERT` en misma TX) para facturas/abonos/presupuestos/proformas. Ver [[postgres-split-tx-counter-burn-serie-numeracion]]
- 2026-06-04 · TuFacturaIA · bot WA multi-org se colgaba tras elegir empresa → al recablear el OCR inline al webhook SSOT eliminé `const bandeja` pero el `return` del Code node seguía usando `bandeja` (ReferenceError); validé solo sintaxis, no runtime → revert al backup en 1 PUT, fix + harness de runtime con mocks, re-deploy; mono+multi-org verdes en vivo. Ver [[n8n-code-node-validar-runtime-con-mocks-no-solo-sintaxis]].
- 2026-06-02 · Simarro · recordatorio de visita 24h falso al cliente → el cron de matching creaba una tarea (task_type Meeting=2, vence +24h) y el workflow Recordatorios disparaba por CUALQUIER tarea que venciera en ~24h, sin mirar el tipo → fix: Recordatorios filtra `task_type_id===2`, matching pasa su tarea a Follow-up (1), y la RESERVA crea la tarea Meeting con `complete_till`=hora de visita (habilita además los recordatorios reales, que no existían). Ver [[recordatorios-visita-por-task-type]].
- 2026-06-02 · EcoBox · voz Alex no transfería, hacía doble-booking y usaba fecha de hace 4 días → (a) `n-transfer-human` en warm_transfer + nº destino = el del caller → falla siempre; (b) `Reservar_cita` sin check de solapamiento (event_id determinista solo bloquea mismo phone+slot); (c) `global_prompt` con fecha hardcodeada stale → fix: cold_transfer, guard overlap server-side (GCal getAll ventana → IF slot libre), y `{{current_time/current_calendar_Europe/Madrid}}` en vez de hardcode/cron. Ver [[retell-cold-vs-warm-transfer-y-numero-distinto-del-caller]] · [[n8n-reservar-overlap-guard-server-side-anti-doble-booking]] · [[retell-current-time-y-current-calendar-dynamic-vars-evitan-fecha-hardcodeada]].
- 2026-06-01 · EcoBox · bot chat WhatsApp confirmaba reservas/cancelaciones inexistentes, ignoraba audios y decía "miércoles ocupado" en falso → (a) `Respond` hardcodeado a éxito + GCal create/delete con onError:continue ocultaban el error; (b) `Edit Fields` de buscar/cancelar/mirar con `body.args.X` sin optional chaining → null → ventana GCal sin acotar (getAll devolvía todo el calendario) y búsqueda con phone vacío; (c) `IF mensaje cliente` exigía content notEmpty → audios (content null, attachment) descartados; (d) no había nodo de envío de plantilla WhatsApp → fix: gatear Respond+efectos en `!$json.error` (IF created ok), optional chaining en los 3 tools + filtro de ventana, rama audio con transcripción Whisper, nodo `WhatsApp confirmacion` HSM. Ver [[n8n-webhook-tool-respond-no-hardcodear-exito-gatear-en-error-nodo]] · [[n8n-edit-fields-optional-chaining-body-args-plano-vs-wrapped]] · [[chatwoot-nota-voz-content-null-audio-en-attachments-transcribir-whisper]].
- 2026-06-01 · TuFacturaIA · modal `/admin/ia-ops` "casi no se veía" + todo el admin plano → tokens CSS `--panel`/`--border`/`--text` usados en 20+ componentes pero NUNCA definidos en `globals.css` (resuelven a nada: fondo transparente, sin borde, color por herencia roto en dark) → definidos como alias `bg-elev`/`line`/`fg` en ambos temas + `box-shadow` en modal (commit `bf96976`). Ver [[facturaia-tokens-admin-sin-definir]].
- 2026-06-01 · TuFacturaIA · tab WhatsApp de ia-ops mostraba 62 "errores" que eran mensajes normales (NIFs, "hola") → backfill `bot_error_log` (`/api/internal/bot-error-backfill`) escanea TODAS las ejecuciones n8n (sin `status=error`) y clasifica por el mensaje del usuario con fallback `validation_error/warn` → llena la tabla de conversaciones normales. Mitigado en el tab (lista solo severity error/fatal/critical; warns como contador+disclaimer, commit `db7c2d7`). **Fix de raíz pendiente** en el backfill (filtrar a status=error o no default-clasificar éxitos) — también ensucia `/admin/system?tab=bot-errors`.
- 2026-06-01 · TuFacturaIA · selección multi-org del bot WhatsApp muerta ~2 semanas → `Cambiar Sticky Org`/`Cambiar Sticky Auto` (refactor HTTP→Code para HMAC v2 del 30-05) dejaron el `_body` como string literal `={...{{ }}...}` en un Code node, que NO interpola `{{ }}` → JSON inválido a `select-org` → 400 → la sticky nunca se fijaba → ni crear-cliente ni OCR ni selector de documentos multi-org funcionaban. Enmascarado por la sticky `web_switch` (herencia web→bot, ya retirada). Detectado en audit cross-PR (3 agentes). Fix: `_body` como objeto JS (`ops/n8n-patches/apply-fix-sticky-body-interpolation.py`). Ver [[n8n-code-node-no-interpola-llaves-dobles]].
- 2026-06-02 · TuFacturaIA · factura por WhatsApp fallaba ("❌ error al generar el documento") → 3 capas: (a) schema exigía `cliente.id` presente → 422; (b) `INSERT` ciego de cliente con `crear:true` violaba `uniq_clientes_org_nif_pais` cuando el NIF ya existía con otro nombre → 500 "Failed to create client"; (c) refactor HTTP→Code Fase 2.15 dejó "Generar Factura" sin neverError → `error_code`/`message` del backend no llegaban → ramas `nif_required`/`exencion_required` muertas, todo al mensaje genérico. Fix: `cliente.id` `.optional()` + find-or-create por NIF con aviso en conflicto (no facturar a nombre no pedido) + neverError en nodo n8n. Ver [[endpoint-crear-entidad-find-or-create-por-identidad-unica]].
- 2026-06-04 · TuFacturaIA · auditoría de seguridad: 50+ RPCs SECURITY DEFINER ejecutables por `anon`/`authenticated` vía PostgREST con el anon key público (default Postgres `GRANT EXECUTE TO PUBLIC`) → `change_billing_status(org,'active')` = bypass de pago, + IDOR/DoS numeración/abuso OTP → mig 213 REVOKE EXECUTE FROM PUBLIC/anon/authenticated en verbos admin-only (service_role ignora el ACL, sin impacto). Validado en prod (anon→401, authenticated→403). Ver [[supabase-rpc-security-definer-execute-public]].
- 2026-06-04 · TuFacturaIA · "recuperar contraseña" no enviaba email a NADIE → CHECK `email_log_kind_check` (mig 152) sin `password_reset` aunque el tipo `EmailLogKind` sí lo mandaba → INSERT outbox-first violaba el CHECK y abortaba antes de Resend; oculto por el 200 anti-enumeración del endpoint → fix mig 214 (amplía CHECK, idempotente). Evidencia: auth log `user_recovery_requested`+`generate_link` 200 pero 0 filas `password_reset` en `email_log`. Ver [[enum-nuevo-en-codigo-sin-ampliar-check-bd-rompe-insert-silencioso]].
- 2026-06-05 · TuFacturaIA · toda factura OCR (WhatsApp) iba a "Requiere revisión" con "falta NIF/nombre/total" pese a tener los datos → `detectAnomalies`/`audit-write` leían claves fantasma (`nif_emisor/importe_total`) que ningún productor emite (shape real: `prov/nif/total`); además bank-match/p95/iva-check muertos en silencio → fix PR #148 (alinear lector + tests al shape real) + backfill prod 13 ingestas. Ver [[consumidor-lee-claves-que-productor-no-emite]].
- 2026-06-09 · TuFacturaIA · panel admin /admin/system?tab=storage mostraba '— sin nombre' en todas las orgs → supabase-js .in() con UUID col devolvía vacío silenciosamente aunque psql+service_role funcionaba → JOIN movido a RPC mig 236 (storage_usage_by_org añade org_nombre). Ver [[supabase-js-in-uuid-column-text-empty]].
- 2026-06-10 · TuFacturaIA · glass/blur invisible (horas de debug) → lightningcss dejaba solo `-webkit-backdrop-filter`; Chrome moderno lo ignora → `backdrop-filter: none`, sin error de build. Fix: escribir solo `backdrop-filter` (el build prefija). Diagnóstico clave: curl al chunk CSS servido (no fiarse de headless). Ver [[frontend-css-mobile]].
- 2026-06-10 · TuFacturaIA · subagente de research runaway (~30 ciclos, no matable con TaskStop) escaló a editar código + git y stasheó WIP ajeno; recuperado sin pérdidas vía worktree aislado + backup. Aislar agentes grandes en worktree. Ver [[claude-code-gotchas]].
- 2026-06-11 · Simarro · proxy n8n con nodo googleCalendar delete devolvió 500 "Error in workflow" pero el evento SÍ se borró (2º intento vía httpRequest dio 410 "Resource has been deleted") → ante error de delete GCal en n8n, verificar con GET antes de concluir fallo o reintentar.
- 2026-06-11 · Elphis · `doctoralia-email-sync`: (a) Filter v1 containsAny rompía activación → Code node; (b) `from` en emailReadImap v2 es objeto no string → normalizar; (c) IMAP trigger muerto por ciclos deactivate/activate → restart contenedor n8n; (d) `alwaysOutputData:true` propagaba `{}` a Code node → `$('NombreNodo').first()`; (e) Clientify `GET /v1/deals/?contact=ID` ignora filtro → movió deal de Susana → filtro client-side con `.endsWith('/id/')`. Ver [[clientify]] + [[n8n]].
- 2026-06-11 · TuFacturaIA · RPC `merge_proveedor` (SECURITY DEFINER) expuesta a anon vía PostgREST → mig 245 hizo `REVOKE FROM PUBLIC` pero los grants individuales de anon/authenticated persisten → mig 252 los revoca explícitamente. Ver [[postgres-revoke-public-no-elimina-grants-individuales]].
- 2026-06-12 · TuFacturaIA · notifs "Declaración 130 desactualizada" falsas a diario (reabiertas como no leídas en cada run) → cron fiscal-recalcular-borrador comparaba rango trimestral vs snapshot YTD del 130 → fix PR #214: rango 1-ene→fin-trimestre espejo del calculador. Ver [[cron-drift-detector-debe-usar-mismo-rango-que-genero-el-snapshot]].
- 2026-06-12 · TuFacturaIA · bot WhatsApp mudo durante smoke cuotas → 3 bugs del patch issue 003: IF cuota con salidas invertidas (flujo normal por rama excedida), jsonBody sin stringify, helper neverError tragándose el 402 → 3 PUTs al workflow `pqSWkDIHqmSVHotB` (final versionId 7ccd6dd1) + PR #217 (panel admin pintaba uso 0 en keys de cuota no consultadas). Ver [[n8n-helpers-httprequest-lanza-en-4xx-sin-ignorehttpstatuserrors]] + [[n8n-jsonbody-stringify-evita-control-characters]].
- 2026-06-12 · TuFacturaIA · OCR de facturas caído (502 "OCR OpenAI call failed") → OpenAI devuelve `forma_pago`/`iban` como `null` y el schema Zod del OUTPUT del LLM los tenía `.optional()` (rechaza null) → un solo null tumbaba `safeParse` entero → fix: todos los opcionales del output a `.nullable().optional()`. Además el item quedaba zombi en `procesando` sin aviso → añadidos estado `error` + botón Reintentar (mig 261) + canal de alertas cross-org a superadmin (mig 260, tabla `system_alerts` + email a info@). Ver [[zod-optional-rechaza-null-en-webhooks-n8n]].
- 2026-06-12 · TuFacturaIA · subir logo en /clientes daba "new row violates row-level security" → `logos_select` policy borrada manualmente del dashboard Supabase (mig 031 aplicada en schema_migrations pero policy ausente en storage.objects) → recreada mig 262 + INSERT manual en schema_migrations. Ver [[supabase-storage-upsert-needs-select-policy]].
- 2026-06-12 · TuFacturaIA · ticket cac5eefc: /agentes "Canales de entrada" no cambiaba al cambiar de org → efecto recargaba por `ctxOrgId` pero los setters mergeaban sobre prev sin reset (config de la org anterior quedaba pintada) → fix `5b190344`: reset estado per-org al inicio de `load()`; smoke E2E prod PASS. Ver [[effect-reload-por-entidad-setters-merge-exigen-reset]].
- 2026-06-15 · TuFacturaIA · Conciliación rota en bucle ("No se pudieron cargar los movimientos") impersonando org de Borja → (1) el proxy de queries de impersonación (`impersonate-client.ts`) no reimplementaba `.not()`/`select({count,head})`/`maybeSingle()` que usa la vista → TypeError; (2) `useOrgClient()` creaba un proxy nuevo cada render (el browser client real es singleton) → efecto de carga en bucle infinito de toasts. Fix PR #264: lista única compartida builder↔servidor + test de paridad + `useMemo` del cliente. Ver [[proxy-impersonacion-reimplementa-api-supabase-se-desincroniza]].
- 2026-06-15 · TuFacturaIA · onboarding caído (cero altas nuevas ~2 días, invisible por bajo volumen) → (1) `complete_onboarding_perfil` (mig 284) insertaba 6 series mientras el trigger de mig 211 ya sembraba B/F → `unique_violation 23505` → 500 en /perfil; (2) `getOnboardingState` ordenaba `org_members` por `created_at` (no existe; es `invited_at`) → 42703 tragado por maybeSingle → loop /onboarding↔/dashboard. Fix mig 286 (`ON CONFLICT DO NOTHING` + advisory lock) aplicada a prod como hotfix + `state.ts` invited_at (PR #250 en main, falta deploy Dokploy del código de app). Detectado por E2E real contra prod tras auditoría multi-agente. Ver [[supabase-errores-que-solo-afloran-contra-schema-real]].
- 2026-06-16 · TuFacturaIA · la numeración avanzaba al pulsar "Nueva factura" sin crear nada → el preview de `/generar` llamaba a `next_invoice_number` (UPDATE contador) solo para mostrarlo → RPC `peek_invoice_number` read-only (mig 299, PR #268). Ver [[postgres-split-tx-counter-burn-serie-numeracion]].
- 2026-06-16 · TuFacturaIA · selección de lista crear_doc en WhatsApp no responde → list_reply tratado como botón sin regla en router (drop silencioso, exec "success") → reclasificar a texto en Parsear Mensaje (#272/#273)
- 2026-06-16 · TuFacturaIA · facturas BORME "Sin datos extraídos" (org Abba, ticket gonzalo.riera) → el OCR las leyó bien; el 502 era el bug Zod-rechaza-null ya fijado (`c5bcf81a`, 12-jun); los 2 items se procesaron antes del fix y quedaron varados en `revisar` (no `error`) por race FAF↔await en ocr-process → sin botón reintento → #277 amplía el reintento a `revisar` sin datos. Ver [[recuperacion-ui-gated-por-estado-pierde-items-en-estado-hermano]].
- 2026-06-16 · TuFacturaIA · toasts "no se pudo extraer datos" reaparecen cada vez que vuelves a la Bandeja IA (mismos 2 docs Abba/gonzalo.riera) → `notifiedRevisarRef` (useRef Set de dedup) se reinicia en cada montaje del componente → al remontar la página re-disparaba el toast para todos los items ya en `revisar` sin datos. Fix: sembrar el ref en la 1ª carga sin avisar (`notifySeededRef`), solo notificar transiciones en vivo (PR #276). Ver [[useref-dedup-set-se-reinicia-en-remontaje]].
- 2026-06-16 · TuFacturaIA · facturas Registro Mercantil mal leídas + cartel "revisión manual" fantasma + barra al 90% colgada (org Abba, ticket gonzalo.riera) → (1) `gpt-4o-mini` confunde emisor/NIF en fotos con transparencia del reverso; (2) `requires_human_review`/`review_reasons` solo se escribían al detectar anomalía y nunca se limpiaban → `ocr_trigger_error` viejo persistía sobre facturas bien extraídas; (3) realtime off en impersonación → estado optimista se cuelga sin fallback. Fix PR #291: modelo→gpt-4o + rama de limpieza en audit-write (`FORCE_REVIEW_TYPES` en requiresReview) + polling de seguridad en use-realtime + re-escaneo superadmin. Ver [[gpt-4o-mini-falla-fotos-baja-calidad-bleed-through]] · [[flag-de-revision-derivado-solo-se-sube-nunca-se-limpia]] · [[supabase-realtime-off-en-impersonacion-ui-optimista-se-cuelga]].
- 2026-06-16 · TuFacturaIA · card "Conciliado este mes" descuadra 2cts (tickets de 190€→189,99; ticket bgchivite) → `emitir-ticket` y prefill WhatsApp pasaban la base redondeada (157,02) y el motor recompone total=round(157,02×1,21)=189,99 → pasar neto sin redondear (190/1,21) + toggle "IVA incluido" reintroducido (PR #298/#299, mig 309); data-fix de los 2 docs históricos (verifactu off, sin huella). Ver [[sembrar-base-desde-total-con-iva-pierde-centimo]].
- 2026-06-16 · TuFacturaIA · ingestas WhatsApp colgadas en "procesando 90%" semanas (estado solo avanza por callback OCR de n8n, que nunca llegó / proceso muerto — push-only sin reaper; el "90%" es simulación de cliente con tope) → cron `ingesta-stale-sweep` (`*/30`) marca `error` las filas `procesando` con `created_at > 30 min` + `ocr-process` autodetecta `error` en early-returns (`OPENAI_API_KEY` ausente) en vez de zombificar a toda org. PR #302. Ver [[n8n-502-deja-items-huerfanos-en-bandeja-sin-retry]].
- 2026-06-16 · TuFacturaIA · "Revertir cobro" daba "No se pudo" + cobro por Copiloto invisible en Historial de la factura → (a) el endpoint SELECTeaba `facturas.cobrada_via`, columna referenciada en 3 archivos pero NUNCA migrada → query fallaba → 404 silencioso (fix: mig 311 añade la columna); (b) el trigger `audit_facturas` solo registra cambios con sesión humana (no service-role) y el tool del copiloto auditaba `entidad='factura'` (singular) vs la ficha lee `'facturas'` (plural) (fix: el copiloto audita `'facturas'` explícito). Ver [[trigger-audit-solo-registra-sesion-humana]] · [[consumidor-lee-claves-que-productor-no-emite]].
- 2026-06-16 · TuFacturaIA · flip n8n del gasto desplegado ANTES que el tool (PR sin merge) → "apúntame un gasto" iba al Copiloto que aún no tenía `registrarGasto` → "no puedo" (regresión vs el bot viejo, que sí lo registraba). Revertido al instante + re-aplicado tras el deploy. Regla: deploy del tool → LUEGO flip del prompt que enruta a él.
- 2026-06-16 · TuFacturaIA · runner "Resolver con Claude" recién desplegado martilleó el endpoint interno `/callback` a ~9 req/s en bucle apretado → el RPC de claim con cola vacía devolvía fila-de-nulls `{id:null}` (no NULL) y el runner la trataba como job fantasma → callback con id null → 400 sin sleep. Fix: guard `data?.id` + RPC PL/pgSQL + guard loop (PR #301). El mismo e2e destapó: push reportaba pr_abierto en falso (no chequeaba exit code, #305) y el `git push` disparaba el pre-push hook `npm build` → OOM (fix `--no-verify`). Ver [[postgrest-rpc-composite-devuelve-fila-de-nulls]] · [[runner-headless-git-push-dispara-pre-push-hook]].
- 2026-06-17 · TuFacturaIA · emisión WEB de facturas rota 5 días (500 prod-wide desde 12/06, solo web; WhatsApp/api/móvil OK) → commit `33ee399b` unificó web+móvil mandando `fuente='web'`/`'mobile'`, pero mig 258 solo añadió `'mobile'` al CHECK `facturas_fuente_check` → `'web'` violaba el constraint. Fix: mig 313 añade `'web'` (#314). Detectado investigando un 500 atribuido a stock-lotes. Ver [[enum-nuevo-en-codigo-sin-ampliar-check-bd-rompe-insert-silencioso]].
- 2026-06-17 · TuFacturaIA · MISMO patrón otra vez en `facturas.fuente`: el tool `emitirFactura` del Copiloto (LIVE en web ese día) emite `fuente='copiloto'`, no admitido por el CHECK → 500 en TODA emisión por Copiloto. El preview smoke previo (181,50€) validaba totales pero NUNCA llegaba al commit, así que pasó desapercibido. Fix: mig 315 añade `'copiloto'` (#323). Detectado en el smoke (runner real + commit) previo al flip de WhatsApp. Ver [[enum-nuevo-en-codigo-sin-ampliar-check-bd-rompe-insert-silencioso]].
- 2026-06-17 · TuFacturaIA · Copiloto WhatsApp: tras flipar `anularFactura`, "anula la factura X" devolvía texto "Voy a proceder a anular…" SIN botones (acción colgada). El agente n8n emitía `accion_copiloto` bien (exec 6264); el bug era del runner: persistía las propuestas destructivas como **texto del assistant**, y al acumularse en el hilo el LLM **imitaba la prosa** en vez de llamar al tool. Fix #331 (persistir propuesta como ciclo tool_use→tool_result sintético) + purga del hilo. Ver [[persistir-propuesta-destructiva-como-texto-entrena-al-llm-a-narrar]].
- 2026-06-29 · TuFacturaIA · email de org_signup_invite nunca salía → CHECK email_log_kind_check no incluía 'org_signup_invite' → INSERT outbox-first abortaba; email silenciosamente perdido → fix mig 416. Ver [[enum-nuevo-en-codigo-sin-ampliar-check-bd-rompe-insert-silencioso]].
- 2026-06-17 · TuFacturaIA · los emails de incidencia (`system_alert`) NUNCA salieron en toda la vida del sistema → doble bloqueante: (a) el CHECK `email_log_kind` no incluía `'system_alert'` → rechazaba el INSERT `'pending'` (outbox-first, ni fila); (b) `emitSystemAlert` hacía `void sendEmail` (fire-and-forget) abandonado al retornar el cron; sin fallback SMTP en prod, además el burst >5/s perdía avisos. Aflorado por el nuevo cron `system-health-sweep` (que destapó 2 crons reales caídos: `fiscal-generar-borradores` 6d, `suspend-overdue` 8d). Fix: mig 316 (kind) + await envíos (#325) + retry 429 Resend (#332). Ver [[enum-nuevo-en-codigo-sin-ampliar-check-bd-rompe-insert-silencioso]] · [[fire-and-forget-en-handler-corto-pierde-side-effect]].
- 2026-06-17 · TuFacturaIA (host Dokploy 185.47.13.170) · el **control-plane de Dokploy** (servicios swarm `dokploy`+`dokploy-postgres`) quedó caído ~7h tras un pico de RAM: host de **8 GiB SIN swap** → durante un build del runner "Resolver con Claude" (~3 GiB) + app + n8n, el OOM dejó **containerd/runc wedged** → `runc create failed: ... can't get final child's PID from pipe: EOF` en CUALQUIER contenedor NUEVO (data-plane sobrevivió porque solo se reiniciaba, no recreaba; autoDeploy muerto → #338 y backlog de 7h sin desplegar). NO lo arregló: force-update de servicios, `systemctl restart docker` (reinicia dockerd, NO containerd), ni subir `kernel.threads-max` (estaba en 6765, anormal, pero no era la causa). **Fix: reboot del VPS** (resetea containerd/runc/cgroups limpio) → destapó la causa secundaria: la **BD interna de Dokploy (postgres) con WAL corrupto** (`PANIC: could not locate a valid checkpoint record`, escritura cortada por el OOM a las 00:21) → recuperada con **`pg_resetwal -f`** en contenedor one-off (`docker run --rm -v dokploy-postgres:/var/lib/postgresql/data --user 999:999 postgres:16 pg_resetwal -f ...`) + scale 0→1. Tras eso: 3 servicios 1/1, panel vivo, deploy manual del app recuperó el backlog. **Pendiente/prevención: añadir swap (4 GiB) al host** — sin swap, un build del runner repite el OOM. Diagnóstico decisivo: `docker run --rm hello-world` también fallaba con el mismo EOF → era host-wide, no de swarm. **→ RESUELTO 2026-06-17: swap 4 GiB añadido + host subido a 16 GiB → runner escalado a `RUNNER_REPLICAS=2` (Q6) verificado LIVE sin OOM.**
- 2026-06-17 · TuFacturaIA · runner "Resolver con Claude" gateaba con `lint+typecheck+build`; el build (pico 2-3G) OOMeaba contra el límite hard 3G del contenedor → TODO PR del bot salía en draft con "Gate FALLÓ" pese a estar verde en CI. El swap del host (añadido el mismo día) no extiende el cgroup. Fix: gate = lint+typecheck (CI ya corre el build en cada PR tras desbloqueo del billing; el bot no mergea sin review) + captura de stdout/stderr al cuerpo del PR (#354). Ver [[container-memory-limit-cgroup-swap-host-no-extiende]].
- 2026-06-17 · TuFacturaIA · panel admin inusable en móvil (menú lateral fuera de pantalla, sin forma de abrirlo) → el layout `(admin)` reusaba las clases globales `.sidebar`/`.app-shell` para heredar estilos pero sin hamburguesa ni estado de drawer (la regla global `≤1023px` manda `.sidebar` a `translateX(-100%)`) → `AdminShell` + `AdminSidebar` con `mobileOpen`/`onMobileClose` (drawer + backdrop + scroll-lock), espejo de `dashboard-shell` (#357).
- 2026-06-17 · TuFacturaIA · botón "Resolver con Claude" del email → el acuse 303 caía en `0.0.0.0:3000/resolver-con-claude?r=ok` (ERR_SSL_PROTOCOL_ERROR) aunque el token se consumía y el job se encolaba bien. Causa: el redirect se armaba con `new URL(req.url).origin`, que tras el proxy de Dokploy es el bind interno del contenedor. Fix: `buildAppUrl()` (misma fuente que el enlace del email, host pin a app.tufacturaia.com), #352. Otra instancia del patrón "no construir URLs externas desde req.url" — ver [[next-public-envs-dokploy-runtime-fallback-headers]].
- 2026-06-17 · TuFacturaIA · check "Visual regression /design-system" daba rojo en cada PR que tocaba CSS → en realidad 3 cosas: (a) trigger `src/**/*.css` lo disparaba con módulos scoped ajenos al showcase; (b) `waitUntil:'load'` colgaba el `goto` en el dev server (RSC, el `load` no llega) → "Timeout in beforeEach"; (c) baselines stale. Fix: trigger acotado (#366) + `domcontentloaded` best-effort load (#361) + pre-warm del route (#358) + baselines regenerados en contenedor jammy local (#363, Actions bloqueado por billing). Ver [[regresion-visual-ci-acotar-trigger-y-baselines-en-contenedor]].
- 2026-06-17 · TuFacturaIA (Dokploy) · cron `module-events-purge` en ámbar ~40h sin que saltara alerta (ámbar no notifica, solo rojo) → consecuencia de la caída de 7h del scheduler (OOM): su expresión `*/30 4 * * *` no la recuperó el catch-up (sí los de disparo único). Fix: schedule → `0 4 * * *` (disparo único, 2º disparo era redundante) + run manual para limpiar. Ver [[dokploy-schedule-step-expression-no-catch-up-tras-caida]].
- 2026-06-19 · MCP server caído ~min: editar el env de Dokploy reserializando el blob completo partió las claves PEM multilínea (PKCS8) → crash al arrancar. Fix: reconstruir a single-line `\n` + verificar con openssl (privada deriva la pública de la app). Regla: editar solo la línea objetivo del env, nunca round-trip del blob con PEM multilínea.
- 2026-06-19 · facturaia · ryomakers@gmail.com: botones editar/eliminar numeración ocultos → can_manage=false (GET /series role=null, mismo gotcha #278) → canActorDoAction en handler.
- 2026-06-19 · TuFacturaIA · 3 emails incidencia ALTA "Cron en fallo" (bot-error-backfill, ingesta/email zombie-sweep) → FALSOS: todos los runs `success`, el scheduler de Dokploy solo perdió el tick 02:30 UTC; el umbral `max_intervalo` estaba a ~1× el intervalo → un único tick perdido lo cruzaba. Fix: umbral a 2×+buffer (#393). Ver [[watchdog-umbral-debe-tolerar-un-tick-perdido]].
- 2026-06-19 · TuFacturaIA · MCP OAuth caído ~1h (21:18→22:35): el redeploy del MCP (compose `autoDeploy=false` → llevaba ~1 día sin recibir merges) lo subió a main HEAD y destapó un bug latente de #409 — el verifier del user-token (`src/lib/mcp/user-token.ts` `VALID_SCOPE`) no espejaba el scope `conciliacion:read` que sí añadió `policy.ts` → token rechazado `401 "non-grantable scope"` → cliente OAuth en bucle "Authorization failed" al reconectar. Fix #427: espejar regex + test anti-drift + `compose.deploy` manual. Ver [[mcp-scope-grantable-vs-verifier-divergen]] · [[dokploy-compose-autodeploy-false-no-recibe-merges]].
- 2026-06-21 · TuFacturaIA · /emitidas parpadeo al paginar → filters inline object en useFacturasData → render loop (setLoading loop) → Fix: useMemo en facturas-view (#439)
- 2026-06-21 · TuFacturaIA · Scheduler Watchdog ~100 failures en 4 días → billing GitHub Actions bloqueado desde 06-17 (runner no arrancaba); scheduler Dokploy sano (1440 runs/24h verificados). Fix: schedule trigger deshabilitado (PR #446) + monitor NotCaído heartbeat (ID 34, ntfy móvil, `notcaido-mdm-PN7YzRN3iIA`) + TuFacturaIA pinga `NOTCAIDO_HEARTBEAT_URL` tras cada run scheduler exitoso (PR #447). Reactivar schedule cuando se restaure billing. Ver [[monitor-en-la-misma-infra-no-detecta-su-propia-muerte]].
- 2026-06-22 · Elphis · wa-inbound-bridge caía con "Module 'crypto' is disallowed" en executions 5319/5330/5337 → todos los mensajes WA reales de Meta rechazados → nodo HMAC añadido en sesión anterior usaba `require('crypto')`, bloqueado en sandbox → reemplazar con `crypto.subtle` (WebCrypto global). Ver [[n8n-crypto-module-bloqueado-task-runner-usar-fnv-puro]].
- 2026-06-22 · Elphis · misma persona llama 2 veces → 2 deals en Clientify + 2 notificaciones al equipo → `registrar-lead` no tenía guard por contact_id (cada llamada sin deal_id entrante → `_has_deal=false` → Crear deal) → añadir nodo Postgres `Check deal abierto` (idempotency_log `key='deal-open-{contact_id}'`, TTL 30d) antes de `Crear deal`; second call → `Shape skip`, notified=false.
- 2026-06-23 · TuFacturaIA · banner superadmin mostraba alerta MCP pero tab Alertas vacío → `systemAlertsCollector` filtraba `.neq('source','health-sweep')` → entradas cron no resueltas invisibles al panel. Fix: eliminar filtro + dedup runtime por tipo en `runSystemCollectors` (PR #451). Ver [[alert-collector-cron-vs-live-dedup-gap]].
- 2026-06-23 · TuFacturaIA · bandeja "va y viene"/vacía en prod (sesión) → script de verificación localhost mutó `active_org_id` del user real vía BD compartida (.env.test→proyecto prod, E2E_EMAIL=cuenta real); restore inicial falló por PK errónea (`id` vs `user_id`) → restaurado a mano. Nunca mutar estado server-side de user real en tests. Ver [[verificacion-no-mutar-estado-prod-cuenta-real]].
- 2026-06-23 · TuFacturaIA · deploy mig 378 (stock) bloqueado → la mig 376 (guard inalterabilidad VeriFACTU) estaba aplicada en prod pero solo vivía en la rama `feat/sif-inalterabilidad-376-compliance-pre-beta` sin mergear → `db push` abortaba por histórico divergente ("Remote migration versions not found in local"). Fix: traer SOLO el archivo 376 a main (no repair-reverted: el trigger está vivo) → db push aplicó 378. Ver [[supabase-cli-db-push-fail-por-historico-desincronizado-workaround]].
- 2026-06-24 · TuFacturaIA · email "Cron en fallo: bot-error-backfill" en bucle (1/ciclo) → 2 causas: (a) `N8N_API_KEY` del env de prod (compose `tufacturaia-app`) revocada → 401 en cada run; (b) `system-health-sweep` flapeaba la incidencia (resuelve↔reabre) porque `computeHealth` veía el run en vuelo (`status=running`, ~1s) como no-rojo. Fix: key nueva en env (compose.update solo esa línea + deploy) + `computeHealth` deriva salud del último run TERMINADO, no del in-flight (PR #469). Verificado: run 20:00 success, incidencia auto-resuelta 20:10. Ver [[cron-health-flapea-con-run-en-vuelo-sobre-error]].
- 2026-06-25 · Simarro · reserva por voz no creaba evento Calendar ni tarea ni email interno (exec 5667) → `Get dirección Retell` colgaba en rama paralela y `Create Calendar Retell` (que lo referencia) corría antes → `ExpressionError: node hasn't been executed` cortaba la cadena → re-cablear Get dirección en serie antes del Create event (ambas ramas voz+WA). Ver [[n8n-ramas-paralelas-no-garantizan-orden-poner-en-serie]].
- 2026-06-25 · TuFacturaIA · no se podía guardar razón social/persona de contacto de un proveedor (ticket cliente, 400 `PGRST204`) → el form compartido de `/clientes` y el tipo TS `Proveedor` escribían `empresa` pero la columna solo existía en `clientes` (mig 037, nunca en `proveedores`) → mig 387 `ADD COLUMN IF NOT EXISTS empresa` aplicada por DDL idempotente a prod (`db push` bloqueado por divergencia 386 ajena, no tocada). Ver [[proveedores-no-tiene-columna-empresa-asimetria-clientes]].
- 2026-06-25 · Elphis · chatbot WA dejó de contestar → 4 bugs encadenados en `wa-inbound-bridge`, todos en la rama "crear contacto/conv nueva" (nunca ejercitada porque en pruebas siempre había conv abierta, y se destapó al borrarse el contacto de test): (1) `Search contact` POST→GET `/contacts/search?q=`, (2+3) dos `If notEquals 0` con `null`→rama errónea (fix `null→0`; canónico sería flag `_has_X`), (4) `Shape new contact` leía `source_id` en `payload.contact_inboxes` inexistente (real: `payload.contact_inbox`). Latente desde edición HMAC 22-jun; no se vio hasta que entró un mensaje real (los "success" intermedios eran webhooks de status ignorados → [[n8n-status-success-no-implica-camino-critico]]). Ver [[chatwoot]] §API contacts.
- 2026-06-25 · TuFacturaIA · ventas de productos con lotes no descontaban stock (ticket Chivite) → `lote_id` se perdía en los bordes API/UI (Zod no-strict + mapeos a createDocument) y el motor `aplicar_movimientos_lotes` fallaba mudo sin `lote_id` → fix: propagar `lote_id` (POST/PATCH/v1) + auto-FEFO en el motor (mig 388, PR #488) + reconciliación de las 3 facturas ya emitidas. Ver [[motor-con-input-requerido-debe-defaultear-no-fallar-mudo]].
- 2026-06-26 · TuFacturaIA · .txt 303 oficial salía con cuotas IVA (4/10/21%) en CERO → motor `iva.ts` ponía cuotas en casillas 02/05/08 (en formato oficial 2026 = constantes de tipo%); spec posicional las lee en 03/06/09 → descartadas. 0 exposición real (0 exports oficiales, 7 borradores). Fix L3 #503: cuotas→03/06/09 + labels regeneradas + ref `.txt`. Ver [[consumidor-lee-claves-que-productor-no-emite]].
- 2026-06-26 · TuFacturaIA · checkout Starter cobraba 19€ pese a que BD/UI mostraban 14€ → `STRIPE_PRICE_ID_STARTER_*` del env apuntaba al price viejo de 19€, desincronizado de `plans.precio_mes` (editado por admin) → price nuevo 14€/134,40€ en Stripe live + rotar env en Dokploy + deploy. Suscripciones existentes a 19€ intactas. Ver [[stripe-price-id-en-env-vs-precio-bd-editable-derivan]].
- 2026-06-26 · TuFacturaIA · ticket /emitidas "No se pudo emitir la factura" (org Borja `50a3cfbf`, solo emitía simplificadas) → org creada 21-abr **no tenía serie "A"**: la siembra de A/R/P/T (mig 227/284/286, junio) nunca se backfilleó a orgs antiguas (solo B/F por trigger 211 y S por 303) → `emit_factura_borrador`→`next_invoice_number_for_org` hacía `RAISE 'Serie no encontrada o inactiva'`, oculto tras un 500 genérico en `emitir-borrador.ts`. Fix: backfill idempotente (mig 402, #512/#514, crea las 6 canónicas a toda org que no las tenga) + 409 accionable propagando el error nombrado de la RPC. Gotcha al numerar: prod iba por delante de main (400/401 de ramas sin mergear) → colisión en 401, renumerado a 402. Ver [[migracion-numerar-contra-prod-schema-migrations]].
- 2026-06-27 · TuFacturaIA · `supabase db push` de `main` a prod falló en mig 408 (slack): PART 1 recreaba `facturas_fuente_check` con lista recortada (8 valores) omitiendo `web`/`mobile`/`copiloto`/`recurrente` reales → SQLSTATE 23514, deploy a medias. Fix #553: lista = existente + `slack`; re-push 408+409 OK. 406 (eventos vencimiento, mía) ya había aplicado antes del fallo; el 408 fallido se revirtió entero. Ver [[check-constraint-recrear-incluye-valores-existentes]].
- 2026-06-28 · TuFacturaIA · alerta "10 errores del bot (backend/IA)" en falso → nodo "Log Bot Error → Error Agente" (n8n `pqSWkDIHqmSVHotB`) clasificaba respuestas conversacionales del agente (pedir NIF, cliente no hallado, confirmaciones, éxitos) como `llm_error`; el flag `conversacional` existía pero no gateaba el POST → fix: guard que omite el log si conversacional (56 filas limpiadas, smoke OK exec 7169, `binaryMode` preservado). La otra alerta (cron webhooks) era falso positivo transitorio ya auto-resuelto (outbox=0). Ver [[rama-error-bot-no-debe-capturar-respuestas-conversacionales]].
- 2026-06-29 · TuFacturaIA · notif cobros "Validar cliente" → 404 — `cta_url` generada como `/clientes/<uuid>` (ruta inexistente) en vez de `/clientes?id=<uuid>` (query param que abre el modal) → fix `orchestrator.ts` PR #585.
- 2026-06-30 · TuFacturaIA · todos los adjuntos de email en Bandeja IA salían "Error al procesar" → `process-attachments.ts` tenía `triggerOcrWebhook` local sin `factura_id`; n8n live (`bUqAABW4ITLXFXIp`) tiraba `Error('factura_id ausente')` → stale-sweep los marcaba `error` a los 30 min → fix: importar función canónica `@/lib/ingesta/trigger-ocr` + añadir `factura_id`. Ver [[n8n-webhook-payload-campo-requerido-funcion-local-duplicada]].
- 2026-06-30 · TuFacturaIA · conciliación cuarentena — S2026-0003 cobrada sin movimiento vinculado (ticket 4e46a1ed, bgchivite): `last_revert_at` activo + UI no ofrecía forzar asignación → revert SQL manual (`estado=pendiente`, `fecha_cobro/cobrada_via=NULL`) + audit_log + PR #593 abierto (force UI con textarea motivo en sugBox del drawer).
- 2026-06-30 · TuFacturaIA · WhatsApp OCR: todos los paths non-success silenciosos (quota/upsell/multi-org/account_suspended) sin feedback al usuario → shapes discriminador divergían entre ingesta↔webhook (caller chequeaba data.quota_exceeded; ingesta devolvía data.error='quota_exceeded') + faltaba !res.ok check → fix shapes canónicas + !res.ok guards (fix/ingesta-progress-faster-feedback). Ver [[whatsapp-internal-http-discriminador-shape]]
- 2026-06-30 · TuFacturaIA · Recibos de seguro (IVA=0) descartados como justificante_pago → Sin datos extraídos · causa: PASO 0 prompt requería IVA en definición de factura · fix: definir por EMISOR identificado + aclarar justificante_pago=solo bancario + UX por doc_type · PR #595
- 2026-06-30 · TuFacturaIA · WhatsApp imágenes >4MB silenciosas (ingesta+OCR) → Traefik body limit cortaba POST base64; ingesta devolvía sin `ok:true` → caller interpretaba fallo. Fix: Option B (`processWhatsappMedia` directo sin HTTP) + `ocr-process` descarga de Storage internamente (payload IDs-only, ~200B vs ~10MB). PR #597.
- 2026-06-30 · TuFacturaIA · "El documento ha expirado" nada más pulsar "Sí, subir" en WhatsApp → upsert intent era fire-and-forget; Meta entregó button_reply antes de que terminara la escritura en BD → await upsert con guard de error antes de sendInteractiveButtons (299d225a). Ver [[whatsapp-await-upsert-antes-de-botones-interactivos]].
- 2026-06-30 · TuFacturaIA · build Dokploy servía código viejo pese a `docker builder prune -f` → capas referenciadas por contenedor activo no se purgan → `docker compose build --no-cache facturaia` desde /etc/dokploy/compose/<stack>/code. Ver [[docker-layer-cache-persiste-con-contenedor-activo]].
- 2026-06-30 · TuFacturaIA · botón "Enviar a Conciliación" en Bandeja IA no hacía nada → commit `d534b834` (endpoint + routing-rules + lib) quedó aislado en rama sin mergear `fix/ingesta-progress-css` mientras el frontend (botón) sí llegó a main vía `5404fe9d`; el fetch caía en 404 silencioso. Al portar se detectó que el endpoint original tampoco habría funcionado: para `doc_type` no-factura el OCR de clasificación no extrae total/fecha/prov. Fix: endpoint reescrito para descargar el documento y correr un segundo pase de extracción (`extractMovimientosFromDoc`), igual que el auto-import email/slack. `b1d61b4d`+`2242db18`. Ver [[ocr-clasificacion-doc-type-no-factura-sin-campos]].
- 2026-07-01 · TuFacturaIA · cambios propios (`types.ts`, `ingesta-view.tsx`) mezclados sin querer en un commit ajeno (`68a0f25c`) ya pusheado a main → sesión paralela en el mismo checkout sin worktree hizo `git add -A` sobre mis cambios sin commitear. Sin daño funcional (build/tests verdes); refuerza el inviolable ya documentado de `git worktree add` por sesión paralela en CLAUDE.md.
- 2026-07-01 · TuFacturaIA · Ticket rápido (feature nueva) en prod quedaba `estado='pendiente'` sin marcar cobrado → contaminaba KPIs "por cobrar"/cashflow/aging/vencidas con dinero ya cobrado en efectivo/Bizum. Fix same-day: `marcarFacturaCobrada` tras `createDocument`. Ver [[documento-dinero-ya-cobrado-debe-marcarse-cobrada-explicito]].
- 2026-07-02 · TuFacturaIA · worker VeriFACTU no-op silencioso desde 2026-05-18 → el select central pedía `factura_origen_num` (columna solo de la vista `facturas_con_autor`) sobre la tabla `facturas` → 42703 tragado, cron reportando `{ok:true, procesadas:0}`; 0 orgs activas aún, pero el cutover habría enviado nada a AEAT sin alerta. Destapado al tipar el cliente (PR #648); fix: columna fuera del select + fallback muerto eliminado. Ver [[supabase-select-columna-inexistente-falla-query-entera-42703]].
- 2026-07-02 · TuFacturaIA · misma clase 42703 silencioso en 9 sitios más (PR #648): emails siempre con branding fallback (`organizations.web` inexistente), duplicar presupuesto v1 siempre 404 (`forma_pago`), `is_admin` al crear org nunca concedido (upsert a `profiles.id`, PK es `user_id`), fallback PDF email nunca renderizó (`facturas.lineas`), export fiscal sin NIF (`nombre_fiscal`), getURLPDF presupuestos roto (`documento_url`), acciones admin globales sin auditar (`audit_log.org_id` NOT NULL), /api/health sin reportar modo lectura (shape `enabled` vs `active`).
- 2026-07-02 · TuFacturaIA · el worker VeriFACTU tenía una SEGUNDA capa muerta: `/api/verifactu/process` no estaba en `isServiceRoute` → middleware 307 a /login antes del handler, y el `curl -sf` del cron trataba el 307 como éxito → 0 runs en cron_runs en toda la historia. Fix 1 línea PR #649; smoke verde (primer run: success, procesadas:0).
- 2026-07-03 · TuFacturaIA · Libro IVA XLSX 500 permanente en prod (misma clase 42703, destapado diseñando export contable) → select a columnas x100 inexistentes en `lineas_factura` (cast local anulaba typecheck) → loader compartido derivando de subtotal×iva_pct como el motor fiscal (PR #653). Audit 3-agentes añadió: paginación max-rows, chunking .in(), periodificación devengo, trial fiscal server-side. Ver [[supabase-select-columna-inexistente-falla-query-entera-42703]] · [[postgrest-max-rows-trunca-silencioso-in-revienta-url]].
- 2026-07-03 · TuFacturaIA · cliente real (org Abba) no podía emitir factura a empresa con NIF ya relleno → `validation_error` genérico → fix a medias en el 1er intento: PR #665/#666 arreglaron que la card/factura mostraran `empresa` en vez del contacto (autocomplete de cliente nunca mandaba `empresa` al backend), pero el payload de "cliente ya existente" solo llevaba `{id, nombre, empresa}` — sin `nif`, que antes tampoco viajaba pero el bug estaba enmascarado porque `assertNifReceptor` nunca se disparaba (sin `empresa` nunca detectaba "hay empresa sin NIF"). Al arreglar `empresa`, la validación se activó por primera vez y expuso el hueco del NIF. Fix PR #667: la rama "cliente existente" manda los mismos campos que "cliente libre". De paso, el toast mostraba el CÓDIGO `validation_error` crudo en vez del mensaje explicativo (leía `error` en vez de `message` de la respuesta). Ver [[payload-parcial-cliente-existente-oculta-bug-hasta-activar-validacion-relacionada]].
- 2026-07-03 · TuFacturaIA · /informes en prod: rangos de trimestre corridos un día (widget IVA/comparativa/CSVs incluían el último día del trimestre anterior), columna IVA=total−base (mal con IRPF), cards muertas 'Modelo 303 XML'/'Enviar al gestor' → `new Date().toISOString()` en local + CSV a mano → PR #657 (rangoIsoDelPeriodo + cards conectadas al export contable). Ver [[new-date-toisostring-desplaza-un-dia-fuera-de-utc]].
- 2026-07-03 · TuFacturaIA · pooler Supabase (`aws-*.pooler.supabase.com:5432`) inalcanzable a mitad de `supabase db push` (timeout a las 3 IPs) → mig 426 aplicada vía MCP `execute_sql` (API HTTPS sigue viva) + `gen:types --linked`; columnas en prod pero pendientes de registrar en `schema_migrations` con `db push` al recuperar (no-op por IF NOT EXISTS). Ver [[supabase-pooler-caido-aplicar-ddl-via-mcp]].
- 2026-07-03 · TuFacturaIA · bot WhatsApp 'amnésico' y con cifra/trimestre erróneos (3 capas): orden del intent multi-org (#662) + `void` sobre builders supabase = writes nunca ejecutados (#676, ver [[supabase-builders-lazy-void-nunca-ejecuta]]) + hilo con 100 mensajes anclando la respuesta vieja (reset + regla 'datos frescos mandan' #681, ver [[llm-hilo-envenenado-ancla-tools-frescos-mandan]]). Además: céntimos-string en consultarIvaTrimestral y v1 (#676), estimación en vivo (#678), trimestre actual + formato es-ES (#679), respuesta dual a-presentar/en-curso (#680). Verificado por el endpoint interno real: respuesta dual correcta.
- 2026-07-03 · TuFacturaIA · plantilla custom: nombre de empresa bajo el logo seguía saliendo pese a toggle en `false` en BD → `sanitizeBlockOpts('cabecera')` reconstruye el objeto por allowlist y corre en CADA render (no solo al guardar); `mostrarNombre` no estaba en la lista → se descartaba siempre. Fix + tests de regresión. De paso, bug real independiente: `clientes/proveedores.empresa` (razón social) no se seleccionaba en 5 sitios (listas, modal, asignar-cliente, generación PDF), así que siempre salía el contacto en vez de la empresa aunque las plantillas ya preferían `empresa`. Ver [[sanitizador-allowlist-reconstruye-objeto-descarta-campos-nuevos]] · [[campo-opcional-en-tipo-compartido-no-implica-seleccionado-en-todos-los-selects]].
- 2026-07-03 · TuFacturaIA/Dokploy · fuga de secrets de prod en chat: `compose.one` llamado solo para revisar si un deploy había terminado devolvió el `env` COMPLETO del compose (service role key, API keys LLM, tokens WhatsApp, claves de cifrado, etc.). Sin daño explotado conocido; usuario decidió revisar y rotar por su cuenta. Ver [[docker-infra]] (gotcha `compose.one`/`schedule.one`).
- 2026-07-04 · TuFacturaIA · `supabase db push` (mig 428 datos-backups) abortó: la BD prod tenía 2 migraciones con versión TIMESTAMP (`20260703141924/141936` = 426/427, el `name` lo delata) que una sesión paralela aplicó ese día, no en el repo → CLI bloquea. Reconciliado con `migration repair --status reverted <timestamps>` + `--status applied 426 427` (SQL ya en prod, idempotente) → `db push` aplicó solo la 428. Anti-patrón timestamp-migration ya en CLAUDE.md del proyecto. Ver [[extender-database-supabase-interface-never-anyclient]].
- 2026-07-04 · TuFacturaIA · docs del roadmap G5 (ADR + `g5-capa-a-plan.md`) daban por pendiente la retirada del Agente Facturador n8n y la Capa B (voz/OCR) → auditoría en vivo (API n8n `/executions` + git log + lectura de `system-prompt.ts`/`registry.ts`/webhook route) mostró que el cutover del webhook Meta→Next.js y la consolidación de voz+OCR YA estaban en `main` (commits `8dc30888`/`dd07fef7`/`8dff6a90`); solo queda huérfano el nodo `Agente Facturador` + 16 toolCode en n8n (issue `g5a-005` reabierto con gate de auditoría de tráfico real antes de tocarlo). Refuerza: verificar contra código/API en vivo antes de planificar sobre docs propios, aunque el usuario diga "contexto ya verificado". Ver [[integracion-en-jsonb-tabla-generica-pierde-observabilidad]] (mismo espíritu de verificación).
- 2026-07-04 · TuFacturaIA · casi-incidente: intento de auditar ejecuciones reales de n8n con `op read` timeout de autorización → pegué el JWT del API key literal en el comando bash como fallback → el clasificador de seguridad de Claude Code lo bloqueó correctamente antes de ejecutar nada dañino, pero quedó expuesto en el comando/transcript de la sesión. Sin explotación conocida. Fix: nunca hardcodear el secreto ni siquiera para un `curl` de solo lectura; parar y pedir reautorización de `op` al usuario. Ver [[op-read-secreto-nunca-en-comando-bash-ni-desde-memoria]].
- 2026-07-04 · TuFacturaIA · RECURRENCIA falso positivo cron: email incidencia ALTA "Cron en fallo: Limpieza conectores MCP (DCR)" con TODOS los runs `success`. `mcp-dcr-cleanup` (purga auto-sanable, borra 0 filas) se saltó UN disparo diario (único cron que falló ese día); umbral 36h (<2×24h) lo cruzó a las 12:00 UTC. El fix #393 (2×intervalo, 2026-06-19) no lo cubría. Fix PR #703: `CronMeta.criticidad:'housekeeping'` → collector emite 'media' (sin email) para mantenimiento auto-sanable + umbral→48h + `expresion` registry corregida (50 4→0 0). Autorresuelto tras run manual (schedule.runManually). Ver [[cron-mantenimiento-auto-sanable-no-debe-paginar-severidad-por-criticidad]].
- 2026-07-04 · TuFacturaIA · login con mitad derecha en blanco en iPad portrait (768px exacto): media queries MOBILE AUTH (`max-width:768px`) y TABLET AUTH (`min-width:768px`) se solapaban en ese punto exacto — rejilla pasaba a 2 columnas pero `.auth-left` seguía oculto por la regla mobile. Detectado y arreglado end-to-end por el runner "Resolver con Claude" (ticket `9edebeee` → PR #704, ciclo 3, sin bugs en revisión manual). Fix: límite mobile a `max-width:767px`. Ver [[facturaia]] §NOW/NEXT y `Stack/frontend-css-mobile.md` §Mobile Auth.
- 2026-07-05 · TuFacturaIA · sidebar de `/settings` desplegado verticalmente en iPad portrait (768-1100px) tapaba el contenido — ticket de soporte del usuario. Causa: el modo compacto (barra horizontal de chips) solo activaba bajo 767px, mientras que el layout de 2 columnas se rompía ya a 1100px; la franja intermedia se quedaba sin columna fija Y sin el tratamiento compacto. Fix PR #754: unificado a un solo breakpoint 1100px. Mismo espíritu que el incidente de login del día anterior (breakpoints inconsistentes en dos zonas del mismo componente). Ver `Stack/frontend-css-mobile.md`.
- 2026-07-05 · TuFacturaIA · bot WhatsApp subía imagen/PDF siempre a la misma org (AgentesiaLab SL) ignorando la org elegida segundos antes por texto (Sandbox) — smoke manual del usuario lo detectó. Causa: `processMedia` resolvía por `phone_number_id` (número receptor Meta) en vez de por remitente; con 2 orgs compartiendo el mismo número personal, solo la que tenía `phone_number_id` real wireado ganaba siempre. Fix PR #764: `from_phone` primero (respeta sticky), fallback a `phone_number_id` solo si el remitente no es miembro de ninguna org (preserva el flujo de proveedor externo, wa-fase2-001). De paso, selector de empresa unificado a lista interactiva en texto y documento (antes 3 botones vs. lista). Ver [[whatsapp-multi-tenant-matching-debe-ser-por-numero-remitente-no-por-phone-number-id]].
- 2026-07-05 · TuFacturaIA · PR #705 (cobro Stripe) rompió `migrated-tools-gate.test.ts` en main — `createOrGetPaymentLink(createAdminClient(), ...)` reintrodujo `createAdminClient` en 2 tools ya migradas al patrón port (`enviarEmailFactura.ts`/`enviarRecordatorioPago.ts`), justo el seam que el gate prohíbe. Ningún caller reutilizaba el client para nada más. Fix PR #765: `createOrGetPaymentLink` crea su propio admin client internamente (mismo patrón que `sendFacturaByEmail`). Suite completa 469/469 tras el fix. Ver [[helper-di-client-mover-dentro-si-ningun-caller-lo-reutiliza]].
- 2026-07-05 · TuFacturaIA · reverificando el fix #764 en real: el selector multi-org funcionó bien, pero tras elegir empresa el bot se quedó completamente mudo (ni éxito ni error). Confirmado por BD (`whatsapp_pending_intent` borrado solo tras descarga exitosa) que el flujo llegó hasta el envío final; logs del contenedor sin ninguna traza después de ese punto. Causa: `postToGraph` (transporte base de todos los envíos WhatsApp) nunca logueaba sus fallos, y ~35 call sites del webhook usan `void sendText(...)` sin mirar el resultado — un fallo real de Meta (token/rate-limit/restricción) quedaba 100% invisible. Fix PR #768: loguear errorCode/errorText en el único punto de transporte compartido (sin PII). No resuelve la causa de fondo del fallo de Meta en sí — solo lo hace diagnosticable la próxima vez. Ver [[fire-and-forget-resultado-descartado-esconde-fallo-loguear-en-transporte]].
- 2026-07-05 · TuFacturaIA · siguiendo la investigación del bot mudo: pregunta del usuario ("¿si spamean el botón 10000 veces siempre responde?") destapó una 2ª causa latente de silencio — el fetch del webhook a `/api/internal/whatsapp/copiloto` no comprobaba `res.status`; bajo el rate-limit interno (429, ~60-120 req/min anti-bucle) el body de error caía en la rama genérica `copiloData.error` (solo log, sin respuesta). Fix PR #769: mensaje de cortesía ("espera unos segundos") si el status es 429, en los 2 puntos que llaman al copiloto (texto y audio). Recurrencia del patrón ya documentado — ver [[internal-fetch-res-ok-silencioso]].
- 2026-07-07 · TuFacturaIA · 4 emails incidencia ALTA "Cron en fallo" (slack-dispatcher, data-export-runner, drive-sync-dispatcher, conciliacion-enrich-batch) SIN fallo real → día de blips Cloudflare/Supabase (520/522) tumbó 1 run de cada cron y el siguiente se recuperó solo (auto-resueltas 10-20 min; 99.9% éxito 24h; 7 días previos con 0 fallos). Causa: `computeHealth` paginaba ALTA ante 1 sola observación mala (1 `error`, o 1 `running` colgado antes de que el watchdog lo reapee como zombie). Fix PR #790: exigir ≥2 fallos consecutivos para 'rojo'/email; 1 blip → 'ambar' (panel, sin email). Ver [[cron-alta-frecuencia-exigir-2-fallos-consecutivos-antes-de-paginar]].
- 2026-07-07 · TuFacturaIA · pérdida real de datos en cuenta de cliente (Dani, Holded — no un trial vacío) durante el smoke E2E de la integración: al activar push+pull de contactos, el pull importó 409 contactos reales y el trigger de BD los re-encoló para push de inmediato; el push hacía `PUT` del contacto completo con solo el subconjunto de campos que mapea FacturaIA → Holded reemplazó el objeto entero, perdiendo `iban`/`swift`/`trade_name`/`client_record`/`supplier_record`/idioma/moneda en los 409 contactos. Dani confirmó después que los datos no se habían perdido realmente, pero la decisión ya estaba tomada: eliminar el push por completo (mig 445, PR #792), pull-only para siempre. Ver [[put-objeto-completo-borra-campos-no-mapeados]] · [[facturaia-holded-integracion]].
- 2026-07-09 · TuFacturaIA · el deploy #802 (cola OCR) retiró el endpoint `ingesta-stale-sweep` pero su schedule Dokploy quedó enabled → 404 cada 30 min, invisible en `cron_runs` (un 404 no crea fila; la fila se escribe dentro del endpoint). Detectado consultando la tabla `schedule` de la BD interna de Dokploy por SSH read-only. Fix: `schedule.delete` del huérfano + del `mcp-dcr-cleanup` duplicado muerto (42→40 schedules). Ver [[cron-endpoint-retirado-deja-schedule-externo-huerfano]].
- 2026-07-10 · TuFacturaIA · Supabase (prod) superó la cuota de **egress** → proyecto restringido → prod devolvía `404 page not found` de Traefik (no la app), login roto y TODOS los crons/OCR en fallo con la misma línea `exceed_egress_quota`. El contenedor Docker seguía `(healthy)` (engaña: es otro healthcheck). Probable acelerador: QA pesado del día (lotes de PDFs + previews + capturas). Fix: subir Supabase a Pro; al restaurarse, Traefik re-registró la ruta solo. Ver [[supabase-egress-restringido-tumba-app-via-healthcheck-traefik-404]].
- 2026-07-10 · TuFacturaIA · el cutover del receptor WhatsApp n8n→app (workflow `pqSWkDIHqmSVHotB` desactivado ~4-jul) dejó 2 flujos huérfanos que estuvieron rotos en prod sin avisar: (1) **opt-out de cobros** — cliente que responde «STOP» a un recordatorio seguía recibiéndolos (n8n detectaba STOP y llamaba `cobros-stop`; el webhook nuevo no lo replicaba) → compliance/spam; fix #814. (2) **extractos/justificantes por foto** — se clasificaban y descartaban en silencio esperando el rebote n8n→`movimiento-ingesta`; con n8n fuera se perdían; fix #816 (mig 451). Detectado auditando el nodo n8n retirado contra el nuevo receptor (paridad de flujos), no por un error visible. Lección: un cutover de topología exige auditar CADA rama del componente retirado, no solo el path principal. Ver `project_facturaia_retirada_n8n` (memoria agente).
- 2026-07-11 · TuFacturaIA · DNIs reenviados por email caían en el OCR → gpt-4o rehúsa el documento de identidad (200 con `"I'm sorry,"`/`"Lo siento,"` no-JSON) → `JSON.parse` fallaba → 502 → el worker reintentaba ×3 → estado `error` + alerta ALTA "OCR de facturas en fallo" (FALSA: saltó en 3 orgs reales — Borja Galván, FacturaIA Sandbox, Laserys). Fix #834: un 200 no-parseable → revisión manual sin reintento ni alerta; retry+alerta reservado al fallo real de API (non-2xx). El conector de email ingesta cualquier adjunto (no filtra por asunto) — correcto: clasificar en el OCR, no por keyword. Ver [[llm-responde-200-no-json-no-es-fallo-pipeline]].
- 2026-07-13 · TuFacturaIA · `copiloto-insights` (cron nuevo, PR #865) no disparó: el schedule Dokploy tipo compose se creó SIN `serviceName` → `runManually` 500 "invalid container name" y el trigger no ejecutaba → 0 filas en `cron_runs` → invisible al watchdog (un cron que NUNCA corrió = `desconocido`, no alerta — mismo blind spot que el huérfano del 07-09 pero por el otro lado). Fix: recrear con `serviceName=facturaia`+`shellType=bash`+`timezone` en el CREATE (el `update` no re-registra el trigger vivo). De paso corregida la TZ de cashflow-alerts/fiscal-avisos/stock-alarmas-email (corrían en UTC = 2h tarde) → Europe/Madrid. Ver [[dokploy-compose-schedule-requiere-servicename]]. **Corolario (misma tarde/noche):** ese `schedule.update` de TZ NO re-registró el trigger vivo → `fiscal-avisos`+`stock-alarmas-email` PERDIERON el disparo del 13 → 2 emails ALTA del health-sweep a las 22:00 CEST (cuadra: último éxito 12 08:00 UTC + max_intervalo 36h). Relanzados a mano vía `ops/cron/sign-call.sh` (HMAC) contra prod (stock: 1 email real/5 productos; fiscal: sin pérdida, hitos plazo_7d ya existían). Regla: tras CUALQUIER `schedule.update`, relanzar a mano (`runManually`/sign-call) o se pierde el disparo del día. **Cierre 2026-07-14:** verificado en `cron_runs` que `fiscal-avisos`+`stock-alarmas-email` dispararon SOLOS el 14 (06:00/06:05 UTC = 08:00/08:05 Madrid) ya con TZ Europe/Madrid → triggers sanos, el cambio de TZ solo costó el disparo de ese día. Incidente cerrado.
- 2026-07-13 · TuFacturaIA · la API de Dokploy (`schedule.one`/`compose.one`/`application.one`) vuelca el `compose.env` completo en plano → el 07-03 un agente lo volcó a chat (service role, Stripe, WhatsApp, Resend, claves de cifrado, TrueLayer/Tink, B2, N8N) → guardarraíl DURO: hook global PreToolUse (Bash+WebFetch) que bloquea las llamadas crudas + wrapper redactor `dokploy-safe.sh` (PR #870, Capa 1 cerrada+deploy verificado). Rotación de secrets (Capa 2) y sacar secrets del env (Capa 3) pendientes. Ver [[dokploy-api-get-schedule-list-expone-env-completo-secrets]].

- 2026-07-13 · TuFacturaIA · al aprobar una recibida a mano (web, individual o masiva) el **nº de factura no se copiaba** de la ingesta: el cliente leía `d.num` pero la clave canónica de `bandeja_ingesta.datos_extraidos` es `num_factura` → `undefined` silencioso (JSONB sin tipo TS). El flujo servidor (n8n/ocr-process) sí usaba `num_factura`. Hallado auditando el shape durante un QA de rendimiento, no reportado. Fix #875: helper único `buildApprovalUpdates`. De paso: aprobación masiva de N×2 round-trips en serie → batch+pool+progreso. Ver [[accion-masiva-cliente-n-round-trips-serie-cuelga-usar-batch-y-pool]].

- 2026-07-14 · TuFacturaIA · "Aprobar seleccionadas" en recibidas dejaba filas **sin aprobar en silencio** (y "seleccionar todo" no deseleccionaba) → la clave de selección era `docKey = num ?? id`, pero los números de recibidas se repiten (mismo `num` de proveedores distintos, scans dup) → `data.find(docKey===key)` resolvía solo la 1ª fila por número y `toggleAll` (`size===length`) nunca limpiaba. Hallado en QA del usuario (screenshots). Fix #888: `docKey = f.id`. Ver [[clave-seleccion-fila-debe-ser-id-unico-no-campo-de-negocio]].
- 2026-07-14 · TuFacturaIA · escaneos (PDF) subidos del revés → OCR devolvía "Sin nombre / 0 € / nº = nombre de archivo" (gpt-4o no lee un PDF a 180°). Fix #888: auto-orientación (reintento rotado en fallos, pdf-lib para PDF + sharp para imagen) + rotar manual + toggle admin. **Verificado en prod (Alpine)**: PDF fallido → `Tamme Air Conditioners · 254,21 €` persistido. Ver [[openai-vision-lee-pdf-del-reves-rotando-con-pdf-lib]] · [[dep-nativa-import-dinamico-defensivo-en-ruta-api]].

- 2026-07-15 · TuFacturaIA · Holded importó 409 contactos (clientes+proveedores) **todos a `clientes`**, 0 a `proveedores` → el pull decidía por `type` e ignoraba `client_record`/`supplier_record` (la señal real de rol en Holded v2; un contacto puede ser ambos). Fix #908: `contactRoles()` + ruteo por rol + `entity_type` `contact_client`/`contact_supplier` (mig 462) + script de reconciliación idempotente. Ver [[holded-v2-contacto-rol-por-record-no-por-type]].
- 2026-07-15 · TuFacturaIA · pull Holded daba 4 errores/ciclo `insert proveedores duplicate key` → `PROVEEDOR_COLS` pedía `updated_at` (col que `proveedores` NO tiene) → 42703 → `findByNif` tragaba el error (null) → INSERT en vez de link → choque unique `lower(nif)`. Fix #918 (quitar col + throw). El síntoma estaba a 3 saltos de la causa; ~1h perdida persiguiendo el deploy (deployments[]=done pero mismo-commit redeploy = no-op). Ver [[column-list-drift-clientes-proveedores-select-inexistente-42703]] · [[dokploy-redeploy-mismo-commit-es-no-op-cambiar-sha-fuerza-recreacion]].

- 2026-07-19 · TuFacturaIA · 7 páginas de módulos gated (cashflow, fiscal ×3, inventario, recurrentes, remesas) devolvían **HTTP 500 en su rama UPSELL** — las orgs SIN el módulo (las de conversión) veían un crash en vez del EmptyState "activar módulo". Causa: `buttonClassName` (función pura) vivía en el módulo `'use client'` `button.tsx`; llamarla desde el Server Component revienta en runtime (Next 16) y el `build` no lo caza. Latente semanas: las orgs de test tienen todos los módulos → happy path, nunca renderizan la rama upsell. Destapado por el smoke E2E del enlace fiscal (misma raíz, PR #1022). Fix #1025: extraer `buttonClassName` a `button-class.ts` sin `'use client'` + re-export (0 consumidores client tocados). Ver [[server-component-no-puede-llamar-funcion-use-client]].

- 2026-07-19 · TuFacturaIA · editar un material no guardaba (y fijar la duración del kill-switch de mantenimiento fallaba) → `<input number>` con `min`/`step` incompatibles = `stepMismatch` → `checkValidity()` false → `requestSubmit()` no enviaba, sin error visible. Fix `step="any"` (#1030 materiales, #1034 admin). Ver [[html-stepmismatch-min-step-bloquea-submit]].

- 2026-07-20 · TuFacturaIA · **Obras: catálogo entero valorado a 0 € de material** en prod. Motor de precio: `PFVC_min = COALESCE(MIN(PFVC entre proveedores), 0)` → material sin proveedor enlazado (todos tras importar Telematel) se vendía solo por mano de obra; accesorio de tarifa 213,97 € salía a 0,00 €. El "=0" era salvaguarda del código, no regla de negocio. Fix #1100 (mig 530): sin proveedor, coste = base×(1+margen); recalculado el catálogo. Ver [[coalesce-agregado-0-anti-null-puede-ser-regla-negocio-erronea]].
- 2026-07-20 · TuFacturaIA · main con **typecheck rojo enmascarado** (2º del día, misma clase): #1091 (drag-scroll) mergeó con un `@ts-expect-error` muerto en el test — el fix estaba en el working tree pero NO en el commit antes del `push`. Gates solo-PR + `--admin` no revalidaron. Fix #1094. Lección: tras editar post-commit, `git add` + verificar `git status` limpio ANTES de push. Ver [[actions-sin-billing-hooks-locales-unico-gate]].
- 2026-07-20 · TuFacturaIA · main quedó con **lint rojo enmascarado**: #1086 (overhaul materiales) mergeó con 2 `setState`-en-effect (`react-hooks/set-state-in-effect`) que los gates solo-PR + merge `--admin` no revalidaron. Como el pre-commit hook lintea TODO el repo, bloqueaba cualquier commit ajeno (destapado al tunear el botón «Ask»). Fix #1087: `eslint-disable-next-line` justificado (loaders async legítimos). Patrón: `--admin` + gates solo-PR → main puede ir rojo y frenar al siguiente. Ver [[actions-sin-billing-hooks-locales-unico-gate]].

- 2026-07-20 · TuFacturaIA · Unidad de Obra con una sub-unidad sin materiales mostraba **precio 0 falso** → el array `lineas` de `obras_uo_calcular` traía `importe: null` en ese grupo (sin COALESCE por-línea; el total sí lo llevaba) y el parse Zod `z.number()` estricto rechazaba TODO el objeto y zerificaba el precio → mig 532 (COALESCE por línea) + parse `nullable→0`. Llegó a prod en #1102; lo cazó `/fia-cierre` (no el QA en vivo, que solo probó líneas con material); fix #1105 verificado en vivo. Ver [[agregado-coalesce-total-vs-null-por-grupo-parse-estricto]].

- 2026-07-22 · TuFacturaIA · #1140 (Cache Components E0-E3) se mergeó a main con un **crash real sin arreglar**: bajo `<Activity>`, ocultar `/emitidas` con el visor PDF abierto y volver → `Cannot read properties of null (reading 'sendWithPromise')` inmediato (react-pdf pinta sobre un `PDFPageProxy` cuyo transporte ya fue destruido al ocultar la ruta; el estado React no se resetea, solo se limpia el efecto interno de pdf.js). Causa raíz de fondo: dos sesiones paralelas trabajando el mismo tema (Cache Components) sin coordinarse — una mergeó #1140 sin saber que la otra ya tenía el bug detectado. Detectado en QA de regresión DESPUÉS del merge, ya en prod unas horas. Fix mismo día (#1142, HOTFIX): `PdfRecoveryBoundary` (error boundary + remount por `key={gen}`, tope 1 reintento). Verificado con repro determinista 2×/2× antes/después, auditoría propia confirmó que ningún otro componente usa un recurso imperativo similar (Worker/canvas/WebSocket) vulnerable al mismo patrón. Ver [[nextjs-activity-no-resetea-estado-libreria-externa-con-recurso-imperativo]].

## Archivo
Incidentes de **mayo 2026 y anteriores** movidos a [[incidents-archive-2026]] (2026-07-13). Aquí solo lo reciente (jun–jul).
- 2026-07-21 · FacturaIA · `supabase db push` de Fase 2 Obras aborta "remote migration versions not found in local" → nº migración 519 DUPLICADO (orphan `519_obras_materiales_trazabilidad_ia` idéntico al 524, colado en #1094) → borrar el orphan; NO correr el `migration repair --reverted` que sugiere el CLI (corrompe historial). Ver [[db-push-remote-versions-not-found-es-checkout-stale-o-num-duplicado-no-repair]]
- 2026-07-21 · Centro Elphis · bot WA se quedaba mudo tras el usuario dar el nombre + nombre vacío en Clientify + "Timed out reading data" en Chatwoot → `chatwoot-event` retenía el lock de conversación ~15s (la extracción CRM con LLM corría dentro del lock, tras responder) y el 2º mensaje en ráfaga caía en `locked_busy` y se DESCARTABA → liberar lock+responder antes de la extracción (que pasa a correr fuera del lock) + reintento en `locked_busy` + 2º upsert de ids. Desplegado y verificado E2E. Ver [[lock-conversacion-liberar-tras-responder-no-tras-trabajo-post]]
- 2026-07-19 · Centro Elphis · mensaje WA de Alba tardó 87 min en llegar al bot → NO era el stack (n8n "Up 3 days", Traefik "Up 2 months", sin redeploy, webhook OK): retención/reintento de entrega del webhook por Meta (número recién migrado a Cloud API el 17-jul). Externo, no reproducible. Diagnóstico de uptime vía API Dokploy sin SSH. Ver [[dokploy-api-docker-getcontainers-estado-sin-ssh]]
